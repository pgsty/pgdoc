# PG10—PG20 顺序校准：2026-09-16 阶段记录

继续以 PG18 当前英文为入口，按书内顺序核对剩余正文；每个发现均核对 PG10—PG20 自身英文。未扫描 Git 历史。本文记录当前阶段，不表示全书完成。

## 已完成的剩余范围

- `postgres.sgml` 章框架；`ddl.sgml` 原剩余正文，包括默认值、约束、表修改、行级安全、模式、分区、外部数据与依赖。
- `func.sgml` 章引言与全部 XML 函数说明、示例和脚注；既有其他函数节校对保留。
- `config.sgml` 剩余引言与说明；`user-manag.sgml` 章引言。
- `monitoring.sgml` 剩余视图／函数引言、注释、示例及动态跟踪说明；既有表项校对保留。
- `wal.sgml` 剩余可靠性、WAL 引言、异步提交与内部机制说明。
- `logical-replication.sgml` 已完成范围之间的最后 51 字符，经检查仅为空白和闭合标签。
- `regress.sgml` 原剩余正文，包括测试运行、结果差异、变体输出、TAP 和覆盖率；既有自定义服务器设置校对保留。
- 前批事务内部机制与 `pg_resetwal` 草稿已核对当前源文并落地。

## 修复与十一版适用情况

| 内容 | 本阶段修复 |
| --- | --- |
| 事务与恢复工具 | epoch／subcommitted 术语；PREPARE 后限制针对该事务；仅部分提交的事务；旧版警告、force 条件与新版参数差异；纪元时间戳。相关英文逐版核对，共 28 个正文文件。 |
| DDL | 外键对应同一行、主键与唯一性、NULL 条件、引用完整性、搜索路径安全、缺失说明、分区索引适用范围、父表与子分区、继承限制、数据局部性等。十一版 DDL 均有修复，各版引入时间和结构保留。 |
| XML | 输入禁限条件、XMLTABLE 的三类输入、列名与类型、求值、默认值与 NOT NULL 的联合条件、上下文节点、脚注否定关系、导出和转换措辞。修改 13 个文件；PG19／20 的拆分文件保留，代码示例保持原样。 |
| 配置与监控 | PG12—20 后续恢复需重新设置参数；PG14 兼容锚点合法位置；十一版动态跟踪断词；PG12—20 GSSAPI 每后端一行的措辞。 |
| 回归测试 | 普通模式与索引词翻译、PG12+ CPU 核心、区域设置和确定顺序、差异行数、PG15+ PROVE_FLAGS 作用及交叉引用句式；删除 PG15 混入的 PG16+ 并行测试说明。十一版均修复，命令与输出保留自身英文。 |

本阶段共修改 **74 个不同的中文正文文件**，并新增词表与规则第 655／656 条。此数是修订文件数，不是全篇阅读文件数。其他任务的正文、规范日志和清理记录改动保留，不纳入本阶段提交。

## 验证

- 当前十一版整书源码通过原生解析：PG10 使用 OpenSP，PG11—20 使用 DocBook DTD 验证，全部退出码 0、诊断 0。
- 最近验证：`outputs/pg18-sequential-20260916/sequential-through-regress-validation.json`。
- 本阶段范围的 `git diff --check` 通过。
- **最终 HTML／A4 PDF／US PDF 验收仍为 0／33**，解析通过不等于渲染构建通过；未推送或发布。

## 续作入口

下一处是 PG18 `logicaldecoding.sgml`：章引言和示例，以及流复制协议接口起至章末；此前完整的逻辑解码概念部分保留。随后继续 SQL／应用参考页和剩余内部机制、附录。较大未完成项包括 `sources.sgml`、`nls.sgml`、`features.sgml`、`release-18.sgml`、`glossary.sgml`、`docguide.sgml` 等。

轻量记录在 `outputs/pg18-sequential-20260916/progress.jsonl`；各 `.patch` 为精确修改记录，`review.py` 提取当前正文，`validate_current.py` 在临时目录解析并自动清理。原覆盖清单只作定位工具，不作为机器阅读或语义验收证据。继续只需读取当前同版英中文，不需恢复历史解析缓存或扫描 Git 历史。
