<!--
$Header: /home/projects/pgsql/cvsroot/pgsql/doc/src/sgml/plperl.sgml,v 2.7 2000/12/22 18:57:50 petere Exp $
-->

<chapter id="plperl">
 <title>PL/Perl - Perl 过程语言</title>

 <para>
  PL/Perl 允许你用
  Perl 编程语言编写函数，这些函数可以像
  <productname>Postgres</productname> 内置的函数一样在
  SQL 查询中使用。
 </para>

 <para>
  PL/Perl 解释器是一个完整的 Perl
  解释器。但为了维护系统的安全，某些操作已被禁用。一般来说，受限制的是那些与环境交互的操作。这包括文件句柄操作、<literal>require</literal> 和
  <literal>use</literal>（用于外部模块）。应当注意这种安全并非绝对。事实上，几种拒绝服务攻击仍然可能——
  内存耗尽和无限循环是两个例子。
 </para>

 <sect1 id="plperl-install">
  <title>构建和安装</title>

  <para>
   如果你从源代码安装
   <productname>Postgres</productname>，要构建并安装 PL/Perl，必须给
   <filename>configure</filename> 脚本加上
   <option>--with-perl</option> 选项。PL/Perl 要求安装
   <productname>Perl</productname> 时
   <filename>libperl</filename> 库被构建为共享对象。在撰写本文时，操作系统发行的 Perl 包几乎都不是这样。构建时会出现类似下面的消息来指出这一事实：
<screen>
<computeroutput>
*****
* Cannot build PL/Perl because libperl is not a shared library.
* Skipped.
*****
</computeroutput>
</screen>
   因此你很可能必须手工重新构建并安装
   <productname>Perl</productname> 才能构建
   PL/Perl。
  </para>

  <para>
   重新安装 Perl 后想重试构建 PL/Perl
   时，切换到
   <productname>Postgres</productname> 源代码树中的
   <filename>src/pl/plperl</filename> 目录并执行命令
<programlisting>
gmake clean
gmake all
gmake install
</programlisting>
  </para>

  <para>
   <command>createlang</command> 命令用来把该语言安装到一个数据库中。
<screen>
<prompt>$</prompt> <userinput>createlang plperl template1</userinput>
</screen>
   如果把它安装到 template1 中，以后创建的所有数据库都会自动安装该语言。
  </para>
 </sect1>

 <sect1 id="plperl-use">
  <title>使用 PL/Perl</title>

  <para>
   假设你有下面这张表：
<programlisting>
CREATE TABLE EMPLOYEE (
    name text,
    basesalary integer,
    bonus integer
);
</programlisting>

   为了得到总薪酬（基本工资 + 奖金），我们可以像下面这样定义一个函数：
<programlisting>
CREATE FUNCTION totalcomp(integer, integer) RETURNS integer
    AS 'return $_[0] + $_[1]'
    LANGUAGE 'plperl';
</programlisting>

   注意，函数的参数如预期那样通过
   <varname>@_</varname> 传递。
  </para>

  <para>
   现在我们可以像这样使用我们的函数：
<programlisting>
SELECT name, totalcomp(basesalary, bonus) FROM employee;
</programlisting>
  </para>

  <para>
   不过，我们也可以把整个元组传给我们的函数：
<programlisting>
CREATE FUNCTION empcomp(employee) RETURNS integer AS '
    my $emp = shift;
    return $emp->{''basesalary''} + $emp->{''bonus''};
' LANGUAGE 'plperl';
</programlisting>
   元组以哈希的引用形式传递。键是元组中字段的名字。哈希的值是元组中对应字段的值。
  </para>

  <tip>
   <para>
    由于函数体是作为 SQL 字符串字面量传给
    <command>CREATE FUNCTION</command> 的，你必须转义
    Perl 源码中的单引号，可以像上面那样将它们加倍，也可以使用扩展引号函数（<literal>q[]</literal>、<literal>qq[]</literal>、<literal>qw[]</literal>）。反斜线必须通过加倍来转义。
   </para>
  </tip>

  <para>
   新函数 <function>empcomp</function> 可以这样使用：
<programlisting>
SELECT name, empcomp(employee) FROM employee;
</programlisting>
  </para>

  <para>
   下面是一个由于安全原因不允许文件系统操作而不能工作的函数例子：
<programlisting>
CREATE FUNCTION badfunc() RETURNS integer AS '
    open(TEMP, ">/tmp/badfile");
    print TEMP "Gotcha!\n";
    return 1;
' LANGUAGE 'plperl';
</programlisting>
   这个函数的创建会成功，但执行它不会成功。
  </para>

 </sect1>
</chapter>

<!-- Keep this comment at the end of the file
Local variables:
mode:sgml
sgml-omittag:nil
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
sgml-parent-document:nil
sgml-default-dtd-file:"./reference.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:("/usr/lib/sgml/catalog")
sgml-local-ecat-files:nil
End:
-->
