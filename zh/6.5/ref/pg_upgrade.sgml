<refentry id="APP-PG-UPGRADE">
 <refmeta>
  <refentrytitle>
   <application>pg_upgrade</application>
  </refentrytitle>
  <refmiscinfo>应用程序</refmiscinfo>
 </refmeta>
 <refnamediv>
  <refname>
   <application>pg_upgrade</application>
  </refname>
  <refpurpose>
   允许从前一个版本升级而无需重新加载数据
  </refpurpose>
 <refsynopsisdiv>
  <refsynopsisdivinfo>
   <date>1998-10-04</date>
  </refsynopsisdivinfo>
  <synopsis>
pg_upgrade
pg_upgrade [ -f <replaceable class="parameter">filename</replaceable> ] <replaceable class="parameter">old_data_dir</replaceable>
  </synopsis>
 </refsynopsisdiv>

 <refsect1 id="R1-APP-PG-UPGRADE-1">
  <refsect1info>
   <date>1998-10-04</date>
  </refsect1info>
  <title>
   描述
  </title>
  <para>
   <application>pg_upgrade</application>
   是一个用于从前一个
   PostgreSQL 版本升级而无需重新加载所有数据的工具。

  <procedure>
   <title>升级 <productname>Postgres</productname></title>

   <step performance="required">
    <para>
     备份你的数据目录。
    </para>
   </step>

   <step performance="required">
    <para>
     使用：
     <programlisting>
% pg_dumpall -s >db.out
     </programlisting>
     转储出旧数据库的定义而不含任何
     数据。停止 postmaster 和所有后端。
    </para>
   </step>

   <step performance="required">
    <para>
     用 mv 把你旧的 pgsql <filename>data/</filename> 目录重命名为
     <filename>data.old/</filename>。
    </para>
   </step>

   <step performance="required">
    <para>
     执行 <command>make install</command> 以安装新的二进制文件。
    </para>
   </step>

   <step performance="required">
    <para>
     运行 <application>initdb</application> 创建一个包含新版本系统表的
     新 template1 数据库。
    </para>
   </step>

   <step performance="required">
    <para>
     启动新的 postmaster。
    </para>
   </step>

   <step performance="required">
    <para>
     把工作目录切换到 
     pgsql 主目录，然后输入：
     <programlisting>
% pg_upgrade -f db.out data.old
     </programlisting>
     系统会做一些检查以确保一切都
     配置正确，然后运行你的 db.out 脚本创建
     你原有的所有数据库和表，但不包含
     数据。接着它会把数据文件从 <filename>data.old/</filename>
     移动到相应的 <filename>data/</filename> 目录。
    </para>
   </step>

   <step performance="required">
    <para>
     <emphasis>仔细</emphasis>检查升级后数据库的内容。
    </para>
   </step>

   <step performance="required">
    <para>
     完成之后你可以删除 <filename>data.old/</filename> 目录。
    </para>
 </refsect1>
</refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:nil
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
sgml-parent-document:nil
sgml-default-dtd-file:"../reference.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:"/usr/lib/sgml/catalog"
sgml-local-ecat-files:nil
End:
-->
