<!-- doc/src/sgml/external-projects.sgml -->

 <appendix id="external-projects">
  <title>外部项目</title>

  <para>
   <productname>PostgreSQL</productname> 是一个复杂的软件项目，对其进行管理并非易事。我们发现，许多对 <productname>PostgreSQL</productname> 的增强若脱离核心项目单独开发，往往能更高效地完成。
  </para>

 <sect1 id="external-interfaces">
  <title>客户端接口</title>

  <indexterm>
   <primary>接口</primary>
   <secondary>外部维护</secondary>
  </indexterm>

  <para>
   基础 <productname>PostgreSQL</productname> 发行版中只包含两个客户端接口：
   <itemizedlist>
    <listitem>
     <para>
      之所以包含 <link linkend="libpq">libpq</link>，是因为它是主要的 C 语言接口，而且许多其他客户端接口都构建在它之上。
     </para>
    </listitem>

    <listitem>
     <para>
      之所以包含 <link linkend="ecpg">ECPG</link>，是因为它依赖服务器端 SQL 语法，因此对 <productname>PostgreSQL</productname> 自身的变更非常敏感。
     </para>
    </listitem>
   </itemizedlist>

   其他所有语言接口都属于外部项目，并单独发布。PostgreSQL Wiki 上维护了一份 <ulink
   url="https://wiki.postgresql.org/wiki/List_of_drivers">语言接口列表</ulink>。请注意，其中有些软件包并非按与 <productname>PostgreSQL</productname> 相同的许可发布。有关各语言接口的更多信息，包括许可条款，请参阅其网站和文档。
  </para>

  <para>
   <ulink url="https://wiki.postgresql.org/wiki/List_of_drivers"></ulink>
  </para>
 </sect1>

 <sect1 id="external-admin-tools">
 <title>管理工具</title>

  <indexterm>
   <primary>管理工具</primary>
   <secondary>外部维护</secondary>
  </indexterm>

  <para>
   有若干管理工具可用于 <productname>PostgreSQL</productname>。其中最流行的是 <application><ulink url="https://www.pgadmin.org/">pgAdmin</ulink></application>，另外也有若干商业可用的工具。
  </para>
 </sect1>

 <sect1 id="external-pl">
  <title>过程语言</title>

  <indexterm>
   <primary>过程语言</primary>
   <secondary>外部维护</secondary>
  </indexterm>

  <para>
   基础 <productname>PostgreSQL</productname> 发行版中包含若干过程语言：<link
   linkend="plpgsql">PL/pgSQL</link>、<link linkend="pltcl">PL/Tcl</link>、
   <link linkend="plperl">PL/Perl</link> 以及 <link
   linkend="plpython">PL/Python</link>。
  </para>

  <para>
   此外，还有一些过程语言是在核心 <productname>PostgreSQL</productname>
   发行版之外开发和维护的。PostgreSQL Wiki 上维护了一份 <ulink
   url="https://wiki.postgresql.org/wiki/PL_Matrix">过程语言列表</ulink>。请注意，其中有些项目并非按与 <productname>PostgreSQL</productname> 相同的许可发布。有关各过程语言的更多信息，包括许可信息，请参阅其网站和文档。
  </para>

  <para>
   <ulink url="https://wiki.postgresql.org/wiki/PL_Matrix"></ulink>
  </para>
 </sect1>

 <sect1 id="external-extensions">
 <title>扩展</title>

  <indexterm>
   <primary>扩展</primary>
   <secondary>外部维护</secondary>
  </indexterm>

  <para>
   <productname>PostgreSQL</productname> 的设计目标之一，就是使其易于扩展。
   因此，载入数据库的扩展可以像内置特性一样工作。随源代码一同提供的
   <filename>contrib/</filename> 目录中包含若干扩展，它们在
   <xref linkend="contrib"/> 中有所描述。其他扩展则是独立开发的，例如
   <application><ulink url="https://postgis.net/">PostGIS</ulink></application>。
   甚至 <productname>PostgreSQL</productname> 的复制解决方案也可以在外部开发。
   例如，<application><ulink
   url="https://www.slony.info">Slony-I</ulink></application> 就是一个流行的主库/后备复制解决方案，它是独立于核心项目开发的。
  </para>
 </sect1>
</appendix>
