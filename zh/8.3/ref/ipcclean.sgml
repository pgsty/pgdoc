<!--
$PostgreSQL: pgsql/doc/src/sgml/ref/ipcclean.sgml,v 1.14 2007/01/31 23:26:04 momjian Exp $
PostgreSQL documentation
-->

<refentry id="APP-IPCCLEAN">
 <refmeta>
  <refentrytitle id="APP-IPCCLEAN-TITLE"><application>ipcclean</application></refentrytitle>
  <manvolnum>1</manvolnum>
  <refmiscinfo>Application</refmiscinfo>
 </refmeta>

 <refnamediv>
  <refname>ipcclean</refname>
  <refpurpose>remove shared memory and semaphores from a failed <productname>PostgreSQL</productname> server</refpurpose>
 </refnamediv>

 <indexterm zone="app-ipcclean">
  <primary>ipcclean</primary>
 </indexterm>

 <refsynopsisdiv>
  <cmdsynopsis>
   <command>ipcclean</command>
  </cmdsynopsis>
 </refsynopsisdiv>

 <refsect1 id="R1-APP-IPCCLEAN-1">
  <title>描述</title>
  <para>
   <command>ipcclean</command>可删除当前用户拥有的所有共享内存段和信号量集。它的用途是在<productname>PostgreSQL</productname>服务器（<xref
   linkend="app-postgres">）崩溃之后进行清理。请注意，立即重启服务器同样会清理共享内存和信号量，因此这条命令实际用处不大。
  </para>

  <para>
   只有数据库管理员才可以运行此程序，因为如果在多用户运行期间执行它，可能导致古怪的行为（也就是崩溃）。如果在一个服务器正在运行时执行这条命令，该服务器分配的共享内存和信号量将被删除，这会给该服务器带来相当严重的后果。
  </para>
 </refsect1>

 <refsect1>
  <title>注意</title>

  <para>
   这个脚本只是一个应急之作，不过在它写成之后的许多年里，还没有人拿出同样有效且可移植的替代方案。既然<command>postgres</command>现在可以自行清理，<command>ipcclean</command>今后也不太可能再被改进。
  </para>

  <para>
   这个脚本对<command>ipcs</command>工具的输出格式做了一些假设，而这些假设在不同的操作系统上未必成立。因此，它可能无法在你使用的操作系统上工作。在尝试运行它之前，先看一看脚本内容是明智的做法。
  </para>
 </refsect1>

</refentry>
