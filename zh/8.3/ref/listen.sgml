<!--
$PostgreSQL: pgsql/doc/src/sgml/ref/listen.sgml,v 1.23 2008/11/14 10:22:47 petere Exp $
PostgreSQL documentation
-->

<refentry id="SQL-LISTEN">
 <refmeta>
  <refentrytitle id="SQL-LISTEN-TITLE">LISTEN</refentrytitle>
  <manvolnum>7</manvolnum>
  <refmiscinfo>SQL - Language Statements</refmiscinfo>
 </refmeta>

 <refnamediv>
  <refname>LISTEN</refname>
  <refpurpose>listen for a notification</refpurpose>
 </refnamediv>

 <indexterm zone="sql-listen">
  <primary>LISTEN</primary>
 </indexterm>

 <refsynopsisdiv>
<synopsis>
LISTEN <replaceable class="PARAMETER">name</replaceable>
</synopsis>
 </refsynopsisdiv>

 <refsect1>
  <title>描述</title>

  <para>
   <command>LISTEN</command>将当前会话注册为名为<replaceable
   class="PARAMETER">channel</replaceable>的通知条件的监听者。
   如果当前会话已经在该通知条件上注册为监听者，则不会执行任何操作。
  </para>

  <para>
   每当命令<command>NOTIFY <replaceable
   class="PARAMETER">name</replaceable></command>被执行时，无论是由当前会话
   还是由另一个连接到同一数据库的会话执行，所有当前正在监听该通知条件的
   会话都会收到通知，而每个会话随后都会通知其所连接的客户端应用。有关更多信息，请参见对<command>NOTIFY</command>的讨论。
  </para>

  <para>
   会话可以使用<command>UNLISTEN</command>命令取消在给定通知条件上的监听注册。
   会话结束时，其监听注册会被自动清除。
  </para>

  <para>
   客户端应用必须采用何种方法来检测通知事件，取决于它所使用的
   <productname>PostgreSQL</productname>应用程序编程接口。使用
   <application>libpq</application>库时，应用程序将<command>LISTEN</command>
   作为普通 SQL 命令发出，然后必须定期调用
   <function>PQnotifies</function>函数，以判断是否已经收到任何通知事件。
   其他接口，例如<application>libpgtcl</application>，则提供了处理通知事件的
   更高层方法；事实上，在<application>libpgtcl</application>中，应用程序员
   甚至不应直接发出<command>LISTEN</command>或<command>UNLISTEN</command>。
   详见所使用接口的相关文档。
  </para>

  <para>
   <xref linkend="sql-notify" endterm="sql-notify-title">中对<command>LISTEN</command>和<command>NOTIFY</command>的使用有更详细的讨论。
  </para>
 </refsect1>

 <refsect1>
  <title>参数</title>

  <variablelist>
   <varlistentry>
    <term><replaceable class="PARAMETER">name</replaceable></term>
    <listitem>
     <para>
      通知条件的名称（任意标识符）。
     </para>
    </listitem>
   </varlistentry>
  </variablelist>
 </refsect1>

 <refsect1>
  <title>示例</title>

  <para>
   在<application>psql</application>中配置并执行一组 listen/notify 操作：

<programlisting>
LISTEN virtual;
NOTIFY virtual;
Asynchronous notification "virtual" received from server process with PID 8448.
</programlisting></para>
 </refsect1>

 <refsect1>
  <title>兼容性</title>

  <para>
   SQL 标准中没有<command>LISTEN</command>语句。
  </para>
 </refsect1>

 <refsect1>
  <title>另见</title>

  <simplelist type="inline">
   <member><xref linkend="sql-notify" endterm="sql-notify-title"></member>
   <member><xref linkend="sql-unlisten" endterm="sql-unlisten-title"></member>
  </simplelist>
 </refsect1>
</refentry>
