<!-- doc/src/sgml/sourcerepo.sgml -->

<appendix id="sourcerepo">
 <title>源代码仓库</title>

 <para>
  <productname>PostgreSQL</productname>的源代码使用<productname>Git</productname>版本控制系统进行存储和管理。官方提供了主仓库的公开镜像；主仓库发生任何变更后，该镜像都会在一分钟内更新。
 </para>

 <para>
  我们的 wiki <ulink url="http://wiki.postgresql.org/wiki/Working_with_Git"></ulink>上有一些关于使用 Git 的讨论。
 </para>

  <para>
   注意，从源代码仓库构建<productname>PostgreSQL</productname>需要相当新的<application>bison</application>、<application>flex</application>和<application>Perl</application>版本。从发行版压缩包构建时不需要这些工具，因为它们所构建的文件已包含在压缩包中。其他工具要求与<xref linkend="installation">中所示相同。
  </para>

 <sect1 id="git">
  <title>通过 <productname>Git</productname> 获取源代码</title>

  <para>
   使用<productname>Git</productname>时，你将在本地机器上创建整个代码仓库的一个副本，因此即使离线也能访问全部历史和分支。这是开发或测试补丁最快且最灵活的方式。
  </para>

  <procedure>
   <title>Git</title>

   <step>
    <para>
     你需要安装<productname>Git</productname>，可以从<ulink url="http://git-scm.com"></ulink>获取。许多系统默认已安装了较新版本的<application>Git</application>，或者可以在其软件包分发系统中找到。
    </para>
   </step>

   <step>
    <para>
     要开始使用 Git 仓库，请克隆官方镜像：

<programlisting>
git clone git://git.postgresql.org/git/postgresql.git
</programlisting>

     这会把整个仓库复制到你的本地机器，因此可能需要一段时间才能完成，尤其是在网络连接较慢的情况下。文件将被放置在当前目录的一个新子目录<filename>postgresql</filename>中。
    </para>

    <para>
     如果防火墙阻止了对 Git 协议的访问，也可以通过 HTTP 协议访问 Git 镜像。只需把 URL 前缀改为<literal>http</literal>，例如：

<programlisting>
git clone http://git.postgresql.org/git/postgresql.git
</programlisting>

     HTTP 协议的效率低于 Git 协议，因此使用起来会更慢。
    </para>
   </step>

   <step>
    <para>
     每当想要获取系统的最新更新时，<command>cd</command>进入该仓库目录并运行：

<programlisting>
git fetch
</programlisting>
    </para>
   </step>
  </procedure>

  <para>
   <productname>Git</productname>除了获取源代码外还能做更多事情。欲了解更多信息，请查阅<productname>Git</productname>的手册页，或访问<ulink url="http://git-scm.com"></ulink>网站。
  </para>
 </sect1>

</appendix>
