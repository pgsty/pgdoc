<!-- $PostgreSQL: pgsql/doc/src/sgml/features.sgml,v 2.30 2008/12/19 16:25:16 petere Exp $ -->

<appendix id="features">
 <title>SQL 符合性</title>

 <para>
  本节尝试概述<productname>PostgreSQL</productname>在多大程度上符合当前的 SQL
  标准。下文并不是一份完整的符合性声明，但会在合理且对用户有用的范围内，
  尽可能详细地介绍主要主题。
 </para>

 <para>
  SQL 标准的正式名称是 ISO/IEC 9075<quote>数据库语言 SQL</quote>。该标准不时发布修订版本；最近一次完整更新发布于 2003 年，另有一次部分更新发布于 2006 年。2003 版本称为 ISO/IEC 9075:2003，或简称 SQL:2003。在此之前的版本是 SQL:1999 和 SQL-92。每个版本都取代前一个版本，因此声称符合早期版本并没有官方意义。<productname>PostgreSQL</productname>的开发目标是在不与传统特性或常识相矛盾的前提下，符合该标准的最新官方版本。在 SQL 标准各版本制定期间，PostgreSQL 项目并未参与 ISO/IEC 9075 工作组，但即便如此，SQL 标准要求的许多特性都得到了支持，尽管有时语法或功能略有差异。今后可以预期会进一步向标准靠拢。
 </para>

 <para>
  <acronym>SQL-92</acronym>为符合性定义了三个特性集：入门（Entry）、中级（Intermediate）
  和完整（Full）。多数宣称符合<acronym>SQL</acronym>标准的数据库管理系统，
  实际上只符合入门（Entry）级别，因为中级（Intermediate）和完整（Full）级别中的全部特性
  要么过于庞杂，要么与既有行为相冲突。
 </para>

 <para>
  从<acronym>SQL:1999</acronym>开始，SQL 标准不再沿用<acronym>SQL-92</acronym>中
  那种效果不佳且过于宽泛的三级分类，而是定义了大量独立特性。其中很大一个子集构成了
  核心（<quote>Core</quote>）特性，也就是每个符合 SQL 标准的实现都必须提供的特性。
  其余特性则完全是可选的。
 
  某些可选特性被组合在一起，形成<quote>包</quote>，SQL 实现可以声明符合这些包，从而表明符合特定的特性组。</para>

 <para>
  <acronym>SQL:2003</acronym>标准也分为若干部分，每部分都有一个简称。注意这些部分的编号并不连续。

  <itemizedlist>
   <listitem><para>ISO/IEC 9075-1 框架（SQL/Framework）</para></listitem>
   <listitem><para>ISO/IEC 9075-2 基础（SQL/Foundation）</para></listitem>
   <listitem><para>ISO/IEC 9075-3 调用级接口（SQL/CLI）</para></listitem>
   <listitem><para>ISO/IEC 9075-4 持久存储模块（SQL/PSM）</para></listitem>
   <listitem><para>ISO/IEC 9075-9 外部数据管理（SQL/MED）</para></listitem>
   <listitem><para>ISO/IEC 9075-10 对象语言绑定（SQL/OLB）</para></listitem>
   <listitem><para>ISO/IEC 9075-11 信息和定义模式（SQL/Schemata）</para></listitem>
   <listitem><para>ISO/IEC 9075-13 使用 Java 语言的例程和类型（SQL/JRT）</para></listitem>
   <listitem><para>ISO/IEC 9075-14 XML 相关规范（SQL/XML）</para></listitem>
  </itemizedlist>
 </para>

 <para>
  <productname>PostgreSQL</productname>涵盖第 1、2、11 和 14 部分。
  第 3 部分类似于 ODBC 接口，第 4 部分类似于
  <application>PL/pgSQL</application>编程语言，但在两种情况下都未专门
  意图或核验确切符合性。
 </para>

 <para>
  PostgreSQL 支持 SQL:2003 的大部分主要特性。在完全符合 Core 所要求的 164 个强制特性中，PostgreSQL 至少符合 150 个。此外，还有一长串受支持的可选特性。值得注意的是，截至撰写时，尚无任何数据库管理系统的当前版本声称完全符合 Core SQL:2003。
 </para>

 <para>
  在下面两节中，我们先列出<productname>PostgreSQL</productname>支持的特性，然后列出<acronym>SQL:2003</acronym>中定义但<productname>PostgreSQL</productname>尚不支持的特性。这两个列表都是近似的：被列为受支持的特性可能存在个别不符合标准的细节，而不受支持的特性也可能实际上已实现了很大一部分。关于什么可用什么不可用的最准确信息，始终以文档正文为准。
 </para>

 <note>
  <para>
   带连字符的特性代码表示子特性。因此，如果某个特定子特性不受支持，
   则即使该主特性的其他子特性受支持，该主特性也会被列为不受支持。
  </para>
 </note>

  <sect1 id="features-sql-standard">
   <title>支持的特性</title>

   <para>
    <informaltable>
     <tgroup cols="4">
      <thead>
       <row>
        <entry>标识符</entry>
        <entry>包</entry>
        <entry>描述</entry>
        <entry>注释</entry>
       </row>
      </thead>

      &features-supported;

     </tgroup>
    </informaltable>
   </para>
  </sect1>

  <sect1 id="unsupported-features-sql-standard">
   <title>不支持的特性</title>

   <para>
    <productname>PostgreSQL</productname>的本版本未实现下列<acronym>SQL:2003</acronym>中定义的特性。在少数情况下，有等价的功能可用。

    <informaltable>
     <tgroup cols="4">
      <thead>
       <row>
        <entry>Identifier</entry>
        <entry>Package</entry>
        <entry>Description</entry>
        <entry>Comment</entry>
       </row>
      </thead>

      &features-unsupported;

     </tgroup>
    </informaltable>
   </para>
  </sect1>

 </appendix>
