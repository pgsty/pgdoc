<REFENTRY ID="APP-PG-UPGRADE">
<REFMETA>
<REFENTRYTITLE>
<application>pg_upgrade</application>
</REFENTRYTITLE>
<REFMISCINFO>应用程序</REFMISCINFO>
</REFMETA>
<REFNAMEDIV>
<REFNAME>
<application>pg_upgrade</application>
</REFNAME>
<REFPURPOSE>
允许从前一个版本升级而无需重新加载数据
</REFPURPOSE>
<REFSYNOPSISDIV>
<REFSYNOPSISDIVINFO>
<DATE>1998-10-04</DATE>
</REFSYNOPSISDIVINFO>
<SYNOPSIS>
pg_upgrade
pg_upgrade [-f <replaceable class="parameter">input_file</replaceable>] <replaceable class="parameter">old_data_dir</replaceable>
</SYNOPSIS>

<REFSECT1 ID="R1-APP-PG-UPGRADE-1">
<REFSECT1INFO>
<DATE>1998-10-04</DATE>
</REFSECT1INFO>
<TITLE>
描述
</TITLE>
<PARA>
<application>pg_upgrade</application>
 是一个用于从前一个
 PostgreSQL 版本升级而无需重新加载所有数据的工具。首先，为了安全起见，备份你的数据目录。然后，使用：
<programlisting>
% pg_dumpall -s -z >db.out
</programlisting>
转储出旧数据库的定义而不含任何数据。停止 postmaster 和所有后端。
</para>

<para>
然后用 mv 把你旧的 pgsql /data 目录重命名为
/data.old，并执行 make install 安装新的二进制文件。运行 initdb 创建一个包含新版本系统表的新 template1 数据库。启动新的 postmaster，cd 到
pgsql 主目录，然后输入：
<programlisting>
% pg_upgrade -f db.out data.old
</programlisting>
系统会做一些检查以确保一切都配置正确，并运行你的 db.out 脚本创建你原有的所有数据库和表，但不包含数据。接着它会把数据文件从 /data.old
移动到相应的 /data 目录。然后你就可以检查数据了。完成之后你可以删除 /data.old
目录。
</REFENTRY>
