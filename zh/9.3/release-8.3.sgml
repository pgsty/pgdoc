<!-- doc/src/sgml/release-8.3.sgml -->
<!-- See header comment in release.sgml about typical markup -->

 <sect1 id="release-8-3-23">
  <title>版本 8.3.23</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2013-02-07</para>
  </formalpara>

  <para>
   本次发布包含对 8.3.22 的多方面修复。有关 8.3 大版本中新特性的信息，请参见<xref linkend="release-8-3">。.
  </para>

  <para>
      这预计是 <productname>PostgreSQL</productname> 8.3.X 系列的最后一个发行版。建议用户尽快更新到较新的发行分支。
     </para>

  <sect2>
   <title>迁移到版本 8.3.23</title>

   <para>
    对于运行 8.3.X 的用户，不需要进行转储/恢复。</para>

   <para>
    不过，如果你是从earlier than 8.3.17 的版本升级，请参见<xref linkend="release-8-3-17">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      防止从 SQL 执行 <function>enum_recv</function>（Tom Lane）
     </para>

     <para>
      该函数声明有误，允许一条简单的 SQL 命令使服务器崩溃。原则上攻击者可能利用它检查服务器内存的内容。感谢 Sumit Soni（通过 Secunia SVCRP）报告此问题。（CVE-2013-0255）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 SQL 文法，允许对子 SELECT 结果做下标或字段选择（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      防止扫描 <structname>pg_tablespace</structname> 时的竞态条件（Stephen Frost、Tom Lane）
     </para>

     <para>
      如果 <structname>pg_tablespace</structname> 条目存在并发更新，<command>CREATE DATABASE</command> 和 <command>DROP DATABASE</command> 可能行为异常。
     </para>
    </listitem>

    <listitem>
     <para>
      防止 <command>DROP OWNED</command> 试图删除整个数据库或表空间（&Aacute;lvaro Herrera）
     </para>

     <para>
      为安全起见，这些对象的所有权必须重新分配，而不是删除。
     </para>
    </listitem>

    <listitem>
     <para>
      防止 <symbol>RowExpr</symbol> 或 <symbol>XmlExpr</symbol> 被二次解析分析时的不当行为（Andres Freund、Tom Lane）
     </para>

     <para>
      这一失误在 <literal>CREATE TABLE LIKE INCLUDING INDEXES</literal> 等上下文中可能被用户看到。
     </para>
    </listitem>

    <listitem>
     <para>
      增强对哈希表大小计算中整数溢出的防御（Jeff Davis）
     </para>
    </listitem>

    <listitem>
     <para>
      确保在 Windows 上非 ASCII 提示符字符串被转换到正确的代码页（Alexander Law、Noah Misch）
     </para>

     <para>
      这一缺陷影响 <application>psql</application> 和其他一些客户端程序。
     </para>
    </listitem>

    <listitem>
     <para>
      修复未连接数据库时 <application>psql</application> 的 <command>\?</command> 命令可能崩溃的问题（Meng Qingzhong）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>libpq</application> 的 <function>PQprintTuples</function> 中的一字节缓冲区越界（Xi Wang）
     </para>

     <para>
      这个古老的函数没有被 <productname>PostgreSQL</productname> 本身在任何地方使用，但仍可能被某些客户端代码使用。
     </para>
    </listitem>

    <listitem>
     <para>
      重新排列 configure 对系统提供函数的测试，使其不被 libedit/libreadline 的虚假导出所欺骗（Christoph Berg）
     </para>
    </listitem>

    <listitem>
     <para>
      确保 Windows 构建号随时间递增（Magnus Hagander）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <application>pgxs</application> 在为 Windows 交叉编译时以正确的 <literal>.exe</literal> 后缀构建可执行文件（Zoltan Boszormenyi）
     </para>
    </listitem>

    <listitem>
     <para>
      新增时区缩写 <literal>FET</literal>（Tom Lane）
     </para>

     <para>
      它现在用于一些东欧时区。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-3-22">
  <title>版本 8.3.22</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2012-12-06</para>
  </formalpara>

  <para>
   本次发布包含对 8.3.21 的多方面修复。有关 8.3 大版本中新特性的信息，请参见<xref linkend="release-8-3">。.
  </para>

  <para>
      <productname>PostgreSQL</productname> 社区将于 2013 年 2 月停止对 8.3.X 版本系列的更新。建议用户尽快更新到较新的版本分支。
     </para>

  <sect2>
   <title>迁移到版本 8.3.22</title>

   <para>
    对于运行 8.3.X 的用户，不需要进行转储/恢复。</para>

   <para>
    不过，如果你是从earlier than 8.3.17 的版本升级，请参见<xref linkend="release-8-3-17">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      修复与 <command>CREATE INDEX CONCURRENTLY</command> 相关的多个缺陷（Andres Freund、Tom Lane）
     </para>

     <para>
      修复 <command>CREATE INDEX CONCURRENTLY</command>，使其在更改索引 <structname>pg_index</structname> 行的状态时使用就地更新。这防止了可能导致并发会话错过更新目标索引（从而产生损坏的并发创建索引）的竞态条件。
     </para>

     <para>
      另外，修复各种其他操作，确保它们忽略由失败的 <command>CREATE INDEX CONCURRENTLY</command> 命令产生的无效索引。其中最重要的是 <command>VACUUM</command>，因为在采取纠正措施修复或删除无效索引之前，很容易就有自动清理在该表上启动。
     </para>
    </listitem>

    <listitem>
     <para>
      避免内存不足时内部哈希表损坏（Hitoshi Harada）
     </para>
    </listitem>

    <listitem>
     <para>
      修复外连接之上非严格等值子句的规划（Tom Lane）
     </para>

     <para>
      规划器可能从把非严格构造与别的值等同的子句中推导出错误的约束，例如当 <literal>foo</literal> 来自外连接的可空侧时的 <literal>WHERE COALESCE(foo, 0) = 0</literal>。
     </para>
    </listitem>

    <listitem>
     <para>
      改进规划器从等价类证明排他约束的能力（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复哈希子计划中的部分行匹配，使其正确处理跨类型情况（Tom Lane）
     </para>

     <para>
      这影响多列 <literal>NOT IN</literal> 子计划，例如 <literal>WHERE (a, b) NOT IN (SELECT x, y FROM ...)</literal>，当 <literal>b</literal> 和 <literal>y</literal> 分别是 <type>int4</type> 和 <type>int8</type> 时。根据所涉及的具体数据类型，这一失误会导致错误答案或崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      在为 <literal>AFTER ROW UPDATE/DELETE</literal> 触发器重新获取旧元组时获取缓冲区锁（Andres Freund）
     </para>

     <para>
      在非常罕见的情况下，这一疏忽可能导致向外键强制触发器的预检查逻辑传递不正确的数据。这可能导致崩溃，或者在是否触发触发器上做出错误决定。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <command>REASSIGN OWNED</command> 对表空间授权的处理（&Aacute;lvaro Herrera）
     </para>
    </listitem>

    <listitem>
     <para>
      忽略视图中系统列的不正确 <structname>pg_attribute</structname> 条目（Tom Lane）
     </para>

     <para>
      视图没有任何系统列。然而，我们在把表转换为视图时忘记移除这类条目。这一问题在 9.3 及更高版本中已正确修复，但在以前的分支中，我们需要防御已经错误转换的视图。
     </para>
    </listitem>

    <listitem>
     <para>
      修复规则打印，使其正确转储 <literal>INSERT INTO <replaceable>table</replaceable> DEFAULT VALUES</literal>（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      防止当查询中有过多 <literal>UNION</literal>/<literal>INTERSECT</literal>/<literal>EXCEPT</literal> 子句时栈溢出（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      防止用最小可能整数值除以 -1 时依赖平台的失败（Xi Wang、Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复日期解析中可能越过字符串末尾的访问（Hitoshi Harada）
     </para>
    </listitem>

    <listitem>
     <para>
      当 Unix 域套接字的路径名长度超过平台特定限制时，生成可理解的错误消息（Tom Lane、Andrew Dunstan）
     </para>

     <para>
      此前这会导致相当无用的结果，例如 <quote>Non-recoverable failure in name resolution</quote>。
     </para>
    </listitem>

    <listitem>
     <para>
      修复向客户端发送组合列值时的内存泄漏（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      使 <application>pg_ctl</application> 读取 <filename>postmaster.pid</filename> 文件更健壮（Heikki Linnakangas）
     </para>

     <para>
      这修复了竞态条件和可能的文件描述符泄漏。
     </para>
    </listitem>

    <listitem>
     <para>
      修复当呈现错误编码的数据且 <varname>client_encoding</varname> 设置为仅客户端编码（如 SJIS）时 <application>psql</application> 可能崩溃的问题（Jiang Guiqing）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_dump</application> 以 <literal>tar</literal> 输出格式生成的 <filename>restore.sql</filename> 脚本中的缺陷（Tom Lane）
     </para>

     <para>
      该脚本在表名包含大写字符时会直接失败。此外，让该脚本既能以常规 COPY 模式也能以 <option>--inserts</option> 模式恢复数据。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_restore</application>，使其接受符合 POSIX 的 <literal>tar</literal> 文件（Brian Weaver、Tom Lane）
     </para>

     <para>
      <application>pg_dump</application> 的 <literal>tar</literal> 输出模式的原始编码生成的文件并不完全符合 POSIX 标准。这在 9.3 版本中已得到纠正。此补丁更新了以前的分支，使它们同时接受错误格式和纠正后的格式，希望借此避免 9.3 发布时的兼容性问题。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_resetxlog</application>，使其在给定数据目录相对路径时正确定位 <filename>postmaster.pid</filename>（Tom Lane）
     </para>

     <para>
      这一失误可能导致 <application>pg_resetxlog</application> 没有注意到有活动 postmaster 正在使用该数据目录。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>libpq</application> 的 <function>lo_import()</function> 和 <function>lo_export()</function> 函数，使其正确报告文件 I/O 错误（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>ecpg</application> 对嵌套结构指针变量的处理（Muhammad Usama）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <filename>contrib/pageinspect</filename> 的 btree 页面检查函数在检查页面时持有缓冲区锁（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pgxs</application> 在 AIX 上构建可加载模块的支持（Tom Lane）
     </para>

     <para>
      在原始源码树之外构建模块在 AIX 上不工作。
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2012j
      for DST law changes in Cuba, Israel, Jordan, Libya, Palestine, Western
      Samoa, and portions of Brazil。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-3-21">
  <title>版本 8.3.21</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2012-09-24</para>
  </formalpara>

  <para>
   本次发布包含对 8.3.20 的多方面修复。有关 8.3 大版本中新特性的信息，请参见<xref linkend="release-8-3">。.
  </para>

  <para>
      <productname>PostgreSQL</productname> 社区将于 2013 年 2 月停止对 8.3.X 版本系列的更新。建议用户尽快更新到较新的版本分支。
     </para>

  <sect2>
   <title>迁移到版本 8.3.21</title>

   <para>
    对于运行 8.3.X 的用户，不需要进行转储/恢复。</para>

   <para>
    不过，如果你是从earlier than 8.3.17 的版本升级，请参见<xref linkend="release-8-3-17">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      改进 GiST 索引的页面分裂决策（Alexander Korotkov、Robert Haas、Tom Lane）
     </para>

     <para>
      多列 GiST 索引可能因此错误而遭受意外膨胀。
     </para>
    </listitem>

    <listitem>
     <para>
      修复级联权限撤销，使其在权限仍被持有时停止（Tom Lane）
     </para>

     <para>
      如果我们撤销某个角色 <replaceable>X</replaceable> 的授予选项，但 <replaceable>X</replaceable> 仍通过别人的授予持有该选项，我们就不应从 <replaceable>X</replaceable> 曾授予该权限的角色 <replaceable>Y</replaceable> 那里递归撤销相应权限。
     </para>
    </listitem>

    <listitem>
     <para>
      修复使用 PL/Perl 时对 <literal>SIGFPE</literal> 的处理（Andres Freund）
     </para>

     <para>
      Perl 会把进程的 <literal>SIGFPE</literal> 处理程序重置为 <literal>SIG_IGN</literal>，这可能导致之后崩溃。在初始化 PL/Perl 之后恢复正常的 Postgres 信号处理程序。
     </para>
    </listitem>

    <listitem>
     <para>
      防止递归 PL/Perl 函数在执行期间被重定义时 PL/Perl 崩溃（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      规避 PL/Perl 中可能的错误优化（Tom Lane）
     </para>

     <para>
      一些 Linux 发行版包含错误版本的 <filename>pthread.h</filename>，导致 PL/Perl 中的编译代码不正确，当一个 PL/Perl 函数调用另一个会抛出错误的函数时会导致崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2014e，以反映克里米亚、埃及和摩洛哥的夏令时法规变更。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-3-20">
  <title>版本 8.3.20</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2012-08-17</para>
  </formalpara>

  <para>
   本次发布包含对 8.3.19 的多方面修复。有关 8.3 大版本中新特性的信息，请参见<xref linkend="release-8-3">。.
  </para>

  <para>
      <productname>PostgreSQL</productname> 社区将于 2013 年 2 月停止对 8.3.X 版本系列的更新。建议用户尽快更新到较新的版本分支。
     </para>

  <sect2>
   <title>迁移到版本 8.3.20</title>

   <para>
    对于运行 8.3.X 的用户，不需要进行转储/恢复。</para>

   <para>
    不过，如果你是从earlier than 8.3.17 的版本升级，请参见<xref linkend="release-8-3-17">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      防止通过 XML 实体引用访问外部文件/URL（Noah Misch、Tom Lane）
     </para>

     <para>
      <function>xml_parse()</function> 会尝试按需获取外部文件或 URL 来解析 XML 值中的 DTD 和实体引用，从而使无特权数据库用户能够尝试以数据库服务器的权限获取数据。虽然外部数据不会直接返回给用户，但如果数据不能解析为有效 XML，其部分内容可能暴露在错误消息中；而且无论如何，仅仅能够检查文件是否存在对攻击者就可能有用。（CVE-2012-3489）
     </para>
    </listitem>

    <listitem>
     <para>
      防止通过 <filename>contrib/xml2</filename> 的 <function>xslt_process()</function> 访问外部文件/URL（Peter Eisentraut）
     </para>

     <para>
      <application>libxslt</application> 提供通过样式表命令读写文件和 URL 的能力，从而使无特权数据库用户能够以数据库服务器的权限读写数据。现通过正确使用 <application>libxslt</application> 的安全选项禁用该能力。（CVE-2012-3488）
     </para>

     <para>
      此外，移除了 <function>xslt_process()</function> 从外部文件/URL 获取文档和样式表的能力。虽然这是一个有记载的<quote>特性</quote>，但长期以来被认为是个坏主意。CVE-2012-3489 的修复破坏了该能力，与其花费精力修复它，我们干脆将其移除。
     </para>
    </listitem>

    <listitem>
     <para>
      防止 btree 索引页被过早回收（Noah Misch）
     </para>

     <para>
      当我们允许只读事务跳过分配 XID 时，引入了已删除 btree 页在只读事务仍在访问它时就被回收的可能性。这会导致错误的索引搜索结果。由于时序要求，这种错误在实际中发生的概率似乎很低，但仍应修复。
     </para>
    </listitem>

    <listitem>
     <para>
      修复新创建或重置序列的崩溃安全缺陷（Tom Lane）
     </para>

     <para>
      如果在新创建或重置的序列上执行了 <command>ALTER SEQUENCE</command>，然后恰好对它调用了一次 <function>nextval()</function>，随后服务器崩溃，WAL 重放会把序列恢复到看起来没有执行过 <function>nextval()</function> 的状态，从而使下一个 <function>nextval()</function> 调用再次返回第一个序列值。这种情况尤其可能出现在 <type>serial</type> 列上，因为创建 serial 列的序列包含一个 <command>ALTER SEQUENCE OWNED BY</command> 步骤。
     </para>
    </listitem>

    <listitem>
     <para>
      确保 <function>pg_start_backup()</function> 之后对 <filename>backup_label</filename> 文件执行 fsync（Dave Kerr）
     </para>
    </listitem>

    <listitem>
     <para>
      回移植压缩 fsync 请求队列的 9.1 改进（Robert Haas）
     </para>

     <para>
      这改进了检查点期间的性能。9.1 的变更已经过足够的实际测试，看起来可以安全回移植。
     </para>
    </listitem>

    <listitem>
     <para>
      只允许被直接阻塞的进程自动取消自动清理（Tom Lane）
     </para>

     <para>
      原始编码在某些情况下可能允许不一致的行为；特别是自动清理可能在不足 <literal>deadlock_timeout</literal> 宽限期之后就被取消。
     </para>
    </listitem>

    <listitem>
     <para>
      改进自动清理取消的日志记录（Robert Haas）
     </para>
    </listitem>

    <listitem>
     <para>
      修复日志收集器，使 <literal>log_truncate_on_rotation</literal> 在服务器启动后的第一次日志轮转期间就能工作（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      确保对子查询的整行引用不包含任何多余的 <literal>GROUP BY</literal> 或 <literal>ORDER BY</literal> 列（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      禁止在 <command>CREATE TABLE</command> 期间复制 <literal>CHECK</literal> 约束和索引定义中的整行引用（Tom Lane）
     </para>

     <para>
      这种情况可能出现在带 <literal>LIKE</literal> 或 <literal>INHERITS</literal> 的 <command>CREATE TABLE</command> 中。被复制的整行变量被错误地标记为原表而不是新表的行类型。对 <literal>LIKE</literal> 拒绝这种情况似乎是合理的，因为行类型之后很可能分化。对 <literal>INHERITS</literal>，理想情况下应该允许它，并隐式强制转换为父表的行类型；但这需要的工作量超出了回移植的安全范围。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>ARRAY(SELECT ...)</literal> 子查询中的内存泄漏（Heikki Linnakangas、Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复从正则表达式中提取公共前缀的问题（Tom Lane）
     </para>

     <para>
      代码可能被带量词的带括号子表达式（例如 <literal>^(foo)?bar</literal>）混淆。这会导致对此类模式搜索的索引优化不正确。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/xml2</filename> 的 <function>xslt_process()</function> 中的错误报告（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2012e，以反映摩洛哥和托克劳的夏令时法规变更
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-3-19">
  <title>版本 8.3.19</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2012-06-04</para>
  </formalpara>

  <para>
   本次发布包含对 8.3.18 的多方面修复。有关 8.3 大版本中新特性的信息，请参见<xref linkend="release-8-3">。.
  </para>

  <sect2>
   <title>迁移到版本 8.3.19</title>

   <para>
    对于运行 8.3.X 的用户，不需要进行转储/恢复。</para>

   <para>
    不过，如果你是从earlier than 8.3.17 的版本升级，请参见<xref linkend="release-8-3-17">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      修复 <filename>contrib/pgcrypto</filename> 的 DES <function>crypt()</function> 函数中不正确的口令变换（Solar Designer）
     </para>

     <para>
      如果口令串包含字节值 <literal>0x80</literal>，口令的其余部分会被忽略，使口令比看起来弱得多。修复后，字符串的其余部分会被正确纳入 DES 哈希。因此，受此缺陷影响的任何已存储口令值将不再匹配，所以可能需要更新已存储的值。（CVE-2012-2143）
     </para>
    </listitem>

    <listitem>
     <para>
      忽略过程语言调用处理程序的 <literal>SECURITY DEFINER</literal> 和 <literal>SET</literal> 属性（Tom Lane）
     </para>

     <para>
      把这类属性应用于调用处理程序可能导致服务器崩溃。（CVE-2012-2655）
     </para>
    </listitem>

    <listitem>
     <para>
      允许 <type>timestamp</type> 输入中的数字时区偏移距 UTC 最多 16 小时（Tom Lane）
     </para>

     <para>
      一些历史时区的偏移超过此前 15 小时的限制。这可能导致转储的数据值在重载期间被拒绝。
     </para>
    </listitem>

    <listitem>
     <para>
      修复时间戳转换，使其在给定时间恰好是当前时区最后一次夏令时转换时间时能正确处理（Tom Lane）
     </para>

     <para>
      这一疏忽已存在很长时间，但此前未被注意到，因为大多数使用夏令时的时区被假定有无限的未来夏令时转换序列。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <type>text</type> 到 <type>name</type> 以及 <type>char</type> 到 <type>name</type> 的转换，使其在多字节编码下正确执行字符串截断（Karl Schnaitter）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>to_tsquery()</function> 中的内存复制缺陷（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <structname>pg_attribute</structname> 非常大会话启动缓慢的问题（Tom Lane）
     </para>

     <para>
      如果 <structname>pg_attribute</structname> 超过 <varname>shared_buffers</varname> 的四分之一，会话启动期间有时需要的缓存重建代码就会触发同步扫描逻辑，使其耗时比正常情况多数倍。如果许多新会话同时启动，问题尤为严重。
     </para>
    </listitem>

    <listitem>
     <para>
      确保顺序扫描以合理的频率检查查询取消（Merlin Moncure）
     </para>

     <para>
      遇到许多不含存活元组的连续页面的扫描在此期间不会响应中断。
     </para>
    </listitem>

    <listitem>
     <para>
      确保 <function>PGSemaphoreLock()</function> 的 Windows 实现在返回前清除 <varname>ImmediateInterruptOK</varname>（Tom Lane）
     </para>

     <para>
      这一疏忽意味着同一查询稍后收到的查询取消中断可能在不安全的时刻被接受，后果不可预测且不好。
     </para>
    </listitem>

    <listitem>
     <para>
      在打印视图或规则时安全地显示整行变量（Abbas Butt、Tom Lane）
     </para>

     <para>
      涉及有歧义名称（即该名称既可能是查询的表名也可能是列名）的极端情况此前以有歧义的方式打印，存在视图或规则在转储和重载后被不同解释的风险。现通过附加无操作转换来避免歧义情况。
     </para>
    </listitem>

    <listitem>
     <para>
      确保自动清理工作进程正确执行栈深度检查（Heikki Linnakangas）
     </para>

     <para>
      此前，由自动 <command>ANALYZE</command> 调用的函数中的无限递归可能导致工作进程崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      修复日志收集器，使其在高负载下不丢失日志连贯性（Andrew Dunstan）
     </para>

     <para>
      此前，如果收集器过于忙碌，可能无法重新组装大消息。
     </para>
    </listitem>

    <listitem>
     <para>
      修复日志收集器，确保它在收到 <systemitem>SIGHUP</systemitem> 后会重新开始文件轮转（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PL/pgSQL 的 <command>GET DIAGNOSTICS</command> 命令在目标是函数第一个变量时的问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复数据库包含大量对象时 <application>pg_dump</application> 的若干性能问题（Jeff Janes、Tom Lane）
     </para>

     <para>
      如果数据库包含许多模式、许多对象处于依赖循环中、或有许多被拥有的序列，<application>pg_dump</application> 可能变得非常慢。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/dblink</filename> 的 <function>dblink_exec()</function>，使其在出错时不泄漏临时数据库连接（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2012c
      for DST law changes in Antarctica, Armenia, Chile, Cuba, Falkland
      Islands, Gaza, Haiti, Hebron, Morocco, Syria, and Tokelau Islands;
      also historical corrections for Canada。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-3-18">
  <title>版本 8.3.18</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2012-02-27</para>
  </formalpara>

  <para>
   本次发布包含对 8.3.17 的多方面修复。有关 8.3 大版本中新特性的信息，请参见<xref linkend="release-8-3">。.
  </para>

  <sect2>
   <title>迁移到版本 8.3.18</title>

   <para>
    对于运行 8.3.X 的用户，不需要进行转储/恢复。</para>

   <para>
    不过，如果你是从earlier than 8.3.17 的版本升级，请参见<xref linkend="release-8-3-17">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      让 <command>CREATE TRIGGER</command> 要求对触发器函数有执行权限（Robert Haas）
     </para>

     <para>
      这一缺失的检查可能允许另一用户通过把触发器函数安装到自己拥有的表上，以伪造的输入数据执行该触发器函数。这只对标记为 <literal>SECURITY DEFINER</literal> 的触发器函数有影响，因为其他情况下触发器函数本来就以表所有者身份运行。（CVE-2012-0866）
     </para>
    </listitem>

    <listitem>
     <para>
      把 <application>pg_dump</application> 注释中写入的名称里的换行符转换为空格（Robert Haas）
     </para>

     <para>
      <application>pg_dump</application> 此前对其输出脚本中 SQL 注释内写出的对象名清理不慎。包含换行符的名称至少会使脚本语法错误。恶意构造的对象名可能在脚本重载时构成 SQL 注入风险。（CVE-2012-0868）
     </para>
    </listitem>

    <listitem>
     <para>
      修复插入与清理并发导致 btree 索引损坏的问题（Tom Lane）
     </para>

     <para>
      插入导致的索引页分裂有时会使并发运行的 <command>VACUUM</command> 漏删本应删除的索引项。在相应的表行被删除后，悬空的索引项会导致错误（例如 <quote>could not read block N in file ...</quote>），或者更糟：当不相关的行被重新插入到现已空闲的表位置后，查询会静默地得到错误结果。该缺陷自 8.2 起就存在，但发生频率极低，直到现在才被诊断出来。如果你有理由怀疑它曾在你的数据库中发生，对受影响的索引重建索引即可修复。
     </para>
    </listitem>

    <listitem>
     <para>
      允许 <command>ALTER USER/DATABASE SET</command> 中某些设置取不存在的值（Heikki Linnakangas）
     </para>

     <para>
      允许把 <varname>default_text_search_config</varname>、<varname>default_tablespace</varname> 和 <varname>temp_tablespaces</varname> 设为未知的名称。这是因为设置要在其中使用的另一个数据库中它们可能是已知的；对表空间的情况，则是因为表空间可能尚未创建。此前 <varname>search_path</varname> 已被认识到有同样的问题，这些设置现在的行为与它一致。
     </para>
    </listitem>

    <listitem>
     <para>
      在 WAL 重放期间正确跟踪 OID 计数器，即使它发生回卷（Tom Lane）
     </para>

     <para>
      此前 OID 计数器会一直停留在高值，直到系统退出重放模式。其实际后果通常为零，但在某些场景下，已提升为主库的备库服务器在需要 OID 值时，可能要很长时间才能把 OID 计数器推进到合理的值。
     </para>
    </listitem>

    <listitem>
     <para>
      修复带 <literal>*</literal> 的正则表达式反向引用（Tom Lane）
     </para>

     <para>
      代码实际上并不强制精确字符串匹配，而是接受任何满足反向引用符号所引用的模式子表达式的字符串。
     </para>

     <para>
      类似问题仍困扰着嵌入在更大带量词表达式中（而非量词直接作用对象）的反向引用。这将在未来的 <productname>PostgreSQL</productname> 版本中解决。
     </para>
    </listitem>

    <listitem>
     <para>
      修复最近引入的 <type>inet</type>/<type>cidr</type> 值处理中的内存泄漏（Heikki Linnakangas）
     </para>

     <para>
      <productname>PostgreSQL</productname> 2011 年 12 月版本中的一个补丁导致这些操作中的内存泄漏，在此类列上构建 btree 索引等场景中可能相当显著。
     </para>
    </listitem>

    <listitem>
     <para>
      避免 Windows 上 syslogger 中文件句柄的双重关闭（MauMau）
     </para>

     <para>
      通常这个错误不可见，但在调试版 Windows 上运行时会导致异常。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 plpgsql 中与 I/O 转换相关的内存泄漏（Andres Freund、Jan Urbanski、Tom Lane）
     </para>

     <para>
      某些操作会一直泄漏内存直到当前函数结束。
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <application>pg_dump</application> 对继承表列的处理（Tom Lane）
     </para>

     <para>
      <application>pg_dump</application> 错误处理了子列与父列默认表达式不同的情况。如果默认值在文本上与父列相同但实际不同（例如因为模式搜索路径差异），它不会被识别为不同，因此转储和恢复后子列会被允许继承父列的默认值。子列为 <literal>NOT NULL</literal> 而父列不是的情况也可能被微妙地错误恢复。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_restore</application> 直接写数据库模式对 INSERT 式表数据的处理（Tom Lane）
     </para>

     <para>
      当使用 2011 年 9 月或 12 月版本的 <application>pg_restore</application> 时，对以 <option>--inserts</option> 或 <option>--column-inserts</option> 选项制作的归档文件直接恢复到数据库会失败，这是对另一个问题的修复中的一个疏忽所致。归档文件本身没有问题，文本模式输出也正常。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/intarray</filename> 的 <literal>int[] &amp; int[]</literal> 操作符中的错误（Guillaume Lelarge）
     </para>

     <para>
      如果两个输入数组共有的最小整数是 1，且任一数组中有更小的值，那么 1 会被错误地从结果中遗漏。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/pgcrypto</filename> 的 <function>encrypt_iv()</function> 和 <function>decrypt_iv()</function> 中的错误检测（Marko Kreen）
     </para>

     <para>
      这些函数未能报告某些类型的无效输入错误，而是对错误输入返回随机垃圾值。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/test_parser</filename> 中的一字节缓冲区越界（Paul Guyot）
     </para>

     <para>
      代码会尝试多读一个字节，这在极端情况下会崩溃。由于 <filename>contrib/test_parser</filename> 只是示例代码，这本身不是安全问题，但坏的示例代码仍然是坏的。
     </para>
    </listitem>

    <listitem>
     <para>
      在 ARM 上如果可用则使用 <function>__sync_lock_test_and_set()</function> 实现自旋锁（Martin Pitt）
     </para>

     <para>
      该函数取代了我们此前对 <literal>SWPB</literal> 指令的使用，该指令已被弃用且在 ARMv6 及更高版本上不可用。报告表明旧代码在较新的 ARM 板上不会以明显方式失败，而只是不对并发访问互锁，导致多进程操作中的离奇失败。
     </para>
    </listitem>

    <listitem>
     <para>
      在使用接受该选项的 gcc 版本构建时使用 <option>-fexcess-precision=standard</option> 选项（Andrew Dunstan）
     </para>

     <para>
      这防止了较新版本 gcc 产生"创造性"结果的各种场景。
     </para>
    </listitem>

    <listitem>
     <para>
      允许在 FreeBSD 上使用线程化 Python（Chris Rees）
     </para>

     <para>
      我们的 configure 脚本此前认为这种组合无法工作；但 FreeBSD 修复了问题，因此移除该错误检查。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-3-17">
  <title>版本 8.3.17</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2011-12-05</para>
  </formalpara>

  <para>
   本次发布包含对 8.3.16 的多方面修复。有关 8.3 大版本中新特性的信息，请参见<xref linkend="release-8-3">。.
  </para>

  <sect2>
   <title>迁移到版本 8.3.17</title>

   <para>
    对于运行 8.3.X 的用户，不需要进行转储/恢复。</para>

   <para>
      不过，<literal>information_schema.referential_constraints</literal> 视图的定义中被发现存在一个长期错误。如果你依赖该视图的正确结果，应按下面第一条变更日志条目的说明替换其定义。
     </para>

   <para>
    不过，如果你是从earlier than 8.3.8 的版本升级，请参见<xref linkend="release-8-3-8">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      修复 <literal>information_schema.referential_constraints</literal> 视图中的缺陷（Tom Lane）
     </para>

     <para>
      该视图在把外键约束与它依赖的主键或唯一键约束匹配时不够谨慎。这可能导致某个外键约束完全不被显示、被显示多次，或者声称它依赖于一个并非其实际依赖的约束。
     </para>

     <para>
      由于该视图定义是由 <application>initdb</application> 安装的，仅靠升级不会修复问题。如果需要修复现有安装，可以（以超级用户身份）删除 <literal>information_schema</literal> 模式，然后通过载入 <filename><replaceable>SHAREDIR</replaceable>/information_schema.sql</filename> 重新创建它。（如果不确定 <replaceable>SHAREDIR</replaceable> 在哪里，运行 <literal>pg_config --sharedir</literal>。）这必须在每个要修复的数据库中重复执行。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>CREATE TABLE dest AS SELECT * FROM src</literal> 或 <literal>INSERT INTO dest SELECT * FROM src</literal> 期间与 TOAST 相关的数据损坏（Tom Lane）
     </para>

     <para>
      如果表曾被 <command>ALTER TABLE ADD COLUMN</command> 修改过，在某些极端情况下，尝试把它的数据原样复制到另一个表可能产生损坏的结果。该问题只会在 8.4 及更高版本中以这种确切形式出现，但我们也给更早的版本打了补丁，以防有其他代码路径可能触发同一缺陷。
     </para>
    </listitem>

    <listitem>
     <para>
      修复从过期 syscache 条目访问 toast 表时的竞态条件（Tom Lane）
     </para>

     <para>
      典型症状是诸如 <quote>missing chunk number 0 for toast value NNNNN in pg_toast_2619</quote> 的瞬时错误，其中引用的 toast 表总是属于某个系统目录。
     </para>
    </listitem>

    <listitem>
     <para>
      让 <function>DatumGetInetP()</function> 解包带 1 字节头的 inet datum，并新增一个不做此事的宏 <function>DatumGetInetPP()</function>（Heikki Linnakangas）
     </para>

     <para>
      此变更不影响任何核心代码，但可能避免期望 <function>DatumGetInetP()</function> 按常规约定产生已解包 datum 的附加代码中的崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <type>money</type> 类型输入和输出的区域支持（Tom Lane）
     </para>

     <para>
      除了不支持所有标准 <link linkend="guc-lc-monetary"><varname>lc_monetary</varname></link> 格式化选项外，输入和输出函数还不一致，这意味着在某些区域设置下转储的 <type>money</type> 值无法重新读取。
     </para>
    </listitem>

    <listitem>
     <para>
      不让 <link linkend="guc-transform-null-equals"><varname>transform_null_equals</varname></link> 影响 <literal>CASE foo WHEN NULL ...</literal> 构造（Heikki Linnakangas）
     </para>

     <para>
      <varname>transform_null_equals</varname> 只应影响用户直接写的 <literal>foo = NULL</literal> 表达式，而不是这种形式的 <literal>CASE</literal> 在内部生成的相等性检查。
     </para>
    </listitem>

    <listitem>
     <para>
      更改外键触发器的创建顺序，以更好地支持自引用外键（Tom Lane）
     </para>

     <para>
      对于引用自身表的级联外键，行更新会作为一个事件同时触发 <literal>ON UPDATE</literal> 触发器和 <literal>CHECK</literal> 触发器。<literal>ON UPDATE</literal> 触发器必须先执行，否则 <literal>CHECK</literal> 会检查行的非最终状态，并可能抛出不恰当的错误。然而这些触发器的触发顺序由其名称决定，而由于触发器使用遵循 <quote>RI_ConstraintTrigger_NNNN</quote> 约定的自动生成名称，它们通常按创建顺序排序。彻底的修复需要修改该约定（我们将在 9.2 中做），但在现有版本中更改它似乎有风险。因此此补丁只更改触发器的创建顺序。遇到这类错误的用户应删除并重新创建外键约束，使其触发器处于正确顺序。
     </para>
    </listitem>

    <listitem>
     <para>
      避免在跟踪缓冲区分配率时发生浮点下溢（Greg Matthews）
     </para>

     <para>
      虽然这本身无害，但在某些平台上会导致烦人的内核日志消息。
     </para>
    </listitem>

    <listitem>
     <para>
      在 <application>psql</application> 的命令历史中保留命令内的空行（Robert Haas）
     </para>

     <para>
      例如，如果从字符串字面量中删除了空行，先前的行为可能导致问题。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_dump</application>，使其转储自动生成类型（如表行类型）之间的用户定义转换（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      使用首选版本的 <application>xsubpp</application> 构建 PL/Perl，而不一定是操作系统的主副本（David Wheeler、Alex Hunsaker）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/dict_int</filename> 和 <filename>contrib/dict_xsyn</filename> 中的错误编码（Tom Lane）
     </para>

     <para>
      一些函数错误地假定 <function>palloc()</function> 返回的内存保证被清零。
     </para>
    </listitem>

    <listitem>
     <para>
      让 <function>pgstatindex()</function> 及时响应查询取消中断（Robert Haas）
     </para>
    </listitem>

    <listitem>
     <para>
      确保 VPATH 构建正确安装所有服务器头文件（Peter Eisentraut）
     </para>
    </listitem>

    <listitem>
     <para>
      缩短详细错误消息中报告的文件名（Peter Eisentraut）
     </para>

     <para>
      常规构建一直只报告包含错误消息调用的 C 文件的名称，但 VPATH 构建此前报告绝对路径名。
     </para>
    </listitem>

    <listitem>
     <para>
      修复中美洲 Windows 时区名的解释（Tom Lane）
     </para>

     <para>
      把<quote>Central America Standard Time</quote>映射为 <literal>CST6</literal> 而不是 <literal>CST6CDT</literal>，因为中美洲各地通常不实行夏令时。
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2011n
      for DST law changes in Brazil, Cuba, Fiji, Palestine, Russia, and Samoa;
      also historical corrections for Alaska and British East Africa。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-3-16">
  <title>版本 8.3.16</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2011-09-26</para>
  </formalpara>

  <para>
   本次发布包含对 8.3.15 的多方面修复。有关 8.3 大版本中新特性的信息，请参见<xref linkend="release-8-3">。.
  </para>

  <sect2>
   <title>迁移到版本 8.3.16</title>

   <para>
      对于运行 8.3.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.3.8 的版本升级，请参阅<xref linkend="release-8-3-8">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      修复对不确定的 HOT 更新元组建立索引的缺陷（Tom Lane）
     </para>

     <para>
      这些缺陷可能导致重建系统目录索引之后索引损坏。据信它们不影响用户索引。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 GiST 索引页面分裂处理中的多个缺陷（Heikki Linnakangas）
     </para>

     <para>
      发生概率很低，但它们可能导致索引损坏。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>tsvector_concat()</function> 中可能的缓冲区越界（Tom Lane）
     </para>

     <para>
      该函数可能低估其结果所需的内存量，导致服务器崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>xml_recv</function> 处理<quote>standalone</quote>参数时的崩溃（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      避免在 <command>ANALYZE</command> 和 SJIS-2004 编码转换中可能越过内存末尾的访问（Noah Misch）
     </para>

     <para>
      这修复了一些概率极低的服务器崩溃场景。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 relcache init 文件失效中的竞态条件（Tom Lane）
     </para>

     <para>
      存在一个时间窗口，新的后端进程可能读到过期的 init 文件，却错过本应告知它数据已过期的失效消息。结果是目录访问中的离奇失败，通常在启动稍后阶段报 <quote>could not read block 0 in file ...</quote>。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 GiST 索引扫描结束时的内存泄漏（Tom Lane）
     </para>

     <para>
      执行许多独立 GiST 索引扫描的命令（例如对已包含许多行的表验证新的基于 GiST 的排他约束）会因此泄漏瞬时需要大量内存。
     </para>
    </listitem>

    <listitem>
     <para>
      修复构造大型有损位图时的性能问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复数组和路径创建函数，确保填充字节为零（Tom Lane）
     </para>

     <para>
      这避免了一些规划器认为语义相等的常量不相等的情况，这种情况会导致糟糕的优化。
     </para>
    </listitem>

    <listitem>
     <para>
      规避破坏 WAL 重放的 gcc 4.6.0 缺陷（Tom Lane）
     </para>

     <para>
      这可能导致服务器崩溃后已提交事务的丢失。
     </para>
    </listitem>

    <listitem>
     <para>
      修复视图中 <literal>VALUES</literal> 的转储缺陷（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      禁止对序列执行 <literal>SELECT FOR UPDATE/SHARE</literal>（Tom Lane）
     </para>

     <para>
      这种操作不像预期那样工作，可能导致失败。
     </para>
    </listitem>

    <listitem>
     <para>
      防御计算哈希表大小时的整数溢出（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <command>CLUSTER</command> 可能尝试访问已移除的 TOAST 数据的情况（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <quote>peer</quote> 认证使用凭据控制消息时的可移植性缺陷（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复需要多次往返时的 SSPI 登录（Ahmed Shinwari、Magnus Hagander）
     </para>

     <para>
      该问题的典型症状是 SSPI 登录期间的 <quote>The function requested is not supported</quote> 错误。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>pg_srand48</function> 种子初始化中的笔误（Andres Freund）
     </para>

     <para>
      这导致未能使用所提供种子的所有位。该函数在大多数平台上不使用（只在没有 <function>srandom</function> 的平台上使用），而且无论如何看待，随机性低于预期的种子带来的潜在安全暴露似乎都很小。
     </para>
    </listitem>

    <listitem>
     <para>
      避免 <literal>LIMIT</literal> 与 <literal>OFFSET</literal> 值之和超过 2^63 时的整数溢出（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      为 <function>generate_series()</function> 的 <type>int4</type> 和 <type>int8</type> 版本添加溢出检查（Robert Haas）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>to_char()</function> 中移除尾随零的问题（Marti Raudsepp）
     </para>

     <para>
      在使用 <literal>FM</literal> 且小数点后没有数字位的格式中，小数点左侧的零可能被错误移除。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>pg_size_pretty()</function>，避免接近 2^63 的输入溢出（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      在 <application>pg_ctl</application> 中，支持 Windows 上服务注册的静默模式（MauMau）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>psql</application> 在从其他文件执行 <literal>COPY</literal> 期间脚本文件行号计数的问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_restore</application> 直接写数据库模式对 <varname>standard_conforming_strings</varname> 的处理（Tom Lane）
     </para>

     <para>
      当从以 <varname>standard_conforming_strings</varname> 设为 <literal>on</literal> 制作的归档文件直接恢复到数据库服务器时，<application>pg_restore</application> 可能发出不正确的命令。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>libpq</application> 的 LDAP 服务查找代码中的越界写入和内存泄漏（Albe Laurenz）
     </para>
    </listitem>

    <listitem>
     <para>
      在 <application>libpq</application> 中，避免使用非阻塞 I/O 和 SSL 连接时的失败（Martin Pihlak、Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      改进 libpq 对连接启动期间失败的处理（Tom Lane）
     </para>

     <para>
      特别是，对 SSL 连接启动期间服务器报告 <function>fork()</function> 失败的响应现在更合理。
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <application>libpq</application> 对 SSL 失败的错误报告（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <application>ecpglib</application> 以 15 位精度写 <type>double</type> 值（Akira Kurosawa）
     </para>
    </listitem>

    <listitem>
     <para>
      在 <application>ecpglib</application> 中，确保出错后恢复 <literal>LC_NUMERIC</literal> 设置（Michael Meskes）
     </para>
    </listitem>

    <listitem>
     <para>
      应用上游对 blowfish 有符号字符缺陷的修复（CVE-2011-2483）（Tom Lane）
     </para>

     <para>
      <filename>contrib/pg_crypto</filename> 的 blowfish 加密代码在 char 为有符号的平台（大多数平台如此）上可能给出错误结果，导致加密口令比应有的强度更弱。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/seg</filename> 中的内存泄漏（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>pgstatindex()</function>，使其对空索引给出一致结果（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      允许使用 perl 5.14 构建（Alex Hunsaker）
     </para>
    </listitem>

    <listitem>
     <para>
      更新 configure 脚本探测系统函数是否存在的方法（Tom Lane）
     </para>

     <para>
      我们在 8.3 和 8.2 中使用的 autoconf 版本可能被执行链接时优化的编译器欺骗。
     </para>
    </listitem>

    <listitem>
     <para>
      修复构建和安装文件路径包含空格的各种问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2011i
      for DST law changes in Canada, Egypt, Russia, Samoa, and South Sudan。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-3-15">
  <title>版本 8.3.15</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2011-04-18</para>
  </formalpara>

  <para>
   本次发布包含对 8.3.14 的多方面修复。有关 8.3 大版本中新特性的信息，请参见<xref linkend="release-8-3">。.
  </para>

  <sect2>
   <title>迁移到版本 8.3.15</title>

   <para>
      对于运行 8.3.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.3.8 的版本升级，请参阅<xref linkend="release-8-3-8">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      禁止在组合类型中包含其自身（Tom Lane）
     </para>

     <para>
      这防止了服务器在处理该组合类型时无限递归的场景。虽然这种结构有一些可能的用途，但它们似乎不足以抵消确保其始终安全工作所需的努力。
     </para>
    </listitem>

    <listitem>
     <para>
      避免目录缓存初始化期间的潜在死锁（Nikhil Sontakke）
     </para>

     <para>
      某些情况下，缓存加载代码会在锁定索引的目录之前获取系统索引上的共享锁。这可能与尝试以另一种更标准的顺序获取排他锁的进程死锁。
     </para>
    </listitem>

    <listitem>
     <para>
      修复当目标元组存在并发更新时 <literal>BEFORE ROW UPDATE</literal> 触发器处理中的悬空指针问题（Tom Lane）
     </para>

     <para>
      已观察到该缺陷在尝试执行 <literal>UPDATE RETURNING ctid</literal> 时导致间歇性的 <quote>cannot extract system attribute from virtual tuple</quote> 失败。发生更严重错误（例如为更新后的元组生成错误索引项）的概率非常小。
     </para>
    </listitem>

    <listitem>
     <para>
      当表存在未决的延迟触发器事件时禁止 <command>DROP TABLE</command>（Tom Lane）
     </para>

     <para>
      此前 <command>DROP</command> 会照常执行，导致触发器最终触发时报 <quote>could not open relation with OID nnn</quote> 错误。
     </para>
    </listitem>

    <listitem>
     <para>
      修复涉及数组切片的 PL/Python 内存泄漏（Daniel Popowich）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_restore</application>，使其能处理 TOC 文件中的长行（超过 1KB）（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      加入更多防护，防止因过于激进的编译器优化导致的除零崩溃（Aurelien Jarno）
     </para>
    </listitem>

    <listitem>
     <para>
      支持在 FreeBSD 和 OpenBSD 的 MIPS 上使用 dlopen()（Tom Lane）
     </para>

     <para>
      此前有一个硬编码的假设：该系统函数在这些系统的 MIPS 硬件上不可用。现在改用编译期测试，因为较新的版本已经有了它。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 HP-UX 上的编译失败（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 Windows 上 <application>libintl</application> 的版本不兼容问题（Hiroshi Inoue）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 Windows 构建脚本中 <application>xcopy</application> 的用法，使其在 Windows 7 下正确工作（Andrew Dunstan）
     </para>

     <para>
      这只影响构建脚本，不影响安装或使用。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_regress</application> 在 Cygwin 上使用的路径分隔符（Andrew Dunstan）
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2011f
      for DST law changes in Chile, Cuba, Falkland Islands, Morocco, Samoa,
      and Turkey; also historical corrections for South Australia, Alaska,
      and Hawaii。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-3-14">
  <title>版本 8.3.14</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2011-01-31</para>
  </formalpara>

  <para>
   本次发布包含对 8.3.13 的多方面修复。有关 8.3 大版本中新特性的信息，请参见<xref linkend="release-8-3">。.
  </para>

  <sect2>
   <title>迁移到版本 8.3.14</title>

   <para>
      对于运行 8.3.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.3.8 的版本升级，请参阅<xref linkend="release-8-3-8">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      避免 <command>EXPLAIN</command> 尝试显示简单形式 <literal>CASE</literal> 表达式时的失败（Tom Lane）
     </para>

     <para>
      如果 <literal>CASE</literal> 的测试表达式是常量，规划器可能把 <literal>CASE</literal> 简化为令表达式显示代码困惑的形式，导致 <quote>unexpected CASE WHEN clause</quote> 错误。
     </para>
    </listitem>

    <listitem>
     <para>
      修复对现有下标范围之前的数组切片赋值的问题（Tom Lane）
     </para>

     <para>
      如果新增下标与第一个既有下标之间存在间隙，代码会错误计算需要从旧数组的空值位图复制多少条目，可能导致数据损坏或崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      避免规划器对非常久远的日期值出现意外的转换溢出（Tom Lane）
     </para>

     <para>
      <type>date</type> 类型支持的日期范围比 <type>timestamp</type> 类型能表示的更宽，但规划器假定它总是可以不受惩罚地把 date 转换为 timestamp。
     </para>
    </listitem>

    <listitem>
     <para>
      修复启用 <varname>standard_conforming_strings</varname> 时 <application>pg_restore</application> 对大对象（BLOB）的文本输出（Tom Lane）
     </para>

     <para>
      虽然直接恢复到数据库时工作正确，但如果要求 <application>pg_restore</application> 生成 SQL 文本输出且源数据库中已启用 <varname>standard_conforming_strings</varname>，字符串转义是不正确的。
     </para>
    </listitem>

    <listitem>
     <para>
      修复对包含 <literal>... &amp; !(subexpression) | ...</literal> 的 <type>tsquery</type> 值的错误解析（Tom Lane）
     </para>

     <para>
      包含这种操作符组合的查询没有被正确执行。同样的错误也存在于 <filename>contrib/intarray</filename> 的 <type>query_int</type> 类型和 <filename>contrib/ltree</filename> 的 <type>ltxtquery</type> 类型中。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/intarray</filename> 的 <type>query_int</type> 类型输入函数中的缓冲区越界（Apple）
     </para>

     <para>
      该缺陷构成安全风险，因为函数的返回地址可能被覆写。感谢 Apple Inc 的安全团队报告此问题并提供修复。（CVE-2010-4015）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/seg</filename> 的 GiST picksplit 算法中的缺陷（Alexander Korotkov）
     </para>

     <para>
      这可能导致 <type>seg</type> 列上的 GiST 索引相当低效，尽管答案并非真正错误。如果你有这样的索引，考虑在安装此更新后对其执行 <command>REINDEX</command>。（这与上一次更新中在 <filename>contrib/cube</filename> 中修复的缺陷相同。）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-3-13">
  <title>版本 8.3.13</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2010-12-16</para>
  </formalpara>

  <para>
   本次发布包含对 8.3.12 的多方面修复。有关 8.3 大版本中新特性的信息，请参见<xref linkend="release-8-3">。.
  </para>

  <sect2>
   <title>迁移到版本 8.3.13</title>

   <para>
      对于运行 8.3.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.3.8 的版本升级，请参阅<xref linkend="release-8-3-8">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      强制 Linux 上的默认 <link linkend="guc-wal-sync-method"><varname>wal_sync_method</varname></link> 为 <literal>fdatasync</literal>（Tom Lane、Marti Raudsepp）
     </para>

     <para>
      Linux 上的默认值实际上多年来一直是 <literal>fdatasync</literal>，但最近的内核变更导致 <productname>PostgreSQL</productname> 改选 <literal>open_datasync</literal>。这一选择没有带来任何性能改进，反而在某些文件系统（特别是使用 <literal>data=journal</literal> 挂载选项的 <literal>ext4</literal>）上导致彻底失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 GIN 索引 WAL 重放逻辑中的若干缺陷（Tom Lane）
     </para>

     <para>
      这可能导致复制期间的 <quote>bad buffer id: 0</quote> 失败或索引内容损坏。
     </para>
    </listitem>

    <listitem>
     <para>
      修复当起始检查点 WAL 记录与其重做点不在同一 WAL 段时从基础备份恢复的问题（Jeff Davis）
     </para>
    </listitem>

    <listitem>
     <para>
      修复多个工作进程长期保持活动时自动清理工作进程的持续变慢（Tom Lane）
     </para>

     <para>
      如果自动清理工作进程处理了足够多的表，其有效 <varname>vacuum_cost_limit</varname> 可能降到接近零，导致其运行极慢。
     </para>
    </listitem>

    <listitem>
     <para>
      新增在 <literal>IA64</literal> 上检测寄存器栈溢出的支持（Tom Lane）
     </para>

     <para>
      <literal>IA64</literal> 架构有两个硬件栈。要完全防止栈溢出失败需要同时检查两者。
     </para>
    </listitem>

    <listitem>
     <para>
      在 <function>copyObject()</function> 中添加栈溢出检查（Tom Lane）
     </para>

     <para>
      给定足够复杂的查询，某些代码路径可能因栈溢出而崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      修复临时 GiST 索引中页面分裂的检测（Heikki Linnakangas）
     </para>

     <para>
      在临时索引中可能出现<quote>并发</quote>页面分裂，例如当插入时有打开的游标正在扫描该索引。GiST 未能检测到这种情况，因此当游标继续执行时可能给出错误结果。
     </para>
    </listitem>

    <listitem>
     <para>
      避免在 <command>ANALYZE</command> 复杂索引表达式时内存泄漏（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      确保使用整行 Var 的索引仍依赖于其表（Tom Lane）
     </para>

     <para>
      像 <literal>create index i on t (foo(t.*))</literal> 这样声明的索引在删除其表时不会自动被删除。
     </para>
    </listitem>

    <listitem>
     <para>
      不要<quote>内联</quote>带多个 <literal>OUT</literal> 参数的 SQL 函数（Tom Lane）
     </para>

     <para>
      这避免了因丢失预期结果行类型信息而可能出现的崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      修复当 <literal>ORDER BY</literal>、<literal>LIMIT</literal>、<literal>FOR UPDATE</literal> 或 <literal>WITH</literal> 附加到 <literal>INSERT ... VALUES</literal> 的 <literal>VALUES</literal> 部分时的正确行为（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>COALESCE()</literal> 表达式的常量折叠（Tom Lane）
     </para>

     <para>
      规划器有时会尝试求值实际上永远不可达的子表达式，可能导致意外错误。
     </para>
    </listitem>

    <listitem>
     <para>
      修复当连接接受（<function>accept()</function> 或紧随其后的调用之一）失败且 postmaster 编译时带 GSSAPI 支持时的 postmaster 崩溃（Alexander Chernikov）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <varname>log_temp_files</varname> 处于活动状态时临时文件漏删的问题（Tom Lane）
     </para>

     <para>
      如果尝试发出日志消息时出错，就不会执行删除，导致临时文件累积。
     </para>
    </listitem>

    <listitem>
     <para>
      为 <structname>InhRelation</structname> 节点添加打印功能（Tom Lane）
     </para>

     <para>
      这避免了启用 <varname>debug_print_parse</varname> 且执行某些类型的查询时的失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复点到水平线段距离的错误计算（Tom Lane）
     </para>

     <para>
      该缺陷影响多个不同的几何距离测量操作符。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>PL/pgSQL</application> 对<quote>简单</quote>表达式的处理，使其在递归或错误恢复情况下不失败（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>PL/Python</application> 对集合返回函数的处理（Jan Urbanski）
     </para>

     <para>
      在生成集合结果的迭代器内尝试调用 SPI 函数会失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/cube</filename> 的 GiST picksplit 算法中的缺陷（Alexander Korotkov）
     </para>

     <para>
      这可能导致 <type>cube</type> 列上的 GiST 索引相当低效，尽管答案并非真正错误。如果你有这样的索引，考虑在安装此更新后对其执行 <command>REINDEX</command>。
     </para>
    </listitem>

    <listitem>
     <para>
      <filename>contrib/dblink</filename> 除了在创建新连接时外不再发出<quote>identifier will be truncated</quote>通知（Itagaki Takahiro）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/pgcrypto</filename> 在缺少公钥时可能的核心转储（Marti Raudsepp）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/xml2</filename> 的 XPath 查询函数中的内存泄漏（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2010o
      for DST law changes in Fiji and Samoa;
      also historical corrections for Hong Kong。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-3-12">
  <title>版本 8.3.12</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2010-10-04</para>
  </formalpara>

  <para>
   本次发布包含对 8.3.11 的多方面修复。有关 8.3 大版本中新特性的信息，请参见<xref linkend="release-8-3">。.
  </para>

  <sect2>
   <title>迁移到版本 8.3.12</title>

   <para>
      对于运行 8.3.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.3.8 的版本升级，请参阅<xref linkend="release-8-3-8">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      在 PL/Perl 和 PL/Tcl 中为每个调用方 SQL 用户 ID 使用单独的解释器（Tom Lane）
     </para>

     <para>
      此变更防止了可通过破坏稍后将在同一会话中以另一 SQL 用户身份执行的 Perl 或 Tcl 代码（例如在 <literal>SECURITY DEFINER</literal> 函数内）而造成的安全问题。大多数脚本语言都提供许多实现这一点的手段，例如重新定义目标函数调用的标准函数或操作符。如果没有此变更，任何拥有 Perl 或 Tcl 语言使用权的 SQL 用户基本上都能以目标函数所有者的 SQL 权限为所欲为。
     </para>

     <para>
      此变更的代价是 Perl 和 Tcl 函数之间的有意通信变得更困难。为提供出路，PL/PerlU 和 PL/TclU 函数继续每会话只使用一个解释器。这不被视为安全问题，因为所有这类函数本来就以数据库超级用户的信任级别执行。
     </para>

     <para>
      声称提供可信执行的第三方过程语言很可能存在类似的安全问题。我们建议就安全关键用途联系你所依赖的任何 PL 的作者。
     </para>

     <para>
      感谢 Tim Bunce 指出此问题（CVE-2010-3433）。
     </para>
    </listitem>

    <listitem>
     <para>
      禁止以并非它预期配合使用的系统目录列的参数调用 <function>pg_get_expr()</function>，以防止可能的崩溃（Heikki Linnakangas、Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      在 Windows 上把退出码 128（<literal>ERROR_WAIT_NO_CHILDREN</literal>）视为非致命（Magnus Hagander）
     </para>

     <para>
      高负载下，Windows 进程有时会在启动时以此错误码失败。此前 postmaster 把这视为 panic 情况并重启整个数据库，但这似乎反应过度。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 Append 索引扫描中对非严格 OR 连接子句的错误使用（Tom Lane）
     </para>

     <para>
      这是 8.4 中一个修复的向后移植，该修复此前在 8.3 分支中被遗漏。这纠正了 8.3.8 中引入的一个错误，当内部关系是继承树或 <literal>UNION ALL</literal> 子查询时，该错误可能导致外连接的结果不正确。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>UNION ALL</literal> 成员关系可能被重复扫描的问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复<quote>无法处理未规划的子查询</quote>错误（Tom Lane）
     </para>

     <para>
      当子查询包含一个连接别名引用，而该引用展开成包含另一个子查询的表达式时，会出现此错误。
     </para>
    </listitem>

    <listitem>
     <para>
      修复未将缓存计划标记为瞬态的失败（Tom Lane）
     </para>

     <para>
      如果在某个被引用表正在执行 <command>CREATE INDEX CONCURRENTLY</command> 期间准备了计划，应当在索引可供使用后重新规划。这此前没有可靠地发生。
     </para>
    </listitem>

    <listitem>
     <para>
      在偶有报告的 btree 失败情形中把 PANIC 降为 ERROR，并在产生的错误消息中提供更多细节（Tom Lane）
     </para>

     <para>
      这应能改进系统对损坏索引的健壮性。
     </para>
    </listitem>

    <listitem>
     <para>
      防止 show_session_authorization() 在自动清理进程中崩溃（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      防御返回 setof record 但并非所有返回行实际为同一行类型的函数（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复对按引用传递的函数结果计算哈希时可能的失败（Tao Ma、Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      改进归并连接对连接列中 NULL 的处理（Tom Lane）
     </para>

     <para>
      如果排序顺序使 NULL 排在高位，归并连接现在可以在到达第一个 NULL 时完全停止。
     </para>
    </listitem>

    <listitem>
     <para>
      写入锁文件（<filename>postmaster.pid</filename> 和套接字锁文件两者）内容时注意对其执行 fsync（Tom Lane）
     </para>

     <para>
      如果机器在 postmaster 启动后不久崩溃，这一疏漏可能导致锁文件内容损坏。这又可能使后续启动 postmaster 的尝试失败，直到手工删除锁文件。
     </para>
    </listitem>

    <listitem>
     <para>
      避免在为深层嵌套的子事务分配 XID 时递归（Andres Freund、Robert Haas）
     </para>

     <para>
      在栈空间有限时，原来的编码可能导致崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      避免 walwriter 进程持有打开的旧 WAL 段（Magnus Hagander、Heikki Linnakangas）
     </para>

     <para>
      以前的编码会阻止删除不再需要的段。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <varname>log_line_prefix</varname> 的 <literal>%i</literal> 转义，它可能在后端启动早期产生垃圾（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复启用归档时 <command>ALTER TABLE ... SET TABLESPACE</command> 中可能的数据损坏（Jeff Davis）
     </para>
    </listitem>

    <listitem>
     <para>
      允许 <command>CREATE DATABASE</command> 和 <command>ALTER DATABASE ... SET TABLESPACE</command> 被查询取消中断（Guillaume Lelarge）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <command>REASSIGN OWNED</command> 以处理操作符类和操作符族（Asko Tiidumaa）
     </para>
    </listitem>

    <listitem>
     <para>
      修复比较两个空 <type>tsquery</type> 值时可能的核心转储（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>LIKE</literal> 对包含 <literal>%</literal> 后跟 <literal>_</literal> 的模式的处理（Tom Lane）
     </para>

     <para>
      我们此前修复过它，但仍有一些被错误处理的情形。
     </para>
    </listitem>

    <listitem>
     <para>
      在 PL/Python 中，防御 <function>PyCObject_AsVoidPtr</function> 和 <function>PyCObject_FromVoidPtr</function> 的空指针结果（Peter Eisentraut）
     </para>
    </listitem>

    <listitem>
     <para>
      让 psql 在自动提交关闭模式下把 <command>DISCARD ALL</command> 识别为不应包裹在事务块中的命令（Itagaki Takahiro）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>ecpg</application> 以正确处理来自 <literal>RETURNING</literal> 子句的数据（Michael Meskes）
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <filename>contrib/dblink</filename> 对包含已删除列的表的处理（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/dblink</filename> 中<quote>重复连接名</quote>错误后的连接泄漏（Itagaki Takahiro）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/dblink</filename> 以正确处理超过 62 字节的连接名（Itagaki Takahiro）
     </para>
    </listitem>

    <listitem>
     <para>
      为 <filename>contrib/hstore</filename> 新增 <function>hstore(text, text)</function> 函数（Robert Haas）
     </para>

     <para>
      此函数是现已废弃的 <literal>=&gt;</literal> 操作符的推荐替代。向后移植它使得面向未来的代码可以用于较旧的服务器版本。注意该补丁只有在 <filename>contrib/hstore</filename> 于特定数据库中安装或重新安装之后才生效。用户也可以改为手工执行 <command>CREATE FUNCTION</command> 命令。
     </para>
    </listitem>

    <listitem>
     <para>
      更新构建基础设施和文档，反映源代码仓库从 CVS 迁移到 Git 的变化（Magnus Hagander 等）
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2010l
      for DST law changes in Egypt and Palestine; also historical corrections
      for Finland。
     </para>

     <para>
      此变更还为两个密克罗尼西亚时区新增了名称：Pacific/Chuuk 现在优先于 Pacific/Truk（首选缩写为 CHUT 而非 TRUT），Pacific/Pohnpei 优先于 Pacific/Ponape。
     </para>
    </listitem>

    <listitem>
     <para>
      让 Windows 的 <quote>N. Central Asia Standard Time</quote> 时区映射到 Asia/Novosibirsk 而不是 Asia/Almaty（Magnus Hagander）
     </para>

     <para>
      Microsoft 在 KB976098 的时区更新中改变了此区域的 DST 行为。Asia/Novosibirsk 更匹配其新行为。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-3-11">
  <title>版本 8.3.11</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2010-05-17</para>
  </formalpara>

  <para>
   本次发布包含对 8.3.10 的多方面修复。有关 8.3 大版本中新特性的信息，请参见<xref linkend="release-8-3">。.
  </para>

  <sect2>
   <title>迁移到版本 8.3.11</title>

   <para>
      对于运行 8.3.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.3.8 的版本升级，请参阅<xref linkend="release-8-3-8">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      用应用于整个解释器的操作码掩码来强制 <literal>plperl</literal> 中的限制，而不是使用 <filename>Safe.pm</filename>（Tim Bunce、Andrew Dunstan）
     </para>

     <para>
      近期的事态发展使我们确信 <filename>Safe.pm</filename> 太不安全，不能依靠它使 <literal>plperl</literal> 可信。此变更完全移除了对 <filename>Safe.pm</filename> 的使用，改为使用始终应用操作码掩码的独立解释器。此变更的良好副作用包括：现在可以在 <literal>plperl</literal> 中以自然的方式使用 Perl 的 <literal>strict</literal> 编译指令，Perl 的 <literal>$a</literal> 和 <literal>$b</literal> 变量在排序例程中按预期工作，以及函数编译显著加快。（CVE-2010-1169）
     </para>
    </listitem>

    <listitem>
     <para>
      防止 PL/Tcl 执行来自 <structname>pltcl_modules</structname> 的不可信代码（Tom）
     </para>

     <para>
      PL/Tcl 从数据库表自动装载 Tcl 代码的特性可能被利用于特洛伊木马攻击，因为对谁能创建该表或向其中插入没有限制。此变更禁用该特性，除非 <structname>pltcl_modules</structname> 由超级用户拥有。（但是，不检查该表上的权限，因此确实需要较不安全的模块表的安装仍可以向受信任的非超级用户授予适当权限。）此外，除非确实要执行 <literal>pltclu</literal> 函数，否则防止把代码装载到无限制的<quote>普通</quote>Tcl 解释器中。（CVE-2010-1170）
     </para>
    </listitem>

    <listitem>
     <para>
      修复在重建 relcache 条目期间收到缓存重置消息时可能的崩溃（Heikki）
     </para>

     <para>
      此错误是在 8.3.10 中修复相关失败时引入的。
     </para>
    </listitem>

    <listitem>
     <para>
      在为函数运行语言验证器时应用逐函数 GUC 设置（Itagaki Takahiro）
     </para>

     <para>
      这避免了函数代码在没有该设置时无效所导致的失败；例如 <varname>search_path</varname> 不正确时 SQL 函数可能无法解析。
     </para>
    </listitem>

    <listitem>
     <para>
      不允许非特权用户重置仅超级用户可用的参数设置（Alvaro）
     </para>

     <para>
      此前，如果非特权用户为自己运行 <literal>ALTER USER ... RESET ALL</literal>，或为他拥有的数据库运行 <literal>ALTER DATABASE ... RESET ALL</literal>，这将移除该用户或数据库的所有特殊参数设置，包括本应只可由超级用户更改的那些。现在，<command>ALTER</command> 只移除用户有权更改的参数。
     </para>
    </listitem>

    <listitem>
     <para>
      避免后端关闭发生在需要向日志条目添加 <literal>CONTEXT</literal> 内容时导致可能的崩溃（Tom）
     </para>

     <para>
      某些情况下上下文打印函数会失败，因为到打印日志消息时当前事务已被回滚。
     </para>
    </listitem>

    <listitem>
     <para>
      确保归档进程尽快响应 <varname>archive_command</varname> 的变更（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      为现代 Perl 版本更新 pl/perl 的 <filename>ppport.h</filename>（Andrew）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 pl/python 中的若干内存泄漏（Andreas Freund、Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      防止 <application>psql</application> 在展开引用自身的变量时无限递归（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>psql</application> 的 <literal>\copy</literal>，不在 <literal>\copy (select ...)</literal> 内的点周围添加空格（Tom）
     </para>

     <para>
      在数字字面量的小数点周围添加空格会导致语法错误。
     </para>
    </listitem>

    <listitem>
     <para>
      修复使用 <filename>contrib/intarray</filename> 操作符的不可满足查询产生不必要的 <quote>GIN 索引不支持整索引扫描</quote>错误（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      确保 <filename>contrib/pgstattuple</filename> 函数及时响应取消中断（Tatsuhito Kasahara）
     </para>
    </listitem>

    <listitem>
     <para>
      让服务器启动正确处理 <function>shmget()</function> 对已存在的共享内存段返回 <literal>EINVAL</literal> 的情形（Tom）
     </para>

     <para>
      此行为曾在包括 OS X 在内的 BSD 衍生内核上观察到。它导致完全误导性的启动失败，抱怨共享内存请求大小过大。
     </para>
    </listitem>

    <listitem>
     <para>
      避免 Windows 上 syslogger 进程可能的崩溃（Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      更健壮地处理 Windows 注册表中不完整的时区信息（Magnus）
     </para>
    </listitem>

    <listitem>
     <para>
      更新已知 Windows 时区名称集合（Magnus）
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2010j
      for DST law changes in Argentina, Australian Antarctic, Bangladesh,
      Mexico, Morocco, Pakistan, Palestine, Russia, Syria, Tunisia;
      also historical corrections for Taiwan。
     </para>

     <para>
      另外，把 <literal>PKST</literal>（巴基斯坦夏令时）加入默认时区缩写集合。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-3-10">
  <title>版本 8.3.10</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2010-03-15</para>
  </formalpara>

  <para>
   本次发布包含对 8.3.9 的多方面修复。有关 8.3 大版本中新特性的信息，请参见<xref linkend="release-8-3">。.
  </para>

  <sect2>
   <title>迁移到版本 8.3.10</title>

   <para>
      对于运行 8.3.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.3.8 的版本升级，请参阅<xref linkend="release-8-3-8">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      新增配置参数 <varname>ssl_renegotiation_limit</varname>，控制对 SSL 连接进行会话密钥重协商的频率（Magnus）
     </para>

     <para>
      可以把它设为零以完全禁用重协商，这在使用有缺陷的 SSL 库时可能是必需的。特别是，某些厂商发布的 CVE-2009-3555 临时补丁会导致重协商尝试失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复后端启动期间可能的死锁（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复因未干净地处理 relcache 重载期间的错误而可能的崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复因使用指向缓存计划的悬空指针而可能的崩溃（Tatsuo）
     </para>
    </listitem>

    <listitem>
     <para>
      修复试图从子事务启动失败中恢复时可能的崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复与使用保存点以及客户端编码不同于服务器编码相关的服务器内存泄漏（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 GIST 索引页分裂的恢复结束清理期间发出的不正确 WAL 数据（Yoichi Hirai）
     </para>

     <para>
      如果在完成未完成的 GIST 插入之后、在恢复结束清理期间崩溃，这会导致索引损坏，或者更可能在 WAL 重放期间出错。
     </para>
    </listitem>

    <listitem>
     <para>
      让 <type>bit</type> 类型的 <function>substring()</function> 把任何负长度都视为<quote>字符串的其余全部</quote>（Tom）
     </para>

     <para>
      以前的编码只对 -1 这样处理，对其他负值会产生无效的结果值，可能导致崩溃（CVE-2010-0442）。
     </para>
    </listitem>

    <listitem>
     <para>
      修复整数到位串的转换：当输出位宽比给定整数宽出非 8 位倍数时，正确处理第一个小数字节（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复若干病态缓慢的正则表达式匹配情形（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <type>xml</type> 处理中因草率内存管理导致的若干崩溃（Tom）
     </para>

     <para>
      这是最先应用于 8.4 的变更的向后移植。已知 8.3 代码有缺陷，但新代码差异足够大，在获得一些实际测试之前不想向后移植它。
     </para>
    </listitem>

    <listitem>
     <para>
      修复试图更新组合类型数组列中某元素的字段时的缺陷（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复备份历史文件中的 <literal>STOP WAL LOCATION</literal> 条目：当结束位置恰好位于段边界时报告下一个 WAL 段的名称（Itagaki Takahiro）
     </para>
    </listitem>

    <listitem>
     <para>
      修复更多临时文件泄漏的情形（Heikki）
     </para>

     <para>
      这纠正了上一个小版本中引入的问题。失败的一种情形是返回集合的 plpgsql 函数在另一个函数的异常处理器中被调用。
     </para>
    </listitem>

    <listitem>
     <para>
      改进布尔变量情形的约束排除处理，特别是使排除带有 <quote>bool_column = false</quote> 约束的分区成为可能（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      读取 <filename>pg_hba.conf</filename> 及相关文件时，如果 <literal>@</literal> 出现在引号内，不要把 <literal>@something</literal> 当作文件包含请求；另外，永远不要把单独的 <literal>@</literal> 当作文件包含请求（Tom）
     </para>

     <para>
      这防止了角色名或数据库名以 <literal>@</literal> 开头时的古怪行为。如果需要包含路径名包含空格的文件，仍然可以做到，但必须写 <literal>@"/path to/file"</literal> 而不是把引号放在整个构造之外。
     </para>
    </listitem>

    <listitem>
     <para>
      防止在某些平台上把目录命名为 <filename>pg_hba.conf</filename> 及相关文件中的包含目标时出现无限循环（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>SSL_read</function> 或 <function>SSL_write</function> 失败而未设置 <varname>errno</varname> 时可能的无限循环（Tom）
     </para>

     <para>
      据报道这在某些 Windows 版本的 <application>openssl</application> 上可能发生。
     </para>
    </listitem>

    <listitem>
     <para>
      不允许在本地连接上使用 <acronym>GSSAPI</acronym> 认证，因为它需要主机名才能正确工作（Magnus）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <application>ecpg</application> 在连接消失时报告正确的 SQLSTATE（Michael）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>psql</application> 的 <literal>numericlocale</literal> 选项，使其不在 latex 和 troff 输出格式中格式化不应格式化的字符串（Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      当同时指定 <literal>ON_ERROR_STOP</literal> 和 <literal>--single-transaction</literal> 且隐含的 <command>COMMIT</command> 期间发生错误时，让 <application>psql</application> 返回正确的退出状态（3）（Bruce）
     </para>
    </listitem>

    <listitem>
     <para>
      修复组合列被设为 NULL 的一种情形下 plpgsql 的失败（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复从 PL/PerlU 调用 PL/Perl 函数或相反时可能的失败（Tim Bunce）
     </para>
    </listitem>

    <listitem>
     <para>
      在 PL/Python 中添加 <literal>volatile</literal> 标记，以避免可能的编译器特定错误行为（Zdenek Kotala）
     </para>
    </listitem>

    <listitem>
     <para>
      确保 PL/Tcl 完全初始化 Tcl 解释器（Tom）
     </para>

     <para>
      此疏漏唯一已知的症状是使用 Tcl 8.5 或更高版本时 Tcl 的 <literal>clock</literal> 命令行为异常。
     </para>
    </listitem>

    <listitem>
     <para>
      防止在向 <function>dblink_build_sql_*</function> 函数指定过多键列时 <filename>contrib/dblink</filename> 崩溃（Rushabh Lathia、Joe Conway）
     </para>
    </listitem>

    <listitem>
     <para>
      允许 <filename>contrib/ltree</filename> 操作中使用零维数组（Tom）
     </para>

     <para>
      这种情形此前被作为错误拒绝，但把它与零元素数组同样处理更方便。特别是，这避免了当 <type>ltree</type> 操作应用于 <literal>ARRAY(SELECT ...)</literal> 的结果而子查询不返回行时不必要的失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/xml2</filename> 中因草率内存管理导致的若干崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <filename>contrib/xml2</filename> 在 Windows 上的构建更健壮（Andrew）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 Windows 信号处理中的竞态条件（Radu Ilie）
     </para>

     <para>
      此缺陷的一个已知症状是 <structname>pg_listener</structname> 中的行可能在重负载下被丢弃。
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2010e
      for DST law changes in Bangladesh, Chile, Fiji, Mexico, Paraguay, Samoa。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-3-9">
  <title>版本 8.3.9</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2009-12-14</para>
  </formalpara>

  <para>
   本次发布包含对 8.3.8 的多方面修复。有关 8.3 大版本中新特性的信息，请参见<xref linkend="release-8-3">。.
  </para>

  <sect2>
   <title>迁移到版本 8.3.9</title>

   <para>
      对于运行 8.3.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.3.8 的版本升级，请参阅<xref linkend="release-8-3-8">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      防范索引函数更改会话局部状态导致的间接安全威胁（Gurjeet Singh、Tom）
     </para>

     <para>
      此变更防止据称不可变的索引函数可能破坏超级用户的会话（CVE-2009-4136）。
     </para>
    </listitem>

    <listitem>
     <para>
      拒绝在公共名（CN）字段中包含嵌入空字节的 SSL 证书（Magnus）
     </para>

     <para>
      这防止在 SSL 验证期间证书与服务器或客户端名称的非预期匹配（CVE-2009-4034）。
     </para>
    </listitem>

    <listitem>
     <para>
      修复后端启动时缓存初始化期间可能的崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      避免在空同义词词典上崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      防止信号在不安全的时刻中断 <literal>VACUUM</literal>（Alvaro）
     </para>

     <para>
      此修复防止了 <literal>VACUUM FULL</literal> 在已提交其元组移动后被取消时的 PANIC，以及普通 <literal>VACUUM</literal> 在截断表之后被中断时的瞬态错误。
     </para>
    </listitem>

    <listitem>
     <para>
      修复哈希表大小计算中因整数溢出而可能的崩溃（Tom）
     </para>

     <para>
      这可能发生在规划器对哈希连接结果大小的估计极大时。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <type>inet</type>/<type>cidr</type> 比较中非常罕见的崩溃（Chris Mikkelson）
     </para>
    </listitem>

    <listitem>
     <para>
      确保预备事务所持有的共享元组级锁不被忽略（Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      修复在子事务内访问的游标所用临时文件被过早删除的问题（Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      修复轮转到新 CSV 日志文件时 syslogger 进程的内存泄漏（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 Windows 权限降级逻辑（Jesse Morris）
     </para>

     <para>
      这修复了数据库在 Windows 上启动失败的一些情形，这些情形常伴随误导性错误消息，例如<quote>找不到匹配的 postgres 可执行文件</quote>。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 GiST 索引页分裂的不正确逻辑：当分裂依赖于索引的非首列时（Paul Ramsey）
     </para>
    </listitem>

    <listitem>
     <para>
      如果回收或删除旧 WAL 文件在检查点结束时失败，不要报错退出（Heikki）
     </para>

     <para>
      更好的做法是把该问题视为非致命并允许检查点完成。未来的检查点会重试删除。这类问题在正常运行中不应出现，但已知可能由设计不当的 Windows 反病毒和备份软件引起。
     </para>
    </listitem>

    <listitem>
     <para>
      确保 WAL 文件不会在 Windows 上被重复归档（Heikki）
     </para>

     <para>
      这是其他进程干扰删除不再需要的文件时可能出现的另一种症状。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PAM 口令处理使其更健壮（Tom）
     </para>

     <para>
      已知以前的代码在 Linux <literal>pam_krb5</literal> PAM 模块与 Microsoft Active Directory 域控制器组合下失败。它在其他地方可能也有问题，因为它对 PAM 栈会传递给它什么参数做了无根据的假设。
     </para>
    </listitem>

    <listitem>
     <para>
      提高 GSSAPI 和 SSPI 认证方法的最大认证令牌（Kerberos 票据）大小（Ian Turner）
     </para>

     <para>
      虽然旧的 2000 字节限制对 Unix Kerberos 实现绰绰有余，Windows 域控制器签发的票据可能大得多。
     </para>
    </listitem>

    <listitem>
     <para>
      重新启用对序列的访问统计信息收集（Akira Kurosawa）
     </para>

     <para>
      这以前可用，但在 8.3 中损坏了。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>CREATE OR REPLACE FUNCTION</literal> 期间所有权依赖的处理（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复对 <literal>WHERE</literal> <replaceable>x</replaceable>=<replaceable>x</replaceable> 条件的不正确处理（Tom）
     </para>

     <para>
      某些情况下它们可能被当作冗余而忽略，但它们并非冗余——它们等价于 <replaceable>x</replaceable> <literal>IS NOT NULL</literal>。
     </para>
    </listitem>

    <listitem>
     <para>
      让文本搜索解析器接受 XML 属性中的下划线（Peter）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <type>xml</type> 二进制输入的编码处理（Heikki）
     </para>

     <para>
      如果 XML 头未指定编码，现在默认采用 UTF-8；以前的处理不一致。
     </para>
    </listitem>

    <listitem>
     <para>
      修复从 <literal>plperl</literal> 调用 <literal>plperlu</literal> 或相反时的缺陷（Tom）
     </para>

     <para>
      内层函数的错误退出可能因未能为外层函数重新选择正确的 Perl 解释器而导致崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      修复重定义 PL/Perl 函数时的会话期内存泄漏（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      确保 Perl 数组在由返回集合的 PL/Perl 函数返回时被正确转换为 <productname>PostgreSQL</productname> 数组（Andrew Dunstan、Abhijit Menon-Sen）
     </para>

     <para>
      对非返回集合的函数这早已正确工作。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PL/Python 异常处理中的罕见崩溃（Peter）
     </para>
    </listitem>

    <listitem>
     <para>
      在 <filename>contrib/pg_standby</filename> 中，禁用在 Windows 上用信号触发故障转移（Fujii Masao）
     </para>

     <para>
      这从未有过任何用处，因为 Windows 没有 Unix 式信号，但最近的变更使它实际上崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      确保 <application>psql</application> 的 flex 模块用正确的系统头文件定义编译（Tom）
     </para>

     <para>
      这修复了在 <literal>--enable-largefile</literal> 导致生成代码不兼容变更的平台上的构建失败。
     </para>
    </listitem>

    <listitem>
     <para>
      让 postmaster 忽略连接请求包中的任何 <literal>application_name</literal> 参数，以改进与未来 libpq 版本的兼容性（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      更新时区缩写文件以符合当前现实（Joachim Wieland）
     </para>

     <para>
      这包括把 <literal>IDT</literal> 和 <literal>SGT</literal> 加入默认时区缩写集合。
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2009s
      for DST law changes in Antarctica, Argentina, Bangladesh, Fiji,
      Novokuznetsk, Pakistan, Palestine, Samoa, Syria; also historical
      corrections for Hong Kong。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-3-8">
  <title>版本 8.3.8</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2009-09-09</para>
  </formalpara>

  <para>
   本次发布包含对 8.3.7 的多方面修复。有关 8.3 大版本中新特性的信息，请参见<xref linkend="release-8-3">。.
  </para>

  <sect2>
   <title>迁移到版本 8.3.8</title>

   <para>
      对于运行 8.3.X 的用户，不需要进行转储/恢复。不过，如果你在 <type>interval</type> 列上有任何哈希索引，必须在更新到 8.3.8 后对它们执行 <command>REINDEX</command>。另外，如果你是从早于 8.3.5 的版本升级，请参阅<xref linkend="release-8-3-5">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      修复 Windows 共享内存分配代码（Tsutomu Yamada、Magnus）
     </para>

     <para>
      此缺陷导致常被报告的<quote>无法重新附加到共享内存</quote>错误消息。
     </para>
    </listitem>

    <listitem>
     <para>
      在 <function>pg_start_backup()</function> 期间强制 WAL 段切换（Heikki）
     </para>

     <para>
      这避免了可能使基础备份不可用的极端情形。
     </para>
    </listitem>

    <listitem>
     <para>
      不允许在安全定义者函数内使用 <command>RESET ROLE</command> 和 <command>RESET SESSION AUTHORIZATION</command>（Tom、Heikki）
     </para>

     <para>
      这覆盖了先前禁止在安全定义者函数内使用 <command>SET ROLE</command> 和 <command>SET SESSION AUTHORIZATION</command> 的补丁中遗漏的一种情形。（参见 CVE-2007-6600）
     </para>
    </listitem>

    <listitem>
     <para>
      把对已装载的可装载模块的 <command>LOAD</command> 变为无操作（Tom）
     </para>

     <para>
      此前 <command>LOAD</command> 会试图卸载并重新装载模块，但这不安全也不太有用。
     </para>
    </listitem>

    <listitem>
     <para>
      不允许在 LDAP 认证期间使用空口令（Magnus）
     </para>
    </listitem>

    <listitem>
     <para>
      修复出现在外层聚合函数参数中的子 SELECT 的处理（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复与从 Sort 或 Materialize 计划节点的输出获取整行值相关的缺陷（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      防止 <varname>synchronize_seqscans</varname> 改变可滚动游标和 <literal>WITH HOLD</literal> 游标的结果（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      回退规划器中当 AND 或 OR 列表中的子句超过 100 个时禁用部分索引和约束排除优化的变更（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <type>interval</type> 数据类型的哈希计算（Tom）
     </para>

     <para>
      这纠正了 interval 值上哈希连接的错误结果。它还更改了 interval 列上哈希索引的内容。如果你有任何这样的索引，必须在更新后对它们执行 <command>REINDEX</command>。
     </para>
    </listitem>

    <listitem>
     <para>
      把 <function>to_char(..., 'TH')</function> 在 <literal>'HH'</literal>/<literal>'HH12'</literal> 下当作大写序数后缀（Heikki）
     </para>

     <para>
      它以前被当作 <literal>'th'</literal>（小写）处理。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <replaceable>x</replaceable> 超过 200 万且使用整数日期时间时 <literal>INTERVAL '<replaceable>x</replaceable> ms'</literal> 的溢出（Alex Hunsaker）
     </para>
    </listitem>

    <listitem>
     <para>
      修复点与线段之间距离的计算（Tom）
     </para>

     <para>
      这导致多个几何操作符的结果不正确。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <type>money</type> 数据类型，使其在货币金额没有小数位的区域设置（例如日本）中工作（Itagaki Takahiro）
     </para>
    </listitem>

    <listitem>
     <para>
      修复模式包含 <literal>%_</literal> 时 <literal>LIKE</literal> 的行为（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      正确地对像 <literal>00:12:57.9999999999999999999999999999</literal> 这样的日期时间输入四舍五入（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 XML 操作中的内存泄漏（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 GiST R-tree 操作符类中页分裂点选择不佳的问题（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      确保<quote>快速关闭</quote>请求会强制终止打开的会话，即使<quote>智能关闭</quote>已在进行中（Fujii Masao）
     </para>
    </listitem>

    <listitem>
     <para>
      避免当输入值（几乎）按排序顺序时批量插入 GIN 索引的性能退化（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      在 PL/pgSQL 的某些上下文中正确强制执行 NOT NULL 域约束（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 plperl 初始化中的可移植性问题（Andrew Dunstan）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_ctl</application>，使其在 <filename>postgresql.conf</filename> 为空时不进入无限循环（Jeff Davis）
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <application>pg_dump</application> 在有许多大对象时的效率（Tamas Vincze）
     </para>
    </listitem>

    <listitem>
     <para>
      用 <literal>SIGUSR1</literal> 而不是 <literal>SIGQUIT</literal> 作为 <application>pg_standby</application> 的故障转移信号（Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <application>pg_standby</application> 的 <literal>maxretries</literal> 选项按文档描述的那样工作（Fujii Masao）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <filename>contrib/hstore</filename> 在键或值太长而无法放入其数据结构时抛出错误，而不是静默截断（Andrew Gierth）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/xml2</filename> 的 <function>xslt_process()</function>，使其正确处理最大参数数量（二十个）（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <application>libpq</application> 从 <command>COPY FROM STDIN</command> 期间错误中恢复的代码的健壮性（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      避免在两个库都安装时包含相互冲突的 readline 和 editline 头文件（Zdenek Kotala）
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2009l
      for DST law changes in Bangladesh, Egypt, Jordan, Pakistan,
      Argentina/San_Luis, Cuba, Jordan (historical correction only),
      Mauritius, Morocco, Palestine, Syria, Tunisia。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-3-7">
  <title>版本 8.3.7</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2009-03-16</para>
  </formalpara>

  <para>
   本次发布包含对 8.3.6 的多方面修复。有关 8.3 大版本中新特性的信息，请参见<xref linkend="release-8-3">。.
  </para>

  <sect2>
   <title>迁移到版本 8.3.7</title>

   <para>
      对于运行 8.3.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.3.5 的版本升级，请参阅<xref linkend="release-8-3-5">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      防止编码转换失败时的错误递归崩溃（Tom）
     </para>

     <para>
      此变更扩展了上两个小版本中针对相关失败情形所做的修复。以前的修复是针对最初的问题报告量身定制的，但我们现在认识到，编码转换函数抛出的<emphasis>任何</emphasis>错误都可能在试图报告该错误时导致无限递归。因此，解决方案是：如果发现已处于递归错误报告情形，就禁用翻译和编码转换并报告错误消息的纯 ASCII 形式。（CVE-2009-0922）
     </para>
    </listitem>

    <listitem>
     <para>
      不允许 <command>CREATE CONVERSION</command> 为指定的转换函数使用错误的编码（Heikki）
     </para>

     <para>
      这防止了编码转换失败的一种可能情形。前一个变更是兜底，防范同一领域其他类型的失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>xpath()</function>，使其仅在必要时修改路径表达式，并在必要时做更合理的尝试（Andrew）
     </para>

     <para>
      SQL 标准暗示 <function>xpath</function> 应能处理文档片段形式的数据，但 <application>libxml</application> 不支持这一点，而且按照 XPath 标准这是否合理也确实不清楚。<function>xpath</function> 试图通过同时修改数据和路径表达式来绕过这一不匹配，但该修改有缺陷，可能导致有效搜索失败。现在 <function>xpath</function> 检查数据是否确实是格式良好的文档，如果是，则不加更改地把数据和路径表达式交给 <application>libxml</application>。否则，使用一种较不容易失败的另一种修改方法。
     </para>

     <note>
      <para>
      新的修改方法仍不能 100% 令人满意，而且似乎不可能有真正的解决方案。因此应把这个补丁视为权宜之计，以避免不必要地破坏现有应用程序。<productname>PostgreSQL</productname> 8.4 很可能干脆拒绝在非格式良好文档的数据上使用 <function>xpath</function>。
     </para>
     </note>
    </listitem>

    <listitem>
     <para>
      修复 <function>to_char()</function> 被给定与数据参数类型不适当的格式代码时的核心转储（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 C 区域设置与多字节编码一起使用时文本搜索可能失败的问题（Teodor）
     </para>

     <para>
      在 <type>wchar_t</type> 比 <type>int</type> 窄的平台（特别是 Windows）上可能崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      修复文本搜索解析器处理包含多个 <literal>@</literal> 字符的类电子邮件字符串时的极端低效（Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      修复较大子查询的输出列表中带子 <command>SELECT</command> 的规划器问题（Tom）
     </para>

     <para>
      此缺陷的已知症状是<quote>定位分组列失败</quote>错误，且与所涉及的数据类型有关；但也可能存在其他问题。
     </para>
    </listitem>

    <listitem>
     <para>
      修复带隐式强制转换的 <literal>CASE WHEN</literal> 的反编译（Tom）
     </para>

     <para>
      在试图检查或转储视图时，这一错误在启用断言的构建中可能导致断言失败，在其他情况下可能导致<quote>意外的 CASE WHEN 子句</quote>错误消息。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 TOAST 表行类型所有者可能的错误分配（Tom）
     </para>

     <para>
      如果 <command>CLUSTER</command> 或重写形式的 <command>ALTER TABLE</command> 由表所有者之外的人执行，表的 TOAST 表的 <structname>pg_type</structname> 条目最终会被标记为那个人所拥有。这不会立即引起问题，因为普通数据库操作不检查 TOAST 行类型的权限。但是，如果后来试图删除发出该命令的角色（在 8.1 或 8.2 中），或者在那之后 <application>pg_dump</application> 发出<quote>数据类型的所有者似乎无效</quote>警告（在 8.3 中），则可能导致意外失败。
     </para>
    </listitem>

    <listitem>
     <para>
      更改 <command>UNLISTEN</command>，如果当前会话从未执行过任何 <command>LISTEN</command> 命令则快速退出（Tom）
     </para>

     <para>
      大多数时候这不是特别有用的优化，但由于 <command>DISCARD ALL</command> 会调用 <command>UNLISTEN</command>，以前的编码对大量使用 <command>DISCARD ALL</command> 的应用程序造成了显著的性能问题。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PL/pgSQL：不再把字符串中任何位置的 <command>INSERT</command> 之后的 <literal>INTO</literal> 当作 INTO-变量子句，而不仅是开头；特别是不要对 <command>CREATE RULE</command> 内的 <command>INSERT INTO</command> 失败（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      在块退出时完全清理 PL/pgSQL 错误状态变量（Ashesh Vashi 和 Dave Page）
     </para>

     <para>
      这对 PL/pgSQL 本身不是问题，但这一疏漏可能导致 PL/pgSQL 调试器在检查函数状态时崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      在 Windows 上重试失败的 <function>CallNamedPipe()</function> 调用（Steve Marshall、Magnus）
     </para>

     <para>
      该函数有时似乎会瞬时失败；我们此前把任何失败都当作硬错误，这可能扰乱 <command>LISTEN</command>/<command>NOTIFY</command> 以及其他操作。
     </para>
    </listitem>

    <listitem>
     <para>
      把 <literal>MUST</literal>（毛里求斯岛夏令时）加入已知时区缩写的默认列表（Xavier Bugaud）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-3-6">
  <title>版本 8.3.6</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2009-02-02</para>
  </formalpara>

  <para>
   本次发布包含对 8.3.5 的多方面修复。有关 8.3 大版本中新特性的信息，请参见<xref linkend="release-8-3">。.
  </para>

  <sect2>
   <title>迁移到版本 8.3.6</title>

   <para>
      对于运行 8.3.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.3.5 的版本升级，请参阅<xref linkend="release-8-3-5">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      让 <command>DISCARD ALL</command> 除了释放已有的一切外还释放咨询锁（Tom）
     </para>

     <para>
      这被认为是最合适的行为。但这可能影响现有应用程序。
     </para>
    </listitem>

    <listitem>
     <para>
      修复整索引 GiST 扫描使其正确工作（Teodor）
     </para>

     <para>
      如果表在 GiST 索引上聚簇，此错误可能导致行丢失。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>xmlconcat(NULL)</literal> 的崩溃（Peter）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>ispell</literal> 词典在高位字符被用作标志时可能的崩溃（Teodor）
     </para>

     <para>
      已知一部广泛可用的挪威语词典会这样做，其他词典可能存在同样的情况。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_dump</application> 对组合类型输出的错误排序（Tom）
     </para>

     <para>
      最可能的问题是用户定义的操作符类在需要它们的索引或视图之后才被转储。
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <function>headline()</function> 函数中对 URL 的处理（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <function>headline()</function> 函数对超长标题的处理（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      防止在为指定的编码对创建了错误转换函数的编码转换时可能的断言失败或错误转换（Tom、Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PL/pgSQL 中执行的语句被重写为另一种语句时可能的断言失败，例如 <command>INSERT</command> 被重写为 <command>UPDATE</command>（Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      确保数据类型输入函数有可用快照（Tom）
     </para>

     <para>
      这主要影响声明了涉及用户定义稳定或不可变函数的 <literal>CHECK</literal> 约束的域。如果没有设置快照，这类函数通常会失败。
     </para>
    </listitem>

    <listitem>
     <para>
      让使用 SPI 的函数更安全地用于数据类型 I/O 中；特别是用于域检查约束（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      避免 <command>VACUUM</command> 中对小表的不必要锁定（Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      修复有时使 <command>ALTER TABLE ENABLE/DISABLE RULE</command> 无法被活动会话识别的问题（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复使 <literal>UPDATE RETURNING tableoid</literal> 返回零而不是正确 OID 的问题（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      允许声明为接受 <type>ANYARRAY</type> 的函数用于该类型的 <structname>pg_statistic</structname> 列（Tom）
     </para>

     <para>
      这以前可用，但在 8.3 中被无意破坏。
     </para>
    </listitem>

    <listitem>
     <para>
      修复传递等式应用于外连接子句时规划器对选择率的错误估计（Tom）
     </para>

     <para>
      这可能导致诸如 <literal>... from a left join b on a.a1 = b.b1 where a.a1 = 42 ...</literal> 之类的查询得到糟糕的计划。
     </para>
    </listitem>

    <listitem>
     <para>
      改进优化器对长 <literal>IN</literal> 列表的处理（Tom）
     </para>

     <para>
      此变更避免了在启用约束排除时在这类列表上浪费大量时间。
     </para>
    </listitem>

    <listitem>
     <para>
      防止 GIN 索引构建期间的同步扫描（Tom）
     </para>

     <para>
      因为 GIN 针对按 TID 递增顺序插入元组做了优化，选择使用同步扫描可能使构建减慢三倍或更多。
     </para>
    </listitem>

    <listitem>
     <para>
      确保可保持游标的内容不依赖于 TOAST 表的内容（Tom）
     </para>

     <para>
      此前，游标结果中的大字段值可能被表示为 TOAST 指针，如果被引用的表在游标被读取之前被删除，或者大值被删除随后被清理掉，这就会失败。普通游标不会发生这种情况，但对于保持到创建事务之后的游标则可能。
     </para>
    </listitem>

    <listitem>
     <para>
      修复返回集合的函数在未读完整个结果就被终止时的内存泄漏（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复数据库编码不是 UTF-8 时 XML 函数中的编码转换问题（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/dblink</filename> 的 <function>dblink_get_result(text,bool)</function> 函数（Joe）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/sslinfo</filename> 函数可能的垃圾输出（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/tsearch2</filename> 兼容触发器在一条命令中被触发多次时的错误行为（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 autovacuum 中可能的错误信号（Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      支持在 Windows 7 beta 上作为服务运行（Dave 和 Magnus）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>ecpg</application> 对 varchar 结构的处理（Michael）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>configure</application> 脚本，使其在无法获得 PL/Perl 的链接信息时正确报告失败（Andrew）
     </para>
    </listitem>

    <listitem>
     <para>
      让所有文档在适当之处引用 <literal>pgsql-bugs</literal> 和/或 <literal>pgsql-hackers</literal>，而不是现已停用的 <literal>pgsql-ports</literal> 和 <literal>pgsql-patches</literal> 邮件列表（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      更新时区数据文件到 <application>tzdata</application> 发行版 2009a（针对加德满都以及瑞士、古巴的历史 DST 修正）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-3-5">
  <title>版本 8.3.5</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2008-11-03</para>
  </formalpara>

  <para>
   本次发布包含对 8.3.4 的多方面修复。有关 8.3 大版本中新特性的信息，请参见<xref linkend="release-8-3">。.
  </para>

  <sect2>
   <title>迁移到版本 8.3.5</title>

   <para>
      对于运行 8.3.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.3.1 的版本升级，请参阅<xref linkend="release-8-3-1">。另外，如果你运行的是之前的 8.3.X 版本，建议在升级后对所有 GiST 索引执行 <command>REINDEX</command>。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      修复 GiST 索引因删除后把错误的索引条目标记为<quote>死</quote>而损坏的问题（Teodor）
     </para>

     <para>
      这会导致索引搜索找不到本应找到的行。损坏的索引可以用 <command>REINDEX</command> 修复。
     </para>
    </listitem>

    <listitem>
     <para>
      修复客户端编码无法表示本地化错误消息时的后端崩溃（Tom）
     </para>

     <para>
      我们此前处理过类似问题，但如果<quote>字符没有等价物</quote>消息本身无法转换，仍会失败。修复方法是在检测到这种情形时禁用本地化并发送纯 ASCII 错误消息。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <type>bytea</type> 到 XML 映射中可能的崩溃（Michael McMaster）
     </para>
    </listitem>

    <listitem>
     <para>
      修复从触发器调用深层嵌套函数时可能的崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <replaceable>expression</replaceable> <literal>IN</literal> (<replaceable>expression-list</replaceable>) 查询的优化（Tom，想法来自 Robert Haas）
     </para>

     <para>
      右侧有查询变量的情形在 8.2.x 和 8.3.x 中的处理效率低于以前的版本。此修复为此类情形恢复了 8.1 的行为。
     </para>
    </listitem>

    <listitem>
     <para>
      修复当子 <literal>SELECT</literal> 出现在 <literal>FROM</literal> 中的函数调用、多行 <literal>VALUES</literal> 列表或 <literal>RETURNING</literal> 列表中时规则查询的错误展开（Tom）
     </para>

     <para>
      此问题通常的症状是<quote>无法识别的节点类型</quote>错误。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 GiST 索引 <literal>IS NULL</literal> 搜索重扫描期间的断言失败（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      修复哈希聚合计划重扫描期间的内存泄漏（Neil）
     </para>
    </listitem>

    <listitem>
     <para>
      确保新定义的 PL/pgSQL 触发器函数被当作普通函数调用时报错（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      在 <command>CREATE DATABASE</command> 开始复制文件之前强制检查点（Heikki）
     </para>

     <para>
      这防止了源数据库中文件最近被删除时可能的失败。
     </para>
    </listitem>

    <listitem>
     <para>
      防止用 <command>ALTER SET TABLESPACE</command> 把表移到另一个表空间时 <structfield>relfilenode</structfield> 号可能的冲突（Heikki）
     </para>

     <para>
      该命令试图重用现有文件名，而不是选取目标目录中已知未使用的文件名。
     </para>
    </listitem>

    <listitem>
     <para>
      修复单个查询项匹配文本首词时文本搜索标题生成不正确的问题（Sushant Sinha）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <option>--enable-integer-datetimes</option> 构建中使用非 ISO datestyle 时 interval 值小数秒显示不当的问题（Ron Mayer）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <literal>ILIKE</literal> 即使在字符被转义时也不区分大小写地比较（Andrew）
     </para>
    </listitem>

    <listitem>
     <para>
      确保语句日志正确处理 <command>DISCARD</command>（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PITR 恢复期间最后完成事务时间的错误日志记录（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      确保 <function>SPI_getvalue</function> 和 <function>SPI_getbinval</function> 在传入的元组和元组描述符列数不同时行为正确（Tom）
     </para>

     <para>
      当表添加或删除过列时这种情况是正常的，但这两个函数没有正确处理它。唯一可能的后果是错误的错误指示。
     </para>
    </listitem>

    <listitem>
     <para>
      把 <varname>SessionReplicationRole</varname> 标记为 <literal>PGDLLIMPORT</literal>，使其能在 Windows 上被 <application>Slony</application> 使用（Magnus）
     </para>
    </listitem>

    <listitem>
     <para>
      修复使用 <application>libpq</application> 的 <literal>gsslib</literal> 参数时的小内存泄漏（Magnus）
     </para>

     <para>
      参数字符串占用的空间在连接关闭时未被释放。
     </para>
    </listitem>

    <listitem>
     <para>
      确保在需要时把 <application>libgssapi</application> 链接到 <application>libpq</application>（Markus Schaaf）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>ecpg</application> 对 <command>CREATE ROLE</command> 的解析（Michael）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>pg_ctl restart</literal> 最近损坏的问题（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      确保 <filename>pg_control</filename> 以二进制模式打开（Itagaki Takahiro）
     </para>

     <para>
      <application>pg_controldata</application> 和 <application>pg_resetxlog</application> 此前做得不正确，因此在 Windows 上可能失败。
     </para>
    </listitem>

    <listitem>
     <para>
      更新时区数据文件到 <application>tzdata</application> 发行版 2008i（针对阿根廷、巴西、毛里求斯、叙利亚的 DST 法规变更）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-3-4">
  <title>版本 8.3.4</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2008-09-22</para>
  </formalpara>

  <para>
   本次发布包含对 8.3.3 的多方面修复。有关 8.3 大版本中新特性的信息，请参见<xref linkend="release-8-3">。.
  </para>

  <sect2>
   <title>迁移到版本 8.3.4</title>

   <para>
      对于运行 8.3.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.3.1 的版本升级，请参阅<xref linkend="release-8-3-1">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      修复 btree WAL 恢复代码中的缺陷（Heikki）
     </para>

     <para>
      如果 WAL 在页分裂操作中途结束，恢复就会失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 HOT 页修剪可能使用错误截断 XID 的问题（Alvaro）
     </para>

     <para>
      此错误在 <command>VACUUM</command> 会查询的系统目录中造成损坏风险：死元组版本可能被过早移除。这对实际数据库操作的影响很小，因为系统在检查目录时不遵循 MVCC 规则，但可能导致 <application>pg_dump</application> 或其他客户端程序的瞬时错误输出。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <structfield>datfrozenxid</structfield> 可能的错误计算（Alvaro）
     </para>

     <para>
      此错误或许能解释最近一些无法删除旧 <structname>pg_clog</structname> 数据的报告。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <structname>pg_class</structname> 被重新索引后的错误 HOT 更新（Tom）
     </para>

     <para>
      如果 <literal>REINDEX TABLE pg_class</literal> 之后在同一会话中执行 <literal>ALTER TABLE RENAME</literal> 或 <literal>ALTER TABLE SET SCHEMA</literal> 命令，<structname>pg_class</structname> 可能损坏。
     </para>
    </listitem>

    <listitem>
     <para>
      修复遗漏的<quote>组合 cid</quote>情形（Karl Schnaitter）
     </para>

     <para>
      此错误使行对某个事务错误地不可见，而这些行在该事务中曾被多个全部中止的子事务删除过。
     </para>
    </listitem>

    <listitem>
     <para>
      防止 autovacuum 在它正在检查的表恰好在错误时刻被删除时崩溃（Alvaro）
     </para>
    </listitem>

    <listitem>
     <para>
      把本地锁计数器从 32 位拓宽到 64 位（Tom）
     </para>

     <para>
      这是针对计数器在足够长的事务中可能溢出的报告，该溢出会导致意外的<quote>锁已被持有</quote>错误。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 GiST 索引扫描期间可能的元组重复输出（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      当任一表被修改时从头重新生成外键检查查询（Tom）
     </para>

     <para>
      此前 8.3 会试图重新规划查询，但基于以前生成的查询文本。如果表或列被重命名，这就会失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复视图包含简单 <literal>UNION ALL</literal> 构造时遗漏的权限检查（Heikki）
     </para>

     <para>
      被引用表的权限被正确检查，但视图本身的权限没有。
     </para>
    </listitem>

    <listitem>
     <para>
      在执行器启动时添加检查，确保 <command>INSERT</command> 或 <command>UPDATE</command> 产生的元组与目标表的当前行类型匹配（Tom）
     </para>

     <para>
      据信这种情况在 8.3 中不可能发生，但在以前的版本中可能，因此检查似乎是明智的。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <command>DROP OWNED</command> 期间可能的重复删除（Tom）
     </para>

     <para>
      这通常会导致奇怪的错误，例如<quote>关系 NNN 的缓存查找失败</quote>。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 XML 操作中的多个内存泄漏（Kris Jurka、Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>xmlserialize()</function>，使其对不可接受的目标数据类型正确抛出错误（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复几处在文本搜索配置文件解析中错误处理多字节字符的地方（Tom）
     </para>

     <para>
      配置文件中出现的某些字符总是会导致<quote>编码的字节序列无效</quote>失败。
     </para>
    </listitem>

    <listitem>
     <para>
      为文本搜索配置文件中报告的所有错误提供文件名和行号位置（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>AT TIME ZONE</literal>：先尝试把时区参数解释为时区缩写，仅在失败时才尝试完整时区名，而不是像以前那样相反（Tom）
     </para>

     <para>
      时间戳输入函数一直按此顺序解析有歧义的时区名。让 <literal>AT TIME ZONE</literal> 也这样做改进了一致性，并修复了 8.1 中引入的兼容性缺陷：在有歧义的情形下，我们现在的行为与 8.0 及更早版本相同，因为在旧版本中 <literal>AT TIME ZONE</literal> <emphasis>只</emphasis>接受缩写。
     </para>
    </listitem>

    <listitem>
     <para>
      修复日期时间输入函数在 64 位平台上正确检测整数溢出（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      防止显示带单位的配置参数时单位转换期间的整数溢出（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      改进向 syslog 写很长日志消息的性能（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      允许 <filename>pg_hba.conf</filename> 中 LDAP URL 的后缀部分包含空格（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>SELECT DISTINCT ON</literal> 查询上游标反向扫描的缺陷（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复可能把 <literal>IS NULL</literal> 测试不当下推到外连接之下的规划器缺陷（Tom）
     </para>

     <para>
      这由上层 <literal>OR</literal> 子句的所有分支中对同一关系的 <literal>IS NULL</literal> 测试触发。
     </para>
    </listitem>

    <listitem>
     <para>
      修复嵌套子查询表达式的规划器缺陷（Tom）
     </para>

     <para>
      如果外层子查询不直接依赖父查询而内层依赖，外层的值可能不会为新的父查询行重新计算。
     </para>
    </listitem>

    <listitem>
     <para>
      修复规划器，使其估计产生布尔结果的 <literal>GROUP BY</literal> 表达式总是产生两个组，而不考虑表达式的内容（Tom）
     </para>

     <para>
      对于某些布尔测试（如 <replaceable>col</replaceable> <literal>IS NULL</literal>），这比常规的 <literal>GROUP BY</literal> 估计准确得多。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PL/pgSQL 在 <literal>FOR</literal> 循环的目标变量是包含组合类型字段的记录时不失败（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PL/Tcl 在 Tcl 8.5 下的正确行为，并更小心地处理发送到 Tcl 或从 Tcl 发出的数据的编码（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <function>PQescapeBytea()</function> 的性能（Rudolf Leitgeb）
     </para>
    </listitem>

    <listitem>
     <para>
      在 Windows 上，通过阻止 <application>libpq</application> 在每次系统调用中发送超过 64kB 来绕过 Microsoft 的一个缺陷（Magnus）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>ecpg</application> 在 <command>SET</command> 命令中正确处理变量（Michael）
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <application>pg_dump</application> 和 <application>pg_restore</application> 在发送 SQL 命令失败后的错误报告（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_ctl</application>，使其在 <literal>restart</literal> 期间正确保留 postmaster 命令行参数（Bruce）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_standby</application> 中 WAL 文件截断点计算的错误（Simon）
     </para>
    </listitem>

    <listitem>
     <para>
      更新时区数据文件到 <application>tzdata</application> 发行版 2008f（针对阿根廷、巴哈马、巴西、毛里求斯、摩洛哥、巴基斯坦、巴勒斯坦和巴拉圭的 DST 法规变更）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-3-3">
  <title>版本 8.3.3</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2008-06-12</para>
  </formalpara>

  <para>
      此发行版在 8.3.2 的基础上包含一个严重和一个次要的修复。有关 8.3 主版本新特性的信息，请参阅<xref linkend="release-8-3">。
     </para>

  <sect2>
   <title>迁移到版本 8.3.3</title>

   <para>
      对于运行 8.3.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.3.1 的版本升级，请参阅<xref linkend="release-8-3-1">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      让 <function>pg_get_ruledef()</function> 给负常数加括号（Tom）
     </para>

     <para>
      在此修复之前，视图或规则中的负常数可能被转储成如 <literal>-42::integer</literal>，这有微妙的错误：由于操作符优先级规则，它应该是 <literal>(-42)::integer</literal>。通常这影响不大，但它可能与另一个最近的补丁相互作用，导致 <productname>PostgreSQL</productname> 拒绝原本有效的 <command>SELECT DISTINCT</command> 视图查询。由于这可能导致 <application>pg_dump</application> 输出无法重载，它被当作高优先级修复。转储输出确实不正确的已发布版本只有 8.3.1 和 8.2.7。
     </para>
    </listitem>

    <listitem>
     <para>
      让 <command>ALTER AGGREGATE ... OWNER TO</command> 更新 <structname>pg_shdepend</structname>（Tom）
     </para>

     <para>
      如果聚合后来涉及 <command>DROP OWNED</command> 或 <command>REASSIGN OWNED</command> 操作，这一疏漏可能导致问题。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-3-2">
  <title>版本 8.3.2</title>

  <formalpara>
  <title>发行日期：</title>
  <para>
      从未发布
     </para>
  </formalpara>

  <para>
   本次发布包含对 8.3.1 的多方面修复。有关 8.3 大版本中新特性的信息，请参见<xref linkend="release-8-3">。.
  </para>

  <sect2>
   <title>迁移到版本 8.3.2</title>

   <para>
      对于运行 8.3.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.3.1 的版本升级，请参阅<xref linkend="release-8-3-1">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      修复在 Windows 上使用 UTF-8 数据库编码和不同客户端编码时发生的 <literal>ERRORDATA_STACK_SIZE exceeded</literal> 崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <varname>recovery_command</varname> 参数中 <literal>%r</literal> 宏的归档截断点计算错误（Simon）
     </para>

     <para>
      如果热备脚本依赖 <literal>%r</literal> 决定何时丢弃 WAL 段文件，这可能导致数据丢失。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <command>ALTER TABLE ADD COLUMN ... PRIMARY KEY</command>，使新列被正确检查是否已全部初始化为非空（Brendan Jurd）
     </para>

     <para>
      以前的版本完全忽略了对这一要求的检查。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <command>REASSIGN OWNED</command>，使其也适用于过程语言（Alvaro）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <command>SELECT FOR UPDATE/SHARE</command> 作为顶层操作非 <command>SELECT</command> 的查询中的子查询时的问题（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复从多个都继承自共同祖先的父关系继承<quote>同一</quote>约束时 <command>CREATE TABLE</command> 可能失败的问题（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>pg_get_ruledef()</function>，显示 <command>UPDATE</command> 或 <command>DELETE</command> 目标表上附加的别名（如果有）（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      恢复 8.3 之前的行为：TidScan 计划中使用的 TID 的块号超出范围时静默不匹配任何行（Tom）
     </para>

     <para>
      8.3.0 和 8.3.1 改为抛出错误。
     </para>
    </listitem>

    <listitem>
     <para>
      修复可能导致 <literal>too many LWLocks taken</literal> 失败的 GIN 缺陷（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <type>tsquery</type> 损坏的 GiST 比较函数（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>tsvector_update_trigger()</function> 和 <function>ts_stat()</function>，使其接受其预期工作类型之上的域（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复不支持枚举数据类型作为外键的问题（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      避免解压损坏数据时可能的崩溃（Zdenek Kotala）
     </para>
    </listitem>

    <listitem>
     <para>
      修复延迟取消链接与 <command>DROP DATABASE</command> 之间的竞态条件（Heikki）
     </para>

     <para>
      最坏情况下这可能导致删除新数据库中恰好获得与最近删除的表相同 OID 的新建表；当然这是概率极低的情形。
     </para>
    </listitem>

    <listitem>
     <para>
      修复两处后端 SIGTERM 退出可能在共享内存中留下损坏状态的地方（Tom）
     </para>

     <para>
      如果 SIGTERM 用于一起关闭整个数据库集群，这两种情形都不太重要，但如果有人试图对单个后端发 SIGTERM，就有问题。
     </para>
    </listitem>

    <listitem>
     <para>
      修复当 <replaceable>x</replaceable> 和 <replaceable>y</replaceable> 数据类型不同时为 <literal><replaceable>x</replaceable> IN (SELECT <replaceable>y</replaceable> FROM ...)</literal> 子句生成的错误计划可能导致的崩溃；并确保从 <replaceable>y</replaceable> 的类型到 <replaceable>x</replaceable> 的转换有损时行为在语义上正确（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复阻止规划器把已知 Param 值当作常数替换的疏漏（Tom）
     </para>

     <para>
      此错误部分禁用了 8.3.0 和 8.3.1 中未命名扩展查询语句的优化：特别是 LIKE 模式作为参数传递时 LIKE 到索引扫描的优化永远不会被应用，依赖参数值的约束排除也不起作用。
     </para>
    </listitem>

    <listitem>
     <para>
      修复可索引的 <function>MIN</function> 或 <function>MAX</function> 聚合与 <literal>DISTINCT</literal> 或 <literal>ORDER BY</literal> 一起使用时规划器失败的问题（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复规划器，确保它从不对向 Sort 节点供数据的计划节点使用<quote>物理 tlist</quote>（Tom）
     </para>

     <para>
      这导致排序不得不搬运比实际需要更多的数据，因为未使用的列值被包含在排序数据中。
     </para>
    </listitem>

    <listitem>
     <para>
      避免不必要的查询字符串复制（Tom）
     </para>

     <para>
      这修复了 8.3.0 中引入的当大量命令作为单个查询字符串提交时的性能问题。
     </para>
    </listitem>

    <listitem>
     <para>
      让 <function>TransactionIdIsCurrentTransactionId()</function> 在检查子事务 XID 时使用二分搜索而不是线性搜索（Heikki）
     </para>

     <para>
      这修复了 8.3.0 在某些情形下明显慢于早期版本的问题。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 ISO-8859-5 与其他编码之间的转换对西里尔<quote>Yo</quote>字符（带两个点的 <literal>e</literal> 和 <literal>E</literal>）的处理（Sergey Burladyan）
     </para>
    </listitem>

    <listitem>
     <para>
      修复几个数据类型输入函数（特别是 <function>array_in()</function>），它们允许结果中未使用的字节包含未初始化的不可预测值（Tom）
     </para>

     <para>
      这可能导致两个看起来相同的字面值不被视为相等的失败，导致解析器抱怨 <literal>ORDER BY</literal> 和 <literal>DISTINCT</literal> 表达式不匹配。
     </para>
    </listitem>

    <listitem>
     <para>
      修复正则表达式子串匹配（<literal>substring(<replaceable>string</replaceable> from <replaceable>pattern</replaceable>)</literal>）中的一个极端情形（Tom）
     </para>

     <para>
      问题发生在模式整体有匹配但用户指定了带括号的子表达式而该子表达式没有匹配时。例子是 <literal>substring('foo' from 'foo(bar)?')</literal>。由于 <literal>(bar)</literal> 未匹配，它应返回 NULL，但它错误地返回了整个模式的匹配（即 <literal>foo</literal>）。
     </para>
    </listitem>

    <listitem>
     <para>
      防止取消为防止 XID 回卷而启动的自动清理（Alvaro）
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <command>ANALYZE</command> 对不确定元组（由尚未提交的事务插入或删除的元组）的处理，使其报告给统计收集器的计数更可能正确（Pavan Deolasee）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>initdb</application>，拒绝其 <literal>--xlogdir</literal>（<literal>-X</literal>）选项的相对路径（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <application>psql</application> 把制表符打印为适当数量的空格，而不是像 8.3.0 和 8.3.1 那样打印 <literal>\x09</literal>（Bruce）
     </para>
    </listitem>

    <listitem>
     <para>
      更新时区数据文件到 <application>tzdata</application> 发行版 2008c（针对摩洛哥、伊拉克、乔巴山、巴基斯坦、叙利亚、古巴和阿根廷/圣路易斯的 DST 法规变更）
     </para>
    </listitem>

    <listitem>
     <para>
      为 <application>ecpglib</application> 新增 <function>ECPGget_PGconn()</function> 函数（Michael）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>ecpg</application> 的 <function>PGTYPEStimestamp_sub()</function> 函数结果不正确的问题（Michael）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>ecpg</application> 中续行标记的处理（Michael）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/cube</filename> 函数可能的崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/xml2</filename> 的 <function>xpath_table()</function> 函数在输入查询返回 NULL 值时的核心转储（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/xml2</filename> 的 makefile，使其不覆盖 <literal>CFLAGS</literal>，并使其针对 <application>libxslt</application> 是否存在正确自动配置（Tom）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-3-1">
  <title>版本 8.3.1</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2008-03-17</para>
  </formalpara>

  <para>
   本次发布包含对 8.3.0 的多方面修复。有关 8.3 大版本中新特性的信息，请参见<xref linkend="release-8-3">。.
  </para>

  <sect2>
   <title>迁移到版本 8.3.1</title>

   <para>
      对于运行 8.3.X 的用户，不需要进行转储/恢复。不过，如果你受到下述 Windows 区域设置问题的影响，可能需要在更新后对文本列上的索引执行 <command>REINDEX</command>。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      修复把不同字符组合视为相等的 Windows 区域设置下的字符串比较（Tom）
     </para>

     <para>
      此修复仅在 Windows 上且仅在使用 UTF-8 数据库编码时适用。所有其他情形的相同修复两年多前就已完成，但使用 UTF-8 的 Windows 走的是未更新的单独代码路径。如果你使用的区域设置把某些不同的字符串视为相等，可能需要 <command>REINDEX</command> 来修复文本列上的现有索引。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <command>VACUUM FULL</command> 的极端情形缺陷（Tom）
     </para>

     <para>
      8.2 中引入了不同系统目录上并发 <command>VACUUM FULL</command> 操作之间的潜在死锁。这已被纠正。8.3 使情况更糟，因为死锁可能发生在临界代码区内，使它成为 PANIC 而不只是 ERROR。
     </para>

     <para>
      此外，在清理系统目录中途失败的 <command>VACUUM FULL</command> 可能导致并发数据库会话中的缓存损坏。
     </para>

     <para>
      8.3 中引入的另一个 <command>VACUUM FULL</command> 缺陷在处理不含活元组的页时可能导致崩溃或内存不足报告。
     </para>
    </listitem>

    <listitem>
     <para>
      修复涉及 <type>character</type> 或 <type>bit</type> 列的外键检查的错误行为（Tom）
     </para>

     <para>
      如果引用列是不同但兼容的类型（例如 <type>varchar</type>），约束的强制执行就不正确。
     </para>
    </listitem>

    <listitem>
     <para>
      避免空操作外键检查中不必要的死锁失败（Stephan Szabo、Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复重新规划预备查询时可能的核心转储（Tom）
     </para>

     <para>
      此缺陷只影响协议级 prepare 操作，不影响 SQL <command>PREPARE</command>，因此往往只出现在大量使用预备语句的 JDBC、DBI 和其他客户端驱动上。
     </para>
    </listitem>

    <listitem>
     <para>
      修复重新规划调用使用 SPI 的函数的查询时可能的失败（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复涉及不同数据类型列的按行比较失败（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复长期存在的 <command>LISTEN</command>/<command>NOTIFY</command> 竞态条件（Tom）
     </para>

     <para>
      极少数情况下，刚执行过 <command>LISTEN</command> 的会话可能收不到通知，即使按理应当收到，因为观察到执行 <command>NOTIFY</command> 的并发事务稍后提交了。
     </para>

     <para>
      此修复的一个副作用是：执行了尚未提交的 <command>LISTEN</command> 命令的事务如果去查看，将看不到该 <command>LISTEN</command> 在 <structname>pg_listener</structname> 中的行；以前能看到。这一行为从未被明确记录，但有些应用程序可能依赖旧行为。
     </para>
    </listitem>

    <listitem>
     <para>
      不允许在预备事务内使用 <command>LISTEN</command> 和 <command>UNLISTEN</command>（Tom）
     </para>

     <para>
      这以前是允许的，但尝试这样做会有各种不愉快的后果，特别是只要 <command>UNLISTEN</command> 未提交，发起的后端就无法退出。
     </para>
    </listitem>

    <listitem>
     <para>
      不允许在预备事务内删除临时表（Heikki）
     </para>

     <para>
      8.1 正确地禁止了这一点，但该检查在 8.2 和 8.3 中被无意破坏。
     </para>
    </listitem>

    <listitem>
     <para>
      修复使用哈希索引的查询期间发生错误时的罕见崩溃（Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <type>tsquery</type> 值的错误比较（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      修复单字节编码中 <literal>LIKE</literal> 对非 ASCII 字符的错误行为（Rolf Jentsch）
     </para>
    </listitem>

    <listitem>
     <para>
      禁用 <function>xmlvalidate</function>（Tom）
     </para>

     <para>
      此函数本应在 8.3 发布前移除，但被无意留在了源代码中。它带来小的安全风险，因为非特权用户可以用它读取服务器可访问的任何文件的前几个字符。
     </para>
    </listitem>

    <listitem>
     <para>
      修复返回集合函数某些用法中的内存泄漏（Neil）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <function>encode(<replaceable>bytea</replaceable>, 'escape')</function> 把所有高位字节值转换为 <literal>\</literal><replaceable>nnn</replaceable> 八进制转义序列（Tom）
     </para>

     <para>
      这对于在数据库编码为多字节时避免编码问题是必要的。此变更可能给期望 <function>encode</function> 特定结果的应用程序带来兼容性问题。
     </para>
    </listitem>

    <listitem>
     <para>
      修复公元前年份 2 月 29 日日期时间值的输入（Tom）
     </para>

     <para>
      以前的编码弄错了哪些年份是闰年。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <command>ALTER OWNER</command> 某些变体中的<quote>无法识别的节点类型</quote>错误（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      避免 <command>CREATE TABLE LIKE INCLUDING INDEXES</command> 中的表空间权限错误（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      确保锁等待中止时清除 <structname>pg_stat_activity</structname>.<structfield>waiting</structfield> 标志（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 Windows Vista 上进程权限的处理（Dave、Magnus）
     </para>

     <para>
      特别是，此修复允许以 Administrator 用户启动服务器。
     </para>
    </listitem>

    <listitem>
     <para>
      更新时区数据文件到 <application>tzdata</application> 发行版 2008a（特别是智利最近的变更）；调整时区缩写 <literal>VET</literal>（委内瑞拉）为 UTC-4:30 而不是 UTC-4:00（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>ecpg</application> 的数组问题（Michael）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_ctl</application>，使其从命令行选项正确提取 postmaster 的端口号（Itagaki Takahiro、Tom）
     </para>

     <para>
      此前 <literal>pg_ctl start -w</literal> 可能试图在错误端口上联系 postmaster，导致虚假的启动失败报告。
     </para>
    </listitem>

    <listitem>
     <para>
      使用 <option>-fwrapv</option> 防御最新 <application>gcc</application> 版本可能的错误优化（Tom）
     </para>

     <para>
      已知用 <application>gcc</application> 4.3 或更高版本构建 <productname>PostgreSQL</productname> 时这是必需的。
     </para>
    </listitem>

    <listitem>
     <para>
      支持用 MSVC 构建 <filename>contrib/uuid-ossp</filename>（Hiroshi Saito）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-3">
  <title>版本 8.3</title>

  <formalpara>
   <title>发行日期：</title>
   <para>2008-02-04</para>
  </formalpara>

  <sect2>
   <title>概述</title>

   <para>
      凭借重要的新功能和性能增强，此发行版代表了 <productname>PostgreSQL</productname> 的重大飞跃。这得益于一个不断壮大的社区，它显著加快了开发步伐。此发行版增加了以下主要特性：
     </para>

   <itemizedlist>

    <listitem>
     <para>
      全文搜索被集成到核心数据库系统中
     </para>
    </listitem>

    <listitem>
     <para>
      支持 SQL/XML 标准，包括新操作符和 <type>XML</type> 数据类型
     </para>
    </listitem>

    <listitem>
     <para>
      枚举数据类型（<type>ENUM</type>）
     </para>
    </listitem>

    <listitem>
     <para>
      组合类型的数组
     </para>
    </listitem>

    <listitem>
     <para>
      通用唯一标识符（<type>UUID</type>）数据类型
     </para>
    </listitem>

    <listitem>
     <para>
      新增对 <literal>NULL</literal> 排在最前还是最后的控制
     </para>
    </listitem>

    <listitem>
     <para>
      可更新游标
     </para>
    </listitem>

    <listitem>
     <para>
      服务器配置参数现在可以逐函数设置
     </para>
    </listitem>

    <listitem>
     <para>
      用户定义类型现在可以带类型修饰符
     </para>
    </listitem>

    <listitem>
     <para>
      当表定义更改或统计信息更新时自动重新规划缓存查询
     </para>
    </listitem>

    <listitem>
     <para>
      日志和统计信息收集的大量改进
     </para>
    </listitem>

    <listitem>
     <para>
      支持在 Windows 上使用安全服务提供程序接口（<acronym>SSPI</acronym>）进行认证
     </para>
    </listitem>

    <listitem>
     <para>
      支持多个并发 autovacuum 进程及其他 autovacuum 改进
     </para>
    </listitem>

    <listitem>
     <para>
      允许整个 <productname>PostgreSQL</productname> 发行版用 <productname>Microsoft Visual C++</productname> 编译
     </para>
    </listitem>

   </itemizedlist>

   <para>
      主要性能改进如下所列。这些增强大多是自动的，不需要用户更改或调优：
     </para>

  <itemizedlist>

    <listitem>
     <para>
      异步提交延迟事务提交期间对 WAL 的写入
     </para>
    </listitem>

    <listitem>
     <para>
      检查点写入可以分散到更长的时间段，以平滑每个检查点期间的 I/O 峰值
     </para>
    </listitem>

    <listitem>
     <para>
      仅堆元组（<acronym>HOT</acronym>）加速大多数 <command>UPDATE</command> 和 <command>DELETE</command> 的空间复用
     </para>
    </listitem>

    <listitem>
     <para>
      即时后台写入器策略改进磁盘写入效率
     </para>
    </listitem>

    <listitem>
     <para>
      对只读事务使用非持久事务 ID 减少开销和 <command>VACUUM</command> 需求
     </para>
    </listitem>

    <listitem>
     <para>
      逐字段和逐行的存储开销已被降低
     </para>
    </listitem>

    <listitem>
     <para>
      大型顺序扫描不再驱逐频繁使用的缓存页
     </para>
    </listitem>

    <listitem>
     <para>
      并发的大型顺序扫描现在可以共享磁盘读取
     </para>
    </listitem>

    <listitem>
     <para>
      <literal>ORDER BY ... LIMIT</literal> 可以不排序完成
     </para>
    </listitem>

   </itemizedlist>

   <para>
     上述各项在下面的章节中有更详细的说明。
    </para>

  </sect2>

  <sect2>
   <title>迁移到版本 8.3</title>

   <para>
      希望从任何以前的版本迁移数据的用户，需要使用 <application>pg_dump</application> 进行转储/恢复。
     </para>

   <para>
      请注意以下不兼容之处：
     </para>

   <sect3>
    <title>常规</title>
    <itemizedlist>

     <listitem>
      <para>
      非字符数据类型不再自动转换为 <type>TEXT</type>（Peter、Tom）
     </para>

      <para>
       此前，如果把非字符值提供给要求 <type>text</type> 输入的操作符或函数，它会被自动转换为 <type>text</type>（对大多数但非全部内建数据类型）。这不再发生：现在所有非字符串类型都需要显式转换为 <type>text</type>。例如，这些表达式以前可用：

<programlisting>
substr(current_date, 1, 4)
23 LIKE '2%'
</programlisting>

       但现在分别会报<quote>函数不存在</quote>和<quote>操作符不存在</quote>错误。请改用显式转换：

<programlisting>
substr(current_date::text, 1, 4)
23::text LIKE '2%'
</programlisting>

       （当然，也可以使用更冗长的 <literal>CAST()</literal> 语法。）此变更的原因是这些自动转换太常引起令人惊讶的行为。例如，在以前的版本中，这个表达式被接受但做的不是预期的事：

<programlisting>
current_date &lt; 2017-11-17
</programlisting>

       这实际上是在把日期与整数比较，本应被拒绝（现在也是）——但在存在自动转换时，两边都被转换为 <type>text</type> 并做了文本比较，因为没有其他 <literal>&lt;</literal>
       操作符能匹配该表达式时 <literal>text &lt; text</literal> 操作符可以匹配。
      </para>

      <para>
      <type>char(<replaceable>n</replaceable>)</type> 和 <type>varchar(<replaceable>n</replaceable>)</type> 类型仍然自动转换为 <type>text</type>。此外，只要至少一个输入是字符串类型，到 <type>text</type> 的自动转换对连接（<literal>||</literal>）操作符的输入仍然有效。
     </para>
     </listitem>

     <listitem>
      <para>
      <filename>contrib/tsearch2</filename> 的全文搜索特性已移入核心服务器，语法有少量变化
     </para>

      <para>
      <filename>contrib/tsearch2</filename> 现在包含一个兼容接口。
     </para>
     </listitem>

     <listitem>
      <para>
      <literal>ARRAY(SELECT ...)</literal> 在 <command>SELECT</command> 不返回行时现在返回空数组而不是 NULL（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      基本数据类型的数组类型名不再总是基本类型名加下划线前缀
     </para>

      <para>
      旧的命名约定在可能时仍被沿用，但应用程序代码不应再依赖它。请改用新的 <literal>pg_type.typarray</literal> 列来标识与给定类型关联的数组数据类型。
     </para>
     </listitem>

     <listitem>
      <para>
      <literal>ORDER BY ... USING</literal> <replaceable>operator</replaceable> 现在必须使用 btree 操作符类中定义的小于或大于 <replaceable>operator</replaceable>
     </para>

      <para>
      添加此限制是为了防止不一致的结果。
     </para>
     </listitem>

     <listitem>
      <para>
      <command>SET LOCAL</command> 的更改现在持续到最外层事务结束，除非回滚（Tom）
     </para>

      <para>
      此前 <command>SET LOCAL</command> 的效果在子事务提交（<command>RELEASE SAVEPOINT</command> 或退出 PL/pgSQL 异常块）后丢失。
     </para>
     </listitem>

     <listitem>
      <para>
      在事务块中被拒绝的命令现在在多语句查询字符串中也被拒绝（Tom）
     </para>

      <para>
      例如，<literal>"BEGIN; DROP DATABASE; COMMIT"</literal> 现在即使作为单个查询消息提交也会被拒绝。
     </para>
     </listitem>

     <listitem>
      <para>
      事务块外的 <command>ROLLBACK</command> 现在发出 <literal>NOTICE</literal> 而不是 <literal>WARNING</literal>（Bruce）
     </para>
     </listitem>

     <listitem>
      <para>
      防止 <command>NOTIFY</command>/<command>LISTEN</command>/<command>UNLISTEN</command> 接受模式限定的名称（Bruce）
     </para>

      <para>
      此前这些命令接受 <literal>schema.relation</literal> 但忽略模式部分，这令人困惑。
     </para>
     </listitem>

     <listitem>
      <para>
      <command>ALTER SEQUENCE</command> 不再影响序列的 <function>currval()</function> 状态（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      外键现在对于跨数据类型引用必须匹配可索引的条件（Tom）
     </para>

      <para>
      这改进了语义一致性并有助于避免性能问题。
     </para>
     </listitem>

     <listitem>
      <para>
      把对象大小函数限制为有合理权限查看此类信息的用户（Tom）
     </para>

      <para>
      例如，<function>pg_database_size()</function> 现在需要 <literal>CONNECT</literal> 权限（默认授予所有人）。<function>pg_tablespace_size()</function> 需要表空间中的 <literal>CREATE</literal> 权限，或者如果该表空间是数据库的默认表空间则允许。
     </para>
     </listitem>

     <listitem>
      <para>
      移除未记录的 <literal>!!=</literal>（not in）操作符（Tom）
     </para>

      <para>
      <literal>NOT IN (SELECT ...)</literal> 是执行此操作的正确方式。
     </para>
     </listitem>

     <listitem>
      <para>
      内部哈希函数现在分布更均匀（Tom）
     </para>

      <para>
      如果应用程序代码使用 <productname>PostgreSQL</productname> 内部哈希函数计算并存储哈希值，必须重新生成这些哈希值。
     </para>
     </listitem>

     <listitem>
      <para>
      处理变长数据值的 C 代码约定已更改（Greg Stark、Tom）
     </para>

      <para>
      <emphasis>必须</emphasis>使用新的 <function>SET_VARSIZE()</function> 宏来设置生成的 <type>varlena</type> 值的长度。此外，可能需要在更多情形下展开（<quote>去 TOAST</quote>）输入值。
     </para>
     </listitem>

     <listitem>
      <para>
      连续归档不再把每个成功的归档操作报告给服务器日志，除非使用 <literal>DEBUG</literal> 级别（Simon）
     </para>
     </listitem>

    </itemizedlist>

    </sect3>

    <sect3>
     <title>配置参数</title>

    <itemizedlist>

     <listitem>
      <para>
      管理服务器参数的大量变更
     </para>

      <para>
      <varname>bgwriter_lru_percent</varname>、<varname>bgwriter_all_percent</varname>、<varname>bgwriter_all_maxpages</varname>、<varname>stats_start_collector</varname> 和 <varname>stats_reset_on_server_start</varname> 被移除。<varname>redirect_stderr</varname> 改名为 <varname>logging_collector</varname>。<varname>stats_command_string</varname> 改名为 <varname>track_activities</varname>。<varname>stats_block_level</varname> 和 <varname>stats_row_level</varname> 合并为 <varname>track_counts</varname>。新的布尔配置参数 <varname>archive_mode</varname> 控制归档。Autovacuum 的默认设置已更改。
     </para>
     </listitem>

     <listitem>
      <para>
      移除 <varname>stats_start_collector</varname> 参数（Tom）
     </para>

      <para>
      现在总是启动收集器进程，除非 <acronym>UDP</acronym> 套接字创建失败。
     </para>
     </listitem>

     <listitem>
      <para>
      移除 <varname>stats_reset_on_server_start</varname> 参数（Tom）
     </para>

      <para>
      移除它是因为 <function>pg_stat_reset()</function> 可用于此目的。
     </para>
     </listitem>

     <listitem>
      <para>
      注释掉 <filename>postgresql.conf</filename> 中的参数现在使其恢复默认值（Joachim Wieland）
     </para>

      <para>
      此前，注释掉条目使参数值保持不变直到下一次服务器重启。
     </para>
     </listitem>

    </itemizedlist>

    </sect3>


    <sect3>
     <title>字符编码</title>

    <itemizedlist>

     <listitem>
      <para>
      新增更多对无效编码数据的检查（Andrew）
     </para>

      <para>
      此变更堵住了字面反斜杠转义字符串处理和 <command>COPY</command> 转义处理中存在的一些漏洞。现在对去转义后的字符串重新检查结果是否产生了无效的多字节字符。
     </para>
     </listitem>

     <listitem>
      <para>
      不允许与服务器区域设置不一致的数据库编码（Tom）
     </para>

      <para>
      在大多数平台上，<literal>C</literal> 区域设置是唯一能用于任何数据库编码的区域设置。其他区域设置隐含特定编码，如果数据库编码不同就会行为异常。（典型症状包括文本排序顺序错误以及 <function>upper()</function> 或 <function>lower()</function> 结果错误。）服务器现在拒绝创建编码不兼容的数据库。
     </para>
     </listitem>

     <listitem>
      <para>
      确保 <function>chr()</function> 不能创建无效编码的值（Andrew）
     </para>

      <para>
      在 UTF8 编码的数据库中，<function>chr()</function> 的参数现在被视为 Unicode 码点。在其他多字节编码中，<function>chr()</function> 的参数必须指定 7 位 ASCII 字符。不再接受零。<function>ascii()</function> 已相应调整。
     </para>
     </listitem>

     <listitem>
      <para>
      调整 <function>convert()</function> 的行为以确保编码有效性（Andrew）
     </para>

      <para>
      <function>convert()</function> 的两参数形式已被移除。三参数形式现在接受 <type>bytea</type> 作为第一个参数并返回 <type>bytea</type>。为弥补功能损失，新增了三个函数：
     </para>

      <itemizedlist>
       <listitem>
        <para>
      <function>convert_from(bytea, name)</function> 返回 <type>text</type>——把第一个参数从指定编码转换为数据库编码
     </para>
       </listitem>

       <listitem>
        <para>
      <function>convert_to(text, name)</function> 返回 <type>bytea</type>——把第一个参数从数据库编码转换为指定编码
     </para>
       </listitem>

       <listitem>
        <para>
      <function>length(bytea, name)</function> 返回 <type>integer</type>——给出第一个参数按指定编码的字符长度
     </para>
       </listitem>
      </itemizedlist>
     </listitem>

     <listitem>
      <para>
      移除 <literal>convert(argument USING conversion_name)</literal>（Andrew）
     </para>

      <para>
      其行为不符合 SQL 标准。
     </para>
     </listitem>

     <listitem>
      <para>
      把 JOHAB 编码改为仅客户端（Tatsuo）
     </para>

      <para>
      JOHAB 作为服务器端编码不安全。
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

  </sect2>

  <sect2>
   <title>变更</title>

   <para>
      下面你将找到 <productname>PostgreSQL</productname> 8.3 与上一个主版本之间变更的详细说明。
     </para>

   <sect3>
    <title>性能</title>
    <itemizedlist>

     <listitem>
      <para>
      异步提交延迟事务提交期间对 WAL 的写入（Simon）
     </para>

      <para>
      此特性显著提高了短的数据修改事务的性能。缺点是由于磁盘写入被延迟，如果数据库或操作系统在数据写入磁盘之前崩溃，已提交的数据将丢失。此特性对能接受一些数据丢失的应用程序有用。与关闭 <varname>fsync</varname> 不同，使用异步提交不会使数据库一致性面临风险；最坏的情况是崩溃后最后几个报告已提交的事务可能实际上没有提交。通过关闭 <varname>synchronous_commit</varname> 启用此特性（如果某些事务关键而另一些不关键，可以逐会话或逐事务进行）。可以调整 <varname>wal_writer_delay</varname> 来控制事务实际到达磁盘前的最大延迟。
     </para>
     </listitem>

     <listitem>
      <para>
      检查点写入可以分散到更长的时间段，以平滑每个检查点期间的 I/O 峰值（Itagaki Takahiro 和 Heikki Linnakangas）
     </para>

      <para>
      此前所有修改过的缓冲区在检查点期间被尽快强制写盘，造成降低服务器性能的 I/O 峰值。新方法把检查点期间的磁盘写分散开，降低峰值 I/O 使用。（用户请求的检查点和关闭检查点仍然尽快写入。）
     </para>
     </listitem>

     <listitem>
      <para>
      仅堆元组（<acronym>HOT</acronym>）加速大多数 <command>UPDATE</command> 和 <command>DELETE</command> 的空间复用（Pavan Deolasee，想法来自许多其他人）
     </para>

      <para>
      <command>UPDATE</command> 和 <command>DELETE</command> 会留下死元组，失败的 <command>INSERT</command> 也是。此前只有 <command>VACUUM</command> 能回收死元组占用的空间。有了 <acronym>HOT</acronym>，如果没有对索引列做更改，死元组空间可以在 <command>INSERT</command> 或 <command>UPDATE</command> 时自动回收。这带来更一致的性能。此外，<acronym>HOT</acronym> 避免添加重复的索引条目。
     </para>
     </listitem>

     <listitem>
      <para>
      即时后台写入器策略改进磁盘写入效率（Greg Smith、Itagaki Takahiro）
     </para>

      <para>
      这大幅减少了对后台写入器的手动调优需求。
     </para>
     </listitem>

     <listitem>
      <para>
      逐字段和逐行的存储开销已被降低（Greg Stark、Heikki Linnakangas）
     </para>

      <para>
      数据值短于 128 字节的变长数据类型的存储将减少 3 到 6 字节。例如，两个相邻的 <type>char(1)</type> 字段现在用 4 字节而不是 16 字节。行头也比以前短 4 字节。
     </para>
     </listitem>

     <listitem>
      <para>
      对只读事务使用非持久事务 ID 减少开销和 <command>VACUUM</command> 需求（Florian Pflug）
     </para>

      <para>
      非持久事务 ID 不递增全局事务计数器。因此，它们降低了 <structname>pg_clog</structname> 的负载，并延长了为防止事务 ID 回卷而强制清理之间的时间间隔。还做了其他应能改进并发性的性能改进。
     </para>
     </listitem>

     <listitem>
      <para>
      避免在只读命令之后递增命令计数器（Tom）
     </para>

      <para>
      此前每个事务有 2<superscript>32</superscript>（40 亿）条命令的硬限制。现在只有实际更改数据库的命令才计数，因此虽然该限制仍存在，但应该明显不那么碍事了。
     </para>
     </listitem>

     <listitem>
      <para>
      创建专门的 <acronym>WAL</acronym> 写入器进程来分担后端的工作（Simon）
     </para>
     </listitem>

     <listitem>
      <para>
      跳过 <command>CLUSTER</command> 和 <command>COPY</command> 不必要的 WAL 写入（Simon）
     </para>

      <para>
      除非启用了 WAL 归档，系统现在避免为 <command>CLUSTER</command> 写 WAL，只是在命令结束时对表执行 <function>fsync()</function>。如果表在同一事务中创建，<command>COPY</command> 也同样处理。
     </para>
     </listitem>

     <listitem>
      <para>
      大型顺序扫描不再驱逐频繁使用的缓存页（Simon、Heikki、Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      并发的大型顺序扫描现在可以共享磁盘读取（Jeff Davis）
     </para>

      <para>
      这是通过在表中间（另一个顺序扫描已在进行的地方）开始新的顺序扫描并回绕到开头来完成来实现的。这会影响未指定 <literal>ORDER BY</literal> 的查询返回行的顺序。必要时可以用 <varname>synchronize_seqscans</varname> 配置参数禁用它。
     </para>
     </listitem>

     <listitem>
      <para>
      <literal>ORDER BY ... LIMIT</literal> 可以不排序完成（Greg Stark）
     </para>

      <para>
      这是通过顺序扫描表并只跟踪<quote>前 N</quote>候选行而不是对整表做完全排序来完成的。这在没有匹配索引且 <literal>LIMIT</literal> 不大时很有用。
     </para>
     </listitem>

     <listitem>
      <para>
      对后端发送给统计收集器的消息设置速率限制（Tom）
     </para>

      <para>
      这降低了短事务的开销，但有时可能增加统计汇总前的延迟。
     </para>
     </listitem>

     <listitem>
      <para>
      改进有许多 NULL 情形下的哈希连接性能（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      加快非精确数据类型匹配情形下的操作符查找（Tom）
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title>服务器</title>
    <itemizedlist>

     <listitem>
      <para>
      Autovacuum 现在默认启用（Alvaro）
     </para>

      <para>
      做了若干更改以消除启用 autovacuum 的缺点，从而证明默认值变更的合理性。还修改了几个其他 autovacuum 参数的默认值。
     </para>
     </listitem>

     <listitem>
      <para>
      支持多个并发 autovacuum 进程（Alvaro、Itagaki Takahiro）
     </para>

      <para>
      这允许多个清理并发运行。这防止大表的清理延迟较小表的清理。
     </para>
     </listitem>

     <listitem>
      <para>
      当表定义更改或统计信息更新时自动重新规划缓存查询（Tom）
     </para>

      <para>
      此前，引用临时表的 PL/pgSQL 函数在临时表于函数调用之间被删除并重建时会失败，除非使用 <literal>EXECUTE</literal>。此改进修复了该问题及许多相关问题。
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <varname>temp_tablespaces</varname> 参数，控制临时表和文件的表空间（Jaime Casanova、Albert Cervera、Bernd Helmle）
     </para>

      <para>
      此参数定义要使用的表空间列表。这使 I/O 负载可以分散到多个表空间。每次创建临时对象时随机选择一个表空间。临时文件不再存储在逐数据库的 <filename>pgsql_tmp/</filename> 目录中，而是存储在逐表空间的目录中。
     </para>
     </listitem>

     <listitem>
      <para>
      把临时表的 TOAST 表放在名为 <literal>pg_toast_temp_<replaceable>nnn</replaceable></literal> 的特殊模式中（Tom）
     </para>

      <para>
      这使低层代码能把这些表识别为临时的，从而支持各种优化，例如不写 WAL 日志更改以及使用本地而非共享缓冲区访问。这还修复了后端意外持有临时 TOAST 表打开文件引用的缺陷。
     </para>
     </listitem>

     <listitem>
      <para>
      修复新连接请求持续不断可能无限期延迟 postmaster 完成关闭或崩溃重启的问题（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      通过防止重用已删除表的 relfilenode 直到下一个检查点之后，防范概率极低的数据丢失情形（Heikki）
     </para>
     </listitem>

     <listitem>
      <para>
      修复 <command>CREATE CONSTRAINT TRIGGER</command>，把旧式外键触发器定义转换为常规外键约束（Tom）
     </para>

      <para>
      这将便于移植从 7.3 之前数据库延续下来的外键约束，如果它们从未用 <filename>contrib/adddepend</filename> 转换过。
     </para>
     </listitem>

     <listitem>
      <para>
      修复 <literal>DEFAULT NULL</literal> 以覆盖继承的默认值（Tom）
     </para>

      <para>
      <literal>DEFAULT NULL</literal> 以前被视为噪声短语，但它应该（现在也确实）覆盖否则会从父表或域继承的非空默认值。
     </para>
     </listitem>

     <listitem>
      <para>
      新增编码 EUC_JIS_2004 和 SHIFT_JIS_2004（Tatsuo）
     </para>

      <para>
      这些新编码可以与 UTF-8 相互转换。
     </para>
     </listitem>

     <listitem>
      <para>
      把服务器启动日志消息从<quote>数据库系统就绪</quote>改为<quote>数据库系统准备好接受连接</quote>，并调整其时机
     </para>

      <para>
      该消息现在只在 postmaster 真正准备好接受连接时出现。
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title>监控</title>
    <itemizedlist>

     <listitem>
      <para>
      新增 <varname>log_autovacuum_min_duration</varname> 参数，支持可配置的 autovacuum 活动日志（Simon、Alvaro）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <varname>log_lock_waits</varname> 参数来记录锁等待（Simon）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <varname>log_temp_files</varname> 参数来记录临时文件使用（Bill Moran）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <varname>log_checkpoints</varname> 参数以改进检查点的日志记录（Greg Smith、Heikki）
     </para>
     </listitem>

     <listitem>
      <para>
      <varname>log_line_prefix</varname> 现在在所有进程中支持 <literal>%s</literal> 和 <literal>%c</literal> 转义（Andrew）
     </para>

      <para>
      此前这些转义只对用户会话有效，对后台数据库进程无效。
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <varname>log_restartpoints</varname> 来控制时间点恢复重启点的日志记录（Simon）
     </para>
     </listitem>

     <listitem>
      <para>
      现在在恢复结束和每个被记录的重启点记录最后事务结束时间（Simon）
     </para>
     </listitem>

     <listitem>
      <para>
      Autovacuum 现在在 <literal>pg_stat_activity</literal> 中报告其活动开始时间（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      允许服务器日志输出为逗号分隔值（CSV）格式（Arul Shaji、Greg Smith、Andrew Dunstan）
     </para>

      <para>
      CSV 格式的日志文件可以轻松装入数据库表以供后续分析。
     </para>
     </listitem>

     <listitem>
      <para>
      使用 PostgreSQL 提供的时区支持来格式化服务器日志中显示的时间戳（Tom）
     </para>

      <para>
      这避免了本地化时区名编码错误这一 Windows 特有的问题。新增了 <varname>log_timezone</varname> 参数，独立于客户端可见的 <varname>timezone</varname> 参数控制日志消息中使用的时区。
     </para>
     </listitem>

     <listitem>
      <para>
      新系统视图 <literal>pg_stat_bgwriter</literal> 显示后台写入器活动的统计信息（Magnus）
     </para>
     </listitem>

     <listitem>
      <para>
      为 <literal>pg_stat_database</literal> 新增数据库范围元组统计的列（Magnus）
     </para>
     </listitem>

     <listitem>
      <para>
      为 <literal>pg_stat_activity</literal> 新增 <literal>xact_start</literal>（事务开始时间）列（Neil）
     </para>

      <para>
      这使识别长运行事务更容易。
     </para>
     </listitem>

     <listitem>
      <para>
      为 <literal>pg_stat_all_tables</literal> 及相关视图新增 <literal>n_live_tuples</literal> 和 <literal>n_dead_tuples</literal> 列（Glen Parker）
     </para>
     </listitem>

     <listitem>
      <para>
      把 <varname>stats_block_level</varname> 和 <varname>stats_row_level</varname> 参数合并为单一参数 <varname>track_counts</varname>，它控制发送给统计收集器进程的所有消息（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      把 <varname>stats_command_string</varname> 参数改名为 <varname>track_activities</varname>（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      修复活元组和死元组的统计计数，使其认识到已提交和中止的事务有不同的影响（Tom）
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title>认证</title>
    <itemizedlist>

     <listitem>
      <para>
      支持在 Windows 上使用安全服务提供程序接口（<acronym>SSPI</acronym>）进行认证（Magnus）
     </para>
     </listitem>

     <listitem>
      <para>
      支持 GSSAPI 认证（Henry Hotz、Magnus）
     </para>

      <para>
      应优先于本机 Kerberos 认证使用它，因为 GSSAPI 是行业标准。
     </para>
     </listitem>

     <listitem>
      <para>
      支持全局 SSL 配置文件（Victor Wagner）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <varname>ssl_ciphers</varname> 参数，控制接受的 SSL 密码（Victor Wagner）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 Kerberos realm 参数 <varname>krb_realm</varname>（Magnus）
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title>Write-Ahead Log (<acronym>WAL</>) and Continuous Archiving</title>
    <itemizedlist>

     <listitem>
      <para>
      把事务 WAL 记录中记录的时间戳从 time_t 改为 TimestampTz 表示（Tom）
     </para>

      <para>
      这为 WAL 提供了亚秒分辨率，对时间点恢复很有用。
     </para>
     </listitem>

     <listitem>
      <para>
      减少热备服务器所需的 WAL 磁盘空间（Simon）
     </para>

      <para>
      此变更允许热备服务器把最早仍需要的 WAL 文件名传递给恢复脚本，从而自动删除不再需要的 WAL 文件。这是通过 <filename>recovery.conf</filename> 的 <varname>restore_command</varname> 参数中的 <literal>%r</literal> 完成的。
     </para>
     </listitem>

     <listitem>
      <para>
      新的布尔配置参数 <varname>archive_mode</varname> 控制归档（Simon）
     </para>

      <para>
      此前把 <varname>archive_command</varname> 设为空字符串会关闭归档。现在 <varname>archive_mode</varname> 独立于 <varname>archive_command</varname> 控制归档的开关。这对临时停止归档很有用。
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title>查询</title>
    <itemizedlist>

     <listitem>
      <para>
      全文搜索被集成到核心数据库系统（Teodor、Oleg）
     </para>

      <para>
      文本搜索得到改进，移入核心代码，现在默认安装。<filename>contrib/tsearch2</filename> 现在包含一个兼容接口。
     </para>
     </listitem>

     <listitem>
      <para>
      新增对 <literal>NULL</literal> 排在最前还是最后的控制（Teodor、Tom）
     </para>

      <para>
      语法为 <literal>ORDER BY ... NULLS FIRST/LAST</literal>。
     </para>
     </listitem>

     <listitem>
      <para>
      允许索引的逐列升序/降序（<literal>ASC</literal>/<literal>DESC</literal>）排序选项（Teodor、Tom）
     </para>

      <para>
      此前混合使用 <literal>ASC</literal>/<literal>DESC</literal> 说明符的 <literal>ORDER BY</literal> 查询无法完全使用索引。现在，如果索引创建时带有匹配的 <literal>ASC</literal>/<literal>DESC</literal> 说明，这类情形可以完全使用索引。索引内的 <literal>NULL</literal> 排序顺序也可以控制。
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <literal>col IS NULL</literal> 使用索引（Teodor）
     </para>
     </listitem>

     <listitem>
      <para>
      可更新游标（Arul Shaji、Tom）
     </para>

      <para>
      这消除了为 <command>UPDATE</command> 或 <command>DELETE</command> 游标返回的行而引用主键的需要。语法为 <literal>UPDATE/DELETE WHERE CURRENT OF</literal>。
     </para>
     </listitem>

     <listitem>
      <para>
      允许游标中使用 <literal>FOR UPDATE</literal>（Arul Shaji、Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      创建一个通用机制，通过调用数据类型的 I/O 函数，支持<emphasis>每个</emphasis>数据类型与标准字符串类型（<type>TEXT</type>、<type>VARCHAR</type>、<type>CHAR</type>）之间的转换（Tom）
     </para>

      <para>
      此前，这类转换只对有专门函数的类型可用。这些新转换在转字符串方向只允许赋值转换，另一方向只允许显式转换，因此不应产生令人惊讶的行为。
     </para>
     </listitem>

     <listitem>
      <para>
      当所有输入都是同一域类型时，允许 <literal>UNION</literal> 及相关构造返回域类型（Tom）
     </para>

      <para>
      此前，输出会被认为属于域的基本类型。
     </para>
     </listitem>

     <listitem>
      <para>
      允许在使用两种不同数据类型时进行有限哈希（Tom）
     </para>

      <para>
      如果数据类型有兼容的哈希函数，这允许在涉及跨数据类型比较的情形中使用哈希连接、哈希索引、哈希子计划和哈希聚合。目前，跨数据类型哈希支持存在于 <type>smallint</type>/<type>integer</type>/<type>bigint</type> 以及 <type>float4</type>/<type>float8</type>。
     </para>
     </listitem>

     <listitem>
      <para>
      改进优化器检测 <literal>WHERE</literal> 子句中变量何时相等的逻辑（Tom）
     </para>

      <para>
      这允许归并连接以降序排序工作，并改进对冗余排序列的识别。
     </para>
     </listitem>

     <listitem>
      <para>
      在大多数表被约束排除的大继承树情形下改进规划性能（Tom）
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title>对象操作</title>
    <itemizedlist>

     <listitem>

      <para>
      组合类型的数组（David Fetter、Andrew、Tom）
     </para>

      <para>
      除显式声明的组合类型的数组外，现在还支持常规表和视图的行类型的数组，但不包括系统目录、序列和 TOAST 表的行类型。
     </para>

     </listitem>

     <listitem>
      <para>
      服务器配置参数现在可以逐函数设置（Tom）
     </para>

      <para>
      例如，函数现在可以设置自己的 <varname>search_path</varname>，以防止运行时存在不同 <varname>search_path</varname> 时出现意外行为。安全定义者函数应设置 <varname>search_path</varname> 以避免安全漏洞。
     </para>
     </listitem>

     <listitem>
      <para>
      <command>CREATE/ALTER FUNCTION</command> 现在支持 <literal>COST</literal> 和 <literal>ROWS</literal> 选项（Tom）
     </para>

      <para>
      <literal>COST</literal> 允许指定函数调用的代价。<literal>ROWS</literal> 允许指定返回集合函数返回的平均行数。这些值由优化器在选择最佳计划时使用。
     </para>
     </listitem>

     <listitem>
      <para>
      实现 <command>CREATE TABLE LIKE ... INCLUDING INDEXES</command>（Trevor Hardcastle、Nikhil Sontakke、Neil）
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <command>CREATE INDEX CONCURRENTLY</command> 忽略其他数据库中的事务（Simon）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <command>ALTER VIEW ... RENAME TO</command> 和 <command>ALTER SEQUENCE ... RENAME TO</command>（David Fetter、Neil）
     </para>

      <para>
      此前这只能通过 <command>ALTER TABLE ... RENAME TO</command> 完成。
     </para>
     </listitem>

     <listitem>
      <para>
      让 <command>CREATE/DROP/RENAME DATABASE</command> 在失败前短暂等待冲突的后端退出（Tom）
     </para>

      <para>
      这提高了这些命令成功的可能性。
     </para>
     </listitem>

     <listitem>
      <para>
      允许使用配置参数成组停用触发器和规则，以用于复制（Jan）
     </para>

      <para>
      这允许复制系统成组禁用触发器和重写规则，而无需直接修改系统目录。该行为由 <command>ALTER TABLE</command> 和新参数 <varname>session_replication_role</varname> 控制。
     </para>
     </listitem>

     <listitem>
      <para>
      用户定义类型现在可以带类型修饰符（Teodor、Tom）
     </para>

      <para>
      这允许用户定义类型带修饰符，例如 <type>ssnum(7)</type>。此前只有内建数据类型可以有修饰符。
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title>工具命令</title>
    <itemizedlist>

     <listitem>
      <para>
      非超级用户的数据库所有者现在默认可以向其数据库添加可信过程语言（Jeremy Drake）
     </para>

      <para>
      虽然这相当安全，但一些管理员可能希望撤销该权限。它由 <structname>pg_pltemplate</structname>.<structfield>tmpldbacreate</structfield> 控制。
     </para>
     </listitem>

     <listitem>
      <para>
      允许把会话的当前参数设置用作未来会话的默认值（Tom）
     </para>

      <para>
      这通过 <command>CREATE/ALTER FUNCTION</command>、<command>ALTER DATABASE</command> 或 <command>ALTER ROLE</command> 中的 <literal>SET ... FROM CURRENT</literal> 完成。
     </para>
     </listitem>

     <listitem>
      <para>
      实现新命令 <command>DISCARD ALL</command>、<command>DISCARD PLANS</command>、<command>DISCARD TEMPORARY</command>、<command>CLOSE ALL</command> 和 <command>DEALLOCATE ALL</command>（Marko Kreen、Neil）
     </para>

      <para>
      这些命令简化了把数据库会话重置为初始状态，对连接池软件特别有用。
     </para>
     </listitem>

     <listitem>
      <para>
      使 <command>CLUSTER</command> MVCC 安全（Heikki Linnakangas）
     </para>

      <para>
      此前，<command>CLUSTER</command> 会丢弃所有已提交为死的元组，即使按照 MVCC 可见性规则仍有事务应当能看到它们。
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <command>CLUSTER</command> 语法：<literal>CLUSTER <replaceable>table</replaceable> USING <replaceable>index</replaceable></literal>（Holger Schurig）
     </para>

      <para>
      旧的 <command>CLUSTER</command> 语法仍受支持，但新形式被认为更合逻辑。
     </para>
     </listitem>

     <listitem>
      <para>
      修复 <command>EXPLAIN</command>，使其能更准确地显示复杂计划（Tom）
     </para>

      <para>
      对子计划输出的引用现在总是正确显示，而不是在复杂情形使用 <literal>?column<replaceable>N</replaceable>?</literal>。
     </para>
     </listitem>

     <listitem>
      <para>
      限制删除用户时报告的信息量（Alvaro）
     </para>

      <para>
      此前，删除（或试图删除）拥有许多对象的用户会产生列出所有这些对象的大型 <literal>NOTICE</literal> 或 <literal>ERROR</literal> 消息；这给一些客户端应用程序带来问题。现在消息长度受限，但完整列表仍会发送到服务器日志。
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title>数据类型</title>
    <itemizedlist>

     <listitem>
      <para>
      支持 SQL/XML 标准，包括新操作符和 <type>XML</type> 数据类型（Nikolay Samokhvalov、Pavel Stehule、Peter）
     </para>
     </listitem>

     <listitem>
      <para>
      枚举数据类型（<type>ENUM</type>）（Tom Dunstan）
     </para>

      <para>
      此特性为有小的固定允许值集合的字段提供便利支持。创建 <literal>ENUM</literal> 类型的例子是 <literal>CREATE TYPE mood AS ENUM ('sad', 'ok', 'happy')</literal>。
     </para>
     </listitem>

     <listitem>
      <para>
      通用唯一标识符（<type>UUID</type>）数据类型（Gevik Babakhani、Neil）
     </para>

      <para>
      这与 <acronym>RFC</acronym> 4122 紧密匹配。
     </para>
     </listitem>

     <listitem>
      <para>
      把 <type>MONEY</type> 数据类型拓宽到 64 位（D'Arcy Cain）
     </para>

      <para>
      这大幅增加了支持的 <type>MONEY</type> 值的范围。
     </para>
     </listitem>

     <listitem>
      <para>
      修复 <type>float4</type>/<type>float8</type>，使其一致地处理 <literal>Infinity</literal> 和 <literal>NAN</literal>（非数字）（Bruce）
     </para>

      <para>
      以前的代码在区分 <literal>Infinity</literal> 与溢出条件方面不一致。
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <type>boolean</type> 值输入时有前导和尾随空白（Neil）
     </para>
     </listitem>

     <listitem>
      <para>
      防止 <command>COPY</command> 用数字和小写字母作为分隔符（Tom）
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title>函数</title>
    <itemizedlist>

     <listitem>
      <para>
      新增正则表达式函数 <function>regexp_matches()</function>、<function>regexp_split_to_array()</function> 和 <function>regexp_split_to_table()</function>（Jeremy Drake、Neil）
     </para>

      <para>
      这些函数提供正则表达式子表达式的提取，并允许用 POSIX 正则表达式拆分字符串。
     </para>
     </listitem>

     <listitem>
      <para>
      新增用于大对象截断的 <function>lo_truncate()</function>（Kris Jurka）
     </para>
     </listitem>

     <listitem>
      <para>
      为 <type>float8</type> 数据类型实现 <function>width_bucket()</function>（Neil）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <function>pg_stat_clear_snapshot()</function>，丢弃当前事务期间收集的统计快照（Tom）
     </para>

      <para>
      事务中第一次请求统计信息时会取一个在事务期间不变的统计快照。此函数允许丢弃该快照并在下一次统计查询期间装载新快照。这对被限制在单个事务中的 PL/pgSQL 函数特别有用。
     </para>
     </listitem>

     <listitem>
      <para>
      为 <function>EXTRACT()</function> 和 <function>date_part()</function> 新增 <literal>isodow</literal> 选项（Bruce）
     </para>

      <para>
      它返回星期几，星期日为七。（<literal>dow</literal> 返回星期日为零。）
     </para>
     </listitem>

     <listitem>
      <para>
      为 <function>to_char()</function>、<function>to_date()</function> 和 <function>to_timestamp()</function> 新增 <literal>ID</literal>（ISO 星期几）和 <literal>IDDD</literal>（ISO 年积日）格式代码（Brendan Jurd）
     </para>
     </listitem>

     <listitem>
      <para>
      让 <function>to_timestamp()</function> 和 <function>to_date()</function> 对可能变宽的字段假定 <literal>TM</literal>（修剪）选项（Bruce）
     </para>

      <para>
      这与 <productname>Oracle</productname> 的行为匹配。
     </para>
     </listitem>

     <listitem>
      <para>
      修复 <function>to_date()</function>/<function>to_timestamp()</function> 的 <literal>D</literal>（非 ISO 星期几）字段中的差一转换错误（Bruce）
     </para>
     </listitem>

     <listitem>
      <para>
      让 <function>setseed()</function> 返回 void，而不是无用的整数值（Neil）
     </para>
     </listitem>

     <listitem>
      <para>
      为 <type>NUMERIC</type> 新增哈希函数（Neil）
     </para>

      <para>
      这允许 <type>NUMERIC</type> 列使用哈希索引和基于哈希的计划。
     </para>
     </listitem>

     <listitem>
      <para>
      改进 <literal>LIKE</literal>/<literal>ILIKE</literal> 的效率，特别是对 UTF-8 等多字节字符集（Andrew、Itagaki Takahiro）
     </para>
     </listitem>

     <listitem>
      <para>
      让 <function>currtid()</function> 函数要求目标表上的 <literal>SELECT</literal> 权限（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      新增若干 <function>txid_*()</function> 函数来查询活动事务 ID（Jan）
     </para>

      <para>
      这对各种复制解决方案很有用。
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title>PL/pgSQL 服务器端语言</title>
    <itemizedlist>

     <listitem>
      <para>
      新增可滚动游标支持，包括 <command>FETCH</command> 中的方向控制（Pavel Stehule）
     </para>
     </listitem>

     <listitem>
      <para>
      为了与后端的 <command>FETCH</command> 命令一致，允许在 PL/pgSQL 的 <command>FETCH</command> 语句中用 <literal>IN</literal> 替代 <literal>FROM</literal>（Pavel Stehule）
     </para>
     </listitem>

     <listitem>
      <para>
      为 PL/pgSQL 新增 <command>MOVE</command>（Magnus、Pavel Stehule、Neil）
     </para>
     </listitem>

     <listitem>
      <para>
      实现 <command>RETURN QUERY</command>（Pavel Stehule、Neil）
     </para>

      <para>
      这为想返回查询结果的 PL/pgSQL 返回集合函数新增了便利语法。<command>RETURN QUERY</command> 比围绕 <command>RETURN NEXT</command> 的循环更容易也更高效。
     </para>
     </listitem>

     <listitem>
      <para>
      允许函数参数名用函数名限定（Tom）
     </para>

      <para>
      例如 <literal>myfunc.myvar</literal>。这在变量名可能与列名匹配的查询中指定变量时特别有用。
     </para>
     </listitem>

     <listitem>
      <para>
      使用块标签限定变量正确工作（Tom）
     </para>

      <para>
      此前，外层块标签可能意外干扰内层记录或行引用的识别。
     </para>
     </listitem>

     <listitem>
      <para>
      收紧对 <literal>FOR</literal> 循环 <literal>STEP</literal> 值的要求（Tom）
     </para>

      <para>
      防止非正的 <literal>STEP</literal> 值，并处理循环溢出。
     </para>
     </listitem>

     <listitem>
      <para>
      改进报告语法错误位置时的准确性（Tom）
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title>其他服务器端语言</title>
    <itemizedlist>

     <listitem>
      <para>
      允许 PL/Perl <function>spi_prepare()</function> 的类型名参数除了 <literal>pg_type</literal> 中的名称外还可以是数据类型别名（Andrew）
     </para>
     </listitem>

     <listitem>
      <para>
      允许 PL/Python <function>plpy.prepare()</function> 的类型名参数除了 <literal>pg_type</literal> 中的名称外还可以是数据类型别名（Andrew）
     </para>
     </listitem>

     <listitem>
      <para>
      允许 PL/Tcl <function>spi_prepare</function> 的类型名参数除了 <literal>pg_type</literal> 中的名称外还可以是数据类型别名（Andrew）
     </para>
     </listitem>

     <listitem>
      <para>
      使 PL/PythonU 能在 Python 2.5 上编译（Marko Kreen）
     </para>
     </listitem>

     <listitem>
      <para>
      在兼容的 Python 版本（Python 2.3 及更高版本）中支持真正的 PL/Python 布尔类型（Marko Kreen）
     </para>
     </listitem>

     <listitem>
      <para>
      修复启用线程的 <filename>libtcl</filename> 在后端内产生多个线程导致的 PL/Tcl 问题（Steve Marshall、Paul Bayer、Doug Knight）
     </para>

      <para>
      这造成了各种各样的麻烦。
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title><link linkend="APP-PSQL"><application>psql</></link></title>
    <itemizedlist>

     <listitem>
      <para>
      在 <literal>\d</literal> 输出中单独列出已禁用的触发器（Brendan Jurd）
     </para>
     </listitem>

     <listitem>
      <para>
      在 <literal>\d</literal> 模式中，始终按字面匹配 <literal>$</literal>（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      在 <literal>\da</literal> 输出中显示聚合返回类型（Greg Sabino Mullane）
     </para>
     </listitem>

     <listitem>
      <para>
      把函数的易变性状态添加到 <literal>\df+</literal> 的输出中（Neil）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <literal>\prompt</literal> 功能（Chad Wagner）
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <literal>\pset</literal>、<literal>\t</literal> 和 <literal>\x</literal> 指定 <literal>on</literal> 或 <literal>off</literal>，而不是仅仅切换（Chad Wagner）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <literal>\sleep</literal> 功能（Jan）
     </para>
     </listitem>

     <listitem>
      <para>
      为 <literal>\copy</literal> 启用 <literal>\timing</literal> 输出（Andrew）
     </para>
     </listitem>

     <listitem>
      <para>
      改进 Windows 上 <literal>\timing</literal> 的分辨率（Itagaki Takahiro）
     </para>
     </listitem>

     <listitem>
      <para>
      在每个反斜杠命令后刷新 <literal>\o</literal> 输出（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      正确检测和报告读取 <literal>-f</literal> 输入文件期间的错误（Peter）
     </para>
     </listitem>

     <listitem>
      <para>
      移除 <literal>-u</literal> 选项（此选项早已废弃）（Tom）
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title><link linkend="APP-PGDUMP"><application>pg_dump</></link></title>
    <itemizedlist>

     <listitem>
      <para>
      为 <application>pg_dumpall</application> 新增 <literal>--tablespaces-only</literal> 和 <literal>--roles-only</literal> 选项（Dave Page）
     </para>
     </listitem>

     <listitem>
      <para>
      为 <application>pg_dumpall</application> 新增输出文件选项（Dave Page）
     </para>

      <para>
      这主要在 Windows 上有用，在那里子 <application>pg_dump</application> 进程的输出重定向不起作用。
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <application>pg_dumpall</application> 接受初始连接数据库名而不是默认的 <literal>template1</literal>（Dave Page）
     </para>
     </listitem>

     <listitem>
      <para>
      在 <literal>-n</literal> 和 <literal>-t</literal> 开关中，始终按字面匹配 <literal>$</literal>（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      改进数据库有数千个对象时的性能（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      移除 <literal>-u</literal> 选项（此选项早已废弃）（Tom）
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title>其他客户端应用程序</title>
    <itemizedlist>

     <listitem>
      <para>
      在 <application>initdb</application> 中，允许指定 <filename>pg_xlog</filename> 目录的位置（Euler Taveira de Oliveira）
     </para>
     </listitem>

     <listitem>
      <para>
      在支持的操作系统上启用 <application>pg_regress</application> 中的服务器核心转储生成（Andrew）
     </para>
     </listitem>

     <listitem>
      <para>
      为 <application>pg_ctl</application> 新增 <literal>-t</literal>（超时）参数（Bruce）
     </para>

      <para>
      这控制 <application>pg_ctl</application> 等待服务器启动或关闭的时间。此前超时被硬编码为 60 秒。
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <application>pg_ctl</application> 选项来控制服务器核心转储的生成（Andrew）
     </para>
     </listitem>

     <listitem>
      <para>
      允许用 Control-C 取消 <application>clusterdb</application>、<application>reindexdb</application> 和 <application>vacuumdb</application>（Itagaki Takahiro、Magnus）
     </para>
     </listitem>

     <listitem>
      <para>
      抑制 <application>createdb</application>、<application>createuser</application>、<application>dropdb</application> 和 <application>dropuser</application> 的命令标签输出（Peter）
     </para>

      <para>
      <literal>--quiet</literal> 选项被忽略并将在 8.4 中移除。对所有数据库操作时的进度消息现在发送到 stdout 而不是 stderr，因为它们实际上不是错误。
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title><link linkend="libpq"><application>libpq</></link></title>
    <itemizedlist>

     <listitem>
      <para>
      如果 <function>PQsetdbLogin()</function> 的 <literal>dbName</literal> 参数包含等号，把它解释为 <literal>conninfo</literal> 字符串（Andrew）
     </para>

      <para>
      这允许仍在使用 <literal>PQsetdbLogin()</literal> 的客户端程序使用 <literal>conninfo</literal> 字符串。
     </para>
     </listitem>

     <listitem>
      <para>
      支持全局 <acronym>SSL</acronym> 配置文件（Victor Wagner）
     </para>
     </listitem>

     <listitem>
      <para>
      新增环境变量 <varname>PGSSLKEY</varname> 来控制 <acronym>SSL</acronym> 硬件密钥（Victor Wagner）
     </para>
     </listitem>

     <listitem>
      <para>
      新增用于大对象截断的 <function>lo_truncate()</function>（Kris Jurka）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <function>PQconnectionNeedsPassword()</function>，如果服务器要求口令但未提供则返回真（Joe Conway、Tom）
     </para>

      <para>
      如果在连接尝试失败后它返回真，客户端应用程序应提示用户输入口令。过去应用程序必须检查特定的错误消息字符串来决定是否需要口令；该方法现已废弃。
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <function>PQconnectionUsedPassword()</function>，如果提供的口令确实被使用则返回真（Joe Conway、Tom）
     </para>

      <para>
      这在某些需要知道用户提供的口令是否确实有效的安全上下文中很有用。
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title><link linkend="ecpg"><application>ecpg</></link></title>
    <itemizedlist>

     <listitem>
      <para>
      使用 V3 前端/后端协议（Michael）
     </para>

      <para>
      这新增了对服务器端预备语句的支持。
     </para>
     </listitem>

     <listitem>
      <para>
      在 Windows 上使用本机线程而不是 pthreads（Magnus）
     </para>
     </listitem>

     <listitem>
      <para>
      改进 ecpglib 的线程安全（Itagaki Takahiro）
     </para>
     </listitem>

     <listitem>
      <para>
      让 ecpg 库只导出必要的 API 符号（Michael）
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title><application>Windows</> Port</title>
    <itemizedlist>

     <listitem>
      <para>
      允许整个 <productname>PostgreSQL</productname> 发行版用 <productname>Microsoft Visual C++</productname> 编译（Magnus 等）
     </para>

      <para>
      这使基于 Windows 的开发者可以使用熟悉的开发和调试工具。用 Visual C++ 制作的 Windows 可执行文件也可能比用其他工具集制作的具有更好的稳定性和性能。仅客户端的 Visual C++ 构建脚本已被移除。
     </para>
     </listitem>

     <listitem>
      <para>
      大幅降低 postmaster 有许多子进程时的内存使用（Magnus）
     </para>
     </listitem>

     <listitem>
      <para>
      允许由管理用户启动回归测试（Magnus）
     </para>
     </listitem>

     <listitem>
      <para>
      新增本机共享内存实现（Magnus）
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title>Server Programming Interface (<acronym>SPI</>)</title>
    <itemizedlist>

     <listitem>
      <para>
      在 SPI 中新增游标相关功能（Pavel Stehule）
     </para>

      <para>
      允许访问与游标相关的规划选项，并新增 <command>FETCH</command>/<command>MOVE</command> 例程。
     </para>
     </listitem>

     <listitem>
      <para>
      允许通过 <function>SPI_execute</function> 执行游标命令（Tom）
     </para>

      <para>
      宏 <literal>SPI_ERROR_CURSOR</literal> 仍然存在但永远不会被返回。
     </para>
     </listitem>

     <listitem>
      <para>
      SPI 计划指针现在声明为 <literal>SPIPlanPtr</literal> 而不是 <literal>void *</literal>（Tom）
     </para>

      <para>
      这不会破坏应用程序代码，但建议切换以帮助捕获简单的编程错误。
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title>构建选项</title>
    <itemizedlist>

     <listitem>
      <para>
      新增 <application>configure</application> 选项 <literal>--enable-profiling</literal> 以启用代码剖析（只与 <application>gcc</application> 一起工作）（Korry Douglas 和 Nikhil Sontakke）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <application>configure</application> 选项 <literal>--with-system-tzdata</literal> 以使用操作系统的时区数据库（Peter）
     </para>
     </listitem>

     <listitem>
      <para>
      修复 <acronym>PGXS</acronym>，使扩展可以针对 <application>pg_config</application> 程序不在 <varname>PATH</varname> 最前面的 PostgreSQL 安装构建（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      构建 <acronym>SGML</acronym> 文档时支持 <command>gmake draft</command>（Bruce）
     </para>

      <para>
      除非使用 <literal>draft</literal>，文档构建现在会在必要时重复进行以确保索引是最新的。
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title>源代码</title>
    <itemizedlist>

     <listitem>
      <para>
      把宏 <literal>DLLIMPORT</literal> 改名为 <literal>PGDLLIMPORT</literal>，以避免与定义 <literal>DLLIMPORT</literal> 的第三方包含（如 Tcl）冲突（Magnus）
     </para>
     </listitem>

     <listitem>
      <para>
      创建<quote>操作符族</quote>以改进涉及跨数据类型比较的查询的规划（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      更新 GIN <function>extractQuery()</function> API，允许发出没有任何东西能满足查询的信号（Teodor）
     </para>
     </listitem>

     <listitem>
      <para>
      把 <literal>NAMEDATALEN</literal> 定义从 <filename>postgres_ext.h</filename> 移到 <filename>pg_config_manual.h</filename>（Peter）
     </para>
     </listitem>

     <listitem>
      <para>
      在所有平台上提供 <function>strlcpy()</function> 和 <function>strlcat()</function>，并替换易出错的 <function>strncpy()</function>、<function>strncat()</function> 等用法（Peter）
     </para>
     </listitem>

     <listitem>
      <para>
      创建钩子，让外部插件监控（甚至替换）规划器并为假想情形创建计划（Gurjeet Singh、Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      创建函数变量 <literal>join_search_hook</literal>，让插件覆盖规划器的连接搜索顺序部分（Julius Stroffek）
     </para>
     </listitem>

     <listitem>
      <para>
      为 Renesas 的 M32R 处理器新增 <function>tas()</function> 支持（Kazuhiro Inaoka）
     </para>
     </listitem>

     <listitem>
      <para>
      <function>quote_identifier()</function> 和 <application>pg_dump</application> 不再对按语法属于非保留的关键字加引号（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      更改 <type>NUMERIC</type> 数据类型的磁盘表示，使 <structfield>sign_dscale</structfield> 字位于权重之前（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      在 Darwin &gt;= 6.0（即 OS X 10.2 及以上）上使用 <acronym>SYSV</acronym> 信号量而不是 POSIX 信号量（Chris Marcellino）
     </para>
     </listitem>

     <listitem>
      <para>
      新增<link linkend="acronyms">缩写词</link>和<link linkend="creating-cluster-nfs">NFS</link>文档节（Bruce）
     </para>
     </listitem>

     <listitem>
      <para>
      文档现在记录 "Postgres" 是 "PostgreSQL" 的可接受别名（Peter）
     </para>
     </listitem>

     <listitem>
      <para>
      新增关于在服务器停机时防止数据库服务器欺骗的文档（Bruce）
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title>附加模块</title>
    <itemizedlist>

     <listitem>
      <para>
      把 <filename>contrib</filename> 的 <filename>README</filename> 内容移入 <productname>PostgreSQL</productname> 主文档（Albert Cervera i Areny）
     </para>
     </listitem>

     <listitem>
      <para>
      新增用于低层页检查的 <filename>contrib/pageinspect</filename> 模块（Simon、Heikki）
     </para>
     </listitem>

     <listitem>
      <para>
      新增用于控制热备操作的 <filename>contrib/pg_standby</filename> 模块（Simon）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <filename>contrib/uuid-ossp</filename> 模块，用 OSSP UUID 库生成 <type>UUID</type> 值（Peter）
     </para>

      <para>
      用 <application>configure</application> 的 <literal>--with-ossp-uuid</literal> 激活。它利用了新的内建 <type>UUID</type> 类型。
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <filename>contrib/dict_int</filename>、<filename>contrib/dict_xsyn</filename> 和 <filename>contrib/test_parser</filename> 模块，提供附加文本搜索词典模板和解析器的示例（Sergey Karpov）
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <application>contrib/pgbench</application> 设置填充因子（Pavan Deolasee）
     </para>
     </listitem>

     <listitem>
      <para>
      为 <application>contrib/pgbench</application> 的 <literal>-l</literal> 添加时间戳（Greg Smith）
     </para>
     </listitem>

     <listitem>
      <para>
      为 <filename>contrib/pgbuffercache</filename> 新增使用计数统计（Greg Smith）
     </para>
     </listitem>

     <listitem>
      <para>
      为 <filename>contrib/hstore</filename> 新增 GIN 支持（Teodor）
     </para>
     </listitem>

     <listitem>
      <para>
      为 <filename>contrib/pg_trgm</filename> 新增 GIN 支持（Guillaume Smet、Teodor）
     </para>
     </listitem>

     <listitem>
      <para>
      更新 <filename>contrib/start-scripts</filename> 中的 OS/X 启动脚本（Mark Cotner、David Fetter）
     </para>
     </listitem>

     <listitem>
      <para>
      把 <function>pgrowlocks()</function> 和 <function>dblink_get_pkey()</function> 限制为对目标表有 <literal>SELECT</literal> 权限的用户（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      把 <filename>contrib/pgstattuple</filename> 函数限制为超级用户（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      <filename>contrib/xml2</filename> 已废弃，计划在 8.4 中移除（Peter）
     </para>

      <para>
      核心 PostgreSQL 中的新 XML 支持取代了此模块。
     </para>
     </listitem>

    </itemizedlist>

   </sect3>
  </sect2>
 </sect1>
