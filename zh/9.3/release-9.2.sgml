<!-- doc/src/sgml/release-9.2.sgml -->
<!-- See header comment in release.sgml about typical markup -->

 <sect1 id="release-9-2-24">
  <title>版本 9.2.24</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2017-11-09</para>
  </formalpara>

  <para>
   本次发布包含对 9.2.23 的多方面修复。有关 9.2 大版本中新特性的信息，请参见<xref linkend="release-9-2">。.
  </para>

  <para>
    这预计是 <productname>PostgreSQL</productname> 9.2.X 系列的最后一个发行版。建议用户尽快更新到较新的发行分支。
   </para>

  <sect2>
   <title>迁移到版本 9.2.24</title>

   <para>
    对于运行 9.2.X 的用户，不需要进行转储/恢复。</para>

   <para>
    不过，如果你是从早于 9.2.22 的版本升级，请参见<xref linkend="release-9-2-22">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      修复示例服务器启动脚本在打开 <literal>$PGLOG</literal> 之前变成
      <literal>$PGUSER</literal>（Noah Misch）
     </para>

     <para>
      此前，postmaster 日志文件是在仍以 root 运行时打开的。因此数据库所有者可以通过让 <literal>$PGLOG</literal> 成为指向某个其他文件的符号链接来攻击另一个系统用户，该文件随后会因追加日志消息而被损坏。
     </para>

     <para>
      默认情况下这些脚本不会安装到任何地方。使用了它们的用户需要手动重新复制，或对其修改版应用相同的更改。如果现有的
      <literal>$PGLOG</literal> 文件为 root 所有，在用修正后的脚本重启服务器之前需要把它移开或重命名。（CVE-2017-12172）
     </para>
    </listitem>

    <listitem>
     <para>
      正确拒绝把无穷大浮点值转换为 <type>numeric</type> 类型的尝试（Tom Lane、KaiGai Kohei）
     </para>

     <para>
      此前行为是与平台相关的。
     </para>
    </listitem>

    <listitem>
     <para>
      修复在视图末尾添加列后的边界崩溃（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      在视图或规则包含 <structname>FieldSelect</structname> 或
      <structname>FieldStore</structname> 表达式节点时记录正确的依赖（Tom Lane）
     </para>

     <para>
      缺少这些依赖可能允许本应失败的列或数据类型 <command>DROP</command>
      成功执行，从而导致之后使用该视图或规则时出错。此补丁不会保护现有视图/规则，只保护将来创建的。
     </para>
    </listitem>

    <listitem>
     <para>
      正确检测范围数据类型的可哈希性（Tom Lane）
     </para>

     <para>
      规划器错误地假定任何范围类型都可以哈希用于哈希连接或哈希聚合，但实际上必须检查范围的子类型是否有哈希支持。这不影响任何内置范围类型，因为它们反正都是可哈希的。
     </para>
    </listitem>

    <listitem>
     <para>
      修复因 XID 回卷导致的低概率 <command>NOTIFY</command> 消息丢失（Marko Tiikkaja、Tom Lane）
     </para>

     <para>
      如果一个会话在超过 20 亿个事务期间不执行查询而只是监听通知，它就开始遗漏来自并发提交事务的一些通知。
     </para>
    </listitem>

    <listitem>
     <para>
      防止嵌套触发器触发处理中的低概率崩溃（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      <command>COPY</command> 或 <function>lo_export()</function> 中文件创建失败时正确恢复 umask 设置（Peter Eisentraut）
     </para>
    </listitem>

    <listitem>
     <para>
      为 <command>ANALYZE</command> 中的重复列名给出更好的错误消息（Nathan Bossart）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>libpq</application> 不要求用户的主目录存在（Tom Lane）
     </para>

     <para>
      在 v10 中，尝试读取 <filename>~/.pgpass</filename> 时找不到主目录被当作硬错误，但它应该只是导致找不到该文件。v10 和以前的发布分支在读取 <filename>~/.pg_service.conf</filename> 时犯了同样的错误，不过这不太明显，因为除非指定了服务名否则不寻找该文件。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>libpq</application> 防范
      <structname>PGresult</structname> 行数中的整数溢出（Michael Paquier）
     </para>
    </listitem>

    <listitem>
     <para>
      把我们的时区库副本与 IANA tzcode2017c 同步（Tom Lane）
     </para>

     <para>
      这修复了若干问题；唯一可能对用户可见的是，当时区数据目录中没有
      <filename>posixrules</filename> 文件时，POSIX 风格时区名的默认夏令时规则现在匹配当前美国法律而不是十几年前的。
     </para>
    </listitem>

    <listitem>
     <para>
      更新时区数据文件到 <application>tzdata</application> 版本 2017c，涉及斐济、纳米比亚、北塞浦路斯、苏丹、汤加以及特克斯和凯科斯群岛的夏令时法规变更，加上阿拉斯加、阿皮亚、缅甸、加尔各答、底特律、爱尔兰、纳米比亚和帕果帕果的历史修正。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-9-2-23">
  <title>版本 9.2.23</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2017-08-31</para>
  </formalpara>

  <para>
   本次发布包含对 9.2.22 的少量修复。有关 9.2 大版本中新特性的信息，请参见<xref linkend="release-9-2">。.
  </para>

  <para>
    <productname>PostgreSQL</productname> 社区将于 2017 年 9 月停止对 9.2.X 版本系列的更新。建议用户尽快更新到较新的版本分支。
   </para>

  <sect2>
   <title>迁移到版本 9.2.23</title>

   <para>
    对于运行 9.2.X 的用户，不需要进行转储/恢复。</para>

   <para>
    不过，如果你是从早于 9.2.22 的版本升级，请参见<xref linkend="release-9-2-22">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      在 <structname>information_schema</structname>.<structname>table_privileges</structname>
      视图中显示外部表（Peter Eisentraut）
     </para>

     <para>
      所有其他相关的 <structname>information_schema</structname> 视图都包含外部表，只有这一个忽略了它们。
     </para>

     <para>
      由于这个视图定义是由 <application>initdb</application> 安装的，仅升级不会修复问题。如果需要在现有安装中修复，可以以超级用户身份在
      <application>psql</application> 中执行以下操作：<programlisting>SET search_path TO information_schema;
CREATE OR REPLACE VIEW table_privileges AS
SELECT CAST(u_grantor.rolname AS sql_identifier) AS grantor,
       CAST(grantee.rolname AS sql_identifier) AS grantee,
       CAST(current_database() AS sql_identifier) AS table_catalog,
       CAST(nc.nspname AS sql_identifier) AS table_schema,
       CAST(c.relname AS sql_identifier) AS table_name,
       CAST(c.prtype AS character_data) AS privilege_type,
       CAST(
         CASE WHEN
          -- object owner always has grant options
          pg_has_role(grantee.oid, c.relowner, 'USAGE')
          OR c.grantable
          THEN 'YES' ELSE 'NO' END AS yes_or_no) AS is_grantable,
       CAST(CASE WHEN c.prtype = 'SELECT' THEN 'YES' ELSE 'NO' END AS yes_or_no) AS with_hierarchy
FROM (
       SELECT oid, relname, relnamespace, relkind, relowner,
              (aclexplode(coalesce(relacl, acldefault('r', relowner))).*
       FROM pg_class ) AS c (oid, relname, relnamespace, relkind, relowner, grantor, grantee, prtype, grantable),
     pg_namespace nc, pg_authid u_grantor,
     (
       SELECT oid, rolname FROM pg_authid
       UNION ALL
       SELECT 0::oid, 'PUBLIC'
     ) AS grantee (oid, rolname)
WHERE c.relnamespace = nc.oid
      AND c.relkind IN ('r', 'v', 'f')
      AND c.grantee = grantee.oid
      AND c.grantor = u_grantor.oid
      AND c.prtype IN ('INSERT', 'SELECT', 'UPDATE', 'DELETE', 'TRUNCATE', 'REFERENCES', 'TRIGGER')
      AND (pg_has_role(u_grantor.oid, 'USAGE')
           OR pg_has_role(grantee.oid, 'USAGE')
           OR grantee.rolname = 'PUBLIC');</programlisting>
      这必须在每个要修复的数据库中重复，包括
      <literal>template0</literal>。
     </para>
    </listitem>

    <listitem>
     <para>
      清理尝试对失败事务执行 <command>ROLLBACK</command> 时发生致命退出（例如因收到 <systemitem>SIGTERM</systemitem>）的处理（Tom Lane）
     </para>

     <para>
      这种情况可能导致断言失败。在生产构建中退出仍会发生，但会记录一条关于
      <quote>cannot drop active portal</quote> 的意外消息。
     </para>
    </listitem>

    <listitem>
     <para>
      移除致命退出期间可能触发的断言（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      正确识别范围类型或基于被搜索复合类型或域类型的域类型之上的列（Tom Lane）
     </para>

     <para>
      某些更改复合类型或域类型定义的 <command>ALTER</command> 命令本应在该类型有存储值时失败，因为它们缺少更新或检查这类值所需的基础设施。此前这些检查可能遗漏包裹在范围类型或子域中的相关值，可能允许数据度变得不一致。
     </para>
    </listitem>

    <listitem>
     <para>
      更改 <application>ecpg</application> 的解析器允许不带 C 变量的
      <literal>RETURNING</literal> 子句（Michael Meskes）
     </para>

     <para>
      这允许 <application>ecpg</application> 程序包含在内部使用
      <literal>RETURNING</literal>（例如在 CTE 内）而不是用它定义返回给客户端的值的 SQL 构造。
     </para>
    </listitem>

    <listitem>
     <para>
      改进 Windows 上 PL/Perl 编译器标志的选择（Tom Lane）
     </para>

     <para>
      此修复避免了因对 <type>time_t</type> 值宽度的假定不一致而可能的
      PL/Perl 崩溃。扩展开发者可能看到的一个副作用是
      <literal>_USE_32BIT_TIME_T</literal> 不再在
      <productname>PostgreSQL</productname> Windows 构建中全局定义。这预计不会引起问题，因为 <type>time_t</type> 类型没有用于任何
      <productname>PostgreSQL</productname> API 定义。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-9-2-22">
  <title>版本 9.2.22</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2017-08-10</para>
  </formalpara>

  <para>
   本次发布包含对 9.2.21 的多方面修复。有关 9.2 大版本中新特性的信息，请参见<xref linkend="release-9-2">。.
  </para>

  <para>
    <productname>PostgreSQL</productname> 社区将于 2017 年 9 月停止对 9.2.X 版本系列的更新。建议用户尽快更新到较新的版本分支。
   </para>

  <sect2>
   <title>迁移到版本 9.2.22</title>

   <para>
    对于运行 9.2.X 的用户，不需要进行转储/恢复。</para>

   <para>
     但是，如果你使用的外部数据服务器依赖用户密码进行认证，请参见下面的第一条变更日志条目。
    </para>

   <para>
    不过，如果你是从早于 9.2.20 的版本升级，请参见<xref linkend="release-9-2-20">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      进一步限制
      <structname>pg_user_mappings</structname>.<structfield>umoptions</structfield>
      的可见性，以保护存储为用户映射选项的密码（Noah Misch）
     </para>

     <para>
      CVE-2017-7486 的修复不正确：它允许用户看到她自己用户映射中的选项，即使她对相关外部服务器没有 <literal>USAGE</literal> 权限。这种选项可能包含由服务器所有者而非用户自己提供的密码。由于
      <structname>information_schema.user_mapping_options</structname>
      在这种情况下不显示选项，<structname>pg_user_mappings</structname>
      也不应该。（CVE-2017-7547）
     </para>

     <para>
      此补丁本身只会修复新 initdb 的数据库中的行为。如果希望在现有数据库中应用此更改，需要执行以下操作：
     </para>

     <procedure>
      <step>
       <para>
      在 <filename>postgresql.conf</filename> 中添加
      <literal>allow_system_table_mods = true</literal> 后重启 postmaster。（在支持 <command>ALTER SYSTEM</command> 的版本中，可以用它进行配置更改，但仍需重启。）
     </para>
      </step>

      <step>
       <para>
      在集簇的<emphasis>每个</emphasis>数据库中，以超级用户身份运行以下命令：<programlisting>SET search_path = pg_catalog;
CREATE OR REPLACE VIEW pg_user_mappings AS
SELECT U.oid AS umid, S.oid AS srvid, S.srvname AS srvname,
       U.umuser AS umuser,
       CASE WHEN U.umuser = 0 THEN 'public' ELSE A.rolname END AS usename,
       CASE WHEN (U.umuser &lt;&gt; 0 AND A.rolname = current_user
                   AND (pg_has_role(S.srvowner, 'USAGE')
                        OR has_server_privilege(S.oid, 'USAGE')))
                OR (U.umuser = 0 AND pg_has_role(S.srvowner, 'USAGE'))
                OR (SELECT rolsuper FROM pg_authid WHERE rolname = current_user)
            THEN U.umoptions ELSE NULL END AS umoptions
FROM pg_user_mapping U
     LEFT JOIN pg_authid A ON (A.oid = U.umuser)
     JOIN pg_foreign_server S ON (U.umserver = S.oid);</programlisting>
     </para>
      </step>

      <step>
       <para>
      不要忘记包括 <literal>template0</literal> 和
      <literal>template1</literal> 数据库，否则漏洞仍会存在于之后创建的数据库中。要修复 <literal>template0</literal>，需要暂时让它接受连接。在 <productname>PostgreSQL</productname> 9.5 及之后，可以使用 <programlisting>ALTER DATABASE template0 WITH ALLOW_CONNECTIONS true;</programlisting>
      修复 <literal>template0</literal> 之后再撤销：
      <programlisting>ALTER DATABASE template0 WITH ALLOW_CONNECTIONS false;</programlisting>
      在更早的版本中，改用：
      <programlisting>UPDATE pg_database SET datallowconn = true WHERE datname = 'template0';
UPDATE pg_database SET datallowconn = false WHERE datname = 'template0';</programlisting>
     </para>
      </step>

      <step>
       <para>
      最后，移除 <literal>allow_system_table_mods</literal> 配置设置，并再次重启 postmaster。
     </para>
      </step>
     </procedure>
    </listitem>

    <listitem>
     <para>
      禁止在所有基于密码的认证方法中使用空密码（Heikki Linnakangas）
     </para>

     <para>
      <application>libpq</application> 忽略空的密码说明，不把它们传输给服务器。因此，如果用户的密码被设为空字符串，就无法通过
      <application>psql</application> 或其他基于
      <application>libpq</application> 的客户端用该密码登录。管理员可能因此认为把密码设为空等同于禁用密码登录。然而，使用修改过的或非
      <application>libpq</application> 的客户端，登录可能是可能的，这取决于配置的认证方法。特别是最常见的方法 <literal>md5</literal> 接受空密码。更改服务器在所有情况下拒绝空密码。（CVE-2017-7546）
     </para>
    </listitem>

    <listitem>
     <para>
      在 Windows 上，如果在新进程中为共享内存保留地址范围失败，则重试进程创建（Tom Lane、Amit Kapila）
     </para>

     <para>
      这有望修复可能由杀毒产品干扰导致的偶发子进程启动失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 Windows 构建中共享谓词锁哈希表的低概率损坏（Thomas Munro、Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      避免把 SSL 连接的干净关闭当作连接重置来记录（Michael Paquier）
     </para>
    </listitem>

    <listitem>
     <para>
      防止向客户端发送 SSL 会话票据（Tom Lane）
     </para>

     <para>
      此修复防止使用票据感知的客户端 SSL 代码时的重连失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 Solaris 上设置<xref linkend="guc-tcp-keepalives-idle">的代码（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复统计收集器处理 postmaster 关闭并立即重启之后发出的查询消息（Tom Lane）
     </para>

     <para>
      在上一个 postmaster 关闭之后半秒内发出的统计查询实际上被忽略。
     </para>
    </listitem>

    <listitem>
     <para>
      确保统计收集器的接收缓冲区大小至少为 100KB（Tom Lane）
     </para>

     <para>
      这降低了默认接收缓冲区大小小于该值的老平台上统计数据丢失的风险。
     </para>
    </listitem>

    <listitem>
     <para>
      修复备库刚处理完 <literal>XLOG_SWITCH</literal> WAL 记录后被提升时可能创建无效 WAL 段的问题（Andres Freund）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 walsender 进程中的 <systemitem>SIGHUP</systemitem> 和
      <systemitem>SIGUSR1</systemitem> 处理（Petr Jelinek、Andres Freund）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 postmaster 中竞态条件导致的
      <application>walreceiver</application> 进程不必要的缓慢重启（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <command>INSERT</command> 或 <command>UPDATE</command> 对数组之上域类型的列的多个元素赋值的情况（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      在 <command>ALTER ... RENAME</command> 期间把自动生成的数组类型移开（Vik Fearing）
     </para>

     <para>
      此前我们在 <command>CREATE</command> 期间会把冲突的自动生成数组类型重命名移开；此修复把该行为扩展到重命名操作。
     </para>
    </listitem>

    <listitem>
     <para>
      确保 <command>ALTER USER ... SET</command> 接受
      <command>ALTER ROLE ... SET</command> 接受的所有语法变体（Peter Eisentraut）
     </para>
    </listitem>

    <listitem>
     <para>
      把数据类型 I/O 函数的参数或返回类型从 <type>opaque</type> 更改为正确类型时正确更新依赖信息（Heikki Linnakangas）
     </para>

     <para>
      <command>CREATE TYPE</command> 会更新以这种早已过时风格声明的 I/O
      函数，但忘记记录对类型的依赖，允许随后的 <command>DROP TYPE</command>
      留下损坏的函数定义。
     </para>
    </listitem>

    <listitem>
     <para>
      减少 <command>ANALYZE</command> 处理 <type>tsvector</type> 列时的内存使用（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <type>money</type> 值乘以或除以整数或浮点数时不必要的精度丢失和草率舍入（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      收紧对解析标识符的函数（如
      <function>regprocedurein()</function>）中空白的检查（Tom Lane）
     </para>

     <para>
      根据当前区域，这些函数可能把多字节字符的片段误解释为空白。
     </para>
    </listitem>

    <listitem>
     <para>
      编译 <application>PL/Perl</application> 时使用来自 Perl 的相关
      <literal>#define</literal> 符号（Ashutosh Sharma、Tom Lane）
     </para>

     <para>
      这避免了可移植性问题（通常表现为库加载期间的<quote>握手</quote>不匹配），在使用最近 Perl 版本时。
     </para>
    </listitem>

    <listitem>
     <para>
      在 <application>psql</application> 中，修复
      <command>COPY FROM STDIN</command> 以键盘 EOF 信号结束后再尝试另一个 <command>COPY FROM STDIN</command> 时失败的问题（Thomas Munro）
     </para>

     <para>
      这种错误行为在 BSD 衍生平台（包括 macOS）上观察到，但在大多数其他平台上没有。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_dump</application> 不再为空操作符类发出无效 SQL（Daniel Gustafsson）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_dump</application> 在 Windows 上输出到 stdout（Kuntal Ghosh）
     </para>

     <para>
      由于未把文件描述符置为二进制模式，写到 stdout 的压缩纯文本转储会包含损坏的数据。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>pg_get_ruledef()</function> 为列被重命名的视图的
      <literal>ON SELECT</literal> 规则打印正确输出（Tom Lane）
     </para>

     <para>
      某些边界情况下 <application>pg_dump</application> 依靠
      <function>pg_get_ruledef()</function> 转储视图，因此这个错误可能导致转储/恢复失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>FROM</literal> 子句中函数表达式不能反解析为看起来像函数调用的东西时的转储（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_basebackup</application> 在 Windows 上输出到
      stdout（Haribabu Kommi）
     </para>

     <para>
      由于未把文件描述符置为二进制模式，写到 stdout 的备份会包含损坏的数据。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_upgrade</application> 确保结束 WAL 记录不具有<xref linkend="guc-wal-level"> = <literal>minimum</literal>（Bruce Momjian）
     </para>

     <para>
      这一条件可能阻止升级后的备库服务器重新连接。
     </para>
    </listitem>

    <listitem>
     <para>
      用 gcc 构建共享库时始终使用 <option>-fPIC</option> 而不是
      <option>-fpic</option>（Tom Lane）
     </para>

     <para>
      这在有差别的平台上支持更大的扩展库。
     </para>
    </listitem>

    <listitem>
     <para>
      修复我们的 Microsoft MSVC 构建脚本中花括号未转义的问题，以避免较新的 Perl 版本发出警告或报错（Andrew Dunstan）
     </para>
    </listitem>

    <listitem>
     <para>
      在 MSVC 构建中，处理 <application>openssl</application> 库不在
      <filename>VC</filename> 子目录内的情况（Andrew Dunstan）
     </para>
    </listitem>

    <listitem>
     <para>
      在 MSVC 构建中，为 <application>libxml2</application> 头文件添加正确的包含路径（Andrew Dunstan）
     </para>

     <para>
      这修复了以前需要移动标准 Windows 安装的
      <application>libxml2</application> 中某些东西的需要。
     </para>
    </listitem>

    <listitem>
     <para>
      在 MSVC 构建中，识别名为 <filename>tcl86.lib</filename> 的 Tcl 库（Noah Misch）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-9-2-21">
  <title>版本 9.2.21</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2017-05-11</para>
  </formalpara>

  <para>
   本次发布包含对 9.2.20 的多方面修复。有关 9.2 大版本中新特性的信息，请参见<xref linkend="release-9-2">。.
  </para>

  <para>
    <productname>PostgreSQL</productname> 社区将于 2017 年 9 月停止对 9.2.X 版本系列的更新。建议用户尽快更新到较新的版本分支。
   </para>

  <sect2>
   <title>迁移到版本 9.2.21</title>

   <para>
    对于运行 9.2.X 的用户，不需要进行转储/恢复。</para>

   <para>
     但是，如果你使用的外部数据服务器依赖用户密码进行认证，请参见下面的第一条变更日志条目。
    </para>

   <para>
    不过，如果你是从早于 9.2.20 的版本升级，请参见<xref linkend="release-9-2-20">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
     限制<structname>pg_user_mappings</structname>.<structfield>umoptions</structfield>的可见性，以保护作为用户映射选项存储的密码（Michael Paquier、Feike Steenbergen）
    </para>

     <para>
     原来的编码允许外部服务器对象的所有者，或任何被他授予服务器 <literal>USAGE</literal> 权限的人，看到与该服务器关联的所有用户映射的选项。这很可能包括其他用户的密码。调整视图定义以匹配 <structname>information_schema.user_mapping_options</structname> 的行为，即这些选项对被映射的用户可见，或者当映射针对 <literal>PUBLIC</literal> 且当前用户是服务器所有者时，或者当前用户是超级用户时可见。（CVE-2017-7486）
    </para>

     <para>
      此补丁本身只会修复新 initdb 的数据库中的行为。如果希望在现有数据库中应用此更改，请按照<xref linkend="release-9-2-22">中 CVE-2017-7547 变更记录里给出的更正程序操作。
     </para>
    </listitem>

    <listitem>
     <para>
      防止通过泄漏型操作符暴露统计信息（Peter Eisentraut）
     </para>

     <para>
      规划器的一些选择率估计函数会对从 <structname>pg_statistic</structname>
      获得的值（如最常见值和直方图条目）应用用户定义操作符。这发生在表权限检查之前，因此恶意用户可以利用这一行为获得他无权读取的表列的这些值。为修复，当操作符的实现函数未被证明防泄漏且调用用户无权读取所需统计信息的表列时，回退到默认估计。实践中大多数情况下至少满足这些准则之一。（CVE-2017-7484）
     </para>
    </listitem>

    <listitem>
     <para>
      修复未记录索引的<quote>init fork</quote>可能的损坏（Robert Haas、Michael Paquier）
     </para>

     <para>
      这可能导致未记录索引在崩溃和重启之后被置为无效状态。这种问题将持续到索引被删除并重建为止。
     </para>
    </listitem>

    <listitem>
     <para>
      修复备库重放已预备但未提交的两阶段事务时
      <structname>pg_subtrans</structname> 条目的错误重建（Tom Lane）
     </para>

     <para>
      大多数情况下这没有可见的不良影响，但在边界情况下可能导致
      <structname>pg_subtrans</structname> 中的循环引用，可能使检查两阶段事务所修改行的查询陷入无限循环。
     </para>
    </listitem>

    <listitem>
     <para>
      确保扩展脚本中查询的解析能看到紧邻的前一条 DDL 的结果（Julien Rouhaud、Tom Lane）
     </para>

     <para>
      由于扩展脚本文件中命令之间缺少缓存刷新步骤，非实用查询可能看不到紧邻的目录更改（如 <command>ALTER TABLE ... RENAME</command>）的效果。
     </para>
    </listitem>

    <listitem>
     <para>
      当 <command>ALTER TABLE ... ALTER COLUMN TYPE</command> 重建现有索引时跳过表空间权限检查（Noah Misch）
     </para>

     <para>
      如果调用用户当前对包含索引的表空间没有 <literal>CREATE</literal>
      权限，命令就会失败。这种行为似乎无益，因此跳过检查，允许索引在原地重建。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <command>ALTER TABLE ... VALIDATE CONSTRAINT</command> 在约束被标记 <literal>NO INHERIT</literal> 时不递归到子表（Amit Langote）
     </para>

     <para>
      此修复防止子表中不存在匹配约束时不需要的 <quote>constraint does not
      exist</quote> 失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <command>VACUUM</command> 正确考虑因页 pin 冲突而无法扫描的页（Andrew Gierth）
     </para>

     <para>
      这往往导致低估表中的元组数。在小型高争用表的最坏情况下，<command>VACUUM</command> 可能错误地报告表不包含任何元组，导致非常糟糕的计划选择。
     </para>
    </listitem>

    <listitem>
     <para>
      确保哈希连接中的批量元组传输循环可被查询取消请求中断（Tom Lane、Thomas Munro）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>cursor_to_xml()</function> 在
      <replaceable>tableforest</replaceable> = false 时产生有效输出（Thomas Munro、Peter Eisentraut）
     </para>

     <para>
      此前它未能产生包裹的 <literal>&lt;table&gt;</literal> 元素。
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <structname>pg_timezone_names</structname> 视图的性能（Tom Lane、David Rowley）
     </para>
    </listitem>

    <listitem>
     <para>
      修复对 <function>lseek()</function> 和 <function>close()</function>
      边界情况错误的草率处理（Tom Lane）
     </para>

     <para>
      这两个系统调用在典型情况下不太可能失败，但如果失败，<filename>fd.c</filename> 会变得相当混乱。
     </para>
    </listitem>

    <listitem>
     <para>
      修复对 postmaster 是否作为 Windows 服务运行的不正确检查（Michael Paquier）
     </para>

     <para>
      这可能导致在事件日志不可访问时尝试写入它，以至于完全不进行日志记录。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>ecpg</application> 支持 <command>COMMIT PREPARED</command>
      和 <command>ROLLBACK PREPARED</command>（Masahiko Sawada）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>ecpg</application> 中处理美元引用字符串字面量时的双重释放错误（Michael Meskes）
     </para>
    </listitem>

    <listitem>
     <para>
      在 <application>pg_dump</application> 中，修复某些类型数据库对象的注释和安全标签的模式和所有者标记错误（Giuseppe Broccolo、Tom Lane）
     </para>

     <para>
      简单情况下这没有不良影响；但例如，选择性模式恢复可能遗漏本应包含的注释，因为它们没有被标记为属于其相关对象的模式。
     </para>
    </listitem>

    <listitem>
     <para>
      避免 SQL 对象名包含换行时 <literal>pg_restore -l</literal> 发出无效列表文件（Tom Lane）
     </para>

     <para>
      用空格替换换行，这足以使输出对 <literal>pg_restore -L</literal> 的用途有效。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_upgrade</application> 传输附加到<quote>大对象</quote>（blob）的注释和安全标签（Stephen Frost）
     </para>

     <para>
      此前，blob 被正确传输到新数据库，但附加到它们的任何注释或安全标签都丢失了。
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <filename>contrib/adminpack</filename> 的
      <function>pg_file_write()</function> 函数中的错误处理（Noah Misch）
     </para>

     <para>
      值得注意的是，它未能检测 <function>fclose()</function> 报告的错误。
     </para>
    </listitem>

    <listitem>
     <para>
      在 <filename>contrib/dblink</filename> 中，建立新的未命名连接时避免泄漏先前的未命名连接（Joe Conway）
     </para>
    </listitem>

    <listitem>
     <para>
     支持 OpenSSL 1.1.0（Heikki Linnakangas、Andreas Karlsson、Tom Lane）
    </para>

     <para>
     这是把先前在较新分支中完成的工作向后移植；由于许多平台正在采用更新的 OpenSSL 版本，因此需要它。
    </para>
    </listitem>

    <listitem>
     <para>
      在 MSVC 构建中支持 Tcl 8.6（&Aacute;lvaro Herrera）
     </para>
    </listitem>

    <listitem>
     <para>
      把我们的时区库副本与 IANA tzcode2017b 同步（Tom Lane）
     </para>

     <para>
      这修复了影响 2038 年 1 月某些夏令时转换的 bug。
     </para>
    </listitem>

    <listitem>
     <para>
      更新时区数据文件到 <application>tzdata</application> 版本 2017b，涉及智利、海地和蒙古的夏令时法规变更，加上厄瓜多尔、哈萨克斯坦、利比里亚和西班牙的历史修正。南美洲、太平洋和印度洋以及一些亚洲和中东国家的众多时区改用数字缩写。
     </para>

     <para>
      IANA 时区数据库以前为所有时区提供文本缩写，有时编造在当地居民中几乎或完全没有使用的缩写。他们正在逆转这一政策，改为在没有证据表明实际使用英文缩写的区域使用数字 UTC 偏移。至少目前，<productname>PostgreSQL</productname> 将继续接受这种被移除的缩写用于时间戳输入。但它们不会显示在
      <structname>pg_timezone_names</structname> 视图中，也不用于输出。
     </para>
    </listitem>

    <listitem>
     <para>
      在 MSVC 构建中对 POSIX 风格时区名使用正确的夏令时规则（David Rowley）
     </para>

     <para>
      Microsoft MSVC 构建脚本忽略了把 <filename>posixrules</filename> 文件安装到时区目录树中。这导致时区代码回退到它内置的关于 POSIX 风格时区名应假定什么 DST 行为的规则。由于历史原因，那仍然对应美国
      2007 年之前使用的 DST 规则（即 4 月第一个星期日和 10 月最后一个星期日更改）。有了此修复，POSIX 风格时区名将使用
      <literal>US/Eastern</literal> 时区的当前和历史 DST 转换日期。如果不希望这样，移除 <filename>posixrules</filename> 文件，或用其他某个时区文件的副本替换它（见<xref linkend="datatype-timezones">）。注意由于缓存，可能需要重启服务器才能使这种更改生效。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-9-2-20">
  <title>版本 9.2.20</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2017-02-09</para>
  </formalpara>

  <para>
   本次发布包含对 9.2.19 的多方面修复。有关 9.2 大版本中新特性的信息，请参见<xref linkend="release-9-2">。.
  </para>

  <sect2>
   <title>迁移到版本 9.2.20</title>

   <para>
    对于运行 9.2.X 的用户，不需要进行转储/恢复。</para>

   <para>
     不过，如果你的安装受到下面第一条变更日志条目所述 bug 的影响，更新之后你可能需要采取行动修复损坏的索引。
    </para>

   <para>
    不过，如果你是从早于 9.2.11 的版本升级，请参见<xref linkend="release-9-2-11">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      修复可能导致用 <command>CREATE INDEX CONCURRENTLY</command> 构建的索引损坏的竞态条件（Pavan Deolasee、Tom Lane）
     </para>

     <para>
      如果 <command>CREATE INDEX CONCURRENTLY</command> 被用于构建一个依赖于先前未被索引的列的索引，那么与 <command>CREATE INDEX</command>
      命令并发运行的事务所更新的行可能收到错误的索引条目。如果怀疑这可能已发生，最可靠的解决方案是在安装此更新后重建受影响的索引。
     </para>
    </listitem>

    <listitem>
     <para>
      无条件地为未记录表的 <quote>init fork</quote> 写 WAL 日志（Michael Paquier）
     </para>

     <para>
      此前当<xref linkend="guc-wal-level"> = <literal>minimum</literal>
      时跳过这一步，但实际上即使在这种情况下也需要它，以确保未记录表在崩溃后被正确重置为空。
     </para>
    </listitem>

    <listitem>
<!--
Author: Fujii Masao <fujii@postgresql.org>
Branch: REL9_2_STABLE [38bec1805] 2017-01-25 07:02:25 +0900
-->
     <para>
      修复重新读取段时 WAL 页头的校验（Takayuki Tsunakawa、Amit Kapila）
     </para>

     <para>
      在极端情况下，恢复期间可能报告虚假的 <quote>out-of-sequence TLI</quote> 错误。
     </para>
    </listitem>

    <listitem>
     <para>
      如果统计收集器在热备期间死亡，重启它（Takayuki Tsunakawa）
     </para>
    </listitem>

    <listitem>
     <para>
      热备等待冲突查询时检查中断（Simon Riggs）
     </para>
    </listitem>

    <listitem>
     <para>
      避免边界情况下自动清理启动器的不断重启（Amit Khandekar）
     </para>

     <para>
      此修复避免了自动清理名义上关闭且有一些需要冻结的表，但所有这种表都已被自动清理工作者处理时的问题。
     </para>
    </listitem>

    <listitem>
     <para>
      修复扩展成员对象何时可被删除的检查（Tom Lane）
     </para>

     <para>
      扩展升级脚本应当能删除成员对象，但对序列列的序列（可能还有其他情况）这不被允许。
     </para>
    </listitem>

    <listitem>
     <para>
      确保 <command>ALTER TABLE</command> 重建索引时保留索引表空间分配（Tom Lane、Michael Paquier）
     </para>

     <para>
      此前，<xref linkend="guc-default-tablespace">的非默认设置可能导致索引损坏。
     </para>
    </listitem>

    <listitem>
     <para>
      如果被引用关系有未决的触发器事件，防止删除外键约束（Tom Lane）
     </para>

     <para>
      这避免了 <quote>could not find trigger <replaceable>NNN</replaceable></quote>
      或 <quote>relation <replaceable>NNN</replaceable> has no triggers</quote>
      错误。
     </para>
    </listitem>

    <listitem>
     <para>
      修复带 OID 的表通过 <command>ALTER TABLE ... INHERIT</command> 关联到带 OID 的父表时 OID 列的处理（Amit Langote）
     </para>

     <para>
      这种情况下 OID 列应当与普通用户列同等对待，但没有，导致之后继承更改中的奇怪行为。
     </para>
    </listitem>

    <listitem>
     <para>
      在报告约束违反失败之前检查可序列化冲突（Thomas Munro）
     </para>

     <para>
      使用可序列化事务隔离时，希望任何因并发事务导致的错误都表现为序列化失败，从而提示应用重试可能成功。不幸的是，对并发插入导致的重复键失败这不能可靠发生。此更改确保如果应用在事务中早些时候显式检查过冲突键的存在（且未找到），这种错误将被报告为序列化错误。
     </para>
    </listitem>

    <listitem>
     <para>
      确保多行 <literal>VALUES</literal> 构造的列 typmod 被准确确定（Tom Lane）
     </para>

     <para>
      这修复了列的第一个值有可确定的 typmod（如 <type>varchar</type> 值的长度）但后面的值不共享同一限制时发生的问题。
     </para>
    </listitem>

    <listitem>
     <para>
      对 Unicode 字符串末尾未完成的 Unicode 代理项对抛出错误（Tom Lane）
     </para>

     <para>
      通常 Unicode 代理项前导字符后面必须跟 Unicode 代理项后随字符，但当前导字符是 Unicode 字符串字面量（<literal>U&amp;'...'</literal>）或 Unicode 标识符（<literal>U&amp;"..."</literal>）的最后一个字符时漏掉了这一检查。
     </para>
    </listitem>

    <listitem>
     <para>
      确保纯否定的文本搜索查询（如 <literal>!foo</literal>）匹配空的
      <type>tsvector</type>（Tom Dunstan）
     </para>

     <para>
      GIN 索引搜索能找到这种匹配，但顺序扫描或 GiST 索引搜索不能。
     </para>
    </listitem>

    <listitem>
     <para>
      防止 <function>ts_rewrite()</function> 用空查询替换非顶层子树时崩溃（Artur Zakirov）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>ts_rewrite()</function> 的性能问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>ts_rewrite()</function> 对嵌套 NOT 操作符的处理（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>array_fill()</function> 正确处理空数组（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>quote_literal_cstr()</function> 中的一字节缓冲区越界（Heikki Linnakangas）
     </para>

     <para>
      只有当输入完全由单引号和/或反斜线组成时才会发生越界。
     </para>
    </listitem>

    <listitem>
     <para>
      防止 <function>pg_start_backup()</function> 和
      <function>pg_stop_backup()</function> 的多次调用并发运行（Michael Paquier）
     </para>

     <para>
      这避免了有人试图并行运行这些函数时的断言失败以及可能更糟的事情。
     </para>
    </listitem>

    <listitem>
     <para>
      避免丢弃并非真正无操作的
      <type>interval</type> 到 <type>interval</type> 强制转换（Tom Lane）
     </para>

     <para>
      某些情况下，本应导致清零低阶 <type>interval</type> 字段的强制转换被错误地当作无操作而丢弃。例如从 <type>INTERVAL MONTH</type> 转换到 <type>INTERVAL YEAR</type> 未能清除月份字段。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_dump</application> 转储使用内建函数的用户定义强制转换和转换（Stephen Frost）
     </para>
    </listitem>

    <listitem>
     <para>
      修复包含 WAL 文件时备库服务器上可能的
      <application>pg_basebackup</application> 失败（Amit Kapila、Robert Haas）
     </para>
    </listitem>

    <listitem>
     <para>
      确保我们为 PL/Python 创建的 Python 异常对象被正确引用计数（Rafa de la Torre、Tom Lane）
     </para>

     <para>
      这避免了对象在 Python 垃圾回收周期发生之后被使用时的失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PL/Tcl 支持以 <literal>.tupno</literal> 为列名的表上的触发器（Tom Lane）
     </para>

     <para>
      这与 PL/Tcl 的 <command>spi_exec</command> 和
      <command>spi_execp</command> 命令（以前未写入文档）的行为一致，即只在不存在该名字的真实列时才插入幻 <literal>.tupno</literal> 列。
     </para>
    </listitem>

    <listitem>
     <para>
      允许 <filename>~/.pgpass</filename> 文件中的 DOS 风格行尾，即使在
      Unix 上也一样（Vik Fearing）
     </para>

     <para>
      这一更改简化了在 Unix 和 Windows 机器之间使用同一密码文件。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>ecpg</application> 被给定以点结尾的文件名时的一字节缓冲区越界（Takayuki Tsunakawa）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>psql</application> 对
      <command>ALTER DEFAULT PRIVILEGES</command> 的 Tab 补全（Gilles Darold、Stephen Frost）
     </para>
    </listitem>

    <listitem>
     <para>
      在 <application>psql</application> 中，把 <envar>PAGER</envar> 环境变量的空或全空白设置当作<quote>不使用分页器</quote>（Tom Lane）
     </para>

     <para>
      此前这种设置导致本应发给分页器的输出完全消失。
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <filename>contrib/dblink</filename> 对低层
      <application>libpq</application> 错误（如内存不足）的报告（Joe Conway）
     </para>
    </listitem>

    <listitem>
     <para>
      在 Windows 上，确保环境变量更改被传播到以调试选项构建的 DLL（Christian Ullrich）
     </para>
    </listitem>

    <listitem>
     <para>
      把我们的时区库副本与 IANA tzcode2016j 同步（Tom Lane）
     </para>

     <para>
      这修复了若干问题，最显著的是当目标目录不支持硬链接时区数据安装失败。
     </para>
    </listitem>

    <listitem>
     <para>
      更新时区数据文件到 <application>tzdata</application> 版本 2016j，涉及北塞浦路斯（新增时区 Asia/Famagusta）、俄罗斯（新增时区
      Europe/Saratov）、汤加和 Antarctica/Casey 的夏令时法规变更。还有意大利、哈萨克斯坦、马耳他和巴勒斯坦的历史修正。汤加改用首选数字时区缩写。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-9-2-19">
  <title>版本 9.2.19</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2016-10-27</para>
  </formalpara>

  <para>
   本次发布包含对 9.2.18 的多方面修复。有关 9.2 大版本中新特性的信息，请参见<xref linkend="release-9-2">。.
  </para>

  <sect2>
   <title>迁移到版本 9.2.19</title>

   <para>
    对于运行 9.2.X 的用户，不需要进行转储/恢复。</para>

   <para>
    不过，如果你是从早于 9.2.11 的版本升级，请参见<xref linkend="release-9-2-11">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      修复涉及 CTE 扫描的 EvalPlanQual 重新检查（Tom Lane）
     </para>

     <para>
      重新检查总是把 CTE 视为不返回任何行，通常导致无法更新最近已更新的行。
     </para>
    </listitem>

    <listitem>
     <para>
      修复子查询中哈希聚合不当重复先前结果的问题（Andrew Gierth）
     </para>

     <para>
      判断能否复用先前计算的聚合状态值哈希表的测试，忽略了聚合参数表达式中出现外层查询引用的可能性。这种引用的值发生变化时本应重新计算哈希表，但实际没有。
     </para>
    </listitem>

    <listitem>
     <para>
      修复<xref linkend="guc-track-io-timing">打开时
      <command>EXPLAIN</command> 发出有效 XML（Markus Winand）
     </para>

     <para>
      此前 XML 输出格式选项产生语法无效的标记，如
      <literal>&lt;I/O-Read-Time&gt;</literal>。现在它被渲染为
      <literal>&lt;I-O-Read-Time&gt;</literal>。
     </para>
    </listitem>

    <listitem>
     <para>
      抑制在 <command>EXPLAIN</command> 中为未测量的时间打印零值（Maksim Milyutin）
     </para>

     <para>
      某些选项组合会为在该组合下实际上从不测量的时间打印零值。<command>EXPLAIN</command> 的一般策略是完全不打印此类字段，因此在所有情况下都保持一致。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <command>VACUUM</command> 为截断表而等待排他表锁时的超时长度（Simon Riggs）
     </para>

     <para>
      该超时本应为 50 毫秒，但实际上只有 50 微秒，导致 <command>VACUUM</command> 比预期更容易放弃截断。将其设置为预期值。
     </para>
    </listitem>

    <listitem>
     <para>
      修复创建或更改表时合并继承 <literal>CHECK</literal> 约束的 bug（Tom Lane、Amit Langote）
     </para>

     <para>
      允许以任一顺序向父表和子表添加相同的 <literal>CHECK</literal> 约束。防止父表的有效约束与子表的 <literal>NOT VALID</literal> 约束合并。同样防止 <literal>NO INHERIT</literal> 子约束与继承约束合并。
     </para>
    </listitem>

    <listitem>
     <para>
      移除对 <function>numeric_in()</function> 和 <function>numeric_recv()</function> 所接受值的人为限制（Tom Lane）
     </para>

     <para>
      我们允许数值达到存储格式的极限（超过 <literal>1e100000</literal>），因此 <function>numeric_in()</function> 拒绝科学计数法指数超过 1000 的做法相当无意义。同样，<function>numeric_recv()</function> 拒绝输入值超过 1000 位数字也很荒谬。
     </para>
    </listitem>

    <listitem>
     <para>
      避免因未持有缓冲区锁测试元组可见性而导致的极低概率数据损坏（Thomas Munro、Peter Geoghegan、Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复截断超过 1GB 的临时关系时文件描述符泄漏的问题（Andres Freund）
     </para>
    </listitem>

    <listitem>
     <para>
      禁止在启用 <literal>standby_mode</literal> 的情况下启动独立后端（Michael Paquier）
     </para>

     <para>
      这不可能有任何用处，因为不会有 WAL 接收器进程来获取更多 WAL 数据；而且这可能导致未针对此种情况设计的代码出现不当行为。
     </para>
    </listitem>

    <listitem>
     <para>
      不要在 <application>libpq</application> 中跨多个连接共享 SSL 上下文（Heikki Linnakangas）
     </para>

     <para>
      这导致了各种边界情况 bug，特别是当尝试对不同连接使用不同 SSL 参数时。
     </para>
    </listitem>

    <listitem>
     <para>
      避免 <application>libpq</application> 中的边界情况内存泄漏（Tom Lane）
     </para>

     <para>
      报告的问题涉及 <function>PQreset()</function> 期间泄漏错误报告，但可能有相关情况。
     </para>
    </listitem>

    <listitem>
     <para>
      使 <application>ecpg</application> 的 <option>--help</option> 和 <option>--version</option> 选项与其他可执行程序保持一致（Haribabu Kommi）
     </para>
    </listitem>

    <listitem>
     <para>
      在 <application>pg_dump</application> 中，永不转储范围构造函数（Tom Lane）
     </para>

     <para>
      这一疏漏导致包含范围类型的扩展在 <application>pg_upgrade</application> 时因重复创建构造函数而失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/intarray/bench/bench.pl</filename>，使其在给定 <option>-e</option> 选项时打印它执行的 <command>EXPLAIN</command> 的结果（Daniel Gustafsson）
     </para>
    </listitem>

    <listitem>
     <para>
      更新 Windows 时区映射，以识别较新 Windows 版本中新增的一些时区名称（Michael Paquier）
     </para>
    </listitem>

    <listitem>
     <para>
      防止已过时的动态时区缩写失效（Tom Lane）
     </para>

     <para>
      如果动态时区缩写与所引用时区中的任何条目都不匹配，就把它视为等效于该时区名称。这避免了当 IANA 从其时区数据库中移除缩写时出现意外失败——他们在 <application>tzdata</application> 2016f 版中就这样做过，而且将来可能再次这样做。后果不仅限于无法识别单个缩写；任何不匹配都会导致 <structname>pg_timezone_abbrevs</structname> 视图完全失败。
     </para>
    </listitem>

    <listitem>
     <para>
      更新时区数据文件到 <application>tzdata</application> 版本 2016h，涉及巴勒斯坦和土耳其的夏令时法规变更，加上土耳其和俄罗斯一些地区的历史修正。南极洲、前苏联和斯里兰卡的一些时区改用数字缩写。
     </para>

     <para>
      IANA 时区数据库以前为所有时区提供文本缩写，有时编造在当地居民中几乎或完全没有使用的缩写。他们正在逆转这一政策，改为在没有证据表明实际使用英文缩写的区域使用数字 UTC 偏移。至少目前，<productname>PostgreSQL</productname> 将继续接受这种被移除的缩写用于时间戳输入。但它们不会显示在
      <structname>pg_timezone_names</structname> 视图中，也不用于输出。
     </para>

     <para>
      在此更新中，<literal>AMT</literal> 不再显示为表示亚美尼亚时间。因此我们把 <literal>Default</literal> 缩写集改为把它解释为亚马逊时间，即 UTC-4 而不是 UTC+4。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-9-2-18">
  <title>版本 9.2.18</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2016-08-11</para>
  </formalpara>

  <para>
   本次发布包含对 9.2.17 的多方面修复。有关 9.2 大版本中新特性的信息，请参见<xref linkend="release-9-2">。.
  </para>

  <sect2>
   <title>迁移到版本 9.2.18</title>

   <para>
    对于运行 9.2.X 的用户，不需要进行转储/恢复。</para>

   <para>
    不过，如果你是从早于 9.2.11 的版本升级，请参见<xref linkend="release-9-2-11">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      修复嵌套 <literal>CASE</literal>-<literal>WHEN</literal> 表达式可能被错误评估的问题（Heikki Linnakangas、Michael Paquier、Tom Lane）
     </para>

     <para>
      出现在另一个 <literal>CASE</literal> 的测试值子表达式中的 <literal>CASE</literal> 表达式，可能会混淆其自身的测试值是否为空。此外，内联实现 <literal>CASE</literal> 表达式所用相等操作符的 SQL 函数时，可能向该 SQL 函数体内 <literal>CASE</literal> 表达式中调用的函数传递错误的测试值。如果测试值的数据类型不同，可能导致崩溃；而且此类情况可被滥用以泄露服务器内存的部分内容。（CVE-2016-5423）
     </para>
    </listitem>

    <listitem>
     <para>
      修复客户端程序对数据库名和角色名中特殊字符的处理（Noah Misch、Nathan Bossart、Michael Paquier）
     </para>

     <para>
      <application>vacuumdb</application> 及其他客户端程序中的许多地方，可能会被包含双引号或反斜杠的数据库名和角色名弄混。收紧引用规则以使其安全。同时确保当 conninfo 字符串被用作这些程序的数据库名参数时，全程都被正确地如此对待。
     </para>

     <para>
      修复 <application>psql</application> 的 <command>\connect</command> 和 <command>\password</command> 命令中对成对双引号的处理，使其与文档一致。
     </para>

     <para>
      在 <application>psql</application> 的 <command>\connect</command> 命令中引入新的 <option>-reuse-previous</option> 选项，以显式控制是否复用先前连接的连接参数。（若不使用该选项，则与以前一样，根据数据库名是否看起来像 conninfo 字符串来决定。）这使 <application>pg_dumpall</application> 脚本能够安全地处理包含特殊字符的数据库名。
     </para>

     <para>
      <application>pg_dumpall</application> 现在拒绝处理包含回车或换行的数据库名和角色名，因为在 Windows 上安全地引用这些字符似乎不切实际。将来我们可能会在服务器端拒绝此类名称，但尚未采取这一步骤。
     </para>

     <para>
      这些被视为安全修复，因为包含特殊字符的恶意构造的对象名，可能在超级用户下次执行 <application>pg_dumpall</application> 或其他例行维护操作时被用于以超级用户权限执行命令。（CVE-2016-5424）
     </para>
    </listitem>

    <listitem>
     <para>
      修复对嵌套组合值应用 <literal>IS NULL</literal>/<literal>IS NOT NULL</literal> 时的边界错误行为（Andrew Gierth、Tom Lane）
     </para>

     <para>
      SQL 标准规定，对于全空值的行，<literal>IS NULL</literal> 应返回 TRUE（因此 <literal>ROW(NULL,NULL) IS NULL</literal> 得到 TRUE），但这并不意味着递归适用（因此 <literal>ROW(NULL, ROW(NULL,NULL)) IS NULL</literal> 得到 FALSE）。核心执行器对此处理正确，但某些规划器优化把该测试当作递归的（因而在两种情况下都产生 TRUE），而 <filename>contrib/postgres_fdw</filename> 可能产生类似错误行为的远程查询。
     </para>
    </listitem>

    <listitem>
     <para>
      使 <type>inet</type> 和 <type>cidr</type> 数据类型正确拒绝包含过多冒号分隔字段的 IPv6 地址（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      防止 <function>close_ps()</function>（<type>point</type> <literal>##</literal> <type>lseg</type> 操作符）在输入坐标为 NaN 时崩溃（Tom Lane）
     </para>

     <para>
      使其返回 NULL 而不是崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>to_number()</function> 中几处一字节缓冲区过读（Peter Eisentraut）
     </para>

     <para>
      在几种情况下，<function>to_number()</function> 函数会从输入字符串中多读取一个字符。如果输入恰好位于内存末尾附近，则有很小的崩溃可能。
     </para>
    </listitem>

    <listitem>
     <para>
      避免在经过 <function>heap_update()</function> 的高开销路径中出现不安全的中间状态（Masahiko Sawada、Andres Freund）
     </para>

     <para>
      以前，这些情况会锁定目标元组（通过设置其 XMAX），但不为该动作记录 WAL，因此如果页面被刷出到磁盘，随后在元组更新完成之前发生数据库崩溃，就有数据完整性问题的风险。
     </para>
    </listitem>

    <listitem>
     <para>
      避免当指定变量的字符串值为空时 <literal>postgres -C</literal> 崩溃（Michael Paquier）
     </para>
    </listitem>

    <listitem>
     <para>
      避免在 <command>VACUUM</command> 期间消耗事务 ID（Alexander Korotkov）
     </para>

     <para>
      <command>VACUUM</command> 中的某些情况会不必要地导致向当前事务分配 XID。通常这可以忽略不计，但如果已接近 XID 回卷限制，在防回卷清理期间消耗更多 XID 就非常糟糕了。
     </para>
    </listitem>

    <listitem>
     <para>
     避免在<command>VACUUM FREEZE</command>期间取消热备查询（Simon Riggs, &Aacute;lvaro Herrera）
    </para>

     <para>
     在原本空闲的主库服务器上执行<command>VACUUM FREEZE</command>可能导致其备库服务器上的查询被不必要地取消。
    </para>
    </listitem>

    <listitem>
     <para>
      当手动 <command>ANALYZE</command> 指定列列表时，不要重置表的 <literal>changes_since_analyze</literal> 计数器（Tom Lane）
     </para>

     <para>
      如果只分析部分列，就不应阻止其他列的例行自动分析。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <command>ANALYZE</command> 对包含大量空值的唯一或近似唯一列的 <literal>n_distinct</literal> 高估（Tom Lane）
     </para>

     <para>
      空值可能被当作它们本身就是不同的值来计数，导致某些类型查询中规划器的严重误估。
     </para>
    </listitem>

    <listitem>
     <para>
      防止自动清理为同一共享目录启动多个工作进程（&Aacute;lvaro Herrera）
     </para>

     <para>
      通常这不是什么大问题，因为清理本来就不会花很长时间；但在目录严重膨胀的情况下，这可能导致除一个工作进程外的所有工作进程都在无用地等待，而不是对其他表做有用的工作。
     </para>
    </listitem>

    <listitem>
     <para>
      防止对包含 NaN 分量值的几何列构建 GiST 索引时出现无限循环（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/btree_gin</filename>，使其正确处理 <type>bigint</type> 的最小可能值（Peter Eisentraut）
     </para>
    </listitem>

    <listitem>
     <para>
      让 libpq 能正确解码来自未来版本服务器的服务器版本号（Peter Eisentraut）
     </para>

     <para>
      计划在 9.6 之后的版本中把服务器版本号从三段式改为两段式。确保 <function>PQserverVersion()</function> 在此类情况下返回正确的值。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>ecpg</application> 中 <literal>unsigned long long</literal> 数组元素的代码（Michael Meskes）
     </para>
    </listitem>

    <listitem>
     <para>
      在同时使用 <option>-c</option> 和 <option>-C</option> 选项的 <application>pg_dump</application> 中，避免发出多余的 <literal>CREATE SCHEMA public</literal> 命令（David Johnston、Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      使 <application>pg_basebackup</application> 接受 <literal>-Z 0</literal> 表示不压缩（Fujii Masao）
     </para>
    </listitem>

    <listitem>
     <para>
      修复构建 AIX 共享库的 makefile 规则，使其在并行 make 下安全（Noah Misch）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 TAP 测试和 MSVC 脚本，使其在构建目录路径名包含空格时也能工作（Michael Paquier、Kyotaro Horiguchi）
     </para>
    </listitem>

    <listitem>
     <para>
      使回归测试在丹麦语和威尔士语区域设置下安全（Jeff Janes、Tom Lane）
     </para>

     <para>
      修改了一些会触发这些区域设置特殊排序规则的测试数据。
     </para>
    </listitem>

    <listitem>
     <para>
      更新我们的时区代码副本，以匹配 IANA 的 <application>tzcode</application> 2016c 版（Tom Lane）
     </para>

     <para>
      这是为了应对时区数据文件未来预期的变更。它还修复了处理特殊时区方面的一些边界 bug。
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新到 <application>tzdata</application> 2016f 版，涉及克麦罗沃和新西伯利亚的夏令时法规变更，以及阿塞拜疆、白俄罗斯和摩洛哥的历史数据修正。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-9-2-17">
  <title>版本 9.2.17</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2016-05-12</para>
  </formalpara>

  <para>
   本次发布包含对 9.2.16 的多方面修复。有关 9.2 大版本中新特性的信息，请参见<xref linkend="release-9-2">。.
  </para>

  <sect2>
   <title>迁移到版本 9.2.17</title>

   <para>
    对于运行 9.2.X 的用户，不需要进行转储/恢复。</para>

   <para>
    不过，如果你是从早于 9.2.11 的版本升级，请参见<xref linkend="release-9-2-11">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      在调用 OpenSSL 之前清空 OpenSSL 错误队列，而不是假设它已经为空；并确保调用之后也让它保持为空（Peter Geoghegan、Dave Vitek、Peter Eisentraut）
     </para>

     <para>
      此变更可防止在单个进程内存在多条使用 OpenSSL 的连接、且相关代码并非全部遵循相同的错误队列清空规则时出现问题。特别是当客户端应用并发使用 <application>libpq</application> 的 SSL 连接和 PHP、Python 或 Ruby 的 OpenSSL 封装库的 SSL 连接时，已有失败报告。如果扩展模块建立外向 SSL 连接，服务器内部也可能出现类似问题。
     </para>
    </listitem>

    <listitem>
     <para>
      修复左连接右侧包含全连接时规划器出现<quote>failed to build any <replaceable>N</replaceable>-way joins</quote>错误的问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复多层级 nestloop 计划中等价类测试的处理不正确的问题（Tom Lane）
     </para>

     <para>
      对于三个或更多变量的等价类（例如 <literal>X.X = Y.Y = Z.Z</literal>），规划器可能遗漏一些确保所有变量确实相等的测试，导致输出不满足 <literal>WHERE</literal> 子句的连接行。由于种种原因，实践中很少选中错误的计划，因此这个 bug 长期未被发现。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>to_timestamp()</function> 中 <literal>TH</literal>、<literal>th</literal> 和 <literal>Y,YYY</literal> 格式代码可能的错误行为（Tom Lane）
     </para>

     <para>
      这些代码可能越过输入字符串末尾，导致后续格式代码读取垃圾数据。
     </para>
    </listitem>

    <listitem>
     <para>
      修复对这样的规则和视图的转储：<literal><replaceable>value</replaceable> <replaceable>operator</replaceable> ANY (<replaceable>array</replaceable>)</literal> 结构的 <replaceable>array</replaceable> 参数是子 SELECT（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      使 <application>pg_regress</application> 在设置了 <envar>PGCTLTIMEOUT</envar> 环境变量时使用其中的启动超时（Tom Lane）
     </para>

     <para>
      这是为了与最近添加到 <application>pg_ctl</application> 的行为保持一致；它方便了在慢速机器上进行自动化测试。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_upgrade</application> 对仅包含一个操作符类的操作符族正确恢复扩展成员关系的问题（Tom Lane）
     </para>

     <para>
      在这种情况下，操作符族会被恢复到新数据库中，但不再被标记为扩展的一部分。这没有直接的坏影响，但会导致后续 <application>pg_dump</application> 运行产生在恢复时引发（无害）错误的输出。
     </para>
    </listitem>

    <listitem>
     <para>
      把 9.4 时代的内存屏障代码变更回移植到 9.2 和 9.3（Tom Lane）
     </para>

     <para>
      这些变更在 9.4 之前的分支中原本并不需要，但我们最近回移植了一个要求屏障代码正常工作的修复。只有 IA64（使用 icc 时）、HPPA 和 Alpha 平台受影响。
     </para>
    </listitem>

    <listitem>
     <para>
      减少配置了 <option>--disable-spinlocks</option> 的构建所使用的 SysV 信号量数量（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      把内部函数 <function>strtoi()</function> 重命名为 <function>strtoint()</function>，以避免与 NetBSD 库函数冲突（Thomas Munro）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 Windows 上 <function>bind()</function> 和 <function>listen()</function> 系统调用错误的报告（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      减少使用 Microsoft Visual Studio 构建时编译器输出的冗长程度（Christian Ullrich）
     </para>
    </listitem>

    <listitem>
     <para>
      避免可能不安全地使用 Windows 的 <function>FormatMessage()</function> 函数（Christian Ullrich）
     </para>

     <para>
      在适当之处使用 <literal>FORMAT_MESSAGE_IGNORE_INSERTS</literal> 标志。目前不知道这里存在实际的 bug，但小心谨慎似乎是明智的。
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新到 <application>tzdata</application> 2016d 版，涉及俄罗斯和委内瑞拉的夏令时法规变更。新增时区名称 <literal>Europe/Kirov</literal> 和 <literal>Asia/Tomsk</literal>，以反映这些地区现在与相邻地区有不同的时区历史。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-9-2-16">
  <title>版本 9.2.16</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2016-03-31</para>
  </formalpara>

  <para>
   本次发布包含对 9.2.15 的多方面修复。有关 9.2 大版本中新特性的信息，请参见<xref linkend="release-9-2">。.
  </para>

  <sect2>
   <title>迁移到版本 9.2.16</title>

   <para>
    对于运行 9.2.X 的用户，不需要进行转储/恢复。</para>

   <para>
    不过，如果你是从早于 9.2.11 的版本升级，请参见<xref linkend="release-9-2-11">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      修复索引化 <literal>ROW()</literal> 比较中对 NULL 索引条目处理不正确的问题（Tom Lane）
     </para>

     <para>
      使用诸如 <literal>ROW(a, b) &gt; ROW('x', 'y')</literal> 的行比较进行索引搜索时，会在到达 <structfield>b</structfield> 列的 NULL 条目时停止，忽略了 <structfield>b</structfield> 列可能有与更靠后的 <structfield>a</structfield> 值关联的非 NULL 值这一事实。
     </para>
    </listitem>

    <listitem>
     <para>
      避免因重命名文件前后未充分调用 <function>fsync()</function> 而导致的不太可能的数据丢失场景（Michael Paquier、Tomas Vondra、Andres Freund）
     </para>
    </listitem>

    <listitem>
     <para>
      正确处理服务器启动期间 <literal>pg_subtrans</literal> 接近 XID 回卷的情况（Jeff Janes）
     </para>
    </listitem>

    <listitem>
     <para>
      修复因多次尝试释放 <function>localeconv()</function> 输出字符串而导致的边界崩溃（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>ispell</literal> 词典词缀文件的解析（Tom Lane）
     </para>

     <para>
      如果词缀文件包含任何在大小写转换期间字节长度会变化的字符（例如土耳其语 UTF8 区域设置中的 <literal>I</literal>），代码就可能出错。
     </para>
    </listitem>

    <listitem>
     <para>
      避免使用 <function>sscanf()</function> 解析 <literal>ispell</literal> 词典文件（Artur Zakirov）
     </para>

     <para>
      这规避了 FreeBSD 衍生平台（包括 macOS）上的一个可移植性问题。
     </para>
    </listitem>

    <listitem>
     <para>
      避免在旧版 Windows（7SP1/2008R2SP1 之前）上配合支持 AVX2 的 CPU 和使用 Visual Studio 2013 构建的 Postgres 时崩溃（Christian Ullrich）
     </para>

     <para>
      这是对 Visual Studio 2013 运行时库中一个 bug 的变通方案，微软已表示不会在该版本中修复它。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>psql</application> 的 Tab 补全逻辑，使其正确处理多字节字符（Kyotaro Horiguchi、Robert Haas）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>psql</application> 对 <literal>SECURITY LABEL</literal> 的 Tab 补全（Tom Lane）
     </para>

     <para>
      在 <literal>SECURITY LABEL</literal> 之后按 TAB 可能导致崩溃或提供不合适的关键字。
     </para>
    </listitem>

    <listitem>
     <para>
      使 <application>pg_ctl</application> 在命令行未指定时接受来自 <envar>PGCTLTIMEOUT</envar> 环境变量的等待超时（Noah Misch）
     </para>

     <para>
      这方便了较慢的 buildfarm 成员的测试，允许它们全局指定比正常更长的 postmaster 启动和关闭超时。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_ctl</application> 中对 Windows 服务状态的不正确测试（Manuel Mathar）
     </para>

     <para>
      上一轮小版本发布试图修复 <application>pg_ctl</application>，使其正确判断是否把日志消息发送到 Windows 事件日志，但把测试条件弄反了。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pgbench</application> 对 <literal>-C</literal> 与 <literal>-M prepared</literal> 选项组合的处理（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      在 PL/Perl 中，把空的 Postgres 数组正确转换为空的 Perl 数组（Alex Hunsaker）
     </para>
    </listitem>

    <listitem>
     <para>
      使 PL/Python 能处理不是有效 Python 标识符的函数名（Jim Nasby）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/pgstattuple</filename> 的 <function>pgstatindex()</function> 函数返回统计信息中的多处错误（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      在 MSVC 构建中移除对 <literal>psed</literal> 的依赖，因为核心 Perl 已不再提供它（Michael Paquier、Andrew Dunstan）
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新到 <application>tzdata</application> 2016c 版，涉及阿塞拜疆、智利、海地、巴勒斯坦和俄罗斯（阿尔泰、阿斯特拉罕、基洛夫、萨哈林、乌里扬诺夫斯克地区）的夏令时法规变更，以及立陶宛、摩尔多瓦和俄罗斯（加里宁格勒、萨马拉、伏尔加格勒）的历史数据修正。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-9-2-15">
  <title>版本 9.2.15</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2016-02-11</para>
  </formalpara>

  <para>
   本次发布包含对 9.2.14 的多方面修复。有关 9.2 大版本中新特性的信息，请参见<xref linkend="release-9-2">。.
  </para>

  <sect2>
   <title>迁移到版本 9.2.15</title>

   <para>
    对于运行 9.2.X 的用户，不需要进行转储/恢复。</para>

   <para>
    不过，如果你是从早于 9.2.11 的版本升级，请参见<xref linkend="release-9-2-11">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      修复正则表达式中的无限循环和缓冲区越界问题（Tom Lane）
     </para>

     <para>
      方括号表达式中非常大的字符范围在某些情况下可能导致无限循环，在另一些情况下可能导致内存覆写。（CVE-2016-0773）
     </para>
    </listitem>

    <listitem>
     <para>
     如果<filename>postmaster.pid</filename>文件被移除则执行立即关闭（Tom Lane）
    </para>

     <para>
     postmaster 现在大约每分钟检查一次<filename>postmaster.pid</filename>是否仍然存在且仍包含自己的 PID。如果不是，它会执行立即关闭，就像收到<systemitem>SIGQUIT</systemitem>一样。此变更的主要目的是确保失败的 buildfarm 运行无需人工干预即可被清理；同时也可在 DBA 强行删除<filename>postmaster.pid</filename>并启动新 postmaster 时限制不良影响。
    </para>
    </listitem>

    <listitem>
     <para>
      在<literal>SERIALIZABLE</literal>事务隔离模式下，插入期间的竞争条件可能导致漏掉串行化异常（Kevin Grittner, Thomas Munro）
     </para>
    </listitem>

    <listitem>
     <para>
     修复对非日志关系执行<literal>ALTER TABLE ... SET TABLESPACE</literal>时未发出适当 WAL 记录的问题（Michael Paquier、Andres Freund）
    </para>

     <para>
     尽管关系的数据是非日志的，但移动操作本身必须被记录，否则在备库提升为主库后该关系将不可访问。
    </para>
    </listitem>

    <listitem>
     <para>
      修复崩溃恢复结束时非日志关系可能被错误初始化的问题（Andres Freund, Michael Paquier）
     </para>
    </listitem>

    <listitem>
     <para>
      修复<command>ALTER COLUMN TYPE</command>以正确重建继承的检查约束（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复<command>REASSIGN OWNED</command>以正确更改组合类型的所有权（&Aacute;lvaro Herrera）
     </para>
    </listitem>

    <listitem>
     <para>
      修复<command>REASSIGN OWNED</command>和<command>ALTER OWNER</command>在更改数据类型、外部数据包装器或外部服务器的所有者时正确更新已授权权限列表的问题（Bruce Momjian, &Aacute;lvaro Herrera）
     </para>
    </listitem>

    <listitem>
     <para>
      修复<command>REASSIGN OWNED</command>忽略外部用户映射而不是报错的问题（&Aacute;lvaro Herrera）
     </para>
    </listitem>

    <listitem>
     <para>
      当 GIN 索引的内部统计信息严重过时时，增加更多针对规划器错误代价估计的防护（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      使规划器能够处理由索引顾问插件建议的假设性 GIN 索引（Julien Rouhaud）
     </para>
    </listitem>

    <listitem>
     <para>
      修复<literal>ROW()</literal>和<literal>VALUES()</literal>列表中整行 Var 的转储问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复<type>numeric</type>除法中可能出现的内部溢出（Dean Rasheed）
     </para>
    </listitem>

    <listitem>
     <para>
     修复正则表达式先行约束中括号内限制的执行（Tom Lane）
    </para>

     <para>
     根据手册，先行约束不允许包含反向引用，且其中的括号始终被视为非捕获括号。但代码未能正确处理括号子表达式内的这些情况，会给出意外的结果。
    </para>
    </listitem>

    <listitem>
     <para>
      将正则表达式转换为索引扫描边界时，可能从包含向前看约束的正则表达式生成错误边界（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
     修复正则表达式编译器使其处理约束弧构成的环（Tom Lane）
    </para>

     <para>
     为 CVE-2007-4772 添加的代码既不完整——未能处理涉及多个状态的环——又不正确——可能导致断言失败（不过在非断言构建中似乎没有不良后果）。多状态环会使编译器一直运行，直到查询被取消或达到状态数过多的错误条件。
    </para>
    </listitem>

    <listitem>
     <para>
     改进正则表达式编译器中的内存用量统计（Tom Lane）
    </para>

     <para>
     这使代码在一些以前耗费不合理时间和内存量的情况下发出 <quote>regular expression is too complex</quote> 错误。
    </para>
    </listitem>

    <listitem>
     <para>
      改进正则表达式编译器的性能（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      使 <varname>log_line_prefix</varname> 中的 <literal>%h</literal> 和 <literal>%r</literal> 转义对因 <varname>log_connections</varname> 而发出的消息也生效（Tom Lane）
     </para>

     <para>
      以前，<literal>%h</literal>/<literal>%r</literal> 要等到新会话发出<quote>connection received</quote>日志消息之后才开始生效；现在它们对该消息也生效。
     </para>
    </listitem>

    <listitem>
     <para>
     在 Windows 上，确保不需要共享内存映射句柄的子进程关闭该句柄（Tom Lane、Amit Kapila）
    </para>

     <para>
     只要 <varname>logging_collector</varname> 处于开启状态，这一疏忽就会导致无法从崩溃中恢复。
    </para>
    </listitem>

    <listitem>
     <para>
     修复 Windows 上非阻塞模式下可能无法检测套接字 EOF 的问题（Tom Lane）
    </para>

     <para>
      目前尚不完全清楚此问题是否会在 9.5 之前的分支中发生，但如果发生，其症状将是 walsender
      进程无限等待而不是察觉到连接丢失。
     </para>
    </listitem>

    <listitem>
     <para>
      避免 SSPI 身份验证期间泄漏令牌句柄（Christian Ullrich）
     </para>
    </listitem>

    <listitem>
     <para>
     在 <application>psql</application> 中，确保当终端窗口大小改变时更新 <application>libreadline</application> 所认知的屏幕尺寸（Merlin Moncure）
    </para>

     <para>
     此前，如果窗口在查询输出期间被调整大小，<application>libreadline</application> 不会察觉，导致之后输入多行查询时出现奇怪的行为。
    </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>psql</application> 的 <literal>\det</literal> 命令，使其以与其他可能带模式限定模式的 <literal>\d</literal> 命令相同的方式解释模式参数（Reece Hart）
     </para>
    </listitem>

    <listitem>
     <para>
      避免在以下情况下<application>psql</application>的<literal>\c</literal>命令可能崩溃：先前连接是通过 Unix 套接字建立的，而命令指定了新的主机名和相同的用户名（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
     在 <literal>pg_ctl start -w</literal> 中，直接测试子进程状态而不是依赖启发式方法（Tom Lane、Michael Paquier）
    </para>

     <para>
     此前，<application>pg_ctl</application> 依赖一个假设：新的 postmaster 总会在五秒内创建 <filename>postmaster.pid</filename>。但在高负载系统上这可能失败，导致 <application>pg_ctl</application> 错误地报告 postmaster 启动失败。
    </para>

     <para>
     除 Windows 外，此变更还意味着紧接另一个此类命令之后执行的 <literal>pg_ctl start -w</literal> 现在会可靠地失败，而此前如果在第一条命令后两秒内执行则会报告成功。
    </para>
    </listitem>

    <listitem>
     <para>
     在 <literal>pg_ctl start -w</literal> 中，不再尝试使用通配监听地址连接 postmaster（Kondo Yuta）
    </para>

     <para>
     在 Windows 上，如果 <varname>listen_addresses</varname> 被设置为 <literal>0.0.0.0</literal> 或 <literal>::</literal>，<application>pg_ctl</application> 将无法检测到 postmaster 的启动，因为它会试图原样使用该值作为要连接的地址，而这行不通。现在改为假定应分别使用 <literal>127.0.0.1</literal> 或 <literal>::1</literal>。
    </para>
    </listitem>

    <listitem>
     <para>
      在 Windows 上的 <application>pg_ctl</application> 中，通过检查服务状态来决定输出发送到哪里，而不是检查标准输出是否为终端（Michael Paquier）
     </para>
    </listitem>

    <listitem>
     <para>
     在 <application>pg_dump</application> 和 <application>pg_basebackup</application> 中，采用 GNU 约定来处理超过 8GB 的 tar 归档成员（Tom Lane）
    </para>

     <para>
     <literal>tar</literal> 文件格式的 POSIX 标准不允许归档成员文件超过 8GB，但大多数现代 <application>tar</application> 实现支持解决此问题的扩展。采用这一扩展后，<application>pg_dump</application> 使用 <option>-Ft</option> 时不再在数据超过 8GB 的表上失败，<application>pg_basebackup</application> 也能处理大于 8GB 的文件。此外，还修复了一些可移植性问题，它们可能在某些平台上导致 4GB 到 8GB 之间的成员失败。这些问题有可能因备份文件不可读而造成不可恢复的数据丢失。
    </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_dump</application> 处理扩展成员对象时的若干边界 bug（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      使 <application>pg_dump</application> 把视图的触发器标记为需要在其规则之后处理，以防止并行 <application>pg_restore</application> 期间可能出现的失败（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
     确保在 <application>pg_dump</application> 中正确引用关系选项值（Kouhei Sutou、Tom Lane）
    </para>

     <para>
     不是简单标识符或数字的 reloption 值可能因 <application>pg_dump</application> 发出的 CREATE 语句中的语法错误而导致转储/重载失败。核心 <productname>PostgreSQL</productname> 目前支持的任何 reloption 都不存在此问题，但扩展可能允许导致该问题的 reloption。
    </para>
    </listitem>

    <listitem>
     <para>
      修复<application>pg_upgrade</application>的文件复制代码在 Windows 上正确处理错误的问题（Bruce Momjian）
     </para>
    </listitem>

    <listitem>
     <para>
      在 <application>pgbench</application> 中加入防护，防止计算脚本指定的除法或取模运算符期间的边界溢出情况（Fabien Coelho、Michael Paquier）
     </para>
    </listitem>

    <listitem>
     <para>
      修复<application>pg_receivexlog</application>和<application>pg_recvlogical</application>发出的消息未能本地化的问题（Ioseph Kim）
     </para>
    </listitem>

    <listitem>
     <para>
      避免同时使用 <application>plpython2</application> 和 <application>plpython3</application> 时的转储/重载问题（Tom Lane）
     </para>

     <para>
      原则上，两个版本的 <application>PL/Python</application> 可以在同一个数据库中使用，但不能在同一个会话中使用（因为两个版本的 <application>libpython</application> 无法安全地并发使用）。然而，<application>pg_restore</application> 和 <application>pg_upgrade</application> 都会做一些可能触犯同会话限制的事情。通过更改检查的时机来规避这一点。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>PL/Python</application> 回归测试，使其在 Python 3.5 下通过（Peter Eisentraut）
     </para>
    </listitem>

    <listitem>
     <para>
      防止某些 <application>PL/Java</application> 参数被非超级用户设置（Noah Misch）
     </para>

     <para>
      此变更缓解了 <application>PL/Java</application> 的一个安全 bug（CVE-2016-0766），该 bug 在 <application>PL/Java</application> 中已通过把这些参数标记为仅超级用户可用来修复。为了保护那些更新 <productname>PostgreSQL</productname> 比 <application>PL/Java</application> 更频繁的站点，核心代码现在也识别这些参数。
     </para>
    </listitem>

    <listitem>
     <para>
      改进<application>libpq</application>对内存耗尽情况的处理（Michael Paquier, Amit Kapila, Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      修复<application>ecpg</application>生成的<literal>typedef</literal>语句中参数顺序错误的问题（Michael Meskes）
     </para>
    </listitem>

    <listitem>
     <para>
      在<application>ecpg</application>的<function>PGTYPESnumeric_from_double()</function>中使用<literal>%g</literal>而非<literal>%f</literal>格式（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>ecpg</application> 提供的头文件，使其不包含从预处理指令行延续到下一行的注释（Michael Meskes）
     </para>

     <para>
      这样的注释会被 <application>ecpg</application> 拒绝。尚不清楚是否应该更改 <application>ecpg</application> 本身。
     </para>
    </listitem>

    <listitem>
     <para>
      确保<filename>contrib/pgcrypto</filename>的<function>crypt()</function>函数可以被查询取消中断（Andreas Karlsson）
     </para>
    </listitem>

    <listitem>
     <para>
     接受晚于 2.5.x 的<application>flex</application>版本（Tom Lane、Michael Paquier）
    </para>

     <para>
     由于 flex 2.6.0 已发布，需要调整构建脚本中的版本检查。
    </para>
    </listitem>

    <listitem>
     <para>
     将我们的<filename>missing</filename>脚本安装到 PGXS 构建能找到的位置（Jim Nasby）
    </para>

     <para>
     这使得在缺少<application>bison</application>等构建工具的机器上进行 PGXS 构建时能有合理的行为。
    </para>
    </listitem>

    <listitem>
     <para>
      确保 <filename>dynloader.h</filename> 包含在 MSVC 构建安装的头文件中（Bruce Momjian、Michael Paquier）
     </para>
    </listitem>

    <listitem>
     <para>
     新增变体回归测试期望输出文件，以匹配当前<application>libxml2</application>的行为（Tom Lane）
    </para>

     <para>
      <application>libxml2</application>对 CVE-2015-7499 的修复导致它在某些过去会输出错误上下文报告的情况下不再输出。这似乎是一个缺陷，但我们可能不得不在一段时间内接受它，因此做了变通处理。
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新到 <application>tzdata</application> 2016a 版，涉及开曼群岛、梅特拉卡特拉和外贝加尔边疆区的夏令时法规变更，以及巴基斯坦的历史数据修正。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-9-2-14">
  <title>版本 9.2.14</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2015-10-08</para>
  </formalpara>

  <para>
   本次发布包含对 9.2.13 的多方面修复。有关 9.2 大版本中新特性的信息，请参见<xref linkend="release-9-2">。.
  </para>

  <sect2>
   <title>迁移到版本 9.2.14</title>

   <para>
    对于运行 9.2.X 的用户，不需要进行转储/恢复。</para>

   <para>
    不过，如果你是从早于 9.2.11 的版本升级，请参见<xref linkend="release-9-2-11">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
     修复<filename>contrib/pgcrypto</filename>以检测并报告过短的<function>crypt()</function>盐值（Josh Kupershmidt）
    </para>

     <para>
     某些无效的盐值参数会导致服务器崩溃或泄露少量服务器内存字节。我们尚未排除通过安排让泄露字节中出现机密信息实施攻击的可行性，但这种攻击似乎不太可能。（CVE-2015-5288）
    </para>
    </listitem>

    <listitem>
     <para>
     修复属于外部子事务的门户（游标）失败后的子事务清理问题（Tom Lane、Michael Paquier）
    </para>

     <para>
     在外部子事务游标中执行的函数可能因引用在内部子事务中创建的关系而导致断言失败或崩溃。
    </para>
    </listitem>

    <listitem>
     <para>
     修复关系插入关系缓存<quote>init 文件</quote>的问题（Tom Lane）
    </para>

     <para>
     最近几个小版本中一个补丁的疏忽导致<structname>pg_trigger_tgrelid_tgname_index</structname>被遗漏在 init 文件之外。后续会话检测到这一点后认定 init 文件已损坏并静默忽略它，导致会话启动时间显著退化。除修复该缺陷外，还加入了一些防护，使未来任何类似错误更明显。
    </para>
    </listitem>

    <listitem>
     <para>
      避免向 SPI 查询结果插入大量元组时的 O(N^2) 行为（Neil Conway）
     </para>
    </listitem>

    <listitem>
     <para>
      改进存在大量未读通知时<command>LISTEN</command>的启动时间（Matt Newell）
     </para>
    </listitem>

<!--
Author: Tom Lane <tgl@sss.pgh.pa.us>
Branch: REL9_2_STABLE [0e933fdf9] 2015-08-27 12:22:10 -0400
Branch: REL9_1_STABLE [9b1b9446f] 2015-08-27 12:22:10 -0400
-->

    <listitem>
     <para>
      回移植 9.3 时代新增的逐资源所有者锁缓存（Jeff Janes）
     </para>

     <para>
      当 <application>pg_dump</application> 尝试转储大量表时，这能显著提升性能。
     </para>
    </listitem>

    <listitem>
     <para>
     默认禁用 SSL 重新协商（Michael Paquier、Andres Freund）
    </para>

     <para>
     虽然使用 SSL 重新协商在理论上是个好主意，但实践中我们看到了太多缺陷，无论是底层 OpenSSL 库中还是我们对它的使用中。重新协商将在 9.5 及更高版本中完全移除。在较旧的分支中，只是把<varname>ssl_renegotiation_limit</varname>的默认值改为零（禁用）。
    </para>
    </listitem>

    <listitem>
     <para>
     降低<literal>*_freeze_max_age</literal>参数的最小值（Andres Freund）
    </para>

     <para>
     这主要是为了使相关行为的测试不那么耗时，但对磁盘空间有限的安装也可能有价值。
    </para>
    </listitem>

    <listitem>
     <para>
      将<varname>wal_buffers</varname>的最大值限制为 2GB 以避免服务器崩溃（Josh Berkus）
     </para>
    </listitem>

    <listitem>
     <para>
      修复<type>numeric</type>值相乘时罕见的内部溢出（Dean Rasheed）
     </para>
    </listitem>

    <listitem>
     <para>
      防御涉及记录类型、范围类型、<type>json</type>、<type>jsonb</type>、<type>tsquery</type>、<type>ltxtquery</type>和<type>query_int</type>的难以触发的栈溢出（Noah Misch）
     </para>
    </listitem>

    <listitem>
     <para>
     修复日期时间输入中<literal>DOW</literal>和<literal>DOY</literal>的处理（Greg Stark）
    </para>

     <para>
     这些记号本不应在日期时间值中使用，但过去它们导致的是难懂的内部错误消息，而不是<quote>无效的输入语法</quote>。
    </para>
    </listitem>

    <listitem>
     <para>
      在正则表达式匹配中增加更多查询取消检查（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
     为正则表达式、<literal>SIMILAR TO</literal>和<literal>LIKE</literal>匹配增加递归深度保护（Tom Lane）
    </para>

     <para>
     合适的搜索模式加上较低的栈深度限制可能导致栈越界崩溃。
    </para>
    </listitem>

    <listitem>
     <para>
     修复正则表达式执行中潜在的无限循环（Tom Lane）
    </para>

     <para>
     一个表面上能匹配零长度字符串、但实际上因反向引用而不匹配的搜索模式可能导致无限循环。
    </para>
    </listitem>

    <listitem>
     <para>
      在正则表达式执行中，即使匹配长度为零，也正确记录量词内捕获括号的匹配数据（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复正则表达式编译中的内存不足失败（Andreas Seltenreich）
     </para>
    </listitem>

    <listitem>
     <para>
      修复正则表达式执行期间低概率的内存泄漏（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复事务中止期间锁清理中罕见的内存不足失败（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复在使用较小<varname>work_mem</varname>设置的元组存储时出现<quote>排序期间意外的内存不足情况</quote>错误的问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复<function>qsort</function>中极低概率的栈越界（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复在使用较大<varname>work_mem</varname>设置的哈希连接中出现<quote>无效的内存分配请求大小</quote>失败的问题（Tomas Vondra, Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
     修复若干规划器缺陷（Tom Lane）
    </para>

     <para>
     这些错误可能导致给出错误结果的错误查询计划、启用断言的构建中的断言失败，或奇怪的规划器错误，例如<quote>could not devise a query plan for the given query</quote>、<quote>could not find pathkey item to sort</quote>、<quote>plan should not reference subplan's variable</quote>或<quote>failed to assign all NestLoopParams to plan nodes</quote>。感谢 Andreas Seltenreich 和 Piotr Stefaniak 通过模糊测试暴露了这些问题。
    </para>
    </listitem>

    <listitem>
     <para>
      改进规划器在大型继承集合上执行<command>UPDATE</command>/<command>DELETE</command>的性能（Tom Lane, Dean Rasheed）
     </para>
    </listitem>

    <listitem>
     <para>
     确保在 postmaster 启动时移除备库提升触发文件（Michael Paquier、Fujii Masao）
    </para>

     <para>
     这可以防止这些文件出现在用于初始化新备库服务器的数据库备份中时发生不需要的提升。
    </para>
    </listitem>

    <listitem>
     <para>
     在 postmaster 关闭期间，确保在移除<filename>postmaster.pid</filename>文件之前移除各套接字的锁文件并关闭监听套接字（Tom Lane）
    </para>

     <para>
     这可以避免外部脚本在<literal>pg_ctl stop</literal>一返回就尝试启动新 postmaster 时出现竞争条件失败。
    </para>
    </listitem>

    <listitem>
     <para>
     修复 postmaster 对崩溃恢复期间启动进程崩溃的处理（Tom Lane）
    </para>

     <para>
     如果在崩溃恢复周期中，启动进程在尚未恢复数据库一致性时崩溃，我们会尝试启动一个新的启动进程，而这通常只会再次崩溃，导致无限循环。
    </para>
    </listitem>

    <listitem>
     <para>
      当我们尝试向某个自动清理工作进程发送信号而它已经消失时，不再打印<literal>WARNING</literal>，并降低此类信号的日志详细程度（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      防止服务器时钟被大幅回拨时自动清理启动器睡眠时间过长（&Aacute;lvaro Herrera）
     </para>
    </listitem>

    <listitem>
     <para>
      确保 GIN 索引待插入列表的清理可被取消请求中断（Jeff Janes）
     </para>
    </listitem>

    <listitem>
     <para>
     允许重用 GIN 索引中的全零页（Heikki Linnakangas）
    </para>

     <para>
     崩溃之后可能遗留这样的页。
    </para>
    </listitem>

    <listitem>
     <para>
     修复 SP-GiST 索引中对全零页的处理（Heikki Linnakangas）
    </para>

     <para>
     <command>VACUUM</command> 曾试图回收此类页，但方式并非崩溃安全。
    </para>
    </listitem>

    <listitem>
     <para>
      修复一个差一错误，该错误会导致 subtrans/multixact 截断中出现本可避免的<quote>表面回卷</quote>警告（Thomas Munro）
     </para>
    </listitem>

    <listitem>
     <para>
      修复<application>PL/pgSQL</application>错误上下文消息中<command>CONTINUE</command>和<command>MOVE</command>语句类型误报的问题（Pavel Stehule, Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复<application>PL/Perl</application>以正确处理非<acronym>ASCII</acronym>错误消息文本（Alex Hunsaker）
     </para>
    </listitem>

    <listitem>
     <para>
      修复<application>PL/Python</application>在返回<type>record</type>结果的字符串表示时崩溃的问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复<application>PL/Tcl</application>中一些未检查<function>malloc()</function>调用失败的位置（Michael Paquier, &Aacute;lvaro Herrera）
     </para>
    </listitem>

    <listitem>
     <para>
     在 <filename>contrib/isn</filename> 中，修复以 979 开头的 ISBN-13 号码的输出（Fabien Coelho）
    </para>

     <para>
     以 979（但不以 9790）开头的 EAN 被视为 ISBN，但它们必须以新的 13 位格式而非 10 位格式打印。
    </para>
    </listitem>

<!--
Author: Joe Conway <mail@joeconway.com>
Branch: REL9_2_STABLE [e90a629e1] 2015-09-22 14:58:38 -0700
-->

    <listitem>
     <para>
      修复 <filename>contrib/sepgsql</filename> 对 <command>SELECT INTO</command> 语句的处理（Kohei KaiGai）
     </para>
    </listitem>

    <listitem>
     <para>
      改进<application>libpq</application>对内存耗尽情况的处理（Michael Paquier, Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      修复<application>ecpg</application>中的内存泄漏和缺失的内存不足检查（Michael Paquier）
     </para>
    </listitem>

    <listitem>
     <para>
     修复 <application>psql</application> 中对数字输出的区域感知格式化代码（Tom Lane）
    </para>

     <para>
     <literal>\pset numericlocale on</literal> 所调用的格式化代码在一些不常见的情况下会出错，例如带指数但没有小数点的数字。它还可能破坏 <type>money</type> 数据类型已经区域化的输出。
    </para>
    </listitem>

    <listitem>
     <para>
      防止在没有当前连接时<application>psql</application>的<command>\c</command>命令崩溃（Noah Misch）
     </para>
    </listitem>

    <listitem>
     <para>
      使<application>pg_dump</application>正确处理继承的<literal>NOT VALID</literal>检查约束（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复<application>pg_dump</application>目录输出格式中默认<application>zlib</application>压缩级别的选择问题（Andrew Dunstan）
     </para>
    </listitem>

    <listitem>
     <para>
      确保<application>pg_dump</application>以<acronym>tar</acronym>格式输出运行期间创建的临时文件不被全局可读（Michael Paquier）
     </para>
    </listitem>

    <listitem>
     <para>
      修复<application>pg_dump</application>和<application>pg_upgrade</application>以支持<literal>postgres</literal>或<literal>template1</literal>数据库位于非默认表空间的情况（Marti Raudsepp, Bruce Momjian）
     </para>
    </listitem>

    <listitem>
     <para>
     修复 <application>pg_dump</application> 从过于陈旧而没有特定权限类型的服务器转储时对对象权限的处理（Tom Lane）
    </para>

     <para>
     从 9.2 之前的服务器转储数据类型，以及从 7.3 之前的服务器转储函数或过程语言时，<application>pg_dump</application> 会生成撤销所有者可授予权限、转而将所有权限授予 <literal>PUBLIC</literal> 的 <command>GRANT</command>/<command>REVOKE</command> 命令。由于所涉及的权限只是 <literal>USAGE</literal> 和 <literal>EXECUTE</literal>，这不构成安全问题，但这种表示与旧系统的行为明显不符。现已修复，在这些情况下保持默认权限状态不变。
    </para>
    </listitem>

    <listitem>
     <para>
     修复<application>pg_dump</application>以转储 shell 类型（Tom Lane）
    </para>

     <para>
     shell 类型（即尚未完全定义的类型）用处不大，但<application>pg_dump</application>仍应转储它们。
    </para>
    </listitem>

    <listitem>
     <para>
      修复<application>pg_dump</application>及其他客户端程序中的若干小内存泄漏（Michael Paquier）
     </para>
    </listitem>

    <listitem>
     <para>
     修复 PPC 硬件的自旋锁汇编代码使其与<acronym>AIX</acronym>本机汇编器兼容（Tom Lane）
    </para>

     <para>
     如果<application>gcc</application>被配置为使用本机汇编器（这正变得越来越普遍），则用<application>gcc</application>构建无法工作。
    </para>
    </listitem>

    <listitem>
     <para>
      在<acronym>AIX</acronym>上，测试<literal>-qlonglong</literal>编译器选项，而不是直接假定它可以安全使用（Noah Misch）
     </para>
    </listitem>

    <listitem>
     <para>
     在<acronym>AIX</acronym>上，使用<literal>-Wl,-brtllib</literal>链接选项以允许符号在运行时解析（Noah Misch）
    </para>

     <para>
     Perl 5.8.0 及更高版本依赖这一能力。
    </para>
    </listitem>

    <listitem>
     <para>
      由于编译器缺陷，在使用 32 位<application>xlc</application>编译时避免使用内联函数（Noah Misch）
     </para>
    </listitem>

    <listitem>
     <para>
      必要时使用<filename>librt</filename>提供<function>sched_yield()</function>，某些 Solaris 版本即是如此（Oskari Saarenmaa）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 Windows<filename>install.bat</filename>脚本以处理包含空格的目标目录名（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      使<productname>PostgreSQL</productname>版本号的数字形式（例如<literal>90405</literal>）以名为<varname>VERSION_NUM</varname>的变量方便地提供给扩展 Makefile 使用（Michael Paquier）
     </para>
    </listitem>

    <listitem>
     <para>
      将时区数据文件更新到<application>tzdata</application>发行版 2015g，以反映开曼群岛、斐济、摩尔多瓦、摩洛哥、诺福克岛、朝鲜、土耳其和乌拉圭的夏令时法规变更。为加拿大北落基山地区新增了时区名<literal>America/Fort_Nelson</literal>。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-9-2-13">
  <title>版本 9.2.13</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2015-06-12</para>
  </formalpara>

  <para>
   本次发布包含对 9.2.12 的少量修复。有关 9.2 大版本中新特性的信息，请参见<xref linkend="release-9-2">。.
  </para>

  <sect2>
   <title>迁移到版本 9.2.13</title>

   <para>
    对于运行 9.2.X 的用户，不需要进行转储/恢复。</para>

   <para>
    不过，如果你是从早于 9.2.11 的版本升级，请参见<xref linkend="release-9-2-11">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
     修复罕见的未能使关系缓存 init 文件失效的问题（Tom Lane）
    </para>

     <para>
     在并发活动时序恰好不对的情况下，对系统目录执行<command>VACUUM FULL</command>可能无法更新用于避免新会话缓存加载工作的<quote>init 文件</quote>。这将导致后续会话完全无法访问该目录。这是一个非常古老的缺陷，但它极难触发，直到最近才出现可复现的案例。
    </para>
    </listitem>

    <listitem>
     <para>
     避免传入会话与<literal>CREATE/DROP DATABASE</literal>之间的死锁（Tom Lane）
    </para>

     <para>
     在作为<command>DROP DATABASE</command>命令目标或<command>CREATE DATABASE</command>命令模板的数据库中启动的新会话，可能导致该命令等待五秒后失败，即使新会话本会在此之前退出。
    </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-9-2-12">
  <title>版本 9.2.12</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2015-06-04</para>
  </formalpara>

  <para>
   本次发布包含对 9.2.11 的少量修复。有关 9.2 大版本中新特性的信息，请参见<xref linkend="release-9-2">。.
  </para>

  <sect2>
   <title>迁移到版本 9.2.12</title>

   <para>
    对于运行 9.2.X 的用户，不需要进行转储/恢复。</para>

   <para>
    不过，如果你是从早于 9.2.11 的版本升级，请参见<xref linkend="release-9-2-11">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
     避免崩溃重启期间对数据目录执行<function>fsync</function>时失败（Abhijit Menon-Sen、Tom Lane）
    </para>

     <para>
     在前几个小版本中，我们加入了一个补丁，在崩溃后对数据目录中的所有内容执行<function>fsync</function>。遗憾的是，它对任何错误情况的响应都是失败，从而阻止服务器启动，即使问题相当无害。例如，数据目录中一个不可写的文件在某些平台上会阻止重启；但把 SSL 证书文件设为服务器不可写是很常见的做法。我们修改了这一行为：权限失败被完全忽略，其他类型的失败会被记录但不会阻止继续。
    </para>
    </listitem>

    <listitem>
     <para>
      修复<function>pg_get_functiondef()</function>以在设置了<literal>LEAKPROOF</literal>属性时显示该属性（Jeevan Chalke）
     </para>
    </listitem>

    <listitem>
     <para>
     移除<application>configure</application>中对在<systemitem class="osname">OpenBSD</systemitem>上链接线程版<application>libpython</application>的禁止性检查（Tom Lane）
    </para>

     <para>
     这一限制原本要防止的失败在当前的<systemitem class="osname">OpenBSD</systemitem>版本上似乎已不再是问题。
    </para>
    </listitem>

    <listitem>
     <para>
      允许 <application>libpq</application> 使用高于 v1 的 TLS 协议版本（Noah Misch）
     </para>

     <para>
      长期以来，<application>libpq</application> 的代码只允许使用 TLS v1 这一种 SSL 协议。随着较新的 TLS 版本日益普及，现在允许它与服务器协商双方共同支持的最高 TLS 版本。（<productname>PostgreSQL</productname> 服务器早已支持这种协商，因此服务器端无需更改。）这是对 9.4.0 中已发布变更的回移植。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-9-2-11">
  <title>版本 9.2.11</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2015-05-22</para>
  </formalpara>

  <para>
   本次发布包含对 9.2.10 的多方面修复。有关 9.2 大版本中新特性的信息，请参见<xref linkend="release-9-2">。.
  </para>

  <sect2>
   <title>迁移到版本 9.2.11</title>

   <para>
    对于运行 9.2.X 的用户，不需要进行转储/恢复。</para>

   <para>
     但是，如果你使用<filename>contrib/citext</filename>的<function>regexp_matches()</function>函数，请参见下面相关的变更日志条目。
    </para>

   <para>
    不过，如果你是从早于 9.2.10 的版本升级，请参见<xref linkend="release-9-2-10">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
     避免客户端恰在认证超时到期前断开时可能出现的崩溃（Benkocs Norbert Attila）
    </para>

     <para>
     如果超时中断在会话关闭序列进行到一半时触发，SSL 相关状态会被释放两次，通常导致崩溃并对其他会话造成服务拒绝。实验表明，未经认证的远程攻击者能够相当稳定地触发此缺陷，因此按安全问题处理。（CVE-2015-3165）
    </para>
    </listitem>

    <listitem>
     <para>
     改进对系统调用失败的检测（Noah Misch）
    </para>

     <para>
     我们替换实现的<function>snprintf()</function>未检查底层系统库调用报告的错误；可能被遗漏的主要情况是内存耗尽。在最坏情况下，由于我们的代码假定缓冲区已被覆写而实际未覆写，这可能导致信息暴露。此外，还有少数地方对安全相关的其他系统库函数调用未检查失败。
    </para>

     <para>
     仍有可能某些对<function>*printf()</function>族函数的调用在恰逢内存耗尽错误时存在信息泄露风险。我们判断风险不大，但将继续在此领域进行分析。（CVE-2015-3166）
    </para>
    </listitem>

    <listitem>
     <para>
     在<filename>contrib/pgcrypto</filename>中，统一将解密失败报告为<quote>Wrong key or corrupt data</quote>（Noah Misch）
    </para>

     <para>
      此前，某些使用错误密钥解密的情况可能报告其他错误消息文本。已经证明，这种错误报告的差异可能帮助攻击者从其他系统恢复密钥。虽然尚不清楚<filename>pgcrypto</filename>的具体行为是否同样可被利用，但使用统一消息来避免风险似乎更好。（CVE-2015-3167）
     </para>
    </listitem>

    <listitem>
     <para>
     修复<filename>contrib/citext</filename>的<function>regexp_matches()</function>函数的错误声明（Tom Lane）
    </para>

     <para>
     这些函数应当像它们所包装的核心函数一样返回<type>setof text[]</type>；但它们被错误地声明为只返回<type>text[]</type>。这一错误有两个后果：第一，如果没有匹配，得到的是标量 null 结果，而正确的应该是空集（零行）。第二，<literal>g</literal>标志实际上被忽略了，因为即使有多个匹配也只会得到一个结果数组。
    </para>

     <para>
      虽然后一种行为显然是缺陷，但可能有应用程序依赖前一种行为；因此在<productname>PostgreSQL</productname> 9.5 之前默认不会更改函数声明。在 9.5 之前的分支中，旧行为存在于<literal>citext</literal>扩展的版本 1.0 中，而我们在版本 1.1 中提供了修正后的声明（<emphasis>不</emphasis>默认安装）。要在 9.5 之前的分支中采用修复，请在安装了<literal>citext</literal>的每个数据库中执行<literal>ALTER EXTENSION citext UPDATE TO '1.1'</literal>。（如果需要撤销，也可以<quote>更新</quote>回 1.0。）请注意，任一更新方向都需要删除并重新创建所有使用<filename>citext</filename>的<function>regexp_matches()</function>函数的视图或规则。
     </para>
    </listitem>

    <listitem>
     <para>
     修复 HOT 更新后延迟排他约束检查不正确的问题（Tom Lane）
    </para>

     <para>
     如果同一事务中稍后对一条可能违反延迟排他约束的新行进行了 HOT 更新（即没有索引列发生变化，且该行可以存回同一表页），那么最终执行检查时将报告排他约束被违反，即使新行最初与之冲突的行已被删除。
    </para>
    </listitem>

    <listitem>
     <para>
     修复星型模式风格查询的规划（Tom Lane）
    </para>

     <para>
     有时，对大表的高效扫描要求索引参数来自不止一个其他表（通常是维度表，其键用于对大型事实表建立索引）。规划器应当能找到此类计划，但一个过于严苛的搜索启发式阻止了这一点。
    </para>
    </listitem>

    <listitem>
     <para>
     防止反连接（NOT EXISTS 连接）与其他外连接之间的不当重排序（Tom Lane）
    </para>

     <para>
     已知规划器中的这一疏忽会导致 <quote>could not find RelOptInfo for given relids</quote> 错误，但有时错误的查询计划似乎有可能通过该一致性检查并导致静默错误的查询输出。
    </para>
    </listitem>

    <listitem>
     <para>
     修复外连接计划节点中子表达式匹配不正确的问题（Tom Lane）
    </para>

     <para>
     此前，如果文本相同的非严格子表达式在外连接的上、下方都被使用，规划器可能试图重用在连接下方计算的值，但这是不正确的，因为对于未匹配的外部行，执行器会将该值强制置为 NULL。
    </para>
    </listitem>

    <listitem>
     <para>
     修复 GEQO 规划器以应对其连接顺序启发式的失败（Tom Lane）
    </para>

     <para>
     已知这一疏忽会在涉及 <literal>LATERAL</literal> 的查询中导致 <quote>failed to join all relations together</quote> 错误，其他情况下也可能发生。
    </para>
    </listitem>

    <listitem>
     <para>
      修复<literal>max_prepared_transactions</literal>过小时启动阶段可能出现的死锁（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      时间线切换后不再归档无用的预分配 WAL 文件（Heikki Linnakangas）
     </para>
    </listitem>

<!--
Author: Alvaro Herrera <alvherre@alvh.no-ip.org>
Branch: REL9_2_STABLE [97ff2a564] 2015-05-18 17:44:21 -0300
Branch: REL9_1_STABLE [2360eea3b] 2015-05-18 17:44:21 -0300
Branch: REL9_0_STABLE [850e1a566] 2015-05-18 17:44:21 -0300
-->

    <listitem>
     <para>
      避免 <quote>cannot GetMultiXactIdMembers() during recovery</quote> 错误（&Aacute;lvaro Herrera）
     </para>
    </listitem>

    <listitem>
     <para>
     崩溃之后递归地对数据目录执行 <function>fsync()</function>（Abhijit Menon-Sen、Robert Haas）
    </para>

     <para>
     这确保了若随后不久再发生崩溃时数据仍保持一致。（要有问题的话，第二次崩溃必须是系统级崩溃，而不只是数据库崩溃。）
    </para>
    </listitem>

    <listitem>
     <para>
      修复自动清理启动器在收到 SIGTERM 之后发生错误时可能无法关闭的问题（&Aacute;lvaro Herrera）
     </para>
    </listitem>

    <listitem>
     <para>
     处理 <function>LockBufferForCleanup()</function> 中的意外信号（Andres Freund）
    </para>

     <para>
     这一疏忽可能导致有关 <quote>multiple backends attempting to wait for pincount 1</quote> 的虚假错误。
    </para>
    </listitem>

    <listitem>
     <para>
     修复向带有包含整行引用的检查约束的表执行 <literal>COPY IN</literal> 时的崩溃（Tom Lane）
    </para>

     <para>
     已知的失败情形只在 9.4 及以上版本中崩溃，但 9.3 和 9.2 中也有非常相似的代码，因此这些分支也被向后修补。
    </para>
    </listitem>

    <listitem>
     <para>
     就用户而言是只读的事务，在提交期间不再等待 WAL 刷写或同步复制（Andres Freund）
    </para>

     <para>
     此前，由于 HOT 页清理而写入了 WAL 的事务在提交时可能出现延迟，导致不良影响，例如所有同步副本都停机时会话在启动时卡住。还观察到使用同步复制时会话卡在追赶中断处理中；此修复也将解决该问题。
    </para>
    </listitem>

    <listitem>
     <para>
      修复在临时表上操作哈希索引时崩溃的问题（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      修复哈希索引桶分裂期间（若其他进程正在并发修改该索引）可能失败的问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
     在分析索引表达式期间检查中断（Jeff Janes）
    </para>

     <para>
     <command>ANALYZE</command> 会多次执行索引表达式；如果此类表达式中存在慢速函数，就希望能在此循环结束之前取消 <command>ANALYZE</command>。
    </para>
    </listitem>

    <listitem>
     <para>
      确保在通过<command>SELECT FOR UPDATE</command>、<command>UPDATE</command>或<command>DELETE</command>锁定行之后发生<literal>READ COMMITTED</literal>复查时，外部表的<structfield>tableoid</structfield>被正确报告（Etsuro Fujita）
     </para>
    </listitem>

    <listitem>
     <para>
      在外部服务器用户映射的对象描述字符串中加入目标服务器的名称（&Aacute;lvaro Herrera）
     </para>
    </listitem>

    <listitem>
     <para>
     建议在使用 Kerberos/GSSAPI/SSPI 认证时将 <literal>include_realm</literal> 设置为 1（Stephen Frost）
    </para>

     <para>
     不这样设置的话，来自不同领域的同名用户无法区分。目前这只是文档变更，但在 <productname>PostgreSQL</productname> 9.5 中它将成为默认设置。
    </para>
    </listitem>

    <listitem>
     <para>
     移除将 IPv4 <filename>pg_hba.conf</filename> 条目匹配到 IPv4-in-IPv6 地址的代码（Tom Lane）
    </para>

     <para>
     这段应急代码于 2003 年添加，起因是当时的某些 Linux 内核会把 IPv4 连接报告为 IPv4-in-IPv6 地址。然而该逻辑在 9.0 中被意外破坏。此后没有任何现场投诉，说明它已不再需要。现在反而有报告称被破坏的代码在某些系统上导致崩溃，因此我们选择直接移除而不是修复它。（如果选择修复，将会使 IPv4 <filename>pg_hba.conf</filename> 条目的实际含义发生微妙且可能涉及安全性的变化，在次版本中这样做似乎并不妥当。）
    </para>
    </listitem>

    <listitem>
     <para>
     在<literal>IDENTIFY_SYSTEM</literal>复制命令中报告 WAL 刷写位置而非插入位置（Heikki Linnakangas）
    </para>

     <para>
     这避免了<application>pg_receivexlog</application>可能出现的启动失败。
    </para>
    </listitem>

    <listitem>
     <para>
      在 Windows 上关闭服务期间，定期向服务控制管理器发送状态更新，以防止它过早终止服务；并确保<application>pg_ctl</application>会等待关闭完成（Krystian Bigaj）
     </para>
    </listitem>

    <listitem>
     <para>
     降低使用<application>libpq</application>非阻塞模式时网络死锁的风险（Heikki Linnakangas）
    </para>

     <para>
     在发送大量数据时，定期排空输入缓冲区很重要，以防服务器已发送足够的响应数据导致其在输出上阻塞。（典型场景是服务器在<literal>COPY FROM STDIN</literal>期间发送一连串 NOTICE 消息。）这在普通阻塞模式下工作正常，但在非阻塞模式下则不然。我们修改了<application>libpq</application>，使其在可能时顺便排空输入，但要完全防御此问题需要应用程序配合：应用程序应同时监视套接字的读就绪和写就绪条件，并确保在读就绪时调用<function>PQconsumeInput()</function>。
    </para>
    </listitem>

    <listitem>
     <para>
      修复<application>libpq</application>中 URI 连接串空值解析错误的问题（Thomas Fanghaenel）
     </para>
    </listitem>

    <listitem>
     <para>
      修复<application>ecpg</application>中的数组处理（Michael Meskes）
     </para>
    </listitem>

    <listitem>
     <para>
     修复<application>psql</application>以合理处理作为<command>\connect</command>第一个参数的 URI 和 conninfo 串（David Fetter、Andrew Dunstan, &Aacute;lvaro Herrera）
    </para>

     <para>
     这一语法已被接受（但未写入文档）很长时间，但此前某些参数可能取自旧连接而非给定字符串，这被认为是不合适的。
    </para>
    </listitem>

    <listitem>
     <para>
     抑制<application>psql</application>在某些平台上关于退出时未能写入<filename>~/.psql_history</filename>的错误抱怨（Tom Lane）
    </para>

     <para>
     这一不当行为源自针对非常老（2006 年之前）的<application>libedit</application>版本中一个缺陷的变通方案。我们通过移除该变通方案修复了问题，但这会使仍在使用此类<application>libedit</application>版本的用户出现类似失败。建议：升级该库，或改用<application>libreadline</application>。
    </para>
    </listitem>

    <listitem>
     <para>
      修复<application>pg_dump</application>判定哪些转换是不应转储的系统提供转换的规则（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复<application>pg_dump</application>在<literal>-Fd</literal>下未遵循<literal>-Z</literal>压缩级别选项的问题（Michael Paquier）
     </para>
    </listitem>

    <listitem>
     <para>
     使<application>pg_dump</application>在选择转储顺序时考虑扩展配置表之间的外键关系（Gilles Darold、Michael Paquier、Stephen Frost）
    </para>

     <para>
     这一疏忽可能导致生成的转储无法重新加载，因为外键约束会被瞬时违反。
    </para>
    </listitem>

    <listitem>
     <para>
      修复对仅由<literal>VALUES(...)</literal>构成但带有列别名的视图的转储（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
     在<application>pg_upgrade</application>中，强制新集簇使用时间线 1（Bruce Momjian）
    </para>

     <para>
     此变更可防止因错误抱怨缺少 WAL 历史文件而导致的升级失败。
    </para>
    </listitem>

    <listitem>
     <para>
      在<application>pg_upgrade</application>中，继续之前先检查是否存在不当不可连接的数据库（Bruce Momjian）
     </para>
    </listitem>

    <listitem>
     <para>
      在<application>pg_upgrade</application>中，在生成的<literal>delete_old_cluster</literal>脚本中对目录路径正确加引号（Bruce Momjian）
     </para>
    </listitem>

    <listitem>
     <para>
     在<application>pg_upgrade</application>中正确保留数据库级冻结信息（Bruce Momjian）
    </para>

     <para>
      这一疏忽可能导致<literal>postgres</literal>和<literal>template1</literal>数据库中的表出现缺少 clog 文件的错误。
     </para>
    </listitem>

    <listitem>
     <para>
      在 Windows 上以受限权限运行<application>pg_upgrade</application>和<application>pg_resetxlog</application>，使它们在由管理员运行时不会失败（Muhammad Asif Naeem）
     </para>
    </listitem>

    <listitem>
     <para>
      改进<application>initdb</application>和<application>pg_basebackup</application>扫描目录时对<function>readdir()</function>失败的处理（Marco Nenciarini）
     </para>
    </listitem>

<!--
Author: Andres Freund <andres@anarazel.de>
Branch: REL9_2_STABLE [6b700301c] 2015-02-17 16:03:00 +0100
-->

    <listitem>
     <para>
      修复 <application>pg_receivexlog</application> 中的失败（Andres Freund）
     </para>

     <para>
      9.2.10 中的一个补丁合并失误导致了 <quote>could not create archive status file</quote> 错误。
     </para>
    </listitem>

    <listitem>
     <para>
      修复<filename>contrib/intarray</filename>中缓慢的排序算法（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 Sparc V8 机器上的编译失败（Rob Rowan）
     </para>
    </listitem>

    <listitem>
     <para>
      将时区数据文件更新到<application>tzdata</application>发行版 2015d，以反映埃及、蒙古和巴勒斯坦的夏令时法规变更，以及加拿大和智利的历史变更。同时采用 America/Adak 时区修订后的时区缩写（HST/HDT 而非 HAST/HADT）。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-9-2-10">
  <title>版本 9.2.10</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2015-02-05</para>
  </formalpara>

  <para>
   本次发布包含对 9.2.9 的多方面修复。有关 9.2 大版本中新特性的信息，请参见<xref linkend="release-9-2">。.
  </para>

  <sect2>
   <title>迁移到版本 9.2.10</title>

   <para>
    对于运行 9.2.X 的用户，不需要进行转储/恢复。</para>

   <para>
     但是，如果你是 Windows 用户且使用<quote>Norwegian (Bokm&aring;l)</quote>区域，升级后需要手工将<productname>PostgreSQL</productname>系统目录中存储的任何<quote>Norwegian (Bokm&aring;l)_Norway</quote>区域名替换为纯 ASCII 别名<quote>Norwegian_Norway</quote>。详情参见<ulink url="http://wiki.postgresql.org/wiki/Changes_To_Norwegian_Locale"></ulink>
    </para>

   <para>
    不过，如果你是从早于 9.2.9 的版本升级，请参见<xref linkend="release-9-2-9">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
     修复<function>to_char()</function>中的缓冲区越界（Bruce Momjian）
    </para>

     <para>
      当<function>to_char()</function>处理要求数字位数很多的数字格式模板时，<productname>PostgreSQL</productname>会读取越过缓冲区末尾。当处理恶意构造的时间戳格式模板时，<productname>PostgreSQL</productname>会写入越过缓冲区末尾。两种情况都可能导致服务器崩溃。我们尚未排除导致权限提升攻击的可能性，尽管这种攻击似乎不太可能。（CVE-2015-0241）
     </para>
    </listitem>

    <listitem>
     <para>
     修复替换实现的<function>*printf()</function>函数中的缓冲区越界（Tom Lane）
    </para>

     <para>
     <productname>PostgreSQL</productname>包含<function>printf</function>及相关函数的替换实现。当代码格式化浮点数（转换说明符<literal>e</literal>、<literal>E</literal>、<literal>f</literal>、<literal>F</literal>、<literal>g</literal>或<literal>G</literal>）且请求的精度超过约 500 时，会发生栈缓冲区越界。这会导致服务器崩溃，我们尚未排除导致权限提升攻击的可能性。数据库用户可以通过<function>to_char()</function>SQL 函数触发这种缓冲区越界。虽然这是唯一受影响的核心<productname>PostgreSQL</productname>功能，但使用 printf 族函数的扩展模块也可能存在风险。
    </para>

     <para>
     此问题主要影响 Windows 上的<productname>PostgreSQL</productname>。<productname>PostgreSQL</productname>在系统实现足够完善时会使用系统实现，而其他现代平台上正是如此。（CVE-2015-0242）
    </para>
    </listitem>

    <listitem>
     <para>
     修复<filename>contrib/pgcrypto</filename>中的缓冲区越界（Marko Tiikkaja、Noah Misch）
    </para>

     <para>
     <filename>pgcrypto</filename>模块中内存大小跟踪的错误导致了栈缓冲区越界和对未初始化内存内容的不当依赖。缓冲区越界情况可能导致服务器崩溃，我们尚未排除导致权限提升攻击的可能性。（CVE-2015-0243）
    </para>
    </listitem>

    <listitem>
     <para>
     修复出错后前端/后端协议可能失去同步的问题（Heikki Linnakangas）
    </para>

     <para>
     如果服务器在从客户端读取协议消息中途发生任何错误，它可能失去同步，错误地把消息数据的一部分解释为新的协议消息。能够在命令参数中提交恶意构造二进制数据的攻击者可能借此注入自己的 SQL 命令。语句超时和查询取消是触发此场景最可能的错误来源。尤其容易受影响的是既使用超时又将任意用户构造数据作为二进制查询参数提交的应用程序。禁用语句超时会降低但不能消除被利用的风险。感谢 Emil Lenngren 报告此问题。（CVE-2015-0244）
    </para>
    </listitem>

    <listitem>
     <para>
     修复通过约束违反错误消息的信息泄露（Stephen Frost）
    </para>

     <para>
     某些服务器错误消息会显示违反约束（如唯一约束）的列的值。如果用户对该表的所有列没有<literal>SELECT</literal>权限，这可能意味着暴露用户不应看到的值。我们调整了代码，使值仅在来自 SQL 命令本身或用户可以查询到时才显示。（CVE-2014-8161）
    </para>
    </listitem>

    <listitem>
     <para>
     锁定 Windows 上回归测试的临时安装（Noah Misch）
    </para>

     <para>
     使用 SSPI 认证，只允许启动测试套件的操作系统用户连接。这在 Windows 上关闭了此前已在其他平台上关闭的同一漏洞，即其他用户可能连接到测试 postmaster。（CVE-2014-0067）
    </para>
    </listitem>

    <listitem>
     <para>
     处理名为<quote>Norwegian (Bokm&aring;l)</quote>的 Windows 区域（Heikki Linnakangas）
    </para>

     <para>
     非 ASCII 区域名是有问题的，因为不清楚应该用什么编码表示它们。将这个麻烦的区域名映射为纯 ASCII 别名<quote>Norwegian_Norway</quote>。
    </para>
    </listitem>

    <listitem>
     <para>
      避免在使用 <command>ALTER DATABASE SET TABLESPACE</command> 把数据库移到新表空间、随后不久又移回原表空间时可能出现的数据损坏（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      避免当事务内的 <command>ANALYZE</command> 被回滚时损坏表（Andres Freund、Tom Lane、Michael Paquier）
     </para>

     <para>
      如果失败的事务此前删除了表上的最后一个索引、规则或触发器，该表会处于损坏状态：相关的 <structname>pg_class</structname> 标志本应设置却未被设置。
     </para>
    </listitem>

    <listitem>
     <para>
      确保在 <command>CREATE DATABASE</command> 或 <command>ALTER DATABASE SET TABLESPACE</command> 期间正确复制不记录日志的表（Pavan Deolasee、Andres Freund）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <command>DROP</command> 的依赖搜索，使其正确处理表的列在其所属表之前被递归访问的情况（Petr Jelinek、Tom Lane）
     </para>

     <para>
      目前已知这种情况只在扩展同时创建一种数据类型和使用该数据类型的表时出现。有问题的代码可能会拒绝 <command>DROP EXTENSION</command>，除非指定 <literal>CASCADE</literal>，而这本不应该是必需的。
     </para>
    </listitem>

    <listitem>
     <para>
     修复 EvalPlanQual 处理中使用已释放内存的问题（Tom Lane）
    </para>

     <para>
     在<literal>READ COMMITTED</literal>模式下，锁定或更新最近更新过的行的查询可能因此缺陷崩溃。
    </para>
    </listitem>

    <listitem>
     <para>
      修复在子表上使用部分索引时 <command>SELECT FOR UPDATE</command> 的规划问题（Kyotaro Horiguchi）
     </para>

     <para>
      在 <literal>READ COMMITTED</literal> 模式下，<command>SELECT FOR UPDATE</command> 在重新检查最近更新的行以确认它是否仍满足查询的 <literal>WHERE</literal> 条件时，还必须重新检查部分索引的 <literal>WHERE</literal> 条件。如果索引属于继承子表，就会遗漏这一要求，因此有可能错误地返回已不再满足查询条件的行。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <command>SELECT FOR UPDATE</command> 在极端情况下可能把同一行返回两次、并且可能漏掉其他行的 corner case（Tom Lane）
     </para>

     <para>
      在 <literal>READ COMMITTED</literal> 模式下，扫描继承树的 <command>SELECT FOR UPDATE</command> 可能错误地返回来自前一个子表的行，而不是本应从后一个子表返回的行。
     </para>
    </listitem>

    <listitem>
     <para>
      拒绝 <literal>FOREIGN KEY</literal> 声明的被引用列列表中出现重复的列名（David Rowley）
     </para>

     <para>
      这一限制符合 SQL 标准。此前我们并未显式拒绝这种情况，但之后代码会以看起来很怪异的错误失败。
     </para>
    </listitem>

    <listitem>
     <para>
      恢复域转换为 JSON 的原有行为（Tom Lane）
     </para>

     <para>
      此变更使基于 numeric 和 boolean 的域在转换为 JSON 时被视为其基础类型。在 9.3.5 和 9.2.9 之前就是这样工作的，但在修复一个相关问题时不慎改变了行为。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <type>numeric</type> 值求大整数次幂时的缺陷（Tom Lane）
     </para>

     <para>
      之前的代码可能得到错误答案，或者在意识到结果必然溢出之前消耗过多的时间和内存。
     </para>
    </listitem>

    <listitem>
     <para>
      在 <function>numeric_recv()</function> 中，按照值的 <literal>dscale</literal> 字段截去会被隐藏的小数位（Tom Lane）
     </para>

     <para>
      <type>numeric</type> 值的显示标度（<literal>dscale</literal>）绝不应小于非零小数位的位数；但显然至少存在一个有缺陷的客户端应用，它发送的二进制 <type>numeric</type> 值违反了这一点。这会导致奇怪的行为，因为多余的数字位虽然不会被打印出来，却会被算术运算考虑在内。风险最小的修复方法似乎是在接收时把这类<quote>隐藏的</quote>数字位截去，使值确实与它打印出来的一致。
     </para>
    </listitem>

    <listitem>
     <para>
      修复正则表达式最短优先匹配搜索不正确的问题（Tom Lane）
     </para>

     <para>
      当允许的迭代次数受到 <literal>?</literal> 量词或边界表达式限制时，匹配常常会失败。
     </para>
    </listitem>

    <listitem>
     <para>
      拒绝超出范围的数字时区规范（Tom Lane）
     </para>

     <para>
      超过 +/- 168 小时（一周）的简单数字时区规范此前会被接受，但随后可能在某些操作中导致空指针解引用崩溃。如此大的 UTC 偏移没有实际使用场景，因此予以拒绝。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <type>tsquery</type> <literal>@&gt;</literal> <type>tsquery</type> 操作符中的缺陷（Heikki Linnakangas）
     </para>

     <para>
      两个不同的词如果 CRC 相同，就会被认为匹配。此外，如果第二个操作数的词条比第一个多，就会被假定不包含于第一个；这是错误的，因为它可能包含重复词条。
     </para>
    </listitem>

    <listitem>
     <para>
      增强 ispell 词典对有问题的词缀文件的防御（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      允许词典（thesaurus）包含超过 64K 个短语（David Boutin）
     </para>

     <para>
      之前的代码在词典过大时会崩溃，因此这被视为一个可回移植的缺陷修复，而不是新特性。
     </para>
    </listitem>

    <listitem>
     <para>
     修复<function>xpath()</function>中的命名空间处理（Ali Akbar）
    </para>

     <para>
     此前，如果命名空间声明附在输入<type>xml</type>值的祖先元素上而非被返回的特定元素上，<function>xpath()</function>调用产生的<type>xml</type>值就不会包含命名空间声明。我们将祖先声明传播下来，使结果在孤立考虑时也是正确的。
    </para>
    </listitem>

    <listitem>
     <para>
     确保整行变量向关注组合参数内列名的函数暴露预期的列名（Tom Lane）
    </para>

     <para>
      在某些上下文中，<literal>row_to_json(tab.*)</literal> 这类构造可能不会产生预期的列名。这个问题在 9.4 中已正确修复；在较旧的分支中，只是确保产生某个非空名称。（某些情况下，这个名称会是底层表的列名，而不是理论上应该可见的由查询赋予的别名。）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 FDW 查询中对系统列（特别是 <structfield>tableoid</structfield>）的错误处理（Etsuro Fujita）
     </para>
    </listitem>

    <listitem>
     <para>
      当把 <literal><replaceable>indexed_column</replaceable> = ANY (<replaceable>array</replaceable>)</literal> 用作索引限定条件会导致更差的计划时，避免这样做（Andrew Gierth）
     </para>

     <para>
      某些情况下，作用于非首个索引列的 <literal>= ANY</literal> 条件会被当作索引条件执行，尽管把它们用作简单的过滤条件会更好。
     </para>
    </listitem>

    <listitem>
     <para>
      修复嵌套 append 关系（例如 <literal>UNION ALL</literal> 子查询中的继承表）的规划器问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      当 GiST 索引元组放不进一个页面时干净地报错，而不是陷入无限递归（Andrew Gierth）
     </para>
    </listitem>

    <listitem>
     <para>
      把设有逐表 <varname>cost_limit</varname> 和/或 <varname>cost_delay</varname> 设置的表排除在自动清理的全局代价平衡规则之外（&Aacute;lvaro Herrera）
     </para>

     <para>
      先前的行为实质上忽略了这些逐表设置，这并非本意。现在，拥有这类设置的表将按照这些设置进行清理，独立于其他自动清理工作进程的状况。这可能导致比以前更重的总 I/O 负载，因此应当重新审视这类设置是否合理。
     </para>
    </listitem>

    <listitem>
     <para>
      避免在自动清理名义上被关闭时仍然进行全面的自动清理（Tom Lane）
     </para>

     <para>
      即使自动清理名义上被关闭，我们仍会启动自动清理工作进程，去清理有 XID 回卷风险的表。然而，这样的工作进程随后会继续清理目标数据库中所有满足常规自动清理阈值的表。这往好里说是相当出乎意料，往坏里说会延误对回卷威胁的响应。现已修复：如果自动清理被关闭，工作进程<emphasis>只</emphasis>执行防回卷清理，不做任何其他工作。
     </para>
    </listitem>

    <listitem>
     <para>
      在崩溃恢复期间，确保不记录日志的关系被重写为空并在恢复被视为完成之前同步到磁盘（Abhijit Menon-Sen、Andres Freund）
     </para>

     <para>
      这可以避免数据库崩溃恢复之后不记录日志的关系中残留垃圾数据的情形。
     </para>
    </listitem>

    <listitem>
     <para>
      修复热备查询与重放整页镜像之间的竞态条件（Heikki Linnakangas）
     </para>

     <para>
      这一失误可能导致热备中正在执行的查询出现瞬时错误。
     </para>
    </listitem>

    <listitem>
     <para>
      修复若干恢复逻辑不当忽略 <literal>COMMIT/ABORT PREPARED</literal> WAL 记录的情况（Heikki Linnakangas）
     </para>

     <para>
      最值得注意的疏漏是 <varname>recovery_target_xid</varname> 无法用于在两阶段提交处停止。
     </para>
    </listitem>

    <listitem>
     <para>
      防止在崩溃恢复完成时把最新的 WAL 文件再次归档（Fujii Masao）
     </para>
    </listitem>

    <listitem>
     <para>
      避免为时间线历史文件创建不必要的 <filename>.ready</filename> 标记文件（Fujii Masao）
     </para>
    </listitem>

    <listitem>
     <para>
      修复当使用空的预备语句且 <varname>log_statement</varname> 设置为 <literal>mod</literal> 或 <literal>ddl</literal> 时可能出现的空指针解引用（Fujii Masao）
     </para>
    </listitem>

    <listitem>
     <para>
     将 <quote>pgstat wait timeout</quote> 警告消息改为 LOG 级别，并重新措辞使其更易理解（Tom Lane）
    </para>

     <para>
     这条消息最初被认为基本属于不可能发生的情况，但它在我们较慢的 buildfarm 成员上出现得足够频繁，成了干扰。现已将其降为 LOG 级别，并在措辞上多下了一点功夫：现在它的内容为 <quote>using stale statistics instead of current ones because stats collector is not responding</quote>。
    </para>
    </listitem>

    <listitem>
     <para>
      修复 SPARC 自旋锁实现，确保当 CPU 以非 TSO 一致性模式运行时（如某些非 Solaris 内核所做的那样）其正确性（Andres Freund）
     </para>
    </listitem>

    <listitem>
     <para>
      当 macOS 的<function>setlocale()</function>在 postmaster 内启动多余的线程时发出警告（Noah Misch）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>PQconnectdbParams()</function> 中重复 <literal>dbname</literal> 参数的处理（Alex Shulgin）
     </para>

     <para>
      如果第一个 <literal>dbname</literal> 出现处包含要展开的连接串或 URI，就会出现意外行为。
     </para>
    </listitem>

    <listitem>
     <para>
      确保 <application>libpq</application> 在遇到意外的套接字 EOF 时报告合适的错误消息（Marko Tiikkaja、Tom Lane）
     </para>

     <para>
      取决于内核行为，当服务器意外关闭套接字时，<application>libpq</application> 可能返回空的错误字符串而不是有用的信息。
     </para>
    </listitem>

    <listitem>
     <para>
      在 <function>PQreset()</function> 期间清除任何旧的错误消息（Heikki Linnakangas）
     </para>

     <para>
      如果反复调用 <function>PQreset()</function> 而连接始终无法重建，失败连接尝试的错误消息会在 <structname>PGconn</structname> 的错误字符串中不断累积。
     </para>
    </listitem>

    <listitem>
     <para>
      正确处理 <application>libpq</application> 解析连接选项期间的内存不足情况（Alex Shulgin、Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>ecpg</application> 版 <function>ParseDateTime()</function> 中的数组越界（Michael Paquier）
     </para>
    </listitem>

    <listitem>
     <para>
      在 <application>initdb</application> 中，当指定的密码文件为空时给出更清晰的错误消息（Mats Erik Andersson）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>psql</application> 的 <command>\s</command> 命令，使其与 libedit 良好配合，并新增分页器支持（Stepan Rutz、Tom Lane）
     </para>

     <para>
      使用 libedit 而非 readline 时，<command>\s</command> 会以一种相当难读的编码格式打印命令历史，而且在较新的 libedit 版本上可能完全失败。现已改为由我们自己打印历史而不是让库来做。一个令人愉快的副作用是，在合适的时候会使用分页器。
     </para>

     <para>
      此补丁还修复了一个在使用 libedit 保存命令历史时换行编码不一致的缺陷。旧版 <application>psql</application> 写出的多行历史条目在此补丁后可以干净地读取，但反过来未必可行，具体取决于所涉及的 libedit 版本。
     </para>
    </listitem>

    <listitem>
     <para>
     改进 <application>psql</application> 特殊变量解析的一致性（Tom Lane）
    </para>

     <para>
     允许 <literal>ECHO_HIDDEN</literal> 和 <literal>ON_ERROR_ROLLBACK</literal> 使用 <literal>on</literal> 和 <literal>off</literal> 的变体拼写（如 <literal>1</literal>/<literal>0</literal>）。对 <literal>COMP_KEYWORD_CASE</literal>、<literal>ECHO</literal>、<literal>ECHO_HIDDEN</literal>、<literal>HISTCONTROL</literal>、<literal>ON_ERROR_ROLLBACK</literal> 和 <literal>VERBOSITY</literal> 的无法识别的值发出警告。对所有这些变量的所有值进行不区分大小写的识别；此前大小写敏感与不敏感的行为混杂在一起。
    </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>psql</application> 扩展模式显示在使用 <literal>border</literal> = 3 且 <literal>linestyle</literal> = <literal>ascii</literal> 或 <literal>unicode</literal> 时的一致性问题（Stephen Frost）
     </para>
    </listitem>

    <listitem>
     <para>
      改进当数据库中大量存在同一对对象之间的多条依赖路径时 <application>pg_dump</application> 的性能（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_dumpall</application>，恢复其从 8.1 之前服务器转储的能力（Gilles Darold）
     </para>
    </listitem>

    <listitem>
     <para>
      修复并行恢复纯模式转储时可能出现的死锁（Robert Haas、Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>pg_dump --binary-upgrade</literal> 在零列组合类型上的核心转储（Rushabh Lathia）
     </para>
    </listitem>

    <listitem>
     <para>
      防止由<literal>pg_basebackup -x/-X</literal>创建的 WAL 文件在备库提升后被再次归档（Andres Freund）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/auto_explain</filename> 在执行 <command>EXPLAIN ANALYZE</command> 时未能打印逐节点计时信息的问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/citext</filename> 的从非打包升级脚本（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/pageinspect</filename> 的 <function>get_raw_page()</function> 中的块号检查（Tom Lane）
     </para>

     <para>
      错误的检查逻辑可能阻止访问非主关系分支中的某些页面。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/pgcrypto</filename> 的 <function>pgp_sym_decrypt()</function>，使其在消息长度比 2 的幂小 6 时不再失败（Marko Tiikkaja）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/pg_test_fsync</filename> 中的文件描述符泄漏（Jeff Janes）
     </para>

     <para>
      这可能导致 Windows 上无法删除临时文件。
     </para>
    </listitem>

    <listitem>
     <para>
     安全地处理 <filename>contrib/tablefunc</filename> 的 <function>connectby()</function> 中的意外查询结果，尤其是 NULL（Michael Paquier）
    </para>

     <para>
     此前 <function>connectby()</function> 遇到 NULL 键值时会崩溃。现在它会打印该行但不再进一步递归。
    </para>
    </listitem>

    <listitem>
     <para>
      避免 <filename>contrib/xml2</filename> 的 <function>xslt_process()</function> 中可能出现的崩溃（Mark Simonetti）
     </para>

     <para>
      <application>libxslt</application> 似乎对资源释放顺序存在未记载的依赖；重新排列我们的调用以避免崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      为一些 <filename>contrib</filename> I/O 函数标记正确的易变性属性（Tom Lane）
     </para>

     <para>
      先前过于保守的标记在正常使用中无关紧要，但可能导致优化问题或拒绝有效的索引表达式定义。由于影响不大，我们只是调整了扩展模块脚本中的函数定义，未更改版本号。
     </para>
    </listitem>

    <listitem>
     <para>
     大量清理 Coverity 静态代码分析器的警告（Andres Freund、Tatsuo Ishii、Marko Kreen、Tom Lane、Michael Paquier）
    </para>

     <para>
     这些变更大多只是表面性的，但在某些情况下修复了极端情况的缺陷，例如内存不足失败后崩溃而不是正确报告错误。据信它们都不构成安全问题。
    </para>
    </listitem>

    <listitem>
     <para>
      在构建期间检测不兼容的 OpenLDAP 版本（Noah Misch）
     </para>

     <para>
      对于 2.4.24 到 2.4.31（含）之间的 OpenLDAP 版本，<productname>PostgreSQL</productname> 后端进程可能在退出时崩溃。现在会根据编译时的 OpenLDAP 版本号在 <application>configure</application> 期间发出警告，并在 <filename>contrib/dblink</filename> 回归测试中测试该崩溃场景。
     </para>
    </listitem>

    <listitem>
     <para>
      在非 MSVC 的 Windows 构建中，确保安装的 <filename>libpq.dll</filename> 带有执行权限（Noah Misch）
     </para>
    </listitem>

    <listitem>
     <para>
     使 <application>pg_regress</application> 成功退出时删除其创建的任何临时安装（Tom Lane）
    </para>

     <para>
     这使 <literal>make check-world</literal> 期间的磁盘空间用量大幅减少，因为该序列会创建大量临时安装。
    </para>
    </listitem>

    <listitem>
     <para>
     支持随时间改变 UTC 偏移的时区缩写（Tom Lane）
    </para>

     <para>
     此前，<productname>PostgreSQL</productname> 假定时区缩写（如 <literal>EST</literal>）关联的 UTC 偏移在任何特定区域设置的用法中永不改变。然而这一假设在现实世界中并不成立，因此引入了让区域缩写表示有时会变化的 UTC 偏移的能力。更新了时区缩写定义文件，在自 1970 年以来缩写的 UTC 偏移发生过变化的时区区域设置中使用此功能（依据 IANA 时区数据库）。在此类时区中，<productname>PostgreSQL</productname> 现在会根据给定日期为缩写关联正确的 UTC 偏移。
    </para>
    </listitem>

    <listitem>
     <para>
      更新时区缩写列表（Tom Lane）
     </para>

     <para>
      把 CST（中国标准时间）加入列表。移除 ADT 作为<quote>阿拉伯夏令时</quote>的引用——这个缩写自 2007 年起已不再使用；因此，声称它与<quote>大西洋夏令时</quote>冲突似乎没有特别的帮助。修正 CKT（库克群岛）、FJT 和 FJST（斐济）完全错误的 GMT 偏移；此前我们甚至把它们放到了日期变更线的错误一侧。
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2015a。</para>

     <para>
      IANA 时区数据库为所有澳大利亚时区采用了 <literal>A<replaceable>x</replaceable>ST</literal>/<literal>A<replaceable>x</replaceable>DT</literal> 形式的缩写，以反映他们认为的当地当前多数用法。这些名称与其他地方的用法不冲突（唯一例外是表示 Acre 夏令时的 ACST，该用法自 1994 年起已废弃）。因此，我们把这些名称采纳进<quote>默认</quote>时区缩写集。<quote>澳大利亚</quote>缩写集现在只包含 CST、EAST、EST、SAST、SAT 和 WST，这些被认为主要是历史用法。注意，在<quote>默认</quote>缩写集中，SAST 也已改为南非标准时间。
     </para>

     <para>
      此外，新增时区缩写 SRET（Asia/Srednekolymsk）和 XJT（Asia/Urumqi），并对西萨摩亚使用 WSST/WSDT。同时，智利、墨西哥、特克斯和凯科斯群岛（America/Grand_Turk）以及斐济的夏令时法规有变更。还为巴布亚新几内亚部分地区新增了时区 Pacific/Bougainville。另外还有大量针对历史（1970 年以前）时区数据的修正。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-9-2-9">
  <title>版本 9.2.9</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2014-07-24</para>
  </formalpara>

  <para>
   本次发布包含对 9.2.8 的多方面修复。有关 9.2 大版本中新特性的信息，请参见<xref linkend="release-9-2">。.
  </para>

  <sect2>
   <title>迁移到版本 9.2.9</title>

   <para>
    对于运行 9.2.X 的用户，不需要进行转储/恢复。</para>

   <para>
      不过，本版本纠正了某些 GiST 索引的索引损坏问题。请参见下面第一条变更日志，以确认你的安装是否受影响以及应采取的步骤。
     </para>

   <para>
    不过，如果你是从早于 9.2.6 的版本升级，请参见<xref linkend="release-9-2-6">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      正确初始化 <filename>contrib/btree_gist</filename> 在 <type>bit</type> 列上索引的填充字节（Heikki Linnakangas）
     </para>

     <para>
      这一错误可能导致本应比较相等的值不被视为相等，从而产生错误的查询结果。在 <type>bit</type> 或 <type>bit varying</type> 列上建有 GiST 索引的用户，在安装此更新后应对这些索引执行 <command>REINDEX</command>。
     </para>
    </listitem>

    <listitem>
     <para>
      在删除 GIN 列表页时防止残缺页（torn page）（Heikki Linnakangas）
     </para>

     <para>
      此修复可防止在页面更新写入磁盘期间发生系统崩溃时可能出现的索引损坏。
     </para>
    </listitem>

    <listitem>
     <para>
      在从 WAL 重放更新时不清除 GiST 索引页的右链接（Heikki Linnakangas）
     </para>

     <para>
      这一错误可能导致热备中执行的 GiST 索引扫描出现瞬时错误的结果。
     </para>
    </listitem>

    <listitem>
     <para>
      修复向 SP-GiST 文本索引插入时极端情况下的无限循环（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复<xref linkend="guc-hot-standby-feedback">被动态关闭时反馈状态的处理（Simon Riggs）
     </para>
    </listitem>

    <listitem>
     <para>
      修复嵌套调用 <function>ReceiveSharedInvalidMessages</function> 期间缓存失效可能不正确的问题（Andres Freund）
     </para>
    </listitem>

    <listitem>
     <para>
      修复规划器对嵌套 nestloop 计划中生成的嵌套 PlaceHolderVar 的错误处理（Tom Lane）
     </para>

     <para>
      这一疏忽可能导致 <quote>variable not found in subplan target lists</quote> 错误，或静默的错误查询结果。
     </para>
    </listitem>

    <listitem>
     <para>
      修复对从带继承子表的表读取的子查询执行 <literal>UNION ALL</literal> 时规划器报 <quote>could not find pathkey item to sort</quote> 失败的问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      当子查询的目标列表中存在集合返回函数时，不再假定其输出是唯一的（David Rowley）
     </para>

     <para>
      这一疏忽可能导致诸如 <literal>WHERE x IN (SELECT y, generate_series(1,10) FROM t GROUP BY y)</literal> 之类的构造被错误优化。
     </para>
    </listitem>

    <listitem>
     <para>
      改进规划器，使其在可能时丢弃 <literal>AND</literal>/<literal>OR</literal> 的常量 NULL 输入（Tom Lane）
     </para>

     <para>
      此变更修复了一些 9.2 及之后版本更激进的参数替换导致比旧版本生成的计划更差的情况。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>to_json()</function> 及相关函数对输入类型类别的识别（Tom Lane）
     </para>

     <para>
      已知这曾导致 <type>JSON</type> 结果中 <type>money</type> 字段加引号不当，其他数据类型也可能出现错误结果。
     </para>
    </listitem>

    <listitem>
     <para>
      修复未能对结构化类型的组合元素中的字段执行解 TOAST 的错误（Tom Lane）
     </para>

     <para>
      这纠正了 TOAST 指针可能未经解引用就被复制到其他表的情况。如果原始数据随后被删除，当这个已悬空的指针被使用时，会导致诸如 <quote>missing chunk number 0 for toast value ...</quote> 之类的错误。
     </para>
    </listitem>

    <listitem>
     <para>
      修复对 Append 计划节点输出的整行引用导致 <quote>record type has not been registered</quote> 失败的问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复在回卷游标时调用用户定义函数可能崩溃的问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复为 <literal>FROM</literal> 中的函数求值参数时查询级内存泄漏的问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复正则表达式处理中的会话级内存泄漏（Tom Lane、Arthur O'Dwyer、Greg Stark）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>hungarian.stop</filename> 中的数据编码错误（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      防止在<xref linkend="guc-default-with-oids">为真时创建带 OIDS 的外部表（Etsuro Fujita）
     </para>
    </listitem>

    <listitem>
     <para>
      修复对在当前事务中插入、随后被一个现已回滚的子事务删除的行的活跃性检查（Andres Freund）
     </para>

     <para>
      如果同一事务中稍后执行了 <command>CREATE INDEX</command> 或 <command>CLUSTER</command>，这可能导致问题（至少是虚假警告，最坏是无限循环）。
     </para>
    </listitem>

    <listitem>
     <para>
      在 <command>PREPARE TRANSACTION</command> 期间清除 <structname>pg_stat_activity</structname>.<structfield>xact_start</structfield>（Andres Freund）
     </para>

     <para>
      在 <command>PREPARE</command> 之后，发起会话已不在事务中，因此不应继续显示事务开始时间。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <command>REASSIGN OWNED</command> 对文本搜索对象失败的问题（&Aacute;lvaro Herrera）
     </para>
    </listitem>

    <listitem>
     <para>
      在 postmaster 启动期间阻塞信号（Tom Lane）
     </para>

     <para>
      这确保了 postmaster 在例如启动过程中收到 <systemitem>SIGINT</systemitem> 时能够正确清理自身。
     </para>
    </listitem>

    <listitem>
     <para>
      修复处理 <filename>pg_hba.conf</filename> 中指定主机名而非 IP 地址的条目时的客户端主机名查找（Tom Lane）
     </para>

     <para>
      确保反向 DNS 查找失败会被报告，而不是只是静默地不匹配这些条目。同时确保每个连接只做一次反向 DNS 查找尝试，而不是每个主机名条目一次——如果查找尝试失败，此前发生的就是后者。
     </para>
    </listitem>

    <listitem>
     <para>
      允许 root 用户使用 <literal>postgres -C variable</literal> 和 <literal>postgres --describe-config</literal>（MauMau）
     </para>

     <para>
      对以 root 身份启动服务器的禁止并不需要延伸到这些操作，放宽它可以避免 <application>pg_ctl</application> 在某些场景下的失败。
     </para>
    </listitem>

    <listitem>
     <para>
      保护 <literal>make check</literal> 期间启动的临时 postmaster 的 Unix 域套接字（Noah Misch）
     </para>

     <para>
      正如我们此前在 CVE-2014-0067 中指出的，任何能够访问套接字文件的本地用户都可以以服务器的引导超级用户身份连接，进而以运行测试的操作系统用户身份执行任意代码。此变更通过把服务器的套接字放到 <filename>/tmp</filename> 下一个临时的、模式为 0700 的子目录中来防御这一风险。但在不支持 Unix 套接字的平台（尤其是 Windows）上风险仍然存在，因为那时临时 postmaster 必须接受本地 TCP 连接。
     </para>

     <para>
      此变更的一个有用副作用是简化了覆盖 <literal>DEFAULT_PGSOCKET_DIR</literal> 的构建中的 <literal>make check</literal> 测试。常用的非默认值（如 <filename>/var/run/postgresql</filename>）往往不允许构建用户写入，此前需要一些变通办法，现在不再需要了。
     </para>
    </listitem>

    <listitem>
     <para>
      修复表空间创建 WAL 重放在 Windows 上无法工作的问题（MauMau）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 Windows 上套接字创建失败的检测（Bruce Momjian）
     </para>
    </listitem>

    <listitem>
     <para>
      在 Windows 上，允许新会话从配置文件获取 PGC_BACKEND 参数（例如<xref linkend="guc-log-connections">）的值（Amit Kapila）
     </para>

     <para>
      此前，如果启动后在文件中更改了这类参数，更改不会生效。
     </para>
    </listitem>

    <listitem>
     <para>
      在 Windows 上正确地为可执行文件路径名加引号（Nikhil Deshpande）
     </para>

     <para>
      如果安装路径同时包含空格和 <literal>@</literal> 符号，这一疏忽可能导致 <application>initdb</application> 和 <application>pg_upgrade</application> 在 Windows 上失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 macOS 上 <application>libpython</application> 的链接（Tom Lane）
     </para>

     <para>
      我们先前使用的方法在 Xcode 5.0 及之后版本提供的 Python 库上可能失败。
     </para>
    </listitem>

    <listitem>
     <para>
      当服务器持续以快于客户端吸收速度的速率发送数据时，避免 <application>libpq</application> 中的缓冲区膨胀（Shin-ichi Morita、Tom Lane）
     </para>

     <para>
      <application>libpq</application> 可能被迫不断增大其输入缓冲区，直到内存耗尽（这会被误导性地报告为 <quote>lost synchronization with server</quote>）。在正常情况下，数据持续以快于 <function>recv()</function> 循环吸收速度的速率传输是相当牵强的，但在客户端因调度器约束被人为减速时曾观察到这种情况。
     </para>
    </listitem>

    <listitem>
     <para>
      确保 <application>libpq</application> 中的 LDAP 查找尝试按预期超时（Laurenz Albe）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>ecpg</application> 在 <type>char *</type> 数组作为返回多行的 FETCH 语句目标时的行为，以及其他一些数组处理修复（Ashutosh Bapat）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_restore</application> 对旧式大对象注释的处理（Tom Lane）
     </para>

     <para>
      如果归档文件由 9.0 之前版本的 <application>pg_dump</application> 生成且包含较多大对象注释，直接恢复到数据库通常会失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复新服务器创建了 TOAST 表而旧版本没有创建时 <application>pg_upgrade</application> 的问题（Bruce Momjian）
     </para>

     <para>
      这种罕见情况会表现为 <quote>relation OID mismatch</quote> 错误。
     </para>
    </listitem>

    <listitem>
     <para>
      防止 <filename>contrib/auto_explain</filename> 改变用户 <command>EXPLAIN</command> 命令的输出（Tom Lane）
     </para>

     <para>
      如果 <filename>auto_explain</filename> 处于活动状态，它可能导致 <literal>EXPLAIN (ANALYZE, TIMING OFF)</literal> 命令仍然打印计时信息。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/dblink</filename> 中的查询级内存泄漏（MauMau、Joe Conway）
     </para>
    </listitem>

    <listitem>
     <para>
      在 <filename>contrib/pgcrypto</filename> 函数中，确保返回前清除栈变量中的敏感信息（Marko Kreen）
     </para>
    </listitem>

    <listitem>
     <para>
      防止 <filename>contrib/pgstattuple</filename> 的 <function>pgstat_heap()</function> 使用已释放的内存（Noah Misch）
     </para>
    </listitem>

    <listitem>
     <para>
      在 <filename>contrib/uuid-ossp</filename> 中，跨调用缓存 OSSP UUID 库的状态（Tom Lane）
     </para>

     <para>
      这提高了 UUID 生成的效率，并在有 <filename>/dev/urandom</filename> 的平台上减少了从中抽取的熵量。
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2014e
      for DST law changes in Crimea, Egypt, and Morocco。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-9-2-8">
  <title>版本 9.2.8</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2014-03-20</para>
  </formalpara>

  <para>
   本次发布包含对 9.2.7 的多方面修复。有关 9.2 大版本中新特性的信息，请参见<xref linkend="release-9-2">。.
  </para>

  <sect2>
   <title>迁移到版本 9.2.8</title>

   <para>
    对于运行 9.2.X 的用户，不需要进行转储/恢复。</para>

   <para>
    不过，如果你是从早于 9.2.6 的版本升级，请参见<xref linkend="release-9-2-6">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      无条件恢复 GIN 元页以避免残缺页风险（Heikki Linnakangas）
     </para>

     <para>
      虽然这一疏忽理论上可能导致索引损坏，但实际上不太可能造成任何问题，因为 GIN 元页的活动部分小于标准的 512 字节磁盘扇区。
     </para>
    </listitem>

    <listitem>
     <para>
      避免在接收 <command>NOTIFY</command> 消息期间检查事务提交状态时的竞态条件（Marko Tiikkaja）
     </para>

     <para>
      这防止了这样一种场景：足够快的客户端可能在通知者所做的数据库更新对接收者可见之前就响应通知。
     </para>
    </listitem>

    <listitem>
     <para>
      允许正则表达式操作符被查询取消请求提前终止（Tom Lane）
     </para>

     <para>
      这防止了病态正则表达式长时间不可中断地锁死服务器进程的场景。
     </para>
    </listitem>

    <listitem>
     <para>
      移除试图允许 <literal>OVERLAPS</literal> 接受单元素行参数的错误代码（Joshua Yanovski）
     </para>

     <para>
      这段代码从未正确工作过，而且该情况既未被 SQL 标准规定也未被记录，因此移除它似乎比修复它更好。
     </para>
    </listitem>

    <listitem>
     <para>
      避免在反解析规则或视图时获取超出 <literal>AccessShareLock</literal> 的锁（Dean Rasheed）
     </para>

     <para>
      这一疏忽导致 <application>pg_dump</application> 对规则中作为 <literal>INSERT</literal>/<literal>UPDATE</literal>/<literal>DELETE</literal> 命令目标的表意外获取了 <literal>RowExclusiveLock</literal> 锁。虽然通常无害，但它可能干扰试图在这些表上获取例如 <literal>ShareLock</literal> 的并发事务。
     </para>
    </listitem>

    <listitem>
     <para>
      改进规划期间索引端点探测的性能（Tom Lane）
     </para>

     <para>
      此变更修复了当索引末尾存在大量尚未提交的行时的显著性能问题，这对于基于顺序赋值（如时间戳或序列生成的标识符）的索引是常见情况。
     </para>
    </listitem>

    <listitem>
     <para>
      修复客户端为 <application>pg_receivexlog</application> 时 <application>walsender</application> 无法干净关闭的问题（Fujii Masao）
     </para>
    </listitem>

    <listitem>
     <para>
      在执行将被归档恢复跟随的崩溃恢复时，正确检查 WAL 级别和热备参数（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      修复崩溃后是否可以立即允许热备连接的测试（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      在报告非 <literal>ERROR</literal> 消息期间防止中断（Tom Lane）
     </para>

     <para>
      这可以防止因递归进入 <function>syslog()</function> 而导致的罕见服务器进程冻结，或许还能防止其他相关问题。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PL/Perl 返回组合结果（包括多 OUT 参数情况）时的内存泄漏（Alex Hunsaker）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>\copy</literal> 从带外数据读取期间 <application>psql</application> 脚本行号跟踪的问题（Kumar Rajeev Rastogi、Amit Khandekar）
     </para>

     <para>
      即使数据并非来自脚本文件，<literal>\copy ... from</literal> 也会为每个数据行递增脚本文件行号。这一失误导致同一脚本文件中稍后发生的任何错误报告了错误的行号。
     </para>
    </listitem>

    <listitem>
     <para>
      防止在较新的 Windows 版本上间歇性出现 <quote>could not reserve shared memory region</quote> 失败（MauMau）
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2014a
      for DST law changes in Fiji and Turkey, plus historical changes in
      Israel and Ukraine。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-9-2-7">
  <title>版本 9.2.7</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2014-02-20</para>
  </formalpara>

  <para>
   本次发布包含对 9.2.6 的多方面修复。有关 9.2 大版本中新特性的信息，请参见<xref linkend="release-9-2">。.
  </para>

  <sect2>
   <title>迁移到版本 9.2.7</title>

   <para>
    对于运行 9.2.X 的用户，不需要进行转储/恢复。</para>

   <para>
    不过，如果你是从早于 9.2.6 的版本升级，请参见<xref linkend="release-9-2-6">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      加固 <literal>GRANT ... WITH ADMIN OPTION</literal> 的限制（Noah Misch）
     </para>

     <para>
      不带 <literal>ADMIN OPTION</literal> 授予角色本应阻止被授予者向该角色添加或移除成员，但这一限制很容易通过先执行 <literal>SET ROLE</literal> 绕过。其安全影响主要在于角色成员可以撤销其他人的访问权限，这违背了授权者的意愿。未经批准添加角色成员的影响相对较小，因为不配合的角色成员本来就可以通过创建视图或 <literal>SECURITY DEFINER</literal> 函数把自己的大部分权限提供给他人。（CVE-2014-0060）
     </para>
    </listitem>

    <listitem>
     <para>
      防止通过手工调用 PL 验证器函数进行权限提升（Andres Freund）
     </para>

     <para>
      PL 验证器函数的主要角色是在 <command>CREATE FUNCTION</command> 期间被隐式调用，但它们也是普通 SQL 函数，用户可以显式调用。对实际以其他语言编写的函数调用验证器此前未被检查，可能被用于权限提升。修复方法是在每个验证器函数中加入对权限检查函数的调用。非核心过程语言如果自带验证器函数，也需要做同样的修改。（CVE-2014-0061）
     </para>
    </listitem>

    <listitem>
     <para>
      避免在表和索引 DDL 期间进行多次名称查找（Robert Haas、Andres Freund）
     </para>

     <para>
      如果由于并发活动导致名称查找得出不同结论，我们可能会对不同的表执行 DDL 的不同部分。至少在 <command>CREATE INDEX</command> 的情况下，这可被用来让权限检查针对与索引创建不同的表执行，从而造成权限提升攻击。（CVE-2014-0062）
     </para>
    </listitem>

    <listitem>
     <para>
      防止长日期时间字符串造成缓冲区越界（Noah Misch）
     </para>

     <para>
      <literal>MAXDATELEN</literal> 常量对于 <type>interval</type> 类型可能的最长值来说太小，导致 <function>interval_out()</function> 中可能出现缓冲区越界。虽然日期时间输入函数在避免缓冲区越界方面更谨慎，但该限制仍短到导致它们拒绝某些有效输入，例如包含很长时区名的输入。<application>ecpg</application> 库存在这些漏洞以及它自身的一些漏洞。（CVE-2014-0063）
     </para>
    </listitem>

    <listitem>
     <para>
      防止大小计算中的整数溢出导致缓冲区越界（Noah Misch、Heikki Linnakangas）
     </para>

     <para>
      若干函数（大多是类型输入函数）在计算分配大小时未检查溢出。如果发生溢出，就会分配过小的缓冲区并被写越界。（CVE-2014-0064）
     </para>
    </listitem>

    <listitem>
     <para>
      防止固定大小缓冲区越界（Peter Eisentraut、Jozef Mlich）
     </para>

     <para>
      使用 <function>strlcpy()</function> 及相关函数，以明确保证固定大小缓冲区不会被越界写入。与前面几项不同，这些情况是否真的构成现实问题尚不清楚，因为在大多数情况下，输入字符串的大小似乎已有先前约束。尽管如此，消除所有此类 Coverity 警告似乎是明智的。（CVE-2014-0065）
     </para>
    </listitem>

    <listitem>
     <para>
      避免在 <function>crypt()</function> 返回 NULL 时崩溃（Honza Horak、Bruce Momjian）
     </para>

     <para>
      <function>crypt()</function> 可能返回 NULL 的场景相对较少，但如果发生，<filename>contrib/chkpass</filename> 就会崩溃。一个可能出问题的实际情况是 <application>libc</application> 被配置为拒绝执行未获批准的哈希算法（例如 <quote>FIPS 模式</quote>）。（CVE-2014-0066）
     </para>
    </listitem>

    <listitem>
     <para>
      在回归测试说明中记录 <literal>make check</literal> 的风险（Noah Misch、Tom Lane）
     </para>

     <para>
      由于 <literal>make check</literal> 启动的临时服务器使用 <quote>trust</quote> 认证，同一台机器上的其他用户可以以数据库超级用户身份连接它，进而可能利用启动测试的操作系统用户的权限。未来版本可能会在测试流程中加入防止此风险的变更，但需要先进行一些公开讨论。因此目前只是警告：当同一台机器上存在不受信任的用户时，不要使用 <literal>make check</literal>。（CVE-2014-0067）
     </para>
    </listitem>

    <listitem>
     <para>
      修复当关系的某些段不是完整大小时 WAL 记录可能被错误重放的问题（Greg Stark、Tom Lane）
     </para>

     <para>
      WAL 更新可能被应用到错误的页面，甚至可能偏后很多页。除了损坏数据外，已观察到这一错误导致备库相比主库显著<quote>膨胀</quote>，因为更新被应用到了远超文件末尾应在位置的地方。这种失败模式在崩溃恢复期间似乎不构成重大风险，只在初始同步从快速变化的主库基础备份创建的备库时出现。
     </para>
    </listitem>

    <listitem>
     <para>
      修复判断恢复何时达到一致性的缺陷（Tomonari Katsumata、Heikki Linnakangas）
     </para>

     <para>
      某些情况下，WAL 重放会错误地在重放开始时就断定数据库已一致，从而可能在数据库真正一致之前允许热备查询。也可能出现其他症状，例如 <quote>PANIC: WAL contains references to invalid pages</quote>。
     </para>
    </listitem>

    <listitem>
     <para>
      修复在热备模式下重放 <literal>VACUUM</literal> 操作时对 btree 索引页的不当锁定（Andres Freund、Heikki Linnakangas、Tom Lane）
     </para>

     <para>
      这一错误可能导致 <quote>PANIC: WAL contains references to invalid pages</quote> 失败。
     </para>
    </listitem>

    <listitem>
     <para>
      确保对 GIN 索引非叶页面的插入在合适时写入整页 WAL 记录（Heikki Linnakangas）
     </para>

     <para>
      先前的代码在系统崩溃期间发生部分页写入时存在索引损坏风险。
     </para>
    </listitem>

    <listitem>
     <para>
      当 <literal>pause_at_recovery_target</literal> 和 <literal>recovery_target_inclusive</literal> 都被设置时，确保目标记录在暂停之前而不是之后被应用（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      修复服务器进程退出期间的竞态条件（Robert Haas）
     </para>

     <para>
      确保信号处理程序不会在进程的 <varname>MyProc</varname> 指针不再有效后尝试使用它。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 walsender 关闭逻辑和 walreceiver SIGHUP 信号处理程序中的竞态条件（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复错误报告逻辑中对 <varname>errno</varname> 的不安全引用（Christian Kruse）
     </para>

     <para>
      这通常会导致奇怪的行为，例如 <literal>HINT</literal> 字段缺失或不恰当。
     </para>
    </listitem>

    <listitem>
     <para>
      修复服务器启动期间过早使用 <function>ereport()</function> 可能导致的崩溃（Tom Lane）
     </para>

     <para>
      我们在实际中见到的主要情况是：如果服务器在一个它没有读取权限的目录中被启动，就会崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      在 OpenSSL 套接字写函数中正确清除重试标志（Alexander Kukushkin）
     </para>

     <para>
      这一遗漏可能导致 SSL 加密连接意外断开后服务器锁死。
     </para>
    </listitem>

    <listitem>
     <para>
      修复对包含转义的 Unicode 标识符（<literal>U&amp;"..."</literal> 语法）的长度检查（Tom Lane）
     </para>

     <para>
      如果标识符的转义形式过长，就会为这类标识符打印虚假的截断警告，但标识符在反转义之后实际上并不需要截断。
     </para>
    </listitem>

    <listitem>
     <para>
      允许作为类型名的关键字用于角色列表（Stephen Frost）
     </para>

     <para>
      先前的补丁允许这类关键字在角色标识符等位置不加引号使用；但漏掉了允许角色标识符列表的情况，例如 <literal>DROP ROLE</literal>。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>EXISTS(SELECT * FROM zero_column_table)</literal> 的解析器崩溃（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复嵌套子选择（例如 <literal>WHERE (... x IN (SELECT ...) ...) IN (SELECT ...)</literal>）因无效计划而可能崩溃的问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复对带 <literal>UNION ALL</literal> 子查询的继承目标表的 <literal>UPDATE/DELETE</literal>（Tom Lane）
     </para>

     <para>
      如果不修复，<literal>UNION ALL</literal> 子查询就不能正确插入第一个之后的继承子表的更新计划中，通常导致那些子表完全没有被更新。
     </para>
    </listitem>

    <listitem>
     <para>
      确保即使表列中的所有值都<quote>过宽</quote>，<command>ANALYZE</command> 也会为该列创建统计信息（Tom Lane）
     </para>

     <para>
      <command>ANALYZE</command> 有意在直方图和最常用值计算中省略非常宽的值，但在所有采样条目都过宽的情况下没有做出合理的处理。
     </para>
    </listitem>

    <listitem>
     <para>
      在 <literal>ALTER TABLE ... SET TABLESPACE</literal> 中，允许不经权限检查就使用数据库的默认表空间（Stephen Frost）
     </para>

     <para>
      <literal>CREATE TABLE</literal> 一直允许这种用法，但 <literal>ALTER TABLE</literal> 没有照做。
     </para>
    </listitem>

    <listitem>
     <para>
      修复当 <literal>CASE</literal> 的某些分支返回集合而另一些不返回时的 <quote>cannot accept a set</quote> 错误（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      在生成 JSON 输出时正确区分数字与非数字（Andrew Dunstan）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 pgstat 函数中对全零客户端地址的检查（Kevin Grittner）
     </para>
    </listitem>

    <listitem>
     <para>
      修复文本搜索解析器可能错误分类多字节字符的问题（Tom Lane）
     </para>

     <para>
      在 C 区域设置下使用多字节编码时，非 ASCII 字符可能被错误分类。在 Cygwin 上，非 C 区域设置也可能失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>plainto_tsquery()</function> 中可能的不当行为（Heikki Linnakangas）
     </para>

     <para>
      在复制重叠内存区域时使用 <function>memmove()</function> 而不是 <function>memcpy()</function>。尚无实际造成问题的现场报告，但这无疑是有风险的。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>pg_start_backup()</function> 和 <function>pg_stop_backup()</function> 中权限检查的位置（Andres Freund、Magnus Hagander）
     </para>

     <para>
      先前的代码可能在不该访问系统目录时尝试访问。
     </para>
    </listitem>

    <listitem>
     <para>
      在区域检查方面接受 <literal>SHIFT_JIS</literal> 作为编码名（Tatsuo Ishii）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 SQL 语言函数中命名参数的 <literal>*</literal> 限定（Tom Lane）
     </para>

     <para>
      对于名为 <literal>foo</literal> 的组合类型参数，<literal>$1.*</literal> 工作正常，但 <literal>foo.*</literal> 就不行了。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>PQhost()</function> 在 Windows 上的不当行为（Fujii Masao）
     </para>

     <para>
      如果未指定主机，它应返回 <literal>localhost</literal>。
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <application>libpq</application> 和 <application>psql</application> 对 <literal>COPY TO STDOUT/FROM STDIN</literal> 期间失败的错误处理（Tom Lane）
     </para>

     <para>
      这尤其修复了 9.2 及更高版本中，如果服务器连接在 <literal>COPY FROM STDIN</literal> 期间丢失可能发生的无限循环。该场景的变体在旧版本或其他客户端应用中也可能出现。
     </para>
    </listitem>

    <listitem>
     <para>
      修复某些 <application>psql</application> <literal>\d</literal> 命令中不正确的翻译处理（Peter Eisentraut、Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      确保 <application>pg_basebackup</application> 的前台进程退出时其后台进程被终止（Magnus Hagander）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_basebackup</application> 详细模式下文件名可能打印错误的问题（Magnus Hagander）
     </para>
    </listitem>

    <listitem>
     <para>
      避免在基础备份中把 PGDATA 内的表空间包含两次（Dimitri Fontaine、Magnus Hagander）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>ecpg</application> 中未对齐的描述符（MauMau）
     </para>
    </listitem>

    <listitem>
     <para>
      在 <application>ecpg</application> 中正确处理连接参数中缺少主机名的情况（Michael Meskes）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/dblink</filename> 连接启动中的性能退化（Joe Conway）
     </para>

     <para>
      在客户端和服务器编码一致时避免一次不必要的往返。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/isn</filename> 中 ISMN 值校验位计算错误的问题（Fabien Coelho）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/pg_stat_statement</filename> 对 <literal>CURRENT_DATE</literal> 及相关构造的处理（Kyotaro Horiguchi）
     </para>
    </listitem>

    <listitem>
     <para>
      确保仅安装客户端代码的流程按文档说明工作（Peter Eisentraut）
     </para>
    </listitem>

    <listitem>
     <para>
      在 Mingw 和 Cygwin 构建中，把 <application>libpq</application> DLL 安装到 <filename>bin</filename> 目录（Andrew Dunstan）
     </para>

     <para>
      这与 MSVC 构建长期以来的做法一致。它应该能解决 <application>psql</application> 等程序因找不到 DLL 而无法启动的问题。
     </para>
    </listitem>

    <listitem>
     <para>
      在 Cygwin 构建中避免使用已弃用的 <literal>dllwrap</literal> 工具（Marco Atzeri）
     </para>
    </listitem>

    <listitem>
     <para>
      不再生成纯文本的 <filename>HISTORY</filename> 和 <filename>src/test/regress/README</filename> 文件（Tom Lane）
     </para>

     <para>
      这些文本文件与主文档的 HTML 和 PDF 格式重复。维护它们的麻烦远远超过纯文本格式可能的读者。发行版压缩包仍会包含这些名字的文件，但它们只是提示读者查阅主文档的存根。纯文本 <filename>INSTALL</filename> 文件仍将维护，因为它可以说还有使用场景。
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2013i
      for DST law changes in Jordan and historical changes in Cuba。
     </para>

     <para>
      此外，时区 <literal>Asia/Riyadh87</literal>、<literal>Asia/Riyadh88</literal> 和 <literal>Asia/Riyadh89</literal> 已被移除，因为 IANA 不再维护它们，而且它们从未代表过实际的民用计时实践。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-9-2-6">
  <title>版本 9.2.6</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2013-12-05</para>
  </formalpara>

  <para>
   本次发布包含对 9.2.5 的多方面修复。有关 9.2 大版本中新特性的信息，请参见<xref linkend="release-9-2">。.
  </para>

  <sect2>
   <title>迁移到版本 9.2.6</title>

   <para>
    对于运行 9.2.X 的用户，不需要进行转储/恢复。</para>

   <para>
      不过，本版本纠正了若干潜在的数据损坏问题。请参见下面前三条变更日志，以确认你的安装是否受影响以及可以采取什么措施。
     </para>

   <para>
    不过，如果你是从早于 9.2.4 的版本升级，请参见<xref linkend="release-9-2-4">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      修复 <command>VACUUM</command> 判断是否可以更新 <structfield>relfrozenxid</structfield> 的测试（Andres Freund）
     </para>

     <para>
      某些情况下，<command>VACUUM</command>（手工或自动）可能错误地推进表的 <structfield>relfrozenxid</structfield> 值，使元组逃过冻结，导致这些行在经过 2^31 个事务后变得不可见。由于需要多次错误推进才会发生实际丢失，数据丢失的概率相当低，但并非为零。在 9.2.0 及更高版本中，丢失概率更高，而且这一缺陷还可能导致 <quote>could not access status of transaction</quote> 错误。从 9.0.4、8.4.8 或更早版本升级的用户不受影响，但之后的所有版本都包含该缺陷。
     </para>

     <para>
      升级后，在把 <link linkend="guc-vacuum-freeze-table-age"><varname>vacuum_freeze_table_age</varname></link> 设为零的情况下对所有数据库中的所有表执行清理，可以缓解此问题。这会修复任何潜伏的损坏，但无法修复所有既有的数据错误。不过，如果安装在其生命周期内执行的事务少于 2^31 个更新（可用 <literal>SELECT txid_current() < 2^31</> 检查），则在执行此清理之后即可认为该安装是安全的。
     </para>
    </listitem>

    <listitem>
     <para>
      修复热备启动期间 <filename>pg_clog</filename> 和 <filename>pg_subtrans</filename> 的初始化（Andres Freund、Heikki Linnakangas）
     </para>

     <para>
      这一缺陷会把已提交事务标记为未提交，从而在备库开始接受热备查询的那一刻造成数据丢失。除非备库启动时主库自上次检查点以来执行了大量更新事务，否则这种损坏的可能性很小。症状包括行缺失、本应删除的行仍可见，以及已更新行的过时版本与新版本同时可见。
     </para>

     <para>
      该缺陷是在 9.3.0、9.2.5、9.1.10 和 9.0.14 版本中引入的。只运行过更早版本的备库没有风险。建议曾运行过任何含缺陷版本的备库在升级后从主库重新克隆（例如用新的基础备份）。
     </para>
    </listitem>

    <listitem>
     <para>
      修复快速路径锁定中的悬空指针问题（Tom Lane）
     </para>

     <para>
      这可能导致共享内存中锁数据结构的损坏，造成 <quote>lock already held</quote> 和其他奇怪的错误。
     </para>
    </listitem>

    <listitem>
     <para>
      在 WAL 重放期间截断 <filename>pg_multixact</filename> 内容（Andres Freund）
     </para>

     <para>
      这避免了备库中磁盘空间消耗不断增加。
     </para>
    </listitem>

    <listitem>
     <para>
      确保防回卷 <command>VACUUM</command> 在仅验证了没有元组需要冻结时也把该页计为已扫描（Sergey Burladyan、Jeff Janes）
     </para>

     <para>
      这一缺陷可能导致 <structfield>relfrozenxid</structfield> 未能推进，使该表仍被认为需要再次防回卷清理。最坏情况下，数据库甚至可能为防止回卷而关闭。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 GIN 索引 posting 树页面删除中的竞态条件（Heikki Linnakangas）
     </para>

     <para>
      这可能导致瞬时的错误结果或查询失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 SP-GiST 索引创建期间的 <quote>unexpected spgdoinsert() failure</quote> 错误（Teodor Sigaev）
     </para>
    </listitem>

    <listitem>
     <para>
      避免展平 <literal>SELECT</literal> 列表中包含包裹在子 <literal>SELECT</literal> 中的易变函数的子查询（Tom Lane）
     </para>

     <para>
      这避免了因易变函数被额外求值而导致的意外结果。
     </para>
    </listitem>

    <listitem>
     <para>
      修复规划器对外连接中嵌套的非简单变量子查询输出的处理（Tom Lane）
     </para>

     <para>
      这一错误可能为涉及 <literal>JOIN</literal> 语法中多级子查询的查询生成错误的计划。
     </para>
    </listitem>

    <listitem>
     <para>
      修复同一非严格表达式出现在多个 <literal>WHERE</literal> 和外连接 <literal>JOIN</literal> 等值子句中时的错误规划（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复对子查询的整行引用导致规划器崩溃的问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复继承树的优化 MIN()/MAX() 计划生成错误（Tom Lane）
     </para>

     <para>
      当 MIN()/MAX() 的参数是表达式而不是简单变量时，规划器可能失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复临时文件被过早删除的问题（Andres Freund）
     </para>
    </listitem>

    <listitem>
     <para>
      防止打印范围值时的事务内内存泄漏（Tom Lane）
     </para>

     <para>
      此修复实际上消除了任何数据类型输出函数中的瞬时内存泄漏，但已知只有范围类型曾出现显著问题。
     </para>
    </listitem>

    <listitem>
     <para>
      防止 NOT NULL 和 CHECK 约束违规消息中错误地显示已删除的列（Michael Paquier、Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      允许窗口函数使用默认参数和命名参数记法（Tom Lane）
     </para>

     <para>
      此前这些情况很可能崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      修复规则打印中可能读取越过内存末尾的问题（Peter Eisentraut）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <type>int2vector</type> 和 <type>oidvector</type> 值的数组切片（Tom Lane）
     </para>

     <para>
      这类表达式现在会被隐式提升为常规的 <type>int2</type> 或 <type>oid</type> 数组。
     </para>
    </listitem>

    <listitem>
     <para>
      修复使用 SQL 标准的简单 GMT 偏移时区时的错误行为（Tom Lane）
     </para>

     <para>
      某些情况下，系统会使用简单 GMT 偏移值，而本应使用选择简单偏移之前一直生效的常规时区设置。此变更还使 <function>timeofday</function> 函数遵循简单 GMT 偏移时区。
     </para>
    </listitem>

    <listitem>
     <para>
      防止记录 Windows 错误码翻译时可能出现的不当行为（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      正确地为 <application>pg_ctl</application> 生成的命令行加引号（Naoya Anzai、Tom Lane）
     </para>

     <para>
      此修复仅适用于 Windows。
     </para>
    </listitem>

    <listitem>
     <para>
      修复当源数据库通过 <command>ALTER DATABASE SET</command> 设置 <link linkend="guc-default-transaction-read-only"><varname>default_transaction_read_only</varname></link> 时 <application>pg_dumpall</application> 无法工作的问题（Kevin Grittner）
     </para>

     <para>
      此前，生成的脚本在恢复期间会失败。
     </para>
    </listitem>

    <listitem>
     <para>
      让 <application>ecpg</application> 以大小写敏感方式搜索带引号的游标名（Zolt&aacute;n B&ouml;sz&ouml;rm&eacute;nyi）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>ecpg</application> 对声明为 <type>varchar</type> 的变量列表的处理（Zolt&aacute;n B&ouml;sz&ouml;rm&eacute;nyi）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <filename>contrib/lo</filename> 防御不正确的触发器定义（Marc Cousin）
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2013h
      for DST law changes in Argentina, Brazil, Jordan, Libya,
      Liechtenstein, Morocco, and Palestine。Also, new timezone
      abbreviations WIB, WIT, WITA for Indonesia.
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-9-2-5">
  <title>版本 9.2.5</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2013-10-10</para>
  </formalpara>

  <para>
   本次发布包含对 9.2.4 的多方面修复。有关 9.2 大版本中新特性的信息，请参见<xref linkend="release-9-2">。.
  </para>

  <sect2>
   <title>迁移到版本 9.2.5</title>

   <para>
    对于运行 9.2.X 的用户，不需要进行转储/恢复。</para>

   <para>
    不过，如果你是从早于 9.2.4 的版本升级，请参见<xref linkend="release-9-2-4">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      防止尝试对标识符做大小写折叠时损坏多字节字符（Andrew Dunstan）
     </para>

     <para>
      <productname>PostgreSQL</productname> 只在使用单字节服务器编码时才对非 ASCII 字符做大小写折叠。
     </para>
    </listitem>

    <listitem>
     <para>
      修复在范围列上创建 B-tree 索引时的内存泄漏（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>wal_level = hot_standby</literal> 时后台写入进程中的检查点内存泄漏（Naoya Anzai）
     </para>
    </listitem>

    <listitem>
     <para>
      修复由 <function>lo_open()</function> 失败导致的内存泄漏（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <varname>work_mem</varname> 使用超过 24GB 内存时的内存过度提交缺陷（Stephen Frost）
     </para>
    </listitem>

    <listitem>
     <para>
      可序列化快照修复（Kevin Grittner、Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      修复使用 SSL 时 libpq 中的死锁缺陷（Stephen Frost）
     </para>
    </listitem>

    <listitem>
     <para>
      修复多线程 libpq 应用中可能的 SSL 状态损坏（Nick Phillips、Stephen Frost）
     </para>
    </listitem>

    <listitem>
     <para>
      改进规划器在通用计划与定制计划之间选择时的代价估计（Tom Lane）
     </para>

     <para>
      当规划代价较高时，此变更将偏向通用计划。
     </para>
    </listitem>

    <listitem>
     <para>
      正确计算包含大量 NULL 值的布尔列的行数估计（Andrew Gierth）
     </para>

     <para>
      此前，<literal>col IS NOT TRUE</literal> 和 <literal>col IS NOT FALSE</literal> 这类测试在估计计划代价时没有正确考虑 NULL 值。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>UNION ALL</literal> 和继承查询中限定条件求值代价的核算（Tom Lane）
     </para>

     <para>
      这修复了当某些 <literal>WHERE</literal> 子句计算开销很大时可能选中次优查询计划的情况。
     </para>
    </listitem>

    <listitem>
     <para>
      防止把 <literal>WHERE</literal> 子句下推到不安全的 <literal>UNION/INTERSECT</literal> 子查询中（Tom Lane）
     </para>

     <para>
      <literal>SELECT</literal> 列表中包含集合返回函数或易变函数的 <literal>UNION</literal> 或 <literal>INTERSECT</literal> 子查询可能被不当优化，导致运行时错误或错误的查询结果。
     </para>
    </listitem>

    <listitem>
     <para>
      修复罕见的 <quote>failed to locate grouping columns</quote> 规划器失败情况（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复带已删除列的外部表的 <application>pg_dump</application> 转储问题（Andrew Dunstan）
     </para>

     <para>
      此前这类情况可能导致 <application>pg_upgrade</application> 错误。
     </para>
    </listitem>

    <listitem>
     <para>
      重新排序 <application>pg_dump</application> 对扩展相关规则和事件触发器的处理（Joe Conway）
     </para>
    </listitem>

    <listitem>
     <para>
      如果通过 <command>pg_dump -t</command> 或 <literal>-n</literal> 指定，则强制转储扩展表（Joe Conway）
     </para>
    </listitem>

    <listitem>
     <para>
      改进视图转储代码对被引用表中已删除列的处理（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复目录归档下 <command>pg_restore -l</command> 显示正确格式名的问题（Fujii Masao）
     </para>
    </listitem>

    <listitem>
     <para>
      正确记录使用 <literal>UNIQUE</literal> 和 <literal>PRIMARY KEY</literal> 语法创建的索引注释（Andres Freund）
     </para>

     <para>
      这修复了一个并行 <application>pg_restore</application> 失败问题。
     </para>
    </listitem>

    <listitem>
     <para>
      让 <command>pg_basebackup -x</command> 在 xlog 目录为空时抛出错误而不是崩溃（Magnus Hagander、Haruka Takatsuka）
     </para>
    </listitem>

    <listitem>
     <para>
      在干净切换之前正确保证 WAL 文件的传输（Fujii Masao）
     </para>

     <para>
      此前，流复制连接可能在所有 WAL 文件都已在备库上重放之前关闭。
     </para>
    </listitem>

    <listitem>
     <para>
      修复恢复期间 WAL 段的时间线处理（Mitsumasa Kondo、Heikki Linnakangas）
     </para>

     <para>
      备库恢复期间的 WAL 文件回收可能导致恢复过早完成，造成数据丢失。
     </para>
    </listitem>

    <listitem>
     <para>
      防止因引用未初始化的空页而导致 WAL 重放错误（Andres Freund）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <command>REINDEX TABLE</command> 和 <command>REINDEX DATABASE</command>，使其正确重新验证约束并把已失效的索引标记为有效（Noah Misch）
     </para>

     <para>
      <command>REINDEX INDEX</command> 一直工作正常。
     </para>
    </listitem>

    <listitem>
     <para>
      避免向 SP-GiST 索引插入时出现死锁（Teodor Sigaev）
     </para>
    </listitem>

    <listitem>
     <para>
      修复并发执行 <command>CREATE INDEX CONCURRENTLY</command> 操作时可能出现的死锁（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 GiST 索引查找崩溃（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>regexp_matches()</function> 对零长度匹配的处理（Jeevan Chalke）
     </para>

     <para>
      此前，像 '^' 这样的零长度匹配可能返回过多的匹配。
     </para>
    </listitem>

    <listitem>
     <para>
      修复过于复杂的正则表达式导致崩溃的问题（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      修复反向引用与非贪婪量词组合时正则表达式匹配失败的问题（Jeevan Chalke）
     </para>
    </listitem>

    <listitem>
     <para>
      除非启用了函数体检查，否则防止 <command>CREATE FUNCTION</command> 检查 <command>SET</command> 变量（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      允许 <command>ALTER DEFAULT PRIVILEGES</command> 在不需要 CREATE 权限的情况下对模式操作（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      放宽对查询中所用关键字的限制（Tom Lane）
     </para>

     <para>
      具体而言，放宽了角色名、语言名、<command>EXPLAIN</command> 和 <command>COPY</command> 选项以及 <command>SET</command> 值的关键字限制。这使得 <literal>COPY ... (FORMAT BINARY)</literal> 能按预期工作；此前 <literal>BINARY</literal> 需要加引号。
     </para>
    </listitem>

    <listitem>
     <para>
      在 <command>COPY</command> 失败时打印正确的行号（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>pgp_pub_decrypt()</function>，使其可用于带口令的密钥（Marko Kreen）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <application>pg_upgrade</application> 使用 <literal>pg_dump --quote-all-identifiers</literal>，以避免版本之间关键字变更带来的问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      移除无索引表清理期间罕见的不准确警告（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      确保当 <command>VACUUM ANALYZE</command> 的文件截断尝试因锁冲突被取消时，仍然执行 ANALYZE 阶段（Kevin Grittner）
     </para>
    </listitem>

    <listitem>
     <para>
      避免在预备查询中执行事务控制命令（例如 <command>ROLLBACK</command>）时可能出现的失败（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      确保浮点数据输入在所有平台上都接受标准的<quote>infinity</quote>（无穷大）写法（Tom Lane）
     </para>

     <para>
      C99 标准规定允许的写法为 <literal>inf</literal>、<literal>+inf</literal>、<literal>-inf</literal>、<literal>infinity</literal>、<literal>+infinity</literal> 和 <literal>-infinity</literal>。即使平台的 <function>strtod</function> 函数不支持，也确保我们能识别这些写法。
     </para>
    </listitem>

    <listitem>
     <para>
      当 <varname>track_activities</varname> 关闭时避免不必要的报告（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      扩展行与记录和数组比较的能力（Rafal Rzepecki、Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      防止 <application>psql</application> 的 <envar>PSQLRC</envar> 变量包含波浪号时崩溃（Bruce Momjian）
     </para>
    </listitem>

    <listitem>
     <para>
      新增 ARM64 的自旋锁支持（Mark Salter）
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2013d
      for DST law changes in Israel, Morocco, Palestine, and Paraguay。Also, historical zone data corrections for Macquarie Island.
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-9-2-4">
  <title>版本 9.2.4</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2013-04-04</para>
  </formalpara>

  <para>
   本次发布包含对 9.2.3 的多方面修复。有关 9.2 大版本中新特性的信息，请参见<xref linkend="release-9-2">。.
  </para>

  <sect2>
   <title>迁移到版本 9.2.4</title>

   <para>
    对于运行 9.2.X 的用户，不需要进行转储/恢复。</para>

   <para>
      不过，本版本纠正了 GiST 索引管理中的若干错误。安装此更新后，建议对满足下述一个或多个条件的 GiST 索引执行 <command>REINDEX</command>。
     </para>

   <para>
    不过，如果你是从早于 9.2.2 的版本升级，请参见<xref linkend="release-9-2-2">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      修复服务器命令行开关解析不安全的问题（Mitsumasa Kondo、Kyotaro Horiguchi）
     </para>

     <para>
      包含以 <quote><literal>-</literal></quote> 开头的数据库名的连接请求可能被精心构造，用于破坏或摧毁服务器数据目录中的文件，即使该请求最终被拒绝也是如此。（CVE-2013-1899）
     </para>
    </listitem>

    <listitem>
     <para>
      在每个 postmaster 子进程中重置 OpenSSL 随机状态（Marko Kreen）
     </para>

     <para>
      这避免了另一种情况：另一数据库用户可能相对容易地猜出 <filename>contrib/pgcrypto</filename> 函数生成的随机数。只有当 postmaster 配置了 <varname>ssl</varname> = <literal>on</literal> 而大多数连接并不使用 SSL 加密时，这一风险才比较显著。（CVE-2013-1900）
     </para>
    </listitem>

    <listitem>
     <para>
      让 REPLICATION 权限检查测试当前用户而不是已认证用户（Noah Misch）
     </para>

     <para>
      无特权数据库用户可能利用这一失误调用 <function>pg_start_backup()</function> 或 <function>pg_stop_backup()</function>，从而可能干扰例程备份的创建。（CVE-2013-1901）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 GiST 索引在不合适的情况下使用<quote>模糊</quote>几何比较的问题（Alexander Korotkov）
     </para>

     <para>
      核心几何类型使用<quote>模糊</quote>相等进行比较，但 <function>gist_box_same</function> 必须做精确比较，否则使用它的 GiST 索引可能变得不一致。安装此更新后，用户应对 <type>box</type>、<type>polygon</type>、<type>circle</type> 或 <type>point</type> 列上的所有 GiST 索引执行 <command>REINDEX</command>，因为它们都使用 <function>gist_box_same</function>。
     </para>
    </listitem>

    <listitem>
     <para>
      修复对可变宽度数据类型（即 <type>text</type>、<type>bytea</type>、<type>bit</type> 和 <type>numeric</type> 列）使用 <filename>contrib/btree_gist</filename> 的 GiST 索引中错误的范围并集和惩罚逻辑（Tom Lane）
     </para>

     <para>
      这些错误可能导致不一致的索引：某些存在的键搜索不到，还会造成无用的索引膨胀。建议用户在安装此更新后对这类索引执行 <command>REINDEX</command>。
     </para>
    </listitem>

    <listitem>
     <para>
      修复多列索引的 GiST 页面分裂代码中的缺陷（Tom Lane）
     </para>

     <para>
      这些错误可能导致不一致的索引：某些存在的键搜索不到，还会使索引的搜索效率不必要地低下。建议用户在安装此更新后对多列 GiST 索引执行 <command>REINDEX</command>。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>gist_point_consistent</function>，使其一致地处理模糊性（Alexander Korotkov）
     </para>

     <para>
      <type>point</type> 列上 GiST 索引的索引扫描有时会得出与顺序扫描不同的结果，因为 <function>gist_point_consistent</function> 与底层操作符代码在是做精确比较还是模糊比较上不一致。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 WAL 重放中的缓冲区泄漏（Heikki Linnakangas）
     </para>

     <para>
      这一缺陷可能导致重放期间出现 <quote>incorrect local pin count</quote> 错误，使恢复无法进行。
     </para>
    </listitem>

    <listitem>
     <para>
      确保当数据库未被干净停止且存在 <filename>recovery.conf</filename> 文件时，在进入归档恢复之前先执行崩溃恢复（Heikki Linnakangas、Kyotaro Horiguchi、Mitsumasa Kondo）
     </para>

     <para>
      这用于确保数据库在某些场景下的一致性，例如用运行中服务器的文件系统快照初始化备库。
     </para>
    </listitem>

    <listitem>
     <para>
      避免在崩溃恢复期间删除尚未归档的 WAL 文件（Heikki Linnakangas、Fujii Masao）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <command>DELETE RETURNING</command> 中的竞态条件（Tom Lane）
     </para>

     <para>
      在合适的条件下，<command>DELETE RETURNING</command> 可能尝试从当前进程已不再持有任何 pin 的共享缓冲区中取数据。如果其他进程在此期间更改了该缓冲区，就会导致垃圾 <literal>RETURNING</literal> 输出，甚至崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      修复正则表达式编译中的无限循环风险（Tom Lane、Don Porter）
     </para>
    </listitem>

    <listitem>
     <para>
      修复正则表达式编译中潜在的空指针解引用（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>to_char()</function>，在适当的地方只使用 ASCII 大小写折叠规则（Tom Lane）
     </para>

     <para>
      这修复了一些本应与区域设置无关的模板模式在土耳其区域下错误处理 <quote><literal>I</literal></quote> 和 <quote><literal>i</literal></quote> 的问题。
     </para>
    </listitem>

    <listitem>
     <para>
      修复时间戳 <literal>1999-12-31 24:00:00</literal> 被不当拒绝的问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 SQL 语言函数，使其可以安全地用作范围类型的支持函数（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复单个事务先执行 <command>UNLISTEN</command> 再执行 <command>LISTEN</command> 时的逻辑错误（Tom Lane）
     </para>

     <para>
      会话最终完全没有监听通知事件，尽管在这种情况下它显然应该监听。
     </para>
    </listitem>

    <listitem>
     <para>
      修复在向被另一视图依赖的视图添加列之后规划器可能崩溃的问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>EXPLAIN (ANALYZE, TIMING OFF)</literal> 中的性能问题（Pavel Stehule）
     </para>
    </listitem>

    <listitem>
     <para>
      移除无用的 <quote>picksplit doesn't support secondary split</quote> 日志消息（Josh Hansen、Tom Lane）
     </para>

     <para>
      这条消息似乎是为预期中从未编写、而且可能永远不会编写的代码而添加的，因为 GiST 对二次分裂的默认处理其实相当好。所以不再为此打扰最终用户。
     </para>
    </listitem>

    <listitem>
     <para>
      移除 <function>gist_box_picksplit()</function> 中残留的二次分裂支持（Tom Lane）
     </para>

     <para>
      这个二次分裂实现不仅不比默认实现好，实际上更差。因此将其移除，让默认代码路径处理这种情况。
     </para>
    </listitem>

    <listitem>
     <para>
      修复会话最后几个事务的提交/中止计数可能未能发送到统计收集器的问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      消除 PL/Perl 的 <function>spi_prepare()</function> 函数中的内存泄漏（Alex Hunsaker、Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_dumpall</application> 对包含 <quote><literal>=</literal></quote> 的数据库名的处理（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      避免在给定不正确的连接串时 <application>pg_dump</application> 崩溃（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <application>pg_dump</application> 和 <application>pg_upgrade</application> 忽略无效索引（Michael Paquier、Bruce Momjian）
     </para>

     <para>
      转储无效索引可能在恢复时造成问题，例如当索引创建失败的原因是它试图强制执行表数据不满足的唯一性条件时。此外，如果索引创建实际上仍在进行中，把它视为未提交的 DDL 变更似乎是合理的，<application>pg_dump</application> 本来也不应转储它。<application>pg_upgrade</application> 现在也跳过无效索引而不是失败。
     </para>
    </listitem>

    <listitem>
     <para>
      在 <application>pg_basebackup</application> 中，备份表空间时只包含当前服务器版本的子目录（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      在 <application>pg_basebackup</application> 和 <application>pg_receivexlog</application> 中加入服务器版本检查，使其在版本组合无法工作时干净地失败（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/dblink</filename>，使其安全处理 <varname>DateStyle</varname> 或 <varname>IntervalStyle</varname> 设置不一致的情况（Daniel Farina、Tom Lane）
     </para>

     <para>
      此前，如果远程服务器这些参数的设置不同，有歧义的日期可能被错误读取。此修复确保 <filename>dblink</filename> 查询获取的日期时间和间隔列会被正确解释。但请注意，设置不一致仍然有风险，因为发送到远程服务器的 SQL 命令中出现的字面值的解释可能与本地不同。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/pg_trgm</filename> 的 <function>similarity()</function> 函数，使其对无三元组的字符串返回零（Tom Lane）
     </para>

     <para>
      此前它由于内部除以零而返回 <literal>NaN</literal>。
     </para>
    </listitem>

    <listitem>
     <para>
      支持使用 Microsoft Visual Studio 2012 构建 <productname>PostgreSQL</productname>（Brar Piening、Noah Misch）
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2013b
      for DST law changes in Chile, Haiti, Morocco, Paraguay, and some
      Russian areas。Also, historical zone data corrections for numerous
      places.
     </para>

     <para>
      此外，更新时区缩写文件以反映俄罗斯和其他地方的近期变更：<literal>CHOT</literal>、<literal>GET</literal>、<literal>IRKT</literal>、<literal>KGT</literal>、<literal>KRAT</literal>、<literal>MAGT</literal>、<literal>MAWT</literal>、<literal>MSK</literal>、<literal>NOVT</literal>、<literal>OMST</literal>、<literal>TKT</literal>、<literal>VLAT</literal>、<literal>WST</literal>、<literal>YAKT</literal>、<literal>YEKT</literal> 现在遵循其当前含义，同时把 <literal>VOLT</literal>（Europe/Volgograd）和 <literal>MIST</literal>（Antarctica/Macquarie）加入默认缩写列表。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-9-2-3">
  <title>版本 9.2.3</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2013-02-07</para>
  </formalpara>

  <para>
   本次发布包含对 9.2.2 的多方面修复。有关 9.2 大版本中新特性的信息，请参见<xref linkend="release-9-2">。.
  </para>

  <sect2>
   <title>迁移到版本 9.2.3</title>

   <para>
    对于运行 9.2.X 的用户，不需要进行转储/恢复。</para>

   <para>
    不过，如果你是从早于 9.2.2 的版本升级，请参见<xref linkend="release-9-2-2">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      防止从 SQL 执行 <function>enum_recv</function>（Tom Lane）
     </para>

     <para>
      该函数声明有误，允许一条简单的 SQL 命令使服务器崩溃。原则上攻击者可能利用它检查服务器内存的内容。感谢 Sumit Soni（通过 Secunia SVCRP）报告此问题。（CVE-2013-0255）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 WAL 重放期间检测何时达到一致数据库状态的多个问题（Fujii Masao、Heikki Linnakangas、Simon Riggs、Andres Freund）
     </para>
    </listitem>

    <listitem>
     <para>
      修复不需要实际重做工作时对备份结束点的检测（Heikki Linnakangas）
     </para>

     <para>
      这一失误可能导致错误的 <quote>WAL ends before end of online backup</quote> 错误。
     </para>
    </listitem>

    <listitem>
     <para>
      在截断关系文件时更新最小恢复点（Heikki Linnakangas）
     </para>

     <para>
      数据一旦被丢弃，在时间线上更早的点停止恢复就不再安全。
     </para>
    </listitem>

    <listitem>
     <para>
      修复更改恢复目标时间线后 WAL 段的回收问题（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      在级联备库服务器上正确地从归档恢复时间线历史文件（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      修复热备服务器上的锁冲突检测（Andres Freund、Robert Haas）
     </para>
    </listitem>

    <listitem>
     <para>
      修复热备模式下缺失的取消操作（Noah Misch、Simon Riggs）
     </para>

     <para>
      取消冲突热备查询的需要有时会被遗漏，使这些查询看到不一致的数据。
     </para>
    </listitem>

    <listitem>
     <para>
      防止恢复暂停功能在用户能够连接之前就暂停（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 SQL 文法，允许对子 SELECT 结果做下标或字段选择（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复繁忙负载下自动清理截断的性能问题（Jan Wieck）
     </para>

     <para>
      截断表末尾的空页需要排他锁，但自动清理被编码为在出现冲突锁请求时失败（并释放表锁）。在负载下，截断很可能永远不会发生，导致表膨胀。修复方法是先执行部分截断，释放锁，然后尝试重新获取锁并继续。此修复还大幅缩短了冲突请求到达后自动清理释放锁的平均时间。
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <function>SPI_execute</function> 及相关函数的性能，从而改进 PL/pgSQL 的 <literal>EXECUTE</literal>（Heikki Linnakangas、Tom Lane）
     </para>

     <para>
      移除 9.2 中因计划缓存机制修改而引入的一些数据复制开销。这消除了相比 9.1 的性能退化，还节省了内存，尤其是当要执行的查询串包含许多 SQL 语句时。
     </para>

     <para>
      一个附带好处是多语句查询串现在完全串行处理，即我们先完成前面语句的执行，再对后续语句做解析分析和规划。这消除了一个长期存在的问题：本应影响后续语句行为的 DDL 现在会按预期工作。
     </para>
    </listitem>

    <listitem>
     <para>
      恢复 9.2 之前对索引使用的代价估计（Tom Lane）
     </para>

     <para>
      一个欠考虑的修正因子变更导致对使用超大索引的代价估计高得离谱。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>DROP INDEX CONCURRENTLY</literal> 中的间歇性崩溃（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <command>CREATE/DROP INDEX CONCURRENTLY</command> 期间共享内存锁表可能损坏的问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <command>COPY</command> 的多元组插入代码在元组大于页大小减去填充因子时的情况（Heikki Linnakangas）
     </para>

     <para>
      先前的代码可能陷入无限循环。
     </para>
    </listitem>

    <listitem>
     <para>
      防止扫描 <structname>pg_tablespace</structname> 时的竞态条件（Stephen Frost、Tom Lane）
     </para>

     <para>
      如果 <structname>pg_tablespace</structname> 条目存在并发更新，<command>CREATE DATABASE</command> 和 <command>DROP DATABASE</command> 可能行为异常。
     </para>
    </listitem>

    <listitem>
     <para>
      防止 <command>DROP OWNED</command> 试图删除整个数据库或表空间（&Aacute;lvaro Herrera）
     </para>

     <para>
      为安全起见，这些对象的所有权必须重新分配，而不是删除。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <link linkend="guc-vacuum-freeze-table-age"><varname>vacuum_freeze_table_age</varname></link> 实现中的错误（Andres Freund）
     </para>

     <para>
      在已存在超过 <link linkend="guc-vacuum-freeze-min-age"><varname>vacuum_freeze_min_age</varname></link> 个事务的安装中，这一失误使自动清理无法使用部分表扫描，而总是进行全表扫描。
     </para>
    </listitem>

    <listitem>
     <para>
      防止 <symbol>RowExpr</symbol> 或 <symbol>XmlExpr</symbol> 被二次解析分析时的不当行为（Andres Freund、Tom Lane）
     </para>

     <para>
      这一失误在 <literal>CREATE TABLE LIKE INCLUDING INDEXES</literal> 等上下文中可能被用户看到。
     </para>
    </listitem>

    <listitem>
     <para>
      增强对哈希表大小计算中整数溢出的防御（Jeff Davis）
     </para>
    </listitem>

    <listitem>
     <para>
      修复一些与数据类型权限相关的缺陷（Tom Lane）
     </para>

     <para>
      类型的默认权限存在一些问题，而且 <application>pg_dump</application> 完全没有转储这类权限。
     </para>
    </listitem>

    <listitem>
     <para>
      修复服务器崩溃后未能忽略遗留临时表的问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 Windows 上 postmaster 日志文件未能因大小而轮转的问题（Jeff Janes、Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      拒绝 <function>to_date()</function> 中超出范围的日期（Hitoshi Harada）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>pg_extension_config_dump()</function>，使其正确处理扩展更新情况（Tom Lane）
     </para>

     <para>
      该函数现在会替换目标表的任何现有条目，使其可以在扩展更新脚本中使用。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PL/pgSQL 对可能简单的表达式中规划期错误的报告（Tom Lane）
     </para>

     <para>
      先前的编码有时会遗漏错误 <literal>CONTEXT</literal> 回溯的第一行。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PL/Python 对用作多个表上触发器的函数的处理（Andres Freund）
     </para>
    </listitem>

    <listitem>
     <para>
      确保在 Windows 上非 ASCII 提示符字符串被转换到正确的代码页（Alexander Law、Noah Misch）
     </para>

     <para>
      这一缺陷影响 <application>psql</application> 和其他一些客户端程序。
     </para>
    </listitem>

    <listitem>
     <para>
      修复未连接数据库时 <application>psql</application> 的 <command>\?</command> 命令可能崩溃的问题（Meng Qingzhong）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_basebackup</application> 运行期间关系文件被删除时可能出错的问题（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <literal>pg_basebackup -X fetch</literal> 在备份备库服务器时容忍时间线切换（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <application>pg_dump</application> 在热备服务器上运行时排除不记录日志的表的数据（Magnus Hagander）
     </para>

     <para>
      由于数据在备库服务器上不可用，这本来就会失败，因此最方便的做法似乎是自动假定 <option>--no-unlogged-table-data</option>。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_upgrade</application>，使其安全处理无效索引（Bruce Momjian）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_upgrade</application> 的 -O/-o 选项（Marti Raudsepp）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>libpq</application> 的 <function>PQprintTuples</function> 中的一字节缓冲区越界（Xi Wang）
     </para>

     <para>
      这个古老的函数没有被 <productname>PostgreSQL</productname> 本身在任何地方使用，但仍可能被某些客户端代码使用。
     </para>
    </listitem>

    <listitem>
     <para>
      让 <application>ecpglib</application> 正确使用翻译后的消息（Chen Huajun）
     </para>
    </listitem>

    <listitem>
     <para>
      在 MSVC 上正确安装 <application>ecpg_compat</application> 和 <application>pgtypes</application> 库（Jiang Guiqing）
     </para>
    </listitem>

    <listitem>
     <para>
      如果系统未提供 <function>isinf()</function>，则在 <application>libecpg</application> 中包含我们自己的版本（Jiang Guiqing）
     </para>
    </listitem>

    <listitem>
     <para>
      重新排列 configure 对系统提供函数的测试，使其不被 libedit/libreadline 的虚假导出所欺骗（Christoph Berg）
     </para>
    </listitem>

    <listitem>
     <para>
      确保 Windows 构建号随时间递增（Magnus Hagander）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <application>pgxs</application> 在为 Windows 交叉编译时以正确的 <literal>.exe</literal> 后缀构建可执行文件（Zoltan Boszormenyi）
     </para>
    </listitem>

    <listitem>
     <para>
      新增时区缩写 <literal>FET</literal>（Tom Lane）
     </para>

     <para>
      它现在用于一些东欧时区。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-9-2-2">
  <title>版本 9.2.2</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2012-12-06</para>
  </formalpara>

  <para>
   本次发布包含对 9.2.1 的多方面修复。有关 9.2 大版本中新特性的信息，请参见<xref linkend="release-9-2">。.
  </para>

  <sect2>
   <title>迁移到版本 9.2.2</title>

   <para>
    对于运行 9.2.X 的用户，不需要进行转储/恢复。</para>

   <para>
      不过，你可能需要执行 <command>REINDEX</command> 操作来纠正并发构建的索引中的问题，如下面第一条变更日志条目所述。
     </para>

   <para>
     另外，如果你是从 9.2.0 版本升级，参见<xref linkend="release-9-2-1">。
    </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      修复与 <command>CREATE/DROP INDEX CONCURRENTLY</command> 相关的多个缺陷（Andres Freund、Tom Lane、Simon Riggs、Pavan Deolasee）
     </para>

     <para>
      添加 <command>DROP INDEX CONCURRENTLY</command> 时引入的一个错误，允许在 <command>CREATE INDEX CONCURRENTLY</command> 的初始阶段做出错误的索引决策，导致该命令构建的索引可能损坏。建议在应用此更新后，重建 9.2.X 中用 <command>CREATE INDEX CONCURRENTLY</command> 构建的所有索引。
     </para>

     <para>
      此外，修复 <command>CREATE/DROP INDEX CONCURRENTLY</command>，使其在更改索引 <structname>pg_index</structname> 行的状态时使用就地更新。这防止了可能导致并发会话错过更新目标索引（从而再次产生损坏的并发创建索引）的竞态条件。
     </para>

     <para>
      另外，修复各种其他操作，确保它们忽略由失败的 <command>CREATE INDEX CONCURRENTLY</command> 命令产生的无效索引。其中最重要的是 <command>VACUUM</command>，因为在采取纠正措施修复或删除无效索引之前，很容易就有自动清理在该表上启动。
     </para>

     <para>
      还修复了 <command>DROP INDEX CONCURRENTLY</command>，使其在使用目标索引的所有查询完成之前不禁止向其插入。
     </para>

     <para>
      还修复了 <command>DROP INDEX CONCURRENTLY</command> 被取消时的不当行为：先前的编码可能留下一个无法删除的索引。
     </para>
    </listitem>

    <listitem>
     <para>
      修正 <command>DROP INDEX CONCURRENTLY</command> 的谓词锁定（Kevin Grittner）
     </para>

     <para>
      此前，SSI 谓词锁的处理时机不对，可能导致与 <command>DROP</command> 并行执行的可序列化事务行为不正确。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 WAL 重放期间的缓冲区锁定（Tom Lane）
     </para>

     <para>
      WAL 重放代码在重放影响多个页面的 WAL 记录时，对缓冲区加锁不够谨慎。这可能导致热备查询瞬时看到不一致的状态，造成错误结果或意外失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 GIN 索引 WAL 生成逻辑中的一个错误（Tom Lane）
     </para>

     <para>
      如果发生残缺页故障，这可能导致索引损坏。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 SP-GiST 索引 WAL 重放逻辑中的一个错误（Tom Lane）
     </para>

     <para>
      这可能导致崩溃之后或备库服务器上的索引损坏。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 WAL 恢复期间对基础备份结束点的不正确检测（Heikki Linnakangas）
     </para>

     <para>
      这一失误使热备模式可能在数据库达到一致状态之前就启动。
     </para>
    </listitem>

    <listitem>
     <para>
      把热备服务器提升为正常运行时，正确移除启动进程的虚拟 XID 锁（Simon Riggs）
     </para>

     <para>
      这一疏忽可能阻止后续执行某些操作，例如 <command>CREATE INDEX CONCURRENTLY</command>。
     </para>
    </listitem>

    <listitem>
     <para>
      避免备库模式下虚假的 <quote>out-of-sequence timeline ID</quote> 错误（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      防止 postmaster 在收到关闭信号后继续启动新的子进程（Tom Lane）
     </para>

     <para>
      这一失误可能导致关闭耗时比应有的更长，甚至在没有额外用户操作的情况下永远无法完成。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <varname>log_rotation_age</varname> 超过 2^31 毫秒（约 25 天）时 syslogger 进程失败的问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>WaitLatch()</function>，使其在请求的超时到期时立即返回（Jeff Janes、Tom Lane）
     </para>

     <para>
      在先前的编码下，持续不断的非等待终止中断可能无限期地延迟 <function>WaitLatch()</function> 的返回。这已被证明会给自动清理启动器进程带来问题，也可能在其他地方造成麻烦。
     </para>
    </listitem>

    <listitem>
     <para>
      避免内存不足时内部哈希表损坏（Hitoshi Harada）
     </para>
    </listitem>

    <listitem>
     <para>
      防止已删除表的文件描述符在事务结束之后仍保持打开（Tom Lane）
     </para>

     <para>
      这应能减少早已删除的表继续占用磁盘空间的问题。
     </para>
    </listitem>

    <listitem>
     <para>
      防止当新子进程无法为其闩锁创建管道时整个数据库崩溃并重启（Tom Lane）
     </para>

     <para>
      虽然新进程必须失败，但没有充分理由强制整个数据库重启，因此避免这样做。这提高了内核文件描述符几乎耗尽时的健壮性。
     </para>
    </listitem>

    <listitem>
     <para>
      避免与未展平子查询连接时规划器崩溃（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复外连接之上非严格等值子句的规划（Tom Lane）
     </para>

     <para>
      规划器可能从把非严格构造与别的值等同的子句中推导出错误的约束，例如当 <literal>foo</literal> 来自外连接的可空侧时的 <literal>WHERE COALESCE(foo, 0) = 0</literal>。9.2 中出现这类错误的情况比以前的版本多，但基本缺陷已经存在很长时间。
     </para>
    </listitem>

    <listitem>
     <para>
      修复在继承树上对索引优化的 <function>MIN</function>/<function>MAX</function> 使用 <command>SELECT DISTINCT</command> 的问题（Tom Lane）
     </para>

     <para>
      在这种因素组合下，规划器会报 <quote>failed to re-find MinMaxAggInfo record</quote> 失败。
     </para>
    </listitem>

    <listitem>
     <para>
      确保规划器在所有用途上把隐式和显式转换视为等价，除了极少数确实存在语义差异的情况（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      在考虑查询能否使用部分索引时把连接子句也考虑在内（Tom Lane）
     </para>

     <para>
      例如，一个严格连接子句就足以确立 <replaceable>x</replaceable> <literal>IS NOT NULL</literal> 谓词。这修复了 9.2 中的一项规划器退化，因为以前的版本可以做类似的推导。
     </para>
    </listitem>

    <listitem>
     <para>
      限制当同一索引有大量可索引连接子句时规划时间的增长（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      改进规划器从等价类证明排他约束的能力（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复哈希子计划中的部分行匹配，使其正确处理跨类型情况（Tom Lane）
     </para>

     <para>
      这影响多列 <literal>NOT IN</literal> 子计划，例如 <literal>WHERE (a, b) NOT IN (SELECT x, y FROM ...)</literal>，当 <literal>b</literal> 和 <literal>y</literal> 分别是 <type>int4</type> 和 <type>int8</type> 时。根据所涉及的具体数据类型，这一失误会导致错误答案或崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 btree 标记/恢复函数，使其处理数组键（Tom Lane）
     </para>

     <para>
      这一疏忽可能导致内层是使用 <literal><replaceable>indexed_column</replaceable> = ANY(<replaceable>array</replaceable>)</literal> 条件的索引扫描的合并连接给出错误答案。
     </para>
    </listitem>

    <listitem>
     <para>
      撤销减少快照获取数量的补丁（Tom Lane）
     </para>

     <para>
      9.2 中减少查询执行期间快照获取数量的变更导致了一些以前版本中未见的异常行为，因为执行会使用在锁定查询所用的表之前获取的快照继续进行。因此，例如即使前一事务持有排他锁，查询也不保证能看到该事务提交的更新。我们可能会在未来版本中重新处理这一点，但目前先恢复为 9.2 之前的方式。
     </para>
    </listitem>

    <listitem>
     <para>
      在为 <literal>AFTER ROW UPDATE/DELETE</literal> 触发器重新获取旧元组时获取缓冲区锁（Andres Freund）
     </para>

     <para>
      在非常罕见的情况下，这一疏忽可能导致向触发器 <literal>WHEN</literal> 条件或外键强制触发器的预检查逻辑传递不正确的数据。这可能导致崩溃，或者在是否触发触发器上做出错误决定。
     </para>
    </listitem>

    <listitem>
     <para>
      修复<command>ALTER COLUMN TYPE</command>以正确重建继承的检查约束（Tom Lane）
     </para>

     <para>
      这在 8.4 之前的版本中工作正常，现在在 8.4 及更高版本中也工作正常。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <command>ALTER EXTENSION SET SCHEMA</command> 未能把一些从属对象移入新模式的问题（&Aacute;lvaro Herrera、Dimitri Fontaine）
     </para>
    </listitem>

    <listitem>
     <para>
      在扩展查询协议中正确处理 <command>CREATE TABLE AS EXECUTE</command>（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      在 <command>DROP RULE IF NOT EXISTS</command> 和 <command>DROP TRIGGER IF NOT EXISTS</command> 中不修改输入解析树（Tom Lane）
     </para>

     <para>
      如果这类已缓存语句被重新执行，这一失误会导致错误。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <command>REASSIGN OWNED</command> 对表空间授权的处理（&Aacute;lvaro Herrera）
     </para>
    </listitem>

    <listitem>
     <para>
      忽略视图中系统列的不正确 <structname>pg_attribute</structname> 条目（Tom Lane）
     </para>

     <para>
      视图没有任何系统列。然而，我们在把表转换为视图时忘记移除这类条目。这一问题在 9.3 及更高版本中已正确修复，但在以前的分支中，我们需要防御已经错误转换的视图。
     </para>
    </listitem>

    <listitem>
     <para>
      修复规则打印，使其正确转储 <literal>INSERT INTO <replaceable>table</replaceable> DEFAULT VALUES</literal>（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      防止当查询中有过多 <literal>UNION</literal>/<literal>INTERSECT</literal>/<literal>EXCEPT</literal> 子句时栈溢出（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      防止用最小可能整数值除以 -1 时依赖平台的失败（Xi Wang、Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复日期解析中可能越过字符串末尾的访问（Hitoshi Harada）
     </para>
    </listitem>

    <listitem>
     <para>
      修复当 XID 回卷发生在检查点期间且 <varname>wal_level</varname> 为 <literal>hot_standby</literal> 时 XID 纪元（epoch）未能推进的问题（Tom Lane、Andres Freund）
     </para>

     <para>
      虽然这一失误对 <productname>PostgreSQL</productname> 本身没有特别影响，但它对依赖 <function>txid_current()</function> 及相关函数的应用不利：TXID 值看起来会倒退。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>pg_terminate_backend()</function> 和 <function>pg_cancel_backend()</function>，使其对不存在的目标进程不抛出错误（Josh Kupershmidt）
     </para>

     <para>
      这种情况下，超级用户调用时已经按预期工作，但普通用户调用时则不然。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <structname>pg_stat_replication</structname>.<structfield>sync_state</structfield> 在页边界处的显示（Kyotaro Horiguchi）
     </para>
    </listitem>

    <listitem>
     <para>
      当 Unix 域套接字的路径名长度超过平台特定限制时，生成可理解的错误消息（Tom Lane、Andrew Dunstan）
     </para>

     <para>
      此前这会导致相当无用的结果，例如 <quote>Non-recoverable failure in name resolution</quote>。
     </para>
    </listitem>

    <listitem>
     <para>
      修复向客户端发送组合列值时的内存泄漏（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      通过在提交时不搜索子事务锁来节省一些周期（Simon Riggs）
     </para>

     <para>
      在持有大量排他锁的事务中，这种无用活动可能开销相当大。
     </para>
    </listitem>

    <listitem>
     <para>
      使 <application>pg_ctl</application> 读取 <filename>postmaster.pid</filename> 文件更健壮（Heikki Linnakangas）
     </para>

     <para>
      这修复了竞态条件和可能的文件描述符泄漏。
     </para>
    </listitem>

    <listitem>
     <para>
      修复当呈现错误编码的数据且 <varname>client_encoding</varname> 设置为仅客户端编码（如 SJIS）时 <application>psql</application> 可能崩溃的问题（Jiang Guiqing）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <application>pg_dump</application> 把 <literal>SEQUENCE SET</literal> 项转储到归档的数据节而不是前数据节（Tom Lane）
     </para>

     <para>
      这修复了 <option>--data-only</option> 与 <option>--section=data</option> 含义之间不合需要的 inconsistency，还修复了被标记为扩展配置表的序列的转储。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_dump</application> 在 <option>--clean</option> 模式下对 <command>DROP DATABASE</command> 命令的处理（Guillaume Lelarge）
     </para>

     <para>
      从 9.2.0 开始，<literal>pg_dump --clean</literal> 会发出一条 <command>DROP DATABASE</command> 命令，根据使用场景的不同，这要么无用要么危险。它不再这样做。此变更还修复了 <option>--clean</option> 与 <option>--create</option> 的组合，使其合理工作，即在重新连接到目标数据库之前发出 <command>DROP DATABASE</command> 和 <command>CREATE DATABASE</command>。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_dump</application> 处理带循环依赖且无关系选项的视图的问题（Tom Lane）
     </para>

     <para>
      先前对涉及循环依赖的视图转储关系选项的修复，在视图没有选项的情况下不能正确工作；它生成了语法无效的 <literal>ALTER VIEW foo SET ()</literal>。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_dump</application> 以 <literal>tar</literal> 输出格式生成的 <filename>restore.sql</filename> 脚本中的缺陷（Tom Lane）
     </para>

     <para>
      该脚本在表名包含大写字符时会直接失败。此外，让该脚本既能以常规 COPY 模式也能以 <option>--inserts</option> 模式恢复数据。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_restore</application>，使其接受符合 POSIX 的 <literal>tar</literal> 文件（Brian Weaver、Tom Lane）
     </para>

     <para>
      <application>pg_dump</application> 的 <literal>tar</literal> 输出模式的原始编码生成的文件并不完全符合 POSIX 标准。这在 9.3 版本中已得到纠正。此补丁更新了以前的分支，使它们同时接受错误格式和纠正后的格式，希望借此避免 9.3 发布时的兼容性问题。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_basebackup</application> 生成的 <literal>tar</literal> 文件，使其符合 POSIX（Brian Weaver、Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_resetxlog</application>，使其在给定数据目录相对路径时正确定位 <filename>postmaster.pid</filename>（Tom Lane）
     </para>

     <para>
      这一失误可能导致 <application>pg_resetxlog</application> 没有注意到有活动 postmaster 正在使用该数据目录。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>libpq</application> 的 <function>lo_import()</function> 和 <function>lo_export()</function> 函数，使其正确报告文件 I/O 错误（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>ecpg</application> 对嵌套结构指针变量的处理（Muhammad Usama）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>ecpg</application> 的 <function>ecpg_get_data</function> 函数，使其正确处理数组（Michael Meskes）
     </para>
    </listitem>

    <listitem>
     <para>
      防止 <application>pg_upgrade</application> 试图处理系统目录的 TOAST 表（Bruce Momjian）
     </para>

     <para>
      这修复了当 <literal>information_schema</literal> 被删除并重建后出现的错误。其他失败也可能出现。
     </para>
    </listitem>

    <listitem>
     <para>
      通过在新集簇中把 <varname>synchronous_commit</varname> 设为 <literal>off</literal> 来改进 <application>pg_upgrade</application> 性能（Bruce Momjian）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <filename>contrib/pageinspect</filename> 的 btree 页面检查函数在检查页面时持有缓冲区锁（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      规避 <literal>malloc(0)</literal> 和 <literal>realloc(NULL, 0)</literal> 不可移植的行为（Tom Lane）
     </para>

     <para>
      在这些调用返回 <literal>NULL</literal> 的平台上，一些代码误以为这意味着内存不足。已知这曾导致 <application>pg_dump</application> 在不含用户定义聚合的数据库上出错。可能还有其他情况。
     </para>
    </listitem>

    <listitem>
     <para>
      确保扩展的 <literal>make install</literal> 会创建 <filename>extension</filename> 安装目录（C&eacute;dric Villemain）
     </para>

     <para>
      此前，如果扩展的 Makefile 设置了 <varname>MODULEDIR</varname>，就会漏掉这一步。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pgxs</application> 在 AIX 上构建可加载模块的支持（Tom Lane）
     </para>

     <para>
      在原始源码树之外构建模块在 AIX 上不工作。
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2012j
      for DST law changes in Cuba, Israel, Jordan, Libya, Palestine, Western
      Samoa, and portions of Brazil。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-9-2-1">
  <title>版本 9.2.1</title>

  <formalpara>
  <title>发行日期：</title>
  <para>2012-09-24</para>
  </formalpara>

  <para>
   本次发布包含对 9.2.0 的多方面修复。有关 9.2 大版本中新特性的信息，请参见<xref linkend="release-9-2">。.
  </para>

  <sect2>
   <title>迁移到版本 9.2.1</title>

   <para>
    对于运行 9.2.X 的用户，不需要进行转储/恢复。</para>

   <para>
      不过，你可能需要执行 <command>REINDEX</command> 和/或 <command>VACUUM</command> 操作，以从下面第一条变更日志条目所述的数据损坏缺陷的影响中恢复。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      修复 WAL 重放期间共享缓冲区的持久性标记（Jeff Davis）
     </para>

     <para>
      这一失误可能导致缓冲区在检查点期间未被写出，如果服务器之后在这些缓冲区从未写出的情况下崩溃，就会造成数据损坏。任何服务器在崩溃恢复之后都可能发生损坏，但在备库从库服务器上发生的可能性显著更高，因为它们执行多得多的 WAL 重放。btree 和 GIN 索引损坏的概率较低。表<quote>可见性映射</quote>损坏的概率要高得多，可能导致仅索引扫描给出错误答案。表数据本身不会因此缺陷损坏。
     </para>

     <para>
      虽然尚无此缺陷导致的索引损坏在实际环境中发生的已知案例，但作为预防措施，建议生产安装在升级到 9.2.1 后的方便时间对所有 btree 和 GIN 索引执行 <command>REINDEX</command>。
     </para>

     <para>
      此外，建议在把 <link linkend="guc-vacuum-freeze-table-age"><varname>vacuum_freeze_table_age</varname></link> 设为零的情况下对所有表执行 <command>VACUUM</command>。这将修复任何不正确的可见性映射数据。可以调整 <link linkend="guc-vacuum-cost-delay"><varname>vacuum_cost_delay</varname></link> 来降低清理的性能影响，代价是需要更长时间完成。
     </para>
    </listitem>

    <listitem>
     <para>
      修复涉及 <literal>WHERE <replaceable>indexed_column</replaceable> IN (<replaceable>list_of_values</replaceable>)</literal> 的查询输出可能排序不正确的问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复涉及 <literal>GROUP BY</literal> 表达式以及窗口函数和聚合的查询的规划器失败（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复规划器对执行器参数的分配（Tom Lane）
     </para>

     <para>
      这一错误可能导致多次扫描同一 <literal>WITH</literal> 子查询的查询给出错误答案。
     </para>
    </listitem>

    <listitem>
     <para>
      改进规划器对索引扫描中连接条件的处理（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      改进对涉及前缀（即 <replaceable>word</replaceable><literal>:*</literal> 模式）的文本搜索查询的选择率估计（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复权限变更识别延迟的问题（Tom Lane）
     </para>

     <para>
      除了其事务已持有的锁之外不需要其他锁的命令，可能注意不到自其事务开始以来提交的并发 <command>GRANT</command> 或 <command>REVOKE</command>。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <command>ANALYZE</command> 在列是范围类型之上的域时失败的问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      防止递归 PL/Perl 函数在执行期间被重定义时 PL/Perl 崩溃（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      规避 PL/Perl 中可能的错误优化（Tom Lane）
     </para>

     <para>
      一些 Linux 发行版包含错误版本的 <filename>pthread.h</filename>，导致 PL/Perl 中的编译代码不正确，当一个 PL/Perl 函数调用另一个会抛出错误的函数时会导致崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      移除 <application>pg_upgrade</application> 对 <application>pg_config</application> 的不必要依赖（Peter Eisentraut）
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2014e，以反映克里米亚、埃及和摩洛哥的夏令时法规变更。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-9-2">
  <title>版本 9.2</title>

  <formalpara>
   <title>发行日期：</title>
   <para>2012-09-10</para>
  </formalpara>

  <sect2>
   <title>概述</title>

   <para>
      本版本主要聚焦于性能改进，但新的 SQL 特性也不缺乏。复制支持方面的工作也在继续。主要增强包括：
     </para>

   <itemizedlist>

    <!-- This list duplicates items below, but without authors or details-->

      <listitem>
       <para>
      允许查询只从索引检索数据，避免堆访问（<firstterm>仅索引扫描</firstterm>）
     </para>
      </listitem>

      <listitem>
       <para>
      允许规划器即使在使用预备语句时，也为特定参数值生成定制计划
     </para>
      </listitem>

      <listitem>
       <para>
      改进规划器使用内层索引扫描的嵌套循环的能力
     </para>
      </listitem>

      <listitem>
       <para>
      允许流复制从库向其他从库转发数据（<link linkend="cascading-replication"><firstterm>级联复制</firstterm></link>）
     </para>
      </listitem>

      <listitem>
       <para>
      允许 <link linkend="app-pgbasebackup"><application>pg_basebackup</application></link> 从备库服务器做基础备份
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <link linkend="app-pgreceivexlog"><application>pg_receivexlog</application></link> 工具，用于在 WAL 文件变更写入时归档它们
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <link linkend="SPGiST">SP-GiST</link>（空间分区 GiST）索引访问方法
     </para>
      </listitem>

      <listitem>
       <para>
      新增对<link linkend="rangetypes">范围数据类型</link>的支持
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <link linkend="datatype-json"><type>JSON</type></link> 数据类型
     </para>
      </listitem>

      <listitem>
       <para>
      为视图新增 <link linkend="SQL-CREATEVIEW"><literal>security_barrier</literal></link> 选项
     </para>
      </listitem>

      <listitem>
       <para>
      允许 <application>libpq</application> 连接串采用 <link linkend="libpq-connstring"><acronym>URI</acronym></link> 格式
     </para>
      </listitem>

      <listitem>
       <para>
      为 <application>libpq</application> 新增<link linkend="libpq-single-row-mode">单行处理模式</link>，以更好地处理大型结果集
     </para>
      </listitem>

   </itemizedlist>

   <para>
     上述各项在下面的章节中有更详细的说明。
    </para>

  </sect2>

  <sect2>

  <title>迁移到版本 9.2</title>

   <para>
      对于希望从任何早期版本迁移数据的用户，需要使用 <application>pg_dump</application> 进行转储/恢复，或者使用 <application>pg_upgrade</application>。
     </para>

   <para>
     版本 9.2 包含许多可能影响与以前版本兼容性的变更。请注意以下不兼容之处：
    </para>

   <sect3>
    <title>系统目录</title>

    <itemizedlist>

      <listitem>
       <para>
      从 <link linkend="catalog-pg-tablespace"><structname>pg_tablespace</structname></link> 中移除 <structfield>spclocation</structfield> 字段（Magnus Hagander）
     </para>

       <para>
      该字段与实际定义表空间位置的符号链接重复，因此在移动表空间时存在遗漏错误的风险。此变更允许在服务器停机期间通过手工调整符号链接来移动表空间目录。为了替代该字段，我们新增了 <link linkend="functions-info-catalog-table"><function>pg_tablespace_location()</function></link> 来查询符号链接。
     </para>
      </listitem>

      <listitem>
       <para>
      把 <type>tsvector</type> 的最常用元素统计移到 <link linkend="view-pg-stats"><structname>pg_stats</structname></link> 的新列中（Alexander Korotkov）
     </para>

       <para>
      对于 <type>tsvector</type> 列，此前在 <structfield>most_common_vals</structfield> 和 <structfield>most_common_freqs</structfield> 中可用的数据，请改查 <structfield>most_common_elems</structfield> 和 <structfield>most_common_elem_freqs</structfield>。
     </para>
      </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title>函数</title>

    <itemizedlist>

      <listitem>
       <para>
      移除 <link linkend="hstore">hstore</link> 的 <literal>=&gt;</literal> 操作符（Robert Haas）
     </para>

       <para>
      用户现在应使用 <function>hstore(text, text)</function>。自 <productname>PostgreSQL</productname> 9.0 起，创建名为 <literal>=&gt;</literal> 的操作符时会发出警告消息，因为 <acronym>SQL</acronym> 标准将该记号保留作其他用途。
     </para>
      </listitem>

      <listitem>
       <para>
      确保 <link linkend="functions-xml-processing"><function>xpath()</function></link> 对字符串值中的特殊字符转义（Florian Pflug）
     </para>

       <para>
      否则结果有可能不是有效的 <acronym>XML</acronym>。
     </para>
      </listitem>

      <listitem>
       <para>
      让 <link linkend="functions-admin-dbobject"><function>pg_relation_size()</function></link> 及相关函数在对象不存在时返回 NULL（Phil Sorber）
     </para>

       <para>
      这防止了调用这些函数的查询在并发 <command>DROP</command> 之后立即返回错误。
     </para>
      </listitem>

      <listitem>
       <para>
      让 <link linkend="functions-datetime-extract"><function>EXTRACT(EPOCH FROM <replaceable>timestamp without time zone</replaceable>)</function></link> 从本地午夜而不是 <acronym>UTC</acronym> 午夜起算纪元（Tom Lane）
     </para>

       <para>
      此变更撤销了 7.3 版本中一个欠考虑的变更。从 <acronym>UTC</acronym> 午夜起算是不一致的，因为它使结果依赖于 <link linkend="guc-timezone"><varname>timezone</varname></link> 设置，而 <type>timestamp without time zone</type> 的计算不应如此。把输入值转换为 <type>timestamp with time zone</type> 仍可获得先前的行为。
     </para>
      </listitem>

      <listitem>
       <para>
      正确解析带尾缀 <literal>yesterday</literal>、<literal>today</literal> 和 <literal>tomorrow</literal> 的时间串（Dean Rasheed）
     </para>

       <para>
      此前，<literal>SELECT '04:00:00 yesterday'::timestamp</literal> 返回的是昨天午夜的日期。
     </para>
      </listitem>

      <listitem>
       <para>
      修复 <link linkend="functions-formatting"><function>to_date()</function></link> 和 <function>to_timestamp()</function>，使不完整日期向 2020 年折算（Bruce Momjian）
     </para>

       <para>
      此前，少于四位的提供年份和年份掩码的折算不一致。
     </para>
      </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title>对象修改</title>

    <itemizedlist>

      <listitem>
       <para>
      防止 <link linkend="SQL-ALTERDOMAIN"><command>ALTER DOMAIN</command></link> 作用于非域类型（Peter Eisentraut）
     </para>

       <para>
      此前在非域类型上可以更改所有者和模式。
     </para>
      </listitem>

      <listitem>
       <para>
      不再在 <link linkend="SQL-CREATEFUNCTION"><command>CREATE FUNCTION</command></link> 中强制小写过程语言名（Robert Haas）
     </para>

       <para>
      虽然不带引号的语言标识符仍会小写，但字符串和带引号的标识符不再被强制小写。因此例如 <literal>CREATE FUNCTION ... LANGUAGE 'C'</literal> 将不再有效；必须写成 <literal>'c'</literal>，或者最好省略引号。
     </para>
      </listitem>

      <listitem>
       <para>
      更改外键强制触发器的系统生成名称（Tom Lane）
     </para>

       <para>
      此变更确保在某些涉及自引用外键约束的极端情况下触发器按正确顺序触发。
     </para>
      </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title>命令行工具</title>

    <itemizedlist>

      <listitem>
       <para>
      为 <link linkend="APP-PSQL"><application>psql</application></link> 元命令参数提供一致的反引号、变量展开和带引号子串行为（Tom Lane）
     </para>

       <para>
      此前，当这类引用与相邻文本之间没有空白分隔时，处理方式很奇怪。例如 <literal>'FOO'BAR</literal> 会输出为 <literal>FOO BAR</literal>（意外插入空格），而 <literal>FOO'BAR'BAZ</literal> 会原样输出（不像大多数人期望的那样去掉引号）。
     </para>
      </listitem>

      <listitem>
       <para>
      不再把 <link linkend="APP-CLUSTERDB"><application>clusterdb</application></link> 的表名视为已加双引号；不再把 <link linkend="APP-REINDEXDB"><application>reindexdb</application></link> 的表名和索引名视为已加双引号（Bruce Momjian）
     </para>

       <para>
      如果需要加引号，用户现在必须在命令参数中包含双引号。
     </para>
      </listitem>

      <listitem>
       <para>
      <link linkend="APP-CREATEUSER"><application>createuser</application></link> 默认不再提示输入选项设置（Peter Eisentraut）
     </para>

       <para>
      使用 <option>--interactive</option> 获得旧行为。
     </para>
      </listitem>

      <listitem>
       <para>
      除非指定 <option>--interactive</option>，否则禁用 <link linkend="APP-DROPUSER"><application>dropuser</application></link> 中的用户名提示（Peter Eisentraut）
     </para>
      </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title>服务器设置</title>

    <itemizedlist>

      <listitem>
       <para>
      新增用于指定<link linkend="guc-ssl-ca-file">服务器端 <acronym>SSL</acronym> 文件位置</link>的服务器参数（Peter Eisentraut）
     </para>

       <para>
      这允许更改此前在数据目录中硬编码为 <filename>server.crt</filename>、<filename>server.key</filename>、<filename>root.crt</filename> 和 <filename>root.crl</filename> 的文件的名称和位置。<emphasis>服务器默认不再检查 <filename>root.crt</filename> 或 <filename>root.crl</filename></emphasis>；要加载这些文件，必须把相关参数设为非默认值。
     </para>
      </listitem>

      <listitem>
       <para>
      移除 <varname>silent_mode</varname> 参数（Heikki Linnakangas）
     </para>

       <para>
      类似的行为可以用 <command>pg_ctl start -l postmaster.log</command> 获得。
     </para>
      </listitem>

      <listitem>
       <para>
      移除 <varname>wal_sender_delay</varname> 参数，因为它不再需要（Tom Lane）
     </para>
      </listitem>

      <listitem>
       <para>
      移除 <varname>custom_variable_classes</varname> 参数（Tom Lane）
     </para>

       <para>
      该设置提供的检查价值存疑。现在任何设置都可以以任何类名为前缀。
     </para>
      </listitem>

    </itemizedlist>
   </sect3>

   <sect3>
    <title>监控</title>

    <itemizedlist>

      <listitem>
       <para>
      把 <link linkend="monitoring-stats-views-table"><structname>pg_stat_activity</structname></link><structfield>.procpid</structfield> 重命名为 <structfield>pid</structfield>，以与其他系统表一致（Magnus Hagander）
     </para>
      </listitem>

      <listitem>
       <para>
      为 <structfield>pg_stat_activity</structfield> 创建单独的列来报告进程状态（Scott Mead、Magnus Hagander）
     </para>

       <para>
      先前的 <structfield>query</structfield> 和 <structfield>query_start</structfield> 值现在对空闲会话仍然可用，从而支持更强的分析。
     </para>
      </listitem>

      <listitem>
       <para>
      把 <structname>pg_stat_activity</structname>.<structfield>current_query</structfield> 重命名为 <structfield>query</structfield>，因为它在查询完成后不会被清除（Magnus Hagander）
     </para>
      </listitem>

      <listitem>
       <para>
      把所有 <acronym>SQL</acronym> 级统计计时值改为以毫秒计的 <type>float8</type> 列（Tom Lane）
     </para>

       <para>
      此变更消除了设计中"值精确到微秒且仅到微秒"的假设（因为 <type>float8</type> 值可以带小数）。受影响的列有 <structname>pg_stat_user_functions</structname>.<structfield>total_time</structfield>、<structname>pg_stat_user_functions</structname>.<structfield>self_time</structfield>、<structname>pg_stat_xact_user_functions</structname>.<structfield>total_time</structfield> 和 <structname>pg_stat_xact_user_functions</structname>.<structfield>self_time</structfield>。这些列底层的统计函数现在也返回 <type>float8</type> 毫秒而不是 <type>bigint</type> 微秒。<filename>contrib/pg_stat_statements</filename> 的 <structfield>total_time</structfield> 列现在也以毫秒计。
     </para>
      </listitem>

    </itemizedlist>

   </sect3>

  </sect2>

  <sect2>
   <title>变更</title>

   <para>
     下面是<productname>PostgreSQL</productname> 9.2 与上一个主版本之间变更的详细说明。
    </para>

   <sect3>
    <title>服务器</title>

    <sect4>
     <title>性能</title>

     <itemizedlist>
      <listitem>
       <para>
      允许查询只从索引检索数据，避免堆访问（Robert Haas、Ibrar Ahmed、Heikki Linnakangas、Tom Lane）
     </para>

       <para>
      此特性常称为<firstterm>仅索引扫描</firstterm>。对于只包含对所有会话可见的元组的堆页（由可见性映射报告），可以跳过堆访问；因此该收益主要适用于基本静态的数据。作为实现此特性的必要部分，可见性映射被做成了崩溃安全的。
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <link linkend="SPGiST">SP-GiST</link>（空间分区 GiST）索引访问方法（Teodor Sigaev、Oleg Bartunov、Tom Lane）
     </para>

       <para>
      SP-GiST 在灵活性上与 GiST 相当，但支持非平衡的分区搜索结构而不是平衡树。对于合适的问题，SP-GiST 在索引构建时间和搜索时间上都可以比 GiST 更快。
     </para>
      </listitem>

      <listitem>
       <para>
      允许组提交在高负载下有效工作（Peter Geoghegan、Simon Riggs、Heikki Linnakangas）
     </para>

       <para>
      此前，随着写负载增加，由于内部锁竞争，提交批处理变得无效。
     </para>
      </listitem>

      <listitem>
       <para>
      允许无竞争锁使用新的快速路径锁机制管理（Robert Haas）
     </para>
      </listitem>

      <listitem>
       <para>
      降低创建虚拟事务 ID 锁的开销（Robert Haas）
     </para>
      </listitem>

      <listitem>
       <para>
      降低可序列化隔离级别锁的开销（Dan Ports）
     </para>
      </listitem>

      <listitem>
       <para>
      改进 PowerPC 和 Itanium 上的自旋锁性能（Manabu Ori、Robert Haas、Tom Lane）
     </para>
      </listitem>

      <listitem>
       <para>
      降低共享失效缓存消息的开销（Robert Haas）
     </para>
      </listitem>

      <listitem>
       <para>
      把 <structname>PGPROC</structname> 共享内存数组中频繁访问的成员移到单独的数组中（Pavan Deolasee、Heikki Linnakangas、Robert Haas）
     </para>
      </listitem>

      <listitem>
       <para>
      通过批量向堆添加元组来改进 <command>COPY</command> 性能（Heikki Linnakangas）
     </para>
      </listitem>

      <listitem>
       <para>
      通过以更少的内存分配开销生成更好的树，改进几何数据类型的 GiST 索引性能（Alexander Korotkov）
     </para>
      </listitem>

      <listitem>
       <para>
      改进 GiST 索引构建时间（Alexander Korotkov、Heikki Linnakangas）
     </para>
      </listitem>

      <listitem>
       <para>
      允许临时表和不记录日志的表更早设置提示位（Robert Haas）
     </para>
      </listitem>

      <listitem>
       <para>
      允许排序由内联的、不可从 <acronym>SQL</acronym> 调用的比较函数执行（Peter Geoghegan、Robert Haas、Tom Lane）
     </para>
      </listitem>

      <listitem>
       <para>
      使 CLOG 缓冲区数量基于 <link linkend="guc-shared-buffers"><varname>shared_buffers</varname></link> 缩放（Robert Haas、Simon Riggs、Tom Lane）
     </para>
      </listitem>

      <listitem>
       <para>
      改进删除表或数据库时发生的缓冲池扫描的性能（Jeff Janes、Simon Riggs）
     </para>
      </listitem>

      <listitem>
       <para>
      改进删除或截断大量表时检查点进程 fsync 请求队列的性能（Tom Lane）
     </para>
      </listitem>

      <listitem>
       <para>
      在 Windows 上把安全的文件描述符数量传递给子进程（Heikki Linnakangas）
     </para>

       <para>
      这允许 Windows 会话使用比以前更多的打开文件描述符。
     </para>
      </listitem>

     </itemizedlist>

    </sect4>

    <sect4>
     <title>进程管理</title>

     <itemizedlist>

      <listitem>
       <para>
      创建专门的后台进程来执行检查点（Simon Riggs）
     </para>

       <para>
      此前后台写入进程既写脏页又做检查点。把这两项工作分到两个进程使每个目标都能更可预期地完成。
     </para>
      </listitem>

      <listitem>
       <para>
      通过更早唤醒 walwriter 来改进异步提交行为（Simon Riggs）
     </para>

       <para>
      此前只有 <link linkend="guc-wal-writer-delay"><varname>wal_writer_delay</varname></link> 触发 <acronym>WAL</acronym> 刷盘；现在填满 <acronym>WAL</acronym> 缓冲区也会触发 <acronym>WAL</acronym> 写入。
     </para>
      </listitem>

      <listitem>
       <para>
      允许 bgwriter、walwriter、checkpointer、统计收集器、日志收集器和归档器等后台进程在空闲期间更高效地休眠（Peter Geoghegan、Tom Lane）
     </para>

       <para>
      这一系列变更降低了无事可做时进程唤醒的频率，大幅降低了空闲服务器的功耗。
     </para>
      </listitem>

     </itemizedlist>

    </sect4>

    <sect4>
     <title>优化器</title>

     <itemizedlist>

      <listitem>
       <para>
      允许规划器即使在使用预备语句时，也为特定参数值生成定制计划（Tom Lane）
     </para>

       <para>
      过去，预备语句总是只有一个用于所有参数值的<quote>通用</quote>计划，它常常远不如用于包含显式常量值的非预备语句的计划。现在，规划器会尝试为特定参数值生成定制计划。只有当定制计划反复被证明没有收益时，才会使用通用计划。此变更应能消除此前使用预备语句（包括 PL/pgSQL 中的非动态语句）所见的性能损失。
     </para>
      </listitem>

      <listitem>
       <para>
      改进规划器使用内层索引扫描的嵌套循环的能力（Tom Lane）
     </para>

       <para>
      新的<quote>参数化路径</quote>机制允许内层索引扫描使用来自比扫描高多个连接层级的关系的值。在语义限制（如外连接）限定允许连接顺序的情况下，这可以大幅提升性能。
     </para>
      </listitem>

      <listitem>
       <para>
      改进外部数据包装器的规划 <acronym>API</acronym>（Etsuro Fujita、Shigeru Hanada、Tom Lane）
     </para>

       <para>
      包装器现在可以为其表提供多个访问<quote>路径</quote>，使连接规划更加灵活。
     </para>
      </listitem>

      <listitem>
       <para>
      识别非表关系的自相矛盾限定子句（Tom Lane）
     </para>

       <para>
      只有当 <link linkend="guc-constraint-exclusion"><varname>constraint_exclusion</varname></link> 为 <literal>on</literal> 时才执行此检查。
     </para>
      </listitem>

      <listitem>
       <para>
      允许 <literal>indexed_col op ANY(ARRAY[...])</literal> 条件用于普通索引扫描和仅索引扫描（Tom Lane）
     </para>

       <para>
      此前这类条件只能用于位图索引扫描。
     </para>
      </listitem>

      <listitem>
       <para>
      支持在 <type>boolean</type> 列上使用 <function>MIN</function>/<function>MAX</function> 索引优化（Marti Raudsepp）
     </para>
      </listitem>

      <listitem>
       <para>
      在设置行数估计时，把 <command>SELECT</command> 目标列表中的集合返回函数考虑在内（Tom Lane）
     </para>
      </listitem>

      <listitem>
       <para>
      修复规划器，使其更可靠地处理含重复列的索引（Tom Lane）
     </para>
      </listitem>

      <listitem>
       <para>
      收集并使用数组的元素频率统计（Alexander Korotkov、Tom Lane）
     </para>

       <para>
      此变更改进了数组 <literal>&lt;@</literal>、<literal>&amp;&amp;</literal> 和 <literal>@&gt;</literal> 操作符（数组包含和重叠）的选择率估计。
     </para>
      </listitem>

      <listitem>
       <para>
      允许为外部表收集统计信息（Etsuro Fujita）
     </para>
      </listitem>

      <listitem>
       <para>
      改进使用部分索引的代价估计（Tom Lane）
     </para>
      </listitem>

      <listitem>
       <para>
      改进规划器对子查询中引用列使用统计信息的能力（Tom Lane）
     </para>
      </listitem>

      <listitem>
       <para>
      改进对使用 <literal>DISTINCT</literal> 的子查询的统计估计（Tom Lane）
     </para>
      </listitem>

     </itemizedlist>

    </sect4>

    <sect4>
     <title>认证</title>

     <itemizedlist>

      <listitem>
       <para>
      不再把 <link linkend="auth-pg-hba-conf"><filename>pg_hba.conf</filename></link> 中指定的角色名和 <literal>samerole</literal> 视为自动包含超级用户（Andrew Dunstan）
     </para>

       <para>
      这使得把 <literal>reject</literal> 行用于组角色更加容易。
     </para>
      </listitem>

      <listitem>
       <para>
      调整 <filename>pg_hba.conf</filename> 处理，使记号解析更一致（Brendan Jurd、&Aacute;lvaro Herrera）
     </para>
      </listitem>

      <listitem>
       <para>
      不允许空的 <filename>pg_hba.conf</filename> 文件（Tom Lane）
     </para>

       <para>
      这样做是为了更快地检测配置错误。
     </para>
      </listitem>

      <listitem>
       <para>
      让超级用户权限隐含复制权限（Noah Misch）
     </para>

       <para>
      这避免了显式分配此类权限的需要。
     </para>
      </listitem>

     </itemizedlist>

    </sect4>

    <sect4>
     <title>监控</title>

     <itemizedlist>

      <listitem>
       <para>
      在后端崩溃时尝试记录当前查询串（Marti Raudsepp）
     </para>
      </listitem>

      <listitem>
       <para>
      让自动清理 I/O 活动的日志更详细（Greg Smith、Noah Misch）
     </para>

       <para>
      此日志由 <link linkend="guc-log-autovacuum-min-duration"><varname>log_autovacuum_min_duration</varname></link> 触发。
     </para>
      </listitem>

      <listitem>
       <para>
      让 <acronym>WAL</acronym> 重放更早报告失败（Fujii Masao）
     </para>

       <para>
      有些情况下，失败直到服务器进入主库模式才被报告。
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <link linkend="functions-admin-backup"><function>pg_xlog_location_diff()</function></link>，简化 WAL 位置比较（Euler Taveira de Oliveira）
     </para>

       <para>
      这对于计算复制延迟很有用。
     </para>
      </listitem>

      <listitem>
       <para>
      支持在 Windows 上配置事件日志应用名（MauMau、Magnus Hagander）
     </para>

       <para>
      这允许不同实例通过设置 <link linkend="guc-event-source"><varname>event_source</varname></link> 服务器参数以不同标识符使用事件日志，其工作方式类似于 <link linkend="guc-syslog-ident"><varname>syslog_ident</varname></link>。
     </para>
      </listitem>

      <listitem>
       <para>
      把<quote>unexpected EOF</quote>消息改为 <literal>DEBUG1</literal> 级别，存在未结束事务的情况除外（Magnus Hagander）
     </para>

       <para>
      此变更减少了以不优雅方式关闭数据库连接的应用所造成的日志噪音。
     </para>
      </listitem>

     </itemizedlist>

    </sect4>

    <sect4>
     <title>统计视图</title>

     <itemizedlist>

      <listitem>
       <para>
      在 <link linkend="pg-stat-database-view"><structname>pg_stat_database</structname></link> 系统视图中跟踪临时文件大小和文件数量（Tomas Vondra）
     </para>
      </listitem>

      <listitem>
       <para>
      为 <structname>pg_stat_database</structname> 系统视图新增死锁计数器（Magnus Hagander）
     </para>
      </listitem>

      <listitem>
       <para>
      新增服务器参数 <link linkend="guc-track-io-timing"><varname>track_io_timing</varname></link>，用于跟踪 I/O 计时（Ants Aasma、Robert Haas）
     </para>
      </listitem>

      <listitem>
       <para>
      在 <link linkend="pg-stat-bgwriter-view"><structname>pg_stat_bgwriter</structname></link> 中报告检查点计时信息（Greg Smith、Peter Geoghegan）
     </para>
      </listitem>

     </itemizedlist>

    </sect4>

    <sect4>
     <title>服务器设置</title>

     <itemizedlist>

      <listitem>
       <para>
      静默忽略 <link linkend="guc-search-path"><varname>search_path</varname></link> 中指定但不存在的模式（Tom Lane）
     </para>

       <para>
      这使得使用通用路径设置更方便，这类设置可能包含并非在所有数据库中都存在的模式。
     </para>
      </listitem>

      <listitem>
       <para>
      允许超级用户按会话设置 <link linkend="guc-deadlock-timeout"><varname>deadlock_timeout</varname></link>，而不仅是按集簇（Noah Misch）
     </para>

       <para>
      这允许对很可能卷入死锁的事务降低 <varname>deadlock_timeout</varname>，从而更快检测到失败。或者，提高该值可以降低会话因死锁而被选中取消的机会。
     </para>
      </listitem>

      <listitem>
       <para>
      新增服务器参数 <link linkend="guc-temp-file-limit"><varname>temp_file_limit</varname></link>，用于限制每会话的临时文件空间使用（Mark Kirkwood）
     </para>
      </listitem>

      <listitem>
       <para>
      允许超级用户在加载相关扩展之前 <command>SET</command> 该扩展的仅超级用户自定义变量（Tom Lane）
     </para>

       <para>
      系统现在会记住 <command>SET</command> 是否由超级用户执行，以便在加载扩展时进行正确的权限检查。
     </para>
      </listitem>

      <listitem>
       <para>
      为 <link linkend="app-postmaster">postmaster</link> 新增 <option>-C</option> 选项，用于查询配置参数（Bruce Momjian）
     </para>

       <para>
      这使 <application>pg_ctl</application> 能更好地处理 <envar>PGDATA</envar> 或 <option>-D</option> 指向仅配置目录的情况。
     </para>
      </listitem>

      <listitem>
       <para>
      在 <command>CREATE DATABASE</command> 中用隐含值替换空的区域名（Tom Lane）
     </para>

       <para>
      这防止了 <structname>pg_database</structname>.<structfield>datcollate</structfield> 或 <structfield>datctype</structfield> 在服务器重启后被不同解释的情况。
     </para>
      </listitem>

     </itemizedlist>

     <sect5>
      <title><filename>postgresql.conf</filename></title>

      <itemizedlist>

      <listitem>
       <para>
      允许报告 <filename>postgresql.conf</filename> 中的多个错误，而不是只有第一个（Alexey Klyukin、Tom Lane）
     </para>
      </listitem>

      <listitem>
       <para>
      允许所有会话处理 <filename>postgresql.conf</filename> 的重新加载，即使某些设置对特定会话无效（Alexey Klyukin）
     </para>

       <para>
      此前，这类会话内无效的值会导致该会话忽略所有设置变更。
     </para>
      </listitem>

      <listitem>
       <para>
      为配置文件新增 <literal>include_if_exists</literal> 机制（Greg Smith）
     </para>

       <para>
      其工作方式与 <literal>include</literal> 相同，只是文件缺失时不抛出错误。
     </para>
      </listitem>

      <listitem>
       <para>
      在 <application>initdb</application> 期间确定服务器时区，并相应地设置 <filename>postgresql.conf</filename> 中的 <link linkend="guc-timezone"><varname>timezone</varname></link> 和 <link linkend="guc-log-timezone"><varname>log_timezone</varname></link> 条目（Tom Lane）
     </para>

       <para>
      这避免了服务器启动期间昂贵的时区探测。
     </para>
      </listitem>

      <listitem>
       <para>
      修复 <link linkend="view-pg-settings"><structname>pg_settings</structname></link>，使其在 Windows 上报告 <filename>postgresql.conf</filename> 行号（Tom Lane）
     </para>
      </listitem>

      </itemizedlist>

     </sect5>

    </sect4>

   </sect3>

   <sect3>
    <title>复制和恢复</title>

     <itemizedlist>

      <listitem>
       <para>
      允许流复制从库向其他从库转发数据（<link linkend="cascading-replication"><firstterm>级联复制</firstterm></link>）（Fujii Masao）
     </para>

       <para>
      此前，只有主服务器能向备库服务器提供流复制日志文件。
     </para>
      </listitem>

      <listitem>
       <para>
      为 <link linkend="guc-synchronous-commit"><varname>synchronous_commit</varname></link> 新增模式 <literal>remote_write</literal>（Fujii Masao、Simon Riggs）
     </para>

       <para>
      此模式等待备库服务器把事务数据写入其自身的操作系统，但不等待数据被刷到备库的磁盘。
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <link linkend="app-pgreceivexlog"><application>pg_receivexlog</application></link> 工具，在 WAL 文件变更写入时就归档，而不是等待 WAL 文件完成（Magnus Hagander）
     </para>
      </listitem>

      <listitem>
       <para>
      允许 <link linkend="app-pgbasebackup"><application>pg_basebackup</application></link> 从备库服务器做基础备份（Jun Ishizuka、Fujii Masao）
     </para>

       <para>
      此特性允许把制作新基础备份的工作从主库服务器上卸载。
     </para>
      </listitem>

      <listitem>
       <para>
      允许在 <application>pg_basebackup</application> 执行备份期间流式传输 WAL 文件（Magnus Hagander）
     </para>

       <para>
      这允许在 WAL 文件在主库上被丢弃之前传给备库。
     </para>
      </listitem>

     </itemizedlist>

   </sect3>

   <sect3>
    <title>查询</title>

     <itemizedlist>

      <listitem>
       <para>
      当客户端断开连接时取消正在运行的查询（Florian Pflug）
     </para>

       <para>
      如果后端在查询期间检测到客户端连接丢失，现在会取消该查询，而不是试图完成它。
     </para>
      </listitem>

      <listitem>
       <para>
      在运行时为行表达式保留列名（Andrew Dunstan、Tom Lane）
     </para>

       <para>
      此变更允许在行值转换为 <type>hstore</type> 或 <type>json</type> 类型时获得更好的结果：结果值的字段现在会具有预期的名称。
     </para>
      </listitem>

      <listitem>
       <para>
      改进子 <command>SELECT</command> 结果所用的列标签（Marti Raudsepp）
     </para>

       <para>
      此前使用的是通用标签 <literal>?column?</literal>。
     </para>
      </listitem>

      <listitem>
       <para>
      改进确定未知值类型的启发式规则（Tom Lane）
     </para>

       <para>
      "未知常量可以与使用它的操作符另一侧的值具有相同类型"这一长期规则，现在在考虑多态操作符时也会应用，而不仅用于简单的操作符匹配。
     </para>
      </listitem>

      <listitem>
       <para>
      对创建到域类型或从域类型出发的转换发出警告（Robert Haas）
     </para>

       <para>
      这类转换没有任何效果。
     </para>
      </listitem>

      <listitem>
       <para>
      当行违反 <literal>CHECK</literal> 或 <literal>NOT NULL</literal> 约束时，把行的内容显示为错误细节（Jan Kundr&aacute;t）
     </para>

       <para>
      当插入或更新正在处理许多行时，这应能更容易识别哪一行有问题。
     </para>
      </listitem>

     </itemizedlist>

   </sect3>

   <sect3>
    <title>对象操作</title>

     <itemizedlist>

      <listitem>
       <para>
      提供并发 <acronym>DDL</acronym> 期间更可靠的操作（Robert Haas、Noah Misch）
     </para>

       <para>
      此变更增加了锁定，应能消除许多场景下的 <quote>cache lookup failed</quote> 错误。此外，也不再可能向正被并发删除的模式添加关系——这种场景以前会导致系统目录内容不一致。
     </para>
      </listitem>

      <listitem>
       <para>
      为 <link linkend="SQL-DROPINDEX"><command>DROP INDEX</command></link> 新增 <literal>CONCURRENTLY</literal> 选项（Simon Riggs）
     </para>

       <para>
      这允许在不阻塞其他会话的情况下移除索引。
     </para>
      </listitem>

      <listitem>
       <para>
      允许外部数据包装器具有逐列选项（Shigeru Hanada）
     </para>
      </listitem>

      <listitem>
       <para>
      改进视图定义的漂亮打印（Andrew Dunstan）
     </para>
      </listitem>

     </itemizedlist>

    <sect4>
     <title>约束</title>

     <itemizedlist>

      <listitem>
       <para>
      允许 <link linkend="ddl-constraints"><literal>CHECK</literal></link> 约束声明为 <literal>NOT VALID</literal>（&Aacute;lvaro Herrera）
     </para>

       <para>
      添加 <literal>NOT VALID</literal> 约束不会导致扫描表以验证现有行满足约束。之后，新添加或更新的行会被检查。规划器在考虑 <varname>constraint_exclusion</varname> 时会忽略这类约束，因为不能确定所有行都满足约束。
     </para>

       <para>
      新的 <command>ALTER TABLE VALIDATE</command> 命令允许对现有行检查 <literal>NOT VALID</literal> 约束，之后它们被转换为普通约束。
     </para>
      </listitem>

      <listitem>
       <para>
      允许 <literal>CHECK</literal> 约束声明为 <literal>NO INHERIT</literal>（Nikhil Sontakke、Alex Hunsaker、&Aacute;lvaro Herrera）
     </para>

       <para>
      这使它们只在父表上强制执行，而不在子表上。
     </para>
      </listitem>

      <listitem>
       <para>
      新增<link linkend="SQL-ALTERTABLE">重命名</link>约束的能力（Peter Eisentraut）
     </para>
      </listitem>
     </itemizedlist>

    </sect4>

    <sect4>
     <title><command>ALTER</></title>

     <itemizedlist>

      <listitem>
       <para>
      减少某些 <link linkend="SQL-ALTERTABLE"><command>ALTER TABLE</command></link> ... <literal>ALTER COLUMN TYPE</literal> 操作重建表和索引的需要（Noah Misch）
     </para>

       <para>
      增大 <type>varchar</type> 或 <type>varbit</type> 列的长度限制，或者完全移除限制，不再需要重写表。类似地，增大 <type>numeric</type> 列允许的精度，或者把列从受约束的 <type>numeric</type> 改为不受约束的 <type>numeric</type>，也不再需要重写表。在涉及 <type>interval</type>、<type>timestamp</type> 和 <type>timestamptz</type> 类型的类似情况下，也可以避免重写表。
     </para>
      </listitem>

      <listitem>
       <para>
      避免 <link linkend="SQL-ALTERTABLE"><command>ALTER TABLE</command></link> 在某些不必要的情况下重新验证外键约束（Noah Misch）
     </para>
      </listitem>

      <listitem>
       <para>
      为一些 <command>ALTER</command> 命令新增 <literal>IF EXISTS</literal> 选项（Pavel Stehule）
     </para>

       <para>
      例如 <command>ALTER FOREIGN TABLE IF EXISTS foo RENAME TO bar</command>。
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <link linkend="SQL-ALTERFOREIGNDATAWRAPPER"><command>ALTER FOREIGN DATA WRAPPER</command></link> ... <literal>RENAME</literal> 和 <link linkend="SQL-ALTERSERVER"><command>ALTER SERVER</command></link> ... <literal>RENAME</literal>（Peter Eisentraut）
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <link linkend="SQL-ALTERDOMAIN"><command>ALTER DOMAIN</command></link> ... <literal>RENAME</literal>（Peter Eisentraut）
     </para>

       <para>
      此前已可以用 <command>ALTER TYPE</command> 重命名域。
     </para>
      </listitem>

      <listitem>
       <para>
      让 <command>ALTER DOMAIN</command> ... <literal>DROP CONSTRAINT</literal> 在约束不存在时抛出错误（Peter Eisentraut）
     </para>

       <para>
      已新增 <literal>IF EXISTS</literal> 选项来提供先前的行为。
     </para>
      </listitem>

     </itemizedlist>

    </sect4>

    <sect4>
     <title><link linkend="SQL-CREATETABLE"><command>CREATE TABLE</></link></title>

     <itemizedlist>

      <listitem>
       <para>
      允许从外部表、视图和组合类型执行 <command>CREATE TABLE (LIKE ...)</command>（Peter Eisentraut）
     </para>

       <para>
      例如，这允许创建一个模式与某个视图匹配的表。
     </para>
      </listitem>

      <listitem>
       <para>
      修复 <command>CREATE TABLE (LIKE ...)</command>，避免复制索引注释时的索引名冲突（Tom Lane）
     </para>
      </listitem>

      <listitem>
       <para>
      修复 <command>CREATE TABLE</command> ... <literal>AS EXECUTE</literal>，使其处理 <literal>WITH NO DATA</literal> 和列名指定（Tom Lane）
     </para>
      </listitem>

     </itemizedlist>

    </sect4>

    <sect4>
     <title>对象权限</title>

     <itemizedlist>

      <listitem>
       <para>
      为视图新增 <link linkend="SQL-CREATEVIEW"><literal>security_barrier</literal></link> 选项（KaiGai Kohei、Robert Haas）
     </para>

       <para>
      此选项防止可能向用户暴露视图保护数据的优化，例如把涉及不安全函数的子句下推到视图的 <literal>WHERE</literal> 子句。这类视图的性能预计会比普通视图差。
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <link linkend="SQL-CREATEFUNCTION"><literal>LEAKPROOF</literal></link> 函数属性，用于标记可以安全下推到 <literal>security_barrier</literal> 视图的函数（KaiGai Kohei）
     </para>
      </listitem>

      <listitem>
       <para>
      新增对数据类型权限的支持（Peter Eisentraut）
     </para>

       <para>
      这新增了对类型和域上符合 <acronym>SQL</acronym> 标准的 <literal>USAGE</literal> 权限的支持。其意图是能够限制哪些用户可以在类型上创建依赖，因为这类依赖会限制所有者更改类型的能力。
     </para>
      </listitem>

      <listitem>
       <para>
      在 <command>SELECT INTO</command> / <command>CREATE TABLE AS</command> 中检查 <command>INSERT</command> 权限（KaiGai Kohei）
     </para>

       <para>
      由于对象是由 <command>SELECT INTO</command> 或 <command>CREATE TABLE AS</command> 创建的，创建者通常应拥有插入权限；但存在一些例外情况，例如 <literal>ALTER DEFAULT PRIVILEGES</literal> 移除了这类权限时。
     </para>
      </listitem>

     </itemizedlist>

    </sect4>

   </sect3>

   <sect3>
    <title>工具操作</title>

    <itemizedlist>

      <listitem>
       <para>
      允许 <link linkend="SQL-VACUUM"><command>VACUUM</command></link> 更容易地跳过无法锁定的页面（Simon Riggs、Robert Haas）
     </para>

       <para>
      此变更应能大幅减少 <command>VACUUM</command> 因等待其他会话而<quote>卡住</quote>的情况。
     </para>
      </listitem>

      <listitem>
       <para>
      让 <link linkend="SQL-EXPLAIN"><command>EXPLAIN</command></link> <literal>(BUFFERS)</literal> 统计被弄脏和写出的块（Robert Haas）
     </para>
      </listitem>

      <listitem>
       <para>
      让 <command>EXPLAIN ANALYZE</command> 报告被过滤步骤拒绝的行数（Marko Tiikkaja）
     </para>
      </listitem>

      <listitem>
       <para>
      允许 <command>EXPLAIN ANALYZE</command> 在不需要时间值时避免计时开销（Tomas Vondra）
     </para>

       <para>
      这通过把新的 <literal>TIMING</literal> 选项设为 <literal>FALSE</literal> 实现。
     </para>
      </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title>数据类型</title>

    <itemizedlist>

      <listitem>
       <para>
      新增对<link linkend="rangetypes">范围数据类型</link>的支持（Jeff Davis、Tom Lane、Alexander Korotkov）
     </para>

       <para>
      范围数据类型存储属于其基础类型的下界和上界。它支持包含、重叠和交集等操作。
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <link linkend="datatype-json"><type>JSON</type></link> 数据类型（Robert Haas）
     </para>

       <para>
      该类型存储经过正确验证的 <acronym>JSON</acronym>（JavaScript 对象表示法）数据。
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <link linkend="functions-json"><function>array_to_json()</function></link> 和 <function>row_to_json()</function>（Andrew Dunstan）
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <link linkend="datatype-serial"><type>SMALLSERIAL</type></link> 数据类型（Mike Pultz）
     </para>

       <para>
      它类似于 <type>SERIAL</type>，只是把序列存储在两字节整数列（<type>int2</type>）中。
     </para>
      </listitem>

      <listitem>
       <para>
      允许<link linkend="SQL-CREATEDOMAIN">域</link>声明为 <literal>NOT VALID</literal>（&Aacute;lvaro Herrera）
     </para>

       <para>
      此选项可以在创建域时设置，或者通过 <command>ALTER DOMAIN</command> ... <literal>ADD CONSTRAINT</literal> ... <literal>NOT VALID</literal> 设置。<command>ALTER DOMAIN</command> ... <literal>VALIDATE CONSTRAINT</literal> 会完全验证约束。
     </para>
      </listitem>

      <listitem>
       <para>
      为 <link linkend="datatype-money"><type>money</type></link> 数据类型支持更多区域特定的格式化选项（Tom Lane）
     </para>

       <para>
      具体而言，遵循 POSIX 中关于货币输出中值、符号和货币符号顺序的所有选项。此外，确保千位分隔符只插入在小数点左侧，这是 POSIX 的要求。
     </para>
      </listitem>

      <listitem>
       <para>
      为 <type>macaddr</type> 数据类型新增按位<quote>与</quote>、<quote>或</quote>和<quote>非</quote>操作符（Brendan Jurd）
     </para>
      </listitem>

      <listitem>
       <para>
      允许 <link linkend="functions-xml-processing"><function>xpath()</function></link> 在给定标量值时返回单元素 <acronym>XML</acronym> 数组（Florian Pflug）
     </para>

       <para>
      此前它返回空数组。此变更还会使 <function>xpath_exists()</function> 对这类表达式返回真而不是假。
     </para>
      </listitem>

      <listitem>
       <para>
      改进 <acronym>XML</acronym> 错误处理的健壮性（Florian Pflug）
     </para>
      </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title>函数</title>

     <itemizedlist>

      <listitem>
       <para>
      允许非超级用户对属于同一用户的其他会话使用 <link linkend="functions-admin-signal"><function>pg_cancel_backend()</function></link> 和 <link linkend="functions-admin-signal"><function>pg_terminate_backend()</function></link>（Magnus Hagander、Josh Kupershmidt、Dan Farina）
     </para>

       <para>
      此前只有超级用户可以使用这些函数。
     </para>
      </listitem>

      <listitem>
       <para>
      允许导入和导出事务快照（Joachim Wieland、Tom Lane）
     </para>

       <para>
      这允许多个事务共享相同的数据库状态视图。快照通过 <link linkend="functions-snapshot-synchronization"><function>pg_export_snapshot()</function></link> 导出，并通过 <link linkend="SQL-SET-TRANSACTION"><command>SET TRANSACTION SNAPSHOT</command></link> 导入。只能导入来自当前正在运行的事务的快照。
     </para>
      </listitem>

      <listitem>
       <para>
      支持对表达式使用 <link linkend="functions-info-catalog-table"><literal>COLLATION FOR</literal></link>（Peter Eisentraut）
     </para>

       <para>
      它返回一个表示该表达式排序规则的字符串。
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <link linkend="functions-info-schema-table"><function>pg_opfamily_is_visible()</function></link>（Josh Kupershmidt）
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <link linkend="functions-admin-dbsize"><function>pg_size_pretty()</function></link> 的 <type>numeric</type> 变体，供 <function>pg_xlog_location_diff()</function> 使用（Fujii Masao）
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <link linkend="functions-info-session-table"><function>pg_trigger_depth()</function></link> 函数（Kevin Grittner）
     </para>

       <para>
      它报告当前触发器调用深度。
     </para>
      </listitem>

      <listitem>
       <para>
      允许 <link linkend="functions-aggregate-table"><function>string_agg()</function></link> 处理 <type>bytea</type> 值（Pavel Stehule）
     </para>
      </listitem>

      <listitem>
       <para>
      修复反向引用出现在更大的带量词子表达式中的正则表达式（Tom Lane）
     </para>

       <para>
      例如 <literal>^(\w+)( \1)+$</literal>。以前的版本不检查反向引用是否真的与第一次出现匹配。
     </para>
      </listitem>

     </itemizedlist>

   </sect3>

   <sect3>
    <title><link linkend="information-schema">Information Schema</link></title>

     <itemizedlist>

      <listitem>
       <para>
      新增信息模式视图 <structname>role_udt_grants</structname>、<structname>udt_privileges</structname> 和 <structname>user_defined_types</structname>（Peter Eisentraut）
     </para>
      </listitem>

      <listitem>
       <para>
      为信息模式 <structname>element_types</structname> 视图新增组合类型属性（Peter Eisentraut）
     </para>
      </listitem>

      <listitem>
       <para>
      在信息模式中实现 <structfield>interval_type</structfield> 列（Peter Eisentraut）
     </para>

       <para>
      此前这些列读取为空值。
     </para>
      </listitem>

      <listitem>
       <para>
      在信息模式 <structname>attributes</structname>、<structname>columns</structname>、<structname>domains</structname> 和 <structname>element_types</structname> 视图中实现排序规则相关列（Peter Eisentraut）
     </para>
      </listitem>

      <listitem>
       <para>
      在信息模式 <structname>table_privileges</structname> 视图中实现 <structfield>with_hierarchy</structfield> 列（Peter Eisentraut）
     </para>
      </listitem>

      <listitem>
       <para>
      在信息模式中新增序列 <literal>USAGE</literal> 权限的显示（Peter Eisentraut）
     </para>
      </listitem>

      <listitem>
       <para>
      让信息模式显示默认权限（Peter Eisentraut）
     </para>

       <para>
      此前，非空的默认权限没有在视图中表示。
     </para>
      </listitem>

     </itemizedlist>

   </sect3>

   <sect3>
    <title>服务器端语言</title>

    <sect4>
     <title><link linkend="plpgsql">PL/pgSQL</link> Server-Side Language</title>

     <itemizedlist>

      <listitem>
       <para>
      允许 PL/pgSQL 的 <command>OPEN</command> 游标命令按名称提供参数（Yeb Havinga）
     </para>
      </listitem>

      <listitem>
       <para>
      新增 PL/pgSQL 命令 <command>GET STACKED DIAGNOSTICS</command>，用于获取异常信息（Pavel Stehule）
     </para>
      </listitem>

      <listitem>
       <para>
      通过缓存类型信息加速 PL/pgSQL 数组赋值（Pavel Stehule）
     </para>
      </listitem>

      <listitem>
       <para>
      改进长链 <literal>ELSIF</literal> 子句的性能和内存消耗（Tom Lane）
     </para>
      </listitem>

      <listitem>
       <para>
      让 PL/pgSQL 错误消息输出函数签名而不仅是名称（Pavel Stehule）
     </para>
      </listitem>

    </itemizedlist>

    </sect4>

    <sect4>
     <title><link linkend="plpython">PL/Python</link> Server-Side Language</title>

     <itemizedlist>

      <listitem>
       <para>
      新增 PL/Python 的 <acronym>SPI</acronym> 游标支持（Jan Urbanski）
     </para>

       <para>
      这允许 PL/Python 读取部分结果集。
     </para>
      </listitem>

      <listitem>
       <para>
      为 PL/Python 新增结果元数据函数（Peter Eisentraut）
     </para>

       <para>
      具体而言，这新增了结果对象函数 <literal>.colnames</literal>、<literal>.coltypes</literal> 和 <literal>.coltypmods</literal>。
     </para>
      </listitem>

      <listitem>
       <para>
      移除对 Python 2.2 的支持（Peter Eisentraut）
     </para>
      </listitem>

     </itemizedlist>

    </sect4>

    <sect4>
     <title><link linkend="xfunc-sql">SQL</link> Server-Side Language</title>

     <itemizedlist>
      <listitem>
       <para>
      允许 <acronym>SQL</acronym> 语言函数按名称引用参数（Matthew Draper）
     </para>

       <para>
      要使用此特性，只需为函数参数命名，然后在 <acronym>SQL</acronym> 函数体中引用参数名。
     </para>
      </listitem>

     </itemizedlist>
    </sect4>

   </sect3>

   <sect3>
    <title>客户端应用程序</title>

    <itemizedlist>

      <listitem>
       <para>
      为 <link linkend="APP-INITDB"><application>initdb</application></link> 新增选项 <option>--auth-local</option> 和 <option>--auth-host</option>（Peter Eisentraut）
     </para>

       <para>
      这允许分别控制 <filename>pg_hba.conf</filename> 中 <literal>local</literal> 和 <literal>host</literal> 的认证设置。<option>--auth</option> 仍然同时控制两者。
     </para>
      </listitem>

      <listitem>
       <para>
      为 <link linkend="APP-CREATEUSER"><application>createuser</application></link> 新增 <option>--replication</option>/<option>--no-replication</option> 标志，用于控制复制权限（Fujii Masao）
     </para>
      </listitem>

      <listitem>
       <para>
      为 <link linkend="APP-DROPDB"><application>dropdb</application></link> 和 <link linkend="APP-DROPUSER"><application>dropuser</application></link> 新增 <option>--if-exists</option> 选项（Josh Kupershmidt）
     </para>
      </listitem>

      <listitem>
       <para>
      使命令行工具能够指定要连接的数据库名，并在连接 <literal>postgres</literal> 数据库失败时回退到 <literal>template1</literal>（Robert Haas）
     </para>
      </listitem>

    </itemizedlist>

    <sect4>
     <title><link linkend="APP-PSQL"><application>psql</></link></title>

     <itemizedlist>

      <listitem>
       <para>
      新增根据显示宽度自动扩展输出的显示模式（Peter Eisentraut）
     </para>

       <para>
      这为 <command>\x</command> 命令新增了 <literal>auto</literal> 选项，当普通输出会比屏幕更宽时切换到扩展模式。
     </para>
      </listitem>

      <listitem>
       <para>
      允许包含相对于调用文件所在目录命名的脚本文件（Gurjeet Singh）
     </para>

       <para>
      这通过新命令 <command>\ir</command> 实现。
     </para>
      </listitem>

      <listitem>
       <para>
      支持 <application>psql</application> 变量名中的非 <acronym>ASCII</acronym> 字符（Tom Lane）
     </para>
      </listitem>

      <listitem>
       <para>
      新增对特定大版本的 <filename>.psqlrc</filename> 文件的支持（Bruce Momjian）
     </para>

       <para>
      <application>psql</application> 此前已支持特定小版本的 <filename>.psqlrc</filename> 文件。
     </para>
      </listitem>

      <listitem>
       <para>
      提供用环境变量覆盖 <application>psql</application> 历史和启动文件位置的能力（Andrew Dunstan）
     </para>

       <para>
      如果设置了 <envar>PSQL_HISTORY</envar> 和 <envar>PSQLRC</envar>，现在将由它们决定这些文件名。
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <command>\setenv</command> 命令，用于修改传递给子进程的环境变量（Andrew Dunstan）
     </para>
      </listitem>

      <listitem>
       <para>
      让 <application>psql</application> 的临时编辑器文件使用 <filename>.sql</filename> 扩展名（Peter Eisentraut）
     </para>

       <para>
      这允许对扩展名敏感的编辑器选择正确的模式。
     </para>
      </listitem>

      <listitem>
       <para>
      允许 <application>psql</application> 使用零字节字段和记录分隔符（Peter Eisentraut）
     </para>

       <para>
      各种 shell 工具使用零字节（NUL）分隔符，例如 <application>find</application>。
     </para>
      </listitem>

      <listitem>
       <para>
      让 <command>\timing</command> 选项报告失败查询的耗时（Magnus Hagander）
     </para>

       <para>
      此前只对成功查询报告耗时。
     </para>
      </listitem>

      <listitem>
       <para>
      统一并收紧 <application>psql</application> 对 <command>\copy</command> 和 SQL <command>COPY</command> 的处理（Noah Misch）
     </para>

       <para>
      此修复使失败行为更可预期，并遵循 <command>\set ON_ERROR_ROLLBACK</command>。
     </para>
      </listitem>

     </itemizedlist>

    </sect4>

    <sect4>
     <title>信息命令</title>

     <itemizedlist>
      <listitem>
       <para>
      让 <command>\d</command> 在序列上显示拥有它的表/列名（Magnus Hagander）
     </para>
      </listitem>

      <listitem>
       <para>
      在 <command>\d+</command> 中显示列的统计目标（Magnus Hagander）
     </para>
      </listitem>

      <listitem>
       <para>
      在 <command>\du</command> 中显示角色口令过期日期（Fabr&iacute;zio de Royes Mello）
     </para>
      </listitem>

      <listitem>
       <para>
      显示转换、编码转换、域和语言的注释（Josh Kupershmidt）
     </para>

       <para>
      它们分别包含在 <command>\dC+</command>、<command>\dc+</command>、<command>\dD+</command> 和 <command>\dL</command> 的输出中。
     </para>
      </listitem>

      <listitem>
       <para>
      显示 <acronym>SQL</acronym>/<acronym>MED</acronym> 对象的注释（Josh Kupershmidt）
     </para>

       <para>
      它们分别包含在外部服务器、外部表和外部数据包装器的 <command>\des+</command>、<command>\det+</command> 和 <command>\dew+</command> 输出中。
     </para>
      </listitem>

      <listitem>
       <para>
      更改 <command>\dd</command>，使其只显示没有专属反斜杠命令的对象类型的注释（Josh Kupershmidt）
     </para>
      </listitem>

     </itemizedlist>

    </sect4>

    <sect4>
     <title>Tab 补全</title>

     <itemizedlist>

      <listitem>
       <para>
      在 <application>psql</application> Tab 补全中，按照新的 <link linkend="APP-PSQL-variables"><literal>COMP_KEYWORD_CASE</literal></link> 设置以大写或小写补全 <acronym>SQL</acronym> 关键字（Peter Eisentraut）
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <command>EXECUTE</command> 的 Tab 补全支持（Andreas Karlsson）
     </para>
      </listitem>

      <listitem>
       <para>
      允许 <command>GRANT</command>/<command>REVOKE</command> 中角色引用的 Tab 补全（Peter Eisentraut）
     </para>
      </listitem>

      <listitem>
       <para>
      允许文件名的 Tab 补全在必要时提供引号（Noah Misch）
     </para>
      </listitem>

      <listitem>
       <para>
      更改 <command>TABLE</command> 的 Tab 补全支持，使其也包含视图（Magnus Hagander）
     </para>
      </listitem>

     </itemizedlist>

    </sect4>

    <sect4>
     <title><link linkend="APP-PGDUMP"><application>pg_dump</></link></title>

     <itemizedlist>

      <listitem>
       <para>
      为 <application>pg_dump</application> 新增 <option>--exclude-table-data</option> 选项（Andrew Dunstan）
     </para>

       <para>
      这允许按表只转储表的定义而不转储其数据。
     </para>
      </listitem>

      <listitem>
       <para>
      为 <application>pg_dump</application> 和 <application>pg_restore</application> 新增 <option>--section</option> 选项（Andrew Dunstan）
     </para>

       <para>
      有效值为 <literal>pre-data</literal>、<literal>data</literal> 和 <literal>post-data</literal>。该选项可以给出多次以选择两个或更多节。
     </para>
      </listitem>

      <listitem>
       <para>
      让 <link linkend="APP-PG-DUMPALL"><application>pg_dumpall</application></link> 先转储所有角色，再转储角色上的所有配置设置（Phil Sorber）
     </para>

       <para>
      这允许角色的配置设置提及其他角色而不产生错误。
     </para>
      </listitem>

      <listitem>
       <para>
      允许 <application>pg_dumpall</application> 在新集簇缺少 <literal>postgres</literal> 数据库时避免错误（Robert Haas）
     </para>
      </listitem>

      <listitem>
       <para>
      按用户名顺序转储外部服务器用户映射（Peter Eisentraut）
     </para>

       <para>
      这有助于产生确定性的转储文件。
     </para>
      </listitem>

      <listitem>
       <para>
      以可预测的顺序转储操作符（Peter Eisentraut）
     </para>
      </listitem>

      <listitem>
       <para>
      收紧 <application>pg_dump</application> 转储扩展配置表的规则（Tom Lane）
     </para>
      </listitem>

      <listitem>
       <para>
      让 <application>pg_dump</application> 生成更有用的依赖信息（Tom Lane）
     </para>

       <para>
      归档格式转储中包含的依赖链接此前用处非常有限，因为它们经常引用转储中根本没有出现的对象。现在它们表示被转储对象之间的实际依赖（可能是间接的）。
     </para>
      </listitem>

      <listitem>
       <para>
      改进 <application>pg_dump</application> 转储大量数据库对象时的性能（Tom Lane）
     </para>
      </listitem>

     </itemizedlist>

    </sect4>

   </sect3>

    <sect3>
     <title><link linkend="libpq"><application>libpq</></link></title>

     <itemizedlist>

      <listitem>
       <para>
      允许 <application>libpq</application> 连接串采用 <link linkend="libpq-connstring"><acronym>URI</acronym></link> 格式（Alexander Shulgin）
     </para>

       <para>
      语法以 <literal>postgres://</literal> 开头。这允许应用避免为表示数据库连接的 URI 实现自己的解析器。
     </para>
      </listitem>

      <listitem>
       <para>
      新增用于禁用 <acronym>SSL</acronym> 压缩的<link linkend="libpq-connect-sslcompression">连接选项</link>（Laurenz Albe）
     </para>

       <para>
      这可用于消除快速网络上 <acronym>SSL</acronym> 压缩的开销。
     </para>
      </listitem>

      <listitem>
       <para>
      新增<link linkend="libpq-single-row-mode">单行处理模式</link>，以更好地处理大型结果集（Kyotaro Horiguchi、Marko Kreen）
     </para>

       <para>
      此前，<application>libpq</application> 总是先把整个查询结果收集到内存中，再传递回应用。
     </para>
      </listitem>

      <listitem>
       <para>
      为函数 <function>PQconnectdbParams</function>、<function>PQconnectStartParams</function> 和 <function>PQpingParams</function> 的声明添加 <literal>const</literal> 限定符（Lionel Elie Mamane）
     </para>
      </listitem>

      <listitem>
       <para>
      允许 <filename>.pgpass</filename> 文件的口令字段包含转义字符（Robert Haas）
     </para>
      </listitem>

      <listitem>
       <para>
      让库函数在需要终止进程时使用 <function>abort()</function> 而不是 <function>exit()</function>（Peter Eisentraut）
     </para>

       <para>
      这一选择不会干扰程序使用的正常退出码，并会生成可被调用方捕获的信号。
     </para>
      </listitem>

     </itemizedlist>

   </sect3>

   <sect3>
    <title>源代码</title>

     <itemizedlist>

      <listitem>
       <para>
      移除已死的平台（Peter Eisentraut）
     </para>

       <para>
      以下平台不再受支持：dgux、nextstep、sunos4、svr4、ultrix4、univel、bsdi。
     </para>
      </listitem>

      <listitem>
       <para>
      新增对使用 <link linkend="install-windows">MS Visual Studio 2010</link> 构建的支持（Brar Piening）
     </para>
      </listitem>

      <listitem>
       <para>
      支持使用 MinGW-w64 32 位编译器编译（Lars Kanis）
     </para>
      </listitem>

      <listitem>
       <para>
      安装时把 <filename>plpgsql.h</filename> 安装到 <filename>include/server</filename>（Heikki Linnakangas）
     </para>
      </listitem>

      <listitem>
       <para>
      改进闩锁机制，加入对 postmaster 死亡的检测（Peter Geoghegan、Heikki Linnakangas、Tom Lane）
     </para>

       <para>
      这消除了后台进程此前必须唤醒以轮询事件的主要原因之一。
     </para>
      </listitem>

      <listitem>
       <para>
      在支持的地方使用 C 弹性数组成员（Peter Eisentraut）
     </para>
      </listitem>

      <listitem>
       <para>
      改进并发事务回归测试（<application>isolationtester</application>）（Noah Misch）
     </para>
      </listitem>

      <listitem>
       <para>
      修改 <application>thread_test</application>，使其在当前目录而不是 <filename>/tmp</filename> 中创建测试文件（Bruce Momjian）
     </para>
      </listitem>

      <listitem>
       <para>
      改进 flex 和 bison 的警告与错误报告（Tom Lane）
     </para>
      </listitem>

      <listitem>
       <para>
      新增内存屏障支持（Robert Haas）
     </para>

       <para>
      目前尚未使用。
     </para>
      </listitem>

      <listitem>
       <para>
      修改 pgindent 以使用 typedef 文件（Bruce Momjian）
     </para>
      </listitem>

      <listitem>
       <para>
      新增用于处理即将发送到服务器日志的消息的钩子（Martin Pihlak）
     </para>
      </listitem>

      <listitem>
       <para>
      为 <command>DROP</command> 命令新增对象访问钩子（KaiGai Kohei）
     </para>
      </listitem>

      <listitem>
       <para>
      集中化某些对象类型的 <command>DROP</command> 处理（KaiGai Kohei）
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <application>pg_upgrade</application> 测试套件（Peter Eisentraut）
     </para>
      </listitem>

      <listitem>
       <para>
      把正则表达式代码与 <acronym>TCL</acronym> 8.5.11 同步，并改进内部处理（Tom Lane）
     </para>
      </listitem>

      <listitem>
       <para>
      把 <acronym>CRC</acronym> 表移到 libpgport，并在单独的头文件中提供（Daniel Farina）
     </para>
      </listitem>

      <listitem>
       <para>
      为 <application>git_changelog</application> 新增用于制作大版本发行说明的选项（Bruce Momjian）
     </para>
      </listitem>

      <listitem>
       <para>
      支持 Linux 的 <filename>/proc/self/oom_score_adj</filename> API（Tom Lane）
     </para>
      </listitem>

     </itemizedlist>

   </sect3>

   <sect3>
    <title>附加模块</title>

    <itemizedlist>

      <listitem>
       <para>
      通过使用 libpq 新的单行处理模式改进 <link linkend="dblink">dblink</link> 的效率（Kyotaro Horiguchi、Marko Kreen）
     </para>

       <para>
      此改进不适用于 <function>dblink_send_query()</function>/<function>dblink_get_result()</function>。
     </para>
      </listitem>

      <listitem>
       <para>
      在 <link linkend="file-fdw">file_fdw</link> 中支持 <literal>force_not_null</literal> 选项（Shigeru Hanada）
     </para>
      </listitem>

      <listitem>
       <para>
      为 <link linkend="pgarchivecleanup"><application>pg_archivecleanup</application></link> 实现空运行模式（Gabriele Bartolini）
     </para>

       <para>
      它只输出要删除文件的名称。
     </para>
      </listitem>

      <listitem>
       <para>
      为 <link linkend="pgbench">pgbench</link> 新增开关 <option>--unlogged-tables</option>、<option>--tablespace</option> 和 <option>--index-tablespace</option>（Robert Haas）
     </para>
      </listitem>

      <listitem>
       <para>
      更改 <link linkend="pgtestfsync"><application>pg_test_fsync</application></link>，使其按固定时长而不是固定周期数测试（Bruce Momjian）
     </para>

       <para>
      移除了 <option>-o</option>/cycles 选项，新增了 <option>-s</option>/seconds。
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <link linkend="pgtesttiming"><application>pg_test_timing</application></link> 工具，用于测量时钟单调性和计时开销（Ants Aasma、Greg Smith）
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <link linkend="tcn">tcn</link>（触发式变更通知）模块，在表变更时生成 <command>NOTIFY</command> 事件（Kevin Grittner）
     </para>
      </listitem>

    </itemizedlist>

    <sect4>
     <title><link linkend="pgupgrade"><application>pg_upgrade</></link></title>

     <itemizedlist>

      <listitem>
       <para>
      调整 <application>pg_upgrade</application> 环境变量（Bruce Momjian）
     </para>

       <para>
      把数据、bin 和端口环境变量重命名为以 <literal>PG</literal> 开头，并支持 <envar>PGPORTOLD</envar>/<envar>PGPORTNEW</envar> 以取代 <envar>PGPORT</envar>。
     </para>
      </listitem>

      <listitem>
       <para>
      彻底改造 <application>pg_upgrade</application> 的日志和失败报告（Bruce Momjian）
     </para>

       <para>
      创建四个只追加的日志文件，成功时删除它们。新增 <option>-r</option>/<option>--retain</option> 选项以无条件保留这些文件。同时移除 <application>pg_upgrade</application> 的 <option>-g</option>/<option>-G</option>/<option>-l</option> 选项（已无必要），并收紧日志文件权限。
     </para>
      </listitem>

      <listitem>
       <para>
      让 <application>pg_upgrade</application> 创建一个脚本，以增量方式生成更准确的优化器统计信息（Bruce Momjian）
     </para>

       <para>
      这减少了升级后生成最小集簇统计信息所需的时间。
     </para>
      </listitem>

      <listitem>
       <para>
      允许 <application>pg_upgrade</application> 升级没有 <literal>postgres</literal> 数据库的旧集簇（Bruce Momjian）
     </para>
      </listitem>

      <listitem>
       <para>
      允许 <application>pg_upgrade</application> 处理某些旧库或新库缺失的情况，只要它们是空的（Bruce Momjian）
     </para>
      </listitem>

      <listitem>
       <para>
      允许 <application>pg_upgrade</application> 处理仅配置目录的安装（Bruce Momjian）
     </para>
      </listitem>

      <listitem>
       <para>
      在 <application>pg_upgrade</application> 中新增 <option>-o</option>/<option>-O</option> 选项，用于向服务器传递参数（Bruce Momjian）
     </para>

       <para>
      这对仅配置目录的安装很有用。
     </para>
      </listitem>

      <listitem>
       <para>
      更改 <application>pg_upgrade</application>，使其默认使用端口 50432（Bruce Momjian）
     </para>

       <para>
      这有助于避免升级期间意外的客户端连接。
     </para>
      </listitem>

      <listitem>
       <para>
      减少 <application>pg_upgrade</application> 中的集簇锁定（Bruce Momjian）
     </para>

       <para>
      具体而言，只有使用链接模式时才锁定旧集簇，并在模式恢复之后立即进行。
     </para>
      </listitem>

    </itemizedlist>

    </sect4>

    <sect4>
     <title><link linkend="pgstatstatements"><application>pg_stat_statements</></link></title>

     <itemizedlist>

      <listitem>
       <para>
      允许 <application>pg_stat_statements</application> 通过 SQL 文本归一化聚合相似查询（Peter Geoghegan、Tom Lane）
     </para>

       <para>
      使用非参数化 SQL 应用的用户现在无需详细的日志分析就能监控查询性能。
     </para>
      </listitem>

      <listitem>
       <para>
      为 <application>pg_stat_statements</application> 新增脏块和写出块计数以及读写时间（Robert Haas、Ants Aasma）
     </para>
      </listitem>

      <listitem>
       <para>
      防止 <application>pg_stat_statements</application> 重复计数 <command>PREPARE</command> 和 <command>EXECUTE</command> 命令（Tom Lane）
     </para>
      </listitem>

    </itemizedlist>

    </sect4>

    <sect4>
     <title><link linkend="sepgsql">sepgsql</link></title>

     <itemizedlist>
      <listitem>
       <para>
      支持在全局对象上使用 <literal>SECURITY LABEL</literal>（KaiGai Kohei、Robert Haas）
     </para>

       <para>
      具体而言，为数据库、表空间和角色添加安全标签。
     </para>
      </listitem>

      <listitem>
       <para>
      允许 sepgsql 遵循数据库标签（KaiGai Kohei）
     </para>
      </listitem>

      <listitem>
       <para>
      在各种对象创建期间执行 sepgsql 权限检查（KaiGai Kohei）
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <function>sepgsql_setcon()</function> 及相关函数，用于控制 sepgsql 安全域（KaiGai Kohei）
     </para>
      </listitem>

      <listitem>
       <para>
      为 sepgsql 新增用户空间访问缓存以提升性能（KaiGai Kohei）
     </para>
      </listitem>

     </itemizedlist>
    </sect4>

   </sect3>

   <sect3>
    <title>文档</title>

    <itemizedlist>

      <listitem>
       <para>
      新增规则，可选择使用网站样式表构建 HTML 文档（Magnus Hagander）
     </para>

       <para>
      使用 <command>gmake STYLE=website draft</command>。
     </para>
      </listitem>

      <listitem>
       <para>
      改进 <command>EXPLAIN</command> 文档（Tom Lane）
     </para>
      </listitem>

      <listitem>
       <para>
      记录命令行工具（如 <application>vacuumdb</application>）通过双引号保留用户/数据库名（Bruce Momjian）
     </para>
      </listitem>

      <listitem>
       <para>
      记录客户端为 MD5 认证返回的实际字符串（Cyan Ogilvie）
     </para>
      </listitem>

      <listitem>
       <para>
      废弃在 <command>CREATE TEMP TABLE</command> 中使用 <literal>GLOBAL</literal> 和 <literal>LOCAL</literal>（Noah Misch）
     </para>

       <para>
      <productname>PostgreSQL</productname> 长期以来把这些关键字视为无操作，并将继续如此；但未来它们可能获得 SQL 标准所规定的含义，因此应用应避免使用它们。
     </para>
      </listitem>

    </itemizedlist>

   </sect3>

  </sect2>
 </sect1>
