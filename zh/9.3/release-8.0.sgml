<!-- doc/src/sgml/release-8.0.sgml -->
<!-- See header comment in release.sgml about typical markup -->

 <sect1 id="release-8-0-26">
  <title>Release 8.0.26</title>

  <formalpara>
  <title>Release date:</title>
  <para>2010-10-04</para>
  </formalpara>

  <para>
   本次发布包含对 8.0.25 的多方面修复。有关 8.0 大版本中新特性的信息，请参见 <xref linkend="release-8-0">。.
  </para>

  <para>
      这预计是 <productname>PostgreSQL</productname> 8.0.X 系列的最后一个发行版。建议用户尽快更新到较新的发行分支。
     </para>

  <sect2>
   <title>Migration to Version 8.0.26</title>

   <para>
      对于运行 8.0.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.0.22 的版本升级，请参阅 <xref linkend="release-8-0-22">。
     </para>

  </sect2>

  <sect2>
   <title>Changes</title>

   <itemizedlist>

    <listitem>
     <para>
      在 PL/Perl 和 PL/Tcl 中为每个调用方 SQL 用户 ID 使用单独的解释器（Tom Lane）
     </para>

     <para>
      此变更防止了可通过破坏稍后将在同一会话中以另一 SQL 用户身份执行的 Perl 或 Tcl 代码（例如在 <literal>SECURITY DEFINER</literal> 函数内）而造成的安全问题。大多数脚本语言都提供许多实现这一点的手段，例如重新定义目标函数调用的标准函数或操作符。如果没有此变更，任何拥有 Perl 或 Tcl 语言使用权的 SQL 用户基本上都能以目标函数所有者的 SQL 权限为所欲为。
     </para>

     <para>
      此变更的代价是 Perl 和 Tcl 函数之间的有意通信变得更困难。为提供出路，PL/PerlU 和 PL/TclU 函数继续每会话只使用一个解释器。这不被视为安全问题，因为所有这类函数本来就以数据库超级用户的信任级别执行。
     </para>

     <para>
      声称提供可信执行的第三方过程语言很可能存在类似的安全问题。我们建议就安全关键用途联系你所依赖的任何 PL 的作者。
     </para>

     <para>
      感谢 Tim Bunce 指出此问题（CVE-2010-3433）。
     </para>
    </listitem>

    <listitem>
     <para>
      禁止以并非它预期配合使用的系统目录列的参数调用 <function>pg_get_expr()</function>，以防止可能的崩溃（Heikki Linnakangas、Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <quote>无法处理未规划的子查询</quote> 错误（Tom Lane）
     </para>

     <para>
      当子查询包含一个连接别名引用，而该引用展开成包含另一个子查询的表达式时，会出现此错误。
     </para>
    </listitem>

    <listitem>
     <para>
      防御返回 setof record 但并非所有返回行实际为同一行类型的函数（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      写入锁文件（<filename>postmaster.pid</filename> 和套接字锁文件两者）内容时注意对其执行 fsync（Tom Lane）
     </para>

     <para>
      如果机器在 postmaster 启动后不久崩溃，这一疏漏可能导致锁文件内容损坏。这又可能使后续启动 postmaster 的尝试失败，直到手工删除锁文件。
     </para>
    </listitem>

    <listitem>
     <para>
      避免在为深层嵌套的子事务分配 XID 时递归（Andres Freund、Robert Haas）
     </para>

     <para>
      在栈空间有限时，原来的编码可能导致崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <varname>log_line_prefix</varname> 的 <literal>%i</literal> 转义，它可能在后端启动早期产生垃圾（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复启用归档时 <command>ALTER TABLE ... SET TABLESPACE</command> 中可能的数据损坏（Jeff Davis）
     </para>
    </listitem>

    <listitem>
     <para>
      允许 <command>CREATE DATABASE</command> 和 <command>ALTER DATABASE ... SET TABLESPACE</command> 被查询取消中断（Guillaume Lelarge）
     </para>
    </listitem>

    <listitem>
     <para>
      在 PL/Python 中，防御 <function>PyCObject_AsVoidPtr</function> 和 <function>PyCObject_FromVoidPtr</function> 的空指针结果（Peter Eisentraut）
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <filename>contrib/dblink</filename> 对包含已删除列的表的处理（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/dblink</filename> 中 <quote>重复连接名</quote> 错误后的连接泄漏（Itagaki Takahiro）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/dblink</filename> 以正确处理超过 62 字节的连接名（Itagaki Takahiro）
     </para>
    </listitem>

    <listitem>
     <para>
      更新构建基础设施和文档，反映源代码仓库从 CVS 迁移到 Git 的变化（Magnus Hagander 等）
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2010l
      for DST law changes in Egypt and Palestine; also historical corrections
      for Finland。
     </para>

     <para>
      此变更还为两个密克罗尼西亚时区新增了名称：Pacific/Chuuk 现在优先于 Pacific/Truk（首选缩写为 CHUT 而非 TRUT），Pacific/Pohnpei 优先于 Pacific/Ponape。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-0-25">
  <title>Release 8.0.25</title>

  <formalpara>
  <title>Release date:</title>
  <para>2010-05-17</para>
  </formalpara>

  <para>
   本次发布包含对 8.0.24 的多方面修复。有关 8.0 大版本中新特性的信息，请参见 <xref linkend="release-8-0">。.
  </para>

  <para>
      <productname>PostgreSQL</productname> 社区将于 2010 年 7 月停止对 8.0.X 版本系列的更新。建议用户尽快更新到较新的版本分支。
     </para>

  <sect2>
   <title>Migration to Version 8.0.25</title>

   <para>
      对于运行 8.0.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.0.22 的版本升级，请参阅 <xref linkend="release-8-0-22">。
     </para>

  </sect2>

  <sect2>
   <title>Changes</title>

   <itemizedlist>

    <listitem>
     <para>
      用应用于整个解释器的操作码掩码来强制 <literal>plperl</literal> 中的限制，而不是使用 <filename>Safe.pm</filename>（Tim Bunce、Andrew Dunstan）
     </para>

     <para>
      近期的事态发展使我们确信 <filename>Safe.pm</filename> 太不安全，不能依靠它使 <literal>plperl</literal> 可信。此变更完全移除了对 <filename>Safe.pm</filename> 的使用，改为使用始终应用操作码掩码的独立解释器。此变更的良好副作用包括：现在可以在 <literal>plperl</literal> 中以自然的方式使用 Perl 的 <literal>strict</literal> 编译指令，Perl 的 <literal>$a</literal> 和 <literal>$b</literal> 变量在排序例程中按预期工作，以及函数编译显著加快。（CVE-2010-1169）
     </para>
    </listitem>

    <listitem>
     <para>
      防止 PL/Tcl 执行来自 <structname>pltcl_modules</structname> 的不可信代码（Tom）
     </para>

     <para>
      PL/Tcl 从数据库表自动装载 Tcl 代码的特性可能被利用于特洛伊木马攻击，因为对谁能创建该表或向其中插入没有限制。此变更禁用该特性，除非 <structname>pltcl_modules</structname> 由超级用户拥有。（但是，不检查该表上的权限，因此确实需要较不安全的模块表的安装仍可以向受信任的非超级用户授予适当权限。）此外，除非确实要执行 <literal>pltclu</literal> 函数，否则防止把代码装载到无限制的<quote>普通</quote>Tcl 解释器中。（CVE-2010-1170）
     </para>
    </listitem>

    <listitem>
     <para>
      不允许非特权用户重置仅超级用户可用的参数设置（Alvaro）
     </para>

     <para>
      此前，如果非特权用户为自己运行 <literal>ALTER USER ... RESET ALL</literal>，或为他拥有的数据库运行 <literal>ALTER DATABASE ... RESET ALL</literal>，这将移除该用户或数据库的所有特殊参数设置，包括本应只可由超级用户更改的那些。现在，<command>ALTER</command> 只移除用户有权更改的参数。
     </para>
    </listitem>

    <listitem>
     <para>
      避免后端关闭发生在需要向日志条目添加 <literal>CONTEXT</literal> 内容时导致可能的崩溃（Tom）
     </para>

     <para>
      某些情况下上下文打印函数会失败，因为到打印日志消息时当前事务已被回滚。
     </para>
    </listitem>

    <listitem>
     <para>
      为现代 Perl 版本更新 pl/perl 的 <filename>ppport.h</filename>（Andrew）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 pl/python 中的若干内存泄漏（Andreas Freund、Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      防止 <application>psql</application> 在展开引用自身的变量时无限递归（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      确保 <filename>contrib/pgstattuple</filename> 函数及时响应取消中断（Tatsuhito Kasahara）
     </para>
    </listitem>

    <listitem>
     <para>
      让服务器启动正确处理 <function>shmget()</function> 对已存在的共享内存段返回 <literal>EINVAL</literal> 的情形（Tom）
     </para>

     <para>
      此行为曾在包括 OS X 在内的 BSD 衍生内核上观察到。它导致完全误导性的启动失败，抱怨共享内存请求大小过大。
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2010j
      for DST law changes in Argentina, Australian Antarctic, Bangladesh,
      Mexico, Morocco, Pakistan, Palestine, Russia, Syria, Tunisia;
      also historical corrections for Taiwan。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-0-24">
  <title>Release 8.0.24</title>

  <formalpara>
  <title>Release date:</title>
  <para>2010-03-15</para>
  </formalpara>

  <para>
   本次发布包含对 8.0.23 的多方面修复。有关 8.0 大版本中新特性的信息，请参见 <xref linkend="release-8-0">。.
  </para>

  <para>
      <productname>PostgreSQL</productname> 社区将于 2010 年 7 月停止对 8.0.X 版本系列的更新。建议用户尽快更新到较新的版本分支。
     </para>

  <sect2>
   <title>Migration to Version 8.0.24</title>

   <para>
      对于运行 8.0.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.0.22 的版本升级，请参阅 <xref linkend="release-8-0-22">。
     </para>

  </sect2>

  <sect2>
   <title>Changes</title>

   <itemizedlist>

    <listitem>
     <para>
      新增配置参数 <varname>ssl_renegotiation_limit</varname>，控制对 SSL 连接进行会话密钥重协商的频率（Magnus）
     </para>

     <para>
      可以把它设为零以完全禁用重协商，这在使用有缺陷的 SSL 库时可能是必需的。特别是，某些厂商发布的 CVE-2009-3555 临时补丁会导致重协商尝试失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复试图从子事务启动失败中恢复时可能的崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复与使用保存点以及客户端编码不同于服务器编码相关的服务器内存泄漏（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <type>bit</type> 类型的 <function>substring()</function> 把任何负长度都视为<quote>字符串的其余全部</quote>（Tom）
     </para>

     <para>
      以前的编码只对 -1 这样处理，对其他负值会产生无效的结果值，可能导致崩溃（CVE-2010-0442）。
     </para>
    </listitem>

    <listitem>
     <para>
      修复整数到位串的转换：当输出位宽比给定整数宽出非 8 位倍数时，正确处理第一个小数字节（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复若干病态缓慢的正则表达式匹配情形（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复备份历史文件中的 <literal>STOP WAL LOCATION</literal> 条目：当结束位置恰好位于段边界时报告下一个 WAL 段的名称（Itagaki Takahiro）
     </para>
    </listitem>

    <listitem>
     <para>
      读取 <filename>pg_hba.conf</filename> 及相关文件时，如果 <literal>@</literal> 出现在引号内，不要把 <literal>@something</literal> 当作文件包含请求；另外，永远不要把单独的 <literal>@</literal> 当作文件包含请求（Tom）
     </para>

     <para>
      这防止了角色名或数据库名以 <literal>@</literal> 开头时的古怪行为。如果需要包含路径名包含空格的文件，仍然可以做到，但必须写 <literal>@"/path to/file"</literal> 而不是把引号放在整个构造之外。
     </para>
    </listitem>

    <listitem>
     <para>
      防止在某些平台上把目录命名为 <filename>pg_hba.conf</filename> 及相关文件中的包含目标时出现无限循环（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复组合列被设为 NULL 的一种情形下 plpgsql 的失败（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      在 PL/Python 中添加 <literal>volatile</literal> 标记，以避免可能的编译器特定错误行为（Zdenek Kotala）
     </para>
    </listitem>

    <listitem>
     <para>
      确保 PL/Tcl 完全初始化 Tcl 解释器（Tom）
     </para>

     <para>
      此疏漏唯一已知的症状是使用 Tcl 8.5 或更高版本时 Tcl 的 <literal>clock</literal> 命令行为异常。
     </para>
    </listitem>

    <listitem>
     <para>
      防止在向 <function>dblink_build_sql_*</function> 函数指定过多键列时 <filename>contrib/dblink</filename> 崩溃（Rushabh Lathia、Joe Conway）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/xml2</filename> 中因草率内存管理导致的若干崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2010e
      for DST law changes in Bangladesh, Chile, Fiji, Mexico, Paraguay, Samoa。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-0-23">
  <title>Release 8.0.23</title>

  <formalpara>
  <title>Release date:</title>
  <para>2009-12-14</para>
  </formalpara>

  <para>
   本次发布包含对 8.0.22 的多方面修复。有关 8.0 大版本中新特性的信息，请参见 <xref linkend="release-8-0">。.
  </para>

  <sect2>
   <title>Migration to Version 8.0.23</title>

   <para>
      对于运行 8.0.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.0.22 的版本升级，请参阅 <xref linkend="release-8-0-22">。
     </para>

  </sect2>

  <sect2>
   <title>Changes</title>

   <itemizedlist>

    <listitem>
     <para>
      防范索引函数更改会话局部状态导致的间接安全威胁（Gurjeet Singh、Tom）
     </para>

     <para>
      此变更防止据称不可变的索引函数可能破坏超级用户的会话（CVE-2009-4136）。
     </para>
    </listitem>

    <listitem>
     <para>
      拒绝在公共名（CN）字段中包含嵌入空字节的 SSL 证书（Magnus）
     </para>

     <para>
      这防止在 SSL 验证期间证书与服务器或客户端名称的非预期匹配（CVE-2009-4034）。
     </para>
    </listitem>

    <listitem>
     <para>
      修复后端启动时缓存初始化期间可能的崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      防止信号在不安全的时刻中断 <literal>VACUUM</literal>（Alvaro）
     </para>

     <para>
      此修复防止了 <literal>VACUUM FULL</literal> 在已提交其元组移动后被取消时的 PANIC，以及普通 <literal>VACUUM</literal> 在截断表之后被中断时的瞬态错误。
     </para>
    </listitem>

    <listitem>
     <para>
      修复哈希表大小计算中因整数溢出而可能的崩溃（Tom）
     </para>

     <para>
      这可能发生在规划器对哈希连接结果大小的估计极大时。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <type>inet</type>/<type>cidr</type> 比较中非常罕见的崩溃（Chris Mikkelson）
     </para>
    </listitem>

    <listitem>
     <para>
      修复在子事务内访问的游标所用临时文件被过早删除的问题（Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PAM 口令处理使其更健壮（Tom）
     </para>

     <para>
      已知以前的代码在 Linux <literal>pam_krb5</literal> PAM 模块与 Microsoft Active Directory 域控制器组合下失败。它在其他地方可能也有问题，因为它对 PAM 栈会传递给它什么参数做了无根据的假设。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PL/Python 异常处理中的罕见崩溃（Peter）
     </para>
    </listitem>

    <listitem>
     <para>
      确保 <application>psql</application> 的 flex 模块用正确的系统头文件定义编译（Tom）
     </para>

     <para>
      这修复了在 <literal>--enable-largefile</literal> 导致生成代码不兼容变更的平台上的构建失败。
     </para>
    </listitem>

    <listitem>
     <para>
      让 postmaster 忽略连接请求包中的任何 <literal>application_name</literal> 参数，以改进与未来 libpq 版本的兼容性（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2009s
      for DST law changes in Antarctica, Argentina, Bangladesh, Fiji,
      Novokuznetsk, Pakistan, Palestine, Samoa, Syria; also historical
      corrections for Hong Kong。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-0-22">
  <title>Release 8.0.22</title>

  <formalpara>
  <title>Release date:</title>
  <para>2009-09-09</para>
  </formalpara>

  <para>
   本次发布包含对 8.0.21 的多方面修复。有关 8.0 大版本中新特性的信息，请参见 <xref linkend="release-8-0">。.
  </para>

  <sect2>
   <title>Migration to Version 8.0.22</title>

   <para>
      对于运行 8.0.X 的用户，不需要进行转储/恢复。不过，如果你在 <type>interval</type> 列上有任何哈希索引，必须在更新到 8.0.22 后对它们执行 <command>REINDEX</command>。另外，如果你是从早于 8.0.6 的版本升级，请参阅 <xref linkend="release-8-0-6">。
     </para>

  </sect2>

  <sect2>
   <title>Changes</title>

   <itemizedlist>

    <listitem>
     <para>
      不允许在安全定义者函数内使用 <command>RESET ROLE</command> 和 <command>RESET SESSION AUTHORIZATION</command>（Tom、Heikki）
     </para>

     <para>
      这覆盖了先前禁止在安全定义者函数内使用 <command>SET ROLE</command> 和 <command>SET SESSION AUTHORIZATION</command> 的补丁中遗漏的一种情形。（参见 CVE-2007-6600）
     </para>
    </listitem>

    <listitem>
     <para>
      修复出现在外层聚合函数参数中的子 SELECT 的处理（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <type>interval</type> 数据类型的哈希计算（Tom）
     </para>

     <para>
      这纠正了 interval 值上哈希连接的错误结果。它还更改了 interval 列上哈希索引的内容。如果你有任何这样的索引，必须在更新后对它们执行 <command>REINDEX</command>。
     </para>
    </listitem>

    <listitem>
     <para>
      把 <function>to_char(..., 'TH')</function> 在 <literal>'HH'</literal>/<literal>'HH12'</literal> 下当作大写序数后缀（Heikki）
     </para>

     <para>
      它以前被当作 <literal>'th'</literal>（小写）处理。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <replaceable>x</replaceable> 超过 200 万且使用整数日期时间时 <literal>INTERVAL '<replaceable>x</replaceable> ms'</literal> 的溢出（Alex Hunsaker）
     </para>
    </listitem>

    <listitem>
     <para>
      修复点与线段之间距离的计算（Tom）
     </para>

     <para>
      这导致多个几何操作符的结果不正确。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <type>money</type> 数据类型，使其在货币金额没有小数位的区域设置（例如日本）中工作（Itagaki Takahiro）
     </para>
    </listitem>

    <listitem>
     <para>
      正确地对像 <literal>00:12:57.9999999999999999999999999999</literal> 这样的日期时间输入四舍五入（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 GiST R-tree 操作符类中页分裂点选择不佳的问题（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 plperl 初始化中的可移植性问题（Andrew Dunstan）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_ctl</application>，使其在 <filename>postgresql.conf</filename> 为空时不进入无限循环（Jeff Davis）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/xml2</filename> 的 <function>xslt_process()</function>，使其正确处理最大参数数量（二十个）（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <application>libpq</application> 从 <command>COPY FROM STDIN</command> 期间错误中恢复的代码的健壮性（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      避免在两个库都安装时包含相互冲突的 readline 和 editline 头文件（Zdenek Kotala）
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2009l
      for DST law changes in Bangladesh, Egypt, Jordan, Pakistan,
      Argentina/San_Luis, Cuba, Jordan (historical correction only),
      Mauritius, Morocco, Palestine, Syria, Tunisia。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-0-21">
  <title>Release 8.0.21</title>

  <formalpara>
  <title>Release date:</title>
  <para>2009-03-16</para>
  </formalpara>

  <para>
   本次发布包含对 8.0.20 的多方面修复。有关 8.0 大版本中新特性的信息，请参见 <xref linkend="release-8-0">。.
  </para>

  <sect2>
   <title>Migration to Version 8.0.21</title>

   <para>
      对于运行 8.0.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.0.6 的版本升级，请参阅 <xref linkend="release-8-0-6">。
     </para>

  </sect2>

  <sect2>
   <title>Changes</title>

   <itemizedlist>

    <listitem>
     <para>
      防止编码转换失败时的错误递归崩溃（Tom）
     </para>

     <para>
      此变更扩展了上两个小版本中针对相关失败情形所做的修复。以前的修复是针对最初的问题报告量身定制的，但我们现在认识到，编码转换函数抛出的<emphasis>任何</emphasis>错误都可能在试图报告该错误时导致无限递归。因此，解决方案是：如果发现已处于递归错误报告情形，就禁用翻译和编码转换并报告错误消息的纯 ASCII 形式。（CVE-2009-0922）
     </para>
    </listitem>

    <listitem>
     <para>
      不允许 <command>CREATE CONVERSION</command> 为指定的转换函数使用错误的编码（Heikki）
     </para>

     <para>
      这防止了编码转换失败的一种可能情形。前一个变更是兜底，防范同一领域其他类型的失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>to_char()</function> 被给定与数据参数类型不适当的格式代码时的核心转储（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      把 <literal>MUST</literal>（毛里求斯岛夏令时）加入已知时区缩写的默认列表（Xavier Bugaud）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-0-20">
  <title>Release 8.0.20</title>

  <formalpara>
  <title>Release date:</title>
  <para>2009-02-02</para>
  </formalpara>

  <para>
   本次发布包含对 8.0.19 的多方面修复。有关 8.0 大版本中新特性的信息，请参见 <xref linkend="release-8-0">。.
  </para>

  <sect2>
   <title>Migration to Version 8.0.20</title>

   <para>
      对于运行 8.0.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.0.6 的版本升级，请参阅 <xref linkend="release-8-0-6">。
     </para>

  </sect2>

  <sect2>
   <title>Changes</title>

   <itemizedlist>

    <listitem>
     <para>
      改进 <function>headline()</function> 函数中对 URL 的处理（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <function>headline()</function> 函数对超长标题的处理（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      防止在为指定的编码对创建了错误转换函数的编码转换时可能的断言失败或错误转换（Tom、Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      避免 <command>VACUUM</command> 中对小表的不必要锁定（Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/tsearch2</filename> 的 <function>get_covers()</function> 函数中的未初始化变量（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      让所有文档在适当之处引用 <literal>pgsql-bugs</literal> 和/或 <literal>pgsql-hackers</literal>，而不是现已停用的 <literal>pgsql-ports</literal> 和 <literal>pgsql-patches</literal> 邮件列表（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      更新时区数据文件到 <application>tzdata</application> 发行版 2009a（针对加德满都以及瑞士、古巴的历史 DST 修正）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-0-19">
  <title>Release 8.0.19</title>

  <formalpara>
  <title>Release date:</title>
  <para>2008-11-03</para>
  </formalpara>

  <para>
   本次发布包含对 8.0.18 的多方面修复。有关 8.0 大版本中新特性的信息，请参见 <xref linkend="release-8-0">。.
  </para>

  <sect2>
   <title>Migration to Version 8.0.19</title>

   <para>
      对于运行 8.0.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.0.6 的版本升级，请参阅 <xref linkend="release-8-0-6">。
     </para>

  </sect2>

  <sect2>
   <title>Changes</title>

   <itemizedlist>

    <listitem>
     <para>
      修复客户端编码无法表示本地化错误消息时的后端崩溃（Tom）
     </para>

     <para>
      我们此前处理过类似问题，但如果 <quote>字符没有等价物</quote>消息本身无法转换，仍会失败。修复方法是在检测到这种情形时禁用本地化并发送纯 ASCII 错误消息。
     </para>
    </listitem>

    <listitem>
     <para>
      修复从触发器调用深层嵌套函数时可能的崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      确保新定义的 PL/pgSQL 触发器函数被当作普通函数调用时报错（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复单个查询项匹配文本首词时 tsearch2 标题生成不正确的问题（Sushant Sinha）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <option>--enable-integer-datetimes</option> 构建中使用非 ISO datestyle 时 interval 值小数秒显示不当的问题（Ron Mayer）
     </para>
    </listitem>

    <listitem>
     <para>
      确保 <function>SPI_getvalue</function> 和 <function>SPI_getbinval</function> 在传入的元组和元组描述符列数不同时行为正确（Tom）
     </para>

     <para>
      当表添加或删除过列时这种情况是正常的，但这两个函数没有正确处理它。唯一可能的后果是错误的错误指示。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>ecpg</application> 对 <command>CREATE USER</command> 的解析（Michael）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>pg_ctl restart</literal> 最近损坏的问题（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      更新时区数据文件到 <application>tzdata</application> 发行版 2008i（针对阿根廷、巴西、毛里求斯、叙利亚的 DST 法规变更）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-0-18">
  <title>Release 8.0.18</title>

  <formalpara>
  <title>Release date:</title>
  <para>2008-09-22</para>
  </formalpara>

  <para>
   本次发布包含对 8.0.17 的多方面修复。有关 8.0 大版本中新特性的信息，请参见 <xref linkend="release-8-0">。.
  </para>

  <sect2>
   <title>Migration to Version 8.0.18</title>

   <para>
      对于运行 8.0.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.0.6 的版本升级，请参阅 <xref linkend="release-8-0-6">。
     </para>

  </sect2>

  <sect2>
   <title>Changes</title>

   <itemizedlist>

    <listitem>
     <para>
      把本地锁计数器从 32 位拓宽到 64 位（Tom）
     </para>

     <para>
      这是针对计数器在足够长的事务中可能溢出的报告，该溢出会导致意外的 <quote>锁已被持有</quote>错误。
     </para>
    </listitem>

    <listitem>
     <para>
      在执行器启动时添加检查，确保 <command>INSERT</command> 或 <command>UPDATE</command> 产生的元组与目标表的当前行类型匹配（Tom）
     </para>

     <para>
      <command>ALTER COLUMN TYPE</command> 之后重用先前缓存的计划可能产生这种情形。该检查防范随之而来的数据损坏和/或崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      修复日期时间输入函数在 64 位平台上正确检测整数溢出（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      改进向 syslog 写很长日志消息的性能（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>SELECT DISTINCT ON</literal> 查询上游标反向扫描的缺陷（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复规划器，使其估计产生布尔结果的 <literal>GROUP BY</literal> 表达式总是产生两个组，而不考虑表达式的内容（Tom）
     </para>

     <para>
      对于某些布尔测试（如 <replaceable>col</replaceable> <literal>IS NULL</literal>），这比常规的 <literal>GROUP BY</literal> 估计准确得多。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PL/Tcl 在 Tcl 8.5 下的正确行为，并更小心地处理发送到 Tcl 或从 Tcl 发出的数据的编码（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PL/Python 使其与 Python 2.5 一起工作
     </para>

     <para>
      这是 8.2 开发周期中所做修复的向后移植。
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <application>pg_dump</application> 和 <application>pg_restore</application> 在发送 SQL 命令失败后的错误报告（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_ctl</application>，使其在 <literal>restart</literal> 期间正确保留 postmaster 命令行参数（Bruce）
     </para>
    </listitem>

    <listitem>
     <para>
      更新时区数据文件到 <application>tzdata</application> 发行版 2008f（针对阿根廷、巴哈马、巴西、毛里求斯、摩洛哥、巴基斯坦、巴勒斯坦和巴拉圭的 DST 法规变更）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-0-17">
  <title>Release 8.0.17</title>

  <formalpara>
  <title>Release date:</title>
  <para>2008-06-12</para>
  </formalpara>

  <para>
      此发行版在 8.0.16 的基础上包含一个严重的缺陷修复。有关 8.0 主版本新特性的信息，请参阅 <xref linkend="release-8-0">。
     </para>

  <sect2>
   <title>Migration to Version 8.0.17</title>

   <para>
      对于运行 8.0.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.0.6 的版本升级，请参阅 <xref linkend="release-8-0-6">。
     </para>

  </sect2>

  <sect2>
   <title>Changes</title>

   <itemizedlist>

    <listitem>
     <para>
      让 <function>pg_get_ruledef()</function> 给负常数加括号（Tom）
     </para>

     <para>
      在此修复之前，视图或规则中的负常数可能被转储成如 <literal>-42::integer</literal>，这有微妙的错误：由于操作符优先级规则，它应该是 <literal>(-42)::integer</literal>。通常这影响不大，但它可能与另一个最近的补丁相互作用，导致 <productname>PostgreSQL</productname> 拒绝原本有效的 <command>SELECT DISTINCT</command> 视图查询。由于这可能导致 <application>pg_dump</application> 输出无法重载，它被当作高优先级修复。转储输出确实不正确的已发布版本只有 8.3.1 和 8.2.7。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-0-16">
  <title>Release 8.0.16</title>

  <formalpara>
  <title>Release date:</title>
  <para>
      从未发布
     </para>
  </formalpara>

  <para>
   本次发布包含对 8.0.15 的多方面修复。有关 8.0 大版本中新特性的信息，请参见 <xref linkend="release-8-0">。.
  </para>

  <sect2>
   <title>Migration to Version 8.0.16</title>

   <para>
      对于运行 8.0.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.0.6 的版本升级，请参阅 <xref linkend="release-8-0-6">。
     </para>

  </sect2>

  <sect2>
   <title>Changes</title>

   <itemizedlist>

    <listitem>
     <para>
      修复 <command>ALTER TABLE ADD COLUMN ... PRIMARY KEY</command>，使新列被正确检查是否已全部初始化为非空（Brendan Jurd）
     </para>

     <para>
      以前的版本完全忽略了对这一要求的检查。
     </para>
    </listitem>

    <listitem>
     <para>
      修复从多个都继承自共同祖先的父关系继承<quote>同一</quote>约束时 <command>CREATE TABLE</command> 可能失败的问题（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 ISO-8859-5 与其他编码之间的转换对西里尔<quote>Yo</quote>字符（带两个点的 <literal>e</literal> 和 <literal>E</literal>）的处理（Sergey Burladyan）
     </para>
    </listitem>

    <listitem>
     <para>
      修复几个数据类型输入函数，它们允许结果中未使用的字节包含未初始化的不可预测值（Tom）
     </para>

     <para>
      这可能导致两个看起来相同的字面值不被视为相等的失败，导致解析器抱怨 <literal>ORDER BY</literal> 和 <literal>DISTINCT</literal> 表达式不匹配。
     </para>
    </listitem>

    <listitem>
     <para>
      修复正则表达式子串匹配（<literal>substring(<replaceable>string</replaceable> from <replaceable>pattern</replaceable>)</literal>）中的一个极端情形（Tom）
     </para>

     <para>
      问题发生在模式整体有匹配但用户指定了带括号的子表达式而该子表达式没有匹配时。例子是 <literal>substring('foo' from 'foo(bar)?')</literal>。由于 <literal>(bar)</literal> 未匹配，它应返回 NULL，但它错误地返回了整个模式的匹配（即 <literal>foo</literal>）。
     </para>
    </listitem>

    <listitem>
     <para>
      更新时区数据文件到 <application>tzdata</application> 发行版 2008c（针对摩洛哥、伊拉克、乔巴山、巴基斯坦、叙利亚、古巴、阿根廷/圣路易斯和智利的 DST 法规变更）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>ecpg</application> 的 <function>PGTYPEStimestamp_sub()</function> 函数结果不正确的问题（Michael）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/xml2</filename> 的 <function>xpath_table()</function> 函数在输入查询返回 NULL 值时的核心转储（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/xml2</filename> 的 makefile，使其不覆盖 <literal>CFLAGS</literal>（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>DatumGetBool</literal> 宏，使其在 <application>gcc</application> 4.3 下不失败（Tom）
     </para>

     <para>
      此问题影响返回布尔的<quote>旧式</quote>（V0）C 函数。修复已在 8.3 中，但当时没有意识到需要向后移植。
     </para>
    </listitem>

    <listitem>
     <para>
      修复长期存在的 <command>LISTEN</command>/<command>NOTIFY</command> 竞态条件（Tom）
     </para>

     <para>
      极少数情况下，刚执行过 <command>LISTEN</command> 的会话可能收不到通知，即使按理应当收到，因为观察到执行 <command>NOTIFY</command> 的并发事务稍后提交了。
     </para>

     <para>
      此修复的一个副作用是：执行了尚未提交的 <command>LISTEN</command> 命令的事务如果去查看，将看不到该 <command>LISTEN</command> 在 <structname>pg_listener</structname> 中的行；以前能看到。这一行为从未被明确记录，但有些应用程序可能依赖旧行为。
     </para>
    </listitem>

    <listitem>
     <para>
      修复使用哈希索引的查询期间发生错误时的罕见崩溃（Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      修复公元前年份 2 月 29 日日期时间值的输入（Tom）
     </para>

     <para>
      以前的编码弄错了哪些年份是闰年。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <command>ALTER OWNER</command> 某些变体中的 <quote>无法识别的节点类型</quote>错误（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_ctl</application>，使其从命令行选项正确提取 postmaster 的端口号（Itagaki Takahiro、Tom）
     </para>

     <para>
      此前 <literal>pg_ctl start -w</literal> 可能试图在错误端口上联系 postmaster，导致虚假的启动失败报告。
     </para>
    </listitem>

    <listitem>
     <para>
      使用 <option>-fwrapv</option> 防御最新 <application>gcc</application> 版本可能的错误优化（Tom）
     </para>

     <para>
      已知用 <application>gcc</application> 4.3 或更高版本构建 <productname>PostgreSQL</productname> 时这是必需的。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>ORDER BY</literal> 和 <literal>GROUP BY</literal> 中常量表达式的显示（Tom）
     </para>

     <para>
      显式转换的常量会被错误显示。这例如可能导致视图定义在转储和重载期间损坏。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>libpq</application>，使其在 COPY OUT 期间正确处理 NOTICE 消息（Tom）
     </para>

     <para>
      只观察到此失败在用户定义数据类型的输出例程发出 NOTICE 时发生，但不能保证它不会因其他原因发生。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-0-15">
  <title>Release 8.0.15</title>

  <formalpara>
  <title>Release date:</title>
  <para>2008-01-07</para>
  </formalpara>

  <para>
      此发行版包含 8.0.14 之后的多种修复，包括对重要安全问题的修复。有关 8.0 主版本新特性的信息，请参阅 <xref linkend="release-8-0">。
     </para>

  <para>
      这是 <productname>PostgreSQL</productname> 社区为 <productname>Windows</productname> 制作二进制包的最后一个 8.0.X 版本。建议 Windows 用户迁移到 8.2.X 或更高版本，因为 8.2.X 中有一些不便于向后移植的 Windows 特定修复。8.0.X 在其他平台上将继续受支持。
     </para>

  <sect2>
   <title>Migration to Version 8.0.15</title>

   <para>
      对于运行 8.0.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.0.6 的版本升级，请参阅 <xref linkend="release-8-0-6">。
     </para>

  </sect2>

  <sect2>
   <title>Changes</title>

   <itemizedlist>

    <listitem>
     <para>
      防止索引中的函数以运行 <command>VACUUM</command>、<command>ANALYZE</command> 等的用户的权限执行（Tom）
     </para>

     <para>
      每当产生新的表条目时，都会求值索引表达式和部分索引谓词中使用的函数。人们早就明白，如果修改不可信用户拥有的表，这会带来特洛伊木马代码执行的风险。（注意触发器、默认值、检查约束等也构成同类风险。）但索引中的函数带来额外危险，因为它们会由例程维护操作（如 <command>VACUUM FULL</command>）执行，而这些操作通常在超级用户账户下自动执行。例如，恶意用户可以通过设置特洛伊木马索引定义并等待下一次例程清理来以超级用户权限执行代码。修复安排标准维护操作（包括 <command>VACUUM</command>、<command>ANALYZE</command>、<command>REINDEX</command> 和 <command>CLUSTER</command>）以表所有者而非调用用户的身份执行，使用已用于 <literal>SECURITY DEFINER</literal> 函数的同一权限切换机制。为防止绕过此安全措施，现在禁止在 <literal>SECURITY DEFINER</literal> 上下文中执行 <command>SET SESSION AUTHORIZATION</command> 和 <command>SET ROLE</command>。（CVE-2007-6600）
     </para>
    </listitem>

    <listitem>
     <para>
      修复正则表达式包中的若干缺陷（Tom、Will Drewry）
     </para>

     <para>
      精心构造的正则表达式模式可能导致崩溃、无限或接近无限的循环和/或大量内存消耗，这些都对接受来自不可信来源的正则搜索模式的应用程序构成拒绝服务风险。（CVE-2007-4769、CVE-2007-4772、CVE-2007-6067）
     </para>
    </listitem>

    <listitem>
     <para>
      作为安全措施，要求使用 <filename>/contrib/dblink</filename> 的非超级用户只使用口令认证（Joe）
     </para>

     <para>
      8.0.14 中出现的修复并不完整，它只堵住了部分 <filename>dblink</filename> 函数的漏洞。（CVE-2007-6601、CVE-2007-3278）
     </para>
    </listitem>

    <listitem>
     <para>
      更新时区数据文件到 <application>tzdata</application> 发行版 2007k（特别是阿根廷最近的变更）（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复某些 <literal>WHERE false AND var IN (SELECT ...)</literal> 情形下的规划器失败（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      保留被 <command>ALTER TABLE ... ALTER COLUMN TYPE</command> 重建的索引的表空间（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      让归档恢复总是开始新的 WAL 时间线，而不是只在使用恢复停止时间时（Simon）
     </para>

     <para>
      这避免了试图覆盖最后一个 WAL 段现有归档副本的极端情形风险，而且看起来比原来的定义更简单干净。
     </para>
    </listitem>

    <listitem>
     <para>
      当表太小而不适用时，让 <command>VACUUM</command> 不使用全部 <varname>maintenance_work_mem</varname>（Alvaro）
     </para>
    </listitem>

    <listitem>
     <para>
      修复使用多字节数据库编码时 <function>translate()</function> 可能的崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PL/Perl 在平台 Perl 把 <literal>bool</literal> 类型定义为 <literal>int</literal> 而非 <literal>char</literal> 时的问题（Tom）
     </para>

     <para>
      虽然理论上这可能在任何地方发生，但没有标准 Perl 构建这样做……直到 <productname>Mac OS X</productname> 10.5。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PL/Python 在长异常消息上不崩溃（Alvaro）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_dump</application>，使其正确处理默认表达式与父表不同的继承子表（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      <application>ecpg</application> 解析器修复（Michael）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <filename>contrib/tablefunc</filename> 的 <function>crosstab()</function> 把 NULL rowid 本身作为一个类别处理，而不是崩溃（Joe）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <type>tsvector</type> 和 <type>tsquery</type> 输出例程以正确转义反斜杠（Teodor、Bruce）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>to_tsvector()</function> 在超长输入字符串上的崩溃（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      要求在重新生成 <command>configure</command> 脚本时使用特定版本的 <productname>Autoconf</productname>（Peter）
     </para>

     <para>
      这只影响开发者和打包者。此变更是为了防止意外使用未经测试的 <productname>Autoconf</productname> 与 <productname>PostgreSQL</productname> 版本组合。如果确实想使用不同的 <productname>Autoconf</productname> 版本，可以移除版本检查，但结果是否可用由你自己负责。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-0-14">
  <title>Release 8.0.14</title>

  <formalpara>
  <title>Release date:</title>
  <para>2007-09-17</para>
  </formalpara>

  <para>
   本次发布包含对 8.0.13 的多方面修复。有关 8.0 大版本中新特性的信息，请参见 <xref linkend="release-8-0">。.
  </para>

  <sect2>
   <title>Migration to Version 8.0.14</title>

   <para>
      对于运行 8.0.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.0.6 的版本升级，请参阅 <xref linkend="release-8-0-6">。
     </para>

  </sect2>

  <sect2>
   <title>Changes</title>

   <itemizedlist>

    <listitem>
     <para>
      防止事务插入行后在同一表上并发 <command>VACUUM</command> 接近结束时中止导致的索引损坏（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <command>CREATE DOMAIN ... DEFAULT NULL</command> 正确工作（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <acronym>SSL</acronym> 错误消息的过度记录（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复日志，使使用 syslogger 进程时日志消息永不交错（Andrew）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <varname>log_min_error_statement</varname> 日志记录内存耗尽时的崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复某些外键极端情形的错误处理（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      防止 <command>CLUSTER</command> 因试图处理其他会话的临时表而失败（Alvaro）
     </para>
    </listitem>

    <listitem>
     <para>
      更新时区数据库规则，特别是新西兰即将到来的变更（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      Windows 套接字改进（Magnus）
     </para>
    </listitem>

    <listitem>
     <para>
      因可能的编码不匹配，在 Windows 上抑制日志时间戳中的时区名（<literal>%Z</literal>）（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      作为安全措施，要求使用 <filename>/contrib/dblink</filename> 的非超级用户只使用口令认证（Joe）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-0-13">
  <title>Release 8.0.13</title>

  <formalpara>
  <title>Release date:</title>
  <para>2007-04-23</para>
  </formalpara>

  <para>
      此发行版包含 8.0.12 之后的多种修复，包括一个安全修复。有关 8.0 主版本新特性的信息，请参阅 <xref linkend="release-8-0">。
     </para>

  <sect2>
   <title>Migration to Version 8.0.13</title>

   <para>
      对于运行 8.0.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.0.6 的版本升级，请参阅 <xref linkend="release-8-0-6">。
     </para>

  </sect2>

  <sect2>
   <title>Changes</title>

   <itemizedlist>

    <listitem>
    <para>
      支持在 <varname>search_path</varname> 中显式放置临时表模式，并禁用为函数和操作符搜索它（Tom）
     </para>
    <para>
      这是允许安全定义者函数设置真正安全的 <varname>search_path</varname> 值所必需的。没有它，非特权 SQL 用户可以利用临时对象以安全定义者函数的权限执行代码（CVE-2007-2138）。更多信息请参阅 <command>CREATE FUNCTION</command>。
     </para>
    </listitem>

    <listitem>
    <para>
      <filename>/contrib/tsearch2</filename> 崩溃修复（Teodor）
     </para>
    </listitem>

    <listitem>
    <para>
      修复 <command>VACUUM FULL</command> 处理 <command>UPDATE</command> 链方式中的潜在数据损坏缺陷（Tom、Pavan Deolasee）
     </para>
    </listitem>

    <listitem>
    <para>
      修复哈希索引扩容期间的 PANIC（8.0.10 中引入的缺陷）（Tom）
     </para>
    </listitem>

    <listitem>
    <para>
      修复 POSIX 风格时区规范以遵循新的美国 DST 规则（Tom）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-0-12">
  <title>Release 8.0.12</title>

  <formalpara>
  <title>Release date:</title>
  <para>2007-02-07</para>
  </formalpara>

  <para>
      此发行版包含 8.0.11 之后的一个修复。有关 8.0 主版本新特性的信息，请参阅 <xref linkend="release-8-0">。
     </para>

  <sect2>
   <title>Migration to Version 8.0.12</title>

   <para>
      对于运行 8.0.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.0.6 的版本升级，请参阅 <xref linkend="release-8-0-6">。
     </para>

  </sect2>

  <sect2>
   <title>Changes</title>

   <itemizedlist>

    <listitem>
    <para>
      移除约束和函数索引中对类型长度的过度严格检查（Tom）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-0-11">
  <title>Release 8.0.11</title>

  <formalpara>
  <title>Release date:</title>
  <para>2007-02-05</para>
  </formalpara>

  <para>
      此发行版包含 8.0.10 之后的多种修复，包括一个安全修复。有关 8.0 主版本新特性的信息，请参阅 <xref linkend="release-8-0">。
     </para>

  <sect2>
   <title>Migration to Version 8.0.11</title>

   <para>
      对于运行 8.0.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.0.6 的版本升级，请参阅 <xref linkend="release-8-0-6">。
     </para>

  </sect2>

  <sect2>
   <title>Changes</title>

   <itemizedlist>

    <listitem>
    <para>
      移除允许已连接用户读取后端内存的安全漏洞（Tom）
     </para>
    <para>
      这些漏洞涉及抑制 SQL 函数返回其声明数据类型的常规检查，以及更改表列的数据类型（CVE-2007-0555、CVE-2007-0556）。这些错误很容易被利用来导致后端崩溃，而且原则上可能被用来读取用户不应能访问的数据库内容。
     </para>
    </listitem>

    <listitem>
    <para>
      修复罕见的缺陷：btree 索引页分裂可能因选择不可行的分裂点而失败（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
    <para>
      修复由 <literal>UNION</literal> 触发的罕见 Assert() 崩溃（Tom）
     </para>
    </listitem>

    <listitem>
    <para>
      收紧对超过三字节的 UTF8 序列的多字节字符处理的安全性（Tom）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-0-10">
  <title>Release 8.0.10</title>

  <formalpara>
  <title>Release date:</title>
  <para>2007-01-08</para>
  </formalpara>

  <para>
   本次发布包含对 8.0.9 的多方面修复。有关 8.0 大版本中新特性的信息，请参见 <xref linkend="release-8-0">。.
  </para>

  <sect2>
   <title>Migration to Version 8.0.10</title>

   <para>
      对于运行 8.0.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.0.6 的版本升级，请参阅 <xref linkend="release-8-0-6">。
     </para>

  </sect2>

  <sect2>
   <title>Changes</title>

   <itemizedlist>

    <listitem>
     <para>
      改进 AIX 上 <function>getaddrinfo()</function> 的处理（Tom）
     </para>

     <para>
      这修复了启动统计收集器等问题。
     </para>
    </listitem>

     <listitem>
      <para>
      修复 <command>VACUUM</command> 中的 <quote>未能重新找到父键</quote>错误（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      修复 <command>VACUUM</command> 截断跨越 GB 边界的大关系时的竞态条件（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      修复影响多 GB 哈希索引的缺陷（Tom）
     </para>
     </listitem>

    <listitem>
     <para>
      修复 Windows 信号处理中可能的死锁（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      修复构造由多个空元素组成的 <literal>ARRAY[]</literal> 时的错误（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 ecpg 连接期间的内存泄漏（Michael）
     </para>
    </listitem>

    <listitem>
     <para>
      对于新的 <application>initdb</application> 安装，<function>to_number()</function> 和 <function>to_char(numeric)</function> 现在是 <literal>STABLE</literal> 而不是 <literal>IMMUTABLE</literal>（Tom）
     </para>

     <para>
      这是因为 <varname>lc_numeric</varname> 可能改变这些函数的输出。
     </para>
    </listitem>

    <listitem>
     <para>
      改进使用括号的正则表达式的索引利用（Tom）
     </para>

     <para>
      这同时也改进了 <application>psql</application> 的 <literal>\d</literal> 性能。
     </para>
    </listitem>

    <listitem>
     <para>
      更新时区数据库
     </para>

     <para>
      这特别影响澳大利亚和加拿大的夏令时规则。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-0-9">
  <title>Release 8.0.9</title>

  <formalpara>
  <title>Release date:</title>
  <para>2006-10-16</para>
  </formalpara>

  <para>
   本次发布包含对 8.0.8 的多方面修复。有关 8.0 大版本中新特性的信息，请参见 <xref linkend="release-8-0">。.
  </para>

  <sect2>
   <title>Migration to Version 8.0.9</title>

   <para>
      对于运行 8.0.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.0.6 的版本升级，请参阅 <xref linkend="release-8-0-6">。
     </para>

  </sect2>

  <sect2>
   <title>Changes</title>

<itemizedlist>
<listitem><para>
      修复在规则 WHERE 表达式中引用 <literal>NEW</literal> 行值时的崩溃（Tom）
     </para></listitem>
<listitem><para>
      修复把无类型字面值当作 ANYARRAY 时的核心转储
     </para></listitem>
<listitem><para>
      修复查询包含返回多行的 SQL 函数时 AFTER 触发器的错误处理（Tom）
     </para></listitem>
<listitem><para>
      修复 <command>ALTER TABLE ... TYPE</>，对 <literal>USING</literal> 子句重新检查 <literal>NOT NULL</literal>（Tom）
     </para></listitem>
<listitem><para>
      修复 <function>string_to_array()</function>，处理分隔符字符串的重叠匹配
     </para>
<para>
      例如 <literal>string_to_array('123xx456xxx789', 'xx')</literal>。
     </para></listitem>
<listitem><para>
      修复 <application>psql</application> 的 <literal>\d</literal> 命令模式匹配的极端情形
     </para></listitem>
<listitem><para>
      修复 /contrib/ltree 中损坏索引的缺陷（Teodor）
     </para></listitem>
<listitem><para>
      <application>ecpg</application> 的大量健壮性修复（Joachim Wieland）
     </para></listitem>
<listitem><para>
      修复 /contrib/dbmirror 中的反斜杠转义
     </para></listitem>
<listitem><para>
      修复 Win32 上统计收集的不稳定（Tom、Andrew）
     </para></listitem>
<listitem><para>
      <systemitem class="osname">AIX</systemitem> 和 <productname>Intel</productname> 编译器的修复（Tom）
     </para></listitem>
</itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-0-8">
  <title>Release 8.0.8</title>

  <formalpara>
  <title>Release date:</title>
  <para>2006-05-23</para>
  </formalpara>

  <para>
      此发行版包含 8.0.7 之后的多种修复，包括针对极其严重安全问题的补丁。有关 8.0 主版本新特性的信息，请参阅 <xref linkend="release-8-0">。
     </para>

  <sect2>
   <title>Migration to Version 8.0.8</title>

   <para>
      对于运行 8.0.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.0.6 的版本升级，请参阅 <xref linkend="release-8-0-6">。
     </para>

   <para>
      要完全防范 CVE-2006-2313 和 CVE-2006-2314 所述的 SQL 注入攻击，可能需要更改应用程序代码。如果你有把不可信字符串嵌入 SQL 命令的应用程序，应尽快检查它们，确保使用了推荐的转义技术。在大多数情况下，应用程序应使用库或驱动提供的子例程（如 <application>libpq</application> 的 <function>PQescapeStringConn()</function>）来执行字符串转义，而不是依靠<foreignphrase>临时</foreignphrase>代码来做。
     </para>
  </sect2>

  <sect2>
   <title>Changes</title>

<itemizedlist>
<listitem><para>
      更改服务器，在所有情形下拒绝无效编码的多字节字符（Tatsuo、Tom）
     </para>
<para>
      虽然 <productname>PostgreSQL</productname> 向这个方向发展已有一段时间，但现在检查统一应用于所有编码和所有文本输入，而且总是错误而不只是警告。此变更可防范 CVE-2006-2313 所述类型的 SQL 注入攻击。
     </para></listitem>

<listitem><para>
      拒绝字符串字面量中对 <literal>\'</literal> 的不安全使用
     </para>
<para>
      作为针对 CVE-2006-2314 所述类型 SQL 注入攻击的服务器端防御，服务器现在只接受 <literal>''</literal> 而不接受 <literal>\'</literal> 作为 SQL 字符串字面量中 ASCII 单引号的表示。默认情况下，只有当 <varname>client_encoding</varname> 设为仅客户端编码（SJIS、BIG5、GBK、GB18030 或 UHC）时才拒绝 <literal>\'</literal>，这正是可能发生 SQL 注入的场景。需要时可用新的配置参数 <varname>backslash_quote</varname> 调整此行为。注意，要完全防范 CVE-2006-2314 可能需要客户端更改；<varname>backslash_quote</varname> 的目的之一是让不安全的客户端明显地不安全。
     </para></listitem>

<listitem><para>
      修改 <application>libpq</application> 的字符串转义例程，使其感知编码问题和 <varname>standard_conforming_strings</varname>
     </para>
<para>
      这为使用 <application>libpq</application> 的应用程序修复了 CVE-2006-2313 和 CVE-2006-2314 所述的安全问题，并使它们为将来转向 SQL 标准字符串字面量语法做好准备。并发使用多个 <productname>PostgreSQL</productname> 连接的应用程序应迁移到 <function>PQescapeStringConn()</function> 和 <function>PQescapeByteaConn()</function>，以确保为每个数据库连接中使用的设置正确转义。以<quote>手工</quote>方式做字符串转义的应用程序应改为依靠库例程。
     </para></listitem>

<listitem><para>
      修复一些不正确的编码转换函数
     </para>
<para>
      <function>win1251_to_iso</function>、<function>alt_to_iso</function>、<function>euc_tw_to_big5</function>、<function>euc_tw_to_mic</function>、<function>mic_to_euc_tw</function> 都在不同程度上损坏。
     </para></listitem>

<listitem><para>
      清理字符串中偶然残留的 <literal>\'</literal> 使用（Bruce、Jan）
     </para></listitem>

<listitem><para>
      修复有时导致 OR 索引扫描漏掉本应返回的行的缺陷
     </para></listitem>

<listitem><para>
      修复 btree 索引被截断情形的 WAL 重放
     </para></listitem>

<listitem><para>
      修复涉及 <literal>|</literal> 的模式的 <literal>SIMILAR TO</literal>（Tom）
     </para></listitem>

<listitem><para>
      修复 <command>SELECT INTO</command> 和 <command>CREATE TABLE AS</command>，在默认表空间而不是基目录中创建表（Kris Jurka）
     </para></listitem>

<listitem><para>
      修复服务器正确使用自定义 DH SSL 参数（Michael Fuhr）
     </para></listitem>

<listitem><para>
      修复 Intel Mac 上的 Bonjour（Ashley Clark）
     </para></listitem>

<listitem><para>
      修复各种小的内存泄漏
     </para></listitem>

<listitem><para>
      修复某些 Win32 系统上的口令提示问题（Robert Kinberg）
     </para></listitem>
</itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-0-7">
  <title>Release 8.0.7</title>

  <formalpara>
  <title>Release date:</title>
  <para>2006-02-14</para>
  </formalpara>

  <para>
   本次发布包含对 8.0.6 的多方面修复。有关 8.0 大版本中新特性的信息，请参见 <xref linkend="release-8-0">。.
  </para>

  <sect2>
   <title>Migration to Version 8.0.7</title>

   <para>
      对于运行 8.0.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.0.6 的版本升级，请参阅 <xref linkend="release-8-0-6">。
     </para>
  </sect2>

  <sect2>
   <title>Changes</title>

<itemizedlist>

<listitem><para>
      修复 <command>SET SESSION AUTHORIZATION</command> 中可能的崩溃（CVE-2006-0553）
     </para>
<para>
      如果服务器编译时启用了断言（这不是默认），非特权用户可以使服务器进程崩溃，导致对其他用户的瞬时拒绝服务。感谢 Akio Ishida 报告此问题。
     </para></listitem>

<listitem><para>
      修复自插入行的行可见性逻辑缺陷（Tom）
     </para>
<para>
      极少数情况下，当前命令插入的行可能被看作已经有效，而不应该如此。修复 8.0.4、7.4.9 和 7.3.11 版本中引入的缺陷。
     </para></listitem>

<listitem><para>
      修复 pg_clog 和 pg_subtrans 文件创建期间可能导致<quote>文件已存在</quote>错误的竞态条件（Tom）
     </para></listitem>

<listitem><para>
      修复缓存失效消息恰好在错误时刻到达时可能导致崩溃的情形（Tom）
     </para></listitem>

<listitem><para>
      为预备语句中的 <literal>UNKNOWN</literal> 参数正确检查 <literal>DOMAIN</literal> 约束（Neil）
     </para></listitem>

<listitem><para>
      确保 <command>ALTER COLUMN TYPE</command> 按正确顺序处理 <literal>FOREIGN KEY</literal>、<literal>UNIQUE</literal> 和 <literal>PRIMARY KEY</literal> 约束（Nakano Yoshihisa）
     </para></listitem>

<listitem><para>
      修复以允许恢复有对自定义操作符或操作符类的跨模式引用的转储（Tom）
     </para></listitem>

<listitem><para>
      允许 <application>pg_restore</application> 在 <command>COPY</command> 失败后正确继续；此前它试图把剩余的 <command>COPY</command> 数据当作 SQL 命令（Stephen Frost）
     </para></listitem>

<listitem><para>
      修复未指定数据目录时 <application>pg_ctl</application> 的 <literal>unregister</literal> 崩溃（Magnus）
     </para></listitem>

<listitem><para>
      修复 AMD64 和 PPC 上 <application>ecpg</application> 的崩溃（Neil）
     </para></listitem>

<listitem><para>
      修复 <application>PL/python</application> 参数传递期间发生错误时的正确恢复（Neil）
     </para></listitem>

<listitem><para>
      修复 <application>PL/perl</application> 在 Win32 上对区域设置的处理以与后端匹配（Andrew）
     </para></listitem>

<listitem><para>
      修复 Win32 上 <filename>postgresql.conf</filename> 中 <literal>log_min_messages</literal> 设为 <literal>DEBUG3</literal> 或更高时的崩溃（Bruce）
     </para></listitem>

<listitem><para>
      修复 Win32、Cygwin、OS X、AIX 的 <application>pgxs</application> <literal>-L</literal> 库路径指定（Bruce）
     </para></listitem>

<listitem><para>
      在检查 Win32 管理员权限时检查 SID 是否启用（Magnus）
     </para></listitem>

<listitem><para>
      正确拒绝超出范围的日期输入（Kris Jurka）
     </para></listitem>

<listitem><para>
      修复 configure 期间测试 <function>finite</function> 和 <function>isinf</function> 存在性的可移植性问题（Tom）
     </para></listitem>

</itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-0-6">
  <title>Release 8.0.6</title>

  <formalpara>
  <title>Release date:</title>
  <para>2006-01-09</para>
  </formalpara>

  <para>
   本次发布包含对 8.0.5 的多方面修复。有关 8.0 大版本中新特性的信息，请参见 <xref linkend="release-8-0">。.
  </para>

  <sect2>
   <title>Migration to Version 8.0.6</title>

   <para>
      对于运行 8.0.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.0.3 的版本升级，请参阅 <xref linkend="release-8-0-3">。另外，如果你受到下述区域设置或 <application>plperl</application> 问题的影响，可能需要在更新后对文本列上的索引执行 <command>REINDEX</command>。
     </para>
  </sect2>

  <sect2>
   <title>Changes</title>

<itemizedlist>

<listitem><para>
      修复 Windows 代码，使 ShmemBackendArray 没有更多空间时 postmaster 继续而不是退出（Magnus）
     </para>
<para>
      如果过多连接请求几乎同时到达，以前的行为可能导致拒绝服务情形。这<emphasis>只</emphasis>适用于 Windows 移植。
     </para></listitem>

<listitem><para>
      修复 8.0 中引入的缺陷，它可能使 ReadBuffer 返回已使用过的页当作新页，可能导致最近提交的数据丢失（Tom）
     </para></listitem>

<listitem><para>
      修复在事务之外或失败事务中发出协议级 Describe 消息的问题（Tom）
     </para></listitem>

<listitem><para>
      修复把不同字符组合视为相等的区域设置（如匈牙利语）下的字符串比较（Tom）
     </para>
<para>
      这可能需要 <command>REINDEX</command> 来修复文本列上的现有索引。
     </para></listitem>

<listitem><para>
      在 postmaster 启动期间设置区域设置环境变量，以确保 <application>plperl</application> 之后不会更改区域设置
     </para>
<para>
      这修复了 <application>postmaster</application> 启动时环境变量指定的区域设置与 <application>initdb</application> 所被告知的不同时发生的问题。在这些条件下，任何 <application>plperl</application> 的使用都可能导致索引损坏。如果你遇到过这种情况，可能需要 <command>REINDEX</command> 来修复文本列上的现有索引。
     </para></listitem>

<listitem><para>
      允许更灵活地重新定位安装目录（Tom）
     </para>
<para>
      以前的版本只有在所有安装目录路径除最后一个组件外都相同时才支持重新定位。
     </para></listitem>

<listitem><para>
      修复某些很少使用的亚洲多字节字符集中 strpos() 和正则表达式处理的长期缺陷（Tatsuo）
     </para></listitem>

<listitem><para>
      对返回 <literal>RECORD</literal> 的函数的各种修复（Tom）
     </para></listitem>

<listitem><para>
      修复 <filename>/contrib/pgcrypto</filename> gen_salt 中的缺陷，该缺陷使 MD5 和 XDES 算法不能使用全部可用盐空间（Marko Kreen、Solar Designer）
     </para>
<para>
      Blowfish 和标准 DES 的盐不受影响。
     </para></listitem>

<listitem><para>
      修复 <filename>/contrib/dblink</filename>，当指定的列数与查询实际返回的列数不同时抛出错误而不是崩溃（Joe）
     </para></listitem>

</itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-0-5">
  <title>Release 8.0.5</title>

  <formalpara>
  <title>Release date:</title>
  <para>2005-12-12</para>
  </formalpara>

  <para>
   本次发布包含对 8.0.4 的多方面修复。有关 8.0 大版本中新特性的信息，请参见 <xref linkend="release-8-0">。.
  </para>

  <sect2>
   <title>Migration to Version 8.0.5</title>

   <para>
      对于运行 8.0.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.0.3 的版本升级，请参阅 <xref linkend="release-8-0-3">。
     </para>
  </sect2>

  <sect2>
   <title>Changes</title>

<itemizedlist>

<listitem><para>
      修复事务日志管理中的竞态条件
     </para>
<para>
      存在一个窄窗口，其中可能为错误的页发起 I/O 操作，导致断言失败或数据损坏。
     </para>
</listitem>

<listitem><para>
      修复从错误恢复后 bgwriter 的问题（Tom）
     </para>
<para>
      发现后台写入器在写错误后泄漏缓冲区固定。虽然本身不致命，但这可能导致后续 VACUUM 命令的神秘阻塞。
     </para>
</listitem>

<listitem><para>
      防止客户端在当前事务已中止时发送 Bind 协议消息导致的失败
     </para></listitem>

<listitem><para>
      <filename>/contrib/ltree</filename> 修复（Teodor）
     </para></listitem>

<listitem><para>
      AIX 和 HPUX 编译修复（Tom）
     </para></listitem>

<listitem><para>
      在 Windows NO_SYSTEM_RESOURCES 错误后重试文件读写（Qingqing Zhou）
     </para></listitem>

<listitem><para>
      修复 <varname>log_line_prefix</varname> 包含 <literal>%i</literal> 时的间歇性失败
     </para></listitem>

<listitem><para>
      修复 Windows 上长脚本的 <application>psql</application> 性能问题（Merlin Moncure）
     </para></listitem>

<listitem><para>
      修复 <filename>pg_group</filename> 平面文件更新的缺失
     </para></listitem>

<listitem><para>
      修复外连接的长期规划错误
     </para>
<para>
      此缺陷有时导致虚假错误 <quote>RIGHT JOIN 只支持可归并连接的连接条件</quote>。
     </para></listitem>

<listitem><para>
      把时区初始化推迟到 <filename>postmaster.pid</filename> 创建之后
     </para>
<para>
      这避免让期待 pid 文件快速出现的启动脚本困惑。
     </para></listitem>

<listitem><para>
      防止表被删除后 <application>pg_autovacuum</application> 的核心转储
     </para></listitem>

<listitem><para>
      修复对子查询结果的整行引用（<literal>foo.*</literal>）的问题
     </para></listitem>
</itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-0-4">
  <title>Release 8.0.4</title>

  <formalpara>
  <title>Release date:</title>
  <para>2005-10-04</para>
  </formalpara>

  <para>
   本次发布包含对 8.0.3 的多方面修复。有关 8.0 大版本中新特性的信息，请参见 <xref linkend="release-8-0">。.
  </para>

  <sect2>
   <title>Migration to Version 8.0.4</title>

   <para>
      对于运行 8.0.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.0.3 的版本升级，请参阅 <xref linkend="release-8-0-3">。
     </para>
  </sect2>

  <sect2>
   <title>Changes</title>

<itemizedlist>
<listitem><para>
      修复允许 <command>VACUUM</command> 过早移除 <literal>ctid</literal> 链的错误，并在跟随 <literal>ctid</literal> 链接的代码中添加更多检查
     </para>
<para>
      这修复了一个在极罕见情况下可能导致崩溃的长期问题。
     </para></listitem>
<listitem><para>
      修复 <type>CHAR()</type> 在使用多字节字符集时正确填充空格到指定长度（Yoshiyuki Asaba）
     </para>
<para>
      在以前的版本中，<type>CHAR()</type> 的填充是不正确的，因为它只填充到指定的字节数而不考虑存储了多少字符。
     </para></listitem>
<listitem><para>
      在提交 <command>CREATE DATABASE</command> 前强制检查点
     </para>
<para>
      这应能修复最近报告的 <command>CREATE DATABASE</command> 之后不久发生崩溃时 <quote>索引不是 btree</quote>失败的报告。
     </para></listitem>
<listitem><para>
      修正 <command>COPY</command> 中只读事务测试的方向
     </para>
<para>
      以前的代码禁止 <command>COPY TO</command>，而它应禁止的是 <command>COPY FROM</command>。
     </para></listitem>
<listitem><para>
      处理 <command>COPY</command> CSV 模式输入中连续的嵌入换行符
     </para></listitem>
<listitem><para>
      修复年末附近日期的 <function>date_trunc(week)</function>
     </para></listitem>
<listitem><para>
      修复只引用内侧关系的外连接 ON 子句的规划问题
     </para></listitem>
<listitem><para>
      对 <literal>x FULL JOIN y ON true</literal> 极端情形的进一步修复
     </para></listitem>
<listitem><para>
      修复对 <literal>x IN (SELECT DISTINCT ...)</literal> 及相关情形的过度优化
     </para></listitem>
<listitem><para>
      修复因考虑不周的<quote>模糊</quote>代价比较导致小 <literal>LIMIT</literal> 值查询的错误规划
     </para></listitem>
<listitem><para>
      让 <function>array_in</function> 和 <function>array_recv</function> 更严格地验证其 OID 参数
     </para></listitem>
<listitem><para>
      修复列 <literal>a</literal> 上有 GiST 索引时 <literal>UPDATE a=... WHERE a...</literal> 之类查询丢行的问题
     </para></listitem>
<listitem><para>
      改进日期时间解析的健壮性
     </para></listitem>
<listitem><para>
      改进对部分写入 WAL 页的检查
     </para></listitem>
<listitem><para>
      改进启用 SSL 时信号处理的健壮性
     </para></listitem>
<listitem><para>
      改进 MIPS 和 M68K 自旋锁代码
     </para></listitem>
<listitem><para>
      postmaster 启动期间不要试图打开超过 <literal>max_files_per_process</literal> 个文件
     </para></listitem>
<listitem><para>
      各种内存泄漏修复
     </para></listitem>
<listitem><para>
      各种可移植性改进
     </para></listitem>
<listitem><para>
      更新时区数据文件
     </para></listitem>
<listitem><para>
      改进 Windows 上 DLL 装载失败的处理
     </para></listitem>
<listitem><para>
      改进 Windows 上的随机数生成
     </para></listitem>
<listitem><para>
      让 <literal>psql -f filename</literal> 在打开文件失败时返回非零退出码
     </para></listitem>
<listitem><para>
      更改 <application>pg_dump</application> 以更可靠地处理继承的检查约束
     </para></listitem>
<listitem><para>
      修复 Windows 上 <application>pg_restore</application> 的口令提示
     </para></listitem>
<listitem><para>
      修复 PL/pgSQL 在变量为按引用传递类型时正确处理 <literal>var := var</literal>
     </para></listitem>
<listitem><para>
      修复 PL/Perl 的 <literal>%_SHARED</literal>，使其真正共享
     </para></listitem>
<listitem><para>
      修复 <filename>contrib/pg_autovacuum</filename>，允许超过 2000 秒的休眠间隔
     </para></listitem>
<listitem><para>
      更新 <filename>contrib/tsearch2</filename> 以使用当前的 Snowball 代码
     </para></listitem>
</itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-0-3">
  <title>Release 8.0.3</title>

  <formalpara>
  <title>Release date:</title>
  <para>2005-05-09</para>
  </formalpara>

  <para>
      此发行版包含 8.0.2 之后的多种修复，包括几个与安全相关的问题。有关 8.0 主版本新特性的信息，请参阅 <xref linkend="release-8-0">。
     </para>

  <sect2>
   <title>Migration to Version 8.0.3</title>

   <para>
      对于运行 8.0.X 的用户，不需要进行转储/恢复。不过，它是处理在 8.0.X 系统目录初始内容中发现的两个重要安全问题的可能方法之一。用 8.0.3 的 initdb 做转储/initdb/重载序列会自动纠正这些问题。
     </para>

   <para>
      较大的安全问题是内建字符集编码转换函数可以由非特权用户从 SQL 命令调用，但这些函数并非为此设计，对恶意选择的参数不安全。修复包括更改这些函数的声明参数列表，使其不能再从 SQL 命令调用。（这不影响编码转换机制对它们的正常使用。）
     </para>

   <para>
      较小的问题是 <filename>contrib/tsearch2</filename> 模块创建了几个被错误声明为返回 <type>internal</type> 的函数，而它们并不接受 <type>internal</type> 参数。这破坏了所有使用 <type>internal</type> 参数的函数的类型安全。
     </para>

   <para>
      强烈建议所有安装通过 initdb 或按照下述手工修复程序修复这些错误。这些错误至少允许非特权数据库用户使其服务器进程崩溃，而且可能允许非特权用户获得数据库超级用户的权限。
     </para>

   <para>
      如果你不想做 initdb，请执行 <link linkend="release-7-4-8">7.4.8 发行说明</link>中所示的相同手工修复程序。
     </para>
  </sect2>

  <sect2>
   <title>Changes</title>

<itemizedlist>
<listitem><para>
      更改编码函数签名以防止误用
     </para></listitem>
<listitem><para>
      更改 <filename>contrib/tsearch2</filename> 以避免不安全地使用 <type>INTERNAL</type> 函数结果
     </para></listitem>
<listitem><para>
      防范 <function>record_out</function> 的错误第二参数
     </para></listitem>
<listitem><para>
      修复古老的竞态条件，它允许事务在某些用途（如 SELECT FOR UPDATE）上比其他用途稍早被视为已提交
     </para>
<para>
      这是一个极其严重的缺陷，因为它可能导致应用程序短暂看到表面的数据不一致。
     </para></listitem>
<listitem><para>
      修复关系扩展与 VACUUM 之间的竞态条件
     </para>
<para>
      这在理论上可能导致丢失一页刚插入的数据，尽管该情形概率似乎很低。没有已知案例造成超过断言失败的后果。
     </para></listitem>
<listitem><para>
      修复 <type>TIME WITH TIME ZONE</type> 值的比较
     </para>
<para>
      在使用了 <literal>--enable-integer-datetimes</literal> 配置开关的情形下比较代码是错误的。注意：如果你在 <type>TIME WITH TIME ZONE</type> 列上有索引，安装此更新后需要对它执行 <command>REINDEX</command>，因为该修复纠正了列值的排序顺序。
     </para></listitem>
<listitem><para>
      修复 <type>TIME WITH TIME ZONE</type> 值的 <function>EXTRACT(EPOCH)</function>
     </para></listitem>
<listitem><para>
      修复 <type>INTERVAL</type> 值负小数秒的错误显示
     </para>
<para>
      此错误只在使用了 <literal>--enable-integer-datetimes</literal> 配置开关时发生。
     </para></listitem>
<listitem><para>
      修复 pg_dump，正确转储包含 <literal>%</literal> 的触发器名（Neil）
     </para></listitem>
<listitem><para>
      <filename>contrib/intagg</filename> 的更多 64 位修复
     </para></listitem>
<listitem><para>
      防止对返回 <type>RECORD</type> 的函数的错误优化
     </para></listitem>
<listitem><para>
      防止 <literal>COALESCE(NULL,NULL)</literal> 的崩溃
     </para></listitem>
<listitem><para>
      修复 libpq 的 Borland makefile
     </para></listitem>
<listitem><para>
      修复 <filename>contrib/btree_gist</filename> 的 <type>timetz</type> 类型（Teodor）
     </para></listitem>
<listitem><para>
      让 <command>pg_ctl</command> 检查在 <filename>postmaster.pid</filename> 中找到的 PID，看它是否仍是活进程
     </para></listitem>
<listitem><para>
      修复因添加转储时间戳导致的 <command>pg_dump</command>/<command>pg_restore</command> 问题
     </para></listitem>
<listitem><para>
      修复事务提交期间物化可保持游标与触发延迟触发器之间的相互作用
     </para></listitem>
<listitem><para>
      修复返回按引用传递数据类型的 SQL 函数中的内存泄漏
     </para></listitem>
</itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-0-2">
  <title>Release 8.0.2</title>

  <formalpara>
  <title>Release date:</title>
  <para>2005-04-07</para>
  </formalpara>

  <para>
   本次发布包含对 8.0.1 的多方面修复。有关 8.0 大版本中新特性的信息，请参见 <xref linkend="release-8-0">。.
  </para>

  <sect2>
   <title>Migration to Version 8.0.2</title>

   <para>
      对于运行 8.0.* 的用户，不需要进行转储/恢复。此发行版更新了 <productname>PostgreSQL</productname> 库的主版本号，因此如果某些用户应用程序找不到正确编号的共享库，可能需要重新链接它们。
     </para>
  </sect2>

  <sect2>
   <title>Changes</title>

<itemizedlist>
<listitem><para>
      递增所有接口库的主版本号（Bruce）
     </para>
<para>
      这本应在 8.0.0 中完成。需要它才能使 7.4.X 版本的 PostgreSQL 客户端应用程序（如 <application>psql</application>）能与 8.0.X 应用程序在同一台机器上使用。这可能需要重新链接使用这些库的用户应用程序。
     </para></listitem>
<listitem><para>
      新增仅 Windows 的 <varname>wal_sync_method</varname> 设置 <option>fsync_writethrough</option>（Magnus、Bruce）
     </para>
<para>
      此设置使 <productname>PostgreSQL</productname> 在写 WAL 时穿透磁盘驱动器的任何写缓存。此行为以前称为 <option>fsync</option>，但因它在其他平台上的行为与 <option>fsync</option> 大不相同而被改名。
     </para>
</listitem>
<listitem><para>
      在 Windows 上启用 <varname>wal_sync_method</varname> 的 <option>open_datasync</option> 设置，并使其成为该平台的默认（Magnus、Bruce）
     </para>
<para>
      由于默认不再是 <option>fsync_writethrough</option>，如果磁盘驱动器启用了写缓存，断电期间可能丢失数据。要在 Windows 上关闭写缓存，请在<application>设备管理器</application>中选择驱动器属性，然后选择<literal>策略</literal>。
     </para>
</listitem>
<listitem><para>
      新缓存管理算法 <acronym>2Q</acronym> 取代 <acronym>ARC</acronym>（Tom）
     </para>
<para>
      这是为了避免 <acronym>ARC</acronym> 的一项待决美国专利。对某些负载，<acronym>2Q</acronym> 代码可能比 <acronym>ARC</acronym> 慢几个百分点。更好的缓存管理算法将出现在 8.1 中。
     </para></listitem>
<listitem><para>
      规划器调整以改进新建表上的行为（Tom）
     </para></listitem>
<listitem><para>
      允许 plpgsql 给初始为 <literal>NULL</literal> 的数组的元素赋值（Tom）
     </para>
<para>
      此前数组会保持 <literal>NULL</literal>，但现在它变成单元素数组。主 SQL 引擎在 8.0 中已改为以这种方式处理空数组值的 <command>UPDATE</command>，但 plpgsql 中的类似情形被遗漏了。
     </para>
</listitem>
<listitem><para>
      把 plpython 函数体中的 <literal>\r\n</literal> 和 <literal>\r</literal> 转换为 <literal>\n</literal>（Michael Fuhr）
     </para>
<para>
      这防止在 Windows 或 Mac 客户端上编写 plpython 代码时出现语法错误。
     </para>
</listitem>
<listitem><para>
      允许 SPI 游标处理返回行的工具命令，如 <command>EXPLAIN</command>（Tom）
     </para></listitem>
<listitem><para>
      修复 <command>ALTER TABLE SET WITHOUT OIDS</command> 之后 <command>CLUSTER</command> 的失败（Tom）
     </para></listitem>
<listitem><para>
      降低 <command>ALTER TABLE ADD COLUMN</command> 的内存使用（Neil）
     </para></listitem>
<listitem><para>
      修复 <command>ALTER LANGUAGE RENAME</command>（Tom）
     </para></listitem>
<listitem><para>
      记录 <application>pg_ctl</application> 仅 Windows 的 <literal>register</literal> 和 <literal>unregister</literal> 选项（Magnus）
     </para></listitem>
<listitem><para>
      确保后端关闭期间完成的操作被统计收集器计数
     </para>
<para>
      这有望解决关于 <application>pg_autovacuum</application> 不够频繁地清理系统目录的报告——它没有被通知后端退出期间临时表删除导致的目录删除。
     </para></listitem>
<listitem><para>
      把 Windows 上配置参数 <varname>log_destination</varname> 的默认改为 <option>eventlog</option>（Magnus）
     </para>
<para>
      默认情况下，在 Windows 上运行的服务器现在将把日志输出发送到 Windows 事件记录器而不是标准错误。
     </para></listitem>
<listitem><para>
      使 Kerberos 认证在 Windows 上工作（Magnus）
     </para></listitem>
<listitem><para>
      允许没有 CREATEDB 权限标志的超级用户执行 <command>ALTER DATABASE RENAME</command>（Tom）
     </para></listitem>
<listitem><para>
      修改 <command>CREATE</command> 和 <command>DROP DATABASE</command> 的 WAL 日志条目，不指定绝对路径（Tom）
     </para>
<para>
      这允许在数据库位置可能不同的另一台机器上做时间点恢复。注意 <command>CREATE TABLESPACE</command> 在这种情形下仍有风险。
     </para></listitem>
<listitem><para>
      修复后端带着创建过表并对其打开游标的未决事务退出时的崩溃（Tom）
     </para></listitem>
<listitem><para>
      修复 <function>array_map()</function>，使其能调用 PL 函数（Tom）
     </para></listitem>
<listitem><para>
      若干 <filename>contrib/tsearch2</filename> 和 <filename>contrib/btree_gist</filename> 修复（Teodor）
     </para></listitem>
<listitem><para>
      修复某些平台上一些 <filename>contrib/pgcrypto</filename> 函数的崩溃（Marko Kreen）
     </para></listitem>
<listitem><para>
      修复 <filename>contrib/intagg</filename> 在 64 位平台上的问题（Tom）
     </para></listitem>
<listitem><para>
      修复 ecpg 解析 <command>CREATE</command> 语句的缺陷（Michael）
     </para></listitem>
<listitem><para>
      绕过 powerpc 和 amd64 上的 gcc 缺陷，它导致 ecpg 中的问题（Christof Petig）
     </para></listitem>
<listitem><para>
      当区域设置为 <literal>C</literal> 时不使用区域感知版本的 <function>upper()</function>、<function>lower()</function> 和 <function>initcap()</function>（Bruce）
     </para>
<para>
      这使这些函数能在区域设置为 <literal>C</literal> 时对非 7 位数据报错的平台上工作。
     </para></listitem>
<listitem><para>
      修复 <function>quote_ident()</function>，给匹配关键字的名称加引号（Tom）
     </para></listitem>
<listitem><para>
      修复 <function>to_date()</function> 在同时使用 <literal>CC</literal> 和 <literal>YY</literal> 字段时合理行事（Karel）
     </para></listitem>
<listitem><para>
      防止 <function>to_char(interval)</function> 在给定零月 interval 时失败（Tom）
     </para></listitem>
<listitem><para>
      修复 <function>date_trunc('week')</function> 返回错误星期的问题（Bruce）
     </para>
<para>
      <function>date_trunc('week')</function> 在某些年份对一月的前几天返回错误的年份。
     </para></listitem>
<listitem><para>
      为 <type>INET</type> 数据类型的 <literal>D</literal> 类地址使用正确的默认掩码长度（Tom）
     </para></listitem>
</itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-0-1">
  <title>Release 8.0.1</title>

  <formalpara>
  <title>Release date:</title>
  <para>2005-01-31</para>
  </formalpara>

  <para>
      此发行版包含 8.0.0 之后的多种修复，包括几个与安全相关的问题。有关 8.0 主版本新特性的信息，请参阅 <xref linkend="release-8-0">。
     </para>

  <sect2>
   <title>Migration to Version 8.0.1</title>

   <para>
      对于运行 8.0.0 的用户，不需要进行转储/恢复。
     </para>
  </sect2>

  <sect2>
   <title>Changes</title>

<itemizedlist>
<listitem><para>
      不允许非超级用户使用 <command>LOAD</command>
     </para>
<para>
      在会自动执行共享库初始化函数的平台（至少包括 Windows 和基于 ELF 的 Unix）上，<command>LOAD</command> 可被用来让服务器执行任意代码。感谢 NGS Software 报告此问题。
     </para></listitem>
<listitem><para>
      检查聚合函数的创建者是否有权执行指定的转换函数
     </para>
<para>
      这一疏漏使得绕过函数 EXECUTE 权限的拒绝成为可能。
     </para></listitem>
<listitem><para>
      修复 contrib/intagg 的安全和 64 位问题
     </para></listitem>
<listitem><para>
      为一些 contrib 函数添加所需的 STRICT 标记（Kris Jurka）
     </para></listitem>
<listitem><para>
      避免 plpgsql 游标声明参数过多时的缓冲区溢出（Neil）
     </para></listitem>
<listitem><para>
      让 <command>ALTER TABLE ADD COLUMN</command> 在所有情形下强制执行域约束
     </para></listitem>
<listitem><para>
      修复 FULL 和 RIGHT 外连接的规划错误
     </para>
<para>
      连接的结果被错误地认为与左输入的排序相同。这不仅会向用户交付排序错误的输出，而且在嵌套归并连接的情形下可能给出完全错误的答案。
     </para></listitem>
<listitem><para>
      改进分组聚合查询的规划
     </para></listitem>
<listitem><para>
      <command>ROLLBACK TO <replaceable>savepoint</replaceable></command> 关闭自保存点以来创建的游标
     </para></listitem>
<listitem><para>
      修复 Windows 上后端栈大小不足的问题
     </para></listitem>
<listitem><para>
      在 Windows 上避免 SHGetSpecialFolderPath()（Magnus）
     </para></listitem>
<listitem><para>
      修复作为 Windows 服务运行 pg_autovacuum 的一些问题（Dave Page）
     </para></listitem>
<listitem><para>
      pg_dump/pg_restore 的多个小缺陷修复
     </para></listitem>
<listitem><para>
      修复 ecpg 在 typedef 中使用命名结构时的段错误（Michael）
     </para></listitem>
</itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-0">
  <title>Release 8.0</title>

  <formalpara>
   <title>Release date:</title>
   <para>2005-01-19</para>
  </formalpara>

  <sect2>
   <title>Overview</title>

   <para>
      此发行版的主要变更：
     </para>

   <variablelist>
    <varlistentry>
     <term>
      Microsoft Windows Native Server
     </term>

     <listitem>
      <para>
      这是第一个作为服务器原生运行于 <trademark class="registered">Microsoft Windows</trademark> 上的 <productname>PostgreSQL</productname> 版本。它可以作为 <productname>Windows</productname> 服务运行。此版本支持基于 NT 的 Windows 版本，如 <productname>Windows 2000 SP4</productname>、<productname>Windows XP</productname> 和 <productname>Windows 2003</productname>。更老的版本如 <productname>Windows 95</productname>、<productname>Windows 98</productname> 和 <productname>Windows ME</productname> 不受支持，因为这些操作系统没有支持 <productname>PostgreSQL</productname> 的基础设施。已创建单独的安装程序项目以简化在 <productname>Windows</productname> 上的安装——参见 <ulink url="http://www.postgresql.org/ftp/win32/"></ulink>。
     </para>

      <para>
      虽然在整个发布周期中都做了测试，但 Windows 移植没有 <productname>PostgreSQL</productname> 在 Unix 平台上多年生产环境使用的积累。因此应像对待新产品一样谨慎对待它。
     </para>

      <para>
      以前的版本需要 Unix 仿真工具包 <productname>Cygwin</productname> 才能在 Windows 操作系统上运行服务器。<productname>PostgreSQL</productname> 在 Windows 上的本机客户端支持已有多年。
     </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>
      Savepoints
     </term>

     <listitem>
      <para>
      保存点允许中止事务的特定部分而不影响事务的其余部分。以前的版本没有这种能力；事务内的语句失败除了中止整个事务外无法恢复。此特性对需要在复杂事务内进行错误恢复的应用程序作者很有价值。
     </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>
      Point-In-Time Recovery
     </term>

     <listitem>
      <para>
      在以前的版本中，除了从以前的备份恢复或使用备用复制服务器外，无法从磁盘驱动器故障中恢复。时间点恢复允许对服务器做连续备份。你可以恢复到故障点或过去的某个事务。
     </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>
      Tablespaces
     </term>

     <listitem>
      <para>
      表空间允许管理员为各个表、索引和数据库的存储选择不同的文件系统。这改进了性能和对磁盘空间使用的控制。以前的版本使用 <application>initlocation</application> 和手工符号链接管理来完成此类任务。
     </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>
      Improved Buffer Management, <command>CHECKPOINT</command>,
      <command>VACUUM</command>
     </term>

     <listitem>
      <para>
      此版本有更智能的缓冲区替换策略，能更好地利用可用共享缓冲区并改进性能。清理和检查点的性能影响也减轻了。
     </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>
      Change Column Types
     </term>

     <listitem>
      <para>
      现在可以用 <command>ALTER TABLE</command> 更改列的数据类型。
     </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>
      New Perl Server-Side Language
     </term>

     <listitem>
      <para>
      新版本的 <application>plperl</application> 服务器端语言现在支持持久共享存储区、触发器、返回记录和记录数组以及访问数据库的 SPI 调用。
     </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>
       Comma-separated-value (CSV) support in <command>COPY</command>
     </term>

     <listitem>
      <para>
      <command>COPY</command> 现在可以读写逗号分隔值文件。它还能灵活解释非标准的引号和分隔字符。
     </para>
     </listitem>
    </varlistentry>

   </variablelist>
  </sect2>

  <sect2>
   <title>Migration to Version 8.0</title>

   <para>
      希望从任何以前的版本迁移数据的用户，需要使用 <application>pg_dump</application> 进行转储/恢复。
     </para>

   <para>
      请注意以下不兼容之处：
     </para>

   <itemizedlist>

    <listitem>
     <para>
      在 <option>READ COMMITTED</option> 序列化模式下，易变函数现在能看到并发事务提交到函数内每条语句开始处为止的结果，而不是到调用函数的交互命令开始处为止。
     </para>
    </listitem>

    <listitem>
     <para>
      声明为 <option>STABLE</option> 或 <option>IMMUTABLE</option> 的函数总是使用调用查询的快照，因此看不到调用查询开始之后采取的动作的效果，无论在其自身事务还是其他事务中。这样的函数也必须是只读的，即它不能使用 <command>SELECT</command> 之外的任何 SQL 命令。
     </para>
    </listitem>

    <listitem>
     <para>
      非延迟的 <option>AFTER</option> 触发器现在在触发查询完成后立即触发，而不是在当前交互命令完成时。当触发查询发生在函数内时这就有区别：触发器在函数继续其下一个操作之前被调用。
     </para>
    </listitem>

    <listitem>
     <para>
      服务器配置参数 <varname>virtual_host</varname> 和 <varname>tcpip_socket</varname> 已被更通用的参数 <varname>listen_addresses</varname> 取代。此外，服务器现在默认监听 <literal>localhost</literal>，这在许多场景中消除了对 <literal>-i</literal> postmaster 开关的需要。
     </para>
    </listitem>

    <listitem>
     <para>
      服务器配置参数 <varname>SortMem</varname> 和 <varname>VacuumMem</varname> 已改名为 <varname>work_mem</varname> 和 <varname>maintenance_work_mem</varname>，以更好地反映其用途。原名在 <command>SET</command> 和 <command>SHOW</command> 中仍受支持。
     </para>
    </listitem>

    <listitem>
     <para>
      服务器配置参数 <varname>log_pid</varname>、<varname>log_timestamp</varname> 和 <varname>log_source_port</varname> 已被更通用的参数 <varname>log_line_prefix</varname> 取代。
     </para>
    </listitem>

    <listitem>
     <para>
      服务器配置参数 <varname>syslog</varname> 已被更合逻辑的 <varname>log_destination</varname> 变量取代，以控制日志输出目的地。
     </para>
    </listitem>

    <listitem>
     <para>
      服务器配置参数 <varname>log_statement</varname> 已更改，可以有选择地只记录数据库修改或数据定义语句。服务器配置参数 <varname>log_duration</varname> 现在只在 <varname>log_statement</varname> 打印查询时打印。
     </para>
    </listitem>

    <listitem>
     <para>
      服务器配置参数 <varname>max_expr_depth</varname> 已被 <varname>max_stack_depth</varname> 取代，后者测量物理栈大小而不是表达式嵌套深度。这有助于防止因递归函数导致的栈溢出造成的会话终止。
     </para>
    </listitem>

    <listitem>
     <para>
      <function>length()</function> 函数不再统计 <type>CHAR(n)</type> 值中的尾随空格。
     </para>
    </listitem>

   <listitem>
    <para>
      把整数转换为 <type>BIT(N)</type> 现在选取整数最右边的 N 位，而不是像以前那样最左边 N 位。
     </para>
   </listitem>

   <listitem>
    <para>
      更新 NULL 数组值的元素或切片现在产生非空数组结果，即只包含被赋值位置的数组。
     </para>
   </listitem>

    <listitem>
     <para>
      数组输入值的语法检查已大幅收紧。以前在奇怪位置被允许并产生奇怪结果的垃圾现在会导致错误。空字符串元素值现在必须写成 <literal>""</literal>，而不是什么也不写。还更改了数组元素周围空白的行为：尾随空白现在被忽略，与前导空白（一直被忽略）对称。
     </para>
    </listitem>

    <listitem>
     <para>
      整数算术运算中的溢出现在被检测并报告为错误。
     </para>
    </listitem>

    <listitem>
     <para>
      与单字节 <type>"char"</type> 数据类型相关的算术操作符已被移除。
     </para>
    </listitem>

    <listitem>
     <para>
      <function>extract()</function> 函数（也称 <function>date_part</function>）现在为 BC 日期返回正确的年份。此前它返回比正确年份小一的值。该函数现在还为千年和世纪返回正确的值。
     </para>
    </listitem>

   <listitem>
    <para>
      <type>CIDR</type> 值现在必须使其未掩码的位为零。例如，我们不再允许 <literal>204.248.199.1/31</literal> 作为 <type>CIDR</type> 值。这类值本就不应被 <productname>PostgreSQL</productname> 接受，现在将被拒绝。
     </para>
   </listitem>

    <listitem>
     <para>
      <command>EXECUTE</command> 现在返回与被执行语句匹配的完成标签。
     </para>
    </listitem>

    <listitem>
     <para>
      <application>psql</application> 的 <command>\copy</command> 命令现在读写查询的 <literal>stdin/stdout</literal>，而不是 <application>psql</application> 的 <literal>stdin/stdout</literal>。以前的行为可通过新的 <option>pstdin</option>/<option>pstdout</option> 参数访问。
     </para>
    </listitem>

    <listitem>
     <para>
      JDBC 客户端接口已从核心发行版中移除，现托管于 <ulink url= "http://jdbc.postgresql.org"></ulink>。
     </para>
    </listitem>

    <listitem>
     <para>
      Tcl 客户端接口也被移除。现在有几个 Tcl 接口托管于 <ulink url= "http://gborg.postgresql.org"></ulink>。
     </para>
    </listitem>

    <listitem>
     <para>
      服务器现在使用自己的时区数据库，而不是操作系统提供的。这将在所有平台上提供一致的行为。在大多数情况下，时区行为应没有明显差异，只是 <command>SET</command>/<command>SHOW</command> <varname>TimeZone</varname> 使用的时区名可能与你的平台提供的不同。
     </para>
    </listitem>

    <listitem>
     <para>
      <application>Configure</application> 的线程选项不再要求用户运行测试或编辑配置文件；线程选项现在自动检测。'
     </para>
    </listitem>

    <listitem>
     <para>
      既然表空间已经实现，<application>initlocation</application> 已被移除。
     </para>
    </listitem>

    <listitem>
     <para>
      用户定义 GiST 索引的 API 已更改。Union 和 PickSplit 方法现在传递指向特殊 <structname>GistEntryVector</structname> 结构的指针，而不是 <type>bytea</type>。
     </para>
    </listitem>

   </itemizedlist>
  </sect2>

 <sect2>
  <title>Deprecated Features</title>

  <para>
      <productname>PostgreSQL</productname> 行为的某些方面已被确定为次优。为了向后兼容，它们在 8.0 中没有被移除，但被认为已废弃，将在下一个主版本中移除。
     </para>

   <itemizedlist>
    <listitem>
     <para>
      8.1 版本将移除 interval 的 <function>to_char()</function> 函数。
     </para>
    </listitem>

     <listitem>
      <para>
      服务器现在对传递给 <type>oid</type>/<type>float4</type>/<type>float8</type> 数据类型的空字符串发出警告，但仍像以前一样把它们解释为零。在下一个主版本中，空字符串将被视为这些数据类型的无效输入。
     </para>
     </listitem>

    <listitem>
     <para>
      默认情况下，<productname>PostgreSQL</productname> 8.0 及更早版本中的表创建时带 <type>OID</type>。在下一个版本中，情况将<emphasis>不</emphasis>再如此：要创建包含 <type>OID</type> 的表，必须指定 <option>WITH OIDS</option> 子句或设置 <varname>default_with_oids</varname> 配置参数。鼓励用户在表需要 OID 时显式指定 <option>WITH OIDS</option>，以与 <productname>PostgreSQL</productname> 的未来版本兼容。
     </para>
    </listitem>

   </itemizedlist>
  </sect2>

  <sect2>
   <title>Changes</title>

   <para>
      下面你将找到 8.0 版本与上一个主版本之间变更的详细说明。
     </para>

   <sect3>
    <title>Performance Improvements</title>
    <itemizedlist>

     <listitem>
      <para>
      支持跨数据类型的索引使用（Tom）
     </para>
      <para>
      在此变更之前，如果数据类型不完全匹配，许多查询不会使用索引。此改进使索引使用更直观一致。
     </para>
     </listitem>

     <listitem>
      <para>
      改进缓存的新缓冲区替换策略（Jan）
     </para>
      <para>
      以前的版本使用最近最少使用（LRU）缓存把最近引用的页保留在内存中。LRU 算法不考虑特定缓存条目被访问的次数，因此大表扫描可能驱逐有用的缓存页。新缓存算法使用四个独立的列表来跟踪最近使用和最常使用的缓存页，并根据负载动态优化其替换。这应能带来对共享缓冲区缓存高效得多的利用。过去测试过共享缓冲区大小的管理员应使用此新缓存替换策略重新测试。
     </para>
     </listitem>

     <listitem>
      <para>
      新增周期性写脏缓冲区的子进程以减少检查点写入（Jan）
     </para>
      <para>
      在以前的版本中，每隔几分钟运行一次的检查点进程会把所有脏缓冲区写入操作系统的缓冲区缓存，然后把所有脏的操作系统缓冲区刷到磁盘。这导致周期性的磁盘使用尖峰，常常损害性能。新代码使用后台写入器以稳定的节奏细流式写磁盘，因此检查点要写到磁盘的脏页少得多。此外，新代码不发出全局 <function>sync()</function> 调用，而是只对自上次检查点以来写入的文件执行 <function>fsync()</function>。这应能改进性能并使检查点期间的退化最小化。
     </para>
     </listitem>

     <listitem>
      <para>
      新增延长清理时间以降低性能影响的能力（Jan）
     </para>
      <para>
      在繁忙系统上，<command>VACUUM</command> 执行许多 I/O 请求，可能损害其他用户的性能。此版本允许放慢 <command>VACUUM</command> 以降低对其他用户的影响，尽管这会增加 <command>VACUUM</command> 的总时长。
     </para>
     </listitem>

     <listitem>
      <para>
      改进重复键的 B-tree 索引性能（Dmitry Tkach、Tom）
     </para>
      <para>
      这改进了索引中存在许多重复值时扫描索引的方式。
     </para>
     </listitem>

     <listitem>
      <para>
      规划时使用动态生成的表大小估计（Tom）
     </para>
      <para>
      此前规划器使用最后一次 <command>VACUUM</command> 或 <command>ANALYZE</command> 看到的值来估计表大小，包括物理表大小（页数）和行数。现在，当前物理表大小从内核获得，行数通过把表大小乘以最后一次 <command>VACUUM</command> 或 <command>ANALYZE</command> 看到的行密度（每页行数）来估计。在表大小自上次维护命令以来变化显著的情形下，这应能产生更可靠的估计。
     </para>
     </listitem>

     <listitem>
      <para>
      改进 <literal>OR</literal> 子句的索引使用（Tom）
     </para>
      <para>
      这允许优化器在有许多 OR 子句的语句中使用索引，这些语句过去不会被索引。它还可以使用多列索引，其中第一列被指定而第二列是 <literal>OR</literal> 子句的一部分。
     </para>
     </listitem>

     <listitem>
      <para>
      改进部分索引子句的匹配（Tom）
     </para>
      <para>
      服务器现在更善于在涉及复杂 <option>WHERE</option> 子句的查询中使用部分索引。
     </para>
     </listitem>

     <listitem>
      <para>
      改进 GEQO 优化器的性能（Tom）
     </para>
      <para>
      GEQO 优化器用于规划涉及许多表的查询（默认十二个或更多）。此版本加快了查询分析方式，以减少花在优化上的时间。
     </para>
     </listitem>

     <listitem>
      <para>
      其他优化器改进
     </para>
      <para>
      这里没有篇幅列出所做的所有小改进，但许多特殊情形比以前的版本工作得更好。
     </para>
     </listitem>

     <listitem>
      <para>
      改进 C 函数的查找速度（Tom）
     </para>
      <para>
      此版本使用哈希表查找动态装载的 C 函数的信息。这改进了它们的速度，使其几乎与内建在服务器可执行文件中的函数一样快。
     </para>
     </listitem>

     <listitem>
      <para>
      新增类型特定的 <command>ANALYZE</command> 统计能力（Mark Cave-Ayland）
     </para>
      <para>
      此特性允许为非标准数据类型生成统计信息时有更多灵活性。
     </para>
     </listitem>

     <listitem>
      <para>
      <command>ANALYZE</command> 现在收集表达式索引的统计信息（Tom）
     </para>
      <para>
      表达式索引（也称函数索引）允许用户不仅索引列，还索引表达式和函数调用的结果。有了此版本，优化器可以收集和使用有关表达式索引内容的统计信息。这将大幅改进表达式索引相关查询的规划质量。
     </para>
     </listitem>

     <listitem>
      <para>
      <command>ANALYZE</command> 的新两阶段采样方法（Manfred Koizar）
     </para>
      <para>
      当表中不同区域有效行的密度差异很大时，这给出更好的统计信息。
     </para>
     </listitem>

     <listitem>
      <para>
      加快 <command>TRUNCATE</command>（Tom）
     </para>
      <para>
      这挽回了一些 7.4 中观察到的性能损失，同时仍保持 <command>TRUNCATE</command> 事务安全。
     </para>
     </listitem>

    </itemizedlist>
   </sect3>


   <sect3>
    <title>Server Changes</title>
    <itemizedlist>

     <listitem>
      <para>
      新增 WAL 文件归档和时间点恢复（Simon Riggs）
     </para>
     </listitem>

     <listitem>
      <para>
      新增表空间，使管理员能控制磁盘布局（Gavin）
     </para>
     </listitem>

     <listitem>
      <para>
      新增内建日志轮转程序（Andreas Pflug）
     </para>
      <para>
      现在可以方便地记录服务器消息，而不依赖 <application>syslog</application> 或外部日志轮转程序。
     </para>
     </listitem>

     <listitem>
      <para>
      新增只读服务器配置参数以显示服务器编译期设置：<varname>block_size</varname>、<varname>integer_datetimes</varname>、<varname>max_function_args</varname>、<varname>max_identifier_length</varname>、<varname>max_index_keys</varname>（Joe）
     </para>
     </listitem>

     <listitem>
      <para>
      使 <literal>sameuser</literal>、<literal>samegroup</literal> 和 <literal>all</literal> 的加引号能移除这些词在 <filename>pg_hba.conf</filename> 中的特殊含义（Andrew）
     </para>
     </listitem>

     <listitem>
      <para>
      在默认 <filename>pg_hba.conf</filename> 中为 <literal>localhost</literal> 使用更清晰的 IPv6 名称 <literal>::1/128</literal>（Andrew）
     </para>
     </listitem>

     <listitem>
      <para>
      在 <filename>pg_hba.conf</filename> 示例中使用 CIDR 格式（Andrew）
     </para>
     </listitem>

     <listitem>
      <para>
      把服务器配置参数 <varname>SortMem</varname> 和 <varname>VacuumMem</varname> 改名为 <varname>work_mem</varname> 和 <varname>maintenance_work_mem</varname>（旧名仍受支持）（Tom）
     </para>
      <para>
      此变更是为了澄清索引和外键创建等批量操作使用 <varname>maintenance_work_mem</varname>，而 <varname>work_mem</varname> 用于查询执行期间使用的工作区。
     </para>
     </listitem>

     <listitem>
      <para>
      允许用服务器配置 <varname>log_disconnections</varname> 记录会话断开（Andrew）
     </para>
     </listitem>

     <listitem>
      <para>
      新增服务器配置参数 <varname>log_line_prefix</varname>，允许控制每行日志中发出的信息（Andrew）
     </para>
      <para>
      可用信息包括用户名、数据库名、远程 IP 地址和会话开始时间。
     </para>
     </listitem>

     <listitem>
      <para>
      移除服务器配置参数 <varname>log_pid</varname>、<varname>log_timestamp</varname>、<varname>log_source_port</varname>；功能被 <varname>log_line_prefix</varname> 取代（Andrew）
     </para>
     </listitem>

     <listitem>
      <para>
      用统一的 <varname>listen_addresses</varname> 参数取代 <varname>virtual_host</varname> 和 <varname>tcpip_socket</varname> 参数（Andrew、Tom）
     </para>
      <para>
      <varname>virtual_host</varname> 只能指定监听单个 IP 地址。<varname>listen_addresses</varname> 允许指定多个地址。
     </para>
     </listitem>

     <listitem>
      <para>
      默认监听 localhost，这在许多场景中消除了对 <option>-i</option> postmaster 开关的需要（Andrew）
     </para>
      <para>
      监听 localhost（<literal>127.0.0.1</literal>）不会打开新的安全漏洞，但允许 Windows 和 JDBC 等不支持本地套接字的配置无需特殊调整即可工作。
     </para>
     </listitem>

     <listitem>
      <para>
      移除 <varname>syslog</varname> 服务器配置参数，并新增更合逻辑的 <varname>log_destination</varname> 变量来控制日志输出位置（Magnus）
     </para>
     </listitem>

     <listitem>
      <para>
      更改服务器配置参数 <varname>log_statement</varname>，取值 <varname>all</varname>、<varname>mod</varname>、<varname>ddl</varname> 或 <varname>none</varname>，以选择记录哪些查询（Bruce）
     </para>
      <para>
      这允许管理员只记录数据定义更改或只记录数据修改语句。
     </para>
     </listitem>

     <listitem>
      <para>
      一些与日志相关的配置参数以前可由普通用户调整，但只能向<quote>更详细</quote>方向。现在对它们的处理更严格：只有超级用户能设置。但超级用户可以用 <command>ALTER USER</command> 为非超级用户提供这些值的逐用户设置。此外，超级用户现在还可以通过 <literal>PGOPTIONS</literal> 设置仅超级用户配置参数的值。
     </para>
     </listitem>

     <listitem>
      <para>
      允许把配置文件放在数据目录之外（mlw）
     </para>
      <para>
      默认情况下，配置文件保存在集群的顶级目录。有了此新增，配置文件可以放在数据目录之外，便于管理。
     </para>
     </listitem>

     <listitem>
      <para>
      只在首次执行时规划预备查询，使常数可用于统计（Oliver Jowett）
     </para>
      <para>
      预备语句一次规划查询并多次执行。虽然预备查询避免了每次使用时重新规划的开销，但计划质量因不知道查询中将使用的确切参数而受损。在此版本中，未命名预备语句的规划推迟到首次执行，并把该执行的实际参数值用作优化提示。这允许使用外线参数传递而不带来性能损失。
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <command>DECLARE CURSOR</command> 接受参数（Oliver Jowett）
     </para>
      <para>
      现在在带参数的 <function>Parse</function> 消息中发出 <command>DECLARE CURSOR</command> 是有用的。<function>Bind</function> 时发送的参数值将被代入游标查询的执行。
     </para>
     </listitem>

     <listitem>
      <para>
      修复 <type>inet</type> 和 <type>cidr</type> 数据类型的哈希连接和聚合（Tom）
     </para>
      <para>
      7.4 版本对混合 <type>inet</type> 和 <type>cidr</type> 值的哈希处理不正确。（此缺陷在以前的版本中不存在，因为它们不会尝试哈希这两种数据类型。）
     </para>
     </listitem>

     <listitem>
      <para>
      让 <varname>log_duration</varname> 只在 <varname>log_statement</varname> 打印查询时打印（Ed L.）
     </para>
     </listitem>

    </itemizedlist>
   </sect3>


   <sect3>
    <title>Query Changes</title>
    <itemizedlist>

     <listitem>
      <para>
      新增保存点（嵌套事务）（Alvaro）
     </para>
     </listitem>

     <listitem>
      <para>
      不支持的隔离级别现在被接受并提升到最接近的受支持级别（Peter）
     </para>
      <para>
      SQL 规范规定，如果数据库不支持特定隔离级别，应使用下一个更严格的级别。此变更遵循该建议。
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <command>BEGIN WORK</command> 像 <command>START TRANSACTION</command> 那样指定事务隔离级别（Bruce）
     </para>
     </listitem>

     <listitem>
      <para>
      修复规则生成的查询类型与最初提交的查询不同情形下的表权限检查（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      实现美元引用以简化单引号的使用（Andrew、Tom、David Fetter）
     </para>
      <para>
      在以前的版本中，由于必须用单引号引用函数体，函数文本中单引号的使用需要用两个单引号或其他易错的记法。有了此版本，我们新增了用"美元引用"引用文本块的能力。在不同嵌套级别使用不同引用定界符的能力大幅简化了正确引用的任务，特别是在复杂函数中。美元引用可用于任何需要引用文本的地方。
     </para>
     </listitem>

     <listitem>
      <para>
      让 <literal>CASE val WHEN compval1 THEN ...</literal> 只求值 <literal>val</literal> 一次（Tom）
     </para>
      <para>
      <option>CASE</option> 不再多次求值被测试的表达式。当表达式复杂或易变时这有好处。
     </para>
     </listitem>

     <listitem>
      <para>
      在计算聚合查询的目标列表之前先测试 <option>HAVING</option>（Tom）
     </para>
      <para>
      修复诸如 <literal>SELECT SUM(win)/SUM(lose) ... GROUP BY ... HAVING SUM(lose) &gt; 0</literal> 之类情形的不当失败。这本应工作，但此前可能因除以零而失败。
     </para>
     </listitem>

     <listitem>
      <para>
      用 <varname>max_stack_depth</varname> 参数（以栈大小千字节计）取代 <varname>max_expr_depth</varname> 参数（Tom）
     </para>
      <para>
      这为防止因失控递归函数而崩溃提供了相当可靠的防御。我们现在直接测量执行栈的大小，而不是测量表达式嵌套的深度。
     </para>
     </listitem>

     <listitem>
      <para>
      允许任意行表达式（Tom）
     </para>
      <para>
      此版本允许 SQL 表达式包含任意组合类型，即行值。它还允许函数更容易地接受行作为参数并返回行值。
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <option>LIKE</option>/<option>ILIKE</option> 用作行和子查询比较中的操作符（Fabien Coelho）
     </para>
     </listitem>

     <listitem>
      <para>
      避免标识符和关键字中的基本 ASCII 字母的区域特定大小写转换（Tom）
     </para>
      <para>
      这解决了包含 <literal>I</literal> 和 <literal>i</literal> 的词被破坏的<quote>土耳其问题</quote>。7 位 ASCII 集之外字符的折叠仍是区域感知的。
     </para>
     </listitem>

     <listitem>
      <para>
      改进语法错误报告（Fabien、Tom）
     </para>
      <para>
      语法错误报告比以前更有用。
     </para>
     </listitem>

     <listitem>
      <para>
      更改 <command>EXECUTE</command>，使其返回与被执行语句匹配的完成标签（Kris Jurka）
     </para>
      <para>
      以前的版本对任何 <command>EXECUTE</command> 调用都返回 <command>EXECUTE</command> 标签。在此版本中，返回的标签将反映被执行的命令。
     </para>
     </listitem>

     <listitem>
      <para>
      避免在规则列表中发出 <option>NATURAL CROSS JOIN</option>（Tom）
     </para>
      <para>
      这样的子句在逻辑上没有意义，但在某些情形下规则反编译器以前会产生这种语法。
     </para>
     </listitem>

    </itemizedlist>
   </sect3>


   <sect3>
    <title>Object Manipulation Changes</title>
    <itemizedlist>

     <listitem>
      <para>
      为转换、编码转换、语言、操作符类和大对象新增 <command>COMMENT ON</command>（Christopher）
     </para>
     </listitem>

     <listitem>
      <para>
      新增服务器配置参数 <varname>default_with_oids</varname>，控制表默认是否带 <type>OID</type> 创建（Neil）
     </para>
      <para>
      这允许管理员控制 <command>CREATE TABLE</command> 命令默认创建带或不带 <type>OID</type> 列的表。（注意：<varname>default_with_oids</varname> 当前的出厂默认设置为 <literal>TRUE</literal>，但未来版本中默认将变为 <literal>FALSE</literal>。）
     </para>
     </listitem>

     <listitem>
      <para>
      为 <command>CREATE TABLE AS</command> 新增 <option>WITH</option> / <option>WITHOUT OIDS</option> 子句（Neil）
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <command>ALTER TABLE DROP COLUMN</command> 删除 <type>OID</type> 列（<command>ALTER TABLE SET WITHOUT OIDS</command> 仍有效）（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      允许组合类型作为表列（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      允许带默认值和 <option>NOT NULL</option> 约束的 <command>ALTER ... ADD COLUMN</command>；按 SQL 规范工作（Rod）
     </para>
      <para>
      现在 <option>ADD COLUMN</option> 可以创建初始不用 NULL 填充而是用指定默认值填充的列。
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <command>ALTER COLUMN TYPE</command> 来更改列的类型（Rod）
     </para>
      <para>
      现在可以在不删除并重新添加列的情况下更改列的数据类型。
     </para>
     </listitem>

     <listitem>
      <para>
      允许在单条 <command>ALTER TABLE</command> 命令中有多个 <command>ALTER</command> 动作（Rod）
     </para>
      <para>
      这对重写表的 <command>ALTER</command> 命令（包括 <option>ALTER COLUMN TYPE</option> 和带默认值的 <option>ADD COLUMN</option>）特别有用。通过把 <command>ALTER</command> 命令分组，表只需重写一次。
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <command>ALTER TABLE</command> 添加 <type>SERIAL</type> 列（Tom）
     </para>
      <para>
      这是新列可指定默认值这一新能力的自然结果。
     </para>
     </listitem>

     <listitem>
      <para>
      允许更改聚合、编码转换、数据库、函数、操作符、操作符类、模式、类型和表空间的所有者（Christopher、Euler Taveira de Oliveira）
     </para>
      <para>
      此前这需要直接修改系统表。
     </para>
     </listitem>

     <listitem>
      <para>
      允许把临时对象创建限制为 <option>SECURITY DEFINER</option> 函数（Sean Chittenden）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <option>ALTER TABLE ... SET WITHOUT CLUSTER</option>（Christopher）
     </para>
      <para>
      在此版本之前，除了修改系统表外没有办法清除自动聚簇规格。
     </para>
     </listitem>

     <listitem>
      <para>
      约束/索引/<type>SERIAL</type> 名称现在为 <replaceable>table_column_type</replaceable> 并附加数字以保证在模式内唯一（Tom）
     </para>
      <para>
      SQL 规范规定此类名称应在模式内唯一。
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <function>pg_get_serial_sequence()</function>，返回 <type>SERIAL</type> 列的序列名（Christopher）
     </para>
      <para>
      这使自动脚本能可靠地找到 <type>SERIAL</type> 序列名。
     </para>
     </listitem>

     <listitem>
      <para>
      当主/外键数据类型不匹配需要昂贵查找时发出警告
     </para>
     </listitem>

     <listitem>
      <para>
      新 <command>ALTER INDEX</command> 命令，允许在表空间之间移动索引（Gavin）
     </para>
     </listitem>

     <listitem>
      <para>
      让 <command>ALTER TABLE OWNER</command> 也更改依赖序列的所有权（Alvaro）
     </para>
     </listitem>


    </itemizedlist>
   </sect3>


   <sect3>
    <title>Utility Command Changes</title>
    <itemizedlist>

     <listitem>
      <para>
      允许 <command>CREATE SCHEMA</command> 创建触发器、索引和序列（Neil）
     </para>
     </listitem>

     <listitem>
      <para>
      为 <command>CREATE RULE</command> 新增 <option>ALSO</option> 关键字（Fabien Coelho）
     </para>
      <para>
      这允许在创建规则时添加 <option>ALSO</option>，与 <option>INSTEAD</option> 规则相对。
     </para>
     </listitem>

     <listitem>
      <para>
      为 <command>LOCK</command> 新增 <option>NOWAIT</option> 选项（Tatsuo）
     </para>
      <para>
      这允许 <command>LOCK</command> 命令在必须等待所请求的锁时失败。
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <command>COPY</command> 读写逗号分隔值（CSV）文件（Andrew、Bruce）
     </para>
     </listitem>

     <listitem>
      <para>
      当 <command>COPY</command> 分隔符与 NULL 字符串冲突时报错（Bruce）
     </para>
     </listitem>

     <listitem>
      <para>
      <command>GRANT</command>/<command>REVOKE</command> 行为更贴近 SQL 规范
     </para>
     </listitem>

     <listitem>
      <para>
      避免 <command>CREATE INDEX</command> 与 <command>CHECKPOINT</command> 之间的锁定冲突（Tom）
     </para>
      <para>
      在 7.3 和 7.4 中，长时间运行的 B-tree 索引构建可能阻塞并发 <command>CHECKPOINT</command> 完成，从而因 WAL 日志无法回收导致 WAL 膨胀。
     </para>
     </listitem>

     <listitem>
      <para>
      数据库范围的 <command>ANALYZE</command> 不再跨表持有锁（Tom）
     </para>
      <para>
      这降低了与其他想独占锁定表的后端发生死锁的可能性。要获得此变更的好处，不要在事务块（<command>BEGIN</command> 块）内执行数据库范围的 <command>ANALYZE</command>；它必须能为每个表提交并开始新事务。
     </para>
     </listitem>

     <listitem>
      <para>
      <command>REINDEX</command> 不再独占锁定索引的父表
     </para>
      <para>
      索引本身仍被独占锁定，但如果表的读者不使用正被重建的特定索引，他们可以继续。
     </para>
     </listitem>

     <listitem>
      <para>
      用户被重命名时擦除 MD5 用户口令（Bruce）
     </para>
      <para>
      <productname>PostgreSQL</productname> 用 MD5 加密口令时把用户名用作盐。当用户名被更改时，盐将不再与存储的 MD5 口令匹配，因此存储的口令变得无用。在此版本中会生成通知并清除口令。之后如果用户要能用口令登录，必须分配新口令。
     </para>
     </listitem>

     <listitem>
      <para>
      <application>pg_ctl</application> 新增用于 Windows 的 <option>kill</option> 选项（Andrew）
     </para>
      <para>
      Windows 没有向后端发送信号的 <literal>kill</literal> 命令，因此把此能力添加到了 <application>pg_ctl</application>。
     </para>
     </listitem>

     <listitem>
      <para>
      信息模式改进
     </para>
     </listitem>

     <listitem>
      <para>
      为 <application>initdb</application> 新增 <option>--pwfile</option> 选项，使初始口令可由 GUI 工具设置（Magnus）
     </para>
     </listitem>

     <listitem>
      <para>
      在 <application>initdb</application> 中检测区域设置/编码不匹配（Peter）
     </para>
     </listitem>

     <listitem>
      <para>
      为 <application>pg_ctl</application> 新增 <option>register</option> 命令来注册 Windows 操作系统服务（Dave Page）
     </para>
     </listitem>

    </itemizedlist>
   </sect3>


   <sect3>
    <title>Data Type and Function Changes</title>
    <itemizedlist>

     <listitem>
      <para>
      更完整的组合类型（行类型）支持（Tom）
     </para>
      <para>
      组合值可以用在许多以前只有标量值可用的地方。
     </para>
     </listitem>

     <listitem>
      <para>
      把非矩形数组值作为错误拒绝（Joe）
     </para>
      <para>
      此前 <function>array_in</function> 会静默构建令人惊讶的结果。
     </para>
     </listitem>

      <listitem>
       <para>
      整数算术运算中的溢出现在被检测（Tom）
     </para>
      </listitem>

      <listitem>
       <para>
      与单字节 <type>"char"</type> 数据类型相关的算术操作符已被移除。
     </para>
       <para>
      此前，解析器会在许多情形下选择这些操作符，而<quote>无法选择操作符</quote>错误会更合适，例如 <literal>null * null</literal>。如果确实想对 <type>"char"</type> 列做算术，可以显式把它转换为整数。
     </para>
      </listitem>

     <listitem>
       <para>
      数组输入值的语法检查大幅收紧（Joe）
     </para>
      <para>
      以前在奇怪位置被允许并产生奇怪结果的垃圾现在导致 <literal>ERROR</literal>，例如右花括号之后的非空白。
     </para>
     </listitem>

     <listitem>
       <para>
      空字符串数组元素值现在必须写成 <literal>""</literal>，而不是什么也不写（Joe）
     </para>
      <para>
      此前两种写空字符串元素值的方式都被允许，但现在要求带引号的空字符串。完全什么都不出现的情形在未来某个版本中可能会被视为 NULL 元素值。
     </para>
     </listitem>

     <listitem>
       <para>
      数组元素的尾随空白现在被忽略（Joe）
     </para>
      <para>
      此前前导空白被忽略，但元素值与分隔符或右花括号之间的尾随空白是有意义的。现在尾随空白也被忽略。
     </para>
     </listitem>

     <listitem>
      <para>
      当下界不为一时以显式数组界限输出数组值（Joe）
     </para>
     </listitem>

     <listitem>
      <para>
      接受 <literal>YYYY-monthname-DD</literal> 作为日期字符串（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      让 <function>netmask</function> 和 <function>hostmask</function> 函数返回最大长度的掩码长度（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      更改阶乘函数使其返回 <type>numeric</type>（Gavin）
     </para>
      <para>
      返回 <type>numeric</type> 使阶乘函数能用于更大范围的输入值。
     </para>
     </listitem>

     <listitem>
      <para>
      <function>to_char</function>/<function>to_date()</function> 日期转换改进（Kurt Roeckx、Fabien Coelho）
     </para>
     </listitem>

     <listitem>
      <para>
      让 <function>length()</function> 无视 <type>CHAR(n)</type> 中的尾随空格（Gavin）
     </para>
      <para>
      此变更是为了改进一致性：尾随空格在 <type>CHAR(n)</type> 数据中语义上无关紧要，因此不应被 <function>length()</function> 统计。
     </para>
     </listitem>

     <listitem>
      <para>
      对传递给 <type>OID</type>/<type>float4</type>/<type>float8</type> 数据类型的空字符串发出警告（Neil）
     </para>
      <para>
      8.1 将改为抛出错误。
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <type>int2</type>/<type>int4</type>/<type>int8</type>/<type>float4</type>/<type>float8</type> 输入例程中的前导或尾随空白（Neil）
     </para>
     </listitem>

     <listitem>
      <para>
      更好地支持 <type>float4</type>/<type>float8</type> 中的 IEEE <literal>Infinity</literal> 和 <literal>NaN</literal> 值（Neil）
     </para>
      <para>
      它们现在应在所有支持 IEEE 兼容浮点运算的平台上工作。
     </para>
     </listitem>

     <listitem>
      <para>
      为 <function>date_trunc()</function> 新增 <option>week</option> 选项（Robert Creager）
     </para>
     </listitem>

     <listitem>
      <para>
      修复 <function>to_char</function> 对 <literal>1 BC</literal> 的处理（此前它返回 <literal>1 AD</literal>）（Bruce）
     </para>
     </listitem>

     <listitem>
      <para>
      修复 BC 日期的 <function>date_part(year)</function>（此前它返回比正确年份小一的值）（Bruce）
     </para>
     </listitem>

     <listitem>
      <para>
      修复 <function>date_part()</function>，使其返回正确的千年和世纪（Fabien Coelho）
     </para>
      <para>
      与这类事物的标准推算相比，以前的版本中世纪和千年的结果数字错误且起始年份错误。
     </para>
     </listitem>

     <listitem>
      <para>
      为符合标准，新增 <function>ceiling()</function> 作为 <function>ceil()</function> 的别名，<function>power()</function> 作为 <function>pow()</function> 的别名（Neil）
     </para>
     </listitem>

     <listitem>
      <para>
      更改 <function>ln()</function>、<function>log()</function>、<function>power()</function> 和 <function>sqrt()</function>，使其按 SQL:2003 的规定在某些错误条件下发出正确的 <literal>SQLSTATE</literal> 错误码（Neil）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 SQL:2003 定义的 <function>width_bucket()</function> 函数（Neil）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <function>generate_series()</function> 函数以简化数字集合的处理（Joe）
     </para>
     </listitem>

     <listitem>
      <para>
      修复 <function>upper/lower/initcap()</function> 函数使其支持多字节编码（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      新增布尔和按位整数 <option>AND</option>/<option>OR</option> 聚合（Fabien Coelho）
     </para>
     </listitem>

     <listitem>
      <para>
      新增返回客户端和服务器网络地址的会话信息函数（Sean Chittenden）
     </para>
     </listitem>

     <listitem>
      <para>
      新增确定闭合路径面积的函数（Sean Chittenden）
     </para>
     </listitem>

     <listitem>
      <para>
      新增向其他后端发送取消请求的函数（Magnus）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <type>interval</type> 加 <type>datetime</type> 操作符（Tom）
     </para>
      <para>
      相反的顺序（<type>datetime</type> 加 <type>interval</type>）此前已支持，但两者都是 SQL 标准要求的。
     </para>
     </listitem>

     <listitem>
      <para>
      把整数转换为 <type>BIT(N)</type> 选取整数最右边的 N 位（Tom）
     </para>
      <para>
      在以前的版本中选取的是最左边 N 位，但这被认为没有用处，更不用说与从 bit 到 int 的转换不一致了。
     </para>
     </listitem>

     <listitem>
      <para>
      要求 <type>CIDR</type> 值的所有未掩码位为零（Kevin Brintnall）
     </para>
     </listitem>

    </itemizedlist>
   </sect3>


   <sect3>
    <title>Server-Side Language Changes</title>
    <itemizedlist>

     <listitem>
      <para>
      在 <literal>READ COMMITTED</literal> 序列化模式下，易变函数现在能看到并发事务提交到函数内每条语句开始处为止的结果，而不是到调用函数的交互命令开始处为止。
     </para>
     </listitem>

     <listitem>
      <para>
      声明为 <literal>STABLE</literal> 或 <literal>IMMUTABLE</literal> 的函数总是使用调用查询的快照，因此看不到调用查询开始之后采取动作的效果，无论在其自身事务还是其他事务中。这样的函数也必须是只读的，即不能使用 <command>SELECT</command> 之外的任何 SQL 命令。把函数声明为 <literal>STABLE</literal> 或 <literal>IMMUTABLE</literal> 而不是 <literal>VOLATILE</literal> 有可观的性能收益。
     </para>
     </listitem>

     <listitem>
      <para>
      非延迟的 <option>AFTER</option> 触发器现在在触发查询完成后立即触发，而不是在当前交互命令完成时。当触发查询发生在函数内时这就有区别：触发器在函数继续其下一个操作之前被调用。例如，如果函数向表插入新行，任何非延迟外键检查都在函数继续之前进行。
     </para>
     </listitem>

     <listitem>
      <para>
      允许函数参数带名称声明（Dennis Bj&ouml;rklund）
     </para>
      <para>
      这允许对函数做更好的文档记录。名称是否真起作用取决于所用的具体函数语言。
     </para>
     </listitem>

     <listitem>
      <para>
      允许在函数中引用 PL/pgSQL 参数名（Dennis Bj&ouml;rklund）
     </para>
      <para>
      这基本上为每个命名参数创建了自动别名。
     </para>
     </listitem>

     <listitem>
      <para>
      在创建时对 PL/pgSQL 函数做最简语法检查（Tom）
     </para>
      <para>
      这使我们能更早捕获简单的语法错误。
     </para>
     </listitem>

     <listitem>
      <para>
      PL/pgSQL 中对组合类型（行和记录变量）的更多支持
     </para>
      <para>
      例如，现在可以把行类型变量作为单个变量传递给另一个函数。
     </para>
     </listitem>

     <listitem>
      <para>
      PL/pgSQL 变量的默认值现在可以引用先前声明的变量
     </para>
     </listitem>

     <listitem>
      <para>
      改进 PL/pgSQL FOR 循环的解析（Tom）
     </para>
      <para>
      解析现在由 <literal>".."</literal> 的存在驱动，而不是 <option>FOR</option> 变量的数据类型。这对正确的函数没有区别，但在出错时应产生更易理解的错误消息。
     </para>
     </listitem>

     <listitem>
      <para>
      PL/Perl 服务器端语言的重大改造（Command Prompt、Andrew Dunstan）
     </para>
     </listitem>

     <listitem>
      <para>
      在 PL/Tcl 中，SPI 命令现在在子事务中运行。如果发生错误，子事务被清理，错误被报告为普通 Tcl 错误，可以用 <literal>catch</literal> 捕获。此前无法捕获此类错误。
     </para>
     </listitem>

     <listitem>
      <para>
      PL/pgSQL 接受 <command>ELSEIF</command>（Neil）
     </para>
      <para>
      此前 PL/pgSQL 只允许 <command>ELSIF</command>，但许多人习惯把此关键字拼写为 <command>ELSEIF</command>。
     </para>
     </listitem>

    </itemizedlist>
   </sect3>


   <sect3>
    <title><application>psql</> Changes</title>
    <itemizedlist>

     <listitem>
      <para>
      改进 <application>psql</application> 对数据库对象的信息显示（Christopher）
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <application>psql</application> 在 <command>\du</command> 和 <command>\dg</command> 中显示组成员身份（Markus Bertheau）
     </para>
     </listitem>

     <listitem>
      <para>
      防止 <application>psql</application> 的 <command>\dn</command> 显示临时模式（Bruce）
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <application>psql</application> 处理文件名的波浪号用户展开（Zach Irmen）
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <application>psql</application> 通过 <application>readline</application> 显示精致提示符，包括颜色（Reece Hart、Chet Ramey）
     </para>
     </listitem>

     <listitem>
      <para>
      让 <application>psql</application> 的 <command>\copy</command> 完全匹配 <command>COPY</command> 命令语法（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      显示语法错误的位置（Fabien Coelho、Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      向 <application>psql</application> 的 <command>\d</command> 显示中添加 <command>CLUSTER</command> 信息（Bruce）
     </para>
     </listitem>

     <listitem>
      <para>
      更改 <application>psql</application> 的 <command>\copy stdin/stdout</command>，使其从命令输入/输出读取（Bruce）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <option>pstdin</option>/<option>pstdout</option>，从 <application>psql</application> 的 <literal>stdin</literal>/<literal>stdout</literal> 读取（Mark Feit）
     </para>
     </listitem>

     <listitem>
      <para>
      新增全局 <application>psql</application> 配置文件 <filename>psqlrc.sample</filename>（Bruce）
     </para>
      <para>
      这允许用一个中心文件存储全局 <application>psql</application> 启动命令。
     </para>
     </listitem>

     <listitem>
      <para>
      让 <application>psql</application> 的 <command>\d+</command> 指示表是否有 <type>OID</type> 列（Neil）
     </para>
     </listitem>

     <listitem>
      <para>
      在 Windows 上，<application>psql</application> 读取文件时使用二进制模式，使 control-Z 不被视为文件结束
     </para>
     </listitem>

     <listitem>
      <para>
      让 <command>\dn+</command> 显示模式的权限和描述（Dennis Bj&ouml;rklund）
     </para>
     </listitem>

     <listitem>
      <para>
      改进 Tab 补全支持（Stefan Kaltenbrunn、Greg Sabino Mullane）
     </para>
     </listitem>

     <listitem>
      <para>
      允许用大写或小写设置布尔设置（Michael Paesold）
     </para>
     </listitem>

    </itemizedlist>
   </sect3>


   <sect3>
    <title><application>pg_dump</> Changes</title>
    <itemizedlist>

     <listitem>
      <para>
      使用依赖信息改进 <application>pg_dump</application> 的可靠性（Tom）
     </para>
      <para>
      这应能解决相关对象有时以错误顺序转储的长期问题。
     </para>
     </listitem>

     <listitem>
      <para>
      让 <application>pg_dump</application> 尽可能按字母顺序输出对象（Tom）
     </para>
      <para>
      这应使识别转储文件之间的更改更容易。
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <application>pg_restore</application> 忽略某些 SQL 错误（Fabien Coelho）
     </para>
      <para>
      这使 <application>pg_restore</application> 的行为类似于把 <application>pg_dump</application> 输出脚本送入 <application>psql</application> 的结果。在大多数情况下，忽略错误继续前进是最有用的做法。还添加了 pg_restore 选项以提供出错即退出的旧行为。
     </para>
     </listitem>

     <listitem>
      <para>
      <application>pg_restore</application> 的 <option>-l</option> 显示现在包含对象的模式名
     </para>
     </listitem>

     <listitem>
      <para>
      <application>pg_dump</application> 文本输出中新增开始/结束标记（Bruce）
     </para>
     </listitem>

     <listitem>
      <para>
      为详细模式下的 <application>pg_dump</application>/<application>pg_dumpall</application> 新增开始/停止时间（Bruce）
     </para>
     </listitem>

     <listitem>
      <para>
      允许在 <application>pg_dumpall</application> 中使用大多数 <application>pg_dump</application> 选项（Christopher）
     </para>
     </listitem>

     <listitem>
      <para>
      让 <application>pg_dump</application> 默认使用 <command>ALTER OWNER</command> 而不是 <command>SET SESSION AUTHORIZATION</command>（Christopher）
     </para>
     </listitem>

    </itemizedlist>
   </sect3>


   <sect3>
    <title>libpq Changes</title>
    <itemizedlist>

     <listitem>
      <para>
      使 libpq 的 <option>SIGPIPE</option> 处理线程安全（Bruce）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <function>PQmbdsplen()</function>，返回字符的显示长度（Tatsuo）
     </para>
     </listitem>

     <listitem>
      <para>
      为 <application>SSL</application> 和 <application>Kerberos</application> 连接添加线程锁定（Manfred Spraul）
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <function>PQoidValue()</function>、<function>PQcmdTuples()</function> 和 <function>PQoidStatus()</function> 用于 <command>EXECUTE</command> 命令（Neil）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <function>PQserverVersion()</function>，提供对服务器版本号更便捷的访问（Greg Sabino Mullane）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <function>PQprepare/PQsendPrepared()</function> 函数，支持预备语句而不必指定其参数的数据类型（Abhijit Menon-Sen）
     </para>
     </listitem>

     <listitem>
      <para>
      许多 ECPG 改进，包括 <command>SET DESCRIPTOR</command>（Michael）
     </para>
     </listitem>

    </itemizedlist>
   </sect3>


   <sect3>
    <title>Source Code Changes</title>
    <itemizedlist>

     <listitem>
      <para>
      允许数据库服务器原生运行于 Windows（Claudio、Magnus、Andrew）
     </para>
     </listitem>

     <listitem>
      <para>
      把 shell 脚本命令转换为 C 版本以支持 Windows（Andrew）
     </para>
     </listitem>

     <listitem>
      <para>
      创建扩展 makefile 框架（Fabien Coelho、Peter）
     </para>
      <para>
      这简化了在原始源代码树之外构建扩展的任务。
     </para>
     </listitem>

     <listitem>
      <para>
      支持可重新定位的安装（Bruce）
     </para>
      <para>
      已安装文件（如 <filename>/share</filename> 目录）的目录路径现在相对于可执行文件的实际位置计算，因此安装树可以移动到其他地方而无需重新配置和重新构建。
     </para>
     </listitem>

     <listitem>
      <para>
      用 <option>--with-docdir</option> 选择文档的安装位置；还允许 <option>--infodir</option>（Peter）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <option>--without-docdir</option> 以阻止安装文档（Peter）
     </para>
     </listitem>

     <listitem>
      <para>
      升级到 <application>DocBook</application> V4.2 SGML（Peter）
     </para>
     </listitem>

     <listitem>
      <para>
      新的 <literal>PostgreSQL</literal> <application>CVS</application> 标签（Marc）
     </para>
      <para>
      这是为了使组织更容易管理自己的 <productname>PostgreSQL</productname> <application>CVS</application> 仓库副本。来自主仓库的文件版本戳不会因向复制的仓库检入或从中检出而被破坏。
     </para>
     </listitem>

     <listitem>
      <para>
      澄清锁定代码（Manfred Koizar）
     </para>
     </listitem>

     <listitem>
      <para>
      缓冲区管理器清理（Neil）
     </para>
     </listitem>

     <listitem>
      <para>
      把平台测试与 CPU 自旋锁代码解耦（Bruce、Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      为 <application>gcc</application> 在 PA-RISC 上新增内联 test-and-set 代码（ViSolve、Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      改进 i386 自旋锁代码（Manfred Spraul）
     </para>
     </listitem>

     <listitem>
      <para>
      清理自旋锁汇编代码以避免较新 <application>gcc</application> 版本的警告（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      从源代码树移除 JDBC；现为独立项目
     </para>
     </listitem>

     <listitem>
      <para>
      移除 libpgtcl 客户端接口；现为独立项目
     </para>
     </listitem>

     <listitem>
      <para>
      更准确地估计内存和文件描述符的使用（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      Mac OS X 启动脚本的改进（Ray A.）
     </para>
     </listitem>

     <listitem>
      <para>
      新的 <function>fsync()</function> 测试程序（Bruce）
     </para>
     </listitem>

     <listitem>
      <para>
      重大文档改进（Neil、Peter）
     </para>
     </listitem>

     <listitem>
      <para>
      移除 <application>pg_encoding</application>；不再需要
     </para>
     </listitem>

     <listitem>
      <para>
      移除 <application>pg_id</application>；不再需要
     </para>
     </listitem>

     <listitem>
      <para>
      移除 <application>initlocation</application>；不再需要
     </para>
     </listitem>

     <listitem>
      <para>
      自动检测线程标志（不再需要手工测试）（Bruce）
     </para>
     </listitem>

     <listitem>
      <para>
      使用 Olson 的公有领域 <application>timezone</application> 库（Magnus）
     </para>
     </listitem>

     <listitem>
      <para>
      启用线程时，Unixware 上的后端可执行文件也使用线程标志（Bruce）
     </para>
      <para>
      Unixware 不能在同一可执行文件中混用线程化和非线程化目标文件，因此所有内容都必须编译为线程化。
     </para>
     </listitem>

     <listitem>
      <para>
      <application>psql</application> 现在使用 <application>flex</application> 生成的词法分析器处理命令字符串
     </para>
     </listitem>

     <listitem>
      <para>
      重新实现后端各处使用的链表数据结构（Neil）
     </para>
      <para>
      这通过使列表追加和长度操作更高效来改进性能。
     </para>
     </listitem>

     <listitem>
      <para>
      允许动态装载的模块创建自己的服务器配置参数（Thomas Hallgren）
     </para>
     </listitem>

     <listitem>
      <para>
      新的巴西葡萄牙语版 FAQ（Euler Taveira de Oliveira）
     </para>
     </listitem>

     <listitem>
      <para>
      新增法语 FAQ（Guillaume Lelarge）
     </para>
     </listitem>

     <listitem>
      <para>
      新的用于 Windows 日志的 <application>pgevent</application>
     </para>
     </listitem>

     <listitem>
      <para>
      使 libpq 和 ECPG 在 OS X 上构建为正确的共享库（Tom）
     </para>
     </listitem>

    </itemizedlist>
   </sect3>


   <sect3>
    <title>Contrib Changes</title>
    <itemizedlist>

     <listitem>
      <para>
      <filename>contrib/dblink</filename> 的彻底改造（Joe）
     </para>
     </listitem>

     <listitem>
      <para>
      <filename>contrib/dbmirror</filename> 改进（Steven Singer）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <filename>contrib/xml2</filename>（John Gray、Torchbox）
     </para>
     </listitem>

     <listitem>
      <para>
      更新了 <filename>contrib/mysql</filename>
     </para>
     </listitem>

     <listitem>
      <para>
      新版本的 <filename>contrib/btree_gist</filename>（Teodor）
     </para>
     </listitem>

     <listitem>
      <para>
      新的 <filename>contrib/trgm</filename>，<productname>PostgreSQL</productname> 的三字母组匹配（Teodor）
     </para>
     </listitem>

     <listitem>
      <para>
      许多 <filename>contrib/tsearch2</filename> 改进（Teodor）
     </para>
     </listitem>

     <listitem>
      <para>
      为 <filename>contrib/fuzzystrmatch</filename> 添加双变音位（Andrew）
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <filename>contrib/pg_autovacuum</filename> 作为 Windows 服务运行（Dave Page）
     </para>
     </listitem>

     <listitem>
      <para>
      为 <filename>contrib/dbsize</filename> 新增函数（Andreas Pflug）
     </para>
     </listitem>

     <listitem>
      <para>
      移除 <filename>contrib/pg_logger</filename>：被集成日志子进程取代
     </para>
     </listitem>

     <listitem>
      <para>
      移除 <filename>contrib/rserv</filename>：被多个独立项目取代
     </para>
     </listitem>

    </itemizedlist>
   </sect3>

  </sect2>
 </sect1>
