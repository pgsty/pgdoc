<!--
$PostgreSQL: pgsql/doc/src/sgml/external-projects.sgml,v 1.9 2005/04/09 03:52:43 momjian Exp $
-->

 <appendix id="external-projects">
  <title>外部项目</title>

  <para>
   <productname>PostgreSQL</productname>是一个复杂的软件项目，管理它并不容易。我们发现，对
   <productname>PostgreSQL</productname>的许多增强功能，如果与核心项目分开开发会更有效率。独立的项目可以有各自的开发团队、邮件列表、错误跟踪和发布时间表。虽然这种独立性使开发更容易，但它也让用户的工作变得更难：他们不得不四处寻找满足自己需要的数据库增强功能。本节介绍一些较受欢迎的外部开发的增强功能，并指导你如何找到它们。
  </para>

  <para>
   许多与<productname>PostgreSQL</productname>相关的项目托管在
   <ulink url="http://gborg.postgresql.org"><productname>GBorg</></ulink>
   或 <ulink url="http://pgfoundry.org"><productname>pgFoundry</></ulink>。
   还有一些与<productname>PostgreSQL</productname>相关的项目托管在其他地方，但你需要通过
   Internet 搜索才能找到它们。
  </para>

 <sect1 id="external-interfaces">
  <title>外部开发的接口</title>

  <indexterm>
   <primary>interfaces</primary>
  </indexterm>

  <para>
   <productname>PostgreSQL</productname>的基础发行版中只包含很少的接口。
   <application>libpq</>被打包在内，因为它是主要的<application>C</>接口，而且许多其他接口都构建在它之上。<application>ecpg</>被打包在内，因为它与服务器端文法紧密相关，对数据库版本有很强的依赖性。所有其他接口都是独立的项目，必须单独安装。
  </para>

  <para>
   一些较受欢迎的接口包括：

   <variablelist>
    <varlistentry>
     <term>psqlODBC</term>
     <listitem>
      <para>
       这是<application>Windows</>应用程序最常用的接口。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>pgjdbc</term>
     <listitem>
      <para>
       一个 <application>JDBC</>接口。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>Npgsql</term>
     <listitem>
      <para>
       面向较新<application>Windows</>应用程序的
       <application>.Net</>接口。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>libpqxx</term>
     <listitem>
      <para>
       一个较新的 <application>C++</>接口。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>libpq++</term>
     <listitem>
      <para>
       一个较老的 <application>C++</>接口。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>pgperl</term>
     <listitem>
      <para>
       一个 <application>Perl</>接口，其<acronym>API</>类似于
       <application>libpq</>。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>DBD-Pg</term>
     <listitem>
      <para>
       一个使用 <acronym>DBD</>标准<application>API</>的
       <application>Perl</>接口。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>pgtclng</term>
     <listitem>
      <para>
       <application>Tcl</>接口的较新版本。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>pgtcl</term>
     <listitem>
      <para>
       <application>Tcl</>接口的原始版本。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>PyGreSQL</term>
     <listitem>
      <para>
       一个 <application>Python</>接口库。
      </para>
     </listitem>
    </varlistentry>

   </variablelist>

   所有这些都可以在
   <ulink url="http://gborg.postgresql.org"><productname>GBorg</></ulink>
   或 <ulink url="http://pgfoundry.org"><productname>pgFoundry</></ulink> 找到。
   </para>

 </sect1>

 <sect1 id="external-extensions">
 <title>扩展</title>

  <indexterm>
   <primary>extensions</primary>
  </indexterm>

  <para>
   <productname>PostgreSQL</>从一开始就被设计为可扩展的。因此，装载到数据库中的扩展可以像随数据库打包的功能一样工作。源码发行版中的
   <filename>contrib/</>目录包含大量扩展。该目录中的
   <filename>README</>文件包含一个摘要。它们包括转换工具、全文索引、<acronym>XML</>工具以及更多的数据类型和索引方法。其他扩展是独立开发的，例如
   <application>PostGIS</>。甚至<application>PostgreSQL</>的复制解决方案也是在外部开发的。例如，<application>Slony-I</>是一种流行的主/从复制解决方案，它独立于核心项目开发。
  </para>

  <para>
   <productname>PostgreSQL</>有多种可用的管理工具。其中最受欢迎的是
   <application>pgAdmin</>，另外还有一些商业化的工具。
  </para>

 </sect1>
</appendix>
