<!--
$PostgreSQL: pgsql/doc/src/sgml/ref/delete.sgml,v 1.35 2008/11/16 17:34:28 tgl Exp $
PostgreSQL documentation
-->

<refentry id="SQL-DELETE">
 <refmeta>
  <refentrytitle id="SQL-DELETE-TITLE">DELETE</refentrytitle>
  <refmiscinfo>SQL - Language Statements</refmiscinfo>
 </refmeta>

 <refnamediv>
  <refname>DELETE</refname>
  <refpurpose>删除表中的行</refpurpose>
 </refnamediv>

 <indexterm zone="sql-delete">
  <primary>DELETE</primary>
 </indexterm>

 <refsynopsisdiv>
<synopsis>
DELETE FROM [ ONLY ] <replaceable class="PARAMETER">table</replaceable>
    [ USING <replaceable class="PARAMETER">usinglist</replaceable> ]
    [ WHERE <replaceable class="PARAMETER">condition</replaceable> ]
</synopsis>
 </refsynopsisdiv>

 <refsect1>
  <title>描述</title>

  <para>
   <command>DELETE</command>从指定表中删除满足
   <literal>WHERE</literal>子句的行。如果省略<literal>WHERE</literal>
   子句，效果就是删除表中的所有行。结果是一个合法但为空的表。
  </para>

  <para>
   默认情况下，<command>DELETE</command>将删除指定表及其所有子表中的行。如果只想从提及的特定表中删除，必须使用<literal>ONLY</literal>子句。
  </para>

   <tip>
    <para>
     <xref linkend="sql-truncate" endterm="sql-truncate-title">是一种<productname>PostgreSQL</productname>扩展，它提供了一种更快的机制来移除表中的所有行。
    </para>
   </tip>

  <para>
   有两种方法可以利用数据库中其他表所包含的信息来删除一个表中的行：使用子选择，或者在<literal>USING</literal>子句中指定附加表。哪种技术更合适取决于具体情况。
  </para>

  <para>
   要从一个表中删除行，必须具有该表上的<literal>DELETE</literal>权限，以及<literal>USING</literal>子句中出现的任何表或者其值会在
   <replaceable class="parameter">condition</replaceable>中读取的任何表上的<literal>SELECT</literal>权限。
  </para>
 </refsect1>

 <refsect1>
  <title>参数</title>

  <variablelist>
   <varlistentry>
    <term><literal>ONLY</></term>
    <listitem>
     <para>
      如果指定，则只从所提及的表中删除行。未指定时，继承自该表的任何表也会被处理。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><replaceable class="parameter">table</replaceable></term>
    <listitem>
     <para>
      现有表的名称（可以是模式限定的）。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><replaceable class="PARAMETER">usinglist</replaceable></term>
    <listitem>
     <para>一个表表达式列表，允许其他表的列出现在<literal>WHERE</>条件中。这类似于<command>SELECT</command>语句的<xref linkend="sql-from" endterm="sql-from-title">中可以指定的表列表；例如，可以为表名指定别名。不要把目标表重复写在<replaceable class="PARAMETER">usinglist</replaceable>中，除非打算进行自连接。</para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><replaceable class="parameter">condition</replaceable></term>
    <listitem>
     <para>
      一个返回<type>boolean</type>类型值的表达式，它决定要删除哪些行。
     </para>
    </listitem>
   </varlistentry>

  </variablelist>
 </refsect1>

 <refsect1>
  <title>输出</title>

  <para>
   成功完成后，<command>DELETE</command> 命令返回如下形式的命令标签：
<screen>
DELETE <replaceable class="parameter">count</replaceable>
</screen>
   其中 <replaceable class="parameter">count</replaceable> 是被删除的行数。如果 <replaceable class="parameter">count</replaceable> 为 0，说明没有行匹配 <replaceable class="parameter">condition</replaceable>（这不算错误）。
  </para>
 </refsect1>

 <refsect1>
  <title>注解</title>

  <para>
   <productname>PostgreSQL</productname>允许通过在
   <literal>USING</literal>子句中指定其他表，在
   <literal>WHERE</literal>条件中引用这些表的列。例如，要删除由给定制片人制作的所有电影，可以这样做：
<programlisting>
DELETE FROM films USING producers
  WHERE producer_id = producers.id AND producers.name = 'foo';
</programlisting>
   本质上，这里发生的是在<structname>films</structname>和
   <structname>producers</structname>之间进行连接，并将所有成功连接到的<structname>films</structname>行标记为删除。这种语法不是标准的。更标准的写法是
<programlisting>
DELETE FROM films
  WHERE producer_id IN (SELECT id FROM producers WHERE name = 'foo');
</programlisting>
   在某些情况下，连接形式比子选择形式更容易书写或者执行更快。
  </para>
 </refsect1>

 <refsect1>
  <title>示例</title>

  <para>
   删除所有电影，但音乐剧除外：
<programlisting>
DELETE FROM films WHERE kind &lt;&gt; 'Musical';
</programlisting>
  </para>

  <para>
   清空表<literal>films</literal>：
<programlisting>
DELETE FROM films;
</programlisting>
  </para>
 </refsect1>

 <refsect1>
  <title>兼容性</title>

  <para>
   这个命令符合 SQL 标准，不过<literal>USING</literal>子句以及在
   <literal>WHERE</literal>子句中引用其他表的能力是<productname>PostgreSQL</productname>扩展。
  </para>
 </refsect1>
</refentry>
