<!--
$PostgreSQL: pgsql/doc/src/sgml/ref/create_table_as.sgml,v 1.40 2008/11/20 14:04:45 petere Exp $
PostgreSQL documentation
-->

<refentry id="SQL-CREATETABLEAS">
 <refmeta>
  <refentrytitle id="sql-createtableas-title">CREATE TABLE AS</refentrytitle>
  <refmiscinfo>SQL - Language Statements</refmiscinfo>
 </refmeta>

 <refnamediv>
  <refname>CREATE TABLE AS</refname>
  <refpurpose>根据查询结果定义一个新表</refpurpose>
 </refnamediv>

 <indexterm zone="sql-createtableas">
  <primary>CREATE TABLE AS</primary>
 </indexterm>

 <refsynopsisdiv>
<synopsis>
CREATE [ [ GLOBAL | LOCAL ] { TEMPORARY | TEMP } ] TABLE <replaceable>table_name</replaceable>
    [ (<replaceable>column_name</replaceable> [, ...] ) ] [ [ WITH | WITHOUT ] OIDS ]
    AS <replaceable>query</replaceable>
</synopsis>
 </refsynopsisdiv>
  
 <refsect1>
  <title>描述</title>

  <para>
   <command>CREATE TABLE AS</command>创建一个表，并用
   <command>SELECT</command>命令或运行已预备的<command>SELECT</command>
   命令的<command>EXECUTE</command>计算得到的数据填充该表。该表的列具有与<command>SELECT</command>输出列对应的名称和数据类型（但可以通过显式给出新的列名列表来覆盖列名）。
  </para>

  <para>
   <command>CREATE TABLE AS</command>与创建视图有些相似，但实际上差异很大：它会创建一个新表，并且只执行一次该查询来完成新表的初始填充。这个新表不会跟踪该查询所引用源表的后续变化。相比之下，视图每次被查询时都会重新执行其定义中的
   <command>SELECT</command>语句。
  </para>
 </refsect1>

 <refsect1>
  <title>参数</title>

  <variablelist>
   <varlistentry>
    <term><literal>GLOBAL</literal> or <literal>LOCAL</literal></term>
    <listitem>
     <para>
      出于兼容性考虑会被忽略。详见<xref linkend="sql-createtable" endterm="sql-createtable-title">。
     </para>
    </listitem>
   </varlistentry>
  </variablelist>

  <variablelist>
   <varlistentry>
    <term><literal>TEMPORARY</> or <literal>TEMP</></term>
    <listitem>
     <para>
      如果指定，将把该表创建为临时表。详见<xref linkend="sql-createtable" endterm="sql-createtable-title">。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><replaceable>table_name</replaceable></term>
    <listitem>
     <para>
      要创建的表名（可选地带模式限定）。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><replaceable>column_name</replaceable></term>
    <listitem>
     <para>
      新表中某一列的名称。如果未提供列名，则采用查询输出列的列名。如果表是从 <command>EXECUTE</command> 命令创建的，则不能指定列名列表。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><literal>WITH OIDS</literal></term>
    <term><literal>WITHOUT OIDS</literal></term>
     <listitem>
      <para>
       这个可选子句指定<command>CREATE TABLE AS</command>创建的表是否应包含 OID。如果未指定该子句的任何一种形式，则使用<xref linkend="guc-default-with-oids">配置参数的值。
      </para>
     </listitem>
    </varlistentry>

   <varlistentry>
    <term><replaceable>query</replaceable></term>
    <listitem>
     <para>
      一条查询语句（也就是一个<command>SELECT</command>命令，或一个运行已预备的<command>SELECT</command>命令的<command>EXECUTE</command>
      命令）。所允许语法的描述请分别参阅<xref linkend="sql-select" endterm="sql-select-title">或<xref linkend="sql-execute" endterm="sql-execute-title">。
     </para>
    </listitem>
   </varlistentry>

  </variablelist>
 </refsect1>

 <refsect1>
  <title>注解</title>

  <para>
   这个命令在功能上类似于<xref linkend="sql-selectinto" endterm="sql-selectinto-title">，但更推荐使用它，因为它更不容易与<command>SELECT INTO</command>语法的其他用法相混淆。此外，<command>CREATE TABLE AS</command>提供的功能是<command>SELECT INTO</command>所提供功能的超集。
  </para>

  <para>在 <productname>PostgreSQL</productname> 8.0 之前，<command>CREATE TABLE AS</command>创建的表总是包含 OID。从 <productname>PostgreSQL</productname> 8.0 起，<command>CREATE TABLE AS</command>命令允许用户显式指定是否包含 OID。如果未显式指定是否存在 OID，则使用<xref linkend="guc-default-with-oids">配置变量。而从 <productname>PostgreSQL</productname> 8.1 起，该变量默认为 false，因此默认行为与 8.0 之前的发行版不再相同。需要在<command>CREATE TABLE AS</command>所创建表中包含 OID 的应用，应显式指定<literal>WITH OIDS</literal>以确保所需行为。</para>
 </refsect1>

 <refsect1>
  <title>示例</title>

  <para>
   创建一个新表<literal>films_recent</literal>，其中只包含表
   <literal>films</literal>中的近期条目：

<programlisting>
CREATE TABLE films_recent AS
  SELECT * FROM films WHERE date_prod &gt;= '2002-01-01';
</programlisting>
  </para>
 </refsect1>

 <refsect1>
  <title>兼容性</title>

  <para>
   <command>CREATE TABLE AS</command>符合<acronym>SQL</acronym>标准，但有以下例外：<itemizedlist spacing="compact">
    <listitem>
     <para>
      标准要求在子查询子句外围加括号；在
      <productname>PostgreSQL</productname>中，这些括号是可选的。
     </para>
    </listitem>

    <listitem>
     <para>
      标准定义了<literal>ON COMMIT</literal>子句；目前
      <productname>PostgreSQL</productname>尚未实现它。
     </para>
    </listitem>

    <listitem>
     <para>
      标准定义了<literal>WITH [ NO ] DATA</literal>子句；目前
      <productname>PostgreSQL</productname>尚未实现它。<productname>PostgreSQL</productname>提供的行为等同于标准中的
      <literal>WITH DATA</literal>情形。
     </para>
    </listitem>

    <listitem>
     <para><literal>WITH/WITHOUT OIDS</literal>是
     <productname>PostgreSQL</productname>的扩展。
     </para>
    </listitem>

    <listitem>
     <para>
      <productname>PostgreSQL</productname>处理临时表的方式与标准有相当大的不同；详见<xref linkend="sql-createtable" endterm="sql-createtable-title">。
     </para>
    </listitem>
   </itemizedlist></para>
 </refsect1>

 <refsect1>
  <title>另见</title>

  <simplelist type="inline">
   <member><xref linkend="sql-createtable" endterm="sql-createtable-title"></member>
   <member><xref linkend="sql-execute" endterm="sql-execute-title"></member>
   <member><xref linkend="sql-select" endterm="sql-select-title"></member>
   <member><xref linkend="sql-selectinto" endterm="sql-selectinto-title"></member>
  </simplelist>
 </refsect1>
 
</refentry>
