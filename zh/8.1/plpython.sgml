<!-- $PostgreSQL: pgsql/doc/src/sgml/plpython.sgml,v 1.51.2.2 2010/09/08 20:35:53 petere Exp $ -->

<chapter id="plpython">
 <title>PL/Python - Python 过程语言</title>

 <indexterm zone="plpython"><primary>PL/Python</></>
 <indexterm zone="plpython"><primary>Python</></>

 <para><application>PL/Python</application> 过程语言允许用 <ulink url="http://www.python.org">Python 语言</ulink>编写 <productname>PostgreSQL</productname> 函数。</para>

 <para>
  要在某个特定数据库中安装 PL/Python，使用
  <literal>createlang plpythonu <replaceable>dbname</replaceable></literal>。
 </para>

  <tip>
   <para>
    如果把某种语言安装到<literal>template1</literal>中，之后创建的所有数据库都会自动安装该语言。
   </para>
  </tip>

 <para>
  从 <productname>PostgreSQL</productname> 7.4 起，<application>PL/Python</application> 只能作为一种<quote>不受信任的</quote>语言使用（这意味着它不提供任何机制来限制用户能在其中做什么）。因此其名称被改为
  <literal>plpythonu</literal>。如果将来能在 Python 中开发出新的安全执行机制，受信任的变体
  <literal>plpython</literal> 可能会重新出现。
 </para>

 <note>
  <para>
   源码包用户必须在安装过程中专门启用 PL/Python 的构建（更多信息请参阅安装说明）。二进制包用户则可能会在单独的子包中找到 PL/Python。
  </para>
 </note>

  <sect1 id="plpython-funcs">
  <title>PL/Python 函数</title>

  <para>
   PL/Python 中的函数通过常用的<xref
   linkend="sql-createfunction" endterm="sql-createfunction-title">语法声明。例如：
<programlisting>
CREATE FUNCTION myfunc(text) RETURNS text
    AS 'return args[0]'
    LANGUAGE plpythonu;
</programlisting>

   作为函数体给出的 Python 代码会被转换成一个 Python 函数。例如，上面的定义会得到

<programlisting>
def __plpython_procedure_myfunc_23456():
        return args[0]
</programlisting>

   这里假定 23456 是 <productname>PostgreSQL</productname> 分配给该函数的 OID。
  </para>

  <para>
   如果你不提供返回值，Python 会返回默认的 <symbol>None</symbol>。<application>PL/Python</application> 会把
   Python 的 <symbol>None</symbol> 转换为 SQL 空值。<indexterm><primary>null value</><secondary
   sortas="PL/Python">in PL/Python</></indexterm>
  </para>

  <para>
   <productname>PostgreSQL</productname> 函数的参数可以在全局 <varname>args</varname> 列表中获得。在
   <function>myfunc</function> 示例中，<varname>args[0]</> 包含作为 text 参数传入的内容。对于
   <literal>myfunc2(text, integer)</literal>，<varname>args[0]</> 将包含
   <type>text</type> 参数，<varname>args[1]</> 包含 <type>integer</type> 参数。
  </para>

  <para>
   全局字典 <varname>SD</varname> 可用于在函数调用之间存储数据。这个变量是私有的静态数据。
   全局字典 <varname>GD</varname> 是公共数据，同一会话中的所有 Python 函数都可以使用。请谨慎使用。<indexterm><primary>global data</><secondary>in
   PL/Python</></indexterm>
  </para>

  <para>
   每个函数在 Python 解释器中都有自己独立的执行环境，因此
   <function>myfunc</function> 的全局数据和函数参数对
   <function>myfunc2</function> 不可用。例外是上面提到的 <varname>GD</varname> 字典中的数据。
  </para>
 </sect1>

 <sect1 id="plpython-trigger">
  <title>触发器函数</title>

  <indexterm zone="plpython-trigger">
   <primary>trigger</primary>
   <secondary>in PL/Python</secondary>
  </indexterm>

  <para>
   当函数被用作触发器时，字典 <literal>TD</literal> 包含与触发器相关的值。触发行位于
   <literal>TD["new"]</> 和/或 <literal>TD["old"]</> 中，取决于触发器事件。
   <literal>TD["event"]</> 以字符串形式包含事件（<literal>INSERT</>、<literal>UPDATE</>、
   <literal>DELETE</> 或 <literal>UNKNOWN</>）。
   <literal>TD["when"]</> 包含 <literal>BEFORE</>、
   <literal>AFTER</> 和 <literal>UNKNOWN</> 之一。
   <literal>TD["level"]</> 包含 <literal>ROW</>、
   <literal>STATEMENT</> 和 <literal>UNKNOWN</> 之一。
   <literal>TD["name"]</> 包含触发器的名称，
   <literal>TD["relid"]</> 包含触发器所作用的表的 OID。
   如果 <command>CREATE TRIGGER</> 命令包含参数，它们可以在 <literal>TD["args"][0]</> 到
   <literal>TD["args"][(<replaceable>n</>-1)]</> 中获得。
  </para>

  <para>
   如果 <literal>TD["when"]</literal> 是 <literal>BEFORE</>，你可以从 Python 函数返回
   <literal>None</literal> 或 <literal>"OK"</literal> 表示行未修改，
   返回 <literal>"SKIP"</> 中止该事件，或返回 <literal>"MODIFY"</>
   表示你修改了该行。
  </para>
 </sect1>

 <sect1 id="plpython-database">
  <title>数据库访问</title>

  <para>
   PL/Python 语言模块会自动导入一个名为 <literal>plpy</literal> 的 Python 模块。该模块中的函数和常量在 Python 代码中可以通过 <literal>plpy.<replaceable>foo</replaceable></literal> 这样的形式访问。目前 <literal>plpy</literal> 实现了函数
   <literal>plpy.debug(<replaceable>msg</>)</literal>、
   <literal>plpy.log(<replaceable>msg</>)</literal>、
   <literal>plpy.info(<replaceable>msg</>)</literal>、
   <literal>plpy.notice(<replaceable>msg</>)</literal>、
   <literal>plpy.warning(<replaceable>msg</>)</literal>、
   <literal>plpy.error(<replaceable>msg</>)</literal> 和
   <literal>plpy.fatal(<replaceable>msg</>)</literal>。<indexterm><primary>elog</><secondary>in PL/Python</></indexterm>
   <function>plpy.error</function> 和 <function>plpy.fatal</function> 实际上会抛出一个 Python 异常；如果该异常未被捕获，会传播到调用查询之外，导致当前事务或子事务中止。
   <literal>raise plpy.ERROR(<replaceable>msg</>)</literal> 和
   <literal>raise plpy.FATAL(<replaceable>msg</>)</literal> 分别等效于调用
   <function>plpy.error</function> 和
   <function>plpy.fatal</function>。
   其余函数只生成不同优先级别的消息。
   特定优先级别的消息是报告给客户端、写入服务器日志还是两者都做，由
   <xref linkend="guc-log-min-messages"> 和
   <xref linkend="guc-client-min-messages"> 配置变量控制。更多信息见 <xref linkend="runtime-config">。
  </para>

  <para>
   此外，<literal>plpy</literal> 模块提供了两个函数：
   <function>execute</function> 和 <function>prepare</function>。使用
   查询字符串和一个可选的限制参数调用
   <function>plpy.execute</function> 会执行该查询，并把结果在一个
   结果对象中返回。结果对象模拟了列表或字典对象。可以通过行号和
   列名访问结果对象。它还有这些附加方法：
   <function>nrows</function> 返回查询返回的行数，
   <function>status</function> 是 <function>SPI_execute()</function>
   的返回值。结果对象可以被修改。
  </para>

  <para>
   例如：
<programlisting>
rv = plpy.execute("SELECT * FROM my_table", 5)
</programlisting>
   会从 <literal>my_table</literal> 中返回最多 5 行。如果
   <literal>my_table</literal> 有一列名为 <literal>my_column</literal>，
   则可以这样访问它：
<programlisting>
foo = rv[i]["my_column"]
</programlisting>
  </para>

  <para>
   <indexterm><primary>preparing a query</><secondary>in PL/Python</></indexterm>
   第二个函数 <function>plpy.prepare</function> 为查询准备执行计划。
   若查询中有参数引用，则它以查询字符串和参数类型列表作为参数。例如：
<programlisting>
plan = plpy.prepare("SELECT last_name FROM my_users WHERE first_name = $1", [ "text" ])
</programlisting>
   <literal>text</literal> 是你要传给 <literal>$1</literal> 的变量的
   类型。准备好语句之后，使用函数 <function>plpy.execute</function>
   运行它：
<programlisting>
rv = plpy.execute(plan, [ "name" ], 5)
</programlisting>
   第三个参数是限制，它是可选的。
  </para>

  <para>
   当你使用 PL/Python 模块准备一个计划时，该计划会被自动保存。
   关于这意味着什么，请阅读 SPI 文档（<xref
   linkend="spi">）。为了在函数调用之间有效地利用这一点，需要使用持久存储字典
   <literal>SD</literal> 或 <literal>GD</literal>（见
   <xref linkend="plpython-funcs">）。例如：
<programlisting>
CREATE FUNCTION usesavedplan() RETURNS trigger AS $$
    if SD.has_key("plan"):
        plan = SD["plan"]
    else:
        plan = plpy.prepare("SELECT 1")
        SD["plan"] = plan
    # rest of function
$$ LANGUAGE plpythonu;
</programlisting>
  </para>
 </sect1>

<![IGNORE[
 <!-- NOT CURRENTLY SUPPORTED -->

 <sect1 id="plpython-trusted">
  <title>受限环境</title>

  <para>
   当前版本的 <application>PL/Python</application> 只作为可信语言发挥作用；对文件系统和其他本地资源的访问被禁用。具体来说，PL/Python 使用 Python 的受限执行环境，并进一步加以限制以防止使用文件 <function>open</> 调用，而且只允许导入特定列表中的模块。目前该列表包括：
   <literal>array</>、<literal>bisect</>、<literal>binascii</>、
   <literal>calendar</>、<literal>cmath</>、<literal>codecs</>、
   <literal>errno</>、<literal>marshal</>、<literal>math</>、<literal>md5</>、
   <literal>mpz</>、<literal>operator</>、<literal>pcre</>、
   <literal>pickle</>、<literal>random</>、<literal>re</>、<literal>regex</>、
   <literal>sre</>、<literal>sha</>、<literal>string</>、<literal>StringIO</>、
   <literal>struct</>、<literal>time</>、<literal>whrandom</> 和
   <literal>zlib</>。
  </para>
 </sect1>

]]>

</chapter>
