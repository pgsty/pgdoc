<!-- $PostgreSQL: pgsql/doc/src/sgml/gist.sgml,v 1.31 2009/06/12 19:48:53 tgl Exp $ -->

<chapter id="GiST">
<title>GiST 索引</title>

<sect1 id="gist-intro">
 <title>简介</title>

 <para>
   <indexterm>
    <primary>index</primary>
    <secondary>GiST</secondary>
   </indexterm>
   <indexterm>
    <primary>GiST</primary>
    <see>index</see>
   </indexterm>
   <acronym>GiST</acronym>是 Generalized Search Tree（通用搜索树）的缩写。它是一种平衡的树形访问方法，可作为实现任意索引方案的基本模板。B+-树、R 树以及许多其他索引方案都可以在<acronym>GiST</acronym>中实现。
 </para>

 <para>
  <acronym>GiST</acronym>的一个优点是，它使数据类型所属领域的专家而不是数据库专家，能够开发带有适当访问方法的自定义数据类型。
 </para>

  <para>
    这里的一些内容来自加州大学伯克利分校的 GiST 索引项目<ulink url="http://gist.cs.berkeley.edu/">网站</ulink>以及
    Marcel Kornacker 的论文
    <ulink url="http://www.sai.msu.su/~megera/postgres/gist/papers/concurrency/access-methods-for-next-generation.pdf.gz">
    Access Methods for Next-Generation Database Systems</ulink>。<productname>PostgreSQL</productname>中的<acronym>GiST</acronym>
    实现主要由 Teodor Sigaev 和 Oleg Bartunov 维护，他们的<ulink url="http://www.sai.msu.su/~megera/postgres/gist/">网站</ulink>上还有更多信息。
  </para>

</sect1>

<sect1 id="gist-extensibility">
 <title>可扩展性</title>

 <para>
   传统上，实现一种新的索引访问方法意味着大量艰难的工作。必须理解数据库的内部机制，例如锁管理器和预写式日志。<acronym>GiST</acronym>接口具有很高的抽象层次，只要求访问方法实现者实现被访问数据类型的语义。<acronym>GiST</acronym>层本身会处理并发、日志记录以及树结构的搜索。
 </para>

 <para>
   这种可扩展性不应与其他标准搜索树在可处理数据方面的可扩展性相混淆。例如，<productname>PostgreSQL</productname>支持可扩展的 B+-树和 R 树。这意味着你可以用<productname>PostgreSQL</productname>在任意数据类型上构建 B+-树或 R 树。但 B+-树只支持范围谓词（<literal>&lt;</literal>、<literal>=</literal>、<literal>&gt;</literal>），而 R 树只支持 n-D 范围查询（包含、被包含、相等）。
 </para>

 <para>
   因此，如果你用<productname>PostgreSQL</productname>的 B+-树为一个图像集合建立索引，你只能发出诸如<quote>imagex 是否等于 imagey</quote>、<quote>imagex 是否小于 imagey</quote>以及<quote>imagex 是否大于 imagey</quote>之类的查询。取决于你如何在这种上下文中定义<quote>等于</quote>、<quote>小于</quote>和<quote>大于</quote>，这可能仍然有用。不过，使用基于<acronym>GiST</acronym>的索引，你就可以构造出能够提出特定领域问题的查询方式，例如<quote>找出所有马的图片</quote>或者<quote>找出所有曝光过度的图片</quote>。
 </para>

 <para>
   要让一个 <acronym>GiST</acronym> 访问方法运行起来，只需实现七个用户定义的方法，这些方法定义了树中键的行为。当然，要支持复杂查询，这些方法本身也必须足够巧妙；但对于所有标准查询（B+-树、R 树等），它们都相对直接。简而言之，<acronym>GiST</acronym> 把可扩展性与通用性、代码复用以及清晰的接口结合了起来。
  </para>

</sect1>

<sect1 id="gist-implementation">
 <title>实现</title>

 <para>
   一个<acronym>GiST</acronym>索引操作符类必须提供七个方法：
 </para>

 <variablelist>
    <varlistentry>
     <term>consistent</term>
     <listitem>
      <para>
       给定树页面上的一个谓词<literal>p</literal>和一个用户查询<literal>q</literal>，如果可以确定对于给定的数据项<literal>p</literal>和<literal>q</literal>不可能同时为真，该方法将返回假。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>union</term>
     <listitem>
      <para>
       此方法合并树中的信息。给定一组项，该函数生成一个对所有这些项都为真的新谓词。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>compress</term>
     <listitem>
      <para>
       把数据项转换为适合在索引页面中物理存储的格式。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>decompress</term>
     <listitem>
      <para>
       <function>compress</function>方法的逆操作。把数据项的索引表示形式转换为数据库可以操纵的格式。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>penalty</term>
     <listitem>
      <para>
       返回一个值，指示把新项插入树的特定分支的<quote>代价</quote>。项将被沿着树中<function>penalty</function>最小的路径插入。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>picksplit</term>
     <listitem>
      <para>
       当必须进行页面分裂时，该函数决定页面上哪些项留在旧页面，哪些项移动到新页面。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>same</term>
     <listitem>
      <para>
       如果两个项完全相同则返回真，否则返回假。
      </para>
     </listitem>
    </varlistentry>

  </variablelist>

</sect1>

<sect1 id="gist-examples">
 <title>示例</title>

 <para>
  <productname>PostgreSQL</productname>源代码发行包包含了若干使用<acronym>GiST</acronym>实现的索引方法示例。核心系统目前为某些内置几何数据类型提供了与 R 树等价的功能（见<filename>src/backend/access/gist/gistproc.c</filename>）。下列<filename>contrib</filename>模块中也包含<acronym>GiST</acronym>操作符类：

 <variablelist>
  <varlistentry>
   <term>btree_gist</term>
   <listitem>
    <para>为多种数据类型提供与 B-Tree 等价的功能</para>
   </listitem>
  </varlistentry>

  <varlistentry>
   <term>cube</term>
   <listitem>
    <para>多维立方体的索引</para>
   </listitem>
  </varlistentry>

  <varlistentry>
   <term>intarray</term>
   <listitem>
    <para>一维 int4 值数组的 RD 树</para>
   </listitem>
  </varlistentry>

  <varlistentry>
   <term>ltree</term>
   <listitem>
    <para>树状结构的索引</para>
   </listitem>
  </varlistentry>

  <varlistentry>
   <term>pg_trgm</term>
   <listitem>
    <para>基于三字符组匹配的文本相似度</para>
   </listitem>
  </varlistentry>

  <varlistentry>
   <term>seg</term>
   <listitem>
    <para><quote>float 范围</quote>的索引</para>
   </listitem>
  </varlistentry>

  <varlistentry>
   <term>tsearch2</term>
   <listitem>
    <para>全文索引</para>
   </listitem>
  </varlistentry>
 </variablelist>
 </para>

</sect1>

<sect1 id="gist-recovery">
 <title>崩溃恢复</title>

 <para>
  通常，重放 WAL 日志足以在数据库崩溃之后恢复 GiST 索引的完整性。但也存在一些边角情况，索引状态在其中不会被完全重建。索引在功能上仍然正确，但可能出现一些性能下降。发生这种情况时，可以对该索引所在的表执行 <command>VACUUM</> 来修复索引，或者用
  <command>REINDEX</> 重建索引。在某些情况下，普通
  <command>VACUUM</> 并不够，需要 <command>VACUUM FULL</> 或
  <command>REINDEX</>。如果崩溃恢复期间出现这条日志消息，就表明需要执行上述操作之一：
<programlisting>
LOG:  index NNN/NNN/NNN needs VACUUM or REINDEX to finish crash recovery
</programlisting>
  或者，在例行的索引插入过程中出现这条日志消息也表明这一点：
<programlisting>
LOG:  index "FOO" needs VACUUM or REINDEX to finish crash recovery
</programlisting>
  如果普通 <command>VACUUM</> 发现自己无法完全完成恢复，它会返回一个提示：
<programlisting>
NOTICE:  index "FOO" needs VACUUM FULL or REINDEX to finish crash recovery
</programlisting>
 </para>
</sect1>

</chapter>
