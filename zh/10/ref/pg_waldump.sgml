<!--
doc/src/sgml/ref/pg_waldump.sgml
PostgreSQL documentation
-->

<refentry id="pgwaldump">
 <indexterm zone="pgwaldump">
  <primary>pg_waldump</primary>
 </indexterm>

 <refmeta>
  <refentrytitle><application>pg_waldump</application></refentrytitle>
  <manvolnum>1</manvolnum>
  <refmiscinfo>Application</refmiscinfo>
 </refmeta>

 <refnamediv>
  <refname>pg_waldump</refname>
  <refpurpose>以人类可读形式显示<productname>PostgreSQL</productname>数据库集簇的预写式日志</refpurpose>
 </refnamediv>

 <refsynopsisdiv>
  <cmdsynopsis>
   <command>pg_waldump</command>
   <arg rep="repeat" choice="opt"><option>option</option></arg>
   <arg choice="opt"><option>startseg</option>
    <arg choice="opt"><option>endseg</option></arg>
   </arg>
  </cmdsynopsis>
 </refsynopsisdiv>

 <refsect1 id="R1-APP-PGWALDUMP-1">
  <title>描述</title>
  <para>
   <command>pg_waldump</command>显示预写式日志（WAL），主要用于调试或教学。
  </para>

  <para>
   该工具只能由安装服务器的用户运行，因为它要求对数据目录具有只读访问权限。
  </para>
 </refsect1>

 <refsect1>
  <title>选项</title>

   <para>以下命令行选项控制输出的位置和格式：<variablelist>

     <varlistentry>
      <term><replaceable class="parameter">startseg</replaceable></term>
      <listitem>
       <para>
        从指定的 WAL 段文件开始读取。这会隐式决定搜索文件时所使用的路径以及要使用的时间线。
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><replaceable class="parameter">endseg</replaceable></term>
      <listitem>
       <para>
        读取完指定的 WAL 段文件后停止。
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-b</option></term>
      <term><option>--bkp-details</option></term>
      <listitem>
       <para>
        输出有关备份块的详细信息。
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-e <replaceable>end</replaceable></option></term>
      <term><option>--end=<replaceable>end</replaceable></option></term>
      <listitem>
       <para>
        在指定的 WAL 位置停止读取，而不是一直读取到日志流末尾。
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-f</option></term>
      <term><option>--follow</option></term>
      <listitem>
       <para>
        在到达有效 WAL 的末尾后，继续每秒轮询一次，等待新的 WAL 出现。
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-n <replaceable>limit</replaceable></option></term>
      <term><option>--limit=<replaceable>limit</replaceable></option></term>
      <listitem>
       <para>
        显示指定数量的记录，然后停止。
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-p <replaceable>path</replaceable></option></term>
      <term><option>--path=<replaceable>path</replaceable></option></term>
      <listitem>
       <para>
        指定一个用于搜索 WAL 段文件的目录，或者指定一个包含
        <literal>pg_wal</literal>子目录且其中包含此类文件的目录。默认会在当前目录、
        当前目录的<literal>pg_wal</literal>子目录以及<envar>PGDATA</envar>的
        <literal>pg_wal</literal>子目录中搜索。
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-r <replaceable>rmgr</replaceable></option></term>
      <term><option>--rmgr=<replaceable>rmgr</replaceable></option></term>
      <listitem>
       <para>仅显示由指定资源管理器生成的记录。如果传入的名称为<literal>list</literal>，则打印有效资源管理器名称的列表并退出。</para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-s <replaceable>start</replaceable></option></term>
      <term><option>--start=<replaceable>start</replaceable></option></term>
      <listitem>
       <para>
        从指定的 WAL 位置开始读取。默认会从找到的最早文件中的第一条有效 WAL 记录开始读取。
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-t <replaceable>timeline</replaceable></option></term>
      <term><option>--timeline=<replaceable>timeline</replaceable></option></term>
      <listitem>
       <para>读取日志记录时所使用的时间线。如果指定了<replaceable>startseg</replaceable>，默认使用其中的值；否则默认值为 1。</para>
      </listitem>
     </varlistentry>

     <varlistentry>
       <term><option>-V</option></term>
       <term><option>--version</option></term>
       <listitem>
       <para>
       打印<application>pg_waldump</application>的版本并退出。
       </para>
       </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-x <replaceable>xid</replaceable></option></term>
      <term><option>--xid=<replaceable>xid</replaceable></option></term>
      <listitem>
       <para>
        仅显示标记有给定事务 ID 的记录。
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-z</option></term>
      <term><option>--stats[=record]</option></term>
      <listitem>
       <para>显示汇总统计信息（记录和整页镜像的数量及大小），而不是单条记录。也可以选择按记录而不是按资源管理器生成统计信息。</para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-?</option></term>
      <term><option>--help</option></term>
       <listitem>
        <para>
         显示<application>pg_waldump</application>命令行参数的帮助信息，并退出。
        </para>
       </listitem>
      </varlistentry>
    </variablelist>
   </para>
 </refsect1>

 <refsect1>
  <title>注解</title>
  <para>
    服务器运行时，可能给出错误的结果。
  </para>

  <para>
    只显示指定的时间线（如果未指定，则使用默认时间线）。
    其他时间线中的记录会被忽略。
  </para>

  <para>
    <application>pg_waldump</application>不能读取带有<literal>.partial</literal>后缀的
    WAL 文件。如果需要读取这些文件，则必须从文件名中移除<literal>.partial</literal>
    后缀。
  </para>
 </refsect1>

 <refsect1>
  <title>另见</title>

  <simplelist type="inline">
   <member><xref linkend="wal-internals"></member>
  </simplelist>
 </refsect1>

</refentry>
