<!--
$PostgreSQL: pgsql/doc/src/sgml/external-projects.sgml,v 1.7 2004/12/29 23:36:46 tgl Exp $
-->

 <appendix id="external-projects">
  <title>外部项目</title>

  <para>
   <productname>PostgreSQL</productname>是一个复杂的软件项目，
   管理它并不容易。我们发现，对<productname>PostgreSQL</productname>的许多增强，
   如果与核心项目分开开发会更加高效。独立的项目可以拥有自己的开发团队、
   邮件列表、缺陷跟踪和发布时间表。虽然这种独立性让开发更容易，但也让用户的
   工作变得更难。用户不得不四处寻找满足自己需求的数据库增强功能。本节描述
   一些较流行的外部开发的增强功能，并指导你如何找到它们。
  </para>

  <para>
   许多与<productname>PostgreSQL</productname>相关的项目托管在
   <productname>GBorg</productname>（<ulink
   url="http://gborg.postgresql.org">http://gborg.postgresql.org</ulink>）
   或<productname>pgFoundry</productname>（<ulink
   url="http://pgfoundry.org">http://pgfoundry.org</ulink>）上。
   还有一些与<productname>PostgreSQL</productname>相关的项目托管在其他地方，
   不过你需要通过 Internet 搜索来找到它们。
  </para>

 <sect1 id="external-interfaces">
  <title>外部开发的接口</title>

  <indexterm>
   <primary>interfaces</primary>
  </indexterm>

  <para>
   <productname>PostgreSQL</productname>的基础发行版只包含很少的接口。
   <application>libpq</application>被打包在内，是因为它是主要的
   <application>C</application>接口，许多其他接口都是在它之上构建的。
   <application>ecpg</application>被打包在内，是因为它与服务器端语法绑定，
   因而在很大程度上依赖于数据库版本。所有其他接口都是独立的项目，
   必须单独安装。
  </para>

  <para>
   一些较流行的接口有：

   <variablelist>
    <varlistentry>
     <term>psqlODBC</term>
     <listitem>
      <para>
       这是<application>Windows</application>应用程序最常用的接口。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>pgjdbc</term>
     <listitem>
      <para>
       一个<application>JDBC</application>接口。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>Npgsql</term>
     <listitem>
      <para>
       面向较新<application>Windows</application>应用程序的
       <application>.Net</application>接口。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>libpqxx</term>
     <listitem>
      <para>
       一个较新的<application>C++</application>接口。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>libpq++</term>
     <listitem>
      <para>
       一个较老的<application>C++</application>接口。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>pgperl</term>
     <listitem>
      <para>
       一个<application>Perl</application>接口，其<acronym>API</acronym>与
       <application>libpq</application>类似。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>DBD-Pg</term>
     <listitem>
      <para>
       一个使用<acronym>DBD</acronym>标准<application>API</application>的
       <application>Perl</application>接口。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>pgtclng</term>
     <listitem>
      <para>
       <application>Tcl</application>接口的一个较新版本。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>pgtcl</term>
     <listitem>
      <para>
       <application>Tcl</application>接口的原始版本。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>PyGreSQL</term>
     <listitem>
      <para>
       一个<application>Python</application>接口库。
      </para>
     </listitem>
    </varlistentry>

   </variablelist>

   所有这些接口都可以在
   <productname>GBorg</productname>（<ulink
   url="http://gborg.postgresql.org">http://gborg.postgresql.org</ulink>）
   或<productname>pgFoundry</productname>（<ulink
   url="http://pgfoundry.org">http://pgfoundry.org</ulink>）找到。
   </para>

 </sect1>

 <sect1 id="external-extensions">
 <title>扩展</title>

  <indexterm>
   <primary>extensions</primary>
  </indexterm>

  <para>
   <productname>PostgreSQL</productname>从一开始就被设计为可扩展的。
   因此，装载到数据库中的扩展可以像数据库自带的功能一样工作。源码附带的
   <filename>contrib/</filename>目录中包含大量扩展。该目录中的
   <filename>README</filename>文件包含一个摘要。其中包括转换工具、
   全文索引、<acronym>XML</acronym>工具以及额外的数据类型和索引方法。
   其他一些扩展是独立开发的，例如<application>PostGIS</application>。
   甚至<application>PostgreSQL</application>的复制解决方案也是在外部开发的。
   例如，<application>Slony-I</application>是一个流行的主/从复制解决方案，
   它独立于核心项目开发。
  </para>

  <para>
   <productname>PostgreSQL</productname>有多种可用的管理工具。其中最流行的
   是<application>pgAdmin</application>，此外还有一些商业工具。
  </para>

 </sect1>
</appendix>
