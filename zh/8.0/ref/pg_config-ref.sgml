<!-- $PostgreSQL: pgsql/doc/src/sgml/ref/pg_config-ref.sgml,v 1.28 2008/02/18 14:51:48 petere Exp $ -->

<refentry id="app-pgconfig">
 <refmeta>
  <refentrytitle id="app-pgconfig-title">pg_config</refentrytitle>
  <manvolnum>1</manvolnum>
  <refmiscinfo>Application</refmiscinfo>
 </refmeta>

 <refnamediv>
  <refname>pg_config</refname>
  <refpurpose>获取已安装的<productname>PostgreSQL</productname>版本的信息</refpurpose>
 </refnamediv>

 <indexterm zone="app-pgconfig">
  <primary>pg_config</primary>
 </indexterm>

 <refsynopsisdiv>
  <cmdsynopsis>
   <command>pg_config</command>
   <group choice="req" rep="repeat">
    <arg>--bindir</arg>
    <arg>--includedir</arg>
    <arg>--includedir-server</arg>
    <arg>--libdir</arg>
    <arg>--pkglibdir</arg>
    <arg>--pgxs</arg>
    <arg>--configure</arg>
    <arg>--version</arg>
   </group>
  </cmdsynopsis>
 </refsynopsisdiv>

 <refsect1>
  <title>描述</>
  <para>
   <application>pg_config</application>工具打印当前已安装的<productname>PostgreSQL</productname>版本的配置参数。例如，希望与<productname>PostgreSQL</productname>进行接口的软件包可以使用它，以便找到所需的头文件和库。
  </para>
 </refsect1>


 <refsect1>
  <title>选项</title>

  <para>
   要使用 <application>pg_config</application>，请提供下列一个或多个选项：
   <variablelist>
    <varlistentry>
     <term><option>--bindir</option></>
     <listitem>
      <para>
       打印用户可执行文件的位置。例如，可以用它来寻找 <command>psql</command> 程序。通常这也是 <filename>pg_config</filename> 程序所在的位置。
      </para>
     </listitem>
    </varlistentry>



    <varlistentry>
     <term><option>--includedir</option></>
     <listitem>
      <para>
       打印 HTML 文档文件所在的位置。
      </para>
     </listitem>
    </varlistentry>


    <varlistentry>
     <term><option>--includedir-server</option></>
     <listitem>
      <para>
       打印其他 C 头文件所在的位置。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>--libdir</option></>
     <listitem>
      <para>
       打印服务器编程所用 C 头文件所在的位置。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>--pkglibdir</option></>
     <listitem>
      <para>
       打印目标代码库所在的位置。
      </para>
     </listitem>
    </varlistentry>





    <varlistentry>
     <term><option>--pgxs</option></>
     <listitem>
      <para>
       打印系统范围的配置文件所在的位置。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>--configure</option></>
     <listitem>
      <para>
       打印为构建 <productname>PostgreSQL</productname> 而配置时传递给 <filename>configure</filename> 脚本的选项。这可用于复现完全相同的配置，或者查明某个二进制包是用什么选项构建的。（不过注意，二进制包通常包含厂商特有的自定义补丁。）
      </para>
     </listitem>
    </varlistentry>








    <varlistentry>
     <term><option>--version</option></>
     <listitem>
      <para>
       打印 <productname>PostgreSQL</productname> 的版本。
      </para>
     </listitem>
    </varlistentry>
   </variablelist>

   如果给出了多个选项（<option>--version</option>除外），信息会按该顺序打印，每行一项。
  </para>
 </refsect1>


 <refsect1>
  <title>注解</title>

  <para>
   <option>--includedir-server</option> 选项是在 <productname>PostgreSQL</productname> 7.2 中加入的。在之前的版本中，服务器头文件与客户端头文件安装在同一位置，可以用 <option>--includedir</option> 选项查询。要让你的软件包同时处理这两种情况，请先尝试较新的选项，并测试退出状态以确定它是否成功。
  </para>
 </refsect1>



 <refsect1>
  <title>历史</title>

  <para>
   <command>pg_config</command>工具最早出现在
   <productname>PostgreSQL</productname> 7.1 中。
  </para>

  <para>
   在 <productname>PostgreSQL</productname> 7.1 之前的发行版中，还没有
   <command>pg_config</command>，当时不存在获取等效配置信息的方法。
  </para>
 </refsect1>

</refentry>
