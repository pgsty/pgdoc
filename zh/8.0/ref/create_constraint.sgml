<!--
$PostgreSQL: pgsql/doc/src/sgml/ref/create_constraint.sgml,v 1.19 2008/11/14 10:22:46 petere Exp $
PostgreSQL documentation
-->

<refentry id="SQL-CREATECONSTRAINT">
 <refmeta>
  <refentrytitle id="sql-createconstraint-title">CREATE CONSTRAINT TRIGGER</refentrytitle>
  <refmiscinfo>SQL - Language Statements</refmiscinfo>
 </refmeta>

 <refnamediv>
  <refname>CREATE CONSTRAINT TRIGGER</refname>
  <refpurpose>定义一个新的约束触发器</refpurpose>
 </refnamediv>

 <indexterm zone="sql-createconstraint">
  <primary>CREATE CONSTRAINT TRIGGER</primary>
 </indexterm>

 <refsynopsisdiv>
<synopsis>
CREATE CONSTRAINT TRIGGER <replaceable class="parameter">name</replaceable>
    AFTER <replaceable class="parameter">event</replaceable> [ OR ... ]
    ON <replaceable class="parameter">tablename</replaceable>
    [ FROM <replaceable class="parameter">referenced_table_name</replaceable> ]
    { NOT DEFERRABLE | [ DEFERRABLE ] { INITIALLY IMMEDIATE | INITIALLY DEFERRED } }
    FOR EACH ROW
    EXECUTE PROCEDURE <replaceable class="parameter">funcname</replaceable> ( <replaceable class="parameter">arguments</replaceable> )
</synopsis>
 </refsynopsisdiv>

 <refsect1>
  <title>描述</title>

  <para>
   <command>CREATE CONSTRAINT TRIGGER</command>创建一个<firstterm>约束触发器</firstterm>。它与常规触发器相同，只是触发器引发的时机可以使用<xref linkend="SQL-SET-CONSTRAINTS" endterm="SQL-SET-CONSTRAINTS-TITLE">来调整。约束触发器必须是<literal>AFTER ROW</literal>触发器。它们既可以在导致触发事件的那条语句结束时引发，也可以在包含它的事务结束时引发；在后一种情况下，称它们为<firstterm>被延迟的</firstterm>（deferred）。一个待处理的延迟触发器引发也可以通过使用<command>SET CONSTRAINTS</command>强制立即发生。
  </para>
 </refsect1>

 <refsect1>
  <title>参数</title>

  <variablelist>
   <varlistentry>
    <term><replaceable class="PARAMETER">name</replaceable></term>
    <listitem>
     <para>
      约束触发器的名称。这也是使用<command>SET CONSTRAINTS</command>修改触发器行为时所用的名称。该名称不能带模式限定&mdash;触发器继承其表的模式。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><replaceable class="PARAMETER">event</replaceable></term>
    <listitem>
     <para>
      <literal>INSERT</literal>、<literal>UPDATE</literal>或<literal>DELETE</literal>之一；它指定将引发该触发器的事件。可以使用<literal>OR</literal>指定多个事件。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><replaceable class="PARAMETER">tablename</replaceable></term>
    <listitem>
     <para>
      触发事件发生的表（可能是模式限定的）的名称。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><replaceable class="PARAMETER">referenced_table_name</replaceable></term>
    <listitem>
     <para>
      被该约束引用的另一张表（可能是模式限定的）的名称。这个选项用于外键约束，不推荐一般用途使用。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><literal>DEFERRABLE</literal></term>
    <term><literal>NOT DEFERRABLE</literal></term>
    <term><literal>INITIALLY IMMEDIATE</literal></term>
    <term><literal>INITIALLY DEFERRED</literal></term>
    <listitem>
     <para>
      触发器的默认时机。这些约束选项的细节见<xref linkend="SQL-CREATETABLE" endterm="SQL-CREATETABLE-TITLE">的文档。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><replaceable class="PARAMETER">funcname</replaceable></term>
    <listitem>
     <para>
      触发器引发时要调用的函数。详见<xref linkend="SQL-CREATETRIGGER" endterm="SQL-CREATETRIGGER-TITLE">。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><replaceable class="PARAMETER">arguments</replaceable></term>
    <listitem>
     <para>
      传递给触发器函数的可选参数字符串。详见<xref linkend="SQL-CREATETRIGGER" endterm="SQL-CREATETRIGGER-TITLE">。
     </para>
    </listitem>
   </varlistentry>
  </variablelist>
  </refsect1>

 <refsect1>
  <title>兼容性</title>
  <para>
   <command>CREATE CONSTRAINT TRIGGER</command>是<productname>PostgreSQL</productname>对<acronym>SQL</acronym>标准的一个扩展。
  </para>
 </refsect1>

 <refsect1>
  <title>See Also</title>

  <simplelist type="inline">
   <member><xref linkend="sql-createtrigger" endterm="sql-createtrigger-title"></member>
   <member><xref linkend="sql-droptrigger" endterm="sql-droptrigger-title"></member>
   <member><xref linkend="sql-set-constraints" endterm="sql-set-constraints-title"></member>
  </simplelist>
 </refsect1>
</refentry>
