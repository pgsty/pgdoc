<!--
$PostgreSQL: pgsql/doc/src/sgml/ref/postgres-ref.sgml,v 1.45 2004/11/15 06:32:15 neilc Exp $
PostgreSQL documentation
-->

<refentry id="APP-POSTGRES">
 <refmeta>
  <refentrytitle id="APP-POSTGRES-TITLE"><application>postgres</application></refentrytitle>
  <manvolnum>1</manvolnum>
  <refmiscinfo>Application</refmiscinfo>
 </refmeta>

 <refnamediv>
  <refname>postgres</refname>
  <refpurpose>以单用户模式运行一个<productname>PostgreSQL</productname>服务器</refpurpose>
 </refnamediv>

 <indexterm zone="app-postgres">
  <primary>postgres（程序）</primary>
 </indexterm>

 <refsynopsisdiv>
  <cmdsynopsis>
   <!-- standalone call -->
   <command>postgres</command>
   <arg>-A <group choice="plain"><arg>0</arg><arg>1</arg></group></arg>
   <arg>-B <replaceable>nbuffers</replaceable></arg>
   <arg>-c <replaceable>name</replaceable>=<replaceable>value</replaceable></arg>
   <arg>-d <replaceable>debug-level</replaceable></arg>
   <arg>--describe-config</arg>
   <arg>-D <replaceable>datadir</replaceable></arg>
   <arg>-e</arg>
   <arg>-E</arg>
   <arg>-f<group choice="plain"><arg>s</arg><arg>i</arg><arg>t</arg><arg>n</arg><arg>m</arg><arg>h</arg></group></arg>
   <arg>-F</arg>
   <arg>-N</arg>
   <arg>-o <replaceable>filename</replaceable></arg>
   <arg>-O</arg>
   <arg>-P</arg>
   <group>
    <arg>-s</arg>
    <arg>-t<group choice="plain"><arg>pa</arg><arg>pl</arg><arg>ex</arg></group></arg>
   </group>
   <arg>-S <replaceable>work-mem</replaceable></arg>
   <arg>-W <replaceable>seconds</replaceable></arg>
   <arg>--<replaceable>name</replaceable>=<replaceable>value</replaceable></arg>
   <arg choice="plain"><replaceable>database</replaceable></arg>
   <sbr>
   <!-- postmaster fork -->
   <command>postgres</command>
   <arg>-A <group choice="plain"><arg>0</arg><arg>1</arg></group></arg>
   <arg>-B <replaceable>nbuffers</replaceable></arg>
   <arg>-c <replaceable>name</replaceable>=<replaceable>value</replaceable></arg>
   <arg>-d <replaceable>debug-level</replaceable></arg>
   <arg>-D <replaceable>datadir</replaceable></arg>
   <arg>-e</arg>
   <arg>-f<group choice="plain"><arg>s</arg><arg>i</arg><arg>t</arg><arg>n</arg><arg>m</arg><arg>h</arg></group></arg>
   <arg>-F</arg>
   <arg>-o <replaceable>filename</replaceable></arg>
   <arg>-O</arg>
   <arg>-p <replaceable>database</replaceable></arg>
   <arg>-P</arg>
   <group>
    <arg>-s</arg>
    <arg>-t<group choice="plain"><arg>pa</arg><arg>pl</arg><arg>ex</arg></group></arg>
   </group>
   <arg>-S <replaceable>work-mem</replaceable></arg>
   <arg>-v <replaceable>protocol</replaceable></arg>
   <arg>-W <replaceable>seconds</replaceable></arg>
   <arg>--<replaceable>name</replaceable>=<replaceable>value</replaceable></arg>
  </cmdsynopsis>
 </refsynopsisdiv>

 <refsect1>
  <title>描述</title>

  <para>
   <command>postgres</command>可执行文件是真正处理查询的
   <productname>PostgreSQL</productname>服务器进程。通常不会直接调用它，而是启动一个<xref
   linkend="app-postmaster">多用户服务器。
  </para>

  <para>
   上面的第二种形式是
   <command>postgres</command>被<xref
   linkend="app-postmaster">调用的形式（这只是概念上的，因为<filename>postmaster</filename>和
   <filename>postgres</filename>实际上是同一个程序）；不应直接以这种方式调用它。第一种形式以交互式单用户模式直接调用服务器。这种模式的主要用途是在<xref linkend="app-initdb">执行引导期间使用。有时也会用它来调试或进行灾难恢复。
  </para>

  <para>
   从 shell 以交互模式调用时，用户可以输入查询，结果会打印到屏幕上，但其形式对开发者更有用，而非面向终端用户。但请注意，运行单用户服务器其实并不适合调试服务器，因为不会发生真实的进程间通信和锁定。
  </para>

  <para>
   运行独立服务器时，会话用户会被设置为 ID 为 1 的用户。该用户实际上不必存在，因此独立服务器可用于手工恢复系统目录遭受某些意外损坏的情况。在独立模式下，会向 ID 为 1 的用户隐式授予超级用户权限。
  </para>
 </refsect1>

 <refsect1>
  <title>选项</title>

   <para>
    当<command>postgres</command>由<xref
    linkend="app-postmaster">启动时，它继承后者设置的所有选项。此外，可以通过
    <command>postmaster</command>的
    <option>-o</option>开关传递<command>postgres</command>特有的选项。
   </para>

   <para>
    通过设置配置文件，你可以避免键入这些选项。详见<xref linkend="runtime-config">。某些（安全的）选项还可以由连接的客户端以应用相关的方式设置。例如，如果设置了环境变量<envar>PGOPTIONS</envar>，那么基于<application>libpq</application>的客户端就会将该字符串传给服务器，服务器会将其解释为
    <command>postgres</command>命令行选项。
   </para>

   <refsect2>
    <title>通用选项</title>

    <para>
     选项<option>-A</option>、<option>-B</option>、
     <option>-c</option>、<option>-d</option>、<option>-D</option>、
     <option>-F</option>和<option>--<replaceable>name</></option>的含义与<xref linkend="app-postmaster">中的相同，只是
     <literal>-d 0</>会阻止<command>postmaster</>的服务器日志级别传播到<command>postgres</>。
    </para>

    <variablelist>
     <varlistentry>
      <term><option>-e</option></term>
      <listitem>
       <para>
        将默认日期风格设置为<quote>European</quote>，即输入日期字段采用
        <literal>DMY</literal>顺序。这也会在某些日期输出格式中使日期显示为日在月前。
        更多信息见<xref linkend="datatype-datetime">。
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-o</option> <replaceable class="parameter">filename</replaceable></term>
      <listitem>
       <para>
        将所有服务器日志输出发送到
        <replaceable class="parameter">filename</replaceable>。
        如果<command>postgres</command>运行在
        <command>postmaster</command>之下，该选项会被忽略，并使用从
        <command>postmaster</command>继承的<systemitem>stderr</systemitem>。
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-P</option></term>
      <listitem>
       <para>
        读取系统表时忽略系统索引，但在修改表时仍然更新
        这些索引。这在从损坏的系统索引中恢复时很有用。
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-s</option></term>
      <listitem>
       <para>
        在每条命令结束时打印时间信息和其他统计信息。
        这对基准测试或者调整缓冲区数量很有用。
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-S</option> <replaceable class="parameter">work-mem</replaceable></term>
      <listitem>
       <para>指定内部排序和散列操作在转而使用临时磁盘文件之前可使用的内存量。有关<varname>work_mem</varname>配置参数的说明，请参阅<xref linkend="runtime-config-resource-memory">。</para>
      </listitem>
     </varlistentry>

    </variablelist>
   </refsect2>

   <refsect2>
    <title>独立模式的选项</title>

    <variablelist>
     <varlistentry>
      <term><replaceable class="parameter">database</replaceable></term>
      <listitem>
       <para>
        指定要访问的数据库名称。如果省略，则默认为用户名。
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-E</option></term>
      <listitem>
       <para>
        回显所有命令。
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-N</option></term>
      <listitem>
       <para>
        禁用换行符作为语句分隔符。
       </para>
      </listitem>
     </varlistentry>
    </variablelist>
   </refsect2>

   <refsect2>
    <title>半内部选项</title>

    <para>
     还有一些其他选项，主要供调试之用。这里列出这些选项，仅供<productname>PostgreSQL</productname>
     系统开发者使用。<emphasis>强烈不建议使用这些选项中的任何一个。</emphasis>此外，这些选项中的任何一个都可能在未来的版本中不经通知而消失或改变。
   </para>
    <variablelist>

     <varlistentry>
      <term><option>-f</option> <literal>{ s | i | m | n | h }</literal></term>
      <listitem>
       <para>
        禁止使用特定的扫描和连接方法：<literal>s</literal> 和 <literal>i</literal> 分别禁用顺序扫描和索引扫描，而 <literal>n</literal>、<literal>m</literal> 和 <literal>h</literal> 分别禁用嵌套循环、归并和哈希连接。
       </para>

       <note>
        <para>
         顺序扫描和嵌套循环连接都无法被完全禁用；<literal>-fs</literal>和
         <literal>-fn</literal>选项只是在优化器
         有其他选择时，尽量不使用这些计划类型。
        </para>
       </note>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-O</option></term>
      <listitem>
       <para>
        允许修改系统表的结构。该选项由
        <command>initdb</command>使用。
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-p</option> <replaceable class="parameter">database</replaceable></term>
      <listitem>
       <para>
        指示该进程是由
        <command>postmaster</command>启动的，并指定要使用的数据库等。
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-t</option> <literal>pa[rser] | pl[anner] | e[xecutor]</literal></term>
      <listitem>
       <para>
        打印与各个主要系统模块相关的每个查询的时间统计信息。该选项不能与<option>-s</option>选项一起
        使用。
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-v</option> <replaceable class="parameter">protocol</replaceable></term>
      <listitem>
       <para>
        指定该特定会话要使用的前端/后端协议
        版本号。
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-W</option> <replaceable class="parameter">seconds</replaceable></term>
      <listitem>
       <para>
        一旦遇到该选项，进程就会休眠指定的秒数。
        这为开发者把调试器附着到服务器进程提供了时间。
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>--describe-config</option></term>
      <listitem>
       <para>
        该选项会以制表符分隔的<command>COPY</command>格式导出服务器内部配置变量、说明以及默认值。
        它主要是为管理工具设计的。
       </para>
      </listitem>
     </varlistentry>

    </variablelist>
   </refsect2>
 </refsect1>

 <refsect1>
  <title>环境</title>

  <variablelist>
   <varlistentry>
    <term><envar>PGDATA</envar></term>

    <listitem>
     <para>
      默认数据目录位置
     </para>
    </listitem>
   </varlistentry>

  </variablelist>

  <para>
   对于在单用户模式下影响不大的其他环境变量，见<xref linkend="app-postmaster">。
  </para>
 </refsect1>

 <refsect1>
  <title>注解</title>

  <para>
   要取消一个正在运行的查询，可向运行该命令的<command>postgres</command>进程发送 <literal>SIGINT</literal> 信号。
  </para>

  <para>
   要告诉<command>postgres</command>重新加载配置文件，可发送<literal>SIGHUP</literal>信号。通常最好向
   <command>postmaster</command>发送<literal>SIGHUP</literal>；
   <command>postmaster</command>会继而向它的每个子进程发送<literal>SIGHUP</literal>。但在某些情况下，可能只需要一个
   <command>postgres</command>进程重新加载配置文件。
  </para>

  <para>
   <command>postmaster</command>使用<literal>SIGTERM</literal>
   通知一个<command>postgres</command>进程正常退出，使用
   <literal>SIGQUIT</literal>在不做正常清理的情况下终止。这些信号<emphasis>不应</emphasis>由用户使用。向一个<command>postgres</command>进程发送<literal>SIGKILL</literal>同样是不明智的&mdash;<command>postmaster</command>会把这解释为<command>postgres</command>中的一次崩溃，并作为其标准崩溃恢复过程的一部分，强制所有兄弟
   <command>postgres</command>进程退出。
  </para>

 </refsect1>

 <refsect1>
  <title>用法</title>

   <para>
    用类似下面的命令启动一个独立服务器：
<screen>
<userinput>postgres -D /usr/local/pgsql/data <replaceable>other-options</replaceable> my_database</userinput>
</screen>
    使用<option>-D</option>提供正确的数据库目录路径，或者
    确保已经设置了环境变量<envar>PGDATA</envar>。
    还要指定你想要操作的那个数据库的名称。
   </para>

   <para>
    通常，独立服务器将换行视为命令
    输入终止符；它不像<application>psql</application>那样会对分号进行智能处理。要让一个命令
    跨越多行，必须在除最后一个换行之外的每个
    换行前输入反斜线。
   </para>

   <para>
    但是，如果你使用<option>-N</option>命令行开关，换行
    并不会终止命令输入。在这种情况下，服务器会读取标准输入直到文件末尾
    （<acronym>EOF</acronym>）标记，然后把输入
    作为单个命令字符串处理。在这种情况下，反斜线-换行不会
    被特殊处理。
   </para>

   <para>
    要退出会话，输入<acronym>EOF</acronym>
    （通常是<keycombo action="simul"><keycap>Control</><keycap>D</></>）。
    如果你使用了<option>-N</option>，则需要连续输入两次<acronym>EOF</acronym>才能退出。
   </para>

   <para>
    注意，独立服务器不提供复杂的行编辑功能（例如没有命令历史）。
   </para>

 </refsect1>

 <refsect1>
  <title>另见</title>

  <para>
   <xref linkend="app-initdb">、
   <xref linkend="app-ipcclean">、
   <xref linkend="app-postmaster">
  </para>
 </refsect1>

</refentry>
