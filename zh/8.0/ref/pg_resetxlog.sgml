<!--
$PostgreSQL: pgsql/doc/src/sgml/ref/pg_resetxlog.sgml,v 1.9 2004/12/20 01:42:09 tgl Exp $
PostgreSQL documentation
-->

<refentry id="APP-PGRESETXLOG">
 <refmeta>
  <refentrytitle id="APP-PGRESETXLOG-TITLE"><application>pg_resetxlog</application></refentrytitle>
  <manvolnum>1</manvolnum>
  <refmiscinfo>Application</refmiscinfo>
 </refmeta>

 <refnamediv>
  <refname>pg_resetxlog</refname>
  <refpurpose>重置一个 <productname>PostgreSQL</productname> 数据库集簇的事务日志及其他控制信息</refpurpose>
 </refnamediv>

 <refsynopsisdiv>
  <cmdsynopsis>
   <command>pg_resetxlog</command>
   <arg> -f </arg>
   <arg> -n </arg>
   <arg> -o <replaceable class="parameter">oid</replaceable> </arg>
   <arg> -x <replaceable class="parameter">xid</replaceable> </arg>
   <arg> -l <replaceable class="parameter">timelineid</replaceable>,<replaceable class="parameter">fileid</replaceable>,<replaceable class="parameter">seg</replaceable> </arg>
   <arg choice="plain"><replaceable>datadir</replaceable></arg>
  </cmdsynopsis>
 </refsynopsisdiv>

 <refsect1 id="R1-APP-PGRESETXLOG-1">
  <title>描述</title>
  <para>
   <command>pg_resetxlog</command> 清空事务日志（WAL），并有选择地重置
   （存储在 <filename>pg_control</filename> 文件中的）一些其他控制信息。当这些文件
   损坏时，有时就需要这项功能。它应当只作为最后的手段使用，即当服务器因这种
   损坏而无法启动时。
  </para>

  <para>
   运行这条命令之后，应当就能启动服务器了，但请记住，由于部分提交的事务，
   数据库中可能包含不一致的数据。你应当立即转储数据、运行 <command>initdb</command>
   并重新装载。重新装载之后，检查不一致之处并按需修复。
  </para>

  <para>
   这个工具只能由安装服务器的用户运行，因为它需要对数据目录的读写访问权限。
   出于安全原因，你必须在命令行上指定数据目录。<command>pg_resetxlog</command>
   不使用环境变量 <envar>PGDATA</envar>。
  </para>

  <para>
   如果 <command>pg_resetxlog</command> 抱怨说无法确定 <filename>pg_control</>
   的有效数据，你可以通过指定 <literal>-f</>（强制）选项迫使它继续执行。
   在这种情况下，缺失的数据会用貌似合理的值代替。大多数字段都可以期望是匹配的，
   但下一个 OID、下一个事务 ID、WAL 起始地址和数据库区域字段可能需要人工协助。
   其中前三个可以用下面讨论的选项来设置。<command>pg_resetxlog</command>
   自身的环境是它对区域字段的猜测来源；请务必确保 <envar>LANG</envar> 等
   环境变量与运行 <command>initdb</command> 时的环境匹配。
   如果你无法为所有这些字段确定正确的值，仍可以使用 <literal>-f</>，但恢复出的数据库
   必须受到比平常更多的怀疑：必须立即转储并重新装载。在转储之前<emphasis>不要</>
   在数据库中执行任何修改数据的操作，因为任何此类操作都很可能使损坏加剧。
  </para>

  <para>
   <literal>-o</>、<literal>-x</> 和 <literal>-l</> 选项允许手动设置
   下一个 OID、下一个事务 ID 和 WAL 起始地址。只有当 <command>pg_resetxlog</command>
   无法通过读取 <filename>pg_control</> 确定合适的值时，才需要
   这些选项。下一个事务 ID 的安全值可以这样确定：在数据目录下的
   <filename>pg_clog</> 目录中查找数值最大的文件名，加一，
   再乘以 1048576。注意文件名为十六进制。通常最简单的做法也是
   用十六进制指定选项值。例如，如果 <filename>0011</> 是
   <filename>pg_clog</> 中最大的项，则
   <literal>-x 0x1200000</> 可行（五个尾随零提供正确的乘数）。
   WAL 起始地址应当大于数据目录下 <filename>pg_xlog</> 目录中现存的
   任何文件名。这些名称也是十六进制的，并且由三部分组成。第一
   部分是<quote>时间线 ID</quote>，通常应保持不变。第三部分不要选择大于
   255（<literal>0xFF</literal>）的值；应当递增第二部分并把第三部分重置为 0。
   例如，如果 <filename>00000001000000320000004A</filename> 是
   <filename>pg_xlog</filename> 中最大的项，<literal>-l 0x1,0x32,0x4B</literal> 即可；
   但如果最大的项是 <filename>000000010000003A000000FF</filename>，请选择
   <literal>-l 0x1,0x3B,0x0</literal> 或更大。
   没有同样简单的方法来确定一个超出数据库中最大 OID 的下一个 OID，
   但幸运的是，下一个 OID 设置得是否正确并不关键。
  </para>

  <para>
   <literal>-n</literal>（无操作）开关指示 <command>pg_resetxlog</command> 打印从 <filename>pg_control</filename> 重构出的值，然后不做任何修改直接退出。
   这主要是一个调试工具，但在真正允许<command>pg_resetxlog</command>继续执行之前，也可用作一次健全性检查。
  </para>
 </refsect1>

 <refsect1>
  <title>注解</title>

  <para>
   服务器正在运行时不得使用这条命令。如果 <command>pg_resetxlog</command>
   在数据目录中发现服务器锁文件，它将拒绝启动。如果服务器崩溃过，可能会留下
   锁文件；这种情况下你可以删除锁文件以允许 <command>pg_resetxlog</command>
   运行。但在这样做之前，请务必再三确认没有 <command>postmaster</command>
   或任何后端服务器进程仍然存活。
  </para>
 </refsect1>

</refentry>
