<!--
$PostgreSQL: pgsql/doc/src/sgml/ref/create_language.sgml,v 1.45 2008/11/14 10:22:46 petere Exp $
PostgreSQL documentation
-->

<refentry id="SQL-CREATELANGUAGE">
 <refmeta>
  <refentrytitle id="sql-createlanguage-title">CREATE LANGUAGE</refentrytitle>
  <refmiscinfo>SQL - Language Statements</refmiscinfo>
 </refmeta>

 <refnamediv>
  <refname>CREATE LANGUAGE</refname>
  <refpurpose>定义一种新的过程语言</refpurpose>
 </refnamediv>

 <indexterm zone="sql-createlanguage">
  <primary>CREATE LANGUAGE</primary>
 </indexterm>

 <refsynopsisdiv>
<synopsis>
CREATE [ TRUSTED ] [ PROCEDURAL ] LANGUAGE <replaceable class="parameter">name</replaceable>
    HANDLER <replaceable class="parameter">call_handler</replaceable> [ VALIDATOR <replaceable>valfunction</replaceable> ]
</synopsis>
 </refsynopsisdiv>

 <refsect1 id="sql-createlanguage-description">
  <title>描述</title>

  <para>使用<command>CREATE LANGUAGE</command>，<productname>PostgreSQL</productname>用户可以在<productname>PostgreSQL</productname>数据库中注册一种新的过程语言。随后，可以用这种新语言定义函数和触发器过程。用户必须拥有<productname>PostgreSQL</productname>超级用户权限才能注册新语言。</para>

  <para>
   <command>CREATE LANGUAGE</command>实际上是将该语言名称与负责执行
   用这种语言编写的函数的处理器函数关联起来。关于语言处理器的更多信息，
   参见<xref linkend="xplang">。
  </para>

  <para>
   注意，过程语言是各个数据库本地的。要让某种语言默认在所有数据库中可用，
   应把它安装到<literal>template1</literal>数据库中。
  </para>
 </refsect1>

 <refsect1 id="sql-createlanguage-parameters">
  <title>参数</title>

   <variablelist>
    <varlistentry>
     <term><literal>TRUSTED</literal></term>

     <listitem>
      <para><literal>TRUSTED</literal>指定该语言的调用处理器是安全的，也就是说，它不会让非特权用户获得任何绕过访问限制的功能。如果在注册该语言时省略这个关键字，只有拥有<productname>PostgreSQL</productname>超级用户权限的用户才能使用该语言创建新函数。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><literal>PROCEDURAL</literal></term>

     <listitem>
      <para>
       这是一个噪声词。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><replaceable class="parameter">name</replaceable></term>

     <listitem>
      <para>
       新过程语言的名称。语言名称不区分大小写。该名称必须在与数据库中的其他语言名称不同。
      </para>

      <para>
       为了向后兼容，名称可以用单引号括起来。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><literal>HANDLER</literal> <replaceable class="parameter">call_handler</replaceable></term>

     <listitem>
      <para><replaceable class="parameter">call_handler</replaceable>
       是一个先前注册的函数名称，它会被调用来执行该过程语言中的函数。
       过程语言的调用处理器必须使用某种编译型语言（例如 C）编写，并采用
       版本 1 调用约定。它必须在<productname>PostgreSQL</productname>
       中注册为一个不带参数且返回<type>language_handler</type>类型的
       函数。该返回类型是一种占位符类型，仅用于把
       该函数标识为调用处理器。
      </para>
     </listitem>
    </varlistentry>


    <varlistentry>
     <term><literal>VALIDATOR</literal> <replaceable class="parameter">valfunction</replaceable></term>

     <listitem>
      <para><replaceable class="parameter">valfunction</replaceable>
       是一个先前注册的函数名称，在创建该语言中的新函数时会调用它来验证
       该新函数。如果没有指定验证器函数，那么新函数在创建时不会被检查。
       验证器函数必须接受一个<type>oid</type>类型的参数，它将是待创建
       函数的 OID，并且它通常返回<type>void</type>。
      </para>

      <para>
       验证器函数通常会检查函数体的语法正确性，但它也可以检查函数的其他
       属性，例如该语言是否支持某些参数类型。要报告错误，验证器函数
       应使用<function>ereport()</function>函数。函数的返回值会被忽略。
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
 </refsect1>

 <refsect1 id="sql-createlanguage-notes">
  <title>注解</title>

  <para>
   通常，用户不应直接执行此命令。对于<productname>PostgreSQL</productname>发行版本中提供的过程语言，应使用<xref
   linkend="app-createlang">程序，它还会安装正确的调用处理器。（<command>createlang</command>会在内部调用<command>CREATE LANGUAGE</command>。）
  </para>

  <para>
   在 7.3 之前的<productname>PostgreSQL</productname>版本中，必须将处理器函数声明为返回占位符类型<type>opaque</type>，而不是<type>language_handler</type>。为了支持装载旧转储文件，<command>CREATE LANGUAGE</command>会接受声明为返回<type>opaque</type>的函数，但会发出提示，并将函数声明的返回类型改为<type>language_handler</type>。
  </para>

  <para>
   使用<xref linkend="sql-createfunction" endterm="sql-createfunction-title">命令创建新函数。
  </para>

  <para>
   使用<xref linkend="sql-droplanguage" endterm="sql-droplanguage-title">删除过程语言，或者更好的方法是使用<xref
   linkend="app-droplang">程序。
  </para>

  <para>
   系统目录<classname>pg_language</classname>（见<xref
   linkend="catalog-pg-language">）记录当前已安装语言的信息。此外，
   <command>createlang</command>有一个可以列出已安装语言的选项。
  </para>

  <para>
   要能够使用某种过程语言，用户必须被授予<literal>USAGE</literal>权限。如果已知某种语言是受信任的，
   <command>createlang</command>程序会自动把权限授予所有人。
  </para>
 </refsect1>

 <refsect1 id="sql-createlanguage-examples">
  <title>示例</title>

  <para>
   以下两条命令依次执行，将注册一种新的过程语言及相关的调用处理器。
 <programlisting>
CREATE FUNCTION plsample_call_handler() RETURNS language_handler
    AS '$libdir/plsample'
    LANGUAGE C;
CREATE LANGUAGE plsample
    HANDLER plsample_call_handler;
</programlisting></para>
 </refsect1>

 <refsect1 id="sql-createlanguage-compat">
  <title>兼容性</title>

  <para>
   <command>CREATE LANGUAGE</command>是一种
   <productname>PostgreSQL</productname>扩展。
  </para>
 </refsect1>

 <refsect1>
  <title>另见</title>

  <simplelist type="inline">
   <member><xref linkend="sql-alterlanguage" endterm="sql-alterlanguage-title"></member>
   <member><xref linkend="sql-createfunction" endterm="sql-createfunction-title"></member>
   <member><xref linkend="sql-droplanguage" endterm="sql-droplanguage-title"></member>
   <member><xref linkend="sql-grant" endterm="sql-grant-title"></member>
   <member><xref linkend="sql-revoke" endterm="sql-revoke-title"></member>
   <member><xref linkend="app-createlang" endterm="app-createlang-title"></member>
   <member><xref linkend="app-droplang" endterm="app-droplang-title"></member>
  </simplelist>
 </refsect1>
</refentry>
