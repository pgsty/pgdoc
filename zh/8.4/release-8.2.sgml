<!-- doc/src/sgml/release-8.2.sgml -->
<!-- See header comment in release.sgml about typical markup -->

 <sect1 id="release-8-2-23">
  <title>版本 8.2.23</title>

  <note>
  <title>发行日期</title>
  <simpara>2011-12-05</simpara>
 </note>

  <para>
   本次发布包含对 8.2.22 的多方面修复。有关 8.2 大版本中新特性的信息，请参见 <xref linkend="release-8-2">。
  </para>

  <para>
      这预计是 <productname>PostgreSQL</productname> 8.2.X 系列的最后一个发行版。建议用户尽快更新到较新的发行分支。
     </para>

  <sect2>
   <title>迁移到版本 8.2.23</title>

   <para>
    对于运行 8.2.X 的用户，不需要进行转储/恢复。</para>

   <para>
      不过，<literal>information_schema.referential_constraints</literal> 视图的定义中被发现存在一个长期错误。如果你依赖该视图的正确结果，应按下面第一条变更日志条目的说明替换其定义。
     </para>

   <para>
    不过，如果你是从早于 8.2.14 的版本升级，
    请参阅 <xref linkend="release-8-2-14">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      修复 <literal>information_schema.referential_constraints</literal> 视图中的缺陷（Tom Lane）
     </para>

     <para>
      该视图在把外键约束与它依赖的主键或唯一键约束匹配时不够谨慎。这可能导致某个外键约束完全不被显示、被显示多次，或者声称它依赖于一个并非其实际依赖的约束。
     </para>

     <para>
      由于该视图定义是由 <application>initdb</application> 安装的，仅靠升级不会修复问题。如果需要修复现有安装，可以（以超级用户身份）删除 <literal>information_schema</literal> 模式，然后通过载入 <filename><replaceable>SHAREDIR</replaceable>/information_schema.sql</filename> 重新创建它。（如果不确定 <replaceable>SHAREDIR</replaceable> 在哪里，运行 <literal>pg_config --sharedir</literal>。）这必须在每个要修复的数据库中重复执行。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>CREATE TABLE dest AS SELECT * FROM src</literal> 或 <literal>INSERT INTO dest SELECT * FROM src</literal> 期间与 TOAST 相关的数据损坏（Tom Lane）
     </para>

     <para>
      如果表曾被 <command>ALTER TABLE ADD COLUMN</command> 修改过，在某些极端情况下，尝试把它的数据原样复制到另一个表可能产生损坏的结果。该问题只会在 8.4 及更高版本中以这种确切形式出现，但我们也给更早的版本打了补丁，以防有其他代码路径可能触发同一缺陷。
     </para>
    </listitem>

    <listitem>
     <para>
      修复从过期 syscache 条目访问 toast 表时的竞态条件（Tom Lane）
     </para>

     <para>
      典型症状是诸如 <quote>missing chunk number 0 for toast value NNNNN in pg_toast_2619</quote> 的瞬时错误，其中引用的 toast 表总是属于某个系统目录。
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <type>money</type> 类型输入和输出的区域支持（Tom Lane）
     </para>

     <para>
      除了不支持所有标准 <link linkend="guc-lc-monetary"><varname>lc_monetary</varname></link> 格式化选项外，输入和输出函数还不一致，这意味着在某些区域设置下转储的 <type>money</type> 值无法重新读取。
     </para>
    </listitem>

    <listitem>
     <para>
      不让 <link linkend="guc-transform-null-equals"><varname>transform_null_equals</varname></link> 影响 <literal>CASE foo WHEN NULL ...</literal> 构造（Heikki Linnakangas）
     </para>

     <para>
      <varname>transform_null_equals</varname> 只应影响用户直接写的 <literal>foo = NULL</literal> 表达式，而不是这种形式的 <literal>CASE</literal> 在内部生成的相等性检查。
     </para>
    </listitem>

    <listitem>
     <para>
      更改外键触发器的创建顺序，以更好地支持自引用外键（Tom Lane）
     </para>

     <para>
      对于引用自身表的级联外键，行更新会作为一个事件同时触发 <literal>ON UPDATE</literal> 触发器和 <literal>CHECK</literal> 触发器。<literal>ON UPDATE</literal> 触发器必须先执行，否则 <literal>CHECK</literal> 会检查行的非最终状态，并可能抛出不恰当的错误。然而这些触发器的触发顺序由其名称决定，而由于触发器使用遵循 <quote>RI_ConstraintTrigger_NNNN</quote> 约定的自动生成名称，它们通常按创建顺序排序。彻底的修复需要修改该约定（我们将在 9.2 中做），但在现有版本中更改它似乎有风险。因此此补丁只更改触发器的创建顺序。遇到这类错误的用户应删除并重新创建外键约束，使其触发器处于正确顺序。
     </para>
    </listitem>

    <listitem>
     <para>
      在 <application>psql</application> 的命令历史中保留命令内的空行（Robert Haas）
     </para>

     <para>
      例如，如果从字符串字面量中删除了空行，先前的行为可能导致问题。
     </para>
    </listitem>

    <listitem>
     <para>
      使用首选版本的 <application>xsubpp</application> 构建 PL/Perl，而不一定是操作系统的主副本（David Wheeler、Alex Hunsaker）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <function>pgstatindex()</function> 及时响应查询取消中断（Robert Haas）
     </para>
    </listitem>

    <listitem>
     <para>
      确保 VPATH 构建正确安装所有服务器头文件（Peter Eisentraut）
     </para>
    </listitem>

    <listitem>
     <para>
      缩短详细错误消息中报告的文件名（Peter Eisentraut）
     </para>

     <para>
      常规构建一直只报告包含错误消息调用的 C 文件的名称，但 VPATH 构建此前报告绝对路径名。
     </para>
    </listitem>

    <listitem>
     <para>
      修复中美洲 Windows 时区名的解释（Tom Lane）
     </para>

     <para>
      把<quote>Central America Standard Time</quote>映射为 <literal>CST6</literal> 而不是 <literal>CST6CDT</literal>，因为中美洲各地通常不实行夏令时。
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 发行版 2011n（针对巴西、古巴、斐济、巴勒斯坦、俄罗斯的 DST 法规变更；以及对阿拉斯加和英属东非的历史修正）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-2-22">
  <title>版本 8.2.22</title>

  <note>
  <title>发行日期</title>
  <simpara>2011-09-26</simpara>
 </note>

  <para>
   本次发布包含对 8.2.21 的多方面修复。有关 8.2 大版本中新特性的信息，请参见 <xref linkend="release-8-2">。
  </para>

  <para>
      <productname>PostgreSQL</productname> 社区将于 2011 年 12 月停止对 8.2.X 版本系列的更新。建议用户尽快更新到较新的版本分支。
     </para>

  <sect2>
   <title>迁移到版本 8.2.22</title>

   <para>
      对于运行 8.2.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.2.14 的版本升级，请参阅 <xref linkend="release-8-2-14">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      修复 GiST 索引页面分裂处理中的多个缺陷（Heikki Linnakangas）
     </para>

     <para>
      发生概率很低，但它们可能导致索引损坏。
     </para>
    </listitem>

    <listitem>
     <para>
      避免 <command>ANALYZE</command> 中可能访问内存末尾之外（Noah Misch）
     </para>

     <para>
      这修复了概率极低的服务器崩溃情形。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 relcache init 文件失效中的竞态条件（Tom Lane）
     </para>

     <para>
      存在一个时间窗口，新的后端进程可能读到过期的 init 文件，却错过本应告知它数据已过期的失效消息。结果是目录访问中的离奇失败，通常在启动稍后阶段报 <quote>could not read block 0 in file ...</quote>。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 GiST 索引扫描结束时的内存泄漏（Tom Lane）
     </para>

     <para>
      执行许多独立 GiST 索引扫描的命令（例如对已包含许多行的表验证新的基于 GiST 的排他约束）会因此泄漏瞬时需要大量内存。
     </para>
    </listitem>

    <listitem>
     <para>
      修复构造大型有损位图时的性能问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复数组和路径创建函数，确保填充字节为零（Tom Lane）
     </para>

     <para>
      这避免了一些规划器认为语义相等的常量不相等的情况，这种情况会导致糟糕的优化。
     </para>
    </listitem>

    <listitem>
     <para>
      规避破坏 WAL 重放的 gcc 4.6.0 缺陷（Tom Lane）
     </para>

     <para>
      这可能导致服务器崩溃后已提交事务的丢失。
     </para>
    </listitem>

    <listitem>
     <para>
      修复视图中 <literal>VALUES</literal> 的转储缺陷（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      禁止对序列执行 <literal>SELECT FOR UPDATE/SHARE</literal>（Tom Lane）
     </para>

     <para>
      这种操作不像预期那样工作，可能导致失败。
     </para>
    </listitem>

    <listitem>
     <para>
      防御计算哈希表大小时的整数溢出（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <quote>peer</quote> 认证使用凭据控制消息时的可移植性缺陷（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>pg_srand48</function> 种子初始化中的笔误（Andres Freund）
     </para>

     <para>
      这导致未能使用所提供种子的所有位。该函数在大多数平台上不使用（只在没有 <function>srandom</function> 的平台上使用），而且无论如何看待，随机性低于预期的种子带来的潜在安全暴露似乎都很小。
     </para>
    </listitem>

    <listitem>
     <para>
      避免 <literal>LIMIT</literal> 与 <literal>OFFSET</literal> 值之和超过 2^63 时的整数溢出（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      为 <function>generate_series()</function> 的 <type>int4</type> 和 <type>int8</type> 版本添加溢出检查（Robert Haas）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>to_char()</function> 中移除尾随零的问题（Marti Raudsepp）
     </para>

     <para>
      在使用 <literal>FM</literal> 且小数点后没有数字位的格式中，小数点左侧的零可能被错误移除。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>pg_size_pretty()</function>，避免接近 2^63 的输入溢出（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>psql</application> 在从其他文件执行 <literal>COPY</literal> 期间脚本文件行号计数的问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_restore</application> 直接写数据库模式对 <varname>standard_conforming_strings</varname> 的处理（Tom Lane）
     </para>

     <para>
      当从以 <varname>standard_conforming_strings</varname> 设为 <literal>on</literal> 制作的归档文件直接恢复到数据库服务器时，<application>pg_restore</application> 可能发出不正确的命令。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>libpq</application> 的 LDAP 服务查找代码中的越界写入和内存泄漏（Albe Laurenz）
     </para>
    </listitem>

    <listitem>
     <para>
      在 <application>libpq</application> 中，避免使用非阻塞 I/O 和 SSL 连接时的失败（Martin Pihlak、Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      改进 libpq 对连接启动期间失败的处理（Tom Lane）
     </para>

     <para>
      特别是，对 SSL 连接启动期间服务器报告 <function>fork()</function> 失败的响应现在更合理。
     </para>
    </listitem>

    <listitem>
     <para>
      让 <application>ecpglib</application> 以 15 位精度写 <type>double</type> 值（Akira Kurosawa）
     </para>
    </listitem>

    <listitem>
     <para>
      应用上游对 blowfish 有符号字符缺陷的修复（CVE-2011-2483）（Tom Lane）
     </para>

     <para>
      <filename>contrib/pg_crypto</filename> 的 blowfish 加密代码在 char 为有符号的平台（大多数平台如此）上可能给出错误结果，导致加密口令比应有的强度更弱。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/seg</filename> 中的内存泄漏（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>pgstatindex()</function>，使其对空索引给出一致结果（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      允许使用 perl 5.14 构建（Alex Hunsaker）
     </para>
    </listitem>

    <listitem>
     <para>
      更新 configure 脚本探测系统函数是否存在的方法（Tom Lane）
     </para>

     <para>
      我们在 8.3 和 8.2 中使用的 autoconf 版本可能被执行链接时优化的编译器欺骗。
     </para>
    </listitem>

    <listitem>
     <para>
      修复构建和安装文件路径包含空格的各种问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 发行版 2011i（针对加拿大、埃及、俄罗斯、萨摩亚和南苏丹的 DST 法规变更）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-2-21">
  <title>版本 8.2.21</title>

  <note>
  <title>发行日期</title>
  <simpara>2011-04-18</simpara>
 </note>

  <para>
   本次发布包含对 8.2.20 的多方面修复。有关 8.2 大版本中新特性的信息，请参见 <xref linkend="release-8-2">。
  </para>

  <sect2>
   <title>迁移到版本 8.2.21</title>

   <para>
      对于运行 8.2.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.2.14 的版本升级，请参阅 <xref linkend="release-8-2-14">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      避免目录缓存初始化期间的潜在死锁（Nikhil Sontakke）
     </para>

     <para>
      某些情况下，缓存加载代码会在锁定索引的目录之前获取系统索引上的共享锁。这可能与尝试以另一种更标准的顺序获取排他锁的进程死锁。
     </para>
    </listitem>

    <listitem>
     <para>
      修复当目标元组存在并发更新时 <literal>BEFORE ROW UPDATE</literal> 触发器处理中的悬空指针问题（Tom Lane）
     </para>

     <para>
      已观察到该缺陷在尝试执行 <literal>UPDATE RETURNING ctid</literal> 时导致间歇性的 <quote>cannot extract system attribute from virtual tuple</quote> 失败。发生更严重错误（例如为更新后的元组生成错误索引项）的概率非常小。
     </para>
    </listitem>

    <listitem>
     <para>
      当表存在未决的延迟触发器事件时禁止 <command>DROP TABLE</command>（Tom Lane）
     </para>

     <para>
      此前 <command>DROP</command> 会照常执行，导致触发器最终触发时报 <quote>could not open relation with OID nnn</quote> 错误。
     </para>
    </listitem>

    <listitem>
     <para>
      修复涉及数组切片的 PL/Python 内存泄漏（Daniel Popowich）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_restore</application>，使其能处理 TOC 文件中的长行（超过 1KB）（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      加入更多防护，防止因过于激进的编译器优化导致的除零崩溃（Aurelien Jarno）
     </para>
    </listitem>

    <listitem>
     <para>
      支持在 FreeBSD 和 OpenBSD 的 MIPS 上使用 dlopen()（Tom Lane）
     </para>

     <para>
      此前有一个硬编码的假设：该系统函数在这些系统的 MIPS 硬件上不可用。现在改用编译期测试，因为较新的版本已经有了它。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 HP-UX 上的编译失败（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_regress</application> 在 Cygwin 上使用的路径分隔符（Andrew Dunstan）
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 发行版 2011f（针对智利、古巴、福克兰群岛、摩洛哥、萨摩亚和土耳其的 DST 法规变更；以及对南澳大利亚、阿拉斯加和夏威夷的历史修正）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-2-20">
  <title>版本 8.2.20</title>

  <note>
  <title>发行日期</title>
  <simpara>2011-01-31</simpara>
 </note>

  <para>
   本次发布包含对 8.2.19 的多方面修复。有关 8.2 大版本中新特性的信息，请参见 <xref linkend="release-8-2">。
  </para>

  <sect2>
   <title>迁移到版本 8.2.20</title>

   <para>
      对于运行 8.2.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.2.14 的版本升级，请参阅 <xref linkend="release-8-2-14">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      避免 <command>EXPLAIN</command> 尝试显示简单形式 <literal>CASE</literal> 表达式时的失败（Tom Lane）
     </para>

     <para>
      如果 <literal>CASE</literal> 的测试表达式是常量，规划器可能把 <literal>CASE</literal> 简化为令表达式显示代码困惑的形式，导致 <quote>unexpected CASE WHEN clause</quote> 错误。
     </para>
    </listitem>

    <listitem>
     <para>
      修复对现有下标范围之前的数组切片赋值的问题（Tom Lane）
     </para>

     <para>
      如果新增下标与第一个既有下标之间存在间隙，代码会错误计算需要从旧数组的空值位图复制多少条目，可能导致数据损坏或崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      避免规划器对非常久远的日期值出现意外的转换溢出（Tom Lane）
     </para>

     <para>
      <type>date</type> 类型支持的日期范围比 <type>timestamp</type> 类型能表示的更宽，但规划器假定它总是可以不受惩罚地把 date 转换为 timestamp。
     </para>
    </listitem>

    <listitem>
     <para>
      修复启用 <varname>standard_conforming_strings</varname> 时 <application>pg_restore</application> 对大对象（BLOB）的文本输出（Tom Lane）
     </para>

     <para>
      虽然直接恢复到数据库时工作正确，但如果要求 <application>pg_restore</application> 生成 SQL 文本输出且源数据库中已启用 <varname>standard_conforming_strings</varname>，字符串转义是不正确的。
     </para>
    </listitem>

    <listitem>
     <para>
      修复对包含 <literal>... &amp; !(subexpression) | ...</literal> 的 <type>tsquery</type> 值的错误解析（Tom Lane）
     </para>

     <para>
      包含这种操作符组合的查询没有被正确执行。同样的错误也存在于 <filename>contrib/intarray</filename> 的 <type>query_int</type> 类型和 <filename>contrib/ltree</filename> 的 <type>ltxtquery</type> 类型中。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/intarray</filename> 的 <type>query_int</type> 类型输入函数中的缓冲区越界（Apple）
     </para>

     <para>
      该缺陷构成安全风险，因为函数的返回地址可能被覆写。感谢 Apple Inc 的安全团队报告此问题并提供修复。（CVE-2010-4015）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/seg</filename> 的 GiST picksplit 算法中的缺陷（Alexander Korotkov）
     </para>

     <para>
      这可能导致 <type>seg</type> 列上的 GiST 索引相当低效，尽管答案并非真正错误。如果你有这样的索引，考虑在安装此更新后对其执行 <command>REINDEX</command>。（这与上一次更新中在 <filename>contrib/cube</filename> 中修复的缺陷相同。）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-2-19">
  <title>版本 8.2.19</title>

  <note>
  <title>发行日期</title>
  <simpara>2010-12-16</simpara>
 </note>

  <para>
   本次发布包含对 8.2.18 的多方面修复。有关 8.2 大版本中新特性的信息，请参见 <xref linkend="release-8-2">。
  </para>

  <sect2>
   <title>迁移到版本 8.2.19</title>

   <para>
      对于运行 8.2.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.2.14 的版本升级，请参阅 <xref linkend="release-8-2-14">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      强制 Linux 上的默认 <link linkend="guc-wal-sync-method"><varname>wal_sync_method</varname></link> 为 <literal>fdatasync</literal>（Tom Lane、Marti Raudsepp）
     </para>

     <para>
      Linux 上的默认值实际上多年来一直是 <literal>fdatasync</literal>，但最近的内核变更导致 <productname>PostgreSQL</productname> 改选 <literal>open_datasync</literal>。这一选择没有带来任何性能改进，反而在某些文件系统（特别是使用 <literal>data=journal</literal> 挂载选项的 <literal>ext4</literal>）上导致彻底失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 GIN 索引 WAL 重放逻辑中的若干缺陷（Tom Lane）
     </para>

     <para>
      这可能导致复制期间的 <quote>bad buffer id: 0</quote> 失败或索引内容损坏。
     </para>
    </listitem>

    <listitem>
     <para>
      修复当起始检查点 WAL 记录与其重做点不在同一 WAL 段时从基础备份恢复的问题（Jeff Davis）
     </para>
    </listitem>

    <listitem>
     <para>
      新增在 <literal>IA64</literal> 上检测寄存器栈溢出的支持（Tom Lane）
     </para>

     <para>
      <literal>IA64</literal> 架构有两个硬件栈。要完全防止栈溢出失败需要同时检查两者。
     </para>
    </listitem>

    <listitem>
     <para>
      在 <function>copyObject()</function> 中添加栈溢出检查（Tom Lane）
     </para>

     <para>
      给定足够复杂的查询，某些代码路径可能因栈溢出而崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      修复临时 GiST 索引中页面分裂的检测（Heikki Linnakangas）
     </para>

     <para>
      在临时索引中可能出现<quote>并发</quote>页面分裂，例如当插入时有打开的游标正在扫描该索引。GiST 未能检测到这种情况，因此当游标继续执行时可能给出错误结果。
     </para>
    </listitem>

    <listitem>
     <para>
      避免在 <command>ANALYZE</command> 复杂索引表达式时内存泄漏（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      确保使用整行 Var 的索引仍依赖于其表（Tom Lane）
     </para>

     <para>
      像 <literal>create index i on t (foo(t.*))</literal> 这样声明的索引在删除其表时不会自动被删除。
     </para>
    </listitem>

    <listitem>
     <para>
      不要<quote>内联</quote>带多个 <literal>OUT</literal> 参数的 SQL 函数（Tom Lane）
     </para>

     <para>
      这避免了因丢失预期结果行类型信息而可能出现的崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      修复当 <literal>ORDER BY</literal>、<literal>LIMIT</literal>、<literal>FOR UPDATE</literal> 或 <literal>WITH</literal> 附加到 <literal>INSERT ... VALUES</literal> 的 <literal>VALUES</literal> 部分时的正确行为（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>COALESCE()</literal> 表达式的常量折叠（Tom Lane）
     </para>

     <para>
      规划器有时会尝试求值实际上永远不可达的子表达式，可能导致意外错误。
     </para>
    </listitem>

    <listitem>
     <para>
      为 <structname>InhRelation</structname> 节点添加打印功能（Tom Lane）
     </para>

     <para>
      这避免了启用 <varname>debug_print_parse</varname> 且执行某些类型的查询时的失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复点到水平线段距离的错误计算（Tom Lane）
     </para>

     <para>
      该缺陷影响多个不同的几何距离测量操作符。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>PL/pgSQL</application> 对<quote>简单</quote>表达式的处理，使其在递归或错误恢复情况下不失败（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>PL/Python</application> 对集合返回函数的处理（Jan Urbanski）
     </para>

     <para>
      在生成集合结果的迭代器内尝试调用 SPI 函数会失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/cube</filename> 的 GiST picksplit 算法中的缺陷（Alexander Korotkov）
     </para>

     <para>
      这可能导致 <type>cube</type> 列上的 GiST 索引相当低效，尽管答案并非真正错误。如果你有这样的索引，考虑在安装此更新后对其执行 <command>REINDEX</command>。
     </para>
    </listitem>

    <listitem>
     <para>
      <filename>contrib/dblink</filename> 除了在创建新连接时外不再发出<quote>identifier will be truncated</quote>通知（Itagaki Takahiro）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/pgcrypto</filename> 在缺少公钥时可能的核心转储（Marti Raudsepp）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/xml2</filename> 的 XPath 查询函数中的内存泄漏（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 发行版 2010o（针对斐济和萨摩亚的 DST 法规变更；以及对香港的历史修正）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-2-18">
  <title>版本 8.2.18</title>

  <note>
  <title>发行日期</title>
  <simpara>2010-10-04</simpara>
 </note>

  <para>
   本次发布包含对 8.2.17 的多方面修复。有关 8.2 大版本中新特性的信息，请参见 <xref linkend="release-8-2">。
  </para>

  <sect2>
   <title>迁移到版本 8.2.18</title>

   <para>
      对于运行 8.2.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.2.14 的版本升级，请参阅 <xref linkend="release-8-2-14">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      在 PL/Perl 和 PL/Tcl 中为每个调用方 SQL 用户 ID 使用单独的解释器（Tom Lane）
     </para>

     <para>
      此变更防止了可通过破坏稍后将在同一会话中以另一 SQL 用户身份执行的 Perl 或 Tcl 代码（例如在 <literal>SECURITY DEFINER</literal> 函数内）而造成的安全问题。大多数脚本语言都提供许多实现这一点的手段，例如重新定义目标函数调用的标准函数或操作符。如果没有此变更，任何拥有 Perl 或 Tcl 语言使用权的 SQL 用户基本上都能以目标函数所有者的 SQL 权限为所欲为。
     </para>

     <para>
      此变更的代价是 Perl 和 Tcl 函数之间的有意通信变得更困难。为提供出路，PL/PerlU 和 PL/TclU 函数继续每会话只使用一个解释器。这不被视为安全问题，因为所有这类函数本来就以数据库超级用户的信任级别执行。
     </para>

     <para>
      声称提供可信执行的第三方过程语言很可能存在类似的安全问题。我们建议就安全关键用途联系你所依赖的任何 PL 的作者。
     </para>

     <para>
      感谢 Tim Bunce 指出此问题（CVE-2010-3433）。
     </para>
    </listitem>

    <listitem>
     <para>
      禁止以并非它预期配合使用的系统目录列的参数调用 <function>pg_get_expr()</function>，以防止可能的崩溃（Heikki Linnakangas、Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 Windows 共享内存分配代码（Tsutomu Yamada、Magnus Hagander）
     </para>

     <para>
      此缺陷导致常被报告的 <quote>无法重新附加到共享内存</quote>错误消息。这是不久前应用于较新分支的修复的向后移植。
     </para>
    </listitem>

    <listitem>
     <para>
      在 Windows 上把退出码 128（<literal>ERROR_WAIT_NO_CHILDREN</literal>）视为非致命（Magnus Hagander）
     </para>

     <para>
      高负载下，Windows 进程有时会在启动时以此错误码失败。此前 postmaster 把这视为 panic 情况并重启整个数据库，但这似乎反应过度。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>UNION ALL</literal> 成员关系可能被重复扫描的问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <quote>无法处理未规划的子查询</quote> 错误（Tom Lane）
     </para>

     <para>
      当子查询包含一个连接别名引用，而该引用展开成包含另一个子查询的表达式时，会出现此错误。
     </para>
    </listitem>

    <listitem>
     <para>
      在偶有报告的 btree 失败情形中把 PANIC 降为 ERROR，并在产生的错误消息中提供更多细节（Tom Lane）
     </para>

     <para>
      这应能改进系统对损坏索引的健壮性。
     </para>
    </listitem>

    <listitem>
     <para>
      防止 show_session_authorization() 在自动清理进程中崩溃（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      防御返回 setof record 但并非所有返回行实际为同一行类型的函数（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复对按引用传递的函数结果计算哈希时可能的失败（Tao Ma、Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      写入锁文件（<filename>postmaster.pid</filename> 和套接字锁文件两者）内容时注意对其执行 fsync（Tom Lane）
     </para>

     <para>
      如果机器在 postmaster 启动后不久崩溃，这一疏漏可能导致锁文件内容损坏。这又可能使后续启动 postmaster 的尝试失败，直到手工删除锁文件。
     </para>
    </listitem>

    <listitem>
     <para>
      避免在为深层嵌套的子事务分配 XID 时递归（Andres Freund、Robert Haas）
     </para>

     <para>
      在栈空间有限时，原来的编码可能导致崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <varname>log_line_prefix</varname> 的 <literal>%i</literal> 转义，它可能在后端启动早期产生垃圾（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复启用归档时 <command>ALTER TABLE ... SET TABLESPACE</command> 中可能的数据损坏（Jeff Davis）
     </para>
    </listitem>

    <listitem>
     <para>
      允许 <command>CREATE DATABASE</command> 和 <command>ALTER DATABASE ... SET TABLESPACE</command> 被查询取消中断（Guillaume Lelarge）
     </para>
    </listitem>

    <listitem>
     <para>
      在 PL/Python 中，防御 <function>PyCObject_AsVoidPtr</function> 和 <function>PyCObject_FromVoidPtr</function> 的空指针结果（Peter Eisentraut）
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <filename>contrib/dblink</filename> 对包含已删除列的表的处理（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/dblink</filename> 中 <quote>重复连接名</quote> 错误后的连接泄漏（Itagaki Takahiro）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/dblink</filename> 以正确处理超过 62 字节的连接名（Itagaki Takahiro）
     </para>
    </listitem>

    <listitem>
     <para>
      为 <filename>contrib/hstore</filename> 新增 <function>hstore(text, text)</function> 函数（Robert Haas）
     </para>

     <para>
      此函数是现已废弃的 <literal>=&gt;</literal> 操作符的推荐替代。向后移植它使得面向未来的代码可以用于较旧的服务器版本。注意该补丁只有在 <filename>contrib/hstore</filename> 于特定数据库中安装或重新安装之后才生效。用户也可以改为手工执行 <command>CREATE FUNCTION</command> 命令。
     </para>
    </listitem>

    <listitem>
     <para>
      更新构建基础设施和文档，反映源代码仓库从 CVS 迁移到 Git 的变化（Magnus Hagander 等）
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 发行版 2010l（针对埃及和巴勒斯坦的 DST 法规变更；以及对芬兰的历史修正）
     </para>

     <para>
      此变更还为两个密克罗尼西亚时区新增了名称：Pacific/Chuuk 现在优先于 Pacific/Truk（首选缩写为 CHUT 而非 TRUT），Pacific/Pohnpei 优先于 Pacific/Ponape。
     </para>
    </listitem>

    <listitem>
     <para>
      让 Windows 的 <quote>N. Central Asia Standard Time</quote> 时区映射到 Asia/Novosibirsk 而不是 Asia/Almaty（Magnus Hagander）
     </para>

     <para>
      Microsoft 在 KB976098 的时区更新中改变了此区域的 DST 行为。Asia/Novosibirsk 更匹配其新行为。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-2-17">
  <title>版本 8.2.17</title>

  <note>
  <title>发行日期</title>
  <simpara>2010-05-17</simpara>
 </note>

  <para>
   本次发布包含对 8.2.16 的多方面修复。有关 8.2 大版本中新特性的信息，请参见 <xref linkend="release-8-2">。
  </para>

  <sect2>
   <title>迁移到版本 8.2.17</title>

   <para>
      对于运行 8.2.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.2.14 的版本升级，请参阅 <xref linkend="release-8-2-14">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      用应用于整个解释器的操作码掩码来强制 <literal>plperl</literal> 中的限制，而不是使用 <filename>Safe.pm</filename>（Tim Bunce、Andrew Dunstan）
     </para>

     <para>
      近期的事态发展使我们确信 <filename>Safe.pm</filename> 太不安全，不能依靠它使 <literal>plperl</literal> 可信。此变更完全移除了对 <filename>Safe.pm</filename> 的使用，改为使用始终应用操作码掩码的独立解释器。此变更的良好副作用包括：现在可以在 <literal>plperl</literal> 中以自然的方式使用 Perl 的 <literal>strict</literal> 编译指令，Perl 的 <literal>$a</literal> 和 <literal>$b</literal> 变量在排序例程中按预期工作，以及函数编译显著加快。（CVE-2010-1169）
     </para>
    </listitem>

    <listitem>
     <para>
      防止 PL/Tcl 执行来自 <structname>pltcl_modules</structname> 的不可信代码（Tom）
     </para>

     <para>
      PL/Tcl 从数据库表自动装载 Tcl 代码的特性可能被利用于特洛伊木马攻击，因为对谁能创建该表或向其中插入没有限制。此变更禁用该特性，除非 <structname>pltcl_modules</structname> 由超级用户拥有。（但是，不检查该表上的权限，因此确实需要较不安全的模块表的安装仍可以向受信任的非超级用户授予适当权限。）此外，除非确实要执行 <literal>pltclu</literal> 函数，否则防止把代码装载到无限制的<quote>普通</quote>Tcl 解释器中。（CVE-2010-1170）
     </para>
    </listitem>

    <listitem>
     <para>
      修复在重建 relcache 条目期间收到缓存重置消息时可能的崩溃（Heikki）
     </para>

     <para>
      此错误是在 8.2.16 中修复相关失败时引入的。
     </para>
    </listitem>

    <listitem>
     <para>
      不允许非特权用户重置仅超级用户可用的参数设置（Alvaro）
     </para>

     <para>
      此前，如果非特权用户为自己运行 <literal>ALTER USER ... RESET ALL</literal>，或为他拥有的数据库运行 <literal>ALTER DATABASE ... RESET ALL</literal>，这将移除该用户或数据库的所有特殊参数设置，包括本应只可由超级用户更改的那些。现在，<command>ALTER</command> 只移除用户有权更改的参数。
     </para>
    </listitem>

    <listitem>
     <para>
      避免后端关闭发生在需要向日志条目添加 <literal>CONTEXT</literal> 内容时导致可能的崩溃（Tom）
     </para>

     <para>
      某些情况下上下文打印函数会失败，因为到打印日志消息时当前事务已被回滚。
     </para>
    </listitem>

    <listitem>
     <para>
      为现代 Perl 版本更新 pl/perl 的 <filename>ppport.h</filename>（Andrew）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 pl/python 中的若干内存泄漏（Andreas Freund、Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      防止 <application>psql</application> 在展开引用自身的变量时无限递归（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>psql</application> 的 <literal>\copy</literal>，不在 <literal>\copy (select ...)</literal> 内的点周围添加空格（Tom）
     </para>

     <para>
      在数字字面量的小数点周围添加空格会导致语法错误。
     </para>
    </listitem>

    <listitem>
     <para>
      确保 <filename>contrib/pgstattuple</filename> 函数及时响应取消中断（Tatsuhito Kasahara）
     </para>
    </listitem>

    <listitem>
     <para>
      让服务器启动正确处理 <function>shmget()</function> 对已存在的共享内存段返回 <literal>EINVAL</literal> 的情形（Tom）
     </para>

     <para>
      此行为曾在包括 OS X 在内的 BSD 衍生内核上观察到。它导致完全误导性的启动失败，抱怨共享内存请求大小过大。
     </para>
    </listitem>

    <listitem>
     <para>
      避免 Windows 上 syslogger 进程可能的崩溃（Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      更健壮地处理 Windows 注册表中不完整的时区信息（Magnus）
     </para>
    </listitem>

    <listitem>
     <para>
      更新已知 Windows 时区名称集合（Magnus）
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 发行版 2010j（针对阿根廷、澳大利亚南极、孟加拉、墨西哥、摩洛哥、巴基斯坦、巴勒斯坦、俄罗斯、叙利亚、突尼斯的 DST 法规变更；以及对台湾的历史修正）
     </para>

     <para>
      另外，把 <literal>PKST</literal>（巴基斯坦夏令时）加入默认时区缩写集合。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-2-16">
  <title>版本 8.2.16</title>

  <note>
  <title>发行日期</title>
  <simpara>2010-03-15</simpara>
 </note>

  <para>
   本次发布包含对 8.2.15 的多方面修复。有关 8.2 大版本中新特性的信息，请参见 <xref linkend="release-8-2">。
  </para>

  <sect2>
   <title>迁移到版本 8.2.16</title>

   <para>
      对于运行 8.2.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.2.14 的版本升级，请参阅 <xref linkend="release-8-2-14">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      新增配置参数 <varname>ssl_renegotiation_limit</varname>，控制对 SSL 连接进行会话密钥重协商的频率（Magnus）
     </para>

     <para>
      可以把它设为零以完全禁用重协商，这在使用有缺陷的 SSL 库时可能是必需的。特别是，某些厂商发布的 CVE-2009-3555 临时补丁会导致重协商尝试失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复后端启动期间可能的死锁（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复因未干净地处理 relcache 重载期间的错误而可能的崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复试图从子事务启动失败中恢复时可能的崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复与使用保存点以及客户端编码不同于服务器编码相关的服务器内存泄漏（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 GIST 索引页分裂的恢复结束清理期间发出的不正确 WAL 数据（Yoichi Hirai）
     </para>

     <para>
      如果在完成未完成的 GIST 插入之后、在恢复结束清理期间崩溃，这会导致索引损坏，或者更可能在 WAL 重放期间出错。
     </para>
    </listitem>

    <listitem>
     <para>
      让 <type>bit</type> 类型的 <function>substring()</function> 把任何负长度都视为<quote>字符串的其余全部</quote>（Tom）
     </para>

     <para>
      以前的编码只对 -1 这样处理，对其他负值会产生无效的结果值，可能导致崩溃（CVE-2010-0442）。
     </para>
    </listitem>

    <listitem>
     <para>
      修复整数到位串的转换：当输出位宽比给定整数宽出非 8 位倍数时，正确处理第一个小数字节（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复若干病态缓慢的正则表达式匹配情形（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复备份历史文件中的 <literal>STOP WAL LOCATION</literal> 条目：当结束位置恰好位于段边界时报告下一个 WAL 段的名称（Itagaki Takahiro）
     </para>
    </listitem>

    <listitem>
     <para>
      修复更多临时文件泄漏的情形（Heikki）
     </para>

     <para>
      这纠正了上一个小版本中引入的问题。失败的一种情形是返回集合的 plpgsql 函数在另一个函数的异常处理器中被调用。
     </para>
    </listitem>

    <listitem>
     <para>
      改进布尔变量情形的约束排除处理，特别是使排除带有 <quote>bool_column = false</quote> 约束的分区成为可能（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      读取 <filename>pg_hba.conf</filename> 及相关文件时，如果 <literal>@</literal> 出现在引号内，不要把 <literal>@something</literal> 当作文件包含请求；另外，永远不要把单独的 <literal>@</literal> 当作文件包含请求（Tom）
     </para>

     <para>
      这防止了角色名或数据库名以 <literal>@</literal> 开头时的古怪行为。如果需要包含路径名包含空格的文件，仍然可以做到，但必须写 <literal>@"/path to/file"</literal> 而不是把引号放在整个构造之外。
     </para>
    </listitem>

    <listitem>
     <para>
      防止在某些平台上把目录命名为 <filename>pg_hba.conf</filename> 及相关文件中的包含目标时出现无限循环（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>SSL_read</function> 或 <function>SSL_write</function> 失败而未设置 <varname>errno</varname> 时可能的无限循环（Tom）
     </para>

     <para>
      据报道这在某些 Windows 版本的 <application>openssl</application> 上可能发生。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>psql</application> 的 <literal>numericlocale</literal> 选项，使其不在 latex 和 troff 输出格式中格式化不应格式化的字符串（Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      当同时指定 <literal>ON_ERROR_STOP</literal> 和 <literal>--single-transaction</literal> 且隐含的 <command>COMMIT</command> 期间发生错误时，让 <application>psql</application> 返回正确的退出状态（3）（Bruce）
     </para>
    </listitem>

    <listitem>
     <para>
      修复组合列被设为 NULL 的一种情形下 plpgsql 的失败（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复从 PL/PerlU 调用 PL/Perl 函数或相反时可能的失败（Tim Bunce）
     </para>
    </listitem>

    <listitem>
     <para>
      在 PL/Python 中添加 <literal>volatile</literal> 标记，以避免可能的编译器特定错误行为（Zdenek Kotala）
     </para>
    </listitem>

    <listitem>
     <para>
      确保 PL/Tcl 完全初始化 Tcl 解释器（Tom）
     </para>

     <para>
      此疏漏唯一已知的症状是使用 Tcl 8.5 或更高版本时 Tcl 的 <literal>clock</literal> 命令行为异常。
     </para>
    </listitem>

    <listitem>
     <para>
      防止在向 <function>dblink_build_sql_*</function> 函数指定过多键列时 <filename>contrib/dblink</filename> 崩溃（Rushabh Lathia、Joe Conway）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/xml2</filename> 中因草率内存管理导致的若干崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <filename>contrib/xml2</filename> 在 Windows 上的构建更健壮（Andrew）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 Windows 信号处理中的竞态条件（Radu Ilie）
     </para>

     <para>
      此缺陷的一个已知症状是 <structname>pg_listener</structname> 中的行可能在重负载下被丢弃。
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 发行版 2010e（针对孟加拉、智利、斐济、墨西哥、巴拉圭、萨摩亚的 DST 法规变更）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-2-15">
  <title>版本 8.2.15</title>

  <note>
  <title>发行日期</title>
  <simpara>2009-12-14</simpara>
 </note>

  <para>
   本次发布包含对 8.2.14 的多方面修复。有关 8.2 大版本中新特性的信息，请参见 <xref linkend="release-8-2">。
  </para>

  <sect2>
   <title>迁移到版本 8.2.15</title>

   <para>
      对于运行 8.2.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.2.14 的版本升级，请参阅 <xref linkend="release-8-2-14">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      防范索引函数更改会话局部状态导致的间接安全威胁（Gurjeet Singh、Tom）
     </para>

     <para>
      此变更防止据称不可变的索引函数可能破坏超级用户的会话（CVE-2009-4136）。
     </para>
    </listitem>

    <listitem>
     <para>
      拒绝在公共名（CN）字段中包含嵌入空字节的 SSL 证书（Magnus）
     </para>

     <para>
      这防止在 SSL 验证期间证书与服务器或客户端名称的非预期匹配（CVE-2009-4034）。
     </para>
    </listitem>

    <listitem>
     <para>
      修复后端启动时缓存初始化期间可能的崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      防止信号在不安全的时刻中断 <literal>VACUUM</literal>（Alvaro）
     </para>

     <para>
      此修复防止了 <literal>VACUUM FULL</literal> 在已提交其元组移动后被取消时的 PANIC，以及普通 <literal>VACUUM</literal> 在截断表之后被中断时的瞬态错误。
     </para>
    </listitem>

    <listitem>
     <para>
      修复哈希表大小计算中因整数溢出而可能的崩溃（Tom）
     </para>

     <para>
      这可能发生在规划器对哈希连接结果大小的估计极大时。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <type>inet</type>/<type>cidr</type> 比较中非常罕见的崩溃（Chris Mikkelson）
     </para>
    </listitem>

    <listitem>
     <para>
      确保预备事务所持有的共享元组级锁不被忽略（Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      修复在子事务内访问的游标所用临时文件被过早删除的问题（Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 GiST 索引页分裂的不正确逻辑：当分裂依赖于索引的非首列时（Paul Ramsey）
     </para>
    </listitem>

    <listitem>
     <para>
      如果回收或删除旧 WAL 文件在检查点结束时失败，不要报错退出（Heikki）
     </para>

     <para>
      更好的做法是把该问题视为非致命并允许检查点完成。未来的检查点会重试删除。这类问题在正常运行中不应出现，但已知可能由设计不当的 Windows 反病毒和备份软件引起。
     </para>
    </listitem>

    <listitem>
     <para>
      确保 WAL 文件不会在 Windows 上被重复归档（Heikki）
     </para>

     <para>
      这是其他进程干扰删除不再需要的文件时可能出现的另一种症状。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PAM 口令处理使其更健壮（Tom）
     </para>

     <para>
      已知以前的代码在 Linux <literal>pam_krb5</literal> PAM 模块与 Microsoft Active Directory 域控制器组合下失败。它在其他地方可能也有问题，因为它对 PAM 栈会传递给它什么参数做了无根据的假设。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>CREATE OR REPLACE FUNCTION</literal> 期间所有权依赖的处理（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复从 <literal>plperl</literal> 调用 <literal>plperlu</literal> 或相反时的缺陷（Tom）
     </para>

     <para>
      内层函数的错误退出可能因未能为外层函数重新选择正确的 Perl 解释器而导致崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      修复重定义 PL/Perl 函数时的会话期内存泄漏（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      确保 Perl 数组在由返回集合的 PL/Perl 函数返回时被正确转换为 <productname>PostgreSQL</productname> 数组（Andrew Dunstan、Abhijit Menon-Sen）
     </para>

     <para>
      对非返回集合的函数这早已正确工作。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PL/Python 异常处理中的罕见崩溃（Peter）
     </para>
    </listitem>

    <listitem>
     <para>
      确保 <application>psql</application> 的 flex 模块用正确的系统头文件定义编译（Tom）
     </para>

     <para>
      这修复了在 <literal>--enable-largefile</literal> 导致生成代码不兼容变更的平台上的构建失败。
     </para>
    </listitem>

    <listitem>
     <para>
      让 postmaster 忽略连接请求包中的任何 <literal>application_name</literal> 参数，以改进与未来 libpq 版本的兼容性（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      更新时区缩写文件以符合当前现实（Joachim Wieland）
     </para>

     <para>
      这包括把 <literal>IDT</literal> 和 <literal>SGT</literal> 加入默认时区缩写集合。
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 发行版 2009s（针对南极洲、阿根廷、孟加拉、斐济、新库兹涅茨克、巴基斯坦、巴勒斯坦、萨摩亚、叙利亚的 DST 法规变更；以及对香港的历史修正）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-2-14">
  <title>版本 8.2.14</title>

  <note>
  <title>发行日期</title>
  <simpara>2009-09-09</simpara>
 </note>

  <para>
   本次发布包含对 8.2.13 的多方面修复。有关 8.2 大版本中新特性的信息，请参见 <xref linkend="release-8-2">。
  </para>

  <sect2>
   <title>迁移到版本 8.2.14</title>

   <para>
      对于运行 8.2.X 的用户，不需要进行转储/恢复。不过，如果你在 <type>interval</type> 列上有任何哈希索引，必须在更新到 8.2.14 后对它们执行 <command>REINDEX</command>。另外，如果你是从早于 8.2.11 的版本升级，请参阅 <xref linkend="release-8-2-11">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      在 <function>pg_start_backup()</function> 期间强制 WAL 段切换（Heikki）
     </para>

     <para>
      这避免了可能使基础备份不可用的极端情形。
     </para>
    </listitem>

    <listitem>
     <para>
      不允许在安全定义者函数内使用 <command>RESET ROLE</command> 和 <command>RESET SESSION AUTHORIZATION</command>（Tom、Heikki）
     </para>

     <para>
      这覆盖了先前禁止在安全定义者函数内使用 <command>SET ROLE</command> 和 <command>SET SESSION AUTHORIZATION</command> 的补丁中遗漏的一种情形。（参见 CVE-2007-6600）
     </para>
    </listitem>

    <listitem>
     <para>
      把对已装载的可装载模块的 <command>LOAD</command> 变为无操作（Tom）
     </para>

     <para>
      此前 <command>LOAD</command> 会试图卸载并重新装载模块，但这不安全也不太有用。
     </para>
    </listitem>

    <listitem>
     <para>
      不允许在 LDAP 认证期间使用空口令（Magnus）
     </para>
    </listitem>

    <listitem>
     <para>
      修复出现在外层聚合函数参数中的子 SELECT 的处理（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复与从 Sort 或 Materialize 计划节点的输出获取整行值相关的缺陷（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      回退规划器中当 AND 或 OR 列表中的子句超过 100 个时禁用部分索引和约束排除优化的变更（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <type>interval</type> 数据类型的哈希计算（Tom）
     </para>

     <para>
      这纠正了 interval 值上哈希连接的错误结果。它还更改了 interval 列上哈希索引的内容。如果你有任何这样的索引，必须在更新后对它们执行 <command>REINDEX</command>。
     </para>
    </listitem>

    <listitem>
     <para>
      把 <function>to_char(..., 'TH')</function> 在 <literal>'HH'</literal>/<literal>'HH12'</literal> 下当作大写序数后缀（Heikki）
     </para>

     <para>
      它以前被当作 <literal>'th'</literal>（小写）处理。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <replaceable>x</replaceable> 超过 200 万且使用整数日期时间时 <literal>INTERVAL '<replaceable>x</replaceable> ms'</literal> 的溢出（Alex Hunsaker）
     </para>
    </listitem>

    <listitem>
     <para>
      修复点与线段之间距离的计算（Tom）
     </para>

     <para>
      这导致多个几何操作符的结果不正确。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <type>money</type> 数据类型，使其在货币金额没有小数位的区域设置（例如日本）中工作（Itagaki Takahiro）
     </para>
    </listitem>

    <listitem>
     <para>
      正确地对像 <literal>00:12:57.9999999999999999999999999999</literal> 这样的日期时间输入四舍五入（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 GiST R-tree 操作符类中页分裂点选择不佳的问题（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      避免当输入值（几乎）按排序顺序时批量插入 GIN 索引的性能退化（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      在 PL/pgSQL 的某些上下文中正确强制执行 NOT NULL 域约束（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 plperl 初始化中的可移植性问题（Andrew Dunstan）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_ctl</application>，使其在 <filename>postgresql.conf</filename> 为空时不进入无限循环（Jeff Davis）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <filename>contrib/hstore</filename> 在键或值太长而无法放入其数据结构时抛出错误，而不是静默截断（Andrew Gierth）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/xml2</filename> 的 <function>xslt_process()</function>，使其正确处理最大参数数量（二十个）（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <application>libpq</application> 从 <command>COPY FROM STDIN</command> 期间错误中恢复的代码的健壮性（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      避免在两个库都安装时包含相互冲突的 readline 和 editline 头文件（Zdenek Kotala）
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 发行版 2009l（针对孟加拉、埃及、约旦、巴基斯坦、阿根廷/圣路易斯、古巴、毛里求斯、摩洛哥、巴勒斯坦、叙利亚、突尼斯的 DST 法规变更；其中对约旦仅作历史修正）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-2-13">
  <title>版本 8.2.13</title>

  <note>
  <title>发行日期</title>
  <simpara>2009-03-16</simpara>
 </note>

  <para>
   本次发布包含对 8.2.12 的多方面修复。有关 8.2 大版本中新特性的信息，请参见 <xref linkend="release-8-2">。
  </para>

  <sect2>
   <title>迁移到版本 8.2.13</title>

   <para>
      对于运行 8.2.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.2.11 的版本升级，请参阅 <xref linkend="release-8-2-11">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      防止编码转换失败时的错误递归崩溃（Tom）
     </para>

     <para>
      此变更扩展了上两个小版本中针对相关失败情形所做的修复。以前的修复是针对最初的问题报告量身定制的，但我们现在认识到，编码转换函数抛出的<emphasis>任何</emphasis>错误都可能在试图报告该错误时导致无限递归。因此，解决方案是：如果发现已处于递归错误报告情形，就禁用翻译和编码转换并报告错误消息的纯 ASCII 形式。（CVE-2009-0922）
     </para>
    </listitem>

    <listitem>
     <para>
      不允许 <command>CREATE CONVERSION</command> 为指定的转换函数使用错误的编码（Heikki）
     </para>

     <para>
      这防止了编码转换失败的一种可能情形。前一个变更是兜底，防范同一领域其他类型的失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>to_char()</function> 被给定与数据参数类型不适当的格式代码时的核心转储（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 C 区域设置与多字节编码一起使用时 <filename>contrib/tsearch2</filename> 可能失败的问题（Teodor）
     </para>

     <para>
      在 <type>wchar_t</type> 比 <type>int</type> 窄的平台（特别是 Windows）上可能崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/tsearch2</filename> 解析器处理包含多个 <literal>@</literal> 字符的类电子邮件字符串时的极端低效（Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      修复带隐式强制转换的 <literal>CASE WHEN</literal> 的反编译（Tom）
     </para>

     <para>
      在试图检查或转储视图时，这一错误在启用断言的构建中可能导致断言失败，在其他情况下可能导致 <quote>意外的 CASE WHEN 子句</quote>错误消息。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 TOAST 表行类型所有者可能的错误分配（Tom）
     </para>

     <para>
      如果 <command>CLUSTER</command> 或重写形式的 <command>ALTER TABLE</command> 由表所有者之外的人执行，表的 TOAST 表的 <structname>pg_type</structname> 条目最终会被标记为那个人所拥有。这不会立即引起问题，因为普通数据库操作不检查 TOAST 行类型的权限。但是，如果后来试图删除发出该命令的角色（在 8.1 或 8.2 中），或者在那之后 <application>pg_dump</application> 发出 <quote>数据类型的所有者似乎无效</quote>警告（在 8.3 中），则可能导致意外失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PL/pgSQL：不再把字符串中任何位置的 <command>INSERT</command> 之后的 <literal>INTO</literal> 当作 INTO-变量子句，而不仅是开头；特别是不要对 <command>CREATE RULE</command> 内的 <command>INSERT INTO</command> 失败（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      在块退出时完全清理 PL/pgSQL 错误状态变量（Ashesh Vashi 和 Dave Page）
     </para>

     <para>
      这对 PL/pgSQL 本身不是问题，但这一疏漏可能导致 PL/pgSQL 调试器在检查函数状态时崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      在 Windows 上重试失败的 <function>CallNamedPipe()</function> 调用（Steve Marshall、Magnus）
     </para>

     <para>
      该函数有时似乎会瞬时失败；我们此前把任何失败都当作硬错误，这可能扰乱 <command>LISTEN</command>/<command>NOTIFY</command> 以及其他操作。
     </para>
    </listitem>

    <listitem>
     <para>
      把 <literal>MUST</literal>（毛里求斯岛夏令时）加入已知时区缩写的默认列表（Xavier Bugaud）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-2-12">
  <title>版本 8.2.12</title>

  <note>
  <title>发行日期</title>
  <simpara>2009-02-02</simpara>
 </note>

  <para>
   本次发布包含对 8.2.11 的多方面修复。有关 8.2 大版本中新特性的信息，请参见 <xref linkend="release-8-2">。
  </para>

  <sect2>
   <title>迁移到版本 8.2.12</title>

   <para>
      对于运行 8.2.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.2.11 的版本升级，请参阅 <xref linkend="release-8-2-11">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      改进 <function>headline()</function> 函数中对 URL 的处理（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <function>headline()</function> 函数对超长标题的处理（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      防止在为指定的编码对创建了错误转换函数的编码转换时可能的断言失败或错误转换（Tom、Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PL/pgSQL 中执行的语句被重写为另一种语句时可能的断言失败，例如 <command>INSERT</command> 被重写为 <command>UPDATE</command>（Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      确保数据类型输入函数有可用快照（Tom）
     </para>

     <para>
      这主要影响声明了涉及用户定义稳定或不可变函数的 <literal>CHECK</literal> 约束的域。如果没有设置快照，这类函数通常会失败。
     </para>
    </listitem>

    <listitem>
     <para>
      让使用 SPI 的函数更安全地用于数据类型 I/O 中；特别是用于域检查约束（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      避免 <command>VACUUM</command> 中对小表的不必要锁定（Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      修复使 <literal>UPDATE RETURNING tableoid</literal> 返回零而不是正确 OID 的问题（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复传递等式应用于外连接子句时规划器对选择率的错误估计（Tom）
     </para>

     <para>
      这可能导致诸如 <literal>... from a left join b on a.a1 = b.b1 where a.a1 = 42 ...</literal> 之类的查询得到糟糕的计划。
     </para>
    </listitem>

    <listitem>
     <para>
      改进优化器对长 <literal>IN</literal> 列表的处理（Tom）
     </para>

     <para>
      此变更避免了在启用约束排除时在这类列表上浪费大量时间。
     </para>
    </listitem>

    <listitem>
     <para>
      确保可保持游标的内容不依赖于 TOAST 表的内容（Tom）
     </para>

     <para>
      此前，游标结果中的大字段值可能被表示为 TOAST 指针，如果被引用的表在游标被读取之前被删除，或者大值被删除随后被清理掉，这就会失败。普通游标不会发生这种情况，但对于保持到创建事务之后的游标则可能。
     </para>
    </listitem>

    <listitem>
     <para>
      修复返回集合的函数在未读完整个结果就被终止时的内存泄漏（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/dblink</filename> 的 <function>dblink_get_result(text,bool)</function> 函数（Joe）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/sslinfo</filename> 函数可能的垃圾输出（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>configure</application> 脚本，使其在无法获得 PL/Perl 的链接信息时正确报告失败（Andrew）
     </para>
    </listitem>

    <listitem>
     <para>
      让所有文档在适当之处引用 <literal>pgsql-bugs</literal> 和/或 <literal>pgsql-hackers</literal>，而不是现已停用的 <literal>pgsql-ports</literal> 和 <literal>pgsql-patches</literal> 邮件列表（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      更新时区数据文件到 <application>tzdata</application> 发行版 2009a（针对加德满都以及瑞士、古巴的历史 DST 修正）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-2-11">
  <title>版本 8.2.11</title>

  <note>
  <title>发行日期</title>
  <simpara>2008-11-03</simpara>
 </note>

  <para>
   本次发布包含对 8.2.10 的多方面修复。有关 8.2 大版本中新特性的信息，请参见 <xref linkend="release-8-2">。
  </para>

  <sect2>
   <title>迁移到版本 8.2.11</title>

   <para>
      对于运行 8.2.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.2.7 的版本升级，请参阅 <xref linkend="release-8-2-7">。另外，如果你运行的是之前的 8.2.X 版本，建议在升级后对所有 GiST 索引执行 <command>REINDEX</command>。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      修复 GiST 索引因删除后把错误的索引条目标记为<quote>死</quote>而损坏的问题（Teodor）
     </para>

     <para>
      这会导致索引搜索找不到本应找到的行。损坏的索引可以用 <command>REINDEX</command> 修复。
     </para>
    </listitem>

    <listitem>
     <para>
      修复客户端编码无法表示本地化错误消息时的后端崩溃（Tom）
     </para>

     <para>
      我们此前处理过类似问题，但如果 <quote>字符没有等价物</quote>消息本身无法转换，仍会失败。修复方法是在检测到这种情形时禁用本地化并发送纯 ASCII 错误消息。
     </para>
    </listitem>

    <listitem>
     <para>
      修复从触发器调用深层嵌套函数时可能的崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <replaceable>expression</replaceable> <literal>IN</literal> (<replaceable>expression-list</replaceable>) 查询的优化（Tom，想法来自 Robert Haas）
     </para>

     <para>
      右侧有查询变量的情形在 8.2.x 和 8.3.x 中的处理效率低于以前的版本。此修复为此类情形恢复了 8.1 的行为。
     </para>
    </listitem>

    <listitem>
     <para>
      修复当子 <literal>SELECT</literal> 出现在 <literal>FROM</literal> 中的函数调用、多行 <literal>VALUES</literal> 列表或 <literal>RETURNING</literal> 列表中时规则查询的错误展开（Tom）
     </para>

     <para>
      此问题通常的症状是 <quote>无法识别的节点类型</quote>错误。
     </para>
    </listitem>

    <listitem>
     <para>
      修复哈希聚合计划重扫描期间的内存泄漏（Neil）
     </para>
    </listitem>

    <listitem>
     <para>
      确保新定义的 PL/pgSQL 触发器函数被当作普通函数调用时报错（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      防止用 <command>ALTER SET TABLESPACE</command> 把表移到另一个表空间时 <structfield>relfilenode</structfield> 号可能的冲突（Heikki）
     </para>

     <para>
      该命令试图重用现有文件名，而不是选取目标目录中已知未使用的文件名。
     </para>
    </listitem>

    <listitem>
     <para>
      修复单个查询项匹配文本首词时 tsearch2 标题生成不正确的问题（Sushant Sinha）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <option>--enable-integer-datetimes</option> 构建中使用非 ISO datestyle 时 interval 值小数秒显示不当的问题（Ron Mayer）
     </para>
    </listitem>

    <listitem>
     <para>
      确保 <function>SPI_getvalue</function> 和 <function>SPI_getbinval</function> 在传入的元组和元组描述符列数不同时行为正确（Tom）
     </para>

     <para>
      当表添加或删除过列时这种情况是正常的，但这两个函数没有正确处理它。唯一可能的后果是错误的错误指示。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>ecpg</application> 对 <command>CREATE ROLE</command> 的解析（Michael）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>pg_ctl restart</literal> 最近损坏的问题（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      确保 <filename>pg_control</filename> 以二进制模式打开（Itagaki Takahiro）
     </para>

     <para>
      <application>pg_controldata</application> 和 <application>pg_resetxlog</application> 此前做得不正确，因此在 Windows 上可能失败。
     </para>
    </listitem>

    <listitem>
     <para>
      更新时区数据文件到 <application>tzdata</application> 发行版 2008i（针对阿根廷、巴西、毛里求斯、叙利亚的 DST 法规变更）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-2-10">
  <title>版本 8.2.10</title>

  <note>
  <title>发行日期</title>
  <simpara>2008-09-22</simpara>
 </note>

  <para>
   本次发布包含对 8.2.9 的多方面修复。有关 8.2 大版本中新特性的信息，请参见 <xref linkend="release-8-2">。
  </para>

  <sect2>
   <title>迁移到版本 8.2.10</title>

   <para>
      对于运行 8.2.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.2.7 的版本升级，请参阅 <xref linkend="release-8-2-7">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      修复 btree WAL 恢复代码中的缺陷（Heikki）
     </para>

     <para>
      如果 WAL 在页分裂操作中途结束，恢复就会失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <structfield>datfrozenxid</structfield> 可能的错误计算（Alvaro）
     </para>

     <para>
      此错误或许能解释最近一些无法删除旧 <structname>pg_clog</structname> 数据的报告。
     </para>
    </listitem>

    <listitem>
     <para>
      把本地锁计数器从 32 位拓宽到 64 位（Tom）
     </para>

     <para>
      这是针对计数器在足够长的事务中可能溢出的报告，该溢出会导致意外的 <quote>锁已被持有</quote>错误。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 GiST 索引扫描期间可能的元组重复输出（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      修复视图包含简单 <literal>UNION ALL</literal> 构造时遗漏的权限检查（Heikki）
     </para>

     <para>
      被引用表的权限被正确检查，但视图本身的权限没有。
     </para>
    </listitem>

    <listitem>
     <para>
      在执行器启动时添加检查，确保 <command>INSERT</command> 或 <command>UPDATE</command> 产生的元组与目标表的当前行类型匹配（Tom）
     </para>

     <para>
      <command>ALTER COLUMN TYPE</command> 之后重用先前缓存的计划可能产生这种情形。该检查防范随之而来的数据损坏和/或崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <command>DROP OWNED</command> 期间可能的重复删除（Tom）
     </para>

     <para>
      这通常会导致奇怪的错误，例如 <quote>关系 NNN 的缓存查找失败</quote>。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>AT TIME ZONE</literal>：先尝试把时区参数解释为时区缩写，仅在失败时才尝试完整时区名，而不是像以前那样相反（Tom）
     </para>

     <para>
      时间戳输入函数一直按此顺序解析有歧义的时区名。让 <literal>AT TIME ZONE</literal> 也这样做改进了一致性，并修复了 8.1 中引入的兼容性缺陷：在有歧义的情形下，我们现在的行为与 8.0 及更早版本相同，因为在旧版本中 <literal>AT TIME ZONE</literal> <emphasis>只</emphasis>接受缩写。
     </para>
    </listitem>

    <listitem>
     <para>
      修复日期时间输入函数在 64 位平台上正确检测整数溢出（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      防止显示带单位的配置参数时单位转换期间的整数溢出（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      改进向 syslog 写很长日志消息的性能（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      允许 <filename>pg_hba.conf</filename> 中 LDAP URL 的后缀部分包含空格（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>SELECT DISTINCT ON</literal> 查询上游标反向扫描的缺陷（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复嵌套子查询表达式的规划器缺陷（Tom）
     </para>

     <para>
      如果外层子查询不直接依赖父查询而内层依赖，外层的值可能不会为新的父查询行重新计算。
     </para>
    </listitem>

    <listitem>
     <para>
      修复规划器，使其估计产生布尔结果的 <literal>GROUP BY</literal> 表达式总是产生两个组，而不考虑表达式的内容（Tom）
     </para>

     <para>
      对于某些布尔测试（如 <replaceable>col</replaceable> <literal>IS NULL</literal>），这比常规的 <literal>GROUP BY</literal> 估计准确得多。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PL/pgSQL 在 <literal>FOR</literal> 循环的目标变量是包含组合类型字段的记录时不失败（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PL/Tcl 在 Tcl 8.5 下的正确行为，并更小心地处理发送到 Tcl 或从 Tcl 发出的数据的编码（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      在 Windows 上，通过阻止 <application>libpq</application> 在每次系统调用中发送超过 64kB 来绕过 Microsoft 的一个缺陷（Magnus）
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <application>pg_dump</application> 和 <application>pg_restore</application> 在发送 SQL 命令失败后的错误报告（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_ctl</application>，使其在 <literal>restart</literal> 期间正确保留 postmaster 命令行参数（Bruce）
     </para>
    </listitem>

    <listitem>
     <para>
      更新时区数据文件到 <application>tzdata</application> 发行版 2008f（针对阿根廷、巴哈马、巴西、毛里求斯、摩洛哥、巴基斯坦、巴勒斯坦和巴拉圭的 DST 法规变更）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-2-9">
  <title>版本 8.2.9</title>

  <note>
  <title>发行日期</title>
  <simpara>2008-06-12</simpara>
 </note>

  <para>
      此发行版在 8.2.8 的基础上包含一个严重和一个次要的修复。有关 8.2 主版本新特性的信息，请参阅 <xref linkend="release-8-2">。
     </para>

  <sect2>
   <title>迁移到版本 8.2.9</title>

   <para>
      对于运行 8.2.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.2.7 的版本升级，请参阅 <xref linkend="release-8-2-7">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      让 <function>pg_get_ruledef()</function> 给负常数加括号（Tom）
     </para>

     <para>
      在此修复之前，视图或规则中的负常数可能被转储成如 <literal>-42::integer</literal>，这有微妙的错误：由于操作符优先级规则，它应该是 <literal>(-42)::integer</literal>。通常这影响不大，但它可能与另一个最近的补丁相互作用，导致 <productname>PostgreSQL</productname> 拒绝原本有效的 <command>SELECT DISTINCT</command> 视图查询。由于这可能导致 <application>pg_dump</application> 输出无法重载，它被当作高优先级修复。转储输出确实不正确的已发布版本只有 8.3.1 和 8.2.7。
     </para>
    </listitem>

    <listitem>
     <para>
      让 <command>ALTER AGGREGATE ... OWNER TO</command> 更新 <structname>pg_shdepend</structname>（Tom）
     </para>

     <para>
      如果聚合后来涉及 <command>DROP OWNED</command> 或 <command>REASSIGN OWNED</command> 操作，这一疏漏可能导致问题。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-2-8">
  <title>版本 8.2.8</title>

  <note>
  <title>发行日期</title>
  <simpara>
      从未发布
     </simpara>
 </note>

  <para>
   本次发布包含对 8.2.7 的多方面修复。有关 8.2 大版本中新特性的信息，请参见 <xref linkend="release-8-2">。
  </para>

  <sect2>
   <title>迁移到版本 8.2.8</title>

   <para>
      对于运行 8.2.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.2.7 的版本升级，请参阅 <xref linkend="release-8-2-7">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      修复在 Windows 上使用 UTF-8 数据库编码和不同客户端编码时发生的 <literal>ERRORDATA_STACK_SIZE exceeded</literal> 崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <command>ALTER TABLE ADD COLUMN ... PRIMARY KEY</command>，使新列被正确检查是否已全部初始化为非空（Brendan Jurd）
     </para>

     <para>
      以前的版本完全忽略了对这一要求的检查。
     </para>
    </listitem>

    <listitem>
     <para>
      修复从多个都继承自共同祖先的父关系继承<quote>同一</quote>约束时 <command>CREATE TABLE</command> 可能失败的问题（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>pg_get_ruledef()</function>，显示 <command>UPDATE</command> 或 <command>DELETE</command> 目标表上附加的别名（如果有）（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复可能导致 <literal>too many LWLocks taken</literal> 失败的 GIN 缺陷（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      避免解压损坏数据时可能的崩溃（Zdenek Kotala）
     </para>
    </listitem>

    <listitem>
     <para>
      修复两处后端 SIGTERM 退出可能在共享内存中留下损坏状态的地方（Tom）
     </para>

     <para>
      如果 SIGTERM 用于一起关闭整个数据库集群，这两种情形都不太重要，但如果有人试图对单个后端发 SIGTERM，就有问题。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 ISO-8859-5 与其他编码之间的转换对西里尔<quote>Yo</quote>字符（带两个点的 <literal>e</literal> 和 <literal>E</literal>）的处理（Sergey Burladyan）
     </para>
    </listitem>

    <listitem>
     <para>
      修复几个数据类型输入函数（特别是 <function>array_in()</function>），它们允许结果中未使用的字节包含未初始化的不可预测值（Tom）
     </para>

     <para>
      这可能导致两个看起来相同的字面值不被视为相等的失败，导致解析器抱怨 <literal>ORDER BY</literal> 和 <literal>DISTINCT</literal> 表达式不匹配。
     </para>
    </listitem>

    <listitem>
     <para>
      修复正则表达式子串匹配（<literal>substring(<replaceable>string</replaceable> from <replaceable>pattern</replaceable>)</literal>）中的一个极端情形（Tom）
     </para>

     <para>
      问题发生在模式整体有匹配但用户指定了带括号的子表达式而该子表达式没有匹配时。例子是 <literal>substring('foo' from 'foo(bar)?')</literal>。由于 <literal>(bar)</literal> 未匹配，它应返回 NULL，但它错误地返回了整个模式的匹配（即 <literal>foo</literal>）。
     </para>
    </listitem>

    <listitem>
     <para>
      更新时区数据文件到 <application>tzdata</application> 发行版 2008c（针对摩洛哥、伊拉克、乔巴山、巴基斯坦、叙利亚、古巴和阿根廷/圣路易斯的 DST 法规变更）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>ecpg</application> 的 <function>PGTYPEStimestamp_sub()</function> 函数结果不正确的问题（Michael）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/tsearch2</filename> 的 <type>tsquery</type> 类型损坏的 GiST 比较函数（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/cube</filename> 函数可能的崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/xml2</filename> 的 <function>xpath_table()</function> 函数在输入查询返回 NULL 值时的核心转储（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/xml2</filename> 的 makefile，使其不覆盖 <literal>CFLAGS</literal>（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>DatumGetBool</literal> 宏，使其在 <application>gcc</application> 4.3 下不失败（Tom）
     </para>

     <para>
      此问题影响返回布尔的<quote>旧式</quote>（V0）C 函数。修复已在 8.3 中，但当时没有意识到需要向后移植。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-2-7">
  <title>版本 8.2.7</title>

  <note>
  <title>发行日期</title>
  <simpara>2008-03-17</simpara>
 </note>

  <para>
   本次发布包含对 8.2.6 的多方面修复。有关 8.2 大版本中新特性的信息，请参见 <xref linkend="release-8-2">。
  </para>

  <sect2>
   <title>迁移到版本 8.2.7</title>

   <para>
      对于运行 8.2.X 的用户，不需要进行转储/恢复。不过，如果你受到下述 Windows 区域设置问题的影响，可能需要在更新后对文本列上的索引执行 <command>REINDEX</command>。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      修复把不同字符组合视为相等的 Windows 区域设置下的字符串比较（Tom）
     </para>

     <para>
      此修复仅在 Windows 上且仅在使用 UTF-8 数据库编码时适用。所有其他情形的相同修复两年多前就已完成，但使用 UTF-8 的 Windows 走的是未更新的单独代码路径。如果你使用的区域设置把某些不同的字符串视为相等，可能需要 <command>REINDEX</command> 来修复文本列上的现有索引。
     </para>
    </listitem>

    <listitem>
     <para>
      修复不同系统目录上并发 <command>VACUUM FULL</command> 操作之间的潜在死锁（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复长期存在的 <command>LISTEN</command>/<command>NOTIFY</command> 竞态条件（Tom）
     </para>

     <para>
      极少数情况下，刚执行过 <command>LISTEN</command> 的会话可能收不到通知，即使按理应当收到，因为观察到执行 <command>NOTIFY</command> 的并发事务稍后提交了。
     </para>

     <para>
      此修复的一个副作用是：执行了尚未提交的 <command>LISTEN</command> 命令的事务如果去查看，将看不到该 <command>LISTEN</command> 在 <structname>pg_listener</structname> 中的行；以前能看到。这一行为从未被明确记录，但有些应用程序可能依赖旧行为。
     </para>
    </listitem>

    <listitem>
     <para>
      不允许在预备事务内使用 <command>LISTEN</command> 和 <command>UNLISTEN</command>（Tom）
     </para>

     <para>
      这以前是允许的，但尝试这样做会有各种不愉快的后果，特别是只要 <command>UNLISTEN</command> 未提交，发起的后端就无法退出。
     </para>
    </listitem>

    <listitem>
     <para>
      不允许在预备事务内删除临时表（Heikki）
     </para>

     <para>
      8.1 正确地禁止了这一点，但该检查在 8.2 中被无意破坏。
     </para>
    </listitem>

    <listitem>
     <para>
      修复使用哈希索引的查询期间发生错误时的罕见崩溃（Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      修复返回集合函数某些用法中的内存泄漏（Neil）
     </para>
    </listitem>

    <listitem>
     <para>
      修复公元前年份 2 月 29 日日期时间值的输入（Tom）
     </para>

     <para>
      以前的编码弄错了哪些年份是闰年。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <command>ALTER OWNER</command> 某些变体中的 <quote>无法识别的节点类型</quote>错误（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      确保锁等待中止时清除 <structname>pg_stat_activity</structname>.<structfield>waiting</structfield> 标志（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 Windows Vista 上进程权限的处理（Dave、Magnus）
     </para>

     <para>
      特别是，此修复允许以 Administrator 用户启动服务器。
     </para>
    </listitem>

    <listitem>
     <para>
      更新时区数据文件到 <application>tzdata</application> 发行版 2008a（特别是智利最近的变更）；调整时区缩写 <literal>VET</literal>（委内瑞拉）为 UTC-4:30 而不是 UTC-4:00（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_ctl</application>，使其从命令行选项正确提取 postmaster 的端口号（Itagaki Takahiro、Tom）
     </para>

     <para>
      此前 <literal>pg_ctl start -w</literal> 可能试图在错误端口上联系 postmaster，导致虚假的启动失败报告。
     </para>
    </listitem>

    <listitem>
     <para>
      使用 <option>-fwrapv</option> 防御最新 <application>gcc</application> 版本可能的错误优化（Tom）
     </para>

     <para>
      已知用 <application>gcc</application> 4.3 或更高版本构建 <productname>PostgreSQL</productname> 时这是必需的。
     </para>
    </listitem>


    <listitem>
     <para>
      正确强制执行超过 <literal>INT_MAX</literal> 微秒（约 35 分钟）的 <varname>statement_timeout</varname> 值（Tom）
     </para>

     <para>
      此缺陷只影响使用 <option>--enable-integer-datetimes</option> 的构建。
     </para>
    </listitem>

    <listitem>
     <para>
      修复常量折叠简化子查询时 <quote>意外的 PARAM_SUBLINK ID</quote>规划器错误（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>IS NULL</literal> 和 <literal>NOT</literal> 表达式的约束排除处理中的逻辑错误（Tom）
     </para>

     <para>
      规划器有时会因 NULL 结果的可能性而排除本不应排除的分区。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <quote>无法构建任何 N 路连接</quote>规划器错误的另一个原因（Tom）
     </para>

     <para>
      这可能发生在无子句连接需要在连接子句可被利用之前被强制的情形。
     </para>
    </listitem>

    <listitem>
     <para>
      修复外连接规划中不正确的常量传播（Tom）
     </para>

     <para>
      规划器有时可能错误地断定变量可被约束为等于常量，导致错误的查询结果。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>ORDER BY</literal> 和 <literal>GROUP BY</literal> 中常量表达式的显示（Tom）
     </para>

     <para>
      显式转换的常量会被错误显示。这例如可能导致视图定义在转储和重载期间损坏。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>libpq</application>，使其在 COPY OUT 期间正确处理 NOTICE 消息（Tom）
     </para>

     <para>
      只观察到此失败在用户定义数据类型的输出例程发出 NOTICE 时发生，但不能保证它不会因其他原因发生。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-2-6">
  <title>版本 8.2.6</title>

  <note>
  <title>发行日期</title>
  <simpara>2008-01-07</simpara>
 </note>

  <para>
      此发行版包含 8.2.5 之后的多种修复，包括对重要安全问题的修复。有关 8.2 主版本新特性的信息，请参阅 <xref linkend="release-8-2">。
     </para>

  <sect2>
   <title>迁移到版本 8.2.6</title>

   <para>
    对于运行 8.2.X 的用户，不需要进行转储/恢复。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      防止索引中的函数以运行 <command>VACUUM</command>、<command>ANALYZE</command> 等的用户的权限执行（Tom）
     </para>

     <para>
      每当产生新的表条目时，都会求值索引表达式和部分索引谓词中使用的函数。人们早就明白，如果修改不可信用户拥有的表，这会带来特洛伊木马代码执行的风险。（注意触发器、默认值、检查约束等也构成同类风险。）但索引中的函数带来额外危险，因为它们会由例程维护操作（如 <command>VACUUM FULL</command>）执行，而这些操作通常在超级用户账户下自动执行。例如，恶意用户可以通过设置特洛伊木马索引定义并等待下一次例程清理来以超级用户权限执行代码。修复安排标准维护操作（包括 <command>VACUUM</command>、<command>ANALYZE</command>、<command>REINDEX</command> 和 <command>CLUSTER</command>）以表所有者而非调用用户的身份执行，使用已用于 <literal>SECURITY DEFINER</literal> 函数的同一权限切换机制。为防止绕过此安全措施，现在禁止在 <literal>SECURITY DEFINER</literal> 上下文中执行 <command>SET SESSION AUTHORIZATION</command> 和 <command>SET ROLE</command>。（CVE-2007-6600）
     </para>
    </listitem>

    <listitem>
     <para>
      修复正则表达式包中的若干缺陷（Tom、Will Drewry）
     </para>

     <para>
      精心构造的正则表达式模式可能导致崩溃、无限或接近无限的循环和/或大量内存消耗，这些都对接受来自不可信来源的正则搜索模式的应用程序构成拒绝服务风险。（CVE-2007-4769、CVE-2007-4772、CVE-2007-6067）
     </para>
    </listitem>

    <listitem>
     <para>
      作为安全措施，要求使用 <filename>/contrib/dblink</filename> 的非超级用户只使用口令认证（Joe）
     </para>

     <para>
      8.2.5 中出现的修复并不完整，它只堵住了部分 <filename>dblink</filename> 函数的漏洞。（CVE-2007-6601、CVE-2007-3278）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 GIN 索引 WAL 重放中的缺陷（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <varname>maintenance_work_mem</varname> 为 4GB 或更多时 GIN 索引构建无法正确工作的问题（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      更新时区数据文件到 <application>tzdata</application> 发行版 2007k（特别是阿根廷最近的变更）（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      改进规划器在非 C 区域设置下对 LIKE/正则表达式估计的处理（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复深层外连接嵌套的规划速度问题以及可能糟糕的连接顺序选择（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复某些 <literal>WHERE false AND var IN (SELECT ...)</literal> 情形下的规划器失败（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <command>CREATE TABLE ... SERIAL</command> 和 <command>ALTER SEQUENCE ... OWNED BY</command> 不更改序列的 <function>currval()</function> 状态（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      保留被 <command>ALTER TABLE ... ALTER COLUMN TYPE</command> 重建的索引的表空间和存储参数（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      让归档恢复总是开始新的 WAL 时间线，而不是只在使用恢复停止时间时（Simon）
     </para>

     <para>
      这避免了试图覆盖最后一个 WAL 段现有归档副本的极端情形风险，而且看起来比原来的定义更简单干净。
     </para>
    </listitem>

    <listitem>
     <para>
      当表太小而不适用时，让 <command>VACUUM</command> 不使用全部 <varname>maintenance_work_mem</varname>（Alvaro）
     </para>
    </listitem>

    <listitem>
     <para>
      修复使用多字节数据库编码时 <function>translate()</function> 可能的崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <function>corr()</function> 对负相关值返回正确结果（Neil）
     </para>
    </listitem>

    <listitem>
     <para>
      修复超过 68 年的 interval 在 <literal>extract(epoch from interval)</literal> 中的溢出（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PL/Perl 在可信函数中使用 UTF-8 正则表达式时不失败（Andrew）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PL/Perl 在平台 Perl 把 <literal>bool</literal> 类型定义为 <literal>int</literal> 而非 <literal>char</literal> 时的问题（Tom）
     </para>

     <para>
      虽然理论上这可能在任何地方发生，但没有标准 Perl 构建这样做……直到 <productname>Mac OS X</productname> 10.5。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PL/Python 在 64 位机器上与 Python 2.5 正确工作（Marko Kreen）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PL/Python 在长异常消息上不崩溃（Alvaro）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_dump</application>，使其正确处理默认表达式与父表不同的继承子表（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <varname>PGPASSFILE</varname> 指向非普通文件时 <application>libpq</application> 的崩溃（Martin Pitt）
     </para>
    </listitem>

    <listitem>
     <para>
      <application>ecpg</application> 解析器修复（Michael）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <filename>contrib/pgcrypto</filename> 防御在超过 128 位密钥上失败的 <application>OpenSSL</application> 库——至少某些 Solaris 版本是这种情形（Marko Kreen）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <filename>contrib/tablefunc</filename> 的 <function>crosstab()</function> 把 NULL rowid 本身作为一个类别处理，而不是崩溃（Joe）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <type>tsvector</type> 和 <type>tsquery</type> 输出例程以正确转义反斜杠（Teodor、Bruce）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>to_tsvector()</function> 在超长输入字符串上的崩溃（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      要求在重新生成 <command>configure</command> 脚本时使用特定版本的 <productname>Autoconf</productname>（Peter）
     </para>

     <para>
      这只影响开发者和打包者。此变更是为了防止意外使用未经测试的 <productname>Autoconf</productname> 与 <productname>PostgreSQL</productname> 版本组合。如果确实想使用不同的 <productname>Autoconf</productname> 版本，可以移除版本检查，但结果是否可用由你自己负责。
     </para>
    </listitem>

    <listitem>
     <para>
      更新 <function>gettimeofday</function> 配置检查，使 <productname>PostgreSQL</productname> 能在更新版本的 <productname>MinGW</productname> 上构建（Magnus）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-2-5">
  <title>版本 8.2.5</title>

  <note>
  <title>发行日期</title>
  <simpara>2007-09-17</simpara>
 </note>

  <para>
   本次发布包含对 8.2.4 的多方面修复。有关 8.2 大版本中新特性的信息，请参见 <xref linkend="release-8-2">。
  </para>

  <sect2>
   <title>迁移到版本 8.2.5</title>

   <para>
    对于运行 8.2.X 的用户，不需要进行转储/恢复。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      防止事务插入行后在同一表上并发 <command>VACUUM</command> 接近结束时中止导致的索引损坏（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复涉及域上之域情形的 <literal>ALTER DOMAIN ADD CONSTRAINT</literal>（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <command>CREATE DOMAIN ... DEFAULT NULL</command> 正确工作（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复外连接的一些规划器问题，特别是 <literal>t1 LEFT JOIN t2 WHERE t2.col IS NULL</literal> 的糟糕大小估计（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      允许 <type>interval</type> 数据类型接受仅由毫秒或微秒组成的输入（Neil）
     </para>
    </listitem>

    <listitem>
     <para>
      允许 <type>timestamp</type> 输入中时区名出现在年份之前（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      <filename>/contrib/tsearch2</filename> 所用 <acronym>GIN</acronym> 索引的修复（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      加快 rtree 索引插入（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <acronym>SSL</acronym> 错误消息的过度记录（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复日志，使使用 syslogger 进程时日志消息永不交错（Andrew）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <varname>log_min_error_statement</varname> 日志记录内存耗尽时的崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复某些外键极端情形的错误处理（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>stddev_pop(numeric)</function> 和 <function>var_pop(numeric)</function>（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      防止 <command>REINDEX</command> 和 <command>CLUSTER</command> 因试图处理其他会话的临时表而失败（Alvaro）
     </para>
    </listitem>

    <listitem>
     <para>
      更新时区数据库规则，特别是新西兰即将到来的变更（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      Windows 套接字和信号量改进（Magnus）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <command>pg_ctl -w</command> 在 Windows 服务模式下正确工作（Dave Page）
     </para>
    </listitem>

    <listitem>
     <para>
      修复在 Windows 上使用 <application>MIT Kerberos</application> 时的内存分配缺陷（Magnus）
     </para>
    </listitem>

    <listitem>
     <para>
      因可能的编码不匹配，在 Windows 上抑制日志时间戳中的时区名（<literal>%Z</literal>）（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      作为安全措施，要求使用 <filename>/contrib/dblink</filename> 的非超级用户只使用口令认证（Joe）
     </para>
    </listitem>

    <listitem>
     <para>
      出于安全原因，把 <filename>/contrib/pgstattuple</filename> 函数限制为超级用户（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      不让 <filename>/contrib/intarray</filename> 试图使其 GIN 操作符类成为默认（这会在转储/恢复时引起问题）（Tom）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-2-4">
  <title>版本 8.2.4</title>

  <note>
  <title>发行日期</title>
  <simpara>2007-04-23</simpara>
 </note>

  <para>
      此发行版包含 8.2.3 之后的多种修复，包括一个安全修复。有关 8.2 主版本新特性的信息，请参阅 <xref linkend="release-8-2">。
     </para>

  <sect2>
   <title>迁移到版本 8.2.4</title>

   <para>
    对于运行 8.2.X 的用户，不需要进行转储/恢复。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      支持在 <varname>search_path</varname> 中显式放置临时表模式，并禁用为函数和操作符搜索它（Tom）
     </para>

     <para>
      这是允许安全定义者函数设置真正安全的 <varname>search_path</varname> 值所必需的。没有它，非特权 SQL 用户可以利用临时对象以安全定义者函数的权限执行代码（CVE-2007-2138）。更多信息请参阅 <command>CREATE FUNCTION</command>。
     </para>
    </listitem>

    <listitem>
     <para>
      通过强制在每个后端中重载来修复 Windows 的 <varname>shared_preload_libraries</varname>（Korry Douglas）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>to_char()</function>，使其正确地对本地化的日或月名做大/小写转换（Pavel Stehule）
     </para>
    </listitem>

    <listitem>
     <para>
      <filename>/contrib/tsearch2</filename> 崩溃修复（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      要求 <command>COMMIT PREPARED</command> 在事务预备时所在的同一数据库中执行（Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      允许 <command>pg_dump</command> 在 Windows 上做超过 2GB 的二进制备份（Magnus）
     </para>
    </listitem>

    <listitem>
     <para>
      新的繁体（台湾）中文 <acronym>FAQ</acronym>（Zhou Daojing）
     </para>
    </listitem>

    <listitem>
     <para>
      防止统计收集器过于频繁地写磁盘（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <command>VACUUM FULL</command> 处理 <command>UPDATE</command> 链方式中的潜在数据损坏缺陷（Tom、Pavan Deolasee）
     </para>
    </listitem>

    <listitem>
     <para>
      修复使用数组类型的域中的缺陷（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <command>pg_dump</command>，使其在不转储所属表时也能用 <option>-t</option> 转储 serial 列的序列（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      规划器修复，包括改进外连接和位图扫描选择逻辑（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PL/pgSQL 函数试图在 <literal>EXCEPTION</literal> 块内 <literal>RETURN</literal> 时可能的错误答案或崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复哈希索引扩容期间的 PANIC（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 POSIX 风格时区规范以遵循新的美国 DST 规则（Tom）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-2-3">
  <title>版本 8.2.3</title>

  <note>
  <title>发行日期</title>
  <simpara>2007-02-07</simpara>
 </note>

  <para>
      此发行版包含 8.2.2 之后的两个修复。有关 8.2 主版本新特性的信息，请参阅 <xref linkend="release-8-2">。
     </para>

  <sect2>
   <title>迁移到版本 8.2.3</title>

   <para>
    对于运行 8.2.X 的用户，不需要进行转储/恢复。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      移除约束和函数索引中对类型长度的过度严格检查（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复优化，使子查询中的 MIN/MAX 再次能使用索引（Tom）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-2-2">
  <title>版本 8.2.2</title>

  <note>
  <title>发行日期</title>
  <simpara>2007-02-05</simpara>
 </note>

  <para>
      此发行版包含 8.2.1 之后的多种修复，包括一个安全修复。有关 8.2 主版本新特性的信息，请参阅 <xref linkend="release-8-2">。
     </para>

  <sect2>
   <title>迁移到版本 8.2.2</title>

   <para>
    对于运行 8.2.X 的用户，不需要进行转储/恢复。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      移除允许已连接用户读取后端内存的安全漏洞（Tom）
     </para>

     <para>
      这些漏洞涉及抑制 SQL 函数返回其声明数据类型的常规检查，以及更改表列的数据类型（CVE-2007-0555、CVE-2007-0556）。这些错误很容易被利用来导致后端崩溃，而且原则上可能被用来读取用户不应能访问的数据库内容。
     </para>
    </listitem>

    <listitem>
     <para>
      修复不再那么罕见的缺陷：btree 索引页分裂可能因选择不可行的分裂点而失败（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 Borland C 编译脚本（L Bayuk）
     </para>
    </listitem>

    <listitem>
     <para>
      正确处理以 <literal>00</literal> 结尾年份的 <function>to_char('CC')</function>（Tom）
     </para>

     <para>
      2000 年属于二十世纪，不是二十一世纪。
     </para>
    </listitem>

    <listitem>
     <para>
      <filename>/contrib/tsearch2</filename> 本地化改进（Tatsuo、Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>information_schema.key_column_usage</literal> 视图中不正确的权限检查（Tom）
     </para>

     <para>
      症状是 <quote>OID 为 nnnnn 的关系不存在</quote>错误。要不经 <command>initdb</command> 获得此修复，用 <command>CREATE OR REPLACE VIEW</command> 安装 <filename>share/information_schema.sql</filename> 中的修正定义。注意需要在每个数据库中这样做。
     </para>
    </listitem>

    <listitem>
     <para>
      改进有大量表的数据库的 <command>VACUUM</command> 性能（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复由 <literal>UNION</literal> 触发的罕见 Assert() 崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复使用 <literal>ROW</literal> 不等条件的索引搜索可能不正确的结果（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      收紧对超过三字节的 UTF8 序列的多字节字符处理的安全性（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 Windows 上因试图 fsync 已删除文件而发生的虚假<quote>权限被拒绝</quote>失败（Magnus、Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复可能导致统计收集器在 Windows 上挂起的缺陷（Magnus）
     </para>

     <para>
      这进而导致 autovacuum 不工作。
     </para>
    </listitem>

    <listitem>
     <para>
      修复已在使用中的 PL/pgSQL 函数被更新时可能的崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      改进 PL/pgSQL 对域类型的处理（Sergiy Vyshnevetskiy、Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PL/pgSQL 异常块处理中可能的错误（Tom）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-2-1">
  <title>版本 8.2.1</title>

  <note>
  <title>发行日期</title>
  <simpara>2007-01-08</simpara>
 </note>

  <para>
   本次发布包含对 8.2 的多方面修复。有关 8.2 大版本中新特性的信息，请参见 <xref linkend="release-8-2">。
  </para>

  <sect2>
   <title>迁移到版本 8.2.1</title>

   <para>
      对于运行 8.2 的用户，不需要进行转储/恢复。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      修复 <literal>SELECT</literal> ... <literal>LIMIT ALL</literal>（以及 <literal>LIMIT NULL</literal>）的崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      若干 <filename>/contrib/tsearch2</filename> 修复（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      在 Windows 上，使来自操作系统的日志消息使用 <acronym>ASCII</acronym> 编码（Hiroshi Saito）
     </para>

     <para>
      这修复了操作系统与数据库服务器编码不匹配时的转换问题。
     </para>
    </listitem>

    <listitem>
     <para>
      修复用 <filename>win32.mak</filename> 在 Windows 上链接 <application>pg_dump</application> 的问题（Hiroshi Saito）
     </para>
    </listitem>

    <listitem>
     <para>
      修复外连接查询的规划器错误（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复涉及子 SELECT 的查询中的若干问题（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复子事务中止期间 SPI 中可能的崩溃（Tom）
     </para>

     <para>
      这影响所有 PL 函数，因为它们都使用 SPI。
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <acronym>PDF</acronym> 文档的构建速度（Peter）
     </para>
    </listitem>

    <listitem>
     <para>
      重新加入 <acronym>JST</acronym>（日本）时区缩写（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      改进与索引扫描相关的优化决策（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <application>psql</application> 像以前一样打印多字节组合字符，而不是输出为 <literal>\u</literal>（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      改进使用括号的正则表达式的索引利用（Tom）
     </para>

     <para>
      这同时也改进了 <application>psql</application> 的 <literal>\d</literal> 性能。
     </para>
    </listitem>

    <listitem>
     <para>
      让 <application>pg_dumpall</application> 在从 8.2 之前的服务器转储时假定数据库有公共 <literal>CONNECT</literal> 权限（Tom）
     </para>

     <para>
      这保留了以前的行为：只要 <filename>pg_hba.conf</filename> 允许，任何人都可以连接到数据库。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-2">
  <title>版本 8.2</title>

  <note>
  <title>发行日期</title>
  <simpara>2006-12-05</simpara>
 </note>

  <sect2>
   <title>概述</title>

   <para>
      此发行版新增了许多用户要求的功能和性能改进，包括：<itemizedlist> <listitem> <para> 查询语言增强，包括 <command>INSERT/UPDATE/DELETE RETURNING</command>、多行 <literal>VALUES</literal> 列表以及 <command>UPDATE</command>/<command>DELETE</command> 中可选的目标表别名
     </para>
     </listitem>

     <listitem>
      <para>
      创建索引而不阻塞并发的 <command>INSERT</command>/<command>UPDATE</command>/<command>DELETE</command> 操作
     </para>
     </listitem>

     <listitem>
      <para>
      许多查询优化改进，包括对外连接重排序的支持
     </para>
     </listitem>

     <listitem>
      <para>
      改进的排序性能和更低的内存使用
     </para>
     </listitem>

     <listitem>
      <para>
      更高效的锁定和更好的并发性
     </para>
     </listitem>

     <listitem>
      <para>
      更高效的清理
     </para>
     </listitem>

     <listitem>
      <para>
      更易管理的热备服务器
     </para>
     </listitem>

     <listitem>
      <para>
      新的表和索引 <literal>FILLFACTOR</literal> 支持
     </para>
     </listitem>

     <listitem>
      <para>
      监控、日志和性能调优的新增内容
     </para>
     </listitem>

     <listitem>
      <para>
      对创建和删除对象的更多控制
     </para>
     </listitem>

     <listitem>
      <para>
      可以为已有表定义和移除表继承关系
     </para>
     </listitem>

     <listitem>
      <para>
      <command>COPY TO</command> 可以复制任意 <command>SELECT</command> 语句的输出
     </para>
     </listitem>

     <listitem>
      <para>
      数组改进，包括数组中的空值
     </para>
     </listitem>

     <listitem>
      <para>
      聚合函数改进，包括多输入聚合和 SQL:2003 统计函数
     </para>
     </listitem>

     <listitem>
      <para>
      许多 <filename>contrib/</filename> 改进
     </para>
     </listitem>

    </itemizedlist>

   </para>

  </sect2>

  <sect2>
   <title>迁移到版本 8.2</title>

   <para>
      希望从任何以前的版本迁移数据的用户，需要使用 <application>pg_dump</application> 进行转储/恢复。
     </para>

   <para>
      请注意以下不兼容之处：
     </para>

   <itemizedlist>

     <listitem>
      <para>
      把 <link linkend="guc-escape-string-warning"><varname>escape_string_warning</varname></link> 默认设为 <literal>on</literal>（Bruce）
     </para>

      <para>
      如果在<link linkend="sql-syntax-strings">非转义（非 <literal>E''</literal>）字符串</link>中使用反斜杠转义，这会发出警告。
     </para>
     </listitem>

     <listitem>
      <para>
      更改<link linkend="sql-syntax-row-constructors">行构造器语法</link>（<literal>ROW(...)</literal>），使列表元素 <literal>foo.*</literal> 展开为其成员字段的列表，而不是像以前那样创建嵌套的行类型字段（Tom）
     </para>

      <para>
      新行为有用得多，因为它允许例如触发器用 <literal>IF row(new.*) IS DISTINCT FROM row(old.*)</literal> 检查数据更改。省略 <literal>.*</literal> 仍可获得旧行为。
     </para>
     </listitem>

     <listitem>
      <para>
      让<link linkend="row-wise-comparison">按行比较</link>遵循 <acronym>SQL</acronym> 标准语义，并允许它们用于索引扫描（Tom）
     </para>

      <para>
      此前，行 = 和 &lt;&gt; 比较遵循标准，但 &lt; &lt;= &gt; &gt;= 不遵循。现在行比较可以用作匹配行值的多列索引的索引约束。
     </para>
     </listitem>

     <listitem>
      <para>
      让<link linkend="functions-comparison">行 <literal>IS <optional>NOT</optional> NULL</literal></link> 测试遵循 <acronym>SQL</acronym> 标准语义（Tom）
     </para>

      <para>
      以前的行为在 <literal>IS NULL</literal> 的简单情形下符合标准，但 <literal>IS NOT NULL</literal> 在任一行字段非空时就返回真，而标准说只有在所有字段都非空时才应返回真。
     </para>
     </listitem>

     <listitem>
      <para>
      让 <link linkend="SQL-SET-CONSTRAINTS"><command>SET CONSTRAINT</command></link> 只影响一个约束（Kris Jurka）
     </para>

      <para>
      在以前的版本中，<command>SET CONSTRAINT</command> 修改所有名称匹配的约束。在此版本中，模式搜索路径用于只修改第一个匹配的约束。也支持模式指定。这更接近 SQL 标准。
     </para>
     </listitem>

     <listitem>
      <para>
      出于安全原因移除表的 <literal>RULE</literal> 权限（Tom）
     </para>

      <para>
      从此版本起，只有表的所有者能为表创建或修改规则。为了向后兼容，<command>GRANT</command>/<command>REVOKE RULE</command> 仍被接受，但它不做任何事情。
     </para>
     </listitem>

     <listitem>
      <para>
      数组比较改进（Tom）
     </para>

      <para>
      现在数组维度也被比较。
     </para>
     </listitem>

     <listitem>
      <para>
      更改<link linkend="functions-array">数组连接</link>以符合文档行为（Tom）
     </para>

      <para>
      这改变了以前连接会修改数组下界的行为。
     </para>
     </listitem>

     <listitem>
      <para>
      使 <application>postmaster</application> 和 <link linkend="app-postgres"><application>postgres</application></link> 的命令行选项一致（Peter）
     </para>

      <para>
      这允许 postmaster 不用 <literal>-o</literal> 就能向每个后端传递参数。注意某些选项现在只有长形式，因为存在冲突的单字母选项。
     </para>
     </listitem>

     <listitem>
      <para>
      废弃 <application>postmaster</application> 符号链接的使用（Peter）
     </para>

      <para>
      <application>postmaster</application> 和 <application>postgres</application> 命令现在行为完全相同，由命令行选项决定行为。<application>postmaster</application> 符号链接为兼容性保留，但已不再真正需要。
     </para>
     </listitem>

     <listitem>
      <para>
      更改 <link linkend="guc-log-duration"><varname>log_duration</varname></link>，即使查询未被输出也输出（Tom）
     </para>

      <para>
      在以前的版本中，<varname>log_duration</varname> 只在查询先前出现在日志中时才打印。
     </para>
     </listitem>

     <listitem>
      <para>
      让 <link linkend="functions-formatting"><function>to_char(time)</function></link> 和 <link linkend="functions-formatting"><function>to_char(interval)</function></link> 把 <literal>HH</literal> 和 <literal>HH12</literal> 当作 12 小时间隔
     </para>

      <para>
      大多数应用程序应使用 <literal>HH24</literal>，除非想要 12 小时显示。
     </para>
     </listitem>

     <listitem>
      <para>
      在从 <link linkend="datatype-inet"><type>INET</type></link> 到 <link linkend="datatype-inet"><type>CIDR</type></link> 的转换中将未掩码的位清零（Tom）
     </para>

      <para>
      这确保转换后的值对 <type>CIDR</type> 确实有效。
     </para>
     </listitem>

     <listitem>
      <para>
      移除 <varname>australian_timezones</varname> 配置变量（Joachim Wieland）
     </para>

      <para>
      此变量已被配置时区缩写的更通用机制取代。
     </para>
     </listitem>

     <listitem>
      <para>
      改进嵌套循环索引扫描的代价估计（Tom）
     </para>

      <para>
      这可能消除设置不切实际的小 <link linkend="guc-random-page-cost"><varname>random_page_cost</varname></link> 值的需要。如果你一直使用很小的 <varname>random_page_cost</varname>，请重新检查你的测试用例。
     </para>
     </listitem>

     <listitem>
      <para>
      更改 <command>pg_dump</command> 的 <literal>-n</literal> 和 <literal>-t</literal> 选项的行为。（Greg Sabino Mullane）
     </para>
      <para>
      详情请参阅 <command>pg_dump</command> 手册页。
     </para>
     </listitem>

     <listitem>
      <para>
      更改 <link linkend="libpq"><application>libpq</application></link> 的 <function>PQdsplen()</function> 以返回有用的值（Martijn van Oosterhout）
     </para>
     </listitem>

     <listitem>
      <para>
      把 <link linkend="libpq"><application>libpq</application></link> 的 <function>PQgetssl()</function> 声明为返回 <literal>void *</literal> 而不是 <literal>SSL *</literal>（Martijn van Oosterhout）
     </para>

      <para>
      这允许应用程序使用该函数而不包含 OpenSSL 头文件。
     </para>
     </listitem>

     <listitem>
      <para>
      C 语言可装载模块现在必须包含 <link linkend="xfunc-c-dynload"><literal>PG_MODULE_MAGIC</literal></link> 宏调用以进行版本兼容性检查（Martijn van Oosterhout）
     </para>
     </listitem>

     <listitem>
      <para>
      出于安全考虑，PL/PerlU 函数使用的模块不再对 PL/Perl 函数可用（Andrew）
     </para>
      <note>
       <para>
      这也意味着数据不能再在 PL/Perl 函数与 PL/PerlU 函数之间共享。某些 Perl 安装编译时未带允许单个进程中存在多个解释器的正确标志。在这种情形下，PL/Perl 和 PL/PerlU 不能在单个后端中同时使用。解决办法是获得支持多解释器的 Perl 安装。
     </para>
      </note>
     </listitem>

     <listitem>
      <para>
      在 <filename>contrib/xml2/</filename> 中，把 <function>xml_valid()</function> 改名为 <function>xml_is_well_formed()</function>（Tom）
     </para>

      <para>
      <function>xml_valid()</function> 将为向后兼容保留，但其行为将在未来版本中改为做模式检查。
     </para>
     </listitem>

     <listitem>
      <para>
      移除 <filename>contrib/ora2pg/</filename>，现位于 <ulink url="http://www.samse.fr/GPL/ora2pg"></ulink>
     </para>
     </listitem>

     <listitem>
      <para>
      移除已迁移到 PgFoundry 的 contrib 模块：<filename>adddepend</filename>、<filename>dbase</filename>、<filename>dbmirror</filename>、<filename>fulltextindex</filename>、<filename>mac</filename>、<filename>userlock</filename>
     </para>
     </listitem>

     <listitem>
      <para>
      移除被废弃的 contrib 模块：<filename>mSQL-interface</filename>、<filename>tips</filename>
     </para>
     </listitem>

     <listitem>
      <para>
      移除 <acronym>QNX</acronym> 和 <acronym>BEOS</acronym> 移植（Bruce）
     </para>

      <para>
      这些移植不再有活跃的维护者。
     </para>
     </listitem>

   </itemizedlist>
  </sect2>

  <sect2>
   <title>变更</title>

   <para>
      下面你将找到 <productname>PostgreSQL</productname> 8.2 与上一个主版本之间变更的详细说明。
     </para>

   <sect3>
    <title>性能改进</title>
    <itemizedlist>

     <listitem>
      <para>
      允许规划器在某些情形下重排<link linkend="queries-join">外连接</link>（Tom）
     </para>

      <para>
      在以前的版本中，外连接总是按照查询中书写的顺序求值。此变更允许查询优化器在能确定连接顺序可以在不改变查询含义的情况下更改时，考虑重排外连接。这对涉及多个外连接或内外连接混合的查询可带来可观的性能差异。
     </para>
     </listitem>

     <listitem>
      <para>
      改进 <link linkend="functions-comparisons"><literal>IN</literal>（表达式列表）</link>子句的效率（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      改进排序速度并降低内存使用（Simon、Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      改进子事务性能（Alvaro、Itagaki Takahiro、Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      为<link linkend="SQL-CREATETABLE">表</link>和<link linkend="SQL-CREATEINDEX">索引</link>创建新增 <literal>FILLFACTOR</literal>（ITAGAKI Takahiro）
     </para>

      <para>
      这在每个表或索引页中留出额外空闲空间，使数据库增长时性能得以改进。这对维护聚簇特别有价值。
     </para>
     </listitem>

     <listitem>
      <para>
      提高 <link linkend="guc-shared-buffers"><varname>shared_buffers</varname></link> 和 <varname>max_fsm_pages</varname> 的默认值（Andrew）
     </para>
     </listitem>

     <listitem>
      <para>
      通过把锁管理器表分成若干节来改进锁定性能（Tom）
     </para>

      <para>
      这使锁定更细粒度，减少争用。
     </para>
     </listitem>

     <listitem>
      <para>
      降低顺序扫描的锁定要求（Qingqing Zhou）
     </para>
     </listitem>

     <listitem>
      <para>
      降低创建和销毁数据库所需的锁定（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      改进优化器对 <link linkend="functions-like"><literal>LIKE</literal></link>、<link linkend="functions-like"><literal>ILIKE</literal></link> 和 <link linkend="functions-posix-regexp">正则表达式</link>操作的选择率估计（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      改进到<link linkend="ddl-inherit">继承表</link>和 <link linkend="queries-union"><literal>UNION ALL</literal></link> 视图连接的规划（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      允许把<link linkend="guc-constraint-exclusion">约束排除</link>应用于<link linkend="ddl-inherit">继承的</link> <command>UPDATE</command> 和 <command>DELETE</command> 查询（Tom）
     </para>

      <para>
      <command>SELECT</command> 已经遵循约束排除。
     </para>
     </listitem>

     <listitem>
      <para>
      改进常量 <literal>WHERE</literal> 子句的规划，例如只依赖于从外层查询级别继承的变量的条件（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      协议级未命名预备语句对每组 <literal>BIND</literal> 值重新规划（Tom）
     </para>

      <para>
      这改进了性能，因为可以在计划中使用确切的参数值。
     </para>
     </listitem>

     <listitem>
      <para>
      加快 B-Tree 索引的清理（Heikki Linnakangas、Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      避免 <link linkend="SQL-VACUUM"><command>VACUUM</command></link> 期间对无索引表的额外扫描（Greg Stark）
     </para>
     </listitem>

     <listitem>
      <para>
      改进多列 <link linkend="GiST"><acronym>GiST</acronym></link> 索引（Oleg、Teodor）
     </para>
     </listitem>

     <listitem>
      <para>
      在 B-Tree 页分裂前移除死索引条目（Junji Teramoto）
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title>服务器变更</title>
    <itemizedlist>

     <listitem>
      <para>
      允许强制切换到新事务日志文件（Simon、Tom）
     </para>

      <para>
      这对保持热备从属服务器与主服务器同步很有价值。事务日志文件切换现在也会在 <link linkend="functions-admin"><function>pg_stop_backup()</function></link> 期间自动发生。这确保恢复所需的全部事务日志文件可以立即归档。
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <acronym>WAL</acronym> 信息函数（Simon）
     </para>

      <para>
      新增查询当前事务日志插入点以及从 <link linkend="functions-admin"><function>pg_stop_backup()</function></link> 和相关函数显示的十六进制 <acronym>WAL</acronym> 位置确定 <acronym>WAL</acronym> 文件名的函数。
     </para>
     </listitem>

     <listitem>
      <para>
      改进 <acronym>WAL</acronym> 重放期间崩溃后的恢复（Simon）
     </para>

      <para>
      服务器现在在 <acronym>WAL</acronym> 恢复期间做周期性检查点，因此如果发生崩溃，未来的 <acronym>WAL</acronym> 恢复会缩短。这还消除了热备服务器崩溃后重放自基础备份以来整个日志的需要。
     </para>
     </listitem>

     <listitem>
      <para>
      改进长期 <acronym>WAL</acronym> 重放的可靠性（Heikki、Simon、Tom）
     </para>

      <para>
      此前，由于 XID 回卷，试图前滚超过 20 亿个事务行不通。这意味着热备服务器必须定期从新的基础备份重载。
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <link linkend="guc-archive-timeout"><varname>archive_timeout</varname></link>，按给定间隔强制事务日志文件切换（Simon）
     </para>

      <para>
      这为热备服务器强制了最大复制延迟。
     </para>
     </listitem>

     <listitem>
      <para>
      新增本机 <link linkend="auth-ldap"><acronym>LDAP</acronym></link> 认证（Magnus Hagander）
     </para>

      <para>
      这对不支持 <acronym>PAM</acronym> 的平台（如 Windows）特别有用。
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <link linkend="sql-grant-description-objects"><literal>GRANT CONNECT ON DATABASE</literal></link>（Gevik Babakhani）
     </para>

      <para>
      这提供了对数据库访问的 SQL 级控制。它作为现有 <link linkend="auth-pg-hba-conf"><filename>pg_hba.conf</filename></link> 控制之上的附加过滤器工作。
     </para>
     </listitem>

     <listitem>
      <para>
      新增对 <link linkend="ssl-tcp"><acronym>SSL</acronym> 证书吊销列表</link>（<acronym>CRL</acronym>）文件的支持（Libor Hoho&scaron;）
     </para>

      <para>
      服务器和 <application>libpq</application> 现在都识别 <acronym>CRL</acronym> 文件。
     </para>
     </listitem>

     <listitem>
      <para>
      <link linkend="GiST"><acronym>GiST</acronym></link> 索引现在可聚簇（Teodor）
     </para>
     </listitem>

     <listitem>
      <para>
      移除 autovacuum 例行的服务器日志条目（Bruce）
     </para>

      <para>
      <link linkend="monitoring-stats-views-table"><literal>pg_stat_activity</literal></link> 现在显示 autovacuum 活动。
     </para>
     </listitem>

     <listitem>
      <para>
      在单个表内而不是整个数据库内跟踪最大 XID 年龄（Alvaro）
     </para>

      <para>
      这通过避免不必要的 VACUUM 降低了防止事务 ID 回卷所涉及的开销。
     </para>
     </listitem>

     <listitem>
      <para>
      为统计收集器新增上次清理和分析的时间戳列（Larry Rosenman）
     </para>

      <para>
      这些值现在出现在 <link linkend="monitoring-stats-views-table"><literal>pg_stat_*_tables</literal></link> 系统视图中。
     </para>
     </listitem>

     <listitem>
      <para>
      改进统计监控的性能，特别是 <varname>stats_command_string</varname>（Tom、Bruce）
     </para>

      <para>
      既然其开销已最小化，此版本默认启用 <varname>stats_command_string</varname>。这意味着 <link linkend="monitoring-stats-views-table"><literal>pg_stat_activity</literal></link> 现在默认显示所有活动查询。
     </para>
     </listitem>

     <listitem>
      <para>
      为 <link linkend="monitoring-stats-views-table"><literal>pg_stat_activity</literal></link> 新增 <literal>waiting</literal> 列（Tom）
     </para>

      <para>
      这使 <structname>pg_stat_activity</structname> 能显示 <application>ps</application> 显示中包含的所有信息。
     </para>
     </listitem>

     <listitem>
      <para>
      新增配置参数 <link linkend="guc-update-process-title"><varname>update_process_title</varname></link>，控制 <application>ps</application> 显示是否随每条命令更新（Bruce）
     </para>

      <para>
      在更新 <application>ps</application> 显示代价高昂的平台上，关闭它并只依靠 <structname>pg_stat_activity</structname> 获取状态信息可能是值得的。
     </para>
     </listitem>

     <listitem>
      <para>
      允许在配置设置中指定单位（Peter）
     </para>

      <para>
      例如，现在可以把 <link linkend="guc-shared-buffers"><varname>shared_buffers</varname></link> 设为 <literal>32MB</literal>，而不用心算换算大小。
     </para>
     </listitem>

     <listitem>
      <para>
      新增对 <filename>postgresql.conf</filename> 中<link linkend="config-setting">include 指令</link>的支持（Joachim Wieland）
     </para>
     </listitem>

     <listitem>
      <para>
      改进协议级 prepare/bind/execute 消息的日志记录（Bruce、Tom）
     </para>

      <para>
      这类日志现在显示语句名、绑定参数值以及被执行查询的文本。此外，当由 <varname>log_min_error_statement</varname> 启用时，查询文本也被正确包含在记录的错误消息中。
     </para>
     </listitem>

     <listitem>
      <para>
      防止把 <link linkend="guc-max-stack-depth"><varname>max_stack_depth</varname></link> 设为不安全的值
     </para>

      <para>
      在能确定实际内核栈深度限制的平台（大多数平台）上，确保 <varname>max_stack_depth</varname> 的初始默认值是安全的，并拒绝把它设为不安全的大值的企图。
     </para>
     </listitem>

     <listitem>
      <para>
      在更多情形中启用查询中错误位置的高亮（Tom）
     </para>

      <para>
      服务器现在能为某些语义错误（如无法识别的列名）报告具体的错误位置，而不像以前那样只针对基本语法错误。
     </para>
     </listitem>

     <listitem>
      <para>
      修复 <command>VACUUM</command> 中的 <quote>未能重新找到父键</quote>错误（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      在服务器重启期间清除 <filename>pg_internal.init</filename> 缓存文件（Simon）
     </para>

      <para>
      这避免了缓存文件在 PITR 恢复后可能包含陈旧数据的风险。
     </para>
     </listitem>

     <listitem>
      <para>
      修复 <command>VACUUM</command> 截断跨越 GB 边界的大关系时的竞态条件（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      修复导致行级锁上不必要死锁错误的缺陷（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      修复影响多 GB 哈希索引的缺陷（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      每个后端进程现在都是自己的进程组长（Tom）
     </para>

      <para>
      这允许查询取消中止从后端或归档/恢复进程调用的子进程。
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title>查询变更</title>
    <itemizedlist>

     <listitem>
      <para>
      新增 <link linkend="SQL-INSERT"><command>INSERT</command></link>/<link linkend="SQL-UPDATE"><command>UPDATE</command></link>/<link linkend="SQL-DELETE"><command>DELETE</command></link> 的 <literal>RETURNING</literal>（Jonah Harris、Tom）
     </para>

      <para>
      这允许这些命令返回值，例如为新行计算的序列键。在 <command>UPDATE</command> 的情形，返回行更新后版本的值。
     </para>
     </listitem>

     <listitem>
      <para>
      按照 SQL 标准，新增对多行 <link linkend="queries-values"><literal>VALUES</literal></link> 子句的支持（Joe、Tom）
     </para>

      <para>
      这允许 <command>INSERT</command> 插入多行常量，或查询用常量生成结果集。例如 <literal>INSERT ... VALUES (...), (...), ....</literal> 和 <literal>SELECT * FROM (VALUES (...), (...), ....) AS alias(f1, ...)</literal>。
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <link linkend="SQL-UPDATE"><command>UPDATE</command></link> 和 <link linkend="SQL-DELETE"><command>DELETE</command></link> 为目标表使用别名（Atsushi Ogawa）
     </para>

      <para>
      SQL 标准不允许这些命令中使用别名，但许多数据库系统为了书写方便仍允许。
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <link linkend="SQL-UPDATE"><command>UPDATE</command></link> 用值列表设置多列（Susanne Ebrecht）
     </para>

      <para>
      这基本上是成对分配列和值的简写。语法为 <literal>UPDATE tab SET (<replaceable>column</replaceable>, ...) = (<replaceable>val</replaceable>, ...)</literal>。
     </para>
     </listitem>

     <listitem>
      <para>
      让行比较按标准工作（Tom）
     </para>

      <para>
      &lt;、&lt;=、&gt;、&gt;= 形式现在按字典序比较行，即比较第一个元素，如果相等就比较第二个元素，依此类推。此前它们展开为对所有元素的 AND 条件，这既不标准也不太有用。
     </para>
     </listitem>

     <listitem>
      <para>
      为 <command>TRUNCATE</command> 新增 <link linkend="SQL-TRUNCATE"><literal>CASCADE</literal></link> 选项（Joachim Wieland）
     </para>

      <para>
      这使 <command>TRUNCATE</command> 自动包含通过外键引用指定表的所有表。虽然方便，但这是危险工具——请谨慎使用！
     </para>
     </listitem>

     <listitem>
      <para>
      支持在同一条 <link linkend="SQL-INSERT"><literal>SELECT</literal></link> 命令中使用 <literal>FOR UPDATE</literal> 和 <literal>FOR SHARE</literal>（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <link linkend="functions-comparisons"><literal>IS NOT DISTINCT FROM</literal></link>（Pavel Stehule）
     </para>

      <para>
      此操作符类似于相等（<literal>=</literal>），但当左右操作数都是 <literal>NULL</literal> 时结果为真，只有一个是时为假，而不是在这些情形下产生 <literal>NULL</literal>。
     </para>
     </listitem>

     <listitem>
      <para>
      改进 <link linkend="queries-union"><literal>UNION</literal></link>/<literal>INTERSECT</literal>/<literal>EXCEPT</literal> 使用的长度输出（Tom）
     </para>

      <para>
      当所有对应列的定义长度相同时，结果使用该长度而不是通用长度。
     </para>
     </listitem>

     <listitem>
      <para>
      让 <link linkend="functions-like"><literal>ILIKE</literal></link> 在多字节编码下工作（Tom）
     </para>

      <para>
      在内部，<literal>ILIKE</literal> 现在先调用 <function>lower()</function> 再使用 <literal>LIKE</literal>。特定于区域设置的正则表达式模式在这些编码下仍不工作。
     </para>
     </listitem>

     <listitem>
      <para>
      使 <link linkend="guc-standard-conforming-strings"><varname>standard_conforming_strings</varname></link> 可以被设为 <literal>on</literal>（Kevin Grittner）
     </para>

      <para>
      这允许禁用字符串中的反斜杠转义，使 <productname>PostgreSQL</productname> 更符合标准。为了向后兼容默认值为 <literal>off</literal>，但未来版本将默认为 <literal>on</literal>。
     </para>
     </listitem>

     <listitem>
      <para>
      不平展目标列表中包含 <literal>volatile</literal> 函数的子查询（Jaime Casanova）
     </para>

      <para>
      这防止了因 <literal>volatile</literal> 函数（如 <function>random()</function> 或 <function>nextval()</function>）被多次求值导致的意外行为。在存在被不必要地标记为 <literal>volatile</literal> 的函数时，它可能导致性能退化。
     </para>
     </listitem>

     <listitem>
      <para>
      新增系统视图 <link linkend="view-pg-prepared-statements"><literal>pg_prepared_statements</literal></link> 和 <link linkend="view-pg-cursors"><literal>pg_cursors</literal></link>，显示预备语句和打开的游标（Joachim Wieland、Neil）
     </para>

      <para>
      这在连接池设置中非常有用。
     </para>
     </listitem>

     <listitem>
      <para>
      支持 <link linkend="SQL-EXPLAIN"><command>EXPLAIN</command></link> 和 <link linkend="SQL-EXECUTE"><command>EXECUTE</command></link> 中的门户参数（Tom）
     </para>

      <para>
      这例如允许 <acronym>JDBC</acronym> 的 <literal>?</literal> 参数在这些命令中工作。
     </para>
     </listitem>

     <listitem>
      <para>
      如果未指定 <acronym>SQL</acronym> 级 <link linkend="SQL-PREPARE"><command>PREPARE</command></link> 的参数，则从查询内容推断其类型（Neil）
     </para>

      <para>
      协议级的 <command>PREPARE</command> 已经这样做。
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <literal>LIMIT</literal> 和 <literal>OFFSET</literal> 超过二十亿（Dhanaraj M）
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title>对象操作变更</title>
    <itemizedlist>

     <listitem>
      <para>
      为 <link linkend="SQL-CREATETABLEAS"><command>CREATE TABLE AS</command></link> 新增 <literal>TABLESPACE</literal> 子句（Neil）
     </para>

      <para>
      这允许为新表指定表空间。
     </para>
     </listitem>

     <listitem>
      <para>
      为 <link linkend="SQL-CREATETABLEAS"><command>CREATE TABLE AS</command></link> 新增 <literal>ON COMMIT</literal> 子句（Neil）
     </para>

      <para>
      这允许临时表在事务提交时被截断或删除。默认行为是表保留到会话结束。
     </para>
     </listitem>

     <listitem>
      <para>
      为 <link linkend="SQL-CREATETABLE"><command>CREATE TABLE LIKE</command></link> 新增 <literal>INCLUDING CONSTRAINTS</literal>（Greg Stark）
     </para>

      <para>
      这允许把 <literal>CHECK</literal> 约束轻松复制到新表。
     </para>
     </listitem>

     <listitem>
      <para>
      允许创建占位符（shell）<link linkend="SQL-CREATETYPE">类型</link>（Martijn van Oosterhout）
     </para>

      <para>
      shell 类型声明只创建类型名，不指定类型的任何细节。创建 shell 类型是有用的，因为它允许更干净地声明类型的输入/输出函数，这些函数必须在类型可以<quote>真正</quote>定义之前存在。语法为 <command>CREATE TYPE <replaceable class="parameter">typename</replaceable></command>。
     </para>
     </listitem>

     <listitem>
      <para>
      <link linkend="SQL-CREATEAGGREGATE">聚合函数</link>现在支持多个输入参数（Sergey Koposov、Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      新增聚合创建<link linkend="SQL-CREATEAGGREGATE">语法</link>（Tom）
     </para>

      <para>
      新语法为 <command>CREATE AGGREGATE <replaceable>aggname</replaceable> (<replaceable>input_type</replaceable>) (<replaceable>parameter_list</replaceable>)</command>。它更自然地支持新的多参数聚合功能。以前的语法仍受支持。
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <link linkend="SQL-ALTERROLE"><command>ALTER ROLE PASSWORD NULL</command></link>，移除先前设置的角色口令（Peter）
     </para>
     </listitem>

     <listitem>
      <para>
      为许多对象类型新增 <command>DROP</command> 对象 <literal>IF EXISTS</literal>（Andrew）
     </para>

      <para>
      这允许对不存在的对象执行 <command>DROP</command> 操作而不产生错误。
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <link linkend="SQL-DROP-OWNED"><literal>DROP OWNED</literal></link>，删除角色拥有的所有对象（Alvaro）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <link linkend="SQL-REASSIGN-OWNED"><literal>REASSIGN OWNED</literal></link>，重新分配角色拥有的所有对象的所有权（Alvaro）
     </para>

      <para>
      它与上面的 <literal>DROP OWNED</literal> 一起便于删除角色。
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <link linkend="SQL-GRANT"><command>GRANT ON SEQUENCE</command></link> 语法（Bruce）
     </para>

      <para>
      这是为设置序列特定的权限而添加的。为向后兼容，对序列的 <literal>GRANT ON TABLE</literal> 仍受支持。
     </para>
     </listitem>

     <listitem>
      <para>
      为序列新增 <link linkend="SQL-GRANT"><literal>USAGE</literal></link> 权限，只允许 <function>currval()</function> 和 <function>nextval()</function>，不允许 <function>setval()</function>（Bruce）
     </para>

      <para>
      <literal>USAGE</literal> 权限允许对序列访问更细粒度的控制。授予 <literal>USAGE</literal> 允许用户递增序列，但防止他们用 <function>setval()</function> 把序列设为任意值。
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <link linkend="SQL-ALTERTABLE"><literal>ALTER TABLE [ NO ] INHERIT</literal></link>（Greg Stark）
     </para>

      <para>
      这允许动态调整继承，而不只是在表创建和销毁时。当用继承实现表分区时这非常有价值。
     </para>
     </listitem>

     <listitem>
      <para>
      允许<link linkend="SQL-COMMENT">注释</link>全局对象被全局存储（Kris Jurka）
     </para>

      <para>
      此前，附加到数据库的注释存储在各个数据库中，使其无效，而且完全没有为角色或表空间的注释做准备。此变更新增了共享目录 <link linkend="catalog-pg-shdescription"><structname>pg_shdescription</structname></link>，并把数据库、角色和表空间的注释存储在其中。
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title>工具命令变更</title>
    <itemizedlist>

     <listitem>
      <para>
      新增允许创建索引而不阻塞对表并发写入的选项（Greg Stark、Tom）
     </para>

      <para>
      新语法为 <link linkend="SQL-CREATEINDEX"><command>CREATE INDEX CONCURRENTLY</command></link>。默认行为仍是在创建索引期间阻止表修改。
     </para>
     </listitem>

     <listitem>
      <para>
      提供<link linkend="functions-advisory-locks">咨询锁定</link>功能（Abhijit Menon-Sen、Tom）
     </para>

      <para>
      这是为取代以前 /contrib/userlock 中的内容而设计的新锁定 API。userlock 代码现在位于 pgfoundry。
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <link linkend="SQL-COPY"><command>COPY</command></link> 转储 <command>SELECT</command> 查询（Zoltan Boszormenyi、Karel Zak）
     </para>

      <para>
      这允许 <command>COPY</command> 转储任意 <acronym>SQL</acronym> 查询。语法为 <literal>COPY (SELECT ...) TO</literal>。
     </para>
     </listitem>

     <listitem>
      <para>
      让 <link linkend="SQL-COPY"><command>COPY</command></link> 命令返回包含复制行数的命令标签（Volkan YAZICI）
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <link linkend="SQL-VACUUM"><command>VACUUM</command></link> 使行过期而不受其他并发 <command>VACUUM</command> 操作影响（Hannu Krossing、Alvaro、Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      让 <link linkend="APP-INITDB"><application>initdb</application></link> 检测操作系统区域设置并相应设置默认 <varname>DateStyle</varname>（Peter）
     </para>

      <para>
      这使安装的 <filename>postgresql.conf</filename> 中 <varname>DateStyle</varname> 值更可能符合期望。
     </para>
     </listitem>

     <listitem>
      <para>
      减少 <application>initdb</application> 显示的进度消息数量（Tom）
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title>日期/时间变更</title>
    <itemizedlist>

     <listitem>
      <para>
      允许 <link linkend="datatype-datetime"><type>timestamp</type></link> 输入值中使用完整时区名（Joachim Wieland）
     </para>

      <para>
      例如 <literal>'2006-05-24 21:11 America/New_York'::timestamptz</literal>。
     </para>
     </listitem>

     <listitem>
      <para>
      支持可配置的时区缩写（Joachim Wieland）
     </para>

      <para>
      可以通过配置参数 <link linkend="guc-timezone-abbreviations"><varname>timezone_abbreviations</varname></link> 选择所需的时区缩写集合。
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <link linkend="view-pg-timezone-abbrevs"><varname>pg_timezone_abbrevs</varname></link> 和 <link linkend="view-pg-timezone-names"><varname>pg_timezone_names</varname></link> 视图以显示受支持的时区（Magnus Hagander）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <link linkend="functions-datetime-table"><function>clock_timestamp()</function></link>、<link linkend="functions-datetime-table"><function>statement_timestamp()</function></link> 和 <link linkend="functions-datetime-table"><function>transaction_timestamp()</function></link>（Bruce）
     </para>

      <para>
      <function>clock_timestamp()</function> 是当前墙上时钟时间，<function>statement_timestamp()</function> 是当前语句到达服务器的时刻，<function>transaction_timestamp()</function> 是 <function>now()</function> 的别名。
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <link linkend="functions-formatting"><function>to_char()</function></link> 打印本地化的月名和日名（Euler Taveira de Oliveira）
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <link linkend="functions-formatting"><function>to_char(time)</function></link> 和 <link linkend="functions-formatting"><function>to_char(interval)</function></link> 输出 <acronym>AM</acronym>/<acronym>PM</acronym> 规格（Bruce）
     </para>

      <para>
      interval 和 time 被视为 24 小时周期，例如 <literal>25 hours</literal> 被认为是 <acronym>AM</acronym>。
     </para>
     </listitem>

     <listitem>
      <para>
      新增函数 <link linkend="functions-datetime-table"><function>justify_interval()</function></link>，调整 interval 单位（Mark Dilger）
     </para>
     </listitem>

     <listitem>
      <para>
      允许距 GMT 最多 14:59 的时区偏移
     </para>

      <para>
      基里巴斯使用 GMT+14，所以我们最好接受它。
     </para>
     </listitem>

     <listitem>
      <para>
      interval 计算改进（Michael Glaesemann、Bruce）
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title>其他数据类型和函数变更</title>
    <itemizedlist>

     <listitem>
      <para>
      允许数组包含 <literal>NULL</literal> 元素（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      允许给与现有条目不连续的数组元素赋值（Tom）
     </para>

      <para>
      中间的数组位置将用空值填充。这符合 SQL 标准。
     </para>
     </listitem>

     <listitem>
      <para>
      新增用于数组子集比较的内建<link linkend="functions-array">操作符</link>（<literal>@&gt;</literal>、<literal>&lt;@</literal>、<literal>&amp;&amp;</literal>）（Teodor、Tom）
     </para>

      <para>
      这些操作符可以用 <acronym>GiST</acronym> 或 <acronym>GIN</acronym> 索引为许多数据类型建立索引。
     </para>
     </listitem>

     <listitem>
      <para>
      新增对 <type>INET</type>/<type>CIDR</type> 值的便捷算术<link linkend="cidr-inet-operators-table">操作</link>（Stephen R. van den Berg）
     </para>

      <para>
      新操作符为 <literal>&amp;</literal>（与）、<literal>|</literal>（或）、<literal>~</literal>（非）、<type>inet</type> <literal>+</literal> <type>int8</type>、<type>inet</type> <literal>-</literal> <type>int8</type> 和 <type>inet</type> <literal>-</literal> <type>inet</type>。
     </para>
     </listitem>

     <listitem>
      <para>
      新增 SQL:2003 的新<link linkend="functions-aggregate-statistics-table">聚合函数</link>（Neil）
     </para>

      <para>
      新函数为 <function>var_pop()</function>、<function>var_samp()</function>、<function>stddev_pop()</function> 和 <function>stddev_samp()</function>。<function>var_samp()</function> 和 <function>stddev_samp()</function> 只是现有聚合 <function>variance()</function> 和 <function>stddev()</function> 的改名。后两个名字仍可为向后兼容使用。
     </para>
     </listitem>

     <listitem>
      <para>
      新增 SQL:2003 统计<link linkend="functions-aggregate-statistics-table">聚合</link>（Sergey Koposov）
     </para>

      <para>
      新函数：<function>regr_intercept()</function>、<function>regr_slope()</function>、<function>regr_r2()</function>、<function>corr()</function>、<function>covar_samp()</function>、<function>covar_pop()</function>、<function>regr_avgx()</function>、<function>regr_avgy()</function>、<function>regr_sxy()</function>、<function>regr_sxx()</function>、<function>regr_syy()</function>、<function>regr_count()</function>。
     </para>
     </listitem>

     <listitem>
      <para>
      允许<link linkend="SQL-CREATEDOMAIN">域</link>基于其他域（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      在所有地方正确强制执行域的 <link linkend="ddl-constraints"><literal>CHECK</literal></link> 约束（Neil、Tom）
     </para>

      <para>
      例如，声明为返回域类型的用户定义函数的结果现在会对照域的约束检查。这堵住了域实现中一个重要的漏洞。
     </para>
     </listitem>

     <listitem>
      <para>
      修复转储重命名的 <link linkend="datatype-serial"><type>SERIAL</type></link> 列的问题（Tom）
     </para>

      <para>
      修复方法是在转储 <type>SERIAL</type> 列时显式指定其 <literal>DEFAULT</literal> 和序列元素，并在重载时用新的 <link linkend="SQL-ALTERSEQUENCE"><command>ALTER SEQUENCE OWNED BY</command></link> 命令重建 <type>SERIAL</type> 列。这也允许删除 <type>SERIAL</type> 列规格。
     </para>
     </listitem>

     <listitem>
      <para>
      新增服务器端休眠函数 <link linkend="functions-datetime-delay"><function>pg_sleep()</function></link>（Joachim Wieland）
     </para>
     </listitem>

     <listitem>
      <para>
      为 <link linkend="datatype-oid"><type>tid</type></link>（元组 id）数据类型新增所有比较操作符（Mark Kirkwood、Greg Stark、Tom）
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title>PL/pgSQL 服务器端语言变更</title>
    <itemizedlist>

     <listitem>
      <para>
      为触发器参数新增 <literal>TG_table_name</literal> 和 <literal>TG_table_schema</literal>（Andrew）
     </para>

      <para>
      <literal>TG_relname</literal> 现已废弃。其他 PL 的触发器参数也做了类似变更。
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <literal>FOR</literal> 语句向标量以及记录和行类型返回值（Pavel Stehule）
     </para>
     </listitem>

     <listitem>
      <para>
      为 <literal>FOR</literal> 循环新增 <literal>BY</literal> 子句，控制迭代增量（Jaime Casanova）
     </para>
     </listitem>

     <listitem>
      <para>
      为 <link linkend="plpgsql-statements-sql-onerow"><command>SELECT INTO</command></link> 新增 <literal>STRICT</literal>（Matt Miller）
     </para>

      <para>
      为了与 <productname>Oracle PL/SQL</productname> 兼容，当 <command>SELECT</command> 返回的行多于或少于一行时，<literal>STRICT</literal> 模式抛出异常。
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title>PL/Perl 服务器端语言变更</title>
    <itemizedlist>

     <listitem>
      <para>
      为触发器参数新增 <literal>table_name</literal> 和 <literal>table_schema</literal>（Adam Sj&oslash;gren）
     </para>
     </listitem>

     <listitem>
      <para>
      新增预备查询（Dmitry Karasik）
     </para>
     </listitem>

     <listitem>
      <para>
      把 <literal>$_TD</literal> 触发器数据改为全局变量（Andrew）
     </para>

      <para>
      此前它是词法作用域的，导致意外的共享冲突。
     </para>
     </listitem>

     <listitem>
      <para>
      出于安全原因，在独立解释器中运行 PL/Perl 和 PL/PerlU（Andrew）
     </para>
      <para>
      因此它们不能再共享数据或已装载的模块。此外，如果 Perl 编译时未带允许多解释器的必需标志，任何给定后端进程中只能使用这两种语言之一。
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title>PL/Python 服务器端语言变更</title>
    <itemizedlist>

     <listitem>
      <para>
      命名参数除了通过 <literal>args[]</literal> 数组传递外，还作为普通变量传递（Sven Suursoho）
     </para>
     </listitem>

     <listitem>
      <para>
      为触发器参数新增 <literal>table_name</literal> 和 <literal>table_schema</literal>（Andrew）
     </para>
     </listitem>

     <listitem>
      <para>
      允许返回组合类型和结果集（Sven Suursoho）
     </para>
     </listitem>

     <listitem>
      <para>
      以 <literal>list</literal>、<literal>iterator</literal> 或 <literal>generator</literal> 返回结果集（Sven Suursoho）
     </para>
     </listitem>

     <listitem>
      <para>
      允许函数返回 <literal>void</literal>（Neil）
     </para>
     </listitem>

     <listitem>
      <para>
      现在支持 Python 2.5（Tom）
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title><link linkend="APP-PSQL"><application>psql</></link> 变更</title>
    <itemizedlist>

     <listitem>
      <para>
      新增命令 <literal>\password</literal>，用客户端口令加密更改角色口令（Peter）
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <literal>\c</literal> 连接到新的主机和端口号（David、Volkan YAZICI）
     </para>
     </listitem>

     <listitem>
      <para>
      为 <literal>\l+</literal> 新增表空间显示（Philip Yarra）
     </para>
     </listitem>

     <listitem>
      <para>
      改进 <literal>\df</literal> 反斜杠命令，使其包含函数的参数名和模式（<literal>OUT</literal> 或 <literal>INOUT</literal>）（David Fetter）
     </para>
     </listitem>

     <listitem>
      <para>
      支持二进制 <command>COPY</command>（Andreas Pflug）
     </para>
     </listitem>

     <listitem>
      <para>
      新增在单个事务中运行整个会话的选项（Simon）
     </para>

      <para>
      使用选项 <literal>-1</literal> 或 <literal>--single-transaction</literal>。
     </para>
     </listitem>

     <listitem>
      <para>
      支持用游标自动分批获取 <command>SELECT</command> 结果（Chris Mair）
     </para>

      <para>
      这通过 <command>\set FETCH_COUNT <replaceable>n</replaceable></command> 启用。此特性允许在 <application>psql</application> 中获取大结果集而不试图把整个结果集缓存在内存中。
     </para>
     </listitem>

     <listitem>
      <para>
      使多行值在正确的列上对齐（Martijn van Oosterhout）
     </para>

      <para>
      包含换行符的字段值现在以更可读的方式显示。
     </para>
     </listitem>

     <listitem>
      <para>
      把多行语句保存为单个条目，而不是一次一行（Sergey E. Koposov）
     </para>

      <para>
      这使用上箭头召回查询更容易。（这在 Windows 上不可用，因为该平台使用操作系统自带的本机命令行编辑。）
     </para>
     </listitem>

     <listitem>
      <para>
      把行计数器改为 64 位，使其能处理超过二十亿行的文件（David Fetter）
     </para>
     </listitem>

     <listitem>
      <para>
      为 <command>INSERT</command>/<command>UPDATE</command>/<command>DELETE RETURNING</command> 同时报告返回的数据和命令状态标签（Tom）
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title><link linkend="APP-PGDUMP"><application>pg_dump</></link> 变更</title>
    <itemizedlist>

     <listitem>
      <para>
      允许 <application>pg_dump</application> 复杂选择要包含或排除的对象（Greg Sabino Mullane）
     </para>

      <para>
      <application>pg_dump</application> 现在支持多个 <literal>-n</literal>（模式）和 <literal>-t</literal>（表）选项，并新增 <literal>-N</literal> 和 <literal>-T</literal> 选项来排除对象。此外，这些开关的参数现在可以是通配表达式而不是单个对象名，例如 <literal>-t 'foo*'</literal>，模式也可以是 <literal>-t</literal> 或 <literal>-T</literal> 开关的一部分，例如 <literal>-t schema1.table1</literal>。
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <link linkend="APP-PGRESTORE"><application>pg_restore</application></link> 的 <literal>--no-data-for-failed-tables</literal> 选项，在表创建失败（即表已存在）时抑制数据装载（Martin Pitt）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <link linkend="APP-PGRESTORE"><application>pg_restore</application></link> 选项，在单个事务中运行整个会话（Simon）
     </para>

      <para>
      使用选项 <literal>-1</literal> 或 <literal>--single-transaction</literal>。
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title><link linkend="libpq"><application>libpq</></link> 变更</title>
    <itemizedlist>

     <listitem>
      <para>
      新增用于加密口令的 <link linkend="libpq-misc"><function>PQencryptPassword()</function></link>（Tom）
     </para>

      <para>
      这允许为 <link linkend="SQL-ALTERROLE"><command>ALTER ROLE ... PASSWORD</command></link> 等命令发送预加密的口令。
     </para>
     </listitem>

     <listitem>
      <para>
      新增函数 <link linkend="libpq-threading"><function>PQisthreadsafe()</function></link>（Bruce）
     </para>

      <para>
      这允许应用程序查询库的线程安全状态。
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <link linkend="libpq-exec-main"><function>PQdescribePrepared()</function></link>、<link linkend="libpq-exec-main"><function>PQdescribePortal()</function></link> 及相关函数，返回有关先前预备的语句和打开的游标的信息（Volkan YAZICI）
     </para>
     </listitem>

     <listitem>
      <para>
      允许从 <link linkend="libpq-pgservice"><filename>pg_service.conf</filename></link> 进行 <link linkend="libpq-ldap"><acronym>LDAP</acronym></link> 查找（Laurenz Albe）
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <link linkend="libpq-pgpass"><filename>~/.pgpass</filename></link> 中的主机名匹配默认套接字目录（Bruce）
     </para>

      <para>
      空白主机名继续匹配任何 Unix 套接字连接，但此新增允许条目特定于机器上多个 postmaster 之一。
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title><link linkend="ecpg"><application>ecpg</></link> 变更</title>
    <itemizedlist>

     <listitem>
      <para>
      允许 <link linkend="SQL-SHOW"><command>SHOW</command></link> 把结果放入变量（Joachim Wieland）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <link linkend="SQL-COPY"><command>COPY TO STDOUT</command></link>（Joachim Wieland）
     </para>
     </listitem>

     <listitem>
      <para>
      新增回归测试（Joachim Wieland、Michael）
     </para>
     </listitem>

     <listitem>
      <para>
      大量源代码清理（Joachim Wieland、Michael）
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title><application>Windows</> 移植</title>
    <itemizedlist>

     <listitem>
      <para>
      允许 <acronym>MSVC</acronym> 编译 <productname>PostgreSQL</productname> 服务器（Magnus、Hiroshi Saito）
     </para>
     </listitem>

     <listitem>
      <para>
      为工具命令和 <link linkend="APP-PGDUMP"><application>pg_dump</application></link> 新增 <acronym>MSVC</acronym> 支持（Hiroshi Saito）
     </para>
     </listitem>

     <listitem>
      <para>
      新增对 Windows 代码页 <literal>1253</literal>、<literal>1254</literal>、<literal>1255</literal> 和 <literal>1257</literal> 的支持（Kris Jurka）
     </para>
     </listitem>

     <listitem>
      <para>
      启动时丢弃权限，使服务器可以从管理账户启动（Magnus）
     </para>
     </listitem>

     <listitem>
      <para>
      稳定性修复（Qingqing Zhou、Magnus）
     </para>
     </listitem>

     <listitem>
      <para>
      新增本机信号量实现（Qingqing Zhou）
     </para>

      <para>
      以前的代码模仿 SysV 信号量。
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title>源代码变更</title>
    <itemizedlist>

     <listitem>
      <para>
      新增 <link linkend="GIN"><acronym>GIN</acronym></link>（通用倒排索引）索引访问方法（Teodor、Oleg）
     </para>
     </listitem>

     <listitem>
      <para>
      移除 R-tree 索引（Tom）
     </para>

      <para>
      Rtree 已用 <link linkend="GiST"><acronym>GiST</acronym></link> 重新实现。除其他差异外，这意味着 rtree 索引现在通过预写日志（WAL）支持崩溃恢复。
     </para>
     </listitem>

     <listitem>
      <para>
      减少不必要链接到后端的库（Martijn van Oosterhout、Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 configure 标志，允许优先使用 libedit 而不是 <acronym>GNU</acronym> readline（Bruce）
     </para>

      <para>
      使用 configure 的 <link linkend="configure"><literal>--with-libedit-preferred</literal></link>。
     </para>
     </listitem>

     <listitem>
      <para>
      允许安装到包含空格的目录（Peter）
     </para>
     </listitem>

     <listitem>
      <para>
      改进重新定位安装目录的能力（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      新增对用 <productname>Solaris</productname> 编译器的 <productname>Solaris x86_64</productname> 的支持（Pierre Girard、Theo Schlossnagle、Bruce）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <application>DTrace</application> 支持（Robert Lor）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <literal>PG_VERSION_NUM</literal>，供想要在 C 中用 &gt; 和 &lt; 比较测试后端版本的第三方应用程序使用（Bruce）
     </para>
     </listitem>

     <listitem>
      <para>
      新增独立于 <literal>BLCKSZ</literal> 的 <literal>XLOG_BLCKSZ</literal>（Mark Wong）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <literal>LWLOCK_STATS</literal> 定义以报告锁定活动（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      对未知的 <application>configure</application> 选项发出警告（Martijn van Oosterhout）
     </para>
     </listitem>

     <listitem>
      <para>
      新增对<quote>插件</quote>库的服务器支持，可用于调试和性能测量等附加任务（Korry Douglas）
     </para>

      <para>
      这由两个特性组成：一张允许分别装载的共享库通信的<quote>会合变量</quote>表，以及新的配置参数 <link linkend="guc-local-preload-libraries"><varname>local_preload_libraries</varname></link>，它允许把库装载到特定会话而无需客户端应用程序的显式配合。这允许外部附加组件实现诸如 PL/pgSQL 调试器之类的特性。
     </para>
     </listitem>

     <listitem>
      <para>
      把现有配置参数 <varname>preload_libraries</varname> 改名为 <link linkend="guc-shared-preload-libraries"><varname>shared_preload_libraries</varname></link>（Tom）
     </para>

      <para>
      这是为了与 <varname>local_preload_libraries</varname> 相比更清晰。
     </para>
     </listitem>

     <listitem>
      <para>
      新增配置参数 <link linkend="guc-server-version-num"><varname>server_version_num</varname></link>（Greg Sabino Mullane）
     </para>

      <para>
      它类似 <varname>server_version</varname>，但是整数，例如 <literal>80200</literal>。这使应用程序更容易做版本检查。
     </para>
     </listitem>

     <listitem>
      <para>
      新增配置参数 <link linkend="guc-seq-page-cost"><varname>seq_page_cost</varname></link>（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      把<link linkend="regress">回归测试</link>脚本重新实现为 C 程序（Magnus、Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      允许可装载模块分配共享内存和轻量锁（Marc Munro）
     </para>
     </listitem>

     <listitem>
      <para>
      新增动态装载库的自动初始化和终结（Ralf Engelschall、Tom）
     </para>

      <para>
      如果库定义了这样的符号，就会调用新<link linkend="xfunc-c-dynload">函数</link> <function>_PG_init()</function> 和 <function>_PG_fini()</function>。因此我们不再需要在 <varname>shared_preload_libraries</varname> 中指定初始化函数；可以假定库使用了 <function>_PG_init()</function> 约定。
     </para>
     </listitem>

     <listitem>
      <para>
      为所有共享目标文件新增 <link linkend="xfunc-c-dynload"><literal>PG_MODULE_MAGIC</literal></link> 头块（Martijn van Oosterhout）
     </para>

      <para>
      魔块防止可装载目标文件与服务器之间的版本不匹配。
     </para>
     </listitem>

     <listitem>
      <para>
      新增 AIX 的共享库支持（Laurenz Albe）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <link linkend="datatype-xml"><acronym>XML</acronym></link> 文档节（Bruce）
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title>附加模块变更</title>
    <itemizedlist>

     <listitem>
      <para>
      重大 tsearch2 改进（Oleg、Teodor）
     </para>

      <itemizedlist>

       <listitem>
        <para>
      多字节编码支持，包括 <acronym>UTF8</acronym>
     </para>
       </listitem>
       <listitem>
        <para>
      查询重写支持
     </para>
       </listitem>
       <listitem>
        <para>
      改进的排名函数
     </para>
       </listitem>
       <listitem>
        <para>
      同义词词典支持
     </para>
       </listitem>
       <listitem>
        <para>
      Ispell 词典现在识别 <application>MySpell</application> 格式（<application>OpenOffice</application> 使用）
     </para>
       </listitem>
       <listitem>
        <para>
      <acronym>GIN</acronym> 支持
     </para>
       </listitem>

      </itemizedlist>

     </listitem>

     <listitem>
      <para>
      新增包含 <application>Pgadmin</application> 管理函数的 adminpack 模块（Dave）
     </para>

      <para>
      这些函数提供默认 <productname>PostgreSQL</productname> 服务器中没有的额外文件系统访问例程。
     </para>
     </listitem>

     <listitem>
      <para>
      新增 sslinfo 模块（Victor Wagner）
     </para>

      <para>
      报告当前连接 <acronym>SSL</acronym> 证书的信息。
     </para>
     </listitem>

     <listitem>
      <para>
      新增 pgrowlocks 模块（Tatsuo）
     </para>

      <para>
      它显示指定表的行锁定信息。
     </para>
     </listitem>

     <listitem>
      <para>
      新增 hstore 模块（Oleg、Teodor）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 isn 模块，取代 isbn_issn（Jeremy Kronuz）
     </para>

      <para>
      新实现支持 <acronym>EAN13</acronym>、<acronym>UPC</acronym>、<acronym>ISBN</acronym>（书籍）、<acronym>ISMN</acronym>（音乐）和 <acronym>ISSN</acronym>（期刊）。
     </para>
     </listitem>

     <listitem>
      <para>
      为 pgstattuple 新增索引信息函数（ITAGAKI Takahiro、Satoshi Nagayasu）
     </para>
     </listitem>

     <listitem>
      <para>
      新增显示空闲空间映射信息的 pg_freespacemap 模块（Mark Kirkwood）
     </para>
     </listitem>

     <listitem>
      <para>
      pgcrypto 现在具有所有计划的功能（Marko Kreen）
     </para>
      <itemizedlist>
       <listitem>
        <para>
      在 pgcrypto 中包含 iMath 库，使公钥加密函数始终可用。
     </para>
       </listitem>
       <listitem>
        <para>
      新增 OpenBSD 代码中缺失的 SHA224 算法。
     </para>
       </listitem>
       <listitem>
        <para>
      在较旧的 OpenSSL 上激活 SHA224/256/384/512 哈希的内建代码，使这些算法始终可用。
     </para>
       </listitem>
       <listitem>
        <para>
      新函数 gen_random_bytes()，返回密码学上强的随机数。适用于生成加密密钥。
     </para>
       </listitem>
       <listitem>
        <para>
      移除 digest_exists()、hmac_exists() 和 cipher_exists() 函数。
     </para>
       </listitem>
      </itemizedlist>
     </listitem>

     <listitem>
      <para>
      cube 模块改进（Joshua Reich）
     </para>

      <para>
      新函数为 <function>cube(float[])</function>、<function>cube(float[], float[])</function> 和 <function>cube_subset(cube, int4[])</function>。
     </para>
     </listitem>

     <listitem>
      <para>
      为 dblink 新增异步查询能力（Kai Londenberg、Joe Conway）
     </para>
     </listitem>

     <listitem>
      <para>
      新增用于数组子集比较的操作符（<literal>@&gt;</literal>、<literal>&lt;@</literal>、<literal>&amp;&amp;</literal>）（Tom）
     </para>

      <para>
      各种 contrib 包已经为它们的数据类型提供了这些操作符，但命名不一致。我们现在为核心代码和所有具有此功能的 contrib 包添加了命名一致的数组子集比较操作符。（旧名称仍可用，但已废弃。）
     </para>
     </listitem>

     <listitem>
      <para>
      为所有有安装脚本的 contrib 包新增卸载脚本（David、Josh Drake）
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

  </sect2>
 </sect1>
