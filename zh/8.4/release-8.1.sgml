<!-- doc/src/sgml/release-8.1.sgml -->
<!-- See header comment in release.sgml about typical markup -->

 <sect1 id="release-8-1-23">
  <title>版本 8.1.23</title>

  <note>
  <title>发行日期</title>
  <simpara>2010-12-16</simpara>
 </note>

  <para>
   本次发布包含对 8.1.22 的多方面修复。有关 8.1 大版本中新特性的信息，请参见<xref linkend="release-8-1">。
  </para>

  <para>
      这预计是 <productname>PostgreSQL</productname> 8.1.X 系列的最后一个发行版。建议用户尽快更新到较新的发行分支。
     </para>

  <sect2>
   <title>迁移到版本 8.1.23</title>

   <para>
      对于运行 8.1.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.1.18 的版本升级，请参阅<xref linkend="release-8-1-18">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      强制 Linux 上的默认 <link linkend="guc-wal-sync-method"><varname>wal_sync_method</varname></link> 为 <literal>fdatasync</literal>（Tom Lane、Marti Raudsepp）
     </para>

     <para>
      Linux 上的默认值实际上多年来一直是 <literal>fdatasync</literal>，但最近的内核变更导致 <productname>PostgreSQL</productname> 改选 <literal>open_datasync</literal>。这一选择没有带来任何性能改进，反而在某些文件系统（特别是使用 <literal>data=journal</literal> 挂载选项的 <literal>ext4</literal>）上导致彻底失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复当起始检查点 WAL 记录与其重做点不在同一 WAL 段时从基础备份恢复的问题（Jeff Davis）
     </para>
    </listitem>

    <listitem>
     <para>
      新增在 <literal>IA64</literal> 上检测寄存器栈溢出的支持（Tom Lane）
     </para>

     <para>
      <literal>IA64</literal> 架构有两个硬件栈。要完全防止栈溢出失败需要同时检查两者。
     </para>
    </listitem>

    <listitem>
     <para>
      在 <function>copyObject()</function> 中添加栈溢出检查（Tom Lane）
     </para>

     <para>
      给定足够复杂的查询，某些代码路径可能因栈溢出而崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      修复临时 GiST 索引中页面分裂的检测（Heikki Linnakangas）
     </para>

     <para>
      在临时索引中可能出现<quote>并发</quote>页面分裂，例如当插入时有打开的游标正在扫描该索引。GiST 未能检测到这种情况，因此当游标继续执行时可能给出错误结果。
     </para>
    </listitem>

    <listitem>
     <para>
      避免在 <command>ANALYZE</command> 复杂索引表达式时内存泄漏（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      确保使用整行 Var 的索引仍依赖于其表（Tom Lane）
     </para>

     <para>
      像 <literal>create index i on t (foo(t.*))</literal> 这样声明的索引在删除其表时不会自动被删除。
     </para>
    </listitem>

    <listitem>
     <para>
      不要<quote>内联</quote>带多个 <literal>OUT</literal> 参数的 SQL 函数（Tom Lane）
     </para>

     <para>
      这避免了因丢失预期结果行类型信息而可能出现的崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>COALESCE()</literal> 表达式的常量折叠（Tom Lane）
     </para>

     <para>
      规划器有时会尝试求值实际上永远不可达的子表达式，可能导致意外错误。
     </para>
    </listitem>

    <listitem>
     <para>
      为 <structname>InhRelation</structname> 节点添加打印功能（Tom Lane）
     </para>

     <para>
      这避免了启用 <varname>debug_print_parse</varname> 且执行某些类型的查询时的失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复点到水平线段距离的错误计算（Tom Lane）
     </para>

     <para>
      该缺陷影响多个不同的几何距离测量操作符。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>PL/pgSQL</application> 对<quote>简单</quote>表达式的处理，使其在递归或错误恢复情况下不失败（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/cube</filename> 的 GiST picksplit 算法中的缺陷（Alexander Korotkov）
     </para>

     <para>
      这可能导致 <type>cube</type> 列上的 GiST 索引相当低效，尽管答案并非真正错误。如果你有这样的索引，考虑在安装此更新后对其执行 <command>REINDEX</command>。
     </para>
    </listitem>

    <listitem>
     <para>
      <filename>contrib/dblink</filename> 除了在创建新连接时外不再发出<quote>identifier will be truncated</quote>通知（Itagaki Takahiro）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/pgcrypto</filename> 在缺少公钥时可能的核心转储（Marti Raudsepp）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/xml2</filename> 的 XPath 查询函数中的内存泄漏（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 发行版 2010o（针对斐济和萨摩亚的 DST 法规变更；以及对香港的历史修正）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-1-22">
  <title>版本 8.1.22</title>

  <note>
  <title>发行日期</title>
  <simpara>2010-10-04</simpara>
 </note>

  <para>
   本次发布包含对 8.1.21 的多方面修复。有关 8.1 大版本中新特性的信息，请参见<xref linkend="release-8-1">。
  </para>

  <para>
      <productname>PostgreSQL</productname> 社区将于 2010 年 11 月停止对 8.1.X 版本系列的更新。建议用户尽快更新到较新的版本分支。
     </para>

  <sect2>
   <title>迁移到版本 8.1.22</title>

   <para>
      对于运行 8.1.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.1.18 的版本升级，请参阅<xref linkend="release-8-1-18">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      在 PL/Perl 和 PL/Tcl 中为每个调用方 SQL 用户 ID 使用单独的解释器（Tom Lane）
     </para>

     <para>
      此变更防止了可通过破坏稍后将在同一会话中以另一 SQL 用户身份执行的 Perl 或 Tcl 代码（例如在 <literal>SECURITY DEFINER</literal> 函数内）而造成的安全问题。大多数脚本语言都提供许多实现这一点的手段，例如重新定义目标函数调用的标准函数或操作符。如果没有此变更，任何拥有 Perl 或 Tcl 语言使用权的 SQL 用户基本上都能以目标函数所有者的 SQL 权限为所欲为。
     </para>

     <para>
      此变更的代价是 Perl 和 Tcl 函数之间的有意通信变得更困难。为提供出路，PL/PerlU 和 PL/TclU 函数继续每会话只使用一个解释器。这不被视为安全问题，因为所有这类函数本来就以数据库超级用户的信任级别执行。
     </para>

     <para>
      声称提供可信执行的第三方过程语言很可能存在类似的安全问题。我们建议就安全关键用途联系你所依赖的任何 PL 的作者。
     </para>

     <para>
      感谢 Tim Bunce 指出此问题（CVE-2010-3433）。
     </para>
    </listitem>

    <listitem>
     <para>
      禁止以并非它预期配合使用的系统目录列的参数调用 <function>pg_get_expr()</function>，以防止可能的崩溃（Heikki Linnakangas、Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复<quote>无法处理未规划的子查询</quote>错误（Tom Lane）
     </para>

     <para>
      当子查询包含一个连接别名引用，而该引用展开成包含另一个子查询的表达式时，会出现此错误。
     </para>
    </listitem>

    <listitem>
     <para>
      防止 show_session_authorization() 在自动清理进程中崩溃（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      防御返回 setof record 但并非所有返回行实际为同一行类型的函数（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复对按引用传递的函数结果计算哈希时可能的失败（Tao Ma、Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      写入锁文件（<filename>postmaster.pid</filename> 和套接字锁文件两者）内容时注意对其执行 fsync（Tom Lane）
     </para>

     <para>
      如果机器在 postmaster 启动后不久崩溃，这一疏漏可能导致锁文件内容损坏。这又可能使后续启动 postmaster 的尝试失败，直到手工删除锁文件。
     </para>
    </listitem>

    <listitem>
     <para>
      避免在为深层嵌套的子事务分配 XID 时递归（Andres Freund、Robert Haas）
     </para>

     <para>
      在栈空间有限时，原来的编码可能导致崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <varname>log_line_prefix</varname> 的 <literal>%i</literal> 转义，它可能在后端启动早期产生垃圾（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复启用归档时 <command>ALTER TABLE ... SET TABLESPACE</command> 中可能的数据损坏（Jeff Davis）
     </para>
    </listitem>

    <listitem>
     <para>
      允许 <command>CREATE DATABASE</command> 和 <command>ALTER DATABASE ... SET TABLESPACE</command> 被查询取消中断（Guillaume Lelarge）
     </para>
    </listitem>

    <listitem>
     <para>
      在 PL/Python 中，防御 <function>PyCObject_AsVoidPtr</function> 和 <function>PyCObject_FromVoidPtr</function> 的空指针结果（Peter Eisentraut）
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <filename>contrib/dblink</filename> 对包含已删除列的表的处理（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/dblink</filename> 中<quote>重复连接名</quote>错误后的连接泄漏（Itagaki Takahiro）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/dblink</filename> 以正确处理超过 62 字节的连接名（Itagaki Takahiro）
     </para>
    </listitem>

    <listitem>
     <para>
      更新构建基础设施和文档，反映源代码仓库从 CVS 迁移到 Git 的变化（Magnus Hagander 等）
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 发行版 2010l（针对埃及和巴勒斯坦的 DST 法规变更；以及对芬兰的历史修正）
     </para>

     <para>
      此变更还为两个密克罗尼西亚时区新增了名称：Pacific/Chuuk 现在优先于 Pacific/Truk（首选缩写为 CHUT 而非 TRUT），Pacific/Pohnpei 优先于 Pacific/Ponape。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-1-21">
  <title>版本 8.1.21</title>

  <note>
  <title>发行日期</title>
  <simpara>2010-05-17</simpara>
 </note>

  <para>
   本次发布包含对 8.1.20 的多方面修复。有关 8.1 大版本中新特性的信息，请参见<xref linkend="release-8-1">。
  </para>

  <sect2>
   <title>迁移到版本 8.1.21</title>

   <para>
      对于运行 8.1.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.1.18 的版本升级，请参阅<xref linkend="release-8-1-18">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      用应用于整个解释器的操作码掩码来强制 <literal>plperl</literal> 中的限制，而不是使用 <filename>Safe.pm</filename>（Tim Bunce、Andrew Dunstan）
     </para>

     <para>
      近期的事态发展使我们确信 <filename>Safe.pm</filename> 太不安全，不能依靠它使 <literal>plperl</literal> 可信。此变更完全移除了对 <filename>Safe.pm</filename> 的使用，改为使用始终应用操作码掩码的独立解释器。此变更的良好副作用包括：现在可以在 <literal>plperl</literal> 中以自然的方式使用 Perl 的 <literal>strict</literal> 编译指令，Perl 的 <literal>$a</literal> 和 <literal>$b</literal> 变量在排序例程中按预期工作，以及函数编译显著加快。（CVE-2010-1169）
     </para>
    </listitem>

    <listitem>
     <para>
      防止 PL/Tcl 执行来自 <structname>pltcl_modules</structname> 的不可信代码（Tom）
     </para>

     <para>
      PL/Tcl 从数据库表自动装载 Tcl 代码的特性可能被利用于特洛伊木马攻击，因为对谁能创建该表或向其中插入没有限制。此变更禁用该特性，除非 <structname>pltcl_modules</structname> 由超级用户拥有。（但是，不检查该表上的权限，因此确实需要较不安全的模块表的安装仍可以向受信任的非超级用户授予适当权限。）此外，除非确实要执行 <literal>pltclu</literal> 函数，否则防止把代码装载到无限制的<quote>普通</quote>Tcl 解释器中。（CVE-2010-1170）
     </para>
    </listitem>

    <listitem>
     <para>
      不允许非特权用户重置仅超级用户可用的参数设置（Alvaro）
     </para>

     <para>
      此前，如果非特权用户为自己运行 <literal>ALTER USER ... RESET ALL</literal>，或为他拥有的数据库运行 <literal>ALTER DATABASE ... RESET ALL</literal>，这将移除该用户或数据库的所有特殊参数设置，包括本应只可由超级用户更改的那些。现在，<command>ALTER</command> 只移除用户有权更改的参数。
     </para>
    </listitem>

    <listitem>
     <para>
      避免后端关闭发生在需要向日志条目添加 <literal>CONTEXT</literal> 内容时导致可能的崩溃（Tom）
     </para>

     <para>
      某些情况下上下文打印函数会失败，因为到打印日志消息时当前事务已被回滚。
     </para>
    </listitem>

    <listitem>
     <para>
      为现代 Perl 版本更新 pl/perl 的 <filename>ppport.h</filename>（Andrew）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 pl/python 中的若干内存泄漏（Andreas Freund、Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      防止 <application>psql</application> 在展开引用自身的变量时无限递归（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      确保 <filename>contrib/pgstattuple</filename> 函数及时响应取消中断（Tatsuhito Kasahara）
     </para>
    </listitem>

    <listitem>
     <para>
      让服务器启动正确处理 <function>shmget()</function> 对已存在的共享内存段返回 <literal>EINVAL</literal> 的情形（Tom）
     </para>

     <para>
      此行为曾在包括 OS X 在内的 BSD 衍生内核上观察到。它导致完全误导性的启动失败，抱怨共享内存请求大小过大。
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 发行版 2010j（针对阿根廷、澳大利亚南极、孟加拉、墨西哥、摩洛哥、巴基斯坦、巴勒斯坦、俄罗斯、叙利亚、突尼斯的 DST 法规变更；以及对台湾的历史修正）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-1-20">
  <title>版本 8.1.20</title>

  <note>
  <title>发行日期</title>
  <simpara>2010-03-15</simpara>
 </note>

  <para>
   本次发布包含对 8.1.19 的多方面修复。有关 8.1 大版本中新特性的信息，请参见<xref linkend="release-8-1">。
  </para>

  <sect2>
   <title>迁移到版本 8.1.20</title>

   <para>
      对于运行 8.1.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.1.18 的版本升级，请参阅<xref linkend="release-8-1-18">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      新增配置参数 <varname>ssl_renegotiation_limit</varname>，控制对 SSL 连接进行会话密钥重协商的频率（Magnus）
     </para>

     <para>
      可以把它设为零以完全禁用重协商，这在使用有缺陷的 SSL 库时可能是必需的。特别是，某些厂商发布的 CVE-2009-3555 临时补丁会导致重协商尝试失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复试图从子事务启动失败中恢复时可能的崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复与使用保存点以及客户端编码不同于服务器编码相关的服务器内存泄漏（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <type>bit</type> 类型的 <function>substring()</function> 把任何负长度都视为<quote>字符串的其余全部</quote>（Tom）
     </para>

     <para>
      以前的编码只对 -1 这样处理，对其他负值会产生无效的结果值，可能导致崩溃（CVE-2010-0442）。
     </para>
    </listitem>

    <listitem>
     <para>
      修复整数到位串的转换：当输出位宽比给定整数宽出非 8 位倍数时，正确处理第一个小数字节（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复若干病态缓慢的正则表达式匹配情形（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复备份历史文件中的 <literal>STOP WAL LOCATION</literal> 条目：当结束位置恰好位于段边界时报告下一个 WAL 段的名称（Itagaki Takahiro）
     </para>
    </listitem>

    <listitem>
     <para>
      修复更多临时文件泄漏的情形（Heikki）
     </para>

     <para>
      这纠正了上一个小版本中引入的问题。失败的一种情形是返回集合的 plpgsql 函数在另一个函数的异常处理器中被调用。
     </para>
    </listitem>

    <listitem>
     <para>
      读取 <filename>pg_hba.conf</filename> 及相关文件时，如果 <literal>@</literal> 出现在引号内，不要把 <literal>@something</literal> 当作文件包含请求；另外，永远不要把单独的 <literal>@</literal> 当作文件包含请求（Tom）
     </para>

     <para>
      这防止了角色名或数据库名以 <literal>@</literal> 开头时的古怪行为。如果需要包含路径名包含空格的文件，仍然可以做到，但必须写 <literal>@"/path to/file"</literal> 而不是把引号放在整个构造之外。
     </para>
    </listitem>

    <listitem>
     <para>
      防止在某些平台上把目录命名为 <filename>pg_hba.conf</filename> 及相关文件中的包含目标时出现无限循环（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>psql</application> 的 <literal>numericlocale</literal> 选项，使其不在 latex 和 troff 输出格式中格式化不应格式化的字符串（Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      修复组合列被设为 NULL 的一种情形下 plpgsql 的失败（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      在 PL/Python 中添加 <literal>volatile</literal> 标记，以避免可能的编译器特定错误行为（Zdenek Kotala）
     </para>
    </listitem>

    <listitem>
     <para>
      确保 PL/Tcl 完全初始化 Tcl 解释器（Tom）
     </para>

     <para>
      此疏漏唯一已知的症状是使用 Tcl 8.5 或更高版本时 Tcl 的 <literal>clock</literal> 命令行为异常。
     </para>
    </listitem>

    <listitem>
     <para>
      防止在向 <function>dblink_build_sql_*</function> 函数指定过多键列时 <filename>contrib/dblink</filename> 崩溃（Rushabh Lathia、Joe Conway）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/xml2</filename> 中因草率内存管理导致的若干崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 发行版 2010e（针对孟加拉、智利、斐济、墨西哥、巴拉圭、萨摩亚的 DST 法规变更）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-1-19">
  <title>版本 8.1.19</title>

  <note>
  <title>发行日期</title>
  <simpara>2009-12-14</simpara>
 </note>

  <para>
   本次发布包含对 8.1.18 的多方面修复。有关 8.1 大版本中新特性的信息，请参见<xref linkend="release-8-1">。
  </para>

  <sect2>
   <title>迁移到版本 8.1.19</title>

   <para>
      对于运行 8.1.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.1.18 的版本升级，请参阅<xref linkend="release-8-1-18">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      防范索引函数更改会话局部状态导致的间接安全威胁（Gurjeet Singh、Tom）
     </para>

     <para>
      此变更防止据称不可变的索引函数可能破坏超级用户的会话（CVE-2009-4136）。
     </para>
    </listitem>

    <listitem>
     <para>
      拒绝在公共名（CN）字段中包含嵌入空字节的 SSL 证书（Magnus）
     </para>

     <para>
      这防止在 SSL 验证期间证书与服务器或客户端名称的非预期匹配（CVE-2009-4034）。
     </para>
    </listitem>

    <listitem>
     <para>
      修复后端启动时缓存初始化期间可能的崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      防止信号在不安全的时刻中断 <literal>VACUUM</literal>（Alvaro）
     </para>

     <para>
      此修复防止了 <literal>VACUUM FULL</literal> 在已提交其元组移动后被取消时的 PANIC，以及普通 <literal>VACUUM</literal> 在截断表之后被中断时的瞬态错误。
     </para>
    </listitem>

    <listitem>
     <para>
      修复哈希表大小计算中因整数溢出而可能的崩溃（Tom）
     </para>

     <para>
      这可能发生在规划器对哈希连接结果大小的估计极大时。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <type>inet</type>/<type>cidr</type> 比较中非常罕见的崩溃（Chris Mikkelson）
     </para>
    </listitem>

    <listitem>
     <para>
      确保预备事务所持有的共享元组级锁不被忽略（Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      修复在子事务内访问的游标所用临时文件被过早删除的问题（Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PAM 口令处理使其更健壮（Tom）
     </para>

     <para>
      已知以前的代码在 Linux <literal>pam_krb5</literal> PAM 模块与 Microsoft Active Directory 域控制器组合下失败。它在其他地方可能也有问题，因为它对 PAM 栈会传递给它什么参数做了无根据的假设。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>CREATE OR REPLACE FUNCTION</literal> 期间所有权依赖的处理（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      确保 Perl 数组在由返回集合的 PL/Perl 函数返回时被正确转换为 <productname>PostgreSQL</productname> 数组（Andrew Dunstan、Abhijit Menon-Sen）
     </para>

     <para>
      对非返回集合的函数这早已正确工作。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PL/Python 异常处理中的罕见崩溃（Peter）
     </para>
    </listitem>

    <listitem>
     <para>
      确保 <application>psql</application> 的 flex 模块用正确的系统头文件定义编译（Tom）
     </para>

     <para>
      这修复了在 <literal>--enable-largefile</literal> 导致生成代码不兼容变更的平台上的构建失败。
     </para>
    </listitem>

    <listitem>
     <para>
      让 postmaster 忽略连接请求包中的任何 <literal>application_name</literal> 参数，以改进与未来 libpq 版本的兼容性（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 发行版 2009s（针对南极洲、阿根廷、孟加拉、斐济、新库兹涅茨克、巴基斯坦、巴勒斯坦、萨摩亚、叙利亚的 DST 法规变更；以及对香港的历史修正）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-1-18">
  <title>版本 8.1.18</title>

  <note>
  <title>发行日期</title>
  <simpara>2009-09-09</simpara>
 </note>

  <para>
   本次发布包含对 8.1.17 的多方面修复。有关 8.1 大版本中新特性的信息，请参见<xref linkend="release-8-1">。
  </para>

  <sect2>
   <title>迁移到版本 8.1.18</title>

   <para>
      对于运行 8.1.X 的用户，不需要进行转储/恢复。不过，如果你在 <type>interval</type> 列上有任何哈希索引，必须在更新到 8.1.18 后对它们执行 <command>REINDEX</command>。另外，如果你是从早于 8.1.15 的版本升级，请参阅<xref linkend="release-8-1-15">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      不允许在安全定义者函数内使用 <command>RESET ROLE</command> 和 <command>RESET SESSION AUTHORIZATION</command>（Tom、Heikki）
     </para>

     <para>
      这覆盖了先前禁止在安全定义者函数内使用 <command>SET ROLE</command> 和 <command>SET SESSION AUTHORIZATION</command> 的补丁中遗漏的一种情形。（参见 CVE-2007-6600）
     </para>
    </listitem>

    <listitem>
     <para>
      修复出现在外层聚合函数参数中的子 SELECT 的处理（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <type>interval</type> 数据类型的哈希计算（Tom）
     </para>

     <para>
      这纠正了 interval 值上哈希连接的错误结果。它还更改了 interval 列上哈希索引的内容。如果你有任何这样的索引，必须在更新后对它们执行 <command>REINDEX</command>。
     </para>
    </listitem>

    <listitem>
     <para>
      把 <function>to_char(..., 'TH')</function> 在 <literal>'HH'</literal>/<literal>'HH12'</literal> 下当作大写序数后缀（Heikki）
     </para>

     <para>
      它以前被当作 <literal>'th'</literal>（小写）处理。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <replaceable>x</replaceable> 超过 200 万且使用整数日期时间时 <literal>INTERVAL '<replaceable>x</replaceable> ms'</literal> 的溢出（Alex Hunsaker）
     </para>
    </listitem>

    <listitem>
     <para>
      修复点与线段之间距离的计算（Tom）
     </para>

     <para>
      这导致多个几何操作符的结果不正确。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <type>money</type> 数据类型，使其在货币金额没有小数位的区域设置（例如日本）中工作（Itagaki Takahiro）
     </para>
    </listitem>

    <listitem>
     <para>
      正确地对像 <literal>00:12:57.9999999999999999999999999999</literal> 这样的日期时间输入四舍五入（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 GiST R-tree 操作符类中页分裂点选择不佳的问题（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 plperl 初始化中的可移植性问题（Andrew Dunstan）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_ctl</application>，使其在 <filename>postgresql.conf</filename> 为空时不进入无限循环（Jeff Davis）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/xml2</filename> 的 <function>xslt_process()</function>，使其正确处理最大参数数量（二十个）（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <application>libpq</application> 从 <command>COPY FROM STDIN</command> 期间错误中恢复的代码的健壮性（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      避免在两个库都安装时包含相互冲突的 readline 和 editline 头文件（Zdenek Kotala）
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 发行版 2009l（针对孟加拉、埃及、约旦、巴基斯坦、阿根廷/圣路易斯、古巴、毛里求斯、摩洛哥、巴勒斯坦、叙利亚、突尼斯的 DST 法规变更；其中对约旦仅作历史修正）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-1-17">
  <title>版本 8.1.17</title>

  <note>
  <title>发行日期</title>
  <simpara>2009-03-16</simpara>
 </note>

  <para>
   本次发布包含对 8.1.16 的多方面修复。有关 8.1 大版本中新特性的信息，请参见<xref linkend="release-8-1">。
  </para>

  <sect2>
   <title>迁移到版本 8.1.17</title>

   <para>
      对于运行 8.1.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.1.15 的版本升级，请参阅<xref linkend="release-8-1-15">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      防止编码转换失败时的错误递归崩溃（Tom）
     </para>

     <para>
      此变更扩展了上两个小版本中针对相关失败情形所做的修复。以前的修复是针对最初的问题报告量身定制的，但我们现在认识到，编码转换函数抛出的<emphasis>任何</emphasis>错误都可能在试图报告该错误时导致无限递归。因此，解决方案是：如果发现已处于递归错误报告情形，就禁用翻译和编码转换并报告错误消息的纯 ASCII 形式。（CVE-2009-0922）
     </para>
    </listitem>

    <listitem>
     <para>
      不允许 <command>CREATE CONVERSION</command> 为指定的转换函数使用错误的编码（Heikki）
     </para>

     <para>
      这防止了编码转换失败的一种可能情形。前一个变更是兜底，防范同一领域其他类型的失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>to_char()</function> 被给定与数据参数类型不适当的格式代码时的核心转储（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复带隐式强制转换的 <literal>CASE WHEN</literal> 的反编译（Tom）
     </para>

     <para>
      在试图检查或转储视图时，这一错误在启用断言的构建中可能导致断言失败，在其他情况下可能导致<quote>意外的 CASE WHEN 子句</quote>错误消息。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 TOAST 表行类型所有者可能的错误分配（Tom）
     </para>

     <para>
      如果 <command>CLUSTER</command> 或重写形式的 <command>ALTER TABLE</command> 由表所有者之外的人执行，表的 TOAST 表的 <structname>pg_type</structname> 条目最终会被标记为那个人所拥有。这不会立即引起问题，因为普通数据库操作不检查 TOAST 行类型的权限。但是，如果后来试图删除发出该命令的角色（在 8.1 或 8.2 中），或者在那之后 <application>pg_dump</application> 发出<quote>数据类型的所有者似乎无效</quote>警告（在 8.3 中），则可能导致意外失败。
     </para>
    </listitem>

    <listitem>
     <para>
      在块退出时完全清理 PL/pgSQL 错误状态变量（Ashesh Vashi 和 Dave Page）
     </para>

     <para>
      这对 PL/pgSQL 本身不是问题，但这一疏漏可能导致 PL/pgSQL 调试器在检查函数状态时崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      把 <literal>MUST</literal>（毛里求斯岛夏令时）加入已知时区缩写的默认列表（Xavier Bugaud）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-1-16">
  <title>版本 8.1.16</title>

  <note>
  <title>发行日期</title>
  <simpara>2009-02-02</simpara>
 </note>

  <para>
   本次发布包含对 8.1.15 的多方面修复。有关 8.1 大版本中新特性的信息，请参见<xref linkend="release-8-1">。
  </para>

  <sect2>
   <title>迁移到版本 8.1.16</title>

   <para>
      对于运行 8.1.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.1.15 的版本升级，请参阅<xref linkend="release-8-1-15">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      修复 autovacuum 中的崩溃（Alvaro）
     </para>

     <para>
      该崩溃只在为防止事务回卷而清理整个数据库之后发生，这意味着它很少发生且难以追查。
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <function>headline()</function> 函数中对 URL 的处理（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <function>headline()</function> 函数对超长标题的处理（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      防止在为指定的编码对创建了错误转换函数的编码转换时可能的断言失败或错误转换（Tom、Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      避免 <command>VACUUM</command> 中对小表的不必要锁定（Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      确保可保持游标的内容不依赖于 TOAST 表的内容（Tom）
     </para>

     <para>
      此前，游标结果中的大字段值可能被表示为 TOAST 指针，如果被引用的表在游标被读取之前被删除，或者大值被删除随后被清理掉，这就会失败。普通游标不会发生这种情况，但对于保持到创建事务之后的游标则可能。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/tsearch2</filename> 的 <function>get_covers()</function> 函数中的未初始化变量（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>configure</application> 脚本，使其在无法获得 PL/Perl 的链接信息时正确报告失败（Andrew）
     </para>
    </listitem>

    <listitem>
     <para>
      让所有文档在适当之处引用 <literal>pgsql-bugs</literal> 和/或 <literal>pgsql-hackers</literal>，而不是现已停用的 <literal>pgsql-ports</literal> 和 <literal>pgsql-patches</literal> 邮件列表（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      更新时区数据文件到 <application>tzdata</application> 发行版 2009a（针对加德满都以及瑞士、古巴的历史 DST 修正）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-1-15">
  <title>版本 8.1.15</title>

  <note>
  <title>发行日期</title>
  <simpara>2008-11-03</simpara>
 </note>

  <para>
   本次发布包含对 8.1.14 的多方面修复。有关 8.1 大版本中新特性的信息，请参见<xref linkend="release-8-1">。
  </para>

  <sect2>
   <title>迁移到版本 8.1.15</title>

   <para>
      对于运行 8.1.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.1.2 的版本升级，请参阅<xref linkend="release-8-1-2">。另外，如果你运行的是之前的 8.1.X 版本，建议在升级后对所有 GiST 索引执行 <command>REINDEX</command>。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      修复 GiST 索引因删除后把错误的索引条目标记为<quote>死</quote>而损坏的问题（Teodor）
     </para>

     <para>
      这会导致索引搜索找不到本应找到的行。损坏的索引可以用 <command>REINDEX</command> 修复。
     </para>
    </listitem>

    <listitem>
     <para>
      修复客户端编码无法表示本地化错误消息时的后端崩溃（Tom）
     </para>

     <para>
      我们此前处理过类似问题，但如果<quote>字符没有等价物</quote>消息本身无法转换，仍会失败。修复方法是在检测到这种情形时禁用本地化并发送纯 ASCII 错误消息。
     </para>
    </listitem>

    <listitem>
     <para>
      修复从触发器调用深层嵌套函数时可能的崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复当子 <literal>SELECT</literal> 出现在 <literal>FROM</literal> 中的函数调用、多行 <literal>VALUES</literal> 列表或 <literal>RETURNING</literal> 列表中时规则查询的错误展开（Tom）
     </para>

     <para>
      此问题通常的症状是<quote>无法识别的节点类型</quote>错误。
     </para>
    </listitem>

    <listitem>
     <para>
      确保新定义的 PL/pgSQL 触发器函数被当作普通函数调用时报错（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      防止用 <command>ALTER SET TABLESPACE</command> 把表移到另一个表空间时 <structfield>relfilenode</structfield> 号可能的冲突（Heikki）
     </para>

     <para>
      该命令试图重用现有文件名，而不是选取目标目录中已知未使用的文件名。
     </para>
    </listitem>

    <listitem>
     <para>
      修复单个查询项匹配文本首词时 tsearch2 标题生成不正确的问题（Sushant Sinha）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <option>--enable-integer-datetimes</option> 构建中使用非 ISO datestyle 时 interval 值小数秒显示不当的问题（Ron Mayer）
     </para>
    </listitem>

    <listitem>
     <para>
      确保 <function>SPI_getvalue</function> 和 <function>SPI_getbinval</function> 在传入的元组和元组描述符列数不同时行为正确（Tom）
     </para>

     <para>
      当表添加或删除过列时这种情况是正常的，但这两个函数没有正确处理它。唯一可能的后果是错误的错误指示。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>ecpg</application> 对 <command>CREATE ROLE</command> 的解析（Michael）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>pg_ctl restart</literal> 最近损坏的问题（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      更新时区数据文件到 <application>tzdata</application> 发行版 2008i（针对阿根廷、巴西、毛里求斯、叙利亚的 DST 法规变更）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-1-14">
  <title>版本 8.1.14</title>

  <note>
  <title>发行日期</title>
  <simpara>2008-09-22</simpara>
 </note>

  <para>
   本次发布包含对 8.1.13 的多方面修复。有关 8.1 大版本中新特性的信息，请参见<xref linkend="release-8-1">。
  </para>

  <sect2>
   <title>迁移到版本 8.1.14</title>

   <para>
      对于运行 8.1.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.1.2 的版本升级，请参阅<xref linkend="release-8-1-2">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      把本地锁计数器从 32 位拓宽到 64 位（Tom）
     </para>

     <para>
      这是针对计数器在足够长的事务中可能溢出的报告，该溢出会导致意外的<quote>锁已被持有</quote>错误。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 GiST 索引扫描期间可能的元组重复输出（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      在执行器启动时添加检查，确保 <command>INSERT</command> 或 <command>UPDATE</command> 产生的元组与目标表的当前行类型匹配（Tom）
     </para>

     <para>
      <command>ALTER COLUMN TYPE</command> 之后重用先前缓存的计划可能产生这种情形。该检查防范随之而来的数据损坏和/或崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>AT TIME ZONE</literal>：先尝试把时区参数解释为时区缩写，仅在失败时才尝试完整时区名，而不是像以前那样相反（Tom）
     </para>

     <para>
      时间戳输入函数一直按此顺序解析有歧义的时区名。让 <literal>AT TIME ZONE</literal> 也这样做改进了一致性，并修复了 8.1 中引入的兼容性缺陷：在有歧义的情形下，我们现在的行为与 8.0 及更早版本相同，因为在旧版本中 <literal>AT TIME ZONE</literal> <emphasis>只</emphasis>接受缩写。
     </para>
    </listitem>

    <listitem>
     <para>
      修复日期时间输入函数在 64 位平台上正确检测整数溢出（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      改进向 syslog 写很长日志消息的性能（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>SELECT DISTINCT ON</literal> 查询上游标反向扫描的缺陷（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复嵌套子查询表达式的规划器缺陷（Tom）
     </para>

     <para>
      如果外层子查询不直接依赖父查询而内层依赖，外层的值可能不会为新的父查询行重新计算。
     </para>
    </listitem>

    <listitem>
     <para>
      修复规划器，使其估计产生布尔结果的 <literal>GROUP BY</literal> 表达式总是产生两个组，而不考虑表达式的内容（Tom）
     </para>

     <para>
      对于某些布尔测试（如 <replaceable>col</replaceable> <literal>IS NULL</literal>），这比常规的 <literal>GROUP BY</literal> 估计准确得多。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PL/pgSQL 在 <literal>FOR</literal> 循环的目标变量是包含组合类型字段的记录时不失败（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PL/Tcl 在 Tcl 8.5 下的正确行为，并更小心地处理发送到 Tcl 或从 Tcl 发出的数据的编码（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PL/Python 使其与 Python 2.5 一起工作
     </para>

     <para>
      这是 8.2 开发周期中所做修复的向后移植。
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <application>pg_dump</application> 和 <application>pg_restore</application> 在发送 SQL 命令失败后的错误报告（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_ctl</application>，使其在 <literal>restart</literal> 期间正确保留 postmaster 命令行参数（Bruce）
     </para>
    </listitem>

    <listitem>
     <para>
      更新时区数据文件到 <application>tzdata</application> 发行版 2008f（针对阿根廷、巴哈马、巴西、毛里求斯、摩洛哥、巴基斯坦、巴勒斯坦和巴拉圭的 DST 法规变更）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-1-13">
  <title>版本 8.1.13</title>

  <note>
  <title>发行日期</title>
  <simpara>2008-06-12</simpara>
 </note>

  <para>
      此发行版在 8.1.12 的基础上包含一个严重和一个次要的修复。有关 8.1 主版本新特性的信息，请参阅<xref linkend="release-8-1">。
     </para>

  <sect2>
   <title>迁移到版本 8.1.13</title>

   <para>
      对于运行 8.1.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.1.2 的版本升级，请参阅<xref linkend="release-8-1-2">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      让 <function>pg_get_ruledef()</function> 给负常数加括号（Tom）
     </para>

     <para>
      在此修复之前，视图或规则中的负常数可能被转储成如 <literal>-42::integer</literal>，这有微妙的错误：由于操作符优先级规则，它应该是 <literal>(-42)::integer</literal>。通常这影响不大，但它可能与另一个最近的补丁相互作用，导致 <productname>PostgreSQL</productname> 拒绝原本有效的 <command>SELECT DISTINCT</command> 视图查询。由于这可能导致 <application>pg_dump</application> 输出无法重载，它被当作高优先级修复。转储输出确实不正确的已发布版本只有 8.3.1 和 8.2.7。
     </para>
    </listitem>

    <listitem>
     <para>
      让 <command>ALTER AGGREGATE ... OWNER TO</command> 更新 <structname>pg_shdepend</structname>（Tom）
     </para>

     <para>
      如果聚合后来涉及 <command>DROP OWNED</command> 或 <command>REASSIGN OWNED</command> 操作，这一疏漏可能导致问题。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-1-12">
  <title>版本 8.1.12</title>

  <note>
  <title>发行日期</title>
  <simpara>
      从未发布
     </simpara>
 </note>

  <para>
   本次发布包含对 8.1.11 的多方面修复。有关 8.1 大版本中新特性的信息，请参见<xref linkend="release-8-1">。
  </para>

  <sect2>
   <title>迁移到版本 8.1.12</title>

   <para>
      对于运行 8.1.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.1.2 的版本升级，请参阅<xref linkend="release-8-1-2">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      修复 <command>ALTER TABLE ADD COLUMN ... PRIMARY KEY</command>，使新列被正确检查是否已全部初始化为非空（Brendan Jurd）
     </para>

     <para>
      以前的版本完全忽略了对这一要求的检查。
     </para>
    </listitem>

    <listitem>
     <para>
      修复从多个都继承自共同祖先的父关系继承<quote>同一</quote>约束时 <command>CREATE TABLE</command> 可能失败的问题（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 ISO-8859-5 与其他编码之间的转换对西里尔<quote>Yo</quote>字符（带两个点的 <literal>e</literal> 和 <literal>E</literal>）的处理（Sergey Burladyan）
     </para>
    </listitem>

    <listitem>
     <para>
      修复几个数据类型输入函数，它们允许结果中未使用的字节包含未初始化的不可预测值（Tom）
     </para>

     <para>
      这可能导致两个看起来相同的字面值不被视为相等的失败，导致解析器抱怨 <literal>ORDER BY</literal> 和 <literal>DISTINCT</literal> 表达式不匹配。
     </para>
    </listitem>

    <listitem>
     <para>
      修复正则表达式子串匹配（<literal>substring(<replaceable>string</replaceable> from <replaceable>pattern</replaceable>)</literal>）中的一个极端情形（Tom）
     </para>

     <para>
      问题发生在模式整体有匹配但用户指定了带括号的子表达式而该子表达式没有匹配时。例子是 <literal>substring('foo' from 'foo(bar)?')</literal>。由于 <literal>(bar)</literal> 未匹配，它应返回 NULL，但它错误地返回了整个模式的匹配（即 <literal>foo</literal>）。
     </para>
    </listitem>

    <listitem>
     <para>
      更新时区数据文件到 <application>tzdata</application> 发行版 2008c（针对摩洛哥、伊拉克、乔巴山、巴基斯坦、叙利亚、古巴、阿根廷/圣路易斯和智利的 DST 法规变更）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>ecpg</application> 的 <function>PGTYPEStimestamp_sub()</function> 函数结果不正确的问题（Michael）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/xml2</filename> 的 <function>xpath_table()</function> 函数在输入查询返回 NULL 值时的核心转储（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/xml2</filename> 的 makefile，使其不覆盖 <literal>CFLAGS</literal>（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>DatumGetBool</literal> 宏，使其在 <application>gcc</application> 4.3 下不失败（Tom）
     </para>

     <para>
      此问题影响返回布尔的<quote>旧式</quote>（V0）C 函数。修复已在 8.3 中，但当时没有意识到需要向后移植。
     </para>
    </listitem>

    <listitem>
     <para>
      修复长期存在的 <command>LISTEN</command>/<command>NOTIFY</command> 竞态条件（Tom）
     </para>

     <para>
      极少数情况下，刚执行过 <command>LISTEN</command> 的会话可能收不到通知，即使按理应当收到，因为观察到执行 <command>NOTIFY</command> 的并发事务稍后提交了。
     </para>

     <para>
      此修复的一个副作用是：执行了尚未提交的 <command>LISTEN</command> 命令的事务如果去查看，将看不到该 <command>LISTEN</command> 在 <structname>pg_listener</structname> 中的行；以前能看到。这一行为从未被明确记录，但有些应用程序可能依赖旧行为。
     </para>
    </listitem>

    <listitem>
     <para>
      不允许在预备事务内使用 <command>LISTEN</command> 和 <command>UNLISTEN</command>（Tom）
     </para>

     <para>
      这以前是允许的，但尝试这样做会有各种不愉快的后果，特别是只要 <command>UNLISTEN</command> 未提交，发起的后端就无法退出。
     </para>
    </listitem>

    <listitem>
     <para>
      修复使用哈希索引的查询期间发生错误时的罕见崩溃（Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      修复公元前年份 2 月 29 日日期时间值的输入（Tom）
     </para>

     <para>
      以前的编码弄错了哪些年份是闰年。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <command>ALTER OWNER</command> 某些变体中的<quote>无法识别的节点类型</quote>错误（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_ctl</application>，使其从命令行选项正确提取 postmaster 的端口号（Itagaki Takahiro、Tom）
     </para>

     <para>
      此前 <literal>pg_ctl start -w</literal> 可能试图在错误端口上联系 postmaster，导致虚假的启动失败报告。
     </para>
    </listitem>

    <listitem>
     <para>
      使用 <option>-fwrapv</option> 防御最新 <application>gcc</application> 版本可能的错误优化（Tom）
     </para>

     <para>
      已知用 <application>gcc</application> 4.3 或更高版本构建 <productname>PostgreSQL</productname> 时这是必需的。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>ORDER BY</literal> 和 <literal>GROUP BY</literal> 中常量表达式的显示（Tom）
     </para>

     <para>
      显式转换的常量会被错误显示。这例如可能导致视图定义在转储和重载期间损坏。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>libpq</application>，使其在 COPY OUT 期间正确处理 NOTICE 消息（Tom）
     </para>

     <para>
      只观察到此失败在用户定义数据类型的输出例程发出 NOTICE 时发生，但不能保证它不会因其他原因发生。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-1-11">
  <title>版本 8.1.11</title>

  <note>
  <title>发行日期</title>
  <simpara>2008-01-07</simpara>
 </note>

  <para>
      此发行版包含 8.1.10 之后的多种修复，包括对重要安全问题的修复。有关 8.1 主版本新特性的信息，请参阅<xref linkend="release-8-1">。
     </para>

  <para>
      这是 <productname>PostgreSQL</productname> 社区为 <productname>Windows</productname> 制作二进制包的最后一个 8.1.X 版本。建议 Windows 用户迁移到 8.2.X 或更高版本，因为 8.2.X 中有一些不便于向后移植的 Windows 特定修复。8.1.X 在其他平台上将继续受支持。
     </para>

  <sect2>
   <title>迁移到版本 8.1.11</title>

   <para>
      对于运行 8.1.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.1.2 的版本升级，请参阅<xref linkend="release-8-1-2">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      防止索引中的函数以运行 <command>VACUUM</command>、<command>ANALYZE</command> 等的用户的权限执行（Tom）
     </para>

     <para>
      每当产生新的表条目时，都会求值索引表达式和部分索引谓词中使用的函数。人们早就明白，如果修改不可信用户拥有的表，这会带来特洛伊木马代码执行的风险。（注意触发器、默认值、检查约束等也构成同类风险。）但索引中的函数带来额外危险，因为它们会由例程维护操作（如 <command>VACUUM FULL</command>）执行，而这些操作通常在超级用户账户下自动执行。例如，恶意用户可以通过设置特洛伊木马索引定义并等待下一次例程清理来以超级用户权限执行代码。修复安排标准维护操作（包括 <command>VACUUM</command>、<command>ANALYZE</command>、<command>REINDEX</command> 和 <command>CLUSTER</command>）以表所有者而非调用用户的身份执行，使用已用于 <literal>SECURITY DEFINER</literal> 函数的同一权限切换机制。为防止绕过此安全措施，现在禁止在 <literal>SECURITY DEFINER</literal> 上下文中执行 <command>SET SESSION AUTHORIZATION</command> 和 <command>SET ROLE</command>。（CVE-2007-6600）
     </para>
    </listitem>

    <listitem>
     <para>
      修复正则表达式包中的若干缺陷（Tom、Will Drewry）
     </para>

     <para>
      精心构造的正则表达式模式可能导致崩溃、无限或接近无限的循环和/或大量内存消耗，这些都对接受来自不可信来源的正则搜索模式的应用程序构成拒绝服务风险。（CVE-2007-4769、CVE-2007-4772、CVE-2007-6067）
     </para>
    </listitem>

    <listitem>
     <para>
      作为安全措施，要求使用 <filename>/contrib/dblink</filename> 的非超级用户只使用口令认证（Joe）
     </para>

     <para>
      8.1.10 中出现的修复并不完整，它只堵住了部分 <filename>dblink</filename> 函数的漏洞。（CVE-2007-6601、CVE-2007-3278）
     </para>
    </listitem>

    <listitem>
     <para>
      更新时区数据文件到 <application>tzdata</application> 发行版 2007k（特别是阿根廷最近的变更）（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      改进规划器在非 C 区域设置下对 LIKE/正则表达式估计的处理（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复某些 <literal>WHERE false AND var IN (SELECT ...)</literal> 情形下的规划器失败（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      保留被 <command>ALTER TABLE ... ALTER COLUMN TYPE</command> 重建的索引的表空间（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      让归档恢复总是开始新的 WAL 时间线，而不是只在使用恢复停止时间时（Simon）
     </para>

     <para>
      这避免了试图覆盖最后一个 WAL 段现有归档副本的极端情形风险，而且看起来比原来的定义更简单干净。
     </para>
    </listitem>

    <listitem>
     <para>
      当表太小而不适用时，让 <command>VACUUM</command> 不使用全部 <varname>maintenance_work_mem</varname>（Alvaro）
     </para>
    </listitem>

    <listitem>
     <para>
      修复使用多字节数据库编码时 <function>translate()</function> 可能的崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复超过 68 年的 interval 在 <literal>extract(epoch from interval)</literal> 中的溢出（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PL/Perl 在可信函数中使用 UTF-8 正则表达式时不失败（Andrew）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PL/Perl 在平台 Perl 把 <literal>bool</literal> 类型定义为 <literal>int</literal> 而非 <literal>char</literal> 时的问题（Tom）
     </para>

     <para>
      虽然理论上这可能在任何地方发生，但没有标准 Perl 构建这样做……直到 <productname>Mac OS X</productname> 10.5。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PL/Python 在长异常消息上不崩溃（Alvaro）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_dump</application>，使其正确处理默认表达式与父表不同的继承子表（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <varname>PGPASSFILE</varname> 指向非普通文件时 <application>libpq</application> 的崩溃（Martin Pitt）
     </para>
    </listitem>

    <listitem>
     <para>
      <application>ecpg</application> 解析器修复（Michael）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <filename>contrib/pgcrypto</filename> 防御在超过 128 位密钥上失败的 <application>OpenSSL</application> 库——至少某些 Solaris 版本是这种情形（Marko Kreen）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <filename>contrib/tablefunc</filename> 的 <function>crosstab()</function> 把 NULL rowid 本身作为一个类别处理，而不是崩溃（Joe）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <type>tsvector</type> 和 <type>tsquery</type> 输出例程以正确转义反斜杠（Teodor、Bruce）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>to_tsvector()</function> 在超长输入字符串上的崩溃（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      要求在重新生成 <command>configure</command> 脚本时使用特定版本的 <productname>Autoconf</productname>（Peter）
     </para>

     <para>
      这只影响开发者和打包者。此变更是为了防止意外使用未经测试的 <productname>Autoconf</productname> 与 <productname>PostgreSQL</productname> 版本组合。如果确实想使用不同的 <productname>Autoconf</productname> 版本，可以移除版本检查，但结果是否可用由你自己负责。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-1-10">
  <title>版本 8.1.10</title>

  <note>
  <title>发行日期</title>
  <simpara>2007-09-17</simpara>
 </note>

  <para>
   本次发布包含对 8.1.9 的多方面修复。有关 8.1 大版本中新特性的信息，请参见<xref linkend="release-8-1">。
  </para>

  <sect2>
   <title>迁移到版本 8.1.10</title>

   <para>
      对于运行 8.1.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.1.2 的版本升级，请参阅<xref linkend="release-8-1-2">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      防止事务插入行后在同一表上并发 <command>VACUUM</command> 接近结束时中止导致的索引损坏（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <command>CREATE DOMAIN ... DEFAULT NULL</command> 正确工作（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      允许 <type>interval</type> 数据类型接受仅由毫秒或微秒组成的输入（Neil）
     </para>
    </listitem>

    <listitem>
     <para>
      加快 rtree 索引插入（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <acronym>SSL</acronym> 错误消息的过度记录（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复日志，使使用 syslogger 进程时日志消息永不交错（Andrew）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <varname>log_min_error_statement</varname> 日志记录内存耗尽时的崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复某些外键极端情形的错误处理（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      防止 <command>REINDEX</command> 和 <command>CLUSTER</command> 因试图处理其他会话的临时表而失败（Alvaro）
     </para>
    </listitem>

    <listitem>
     <para>
      更新时区数据库规则，特别是新西兰即将到来的变更（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      Windows 套接字改进（Magnus）
     </para>
    </listitem>

    <listitem>
     <para>
      因可能的编码不匹配，在 Windows 上抑制日志时间戳中的时区名（<literal>%Z</literal>）（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      作为安全措施，要求使用 <filename>/contrib/dblink</filename> 的非超级用户只使用口令认证（Joe）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-1-9">
  <title>版本 8.1.9</title>

  <note>
  <title>发行日期</title>
  <simpara>2007-04-23</simpara>
 </note>

  <para>
      此发行版包含 8.1.8 之后的多种修复，包括一个安全修复。有关 8.1 主版本新特性的信息，请参阅<xref linkend="release-8-1">。
     </para>

  <sect2>
   <title>迁移到版本 8.1.9</title>

   <para>
      对于运行 8.1.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.1.2 的版本升级，请参阅<xref linkend="release-8-1-2">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
    <para>
      支持在 <varname>search_path</varname> 中显式放置临时表模式，并禁用为函数和操作符搜索它（Tom）
     </para>
    <para>
      这是允许安全定义者函数设置真正安全的 <varname>search_path</varname> 值所必需的。没有它，非特权 SQL 用户可以利用临时对象以安全定义者函数的权限执行代码（CVE-2007-2138）。更多信息请参阅 <command>CREATE FUNCTION</command>。
     </para>
    </listitem>

    <listitem>
    <para>
      <filename>/contrib/tsearch2</filename> 崩溃修复（Teodor）
     </para>
    </listitem>

    <listitem>
    <para>
      要求 <command>COMMIT PREPARED</command> 在事务预备时所在的同一数据库中执行（Heikki）
     </para>
    </listitem>

    <listitem>
    <para>
      修复 <command>VACUUM FULL</command> 处理 <command>UPDATE</command> 链方式中的潜在数据损坏缺陷（Tom、Pavan Deolasee）
     </para>
    </listitem>

    <listitem>
    <para>
      规划器修复，包括改进外连接和位图扫描选择逻辑（Tom）
     </para>
    </listitem>

    <listitem>
    <para>
      修复哈希索引扩容期间的 PANIC（8.1.6 中引入的缺陷）（Tom）
     </para>
    </listitem>

    <listitem>
    <para>
      修复 POSIX 风格时区规范以遵循新的美国 DST 规则（Tom）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-1-8">
  <title>版本 8.1.8</title>

  <note>
  <title>发行日期</title>
  <simpara>2007-02-07</simpara>
 </note>

  <para>
      此发行版包含 8.1.7 之后的一个修复。有关 8.1 主版本新特性的信息，请参阅<xref linkend="release-8-1">。
     </para>

  <sect2>
   <title>迁移到版本 8.1.8</title>

   <para>
      对于运行 8.1.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.1.2 的版本升级，请参阅<xref linkend="release-8-1-2">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
    <para>
      移除约束和函数索引中对类型长度的过度严格检查（Tom）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-1-7">
  <title>版本 8.1.7</title>

  <note>
  <title>发行日期</title>
  <simpara>2007-02-05</simpara>
 </note>

  <para>
      此发行版包含 8.1.6 之后的多种修复，包括一个安全修复。有关 8.1 主版本新特性的信息，请参阅<xref linkend="release-8-1">。
     </para>

  <sect2>
   <title>迁移到版本 8.1.7</title>

   <para>
      对于运行 8.1.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.1.2 的版本升级，请参阅<xref linkend="release-8-1-2">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
    <para>
      移除允许已连接用户读取后端内存的安全漏洞（Tom）
     </para>
    <para>
      这些漏洞涉及抑制 SQL 函数返回其声明数据类型的常规检查，以及更改表列的数据类型（CVE-2007-0555、CVE-2007-0556）。这些错误很容易被利用来导致后端崩溃，而且原则上可能被用来读取用户不应能访问的数据库内容。
     </para>
    </listitem>

    <listitem>
    <para>
      修复罕见的缺陷：btree 索引页分裂可能因选择不可行的分裂点而失败（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
    <para>
      改进有大量表的数据库的 <command>VACUUM</command> 性能（Tom）
     </para>
    </listitem>

    <listitem>
    <para>
      修复 autovacuum，避免在不可连接的数据库中留下非持久事务 ID（Alvaro）
     </para>

    <para>
      此缺陷只影响 8.1 分支。
     </para>
    </listitem>

    <listitem>
    <para>
      修复由 <literal>UNION</literal> 触发的罕见 Assert() 崩溃（Tom）
     </para>
    </listitem>

    <listitem>
    <para>
      收紧对超过三字节的 UTF8 序列的多字节字符处理的安全性（Tom）
     </para>
    </listitem>

    <listitem>
    <para>
      修复 Windows 上因试图 fsync 已删除文件而发生的虚假<quote>权限被拒绝</quote>失败（Magnus、Tom）
     </para>
    </listitem>

    <listitem>
    <para>
      修复已在使用中的 PL/pgSQL 函数被更新时可能的崩溃（Tom）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-1-6">
  <title>版本 8.1.6</title>

  <note>
  <title>发行日期</title>
  <simpara>2007-01-08</simpara>
 </note>

  <para>
   本次发布包含对 8.1.5 的多方面修复。有关 8.1 大版本中新特性的信息，请参见<xref linkend="release-8-1">。
  </para>

  <sect2>
   <title>迁移到版本 8.1.6</title>

   <para>
      对于运行 8.1.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.1.2 的版本升级，请参阅<xref linkend="release-8-1-2">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      改进 AIX 上 <function>getaddrinfo()</function> 的处理（Tom）
     </para>

     <para>
      这修复了启动统计收集器等问题。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_restore</application> 处理包含带注释的大对象（blob）的 tar 格式备份（Tom）
     </para>
    </listitem>

     <listitem>
      <para>
      修复 <command>VACUUM</command> 中的<quote>未能重新找到父键</quote>错误（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      在服务器重启期间清除 <filename>pg_internal.init</filename> 缓存文件（Simon）
     </para>

      <para>
      这避免了缓存文件在 PITR 恢复后可能包含陈旧数据的风险。
     </para>
     </listitem>

     <listitem>
      <para>
      修复 <command>VACUUM</command> 截断跨越 GB 边界的大关系时的竞态条件（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      修复导致行级锁上不必要死锁错误的缺陷（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      修复影响多 GB 哈希索引的缺陷（Tom）
     </para>
     </listitem>

    <listitem>
     <para>
      修复 Windows 信号处理中可能的死锁（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      修复构造由多个空元素组成的 <literal>ARRAY[]</literal> 时的错误（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 ecpg 连接期间的内存泄漏（Michael）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 Darwin（OS X）编译（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      对于新的 <application>initdb</application> 安装，<function>to_number()</function> 和 <function>to_char(numeric)</function> 现在是 <literal>STABLE</literal> 而不是 <literal>IMMUTABLE</literal>（Tom）
     </para>

     <para>
      这是因为 <varname>lc_numeric</varname> 可能改变这些函数的输出。
     </para>
    </listitem>

    <listitem>
     <para>
      改进使用括号的正则表达式的索引利用（Tom）
     </para>

     <para>
      这同时也改进了 <application>psql</application> 的 <literal>\d</literal> 性能。
     </para>
    </listitem>

    <listitem>
     <para>
      更新时区数据库
     </para>

     <para>
      这特别影响澳大利亚和加拿大的夏令时规则。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-1-5">
  <title>版本 8.1.5</title>

  <note>
  <title>发行日期</title>
  <simpara>2006-10-16</simpara>
 </note>

  <para>
   本次发布包含对 8.1.4 的多方面修复。有关 8.1 大版本中新特性的信息，请参见<xref linkend="release-8-1">。
  </para>

  <sect2>
   <title>迁移到版本 8.1.5</title>

   <para>
      对于运行 8.1.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.1.2 的版本升级，请参阅<xref linkend="release-8-1-2">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

<itemizedlist>
<listitem><para>
      不允许在 <command>UPDATE</command> 命令中使用聚合函数，子 SELECT 中除外（Tom）
     </para>
<para>
      这种聚合的行为不可预测，而且在 8.1.X 中可能导致崩溃，因此已被禁用。SQL 标准也不允许这样做。
     </para></listitem>
<listitem><para>
      修复把无类型字面值当作 ANYARRAY 时的核心转储
     </para></listitem>
<listitem><para>
      修复扩展查询协议中执行 <command>COMMIT</command> 或 <command>ROLLBACK</command> 时持续时间日志记录的核心转储
     </para></listitem>
<listitem><para>
      修复查询包含返回多行的 SQL 函数时 AFTER 触发器的错误处理（Tom）
     </para></listitem>
<listitem><para>
      修复 <command>ALTER TABLE ... TYPE</>，对 <literal>USING</literal> 子句重新检查 <literal>NOT NULL</literal>（Tom）
     </para></listitem>
<listitem><para>
      修复 <function>string_to_array()</function>，处理分隔符字符串的重叠匹配
     </para>
<para>
      例如 <literal>string_to_array('123xx456xxx789', 'xx')</literal>。
     </para></listitem>
<listitem><para>
      修复 <literal>AM</literal>/<literal>PM</literal> 格式的 <function>to_timestamp()</function>（Bruce）
     </para></listitem>
<listitem><para>
      修复 autovacuum 决定是否需要 <command>ANALYZE</command> 的计算（Alvaro）
     </para></listitem>
<listitem><para>
      修复 <application>psql</application> 的 <literal>\d</literal> 命令模式匹配的极端情形
     </para></listitem>
<listitem><para>
      修复 /contrib/ltree 中损坏索引的缺陷（Teodor）
     </para></listitem>
<listitem><para>
      <application>ecpg</application> 的大量健壮性修复（Joachim Wieland）
     </para></listitem>
<listitem><para>
      修复 /contrib/dbmirror 中的反斜杠转义
     </para></listitem>
<listitem><para>
      /contrib/dblink 和 /contrib/tsearch2 的小修复
     </para>
</listitem>
<listitem><para>
      哈希表和位图索引扫描的效率改进（Tom）
     </para></listitem>
<listitem><para>
      修复 Windows 上统计收集的不稳定（Tom、Andrew）
     </para></listitem>
<listitem><para>
      修复 <varname>statement_timeout</varname> 在 Win32 上使用正确的单位（Bruce）
     </para>
<para>
      在以前的 Win32 8.1.X 版本中，延迟偏差了 100 倍。
     </para></listitem>
<listitem><para>
      <acronym>MSVC</acronym> 和 <productname>Borland C++</productname> 编译器的修复（Hiroshi Saito）
     </para></listitem>
<listitem><para>
      <systemitem class="osname">AIX</systemitem> 和 <productname>Intel</productname> 编译器的修复（Tom）
     </para></listitem>
<listitem><para>
      修复连续归档中的罕见缺陷（Tom）
     </para></listitem>
</itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-1-4">
  <title>版本 8.1.4</title>

  <note>
  <title>发行日期</title>
  <simpara>2006-05-23</simpara>
 </note>

  <para>
      此发行版包含 8.1.3 之后的多种修复，包括针对极其严重安全问题的补丁。有关 8.1 主版本新特性的信息，请参阅<xref linkend="release-8-1">。
     </para>

  <sect2>
   <title>迁移到版本 8.1.4</title>

   <para>
      对于运行 8.1.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.1.2 的版本升级，请参阅<xref linkend="release-8-1-2">。
     </para>

   <para>
      要完全防范 CVE-2006-2313 和 CVE-2006-2314 所述的 SQL 注入攻击，可能需要更改应用程序代码。如果你有把不可信字符串嵌入 SQL 命令的应用程序，应尽快检查它们，确保使用了推荐的转义技术。在大多数情况下，应用程序应使用库或驱动提供的子例程（如 <application>libpq</application> 的 <function>PQescapeStringConn()</function>）来执行字符串转义，而不是依靠<foreignphrase>临时</foreignphrase>代码来做。
     </para>
  </sect2>

  <sect2>
   <title>变更</title>

<itemizedlist>
<listitem><para>
      更改服务器，在所有情形下拒绝无效编码的多字节字符（Tatsuo、Tom）
     </para>
<para>
      虽然 <productname>PostgreSQL</productname> 向这个方向发展已有一段时间，但现在检查统一应用于所有编码和所有文本输入，而且总是错误而不只是警告。此变更可防范 CVE-2006-2313 所述类型的 SQL 注入攻击。
     </para></listitem>

<listitem><para>
      拒绝字符串字面量中对 <literal>\'</literal> 的不安全使用
     </para>
<para>
      作为针对 CVE-2006-2314 所述类型 SQL 注入攻击的服务器端防御，服务器现在只接受 <literal>''</literal> 而不接受 <literal>\'</literal> 作为 SQL 字符串字面量中 ASCII 单引号的表示。默认情况下，只有当 <varname>client_encoding</varname> 设为仅客户端编码（SJIS、BIG5、GBK、GB18030 或 UHC）时才拒绝 <literal>\'</literal>，这正是可能发生 SQL 注入的场景。需要时可用新的配置参数 <varname>backslash_quote</varname> 调整此行为。注意，要完全防范 CVE-2006-2314 可能需要客户端更改；<varname>backslash_quote</varname> 的目的之一是让不安全的客户端明显地不安全。
     </para></listitem>

<listitem><para>
      修改 <application>libpq</application> 的字符串转义例程，使其感知编码问题和 <varname>standard_conforming_strings</varname>
     </para>
<para>
      这为使用 <application>libpq</application> 的应用程序修复了 CVE-2006-2313 和 CVE-2006-2314 所述的安全问题，并使它们为将来转向 SQL 标准字符串字面量语法做好准备。并发使用多个 <productname>PostgreSQL</productname> 连接的应用程序应迁移到 <function>PQescapeStringConn()</function> 和 <function>PQescapeByteaConn()</function>，以确保为每个数据库连接中使用的设置正确转义。以<quote>手工</quote>方式做字符串转义的应用程序应改为依靠库例程。
     </para></listitem>

<listitem><para>
      修复 pgcrypto 中的弱密钥选择（Marko Kreen）
     </para>
<para>
      fortuna PRNG 重播种逻辑中的错误可能在某些情况下导致 <function>pgp_sym_encrypt()</function> 选择可预测的会话密钥。这只影响不使用 OpenSSL 的构建。
     </para></listitem>

<listitem><para>
      修复一些不正确的编码转换函数
     </para>
<para>
      <function>win1251_to_iso</function>、<function>win866_to_iso</function>、<function>euc_tw_to_big5</function>、<function>euc_tw_to_mic</function>、<function>mic_to_euc_tw</function> 都在不同程度上损坏。
     </para></listitem>

<listitem><para>
      清理字符串中偶然残留的 <literal>\'</literal> 使用（Bruce、Jan）
     </para></listitem>

<listitem><para>
      让 autovacuum 在 <structname>pg_stat_activity</structname> 中可见（Alvaro）
     </para></listitem>

<listitem><para>
      禁用 <literal>full_page_writes</literal>（Tom）
     </para>
<para>
      某些情况下，关闭 <literal>full_page_writes</literal> 会导致崩溃恢复失败。适当的修复将出现在 8.2 中；目前只是禁用它。
     </para></listitem>

<listitem><para>
      各种规划器修复，特别是位图索引扫描和 MIN/MAX 优化（Tom）
     </para></listitem>

<listitem><para>
      修复归并连接中的错误优化（Tom）
     </para>
<para>
      外连接有时可能发出未匹配行的多个副本。
     </para></listitem>

<listitem><para>
      修复在同一事务中使用并修改 plpgsql 函数导致的崩溃
     </para></listitem>

<listitem><para>
      修复 B-Tree 索引被截断情形的 WAL 重放
     </para></listitem>

<listitem><para>
      修复涉及 <literal>|</literal> 的模式的 <literal>SIMILAR TO</literal>（Tom）
     </para></listitem>

<listitem><para>
      修复 <command>SELECT INTO</command> 和 <command>CREATE TABLE AS</command>，在默认表空间而不是基目录中创建表（Kris Jurka）
     </para></listitem>

<listitem><para>
      修复服务器正确使用自定义 DH SSL 参数（Michael Fuhr）
     </para></listitem>

<listitem><para>
      改进 qsort 性能（Dann Corbit）
     </para>
<para>
      目前此代码只在 Solaris 上使用。
     </para></listitem>

<listitem><para>
      修复 x86 系统上的 OS/X Bonjour（Ashley Clark）
     </para></listitem>

<listitem><para>
      修复各种小的内存泄漏
     </para></listitem>

<listitem><para>
      修复某些 Win32 系统上的口令提示问题（Robert Kinberg）
     </para></listitem>

<listitem><para>
      改进 <application>pg_dump</application> 对域默认值的处理
     </para></listitem>

<listitem><para>
      修复 <application>pg_dumpall</application> 合理处理同名的用户和组（只在从 8.1 之前的服务器转储时可能）（Tom）
     </para>
<para>
      用户和组将被合并为带 <literal>LOGIN</literal> 权限的单个角色。此前合并后的角色没有 <literal>LOGIN</literal> 权限，使其不能作为用户使用。
     </para></listitem>

<listitem><para>
      修复 <application>pg_restore</application> 的 <literal>-n</literal>，使其按文档工作（Tom）
     </para></listitem>
</itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-1-3">
  <title>版本 8.1.3</title>

  <note>
  <title>发行日期</title>
  <simpara>2006-02-14</simpara>
 </note>

  <para>
      此发行版包含 8.1.2 之后的多种修复，包括一个非常严重的安全问题。有关 8.1 主版本新特性的信息，请参阅<xref linkend="release-8-1">。
     </para>

  <sect2>
   <title>迁移到版本 8.1.3</title>

   <para>
      对于运行 8.1.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.1.2 的版本升级，请参阅<xref linkend="release-8-1-2">。
     </para>
  </sect2>

  <sect2>
   <title>变更</title>

<itemizedlist>

<listitem><para>
      修复允许任何已登录用户把 <command>SET ROLE</command> 设为任何其他数据库用户 id 的缺陷（CVE-2006-0553）
     </para>
<para>
      由于有效性检查不充分，用户可以利用 <command>SET ROLE</command> 通常在错误后用来恢复先前角色设置的特殊情形。这例如允许普通用户获得超级用户状态。权限提升风险只存在于 8.1.0-8.1.2。但在直到 7.3 的所有版本中，<command>SET SESSION AUTHORIZATION</command> 都有一个相关缺陷，如果服务器编译时启用了断言（这不是默认），非特权用户可使服务器崩溃。感谢 Akio Ishida 报告此问题。
     </para></listitem>

<listitem><para>
      修复自插入行的行可见性逻辑缺陷（Tom）
     </para>
<para>
      极少数情况下，当前命令插入的行可能被看作已经有效，而不应该如此。修复 8.0.4、7.4.9 和 7.3.11 版本中引入的缺陷。
     </para></listitem>

<listitem><para>
      修复 pg_clog 和 pg_subtrans 文件创建期间可能导致<quote>文件已存在</quote>错误的竞态条件（Tom）
     </para></listitem>

<listitem><para>
      修复缓存失效消息恰好在错误时刻到达时可能导致崩溃的情形（Tom）
     </para></listitem>

<listitem><para>
      为预备语句中的 <literal>UNKNOWN</literal> 参数正确检查 <literal>DOMAIN</literal> 约束（Neil）
     </para></listitem>

<listitem><para>
      确保 <command>ALTER COLUMN TYPE</command> 按正确顺序处理 <literal>FOREIGN KEY</literal>、<literal>UNIQUE</literal> 和 <literal>PRIMARY KEY</literal> 约束（Nakano Yoshihisa）
     </para></listitem>

<listitem><para>
      修复以允许恢复有对自定义操作符或操作符类的跨模式引用的转储（Tom）
     </para></listitem>

<listitem><para>
      允许 <application>pg_restore</application> 在 <command>COPY</command> 失败后正确继续；此前它试图把剩余的 <command>COPY</command> 数据当作 SQL 命令（Stephen Frost）
     </para></listitem>

<listitem><para>
      修复未指定数据目录时 <application>pg_ctl</application> 的 <literal>unregister</literal> 崩溃（Magnus）
     </para></listitem>

<listitem><para>
      修复 <application>libpq</application> 的 <function>PQprint</function> HTML 标签（Christoph Zwerschke）
     </para></listitem>

<listitem><para>
      修复 AMD64 和 PPC 上 <application>ecpg</application> 的崩溃（Neil）
     </para></listitem>

<listitem><para>
      允许在函数结果类型声明中同时使用 <literal>SETOF</literal> 和 <literal>%TYPE</literal>
     </para></listitem>

<listitem><para>
      修复 <application>PL/python</application> 参数传递期间发生错误时的正确恢复（Neil）
     </para></listitem>

<listitem><para>
      修复 <function>plperl_return_next</function> 的内存泄漏（Neil）
     </para></listitem>

<listitem><para>
      修复 <application>PL/perl</application> 在 Win32 上对区域设置的处理以与后端匹配（Andrew）
     </para></listitem>

<listitem><para>
      各种优化器修复（Tom）
     </para></listitem>

<listitem><para>
      修复 Win32 上 <filename>postgresql.conf</filename> 中 <literal>log_min_messages</literal> 设为 <literal>DEBUG3</literal> 或更高时的崩溃（Bruce）
     </para></listitem>

<listitem><para>
      修复 Win32、Cygwin、OS X、AIX 的 <application>pgxs</application> <literal>-L</literal> 库路径指定（Bruce）
     </para></listitem>

<listitem><para>
      在检查 Win32 管理员权限时检查 SID 是否启用（Magnus）
     </para></listitem>

<listitem><para>
      正确拒绝超出范围的日期输入（Kris Jurka）
     </para></listitem>

<listitem><para>
      修复 configure 期间测试 <function>finite</function> 和 <function>isinf</function> 存在性的可移植性问题（Tom）
     </para></listitem>

<listitem><para>
      通过避免每数据行一次内核调用，改进经 libpq 的 <command>COPY IN</command> 速度（Alon Goldshuv）
     </para></listitem>

<listitem><para>
      改进 <filename>/contrib/tsearch2</filename> 索引创建的速度（Tom）
     </para></listitem>

</itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-1-2">
  <title>版本 8.1.2</title>

  <note>
  <title>发行日期</title>
  <simpara>2006-01-09</simpara>
 </note>

  <para>
   本次发布包含对 8.1.1 的多方面修复。有关 8.1 大版本中新特性的信息，请参见<xref linkend="release-8-1">。
  </para>

  <sect2>
   <title>迁移到版本 8.1.2</title>

   <para>
      对于运行 8.1.X 的用户，不需要进行转储/恢复。不过，如果你受到下述区域设置或 <application>plperl</application> 问题的影响，可能需要在更新后对文本列上的索引执行 <command>REINDEX</command>。
     </para>
  </sect2>

  <sect2>
   <title>变更</title>

<itemizedlist>

<listitem><para>
      修复 Windows 代码，使 ShmemBackendArray 没有更多空间时 postmaster 继续而不是退出（Magnus）
     </para>
<para>
      如果过多连接请求几乎同时到达，以前的行为可能导致拒绝服务情形。这<emphasis>只</emphasis>适用于 Windows 移植。
     </para></listitem>

<listitem><para>
      修复 8.0 中引入的缺陷，它可能使 ReadBuffer 返回已使用过的页当作新页，可能导致最近提交的数据丢失（Tom）
     </para></listitem>

<listitem><para>
      修复在事务之外或失败事务中发出协议级 Describe 消息的问题（Tom）
     </para></listitem>

<listitem><para>
      修复把不同字符组合视为相等的区域设置（如匈牙利语）下的字符串比较（Tom）
     </para>
<para>
      这可能需要 <command>REINDEX</command> 来修复文本列上的现有索引。
     </para></listitem>

<listitem><para>
      在 postmaster 启动期间设置区域设置环境变量，以确保 <application>plperl</application> 之后不会更改区域设置
     </para>
<para>
      这修复了 <application>postmaster</application> 启动时环境变量指定的区域设置与 <application>initdb</application> 所被告知的不同时发生的问题。在这些条件下，任何 <application>plperl</application> 的使用都可能导致索引损坏。如果你遇到过这种情况，可能需要 <command>REINDEX</command> 来修复文本列上的现有索引。
     </para></listitem>

<listitem><para>
      允许更灵活地重新定位安装目录（Tom）
     </para>
<para>
      以前的版本只有在所有安装目录路径除最后一个组件外都相同时才支持重新定位。
     </para></listitem>

<listitem><para>
      防止因使用 <literal>ISO-8859-5</literal> 和 <literal>ISO-8859-9</literal> 编码导致的崩溃（Tatsuo）
     </para></listitem>

<listitem><para>
      修复某些很少使用的亚洲多字节字符集中 strpos() 和正则表达式处理的长期缺陷（Tatsuo）
     </para></listitem>

<listitem><para>
      修复 COPY CSV 模式把任何 <literal>\.</literal> 视为复制数据结束的缺陷
     </para> <para>
      新代码要求 <literal>\.</literal> 单独出现在一行上，如文档所述。
     </para></listitem>

<listitem><para>
      让 COPY CSV 模式给字面数据值 <literal>\.</literal> 加引号，以确保它不会被解释为数据结束标记（Bruce）
     </para></listitem>

<listitem><para>
      对返回 <literal>RECORD</literal> 的函数的各种修复（Tom）
     </para></listitem>

<listitem><para>
      修复 <filename>postgresql.conf</filename> 的处理，使没有换行符的最后一行被正确处理（Tom）
     </para></listitem>

<listitem><para>
      修复 <filename>/contrib/pgcrypto</filename> gen_salt 中的缺陷，该缺陷使 MD5 和 XDES 算法不能使用全部可用盐空间（Marko Kreen、Solar Designer）
     </para>
<para>
      Blowfish 和标准 DES 的盐不受影响。
     </para></listitem>

<listitem><para>
      修复处理表达式索引时 autovacuum 的崩溃
     </para></listitem>

<listitem><para>
      修复 <filename>/contrib/dblink</filename>，当指定的列数与查询实际返回的列数不同时抛出错误而不是崩溃（Joe）
     </para></listitem>

</itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-1-1">
  <title>版本 8.1.1</title>

  <note>
  <title>发行日期</title>
  <simpara>2005-12-12</simpara>
 </note>

  <para>
   本次发布包含对 8.1.0 的多方面修复。有关 8.1 大版本中新特性的信息，请参见<xref linkend="release-8-1">。
  </para>

  <sect2>
   <title>迁移到版本 8.1.1</title>

   <para>
    对于运行 8.1.X 的用户，不需要进行转储/恢复。</para>
  </sect2>

  <sect2>
   <title>变更</title>

<itemizedlist>
<listitem><para>
      修复外连接条件的错误优化（Tom）
     </para></listitem>

<listitem><para>
      修复涉及被优化器平展的子查询的情形下报告列名错误的问题（Tom）
     </para></listitem>

<listitem><para>
      修复涉及 CHECK 约束、TOAST 列<emphasis>和</emphasis>索引的场景中的更新失败（Tom）
     </para></listitem>

<listitem><para>
      修复从错误恢复后 bgwriter 的问题（Tom）
     </para>
<para>
      发现后台写入器在写错误后泄漏缓冲区固定。虽然本身不致命，但这可能导致后续 VACUUM 命令的神秘阻塞。
     </para>
</listitem>

<listitem><para>
      防止客户端在当前事务已中止时发送 Bind 协议消息导致的失败
     </para></listitem>

<listitem><para>
      <filename>/contrib/tsearch2</filename> 和 <filename>/contrib/ltree</filename> 修复（Teodor）
     </para></listitem>

<listitem><para>
      修复需要单词重排的语言（如土耳其语）中翻译错误消息的问题；还有输出字符串意外截断和 bigint 最小可能值显示错误的问题（Andrew、Tom）
     </para>
<para>
      这些问题只出现在使用我们的 <filename>port/snprintf.c</filename> 代码的平台上，包括指定了 <literal>--enable-nls</literal> 的 BSD 变体，可能还有其他平台。此外，根据所使用的 <filename>libintl</filename> 版本，翻译错误消息问题的另一种形式可能出现在 Windows 上。
     </para></listitem>

<listitem><para>
      重新允许 <function>to_char(time)</function> 和 <function>to_char(interval)</function> 使用 <literal>AM</literal>/<literal>PM</literal>、<literal>HH</literal>、<literal>HH12</literal> 和 <literal>D</literal> 格式说明符。（<function>to_char(interval)</function> 或许应该用 <literal>HH24</literal>。）（Bruce）
     </para></listitem>

<listitem><para>
      AIX、HPUX 和 MSVC 编译修复（Tom、Hiroshi Saito）
     </para></listitem>

<listitem><para>
      优化器改进（Tom）
     </para></listitem>

<listitem><para>
      在 Windows NO_SYSTEM_RESOURCES 错误后重试文件读写（Qingqing Zhou）
     </para></listitem>

<listitem><para>
      防止 <application>autovacuum</application> 在分析表达式索引期间崩溃（Alvaro）
     </para></listitem>

<listitem><para>
      修复 ON COMMIT DELETE ROWS 临时表的问题
     </para></listitem>

<listitem><para>
      修复触发器更改 SELECT DISTINCT 查询输出时的问题
     </para></listitem>

<listitem><para>
      新增关于如何迁移无效 <literal>UTF-8</literal> 字节序列的 8.1.0 发行说明条目（Paul Lindner）
     </para></listitem>
</itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-1">
  <title>版本 8.1</title>

  <note>
  <title>发行日期</title>
  <simpara>2005-11-08</simpara>
 </note>

  <sect2>
   <title>概述</title>

   <para>
      此发行版的主要变更：
     </para>

   <variablelist>

    <varlistentry>
     <term>
      改进对共享缓冲区缓存的并发访问（Tom）
     </term>

     <listitem>
      <para>
      对共享缓冲区缓存的访问被认定为显著的可伸缩性问题，特别是在多 CPU 系统上。在此版本中，缓冲区管理器中加锁的方式被彻底改造，以减少锁争用并改进可伸缩性。缓冲区管理器还被改为使用<quote>时钟扫描</quote>替换策略。
     </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>
      允许索引扫描使用中间内存位图（Tom）
     </term>

     <listitem>
      <para>
      在以前的版本中，对表做查找只能使用单个索引。有了此特性，如果查询有 <command>WHERE tab.col1 = 4 and tab.col2 = 9</command>，而 <literal>col1</literal> 和 <literal>col2</literal> 上没有多列索引，但 <literal>col1</literal> 上有一个索引、<literal>col2</literal> 上有另一个索引，则可以搜索两个索引并在内存中合并结果，然后只对同时匹配 <literal>col1</literal> 和 <literal>col2</literal> 限制的行做堆获取。这在有大量非结构化查询、不可能创建匹配所有可能访问条件的索引的环境中非常有用。位图扫描即使对单个索引也有用，因为它减少了所需的随机访问量；位图索引扫描对于检索整表中相当大的部分是高效的，而普通索引扫描则不是。
     </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>
      新增两阶段提交（Heikki Linnakangas、Alvaro、Tom）
     </term>

     <listitem>
      <para>
      两阶段提交允许在多台计算机上"预备"事务，一旦所有计算机都成功预备了各自的事务（无一失败），所有事务就可以提交。即使机器在预备后崩溃，预备的事务也可以在机器重启后提交。新语法包括 <command>PREPARE TRANSACTION</command> 和 <command>COMMIT/ROLLBACK PREPARED</command>。还新增了系统视图 <literal>pg_prepared_xacts</literal>。
     </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>
      创建取代用户和组的新角色系统（Stephen Frost）
     </term>

     <listitem>
      <para>
      角色是用户和组的组合。像用户一样，它们可以有登录能力；像组一样，角色可以拥有其他角色作为成员。角色基本上消除了用户和组的区别。例如，角色可以：
     </para>

      <itemizedlist>

       <listitem>
        <para>
      （可选地）拥有登录能力
     </para>
       </listitem>

       <listitem>
        <para>
      拥有对象
     </para>
       </listitem>

       <listitem>
        <para>
      持有数据库对象的访问权限
     </para>
       </listitem>

       <listitem>
        <para>
      从它是成员的其他角色继承权限
     </para>
       </listitem>

      </itemizedlist>
      <para>
      用户登录到角色后，就获得该登录角色加上任何继承角色的能力，并可以用 <command>SET ROLE</command> 切换到她是成员的其他角色。此特性是 SQL 标准角色概念的推广。此变更还用支持角色的新目录 <structname>pg_authid</structname> 和 <structname>pg_auth_members</structname> 取代了 <structname>pg_shadow</structname> 和 <structname>pg_group</structname>。旧表被重新定义为新角色表上的只读视图。
     </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>
      自动为 <function>MIN()</function> 和 <function>MAX()</function> 使用索引（Tom）
     </term>

     <listitem>
      <para>
      在以前的版本中，让 <function>MIN()</function> 或 <function>MAX()</function> 使用索引的唯一方法是把查询改写为 <command>SELECT col FROM tab ORDER BY col LIMIT 1</command>。现在索引的使用自动发生。
     </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>
      把 <filename>/contrib/pg_autovacuum</filename> 移入主服务器（Alvaro）
     </term>

     <listitem>
      <para>
      把 autovacuum 集成到服务器中使其可以随数据库服务器同步自动启动和停止，并允许从 <filename>postgresql.conf</filename> 配置 autovacuum。
     </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>
      使用 <command>SELECT ... FOR SHARE</command> 增加共享行级锁（Alvaro）
     </term>

     <listitem>
      <para>
      虽然 <productname>PostgreSQL</productname> 的 MVCC 锁定使 <command>SELECT</command> 从不被写入者阻塞，因此典型操作不需要共享行锁，但共享锁对需要共享行锁定的应用程序很有用。特别是这降低了参照完整性检查施加的锁定要求。
     </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>
      增加对共享对象（特别是角色）的依赖（Alvaro）
     </term>

     <listitem>
      <para>
      依赖机制的这一扩展防止角色在其仍拥有数据库对象时被删除。此前可能通过删除所有者而意外地使对象<quote>孤立</quote>。虽然这可以恢复，但既混乱又令人不快。
     </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>
      改进分区表的性能（Simon）
     </term>

     <listitem>
      <para>
      新的 <varname>constraint_exclusion</varname> 配置参数避免在约束表明子表中不存在匹配行时查找子表。
     </para>
      <para>
      这支持一种基本的表分区。如果子表存储独立的键范围并用适当的 <command>CHECK</command> 约束强制执行，当约束保证子表中不存在匹配行时，优化器将跳过对子表的访问。
     </para>
     </listitem>
    </varlistentry>

   </variablelist>
  </sect2>

  <sect2>
   <title>迁移到版本 8.1</title>

   <para>
      希望从任何以前的版本迁移数据的用户，需要使用 <application>pg_dump</application> 进行转储/恢复。
     </para>

   <para>
      8.0 发行版宣布 interval 的 <function>to_char()</function> 函数将在 8.1 中移除。但由于没有提出更好的 API，<function>to_char(interval)</function> 在 8.1 中得到增强并将保留在服务器中。
     </para>

   <para>
      请注意以下不兼容之处：
     </para>

   <itemizedlist>

    <listitem>
     <para>
      <varname>add_missing_from</varname> 现在默认为 false（Neil）
     </para>
     <para>
      默认情况下，如果查询中使用的表没有 <literal>FROM</literal> 引用，我们现在会产生错误。旧行为仍可用，但必须把参数设为 'true' 才能获得。
     </para>

     <para>
      如果转储包含用隐式 <literal>FROM</literal> 语法创建的视图或规则，可能需要把 <varname>add_missing_from</varname> 设为 true 才能装载现有转储文件。这应当只是一次性的麻烦，因为 <productname>PostgreSQL</productname> 8.1 会把这样的视图和规则转换为标准的显式 <literal>FROM</literal> 语法。因此后续转储不会有此问题。
     </para>
    </listitem>

    <listitem>
     <para>
      使 <type>float4</type>/<type>float8</type>/<type>oid</type> 输入零长度字符串（<literal>''</literal>）抛出错误，而不是把它当作零（Neil）
     </para>
     <para>
      此变更与当前整数的零长度字符串处理一致。此变更的时间表在 8.0 中已宣布。
     </para>
    </listitem>

    <listitem>
     <para>
      <varname>default_with_oids</varname> 现在默认为 false（Neil）
     </para>
     <para>
      此选项设为 false 后，用户创建的表不再有 OID 列，除非在 <command>CREATE TABLE</command> 中指定 <command>WITH OIDS</command>。虽然 OID 在 <productname>PostgreSQL</productname> 的所有版本中都存在，但它们的用途有限，因为它们只有四字节长，而且计数器在所有已安装数据库之间共享。唯一标识行的首选方式是通过序列和 <type>SERIAL</type> 类型，它们自 <productname>PostgreSQL</productname> 6.4 起就受支持。
     </para>
    </listitem>

    <listitem>
     <para>
      新增 <literal>E''</literal> 语法，使最终普通字符串能按字面处理反斜杠（Bruce）
     </para>
     <para>
      目前 <productname>PostgreSQL</productname> 把字符串字面量中的反斜杠当作引入特殊转义序列（如 <literal>\n</literal> 或 <literal>\010</literal>）处理。虽然这便于输入特殊值，但它非标准且使从其他数据库移植应用程序更困难。因此，<productname>PostgreSQL</productname> 项目计划移除字符串中反斜杠的特殊含义。为了向后兼容和满足想要特殊反斜杠处理的用户，创建了新的字符串语法。新字符串语法的写法是在启动字符串的单引号前紧挨着写 <literal>E</literal>，例如 <literal>E'hi\n'</literal>。虽然此版本不改变字符串中反斜杠的处理，但它确实新增了帮助用户为未来版本迁移应用程序的配置参数：
     </para>
     <itemizedlist>

      <listitem>
       <para>
      <varname>standard_conforming_strings</varname>——此版本是否按字面处理普通字符串中的反斜杠？
     </para>
      </listitem>

      <listitem>
      <para>
      <varname>escape_string_warning</varname>——对普通（非 E）字符串中的反斜杠发出警告
     </para>
     </listitem>

     </itemizedlist>

     <para>
      <varname>standard_conforming_strings</varname> 的值是只读的。应用程序可以检索该值以了解反斜杠如何被处理。（参数的存在也可被视为支持 <literal>E''</literal> 字符串语法的标志。）在未来版本中，<varname>standard_conforming_strings</varname> 将为 true，意味着非 E 字符串中的反斜杠将按字面处理。为此变更做准备：在需要特殊反斜杠处理的地方使用 <literal>E''</literal> 字符串，并打开 <varname>escape_string_warning</varname> 以找出其他需要转换为使用 <literal>E''</literal> 的字符串。此外，用两个单引号（<literal>''</literal>）在字符串中嵌入字面单引号，而不要用 <productname>PostgreSQL</productname> 支持的反斜杠单引号（<literal>\'</literal>）语法。前者符合标准且不需要使用 <literal>E''</literal> 字符串语法。也可以使用 <literal>$$</literal> 字符串语法，它不特殊处理反斜杠。
     </para>
    </listitem>

    <listitem>
     <para>
      让 <command>REINDEX DATABASE</command> 重新索引数据库中的所有索引（Tom）
     </para>
     <para>
      此前 <command>REINDEX DATABASE</command> 只重新索引系统表。新行为看起来更直观。新命令 <command>REINDEX SYSTEM</command> 提供只重新索引系统表的旧功能。
     </para>
    </listitem>

    <listitem>
     <para>
      只读大对象描述符现在遵循 MVCC 快照语义
     </para>
     <para>
      当大对象以 <literal>INV_READ</literal>（而非 <literal>INV_WRITE</literal>）打开时，从描述符读取的数据现在将反映调用 <function>lo_open()</function> 的查询所用事务快照时刻大对象状态的<quote>快照</quote>。要获得总是返回最新已提交数据的旧行为，请在 <function>lo_open()</function> 的模式标志中包含 <literal>INV_WRITE</literal>。
     </para>
    </listitem>

    <listitem>
     <para>
      为序列函数的参数添加适当的依赖（Tom）
     </para>
     <para>
      在以前的版本中，传递给 <function>nextval()</function>、<function>currval()</function> 和 <function>setval()</function> 的序列名存储为简单文本字符串，这意味着重命名或删除 <literal>DEFAULT</literal> 子句中使用的序列会使子句无效。此版本把所有新创建的序列函数参数存储为内部 OID，使它们能跟踪序列重命名，并添加防止不当删除序列的依赖信息。它还使这样的 <literal>DEFAULT</literal> 子句不受模式重命名和搜索路径更改的影响。
     </para>
     <para>
      有些应用程序可能依赖旧的运行时查找序列名的行为。仍然可以通过把参数显式转换为 <type>text</type> 来做到，例如 <literal>nextval('myseq'::text)</literal>。
     </para>
     <para>
      装载到 8.1 的 8.1 之前数据库转储将使用旧的基于文本的表示，因此不会有 OID 存储参数的特性。但可以更新包含基于文本的 <literal>DEFAULT</literal> 子句的数据库。首先，把下面这个查询保存到文件中，例如 <filename>fixseq.sql</filename>：
<programlisting>
SELECT  'ALTER TABLE ' ||
   pg_catalog.quote_ident(n.nspname) || '.' ||
   pg_catalog.quote_ident(c.relname) ||
   ' ALTER COLUMN ' || pg_catalog.quote_ident(a.attname) ||
   ' SET DEFAULT ' ||
   regexp_replace(d.adsrc,
                  $$val\(\(('[^']*')::text\)::regclass$$,
                  $$val(\1$$,
                  'g') ||
   ';'
FROM    pg_namespace n, pg_class c, pg_attribute a, pg_attrdef d
WHERE   n.oid = c.relnamespace AND
   c.oid = a.attrelid AND
   a.attrelid = d.adrelid AND
   a.attnum = d.adnum AND
   d.adsrc ~ $$val\(\('[^']*'::text\)::regclass$$;
</programlisting>
      然后，对数据库运行该查询以找出需要哪些调整，例如对数据库 <literal>db1</literal>：
<programlisting>
psql -t -f fixseq.sql db1
</programlisting>
      这会显示把数据库转换为较新的基于 OID 的表示所需的 <command>ALTER TABLE</command> 命令。如果这些命令看起来合理，运行以下命令更新数据库：
<programlisting>
psql -t -f fixseq.sql db1 | psql -e db1
</programlisting>
      此过程必须在每个要更新的数据库中重复进行。
     </para>
    </listitem>

    <listitem>
     <para>
      在 <application>psql</application> 中，把未加引号的 <literal>\{digit}+</literal> 序列当作八进制（Bruce）
     </para>
     <para>
      在以前的版本中，<literal>\{digit}+</literal> 序列被当作十进制，只有 <literal>\0{digit}+</literal> 被当作八进制。此变更是为了保持一致。
     </para>
    </listitem>

    <listitem>
     <para>
      移除前缀和后缀 <literal>%</literal> 和 <literal>^</literal> 操作符的语法产生式（Tom）
     </para>
     <para>
      它们从未被记录，而且使取模操作符（<literal>%</literal>）与负数一起使用变得复杂。
     </para>
    </listitem>

    <listitem>
     <para>
      让多边形的 <literal>&amp;&lt;</literal> 和 <literal>&amp;&gt;</literal> 与 box 的"over"操作符一致（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      <command>CREATE LANGUAGE</command> 可以忽略提供的参数而采用 <structname>pg_pltemplate</structname> 中的信息（Tom）
     </para>
     <para>
      定义了新系统目录 <structname>pg_pltemplate</structname>，携带过程语言首选定义的信息（例如是否有验证器函数）。当该目录中存在正要创建的语言的条目时，<command>CREATE LANGUAGE</command> 将忽略除语言名之外的所有参数而改用目录信息。采取此措施是因为旧转储文件装载过时语言定义的问题日益增多。从 8.1 起，<application>pg_dump</application> 将把过程语言定义只转储为 <command>CREATE LANGUAGE <replaceable>name</replaceable></command>，依赖装载时存在模板条目。我们预期这将是更面向未来的表示。
     </para>
    </listitem>

    <listitem>
     <para>
      让 <function>pg_cancel_backend(int)</function> 返回 <type>boolean</type> 而不是 <type>integer</type>（Neil）
     </para>
    </listitem>

    <listitem>
     <para>
      一些用户在向 8.1.X 装载 UTF-8 数据时遇到问题。这是因为以前的版本允许把无效 UTF-8 字节序列输入数据库，而此版本只正确接受有效的 UTF-8 序列。纠正转储文件的一种方法是运行命令 <command>iconv -c -f UTF-8 -t UTF-8 -o cleanfile.sql dumpfile.sql</command>。<literal>-c</literal> 选项移除无效字符序列。两个文件的 diff 会显示无效的序列。<command>iconv</command> 把整个输入文件读入内存，因此可能需要用 <application>split</application> 把转储拆成多个较小的文件处理。
     </para>
    </listitem>

   </itemizedlist>
  </sect2>

  <sect2>
   <title>附加变更</title>

   <para>
      下面你将找到 <productname>PostgreSQL</productname> 8.1 与上一个主版本之间额外变更的详细说明。
     </para>

   <sect3>
    <title>性能改进</title>
    <itemizedlist>

     <listitem>
      <para>
      改进 GiST 和 R-tree 索引性能（Neil）
     </para>
     </listitem>

     <listitem>
      <para>
      改进优化器，包括哈希连接的自动调整大小（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      彻底改造若干领域的内部 API
     </para>
     </listitem>

     <listitem>
      <para>
      把 WAL 记录的 CRC 从 64 位改为 32 位（Tom）
     </para>
      <para>
      我们认定计算 64 位 CRC 的额外代价显著，而可靠性收益太小不值得。
     </para>
     </listitem>

     <listitem>
      <para>
      防止在 WAL 页中写入大的空隙（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      改进 SMP 机器（特别是 Opteron）上的自旋锁行为（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      允许多列索引使用非连续的索引列（Tom）
     </para>
      <para>
      例如，这允许 a,b,c 列上的索引用于 <command>WHERE a = 4 and c = 10</command> 的查询。
     </para>
     </listitem>

     <listitem>
      <para>
      跳过 <command>CREATE TABLE AS</command> / <command>SELECT INTO</command> 的 WAL 日志（Simon）
     </para>
      <para>
      由于 <command>CREATE TABLE AS</command> 期间的崩溃会导致表在恢复期间被删除，没有理由在装载表时写 WAL 日志。（但如果启用了 WAL 归档，仍会记录。）
     </para>
     </listitem>

     <listitem>
      <para>
      允许并发 GiST 索引访问（Teodor、Oleg）
     </para>
     </listitem>

     <listitem>
      <para>
      新增配置参数 <varname>full_page_writes</varname>，控制向 WAL 写整页（Bruce）
     </para>
      <para>
      为防止部分磁盘写损坏数据库，<productname>PostgreSQL</productname> 在检查点后首次修改每个数据库磁盘页时把它的完整副本写入 WAL。此选项为提高速度关闭该功能。在使用电池供电的磁盘缓存（不会发生部分页写）时可以安全使用。
     </para>
     </listitem>

     <listitem>
      <para>
      当 <varname>wal_sync_method</varname> 使用 <literal>O_SYNC</literal> 时，如果可用则使用 <literal>O_DIRECT</literal>（Itagaki Takahiro）
     </para>
      <para>
      <literal>O_DIRECT</literal> 使磁盘写绕过内核缓存，对 WAL 写入而言这改进了性能。
     </para>
     </listitem>

     <listitem>
      <para>
      改进 <command>COPY FROM</command> 的性能（Alon Goldshuv）
     </para>
      <para>
      这是通过以更大的块读取 <command>COPY</command> 输入而不是逐字符读取实现的。
     </para>
     </listitem>

     <listitem>
      <para>
      改进 <function>COUNT()</function>、<function>SUM</function>、<function>AVG()</function>、<function>STDDEV()</function> 和 <function>VARIANCE()</function> 的性能（Neil、Tom）
     </para>
     </listitem>
    </itemizedlist>
   </sect3>

   <sect3>
    <title>服务器变更</title>
    <itemizedlist>

     <listitem>
      <para>
      防止事务 ID（XID）回卷导致的问题（Tom）
     </para>
      <para>
      当事务计数器接近回卷点时，服务器现在会发出警告。如果计数器过于接近回卷，服务器将停止接受查询。这确保在执行所需清理之前数据不会丢失。
     </para>
     </listitem>

     <listitem>
      <para>
      修复 OID 计数器回卷后对象 ID（OID）与现有系统对象冲突的问题（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      新增关于 <command>VACUUM</command> 期间需要增大 <varname>max_fsm_relations</varname> 和 <varname>max_fsm_pages</varname> 的警告（Ron Mayer）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <varname>temp_buffers</varname> 配置参数，允许用户确定临时表访问的本地缓冲区大小（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      为 <literal>pg_stat_activity</literal> 新增会话开始时间和客户端 IP 地址（Magnus）
     </para>
     </listitem>

     <listitem>
      <para>
      为位图扫描调整 <literal>pg_stat</literal> 视图（Tom）
     </para>
      <para>
      某些字段的含义略有变化。
     </para>
     </listitem>

     <listitem>
      <para>
      增强 <literal>pg_locks</literal> 视图（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      记录客户端 <command>PREPARE</command> 和 <command>EXECUTE</command> 的查询（Simon）
     </para>
     </listitem>

     <listitem>
      <para>
      允许在 <filename>postgresql.conf</filename> 中指定 Kerberos 名和用户名的大小写敏感性（Magnus）
     </para>
     </listitem>

     <listitem>
      <para>
      新增配置参数 <varname>krb_server_hostname</varname>，使服务器主机名可以被指定为服务主体的一部分（Todd Kover）
     </para>
      <para>
      如果未设置，可以使用与 keytab 中条目匹配的任何服务主体。这是此版本新的 Kerberos 匹配行为。
     </para>
     </listitem>

     <listitem>
      <para>
      为 <varname>log_line_prefix</varname> 新增毫秒时间戳（<literal>%m</literal>）和远程主机（<literal>%h</literal>）选项（Ed L.）
     </para>
     </listitem>

     <listitem>
      <para>
      为 GiST 索引新增 WAL 日志（Teodor、Oleg）
     </para>
      <para>
      GiST 索引现在对崩溃和时间点恢复是安全的。
     </para>
     </listitem>

     <listitem>
      <para>
      执行 <function>pg_stop_backup()</function> 时移除旧的 <filename>*.backup</filename> 文件（Bruce）
     </para>
      <para>
      这防止 <filename>pg_xlog/</filename> 中存在大量 <filename>*.backup</filename> 文件。
     </para>
     </listitem>

     <listitem>
      <para>
      新增控制 TCP/IP keep-alive 空闲时间、间隔和次数的配置参数（Oliver Jowett）
     </para>

      <para>
      可以更改这些值以更快检测丢失的客户端连接。
     </para>
     </listitem>

     <listitem>
      <para>
      新增逐用户和逐数据库的连接限制（Petr Jelinek）
     </para>
      <para>
      使用 <command>ALTER USER</command> 和 <command>ALTER DATABASE</command>，现在可以对以特定用户身份或到特定数据库并发连接的最大会话数施加限制。把限制设为零禁用用户或数据库连接。
     </para>
     </listitem>

     <listitem>
      <para>
      允许 64 位机器上超过 2GB 的共享内存和逐后端工作内存（Koichi Suzuki）
     </para>
     </listitem>

     <listitem>
      <para>
      新系统目录 <structname>pg_pltemplate</structname> 允许覆盖转储文件中过时的过程语言定义（Tom）
     </para>
     </listitem>

    </itemizedlist>
   </sect3>


   <sect3>
    <title>查询变更</title>
    <itemizedlist>

     <listitem>
      <para>
      新增临时视图（Koju Iijima、Neil）
     </para>
     </listitem>

     <listitem>
      <para>
      修复没有任何聚合函数或 <command>GROUP BY</command> 的 <command>HAVING</command>，使查询返回单个组（Tom）
     </para>
      <para>
      此前这种情形会把 <command>HAVING</command> 子句当作 <command>WHERE</command> 子句。这不符合规范。
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <command>USING</command> 子句，允许为 <command>DELETE</command> 指定额外的表（Euler Taveira de Oliveira、Neil）
     </para>
      <para>
      在以前的版本中，没有明确的方法为 <command>DELETE</command> 语句指定用于连接的额外表。<command>UPDATE</command> 已有用于此目的的 <literal>FROM</literal> 子句。
     </para>
     </listitem>

     <listitem>
      <para>
      新增对后端和 ecpg 字符串中 <literal>\x</literal> 十六进制转义的支持（Bruce）
     </para>
      <para>
      这就像标准的 C <literal>\x</literal> 转义语法。八进制转义此前已支持。
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <command>BETWEEN SYMMETRIC</command> 查询语法（Pavel Stehule）
     </para>
      <para>
      此特性允许 <command>BETWEEN</command> 比较而不要求第一个值小于第二个值。例如 <command>2 BETWEEN [ASYMMETRIC] 3 AND 1</command> 返回假，而 <command>2 BETWEEN SYMMETRIC 3 AND 1</command> 返回真。<command>BETWEEN ASYMMETRIC</command> 此前已支持。
     </para>
     </listitem>

     <listitem>
      <para>
      为 <command>SELECT ... FOR UPDATE/SHARE</command> 新增 <command>NOWAIT</command> 选项（Hans-Juergen Schoenig）
     </para>
      <para>
      虽然 <varname>statement_timeout</varname> 配置参数允许取消耗时超过一定时间的查询，但 <command>NOWAIT</command> 选项允许在 <command>SELECT ... FOR UPDATE/SHARE</command> 命令不能立即获得行锁时立刻取消查询。
     </para>
     </listitem>
    </itemizedlist>
   </sect3>


   <sect3>
    <title>对象操作变更</title>
    <itemizedlist>

     <listitem>
      <para>
      跟踪共享对象的依赖（Alvaro）
     </para>
      <para>
      <productname>PostgreSQL</productname> 允许全局表（用户、数据库、表空间）引用多个数据库中的信息。此新增为全局表添加了依赖信息，因此例如用户所有权可以跨数据库跟踪，在任何数据库中拥有某些东西的用户不能再被删除。数据库本地对象的依赖跟踪此前已存在。
     </para>
     </listitem>

     <listitem>
      <para>
      允许对象所有者执行有限的 <command>ALTER OWNER</command> 命令（Stephen Frost）
     </para>
      <para>
      以前的版本只允许超级用户更改对象所有者。现在，如果执行命令的用户拥有该对象并能以新所有者身份创建它（即该用户是新所有角色的成员且该角色具有重新创建对象所需的 CREATE 权限），就可以转移所有权。
     </para>
     </listitem>

     <listitem>
      <para>
      为某些对象类型（表、函数、类型）新增 <command>ALTER</command> 对象 <command>SET SCHEMA</command> 能力（Bernd Helmle）
     </para>
      <para>
      这允许把对象移到不同的模式。
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <command>ALTER TABLE ENABLE/DISABLE TRIGGER</command> 来禁用触发器（Satoshi Nagayasu）
     </para>
     </listitem>

    </itemizedlist>
   </sect3>


   <sect3>
    <title>工具命令变更</title>
    <itemizedlist>

     <listitem>
      <para>
      允许 <command>TRUNCATE</command> 用单条命令截断多个表（Alvaro）
     </para>
      <para>
      由于参照完整性检查，不允许截断作为参照完整性约束一部分的表。使用此新功能，如果参照完整性约束涉及的两个表在同一条 <command>TRUNCATE</command> 命令中被截断，就可以用 <command>TRUNCATE</command> 截断这样的表。
     </para>
     </listitem>

     <listitem>
      <para>
      在 <command>COPY CSV</command> 模式中正确处理回车和换行（Andrew）
     </para>
      <para>
      在 8.0 版本中，<command>CSV COPY TO</command> 中的回车和换行的处理方式不一致。（这记录在 TODO 列表中。）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <command>COPY WITH CSV HEADER</command>，允许 <command>COPY</command> 的第一行为头行（Andrew）
     </para>
      <para>
      这允许处理把列名放在数据文件首行的常见 <command>CSV</command> 用法。对 <command>COPY TO</command>，首行包含列名；对 <command>COPY FROM</command>，首行被忽略。
     </para>
     </listitem>

     <listitem>
      <para>
      在 Windows 上，<command>EXPLAIN ANALYZE</command> 显示更好的亚秒精度（Magnus）
     </para>
     </listitem>

     <listitem>
      <para>
      为 <command>EXPLAIN ANALYZE</command> 新增触发器时长显示（Tom）
     </para>
      <para>
      以前的版本把触发器执行时间计入总执行时间，但没有单独显示。现在可以看到每个触发器花费多少时间。
     </para>
     </listitem>

     <listitem>
      <para>
      新增对 <command>COPY</command> 中 <literal>\x</literal> 十六进制转义的支持（Sergey Ten）
     </para>
      <para>
      以前的版本只支持八进制转义。
     </para>
     </listitem>

     <listitem>
      <para>
      让 <command>SHOW ALL</command> 包含变量描述（Matthias Schmidt）
     </para>
      <para>
      <command>SHOW</command> varname 仍然只显示变量的值，不包含描述。
     </para>
     </listitem>

     <listitem>
      <para>
      让 <application>initdb</application> 创建名为 <literal>postgres</literal> 的新标准数据库，并把工具转换为用 <literal>postgres</literal> 而不是 <literal>template1</literal> 做标准查找（Dave）
     </para>
      <para>
      在以前的版本中，<literal>template1</literal> 既被用作 <application>createuser</application> 等工具的默认连接，又被用作新数据库的模板。这导致 <command>CREATE DATABASE</command> 有时失败，因为如果其他人在模板数据库中就无法创建新数据库。有了此变更，默认连接数据库现在是 <literal>postgres</literal>，意味着 <command>CREATE DATABASE</command> 期间有人正在使用 <literal>template1</literal> 的可能性小得多。
     </para>
     </listitem>

     <listitem>
      <para>
      通过把 <filename>/contrib/reindexdb</filename> 移入服务器来创建新的 <application>reindexdb</application> 命令行工具（Euler Taveira de Oliveira）
     </para>
     </listitem>

    </itemizedlist>
   </sect3>


   <sect3>
    <title>数据类型和函数变更</title>
    <itemizedlist>

     <listitem>
      <para>
      为数组类型新增 <function>MAX()</function> 和 <function>MIN()</function> 聚合（Koju Iijima）
     </para>
     </listitem>

     <listitem>
      <para>
      修复 <function>to_date()</function> 和 <function>to_timestamp()</function> 在同时使用 <literal>CC</literal> 和 <literal>YY</literal> 字段时合理行事（Karel Zak）
     </para>
      <para>
      如果格式说明包含 <literal>CC</literal> 而年规格为 <literal>YYY</literal> 或更长，忽略 <literal>CC</literal>。如果年规格为 <literal>YY</literal> 或更短，把 <literal>CC</literal> 解释为上一个世纪。
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <function>md5(bytea)</function>（Abhijit Menon-Sen）
     </para>
      <para>
      <function>md5(text)</function> 此前已存在。
     </para>
     </listitem>

     <listitem>
      <para>
      新增对 <command>numeric ^ numeric</command> 的支持，基于 <function>power(numeric, numeric)</function>
     </para>
      <para>
      该函数此前已存在，但没有为它分配操作符。
     </para>
     </listitem>

     <listitem>
      <para>
      修复 <type>NUMERIC</type> 取模，在计算期间正确截断商（Bruce）
     </para>
      <para>
      在以前的版本中，大值的取模有时因商的四舍五入而返回负结果。
     </para>
     </listitem>

     <listitem>
      <para>
      新增函数 <function>lastval()</function>（Dennis Bj&ouml;rklund）
     </para>
      <para>
      <function>lastval()</function> 是 <function>currval()</function> 的简化版本。它根据当前会话最近执行的 <function>nextval()</function> 或 <function>setval()</function> 调用自动确定正确的序列名。
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <function>to_timestamp(DOUBLE PRECISION)（Michael Glaesemann）</function>
     </para>
      <para>
      把自 1970 年以来的 Unix 秒数转换为 <type>TIMESTAMP WITH TIMEZONE</type>。
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <function>pg_postmaster_start_time()</function> 函数（Euler Taveira de Oliveira、Matthias Schmidt）
     </para>
     </listitem>

     <listitem>
      <para>
      允许在 <command>AT TIME ZONE</command> 中完整使用时区名，而不只是以前可用的短列表（Magnus）
     </para>
      <para>
      此前 <command>AT TIME ZONE</command> 只支持预定义的时区名列表。现在可以使用任何受支持的时区名，例如：
<programlisting>
SELECT CURRENT_TIMESTAMP AT TIME ZONE 'Europe/London';
</programlisting>
      在上面的查询中，使用的时区根据所提供日期当时有效的夏令时规则调整。
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <function>GREATEST()</function> 和 <function>LEAST()</function> 可变参数函数（Pavel Stehule）
     </para>
      <para>
      这些函数接受可变数量的参数，返回参数中的最大或最小值。
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <function>pg_column_size()</function>（Mark Kirkwood）
     </para>
      <para>
      它返回列的存储大小，该列可能被压缩过。
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <function>regexp_replace()</function>（Atsushi Ogawa）
     </para>
      <para>
      这允许像 sed 那样做正则表达式替换。可选的标志参数允许选择全局（全部替换）和不区分大小写模式。
     </para>
     </listitem>

     <listitem>
      <para>
      修复 interval 的除法和乘法（Bruce）
     </para>
      <para>
      以前的版本有时返回没有道理的结果，例如 <command>'4 months'::interval / 5</command> 返回 <command>'1 mon -6 days'</command>。
     </para>
     </listitem>

     <listitem>
      <para>
      修复 timestamp、time 和 interval 输出的舍入行为（Tom）
     </para>
      <para>
      这修复了秒字段会显示为 <literal>60</literal> 而不是进位到更高字段的一些情形。
     </para>
     </listitem>

     <listitem>
      <para>
      为 <type>interval</type> 类型新增单独的天字段，使一天间隔可与 24 小时间隔区分（Michael Glaesemann）
     </para>
      <para>
      包含夏令时调整的天不是 24 小时长，而通常是 23 或 25 小时。此变更创建了<quote>多少天</quote>间隔与<quote>多少小时</quote>间隔之间的概念区别。向时间戳加 <literal>1 day</literal> 现在即使中间发生夏令时调整，也在第二天给出相同的本地时间，而加 <literal>24 hours</literal> 在这种情况下会给出不同的本地时间。例如，按美国 DST 规则：
<programlisting>
'2005-04-03 00:00:00-05' + '1 day' = '2005-04-04 00:00:00-04'
'2005-04-03 00:00:00-05' + '24 hours' = '2005-04-04 01:00:00-04'
</programlisting>
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <function>justify_days()</function> 和 <function>justify_hours()</function>（Michael Glaesemann）
     </para>
      <para>
      这两个函数分别把天调整为适当的整月数加天数，把小时调整为适当的整天数加小时数。
     </para>
     </listitem>

     <listitem>
      <para>
      把 <filename>/contrib/dbsize</filename> 移入后端，并重命名其中一些函数（Dave Page、Andreas Pflug）
     </para>
      <para>
      <itemizedlist> <listitem> <para> <function>pg_tablespace_size()</function>
     </para>
        </listitem>

        <listitem>
         <para>
      <function>pg_database_size()</function>
     </para>
        </listitem>

        <listitem>
         <para>
      <function>pg_relation_size()</function>
     </para>
        </listitem>

        <listitem>
         <para>
      <function>pg_total_relation_size()</function>
     </para>
        </listitem>

        <listitem>
         <para>
      <function>pg_size_pretty()</function>
     </para>
        </listitem>

       </itemizedlist>
      </para>
      <para>
      <function>pg_total_relation_size()</function> 包括索引和 TOAST 表。
     </para>
     </listitem>

     <listitem>
      <para>
      新增对集群目录只读文件访问的函数（Dave Page、Andreas Pflug）
     </para>
      <para>
      <itemizedlist> <listitem> <para> <function>pg_stat_file()</function>
     </para>
        </listitem>

        <listitem>
         <para>
      <function>pg_read_file()</function>
     </para>
        </listitem>

        <listitem>
         <para>
      <function>pg_ls_dir()</function>
     </para>
        </listitem>

       </itemizedlist>
      </para>
     </listitem>

     <listitem>
      <para>
      新增 <function>pg_reload_conf()</function>，强制重新装载配置文件（Dave Page、Andreas Pflug）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <function>pg_rotate_logfile()</function>，强制轮转服务器日志文件（Dave Page、Andreas Pflug）
     </para>
     </listitem>

     <listitem>
      <para>
      更改 <literal>pg_stat_*</literal> 视图以包含 TOAST 表（Tom）
     </para>
     </listitem>

    </itemizedlist>
   </sect3>


   <sect3>
    <title>编码和区域设置变更</title>
    <itemizedlist>

     <listitem>
      <para>
      重命名一些编码使其更一致并遵循国际标准（Bruce）
     </para>
      <para>
      <itemizedlist> <listitem> <para> <literal>UNICODE</literal> 现在是 <literal>UTF8</literal>
     </para>
        </listitem>

        <listitem>
         <para>
      <literal>ALT</literal> 现在是 <literal>WIN866</literal>
     </para>
        </listitem>

        <listitem>
         <para>
      <literal>WIN</literal> 现在是 <literal>WIN1251</literal>
     </para>
        </listitem>

        <listitem>
         <para>
      <literal>TCVN</literal> 现在是 <literal>WIN1258</literal>
     </para>
        </listitem>

       </itemizedlist>
      </para>

      <para>
      原名仍然有效。
     </para>
     </listitem>

     <listitem>
      <para>
      新增对 <literal>WIN1252</literal> 编码的支持（Roland Volkmann）
     </para>
     </listitem>

     <listitem>
      <para>
      新增对四字节 <literal>UTF8</literal> 字符的支持（John Hansen）
     </para>
      <para>
      此前只支持一、二、三字节的 <literal>UTF8</literal> 字符。这对支持某些中文字符集特别重要。
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <literal>EUC_JP</literal> 和 <literal>SJIS</literal> 之间的直接转换以改进性能（Atsushi Ogawa）
     </para>
     </listitem>

     <listitem>
      <para>
      允许 UTF8 编码在 Windows 上工作（Magnus）
     </para>
      <para>
      这是通过把 UTF8 映射到 Windows 本机 UTF16 实现完成的。
     </para>
     </listitem>

    </itemizedlist>
   </sect3>


   <sect3>
    <title>常规服务器端语言变更</title>
    <itemizedlist>

     <listitem>
      <para>
      修复 <command>ALTER LANGUAGE RENAME</command>（Sergey Yatskevich）
     </para>
     </listitem>

     <listitem>
      <para>
      允许通过 <command>ALTER FUNCTION</command> 修改函数特性，如严格性和易变性（Neil）
     </para>
     </listitem>

     <listitem>
      <para>
      把函数参数的最大数量提高到 100（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      允许 SQL 和 PL/pgSQL 函数使用 <command>OUT</command> 和 <command>INOUT</command> 参数（Tom）
     </para>
      <para>
      <command>OUT</command> 是函数返回值的另一种方式。除了使用 <command>RETURN</command>，还可以通过给声明为 <command>OUT</command> 或 <command>INOUT</command> 的参数赋值来返回值。这在某些情形下书写更简单，特别是需要返回多个值时。虽然以前的版本也可以从函数返回多个值，但这大幅简化了该过程。（该特性将在未来版本中扩展到其他服务器端语言。）
     </para>
     </listitem>

     <listitem>
      <para>
      把语言处理函数移入 <literal>pg_catalog</literal> 模式
     </para>
      <para>
      这使按需删除 public 模式变得更容易。
     </para>
     </listitem>

     <listitem>
      <para>
      为 SPI 新增 <function>SPI_getnspname()</function>（Neil）
     </para>
     </listitem>

    </itemizedlist>
   </sect3>

   <sect3>
    <title>PL/pgSQL 服务器端语言变更</title>
    <itemizedlist>

     <listitem>
      <para>
      彻底改造 PL/pgSQL 函数的内存管理（Neil）
     </para>
      <para>
      每个函数的分析树现在存储在单独的内存上下文中。这使该内存在不再需要时可以轻松回收。
     </para>
     </listitem>

     <listitem>
      <para>
      在 <command>CREATE FUNCTION</command> 时检查函数语法，而不是在运行时（Neil）
     </para>
      <para>
      此前，大多数语法错误只在函数执行时才被报告。
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <command>OPEN</command> 打开非 <command>SELECT</command> 查询，如 <command>EXPLAIN</command> 和 <command>SHOW</command>（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      不再要求函数发出 <command>RETURN</command> 语句（Tom）
     </para>
      <para>
      这是新增的 <command>OUT</command> 和 <command>INOUT</command> 功能的副产品。当不需要提供函数返回值时可以省略 <command>RETURN</command>。
     </para>
     </listitem>

     <listitem>
      <para>
      为 PL/pgSQL 的 <command>EXECUTE</command> 语句新增可选的 <command>INTO</command> 子句（Pavel Stehule、Neil）
     </para>
     </listitem>

     <listitem>
      <para>
      让 <command>CREATE TABLE AS</command> 设置 <command>ROW_COUNT</command>（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      定义 <literal>SQLSTATE</literal> 和 <literal>SQLERRM</literal>，返回当前异常的 <literal>SQLSTATE</literal> 和错误消息（Pavel Stehule、Neil）
     </para>
      <para>
      这些变量只在异常块内定义。
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <command>RAISE</command> 语句的参数为表达式（Pavel Stehule、Neil）
     </para>
     </listitem>

     <listitem>
      <para>
      新增循环 <command>CONTINUE</command> 语句（Pavel Stehule、Neil）
     </para>
     </listitem>

     <listitem>
      <para>
      允许块和循环标签（Pavel Stehule）
     </para>
     </listitem>

    </itemizedlist>
   </sect3>


   <sect3>
    <title>PL/Perl 服务器端语言变更</title>
    <itemizedlist>

     <listitem>
      <para>
      允许高效返回大结果集（Abhijit Menon-Sen）
     </para>
      <para>
      这允许函数使用 <function>return_next()</function> 来避免在内存中构建整个结果集。
     </para>
     </listitem>

     <listitem>
      <para>
      允许逐行获取查询结果（Abhijit Menon-Sen）
     </para>
      <para>
      这允许函数使用 <function>spi_query()</function> 和 <function>spi_fetchrow()</function> 来避免在内存中累积整个结果集。
     </para>
     </listitem>

     <listitem>
      <para>
      如果服务器编码为 <literal>UTF8</literal>，强制 PL/Perl 把字符串当作 <literal>UTF8</literal> 处理（David Kamholz）
     </para>
     </listitem>

     <listitem>
      <para>
      为 PL/Perl 新增验证器函数（Andrew）
     </para>
      <para>
      这使语法错误可以在定义时而不是执行时报告。
     </para>
     </listitem>

     <listitem>
      <para>
      当函数返回数组类型时允许 PL/Perl 返回 Perl 数组（Andrew）
     </para>
      <para>
      这基本上把 <productname>PostgreSQL</productname> 数组映射到 Perl 数组。
     </para>
     </listitem>

     <listitem>
      <para>
      允许 Perl 非致命警告生成 <command>NOTICE</command> 消息（Andrew）
     </para>
     </listitem>

     <listitem>
      <para>
      允许启用 Perl 的 <literal>strict</literal> 模式（Andrew）
     </para>
     </listitem>

    </itemizedlist>
   </sect3>


   <sect3>
    <title><application>psql</application> 变更</title>
    <itemizedlist>

     <listitem>
      <para>
      新增 <command>\set ON_ERROR_ROLLBACK</command>，允许事务中的语句出错而不影响事务的其余部分（Greg Sabino Mullane）
     </para>
      <para>
      这基本上是通过把每条语句包装在子事务中实现的。
     </para>
     </listitem>

     <listitem>
      <para>
      新增对 <application>psql</application> 变量中 <literal>\x</literal> 十六进制字符串的支持（Bruce）
     </para>
      <para>
      八进制转义此前已支持。
     </para>
     </listitem>

     <listitem>
      <para>
      新增对 <command>troff -ms</command> 输出格式的支持（Roger Leigh）
     </para>
     </listitem>

     <listitem>
      <para>
      允许历史文件的位置由 <envar>HISTFILE</envar> 控制（Andreas Seltenreich）
     </para>
      <para>
      这允许配置逐数据库的历史存储。
     </para>
     </listitem>

     <listitem>
      <para>
      防止 <command>\x</command>（扩展模式）影响 <command>\d tablename</command> 的输出（Neil）
     </para>
     </listitem>

     <listitem>
      <para>
      为 <application>psql</application> 新增 <option>-L</option> 选项以记录会话（Lorne Sunley）
     </para>
      <para>
      添加此选项是因为某些操作系统没有简单的命令行活动记录功能。
     </para>
     </listitem>

     <listitem>
      <para>
      让 <command>\d</command> 显示索引的表空间（Qingqing Zhou）
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <application>psql</application> 帮助（<command>\h</command>）对正确的帮助信息做最佳猜测（Greg Sabino Mullane）
     </para>
      <para>
      这使用户只需在语法错误的查询前加上 <command>\h</command> 就能获得支持语法的帮助。此前必须删除命令名之外的任何附加查询文本才能使用 <command>\h</command>。
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <command>\pset numericlocale</command>，允许以区域感知的格式输出数字（Eugen Nedelcu）
     </para>
      <para>
      例如，使用 <literal>C</literal> 区域设置时 <literal>100000</literal> 会输出为 <literal>100,000.0</literal>，而欧洲区域设置可能输出为 <literal>100.000,0</literal>。
     </para>
     </listitem>

     <listitem>
      <para>
      让启动横幅在服务器版本号与 <application>psql</application> 版本号不同时同时显示两者（Bruce）
     </para>
      <para>
      此外，如果服务器和 <application>psql</application> 来自不同的主版本，会显示警告。
     </para>
     </listitem>

    </itemizedlist>
   </sect3>


   <sect3>
    <title><application>pg_dump</application> 变更</title>
    <itemizedlist>

     <listitem>
      <para>
      为 <application>pg_restore</application> 新增 <option>-n</option> / <option>--schema</option> 开关（Richard van den Berg）
     </para>
      <para>
      这允许只恢复指定模式中的对象。
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <application>pg_dump</application> 即使在文本模式也转储大对象（Tom）
     </para>
      <para>
      有了此变更，大对象现在总是被转储；以前的 <option>-b</option> 开关是无操作。
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <application>pg_dump</application> 转储大对象的一致快照（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      转储大对象的注释（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      为 <application>pg_dump</application> 新增 <option>--encoding</option>（Magnus Hagander）
     </para>
      <para>
      这允许以不同于服务器编码的编码转储数据库。当把转储传送到不同编码的机器时这很有价值。
     </para>
     </listitem>

     <listitem>
      <para>
      过程语言依靠 <structname>pg_pltemplate</structname>（Tom）
     </para>
      <para>
      如果过程语言的调用处理器在 <literal>pg_catalog</literal> 模式中，<application>pg_dump</application> 不转储该处理器。而是只用 <command>CREATE LANGUAGE <replaceable>name</replaceable></command> 转储该语言，依赖 <structname>pg_pltemplate</structname> 目录在装载时提供该语言的创建参数。
     </para>
     </listitem>

    </itemizedlist>
   </sect3>


   <sect3>
    <title><application>libpq</application> 变更</title>
    <itemizedlist>

     <listitem>
      <para>
      新增 <envar>PGPASSFILE</envar> 环境变量，指定口令文件的文件名（Andrew）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <function>lo_create()</function>，类似于 <function>lo_creat()</function> 但允许指定大对象的 OID（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      让 <application>libpq</application> 在 <function>malloc()</function> 失败时一致地向客户端应用程序返回错误（Neil）
     </para>
     </listitem>
    </itemizedlist>
   </sect3>


   <sect3>
    <title>源代码变更</title>
    <itemizedlist>

     <listitem>
      <para>
      修复 <application>pgxs</application> 以支持针对重新定位的安装构建
     </para>
     </listitem>

     <listitem>
      <para>
      新增使用 Intel 编译器的 Itanium 处理器自旋锁支持（Vikram Kalsi）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 Windows 的 Kerberos 5 支持（Magnus）
     </para>
     </listitem>

     <listitem>
      <para>
      新增中文 FAQ（laser@pgsqldb.com）
     </para>
     </listitem>

     <listitem>
      <para>
      把 Rendezvous 改名为 Bonjour 以与 OS/X 特性改名一致（Bruce）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 Darwin 上对 <literal>fsync_writethrough</literal> 的支持（Chris Campbell）
     </para>
     </listitem>

     <listitem>
      <para>
      精简服务器、优化器和锁系统内部的信息传递（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      允许用 MSVC 编译 <application>pg_config</application>（Andrew）
     </para>
      <para>
      这是用 <application>MSVC</application> 构建 DBD::Pg 所必需的。
     </para>
     </listitem>

     <listitem>
      <para>
      移除对 Kerberos V4 的支持（Magnus）
     </para>
      <para>
      Kerberos 4 有安全漏洞且不再维护。
     </para>
     </listitem>

     <listitem>
      <para>
      代码清理（由 EnterpriseDB 执行 Coverity 静态分析）
     </para>
     </listitem>

     <listitem>
      <para>
      修改 <filename>postgresql.conf</filename>，使用文档默认的 <literal>on</literal>/<literal>off</literal> 而不是 <literal>true</literal>/<literal>false</literal>（Bruce）
     </para>
     </listitem>

     <listitem>
      <para>
      增强 <application>pg_config</application>，使其能报告更多构建期值（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      允许在 Windows 上把 <application>libpq</application> 构建为线程安全（Dave Page）
     </para>
     </listitem>

     <listitem>
      <para>
      允许在 Windows 上使用 IPv6 连接（Andrew）
     </para>
     </listitem>

     <listitem>
      <para>
      新增关于 I/O 子系统可靠性的服务器管理文档（Bruce）
     </para>
     </listitem>

     <listitem>
      <para>
      把私有声明从 <filename>gist.h</filename> 移到 <filename>gist_private.h</filename>（Neil）
     </para>

      <para>
      在以前的版本中，<filename>gist.h</filename> 既包含公共 GiST API（供 GiST 索引实现的作者使用），也包含 GiST 自身实现使用的一些私有声明。后者已被移到单独的文件 <filename>gist_private.h</filename>。大多数 GiST 索引实现应不受影响。
     </para>
     </listitem>

     <listitem>
      <para>
      彻底改造 GiST 内存管理（Neil）
     </para>

      <para>
      GiST 方法现在总是在短生命周期的内存上下文中调用。因此，通过 <function>palloc()</function> 分配的内存将被自动回收，GiST 索引实现不需要通过 <function>pfree()</function> 手动释放分配的内存。
     </para>
     </listitem>
    </itemizedlist>
   </sect3>


   <sect3>
    <title>附加模块变更</title>
    <itemizedlist>

     <listitem>
      <para>
      新增 <filename>/contrib/pg_buffercache</filename> contrib 模块（Mark Kirkwood）
     </para>
      <para>
      它显示缓冲区缓存的内容，用于调试和性能调优。
     </para>
     </listitem>

     <listitem>
      <para>
      移除 <filename>/contrib/array</filename>，因为它已过时（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      清理 <filename>/contrib/lo</filename> 模块（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      把 <filename>/contrib/findoidjoins</filename> 移到 <filename>/src/tools</filename>（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      从 <filename>/contrib/cube</filename> 移除 <literal>&lt;&lt;</literal>、<literal>&gt;&gt;</literal>、<literal>&amp;&lt;</literal> 和 <literal>&amp;&gt;</literal> 操作符
     </para>
      <para>
      这些操作符没有用处。
     </para>
     </listitem>

     <listitem>
      <para>
      改进 <filename>/contrib/btree_gist</filename>（Janko Richter）
     </para>
     </listitem>

     <listitem>
      <para>
      改进 <filename>/contrib/pgbench</filename>（Tomoaki Sato、Tatsuo）
     </para>
      <para>
      现在有一套用用户提供的 SQL 命令脚本进行测试的设施，而不是只有硬编码的命令序列。
     </para>
     </listitem>

     <listitem>
      <para>
      改进 <filename>/contrib/pgcrypto</filename>（Marko Kreen）
     </para>

      <itemizedlist>

       <listitem>
        <para>
      实现 OpenPGP 对称密钥和公钥加密
     </para>
        <para>
      支持 RSA 和 Elgamal 两种公钥算法。
     </para>
       </listitem>

       <listitem>
        <para>
      独立构建：包含 SHA256/384/512 哈希、Fortuna PRNG
     </para>
       </listitem>

       <listitem>
        <para>
      OpenSSL 构建：支持 3DES，OpenSSL &lt; 0.9.7 时使用内建 AES
     </para>
       </listitem>

       <listitem>
        <para>
      从 <filename>configure</filename> 结果获取构建参数（OpenSSL、zlib）
     </para>
        <para>
      不再需要编辑 <filename>Makefile</filename>。
     </para>
       </listitem>

       <listitem>
        <para>
      移除对 <filename>libmhash</filename> 和 <filename>libmcrypt</filename> 的支持
     </para>
       </listitem>

      </itemizedlist>
     </listitem>

    </itemizedlist>
   </sect3>

  </sect2>
 </sect1>
