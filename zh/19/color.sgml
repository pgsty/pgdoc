<!-- doc/src/sgml/color.sgml -->

<appendix id="color">
 <title>颜色支持</title>

 <indexterm zone="color">
  <primary>颜色</primary>
 </indexterm>

 <para>
  <productname>PostgreSQL</productname>软件包中的大多数程序都能生成彩色控制台输出。本附录描述其配置方式。
 </para>

 <sect1 id="color-when">
  <title>何时使用颜色</title>

  <para>
   要使用彩色输出，请按如下方式设置环境变量
   <envar>PG_COLOR</envar><indexterm><primary>PG_COLOR</primary></indexterm>：

   <orderedlist>
    <listitem>
     <para>
      如果其值为 <literal>always</literal>，则总是使用颜色。
     </para>
    </listitem>

    <listitem>
     <para>
      如果其值为 <literal>auto</literal>，并且标准错误流与终端设备相关联，则使用颜色。
     </para>
    </listitem>

    <listitem>
     <para>
      否则，不使用颜色。
     </para>
    </listitem>
   </orderedlist>
  </para>
 </sect1>

 <sect1 id="color-which">
  <title>配置颜色</title>

  <para>
   实际使用的颜色通过环境变量
   <envar>PG_COLORS</envar><indexterm><primary>PG_COLORS</primary></indexterm>
   进行配置（注意是复数形式）。其值是由冒号分隔的
   <literal><replaceable>key</replaceable>=<replaceable>value</replaceable></literal>
   对的列表。键指定该颜色的用途，值则是由终端解释的 SGR
   （Select Graphic Rendition）规范。
  </para>

  <para>
   目前使用的键如下：
   <variablelist>
    <varlistentry id="zh18-auto-color-varlistentry-5">
     <term><literal>error</literal></term>
     <listitem>
      <para>用于高亮错误消息中的文本 <quote>error</quote></para>
     </listitem>
    </varlistentry>

    <varlistentry id="zh18-auto-color-varlistentry-6">
     <term><literal>warning</literal></term>
     <listitem>
      <para>用于高亮警告消息中的文本 <quote>warning</quote></para>
     </listitem>
    </varlistentry>

    <varlistentry id="zh18-auto-color-varlistentry-7">
     <term><literal>note</literal></term>
     <listitem>
      <para>用于高亮这类消息中的文本 <quote>detail</quote> 和 <quote>hint</quote></para>
     </listitem>
    </varlistentry>

    <varlistentry id="zh18-auto-color-varlistentry-8">
     <term><literal>locus</literal></term>
     <listitem>
      <para>用于高亮消息中的位置信息（例如程序名和文件名）</para>
     </listitem>
    </varlistentry>
   </variablelist>
  </para>

  <para>
   默认值为
   <literal>error=01;31:warning=01;35:note=01;36:locus=01</literal>
   （<literal>01;31</literal> = 粗体红色，<literal>01;35</literal> = 粗体洋红色，
   <literal>01;36</literal> = 粗体青色，<literal>01</literal> = 粗体默认颜色）。
  </para>

  <tip>
   <para>
    这种颜色规范格式也被其他软件包使用，例如
    <productname>GCC</productname>、<productname>GNU coreutils</productname>
    和 <productname>GNU grep</productname>。
   </para>
  </tip>
 </sect1>
</appendix>
