<!-- doc/src/sgml/recovery-config.sgml -->

<chapter id="recovery-config">
  <title>恢复配置</title>

  <indexterm>
   <primary>configuration</primary>
   <secondary>of recovery</secondary>
   <tertiary>of a standby server</tertiary>
  </indexterm>

   <para>本章介绍 <filename>recovery.conf</filename><indexterm><primary>recovery.conf</primary></indexterm> 文件中可用的设置。它们只在恢复期间生效。如果之后还要进行恢复，必须重新设置这些参数。恢复开始后，就不能再更改它们。</para>

   <para><filename>recovery.conf</filename> 中的设置使用 <literal>name = 'value'</literal> 格式，每行指定一个参数。井号（<literal>#</literal>）表示该行余下内容为注释。要在参数值中嵌入单引号，请写两个单引号（<literal>''</literal>）。</para>

   <para>安装目录的 <filename>share/</filename> 目录中提供了示例文件 <filename>share/recovery.conf.sample</filename>。</para>

  <sect1 id="archive-recovery-settings">

    <title>归档恢复设置</title>
     <variablelist>

     <varlistentry id="restore-command" xreflabel="restore_command">
      <term><varname>restore_command</varname> (<type>string</type>)</term>
      <indexterm>
        <primary><varname>restore_command</> recovery parameter</primary>
      </indexterm>
      <listitem>
       <para>
        要执行的 shell 命令，用于取回 WAL 文件系列中的一个已归档段。这个参数是归档恢复所必需的，但对流复制是可选的。字符串中的任何 <literal>%f</literal> 都会被替换为要从归档中取回的文件名，而任何 <literal>%p</literal> 都会被替换为服务器上的副本目标路径名。（路径名相对于当前工作目录，即集簇的数据目录。）任何 <literal>%r</literal> 都会被替换为包含最后一个有效重启点的文件的名称。这是允许恢复可重启而必须保留的最早文件，因此这一信息可以用来把归档截断到仅包含支持从当前恢复重启所需的最小内容。<literal>%r</literal> 通常只被温备配置使用（见 <xref linkend="warm-standby">）。要嵌入一个实际的 <literal>%</literal> 字符，可以写 <literal>%%</literal>。
       </para>

       <para>
        只有当命令成功时才返回零退出状态，这一点很重要。系统<emphasis>会</emphasis>请求归档中不存在的文件名；遇到这种情况时它必须返回非零值。例子：
<programlisting>
restore_command = 'cp /mnt/server/archivedir/%f "%p"'
restore_command = 'copy "C:\\server\\archivedir\\%f" "%p"'  # Windows
</programlisting>
       </para>
      </listitem>
     </varlistentry>

     <varlistentry id="archive-cleanup-command" xreflabel="archive_cleanup_command">
      <term><varname>archive_cleanup_command</varname> (<type>string</type>)</term>
      <indexterm>
        <primary><varname>archive_cleanup_command</> recovery parameter</primary>
      </indexterm>
      <listitem>
       <para>
        这个可选参数指定了一个 shell 命令，它将在每一个重启点被执行。
        <varname>archive_cleanup_command</varname>的目的是提供一种清除不再被备库需要的旧的已归档 WAL 文件的机制。
        任何<literal>%r</literal>会被替换为包含最后一个可用重启点的文件的名称。
        那是使一次恢复变成可重启的所必须被<emphasis>保留</emphasis>的最早的文件，并且因此比<literal>%r</literal>更早的所有文件可以被安全地移除。
        这个信息可以被用来把归档截断为支持从当前恢复重启所需的最小值。
        对于单一备库配置，<xref linkend="pgarchivecleanup">模块常常被用在<varname>archive_cleanup_command</varname>中，例如：
<programlisting>archive_cleanup_command = 'pg_archivecleanup /mnt/server/archivedir %r'</programlisting>
        但是注意，如果多个备库正在从同一个归档目录中恢复，你将需要保证只有当所有服务器都不再需要这些 WAL 文件时才会删除它们。
        <varname>archive_cleanup_command</varname>通常被用于一种温备配置（见<xref linkend="warm-standby">）中。
        要在该命令中嵌入一个真正的<literal>%</literal>字符，需要写成<literal>%%</literal>。
       </para>
       <para>
        如果命令返回非零退出状态，将写入一条 WARNING 日志消息。
       </para>
      </listitem>
     </varlistentry>

     <varlistentry id="recovery-end-command" xreflabel="recovery_end_command">
      <term><varname>recovery_end_command</varname> (<type>string</type>)</term>
      <indexterm>
        <primary><varname>recovery_end_command</> recovery parameter</primary>
      </indexterm>
      <listitem>
       <para>此参数指定一个 shell 命令，在恢复结束时仅执行一次。此参数可选。<varname>recovery_end_command</varname> 用于提供复制或恢复后的清理机制。与 <xref linkend="archive-cleanup-command"> 一样，所有 <literal>%r</literal> 都会被替换为包含最后一个有效重启点的文件的名称。</para>
       <para>
        如果命令返回非零退出状态，将写入一条 WARNING 日志消息，但数据库仍会继续启动。例外是，如果命令被信号终止，数据库将不会继续启动。
       </para>
      </listitem>
     </varlistentry>

    </variablelist>

  </sect1>

  <sect1 id="recovery-target-settings">

    <title>恢复目标设置</title>
     <variablelist>

     <varlistentry id="recovery-target-name" xreflabel="recovery_target_name">
      <term><varname>recovery_target_name</varname>
           (<type>string</type>)
      </term>
      <indexterm>
        <primary><varname>recovery_target_name</> recovery parameter</primary>
      </indexterm>
      <listitem>
       <para>
        这个参数指定（<function>pg_create_restore_point()</function>所创建）的已命名的恢复点，恢复将进行到该恢复点。在 <varname>recovery_target_name</varname>、<xref linkend="recovery-target-time"> 或 <xref linkend="recovery-target-xid"> 中，最多只能指定一个。默认行为是恢复到 WAL 日志的末尾。
       </para>
      </listitem>
     </varlistentry>

     <varlistentry id="recovery-target-time" xreflabel="recovery_target_time">
      <term><varname>recovery_target_time</varname>
           (<type>timestamp</type>)
      </term>
      <indexterm>
        <primary><varname>recovery_target_time</> recovery parameter</primary>
      </indexterm>
      <listitem>
       <para>此参数指定恢复要进行到的时间戳。在 <varname>recovery_target_time</varname>、<xref linkend="recovery-target-name"> 或 <xref linkend="recovery-target-xid"> 中，最多只能指定一个。默认行为是恢复到 WAL 日志的末尾。精确的停止点还受 <xref linkend="recovery-target-inclusive"> 影响。</para>
      </listitem>
     </varlistentry>

     <varlistentry id="recovery-target-xid" xreflabel="recovery_target_xid">
      <term><varname>recovery_target_xid</varname> (<type>string</type>)</term>
      <indexterm>
        <primary><varname>recovery_target_xid</> recovery parameter</primary>
      </indexterm>
      <listitem>
       <para>此参数指定恢复要进行到的事务 ID。请注意，虽然事务 ID 在事务开始时按顺序分配，但事务完成的顺序可能与 ID 的数值顺序不同。恢复的事务是那些在指定事务之前提交的事务，并可选择包括指定事务本身。在 <varname>recovery_target_xid</varname>、<xref linkend="recovery-target-name"> 或 <xref linkend="recovery-target-time"> 中，最多只能指定一个。默认行为是恢复到 WAL 日志的末尾。精确的停止点还受 <xref linkend="recovery-target-inclusive"> 影响。</para>
      </listitem>
     </varlistentry>

     <varlistentry id="recovery-target-inclusive"
                   xreflabel="recovery_target_inclusive">
      <term><varname>recovery_target_inclusive</varname>
        (<type>boolean</type>)
      </term>
      <indexterm>
        <primary><varname>recovery_target_inclusive</> recovery parameter</primary>
      </indexterm>
      <listitem>
       <para>
        指定是在指定的恢复目标之后停止（<literal>true</literal>），还是在恢复目标之前停止（<literal>false</literal>）。适用于 <xref linkend="recovery-target-time"> 和 <xref linkend="recovery-target-xid"> 中为此恢复指定的那一个。它分别表示提交时间或 ID 恰好等于目标值的事务是否会被包含在恢复中。默认值是 <literal>true</literal>。
       </para>
      </listitem>
     </varlistentry>

     <varlistentry id="recovery-target-timeline"
                   xreflabel="recovery_target_timeline">
      <term><varname>recovery_target_timeline</varname>
        (<type>string</type>)
      </term>
      <indexterm>
        <primary><varname>recovery_target_timeline</> recovery parameter</primary>
      </indexterm>
      <listitem>
       <para>指定恢复到某条特定时间线。默认沿着制作基础备份时的当前时间线恢复。设为 <literal>latest</literal> 时，会恢复到归档中找到的最新时间线，这对备库很有用。除此之外，只有在复杂的再次恢复场景中才需要设置此参数：需要返回的状态本身就是一次时间点恢复之后达到的状态。相关讨论参见 <xref linkend="backup-timelines">。</para>
      </listitem>
     </varlistentry>

     <varlistentry id="pause-at-recovery-target"
                   xreflabel="pause_at_recovery_target">
      <term><varname>pause_at_recovery_target</varname>
        (<type>boolean</type>)
      </term>
      <indexterm>
        <primary><varname>pause_at_recovery_target</> recovery parameter</primary>
      </indexterm>
      <listitem>
       <para>
       指定在达到恢复目标时恢复是否应暂停。默认值为 true。
       这是为了允许对数据库执行查询，以检查这个恢复目标是否为最合适的恢复位置。
       暂停的状态可以使用<function>pg_xlog_replay_resume()</function>（见<xref linkend="functions-recovery-control-table">）继续，这会让恢复终结。
       如果这个恢复目标不是想要的停止点，那么关闭服务器，将恢复目标设置改为一个稍后的目标并且重启以继续恢复。
      </para>
       <para>如果未启用<xref linkend="guc-hot-standby">，或者未设置恢复目标，此设置不起作用。</para>
      </listitem>
     </varlistentry>

     </variablelist>
   </sect1>

  <sect1 id="standby-settings">

    <title>备库设置</title>
     <variablelist>

       <varlistentry id="standby-mode" xreflabel="standby_mode">
        <term><varname>standby_mode</varname> (<type>boolean</type>)</term>
        <indexterm>
          <primary><varname>standby_mode</> recovery parameter</primary>
        </indexterm>
        <listitem>
         <para>指定是否将 <productname>PostgreSQL</productname> 服务器作为备库启动。如果此参数为 <literal>on</literal>，服务器到达已归档 WAL 的末尾时不会停止恢复，而会继续尝试使用 <varname>restore_command</varname> 获取新的 WAL 段，和/或按照 <varname>primary_conninfo</varname> 设置连接主库，以继续恢复。</para>
        </listitem>
       </varlistentry>
       <varlistentry id="primary-conninfo" xreflabel="primary_conninfo">
        <term><varname>primary_conninfo</varname> (<type>string</type>)</term>
        <indexterm>
          <primary><varname>primary_conninfo</> recovery parameter</primary>
        </indexterm>
        <listitem>
         <para>
          指定备库用来连接主库的连接串。该串采用 libpq 的 <function>PQconnectdb</function> 函数所接受的格式，见 <xref linkend="libpq-connect">。如果串中未指定某个选项，则会检查相应的环境变量（见 <xref linkend="libpq-envars">）。如果环境变量也未设置，则使用默认值。
         </para>
         <para>
          连接串应当指定主服务器的主机名（或地址），如果端口与备库的默认端口不同，还应指定端口号。还要指定一个用户名，它对应主库上一个具有 <literal>REPLICATION</literal> 和 <literal>LOGIN</literal> 权限的角色（见 <xref linkend="streaming-replication-authentication">）。如果主库要求密码认证，还需要提供密码。密码可以放在 <varname>primary_conninfo</varname> 串中，也可以放在备库服务器上一个单独的 <filename>~/.pgpass</filename> 文件中（数据库名使用 <literal>replication</literal>）。不要在 <varname>primary_conninfo</varname> 串中指定数据库名。
         </para>
         <para>如果 <varname>standby_mode</varname> 为 <literal>off</literal>，此设置不起作用。</para>
        </listitem>
       </varlistentry>
       <varlistentry id="trigger-file" xreflabel="trigger_file">
        <term><varname>trigger_file</varname> (<type>string</type>)</term>
        <indexterm>
          <primary><varname>trigger_file</> recovery parameter</primary>
        </indexterm>
        <listitem>
         <para>指定一个触发文件，其出现会使备库结束恢复。即使未设置此值，也仍可以使用 <command>pg_ctl promote</command> 提升备库。如果 <varname>standby_mode</varname> 为 <literal>off</literal>，此设置不起作用。</para>
        </listitem>
       </varlistentry>

     </variablelist>
   </sect1>

</chapter>
