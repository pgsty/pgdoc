<!-- doc/src/sgml/appendix-obsolete-libpq-fastpath.sgml -->
<!--
  See doc/src/sgml/appendix-obsolete.sgml for why this file exists. Do not change the id attribute.
-->

<sect1 id="libpq-fastpath">

  <title><application>libpq</application> 快速路径接口已移除</title>

  <indexterm zone="libpq-fastpath">
   <primary>fast path</primary>
  </indexterm><indexterm zone="libpq-fastpath">
    <primary>PQfn</primary>
   </indexterm>

   <para>
    在 <productname>PostgreSQL</productname> 19 及更早版本中，<application>libpq</application> 支持通过 <function>PQfn</function> 函数向服务器发送简单函数调用的快速路径接口。该接口既不安全，又已过时，因此在 <productname>PostgreSQL</productname> 20 中被移除。为使应用程序仍能完成链接，<function>PQfn</function> 符号依然存在，但现在调用它总是失败。可以通过创建预备语句来定义函数调用，从而获得相近的性能和更强的功能。然后，以二进制形式传输参数和结果来执行该语句，就可以替代快速路径函数调用。
   </para>

   

  

  

  

  

  

  

  

  

 </sect1>
