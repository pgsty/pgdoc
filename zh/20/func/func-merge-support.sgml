 <sect1 id="functions-merge-support">
  <title>合并支持函数</title>

  <indexterm>
   <primary>MERGE</primary>
   <secondary>RETURNING</secondary>
  </indexterm>

  <para>
   <productname>PostgreSQL</productname>包含一个合并支持函数，可以在<xref linkend="sql-merge"/>命令的<literal>RETURNING</literal>列表中使用，以标识对每行所执行的操作；参见<xref linkend="functions-merge-support-table"/>。
  </para>

  <table id="functions-merge-support-table">
   <title>合并支持函数</title>

   <tgroup cols="1">
    <thead>
     <row>
      <entry role="func_table_entry"><para role="func_signature">
       函数
      </para>
      <para>
       描述
      </para></entry>
     </row>
    </thead>

    <tbody>
     <row>
      <entry id="merge-action" role="func_table_entry"><para role="func_signature">
       <indexterm>
        <primary>merge_action</primary>
       </indexterm>
       <function>merge_action</function> ( )
       <returnvalue>text</returnvalue>
      </para>
      <para>
       返回为当前行执行的合并操作命令。结果为<literal>'INSERT'</literal>、<literal>'UPDATE'</literal>或<literal>'DELETE'</literal>。
      </para></entry>
     </row>
    </tbody>
   </tgroup>
  </table>

  <para>
   示例：
<screen><![CDATA[
MERGE INTO products p
  USING stock s ON p.product_id = s.product_id
  WHEN MATCHED AND s.quantity > 0 THEN
    UPDATE SET in_stock = true, quantity = s.quantity
  WHEN MATCHED THEN
    UPDATE SET in_stock = false, quantity = 0
  WHEN NOT MATCHED THEN
    INSERT (product_id, in_stock, quantity)
      VALUES (s.product_id, true, s.quantity)
  RETURNING merge_action(), p.*;

 merge_action | product_id | in_stock | quantity
--------------+------------+----------+----------
 UPDATE       |       1001 | t        |       50
 UPDATE       |       1002 | f        |        0
 INSERT       |       1003 | t        |       10
]]></screen>
  </para>

  <para>
   注意，此函数只能在<command>MERGE</command>命令的<literal>RETURNING</literal>列表中使用。在查询的任何其他部分使用它都会导致错误。
  </para>

 </sect1>
