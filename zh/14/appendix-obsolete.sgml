<!-- doc/src/sgml/obsolete.sgml -->

<appendix id="appendix-obsolete">

 <title>过时或重命名的功能</title>

 <para>
   有时，某些功能会从 <productname>PostgreSQL</productname> 中移除，某些特性、设置和文件名会发生变化，文档也可能移到别处。本节会将来自旧版本文档或外部链接的用户引导到适当的新位置，以便找到他们所需的信息。
 </para>

 <!--
  保留这一节，是为了让跟随文档 /current/ 链接的用户在我们移动或重命名内容时不至于遇到 404。用户在搜索中找到旧版文档，或者在检查索引时看到旧命令名后，也可以通过这里的链接跳转到新命令。

  此处每个子节都应保留该文档在重命名或移动之前所使用的同一个 <chapter>、<appendix> 和/或 <sect1> 的 "id" 属性。不要加上 "obsolete-" 之类的前缀，必须完全保持一致。这些 id 用于决定生成 HTML 文档时的文件名，因此改动它们会破坏链接。

  每个条目还应插入索引项，把旧名称重定向到新名称。推荐写法是

      <indexterm><primary>oldname</primary><see>newname</see></indexterm>

  我们不打算维护指向更低版本的链接，例如从 pg_waldump 指到 pg_xlogdump。旧版本用户应查阅旧版文档，也不需要添加从新名称指回旧名称的索引项。
 -->

 &obsolete-recovery-config;
 &obsolete-default-roles;
 &obsolete-pgxlogdump;
 &obsolete-pgresetxlog;
 &obsolete-pgreceivexlog;

</appendix>
