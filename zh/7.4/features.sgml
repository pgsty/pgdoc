<!--
$Header: /cvsroot/pgsql/doc/src/sgml/features.sgml,v 2.20.2.1 2003/11/04 09:45:29 petere Exp $
-->

<appendix id="features">
 <title>SQL 符合性</title>

 <para>
  本节尝试概述 <productname>PostgreSQL</productname> 在多大程度上符合 SQL
  标准。完全符合该标准或给出一份完整的符合性声明既复杂又没有特别的用处，因此本节只能给出一个概览。
  </para>

 <para>
  SQL 标准的正式名称是 ISO/IEC 9075<quote>数据库语言 SQL</quote>。该标准不时发布修订版本；最近一次出现在 1999 年。那个版本称为 ISO/IEC 9075:1999，或非正式地称为 SQL99。在此之前的版本是 SQL92。<productname>PostgreSQL</productname> 的开发倾向于在不与传统特性或常识相矛盾的前提下，以符合该标准的最新官方版本为目标。在撰写本文时，该标准的一次新修订正在投票表决中，如果获得批准，它最终将成为未来
  <productname>PostgreSQL</productname> 开发的符合性目标。
 </para>

 <para>
  <acronym>SQL92</acronym> 为符合性定义了三个特性集：入门（Entry）、中级（Intermediate）和完整（Full）。多数宣称符合 <acronym>SQL</acronym> 标准的数据库管理系统，实际上只符合入门级别，因为中级和完整级别中的全部特性要么过于庞杂，要么与既有行为相冲突。
 </para>

 <para>
  <acronym>SQL99</acronym> 定义了大量独立特性，而不是
  <acronym>SQL92</acronym> 中那种效果不佳且过于宽泛的三级分类。其中很大一个子集构成了<quote>核心</quote>特性，也就是每个符合标准的 SQL 实现都必须提供的特性。其余的特性则完全是可选的。某些可选特性被组合在一起，形成<quote>包</quote>，SQL 实现可以声明符合这些包，从而表明符合特定的特性组。
 </para>

 <para>
  <acronym>SQL99</acronym> 标准也分为 5 个部分：框架（Framework）、基础（Foundation）、调用级接口（Call Level Interface）、持久存储模块（Persistent Stored Modules）和宿主语言绑定（Host Language Bindings）。<productname>PostgreSQL</productname> 只涵盖第 1、2、5 部分。第 3 部分类似于 ODBC 接口，第 4 部分类似于
  <application>PL/pgSQL</application> 编程语言，但这两种情况下都没有特别意图实现精确的符合。
 </para>

 <para>
  在下面两节中，我们先列出 <productname>PostgreSQL</productname> 支持的特性，然后列出 SQL99 中定义但 <productname>PostgreSQL</productname> 尚不支持的特性。这两个列表都是近似的：被列为受支持的特性可能存在个别不符合标准的细节，而不受支持的特性也可能实际上已实现了很大一部分。关于什么可用什么不可用的最准确信息，始终以文档正文为准。
 </para>

 <note>
  <para>
   特性代码中包含连字符的是子特性。因此，如果某个特定的子特性不受支持，那么即使其他一些子特性受支持，该主特性也会被列为不受支持。
  </para>
 </note>

  <sect1 id="features-sql99">
   <title>支持的特性</title>

   <para>
    <informaltable>
     <tgroup cols="4">
      <thead>
       <row>
        <entry>标识符</entry>
        <entry>包</entry>
        <entry>描述</entry>
        <entry>注释</entry>
       </row>
      </thead>

      &features-supported;

     </tgroup>
    </informaltable>
   </para>
  </sect1>

  <sect1 id="unsupported-features-sql99">
   <title>不支持的特性</title>

   <para>
    下面这些 <acronym>SQL99</acronym> 中定义的特性在这个版本的
    <productname>PostgreSQL</productname> 中尚未实现。在少数情况下，有等价的功能可用。

    <informaltable>
     <tgroup cols="4">
      <thead>
       <row>
        <entry>标识符</entry>
        <entry>包</entry>
        <entry>描述</entry>
        <entry>注释</entry>
       </row>
      </thead>

      &features-unsupported;

     </tgroup>
    </informaltable>
   </para>
  </sect1>

 </appendix>

<!-- Keep this comment at the end of the file
Local variables:
mode:sgml
sgml-omittag:nil
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
sgml-parent-document:nil
sgml-default-dtd-file:"./reference.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:("/usr/lib/sgml/catalog")
sgml-local-ecat-files:nil
End:
-->
