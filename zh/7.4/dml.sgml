<!-- $Header: /cvsroot/pgsql/doc/src/sgml/dml.sgml,v 1.6 2003/08/31 17:32:18 petere Exp $ -->

<chapter id="dml">
 <title>数据操纵</title>

 <comment>
  本章内容仍然相当不完整。
 </comment>

 <para>
  上一章讨论了如何创建表以及其他用来保存数据的结构。现在该向表中填充数据了。本章介绍如何插入、更新和删除表中的数据。我们还介绍在特定事件发生时实现自动数据更改的方法：触发器和重写规则。下一章将最终解释如何把你久违的数据从数据库中提取出来。
 </para>

 <sect1 id="dml-insert">
  <title>插入数据</title>

  <indexterm zone="dml-insert">
   <primary>inserting</primary>
  </indexterm>

  <indexterm zone="dml-insert">
   <primary>INSERT</primary>
  </indexterm>

  <para>
   表创建时不包含任何数据。在数据库能派上用场之前，首先要做的事情就是插入数据。从概念上讲，数据是一次一行地插入的。当然你也可以一次插入多行，但没有办法一次插入不到一行的数据。即使你只知道某些列的值，也必须创建一个完整的行。
  </para>

  <para>
   要创建一个新行，使用<literal>INSERT</literal>命令。该命令需要表名以及表中每一列的一个值。例如，考虑<xref
   linkend="ddl">中的 products 表：
<programlisting>
CREATE TABLE products (
    product_no integer,
    name text,
    price numeric
);
</programlisting>
   插入一行的一个示例命令是：
<programlisting>
INSERT INTO products VALUES (1, 'Cheese', 9.99);
</programlisting>
   数据值按各列在表中出现的顺序列出，用逗号分隔。通常，数据值是字面量（常量），但也允许标量表达式。
  </para>

  <para>
   上面的语法有一个缺点：你需要知道表中各列的顺序。为避免这一点，你也可以显式列出列。例如，下面两条命令与上面的命令效果相同：
<programlisting>
INSERT INTO products (product_no, name, price) VALUES (1, 'Cheese', 9.99);
INSERT INTO products (name, price, product_no) VALUES ('Cheese', 9.99, 1);
</programlisting>
   许多用户认为始终列出列名是一种好习惯。
  </para>

  <para>
   如果你没有所有列的值，可以省略其中一些。这种情况下，被省略的列将用它们的默认值填充。例如，
<programlisting>
INSERT INTO products (product_no, name) VALUES (1, 'Cheese');
INSERT INTO products VALUES (1, 'Cheese');
</programlisting>
   第二种形式是<productname>PostgreSQL</productname>的扩展。它从左边开始用给定的值填充各列，其余列将使用默认值。
  </para>

  <para>
   为了清楚起见，你也可以显式地请求默认值，可以对单个列，也可以对整行：
<programlisting>
INSERT INTO products (product_no, name, price) VALUES (1, 'Cheese', DEFAULT);
INSERT INTO products DEFAULT VALUES;
</programlisting>
  </para>

  <tip>
   <para>
    要做<quote>批量装载</quote>，也就是插入大量数据，可以看看<xref linkend="sql-copy"
    endterm="sql-copy-title">命令。它不如<command>INSERT</command>
    命令灵活，但效率更高。
   </para>
  </tip>
 </sect1>

 <sect1 id="dml-update">
  <title>更新数据</title>

  <indexterm zone="dml-update">
   <primary>updating</primary>
  </indexterm>

  <indexterm zone="dml-update">
   <primary>UPDATE</primary>
  </indexterm>

  <para>
   对数据库中已有数据的修改称为更新。你可以更新单独的行、表中的所有行或所有行的一个子集。每一列都可以单独更新；其他列不受影响。
  </para>

  <para>
   要执行一次更新，你需要三条信息：
   <orderedlist spacing=compact>
    <listitem>
     <para>要更新的表和列的名称，</para>
    </listitem>

    <listitem>
     <para>列的新值，</para>
    </listitem>

    <listitem>
     <para>要更新哪些行。</para>
    </listitem>
   </orderedlist>
  </para>

  <para>
   回想<xref linkend="ddl">中所说的，SQL 一般不为行提供唯一的标识符。因此不一定能直接指定要更新哪一行。相反，你指定的是行必须满足什么条件才会被更新。只有当表中有主键时（无论你是否声明了它），你才能通过选择一个与主键匹配的条件来可靠地定位单独的行。图形化的数据库访问工具正是依靠这一点来让你逐行更新的。
  </para>

  <para>
   例如，这条命令把所有价格为 5 的产品的价格更新为 10：
<programlisting>
UPDATE products SET price = 10 WHERE price = 5;
</programlisting>
    这可能导致零行、一行或多行被更新。尝试一次不匹配任何行的更新并不是错误。
  </para>

  <para>
   让我们详细看看这条命令：首先是关键字<literal>UPDATE</literal>，后跟表名。与通常一样，表名可以是模式限定的，否则将在路径中查找。接下来是关键字<literal>SET</literal>，后跟列名、一个等号和列的新值。新的列值可以是任何标量表达式，而不仅仅是常量。例如，如果你想把所有产品的价格提高 10%，可以使用：
<programlisting>
UPDATE products SET price = price * 1.10;
</programlisting>
   如你所见，新值的表达式也可以引用旧值。我们还省略了
   <literal>WHERE</literal>子句。如果省略它，就意味着表中的所有行都被更新。如果给出它，则只有满足<literal>WHERE</literal>
   之后条件的那些行被更新。注意，<literal>SET</literal>子句中的等号是赋值，而<literal>WHERE</literal>子句中的等号是比较，但这不会产生任何歧义。当然，条件不必是相等测试。还有许多其他操作符可用（见<xref linkend="functions">）。但表达式必须求值为布尔结果。
  </para>

  <para>
   你也可以在一条<literal>UPDATE</literal>命令中更新多列，方法是在<literal>SET</literal>子句中列出多个赋值。例如：
<programlisting>
UPDATE mytable SET a = 5, b = 3, c = 1 WHERE a > 0;
</programlisting>
  </para>
 </sect1>

 <sect1 id="dml-delete">
  <title>删除数据</title>

  <indexterm zone="dml-delete">
   <primary>deleting</primary>
  </indexterm>

  <indexterm zone="dml-delete">
   <primary>DELETE</primary>
  </indexterm>

  <para>
   到目前为止我们已经解释了如何向表中添加数据以及如何更改数据。剩下的是讨论如何删除不再需要的数据。正如添加数据只能整行进行一样，你也只能从表中删除整行。上一节我们讨论过，SQL 不提供直接定位单独行的办法。因此，删除行只能通过指定要删除的行必须满足的条件来完成。如果表中有主键，那么你可以指定精确的行。但你也可以删除匹配某个条件的成组的行，或者一次删除表中的所有行。
  </para>

  <para>
   你使用<literal>DELETE</literal>命令删除行；其语法与
   <literal>UPDATE</literal>命令非常相似。例如，要从 products
   表中删除所有价格为 10 的行，使用
<programlisting>
DELETE FROM products WHERE price = 10;
</programlisting>
  </para>

  <para>
   如果你直接写
<programlisting>
DELETE FROM products;
</programlisting>
   那么表中的所有行都将被删除！程序员当心。
  </para>
 </sect1>
</chapter>
