<!-- doc/src/sgml/sourcerepo.sgml -->

<appendix id="sourcerepo">
 <title>源代码仓库</title>

 <para>
  <productname>PostgreSQL</productname> 源代码使用
  <productname>Git</productname> 版本控制系统存储和管理。主仓库的一个公共镜像可用；它在主仓库的任何变更之后一分钟内更新。
 </para>

 <para>
  我们的 wiki，<ulink
  url="http://wiki.postgresql.org/wiki/Working_with_Git"></ulink>，上有一些关于使用 Git 工作的讨论。
 </para>

 <para>
  注意，从源代码仓库构建
  <productname>PostgreSQL</productname> 需要相当新的版本的
  <application>bison</>、
  <application>flex</> 和
  <application>Perl</>。从发行版 tarball 构建时不需要这些工具，因为它们用来构建的文件已包含在 tarball
  中。其他工具要求与<xref linkend="installation">中所示的相同。
 </para>

 <sect1 id="git">
  <title>通过 <productname>Git</> 获取源代码</title>

  <para>
   使用
   <productname>Git</> 你将在本地机器上复制整个代码仓库，因此你可以离线访问所有历史和分支。这是开发或测试补丁的最快、最灵活的方式。
  </para>

  <procedure>
   <title>Git</title>

   <step>
    <para>
     你需要一份已安装的
     <productname>Git</>，可以从
     <ulink url="http://git-scm.com"></ulink> 获得。许多系统默认已经安装了较新版本的
     <application>Git</>，或者可以在它们的软件包分发系统中获得。
    </para>
   </step>

   <step>
    <para>
     要开始使用 Git 仓库，克隆官方镜像：

<programlisting>
git clone git://git.postgresql.org/git/postgresql.git
</programlisting>

     这会把完整的仓库复制到你的本地机器，所以可能需要一段时间才能完成，尤其是在网络连接较慢时。文件将被放在当前目录的一个新子目录
     <filename>postgresql</>
     中。
    </para>

    <para>
     如果例如防火墙阻挡了对 Git 协议的访问，也可以通过 HTTP
     协议访问 Git 镜像。只需把 URL 前缀改为
     <literal>http</>，如下：

<programlisting>
git clone http://git.postgresql.org/git/postgresql.git
</programlisting>

     HTTP 协议不如 Git 协议高效，所以使用起来会更慢。
    </para>
   </step>

   <step>
    <para>
     每当你想获取系统中的最新更新时，<command>cd</>
     进入仓库，运行：

<programlisting>
git fetch
</programlisting>
    </para>
   </step>
  </procedure>

  <para>
   <productname>Git</> 能做的事情远不止获取源代码。要获取更多信息，请查阅
   <productname>Git</> 的 man 手册页，或访问
   <ulink url="http://git-scm.com"></> 上的网站。
  </para>
 </sect1>

</appendix>
