<!-- doc/src/sgml/release-7.4.sgml -->
<!-- See header comment in release.sgml about typical markup -->

 <sect1 id="release-7-4-30">
  <title>版本 7.4.30</title>

  <note>
  <title>发行日期</title>
  <simpara>2010-10-04</simpara>
 </note>

  <para>
   本次发布包含对 7.4.29 的多方面修复。有关 7.4 大版本中新特性的信息，请参见<xref linkend="release-7-4">。
  </para>

  <para>
      这预计是 <productname>PostgreSQL</productname> 7.4.X 系列的最后一个发行版。建议用户尽快更新到较新的发行分支。
     </para>

  <sect2>
   <title>迁移到版本 7.4.30</title>

   <para>
      对于运行 7.4.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 7.4.26 的版本升级，请参阅 7.4.26 的发行说明。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      在 PL/Perl 和 PL/Tcl 中为每个调用方 SQL 用户 ID 使用单独的解释器（Tom Lane）
     </para>

     <para>
      此变更防止了可通过破坏稍后将在同一会话中以另一 SQL 用户身份执行的 Perl 或 Tcl 代码（例如在 <literal>SECURITY DEFINER</literal> 函数内）而造成的安全问题。大多数脚本语言都提供许多实现这一点的手段，例如重新定义目标函数调用的标准函数或操作符。如果没有此变更，任何拥有 Perl 或 Tcl 语言使用权的 SQL 用户基本上都能以目标函数所有者的 SQL 权限为所欲为。
     </para>

     <para>
      此变更的代价是 Perl 和 Tcl 函数之间的有意通信变得更困难。为提供出路，PL/PerlU 和 PL/TclU 函数继续每会话只使用一个解释器。这不被视为安全问题，因为所有这类函数本来就以数据库超级用户的信任级别执行。
     </para>

     <para>
      声称提供可信执行的第三方过程语言很可能存在类似的安全问题。我们建议就安全关键用途联系你所依赖的任何 PL 的作者。
     </para>

     <para>
      感谢 Tim Bunce 指出此问题（CVE-2010-3433）。
     </para>
    </listitem>

    <listitem>
     <para>
      禁止以并非它预期配合使用的系统目录列的参数调用 <function>pg_get_expr()</function>，以防止可能的崩溃（Heikki Linnakangas、Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复<quote>无法处理未规划的子查询</quote>错误（Tom Lane）
     </para>

     <para>
      当子查询包含一个连接别名引用，而该引用展开成包含另一个子查询的表达式时，会出现此错误。
     </para>
    </listitem>

    <listitem>
     <para>
      写入锁文件（<filename>postmaster.pid</filename> 和套接字锁文件两者）内容时注意对其执行 fsync（Tom Lane）
     </para>

     <para>
      如果机器在 postmaster 启动后不久崩溃，这一疏漏可能导致锁文件内容损坏。这又可能使后续启动 postmaster 的尝试失败，直到手工删除锁文件。
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <filename>contrib/dblink</filename> 对包含已删除列的表的处理（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/dblink</filename> 中<quote>重复连接名</quote>错误后的连接泄漏（Itagaki Takahiro）
     </para>
    </listitem>

    <listitem>
     <para>
      更新构建基础设施和文档，反映源代码仓库从 CVS 迁移到 Git 的变化（Magnus Hagander 等）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-7-4-29">
  <title>版本 7.4.29</title>

  <note>
  <title>发行日期</title>
  <simpara>2010-05-17</simpara>
 </note>

  <para>
   本次发布包含对 7.4.28 的多方面修复。有关 7.4 大版本中新特性的信息，请参见<xref linkend="release-7-4">。
  </para>

  <para>
      <productname>PostgreSQL</productname> 社区将于 2010 年 7 月停止对 7.4.X 版本系列的更新。建议用户尽快更新到较新的版本分支。
     </para>

  <sect2>
   <title>迁移到版本 7.4.29</title>

   <para>
      对于运行 7.4.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 7.4.26 的版本升级，请参阅 7.4.26 的发行说明。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      用应用于整个解释器的操作码掩码来强制 <literal>plperl</literal> 中的限制，而不是使用 <filename>Safe.pm</filename>（Tim Bunce、Andrew Dunstan）
     </para>

     <para>
      近期的事态发展使我们确信 <filename>Safe.pm</filename> 太不安全，不能依靠它使 <literal>plperl</literal> 可信。此变更完全移除了对 <filename>Safe.pm</filename> 的使用，改为使用始终应用操作码掩码的独立解释器。此变更的良好副作用包括：现在可以在 <literal>plperl</literal> 中以自然的方式使用 Perl 的 <literal>strict</literal> 编译指令，Perl 的 <literal>$a</literal> 和 <literal>$b</literal> 变量在排序例程中按预期工作，以及函数编译显著加快。（CVE-2010-1169）
     </para>
    </listitem>

    <listitem>
     <para>
      防止 PL/Tcl 执行来自 <structname>pltcl_modules</structname> 的不可信代码（Tom）
     </para>

     <para>
      PL/Tcl 从数据库表自动装载 Tcl 代码的特性可能被利用于特洛伊木马攻击，因为对谁能创建该表或向其中插入没有限制。此变更禁用该特性，除非 <structname>pltcl_modules</structname> 由超级用户拥有。（但是，不检查该表上的权限，因此确实需要较不安全的模块表的安装仍可以向受信任的非超级用户授予适当权限。）此外，除非确实要执行 <literal>pltclu</literal> 函数，否则防止把代码装载到无限制的<quote>普通</quote>Tcl 解释器中。（CVE-2010-1170）
     </para>
    </listitem>

    <listitem>
     <para>
      不允许非特权用户重置仅超级用户可用的参数设置（Alvaro）
     </para>

     <para>
      此前，如果非特权用户为自己运行 <literal>ALTER USER ... RESET ALL</literal>，或为他拥有的数据库运行 <literal>ALTER DATABASE ... RESET ALL</literal>，这将移除该用户或数据库的所有特殊参数设置，包括本应只可由超级用户更改的那些。现在，<command>ALTER</command> 只移除用户有权更改的参数。
     </para>
    </listitem>

    <listitem>
     <para>
      避免后端关闭发生在需要向日志条目添加 <literal>CONTEXT</literal> 内容时导致可能的崩溃（Tom）
     </para>

     <para>
      某些情况下上下文打印函数会失败，因为到打印日志消息时当前事务已被回滚。
     </para>
    </listitem>

    <listitem>
     <para>
      为现代 Perl 版本更新 pl/perl 的 <filename>ppport.h</filename>（Andrew）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 pl/python 中的若干内存泄漏（Andreas Freund、Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      确保 <filename>contrib/pgstattuple</filename> 函数及时响应取消中断（Tatsuhito Kasahara）
     </para>
    </listitem>

    <listitem>
     <para>
      让服务器启动正确处理 <function>shmget()</function> 对已存在的共享内存段返回 <literal>EINVAL</literal> 的情形（Tom）
     </para>

     <para>
      此行为曾在包括 OS X 在内的 BSD 衍生内核上观察到。它导致完全误导性的启动失败，抱怨共享内存请求大小过大。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-7-4-28">
  <title>版本 7.4.28</title>

  <note>
  <title>发行日期</title>
  <simpara>2010-03-15</simpara>
 </note>

  <para>
   本次发布包含对 7.4.27 的多方面修复。有关 7.4 大版本中新特性的信息，请参见<xref linkend="release-7-4">。
  </para>

  <para>
      <productname>PostgreSQL</productname> 社区将于 2010 年 7 月停止对 7.4.X 版本系列的更新。建议用户尽快更新到较新的版本分支。
     </para>

  <sect2>
   <title>迁移到版本 7.4.28</title>

   <para>
      对于运行 7.4.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 7.4.26 的版本升级，请参阅 7.4.26 的发行说明。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      新增配置参数 <varname>ssl_renegotiation_limit</varname>，控制对 SSL 连接进行会话密钥重协商的频率（Magnus）
     </para>

     <para>
      可以把它设为零以完全禁用重协商，这在使用有缺陷的 SSL 库时可能是必需的。特别是，某些厂商发布的 CVE-2009-3555 临时补丁会导致重协商尝试失败。
     </para>
    </listitem>

    <listitem>
     <para>
      让 <type>bit</type> 类型的 <function>substring()</function> 把任何负长度都视为<quote>字符串的其余全部</quote>（Tom）
     </para>

     <para>
      以前的编码只对 -1 这样处理，对其他负值会产生无效的结果值，可能导致崩溃（CVE-2010-0442）。
     </para>
    </listitem>

    <listitem>
     <para>
      修复若干病态缓慢的正则表达式匹配情形（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      读取 <filename>pg_hba.conf</filename> 及相关文件时，如果 <literal>@</literal> 出现在引号内，不要把 <literal>@something</literal> 当作文件包含请求；另外，永远不要把单独的 <literal>@</literal> 当作文件包含请求（Tom）
     </para>

     <para>
      这防止了角色名或数据库名以 <literal>@</literal> 开头时的古怪行为。如果需要包含路径名包含空格的文件，仍然可以做到，但必须写 <literal>@"/path to/file"</literal> 而不是把引号放在整个构造之外。
     </para>
    </listitem>

    <listitem>
     <para>
      防止在某些平台上把目录命名为 <filename>pg_hba.conf</filename> 及相关文件中的包含目标时出现无限循环（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      确保 PL/Tcl 完全初始化 Tcl 解释器（Tom）
     </para>

     <para>
      此疏漏唯一已知的症状是使用 Tcl 8.5 或更高版本时 Tcl 的 <literal>clock</literal> 命令行为异常。
     </para>
    </listitem>

    <listitem>
     <para>
      防止在向 <function>dblink_build_sql_*</function> 函数指定过多键列时 <filename>contrib/dblink</filename> 崩溃（Rushabh Lathia、Joe Conway）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-7-4-27">
  <title>版本 7.4.27</title>

  <note>
  <title>发行日期</title>
  <simpara>2009-12-14</simpara>
 </note>

  <para>
   本次发布包含对 7.4.26 的多方面修复。有关 7.4 大版本中新特性的信息，请参见<xref linkend="release-7-4">。
  </para>

  <sect2>
   <title>迁移到版本 7.4.27</title>

   <para>
      对于运行 7.4.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 7.4.26 的版本升级，请参阅 7.4.26 的发行说明。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      防范索引函数更改会话局部状态导致的间接安全威胁（Gurjeet Singh、Tom）
     </para>

     <para>
      此变更防止据称不可变的索引函数可能破坏超级用户的会话（CVE-2009-4136）。
     </para>
    </listitem>

    <listitem>
     <para>
      拒绝在公共名（CN）字段中包含嵌入空字节的 SSL 证书（Magnus）
     </para>

     <para>
      这防止在 SSL 验证期间证书与服务器或客户端名称的非预期匹配（CVE-2009-4034）。
     </para>
    </listitem>

    <listitem>
     <para>
      修复后端启动时缓存初始化期间可能的崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      防止信号在不安全的时刻中断 <literal>VACUUM</literal>（Alvaro）
     </para>

     <para>
      此修复防止了 <literal>VACUUM FULL</literal> 在已提交其元组移动后被取消时的 PANIC，以及普通 <literal>VACUUM</literal> 在截断表之后被中断时的瞬态错误。
     </para>
    </listitem>

    <listitem>
     <para>
      修复哈希表大小计算中因整数溢出而可能的崩溃（Tom）
     </para>

     <para>
      这可能发生在规划器对哈希连接结果大小的估计极大时。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <type>inet</type>/<type>cidr</type> 比较中非常罕见的崩溃（Chris Mikkelson）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PAM 口令处理使其更健壮（Tom）
     </para>

     <para>
      已知以前的代码在 Linux <literal>pam_krb5</literal> PAM 模块与 Microsoft Active Directory 域控制器组合下失败。它在其他地方可能也有问题，因为它对 PAM 栈会传递给它什么参数做了无根据的假设。
     </para>
    </listitem>

    <listitem>
     <para>
      让 postmaster 忽略连接请求包中的任何 <literal>application_name</literal> 参数，以改进与未来 libpq 版本的兼容性（Tom）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-7-4-26">
  <title>版本 7.4.26</title>

  <note>
  <title>发行日期</title>
  <simpara>2009-09-09</simpara>
 </note>

  <para>
   本次发布包含对 7.4.25 的多方面修复。有关 7.4 大版本中新特性的信息，请参见<xref linkend="release-7-4">。
  </para>

  <sect2>
   <title>迁移到版本 7.4.26</title>

   <para>
      对于运行 7.4.X 的用户，不需要进行转储/恢复。不过，如果你在 <type>interval</type> 列上有任何哈希索引，必须在更新到 7.4.26 后对它们执行 <command>REINDEX</command>。另外，如果你是从早于 7.4.11 的版本升级，请参阅 7.4.11 的发行说明。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      不允许在安全定义者函数内使用 <command>RESET ROLE</command> 和 <command>RESET SESSION AUTHORIZATION</command>（Tom、Heikki）
     </para>

     <para>
      这覆盖了先前禁止在安全定义者函数内使用 <command>SET ROLE</command> 和 <command>SET SESSION AUTHORIZATION</command> 的补丁中遗漏的一种情形。（参见 CVE-2007-6600）
     </para>
    </listitem>

    <listitem>
     <para>
      修复出现在外层聚合函数参数中的子 SELECT 的处理（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <type>interval</type> 数据类型的哈希计算（Tom）
     </para>

     <para>
      这纠正了 interval 值上哈希连接的错误结果。它还更改了 interval 列上哈希索引的内容。如果你有任何这样的索引，必须在更新后对它们执行 <command>REINDEX</command>。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <replaceable>x</replaceable> 超过 200 万且使用整数日期时间时 <literal>INTERVAL '<replaceable>x</replaceable> ms'</literal> 的溢出（Alex Hunsaker）
     </para>
    </listitem>

    <listitem>
     <para>
      修复点与线段之间距离的计算（Tom）
     </para>

     <para>
      这导致多个几何操作符的结果不正确。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <type>money</type> 数据类型，使其在货币金额没有小数位的区域设置（例如日本）中工作（Itagaki Takahiro）
     </para>
    </listitem>

    <listitem>
     <para>
      正确地对像 <literal>00:12:57.9999999999999999999999999999</literal> 这样的日期时间输入四舍五入（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 GiST R-tree 操作符类中页分裂点选择不佳的问题（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 plperl 初始化中的可移植性问题（Andrew Dunstan）
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <application>libpq</application> 从 <command>COPY FROM STDIN</command> 期间错误中恢复的代码的健壮性（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      避免在两个库都安装时包含相互冲突的 readline 和 editline 头文件（Zdenek Kotala）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-7-4-25">
  <title>版本 7.4.25</title>

  <note>
  <title>发行日期</title>
  <simpara>2009-03-16</simpara>
 </note>

  <para>
   本次发布包含对 7.4.24 的多方面修复。有关 7.4 大版本中新特性的信息，请参见<xref linkend="release-7-4">。
  </para>

  <sect2>
   <title>迁移到版本 7.4.25</title>

   <para>
      对于运行 7.4.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 7.4.11 的版本升级，请参阅 7.4.11 的发行说明。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      防止编码转换失败时的错误递归崩溃（Tom）
     </para>

     <para>
      此变更扩展了上两个小版本中针对相关失败情形所做的修复。以前的修复是针对最初的问题报告量身定制的，但我们现在认识到，编码转换函数抛出的<emphasis>任何</emphasis>错误都可能在试图报告该错误时导致无限递归。因此，解决方案是：如果发现已处于递归错误报告情形，就禁用翻译和编码转换并报告错误消息的纯 ASCII 形式。（CVE-2009-0922）
     </para>
    </listitem>

    <listitem>
     <para>
      不允许 <command>CREATE CONVERSION</command> 为指定的转换函数使用错误的编码（Heikki）
     </para>

     <para>
      这防止了编码转换失败的一种可能情形。前一个变更是兜底，防范同一领域其他类型的失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>to_char()</function> 被给定与数据参数类型不适当的格式代码时的核心转储（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      把 <literal>MUST</literal>（毛里求斯岛夏令时）加入已知时区缩写的默认列表（Xavier Bugaud）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-7-4-24">
  <title>版本 7.4.24</title>

  <note>
  <title>发行日期</title>
  <simpara>2009-02-02</simpara>
 </note>

  <para>
   本次发布包含对 7.4.23 的多方面修复。有关 7.4 大版本中新特性的信息，请参见<xref linkend="release-7-4">。
  </para>

  <sect2>
   <title>迁移到版本 7.4.24</title>

   <para>
      对于运行 7.4.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 7.4.11 的版本升级，请参阅 7.4.11 的发行说明。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      改进 <function>headline()</function> 函数中对 URL 的处理（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <function>headline()</function> 函数对超长标题的处理（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      防止在为指定的编码对创建了错误转换函数的编码转换时可能的断言失败或错误转换（Tom、Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      避免 <command>VACUUM</command> 中对小表的不必要锁定（Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/tsearch2</filename> 的 <function>get_covers()</function> 函数中的未初始化变量（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>to_char()</function> 处理 <literal>TH</literal> 格式代码中的缺陷（Andreas Scherbaum）
     </para>
    </listitem>

    <listitem>
     <para>
      让所有文档在适当之处引用 <literal>pgsql-bugs</literal> 和/或 <literal>pgsql-hackers</literal>，而不是现已停用的 <literal>pgsql-ports</literal> 和 <literal>pgsql-patches</literal> 邮件列表（Tom）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-7-4-23">
  <title>版本 7.4.23</title>

  <note>
  <title>发行日期</title>
  <simpara>2008-11-03</simpara>
 </note>

  <para>
   本次发布包含对 7.4.22 的多方面修复。有关 7.4 大版本中新特性的信息，请参见<xref linkend="release-7-4">。
  </para>

  <sect2>
   <title>迁移到版本 7.4.23</title>

   <para>
      对于运行 7.4.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 7.4.11 的版本升级，请参阅 7.4.11 的发行说明。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      修复客户端编码无法表示本地化错误消息时的后端崩溃（Tom）
     </para>

     <para>
      我们此前处理过类似问题，但如果<quote>字符没有等价物</quote>消息本身无法转换，仍会失败。修复方法是在检测到这种情形时禁用本地化并发送纯 ASCII 错误消息。
     </para>
    </listitem>

    <listitem>
     <para>
      修复单个查询项匹配文本首词时 tsearch2 标题生成不正确的问题（Sushant Sinha）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <option>--enable-integer-datetimes</option> 构建中使用非 ISO datestyle 时 interval 值小数秒显示不当的问题（Ron Mayer）
     </para>
    </listitem>

    <listitem>
     <para>
      确保 <function>SPI_getvalue</function> 和 <function>SPI_getbinval</function> 在传入的元组和元组描述符列数不同时行为正确（Tom）
     </para>

     <para>
      当表添加或删除过列时这种情况是正常的，但这两个函数没有正确处理它。唯一可能的后果是错误的错误指示。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>ecpg</application> 对 <command>CREATE USER</command> 的解析（Michael）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-7-4-22">
  <title>版本 7.4.22</title>

  <note>
  <title>发行日期</title>
  <simpara>2008-09-22</simpara>
 </note>

  <para>
   本次发布包含对 7.4.21 的多方面修复。有关 7.4 大版本中新特性的信息，请参见<xref linkend="release-7-4">。
  </para>

  <sect2>
   <title>迁移到版本 7.4.22</title>

   <para>
      对于运行 7.4.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 7.4.11 的版本升级，请参阅 7.4.11 的发行说明。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      修复日期时间输入函数在 64 位平台上正确检测整数溢出（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      改进向 syslog 写很长日志消息的性能（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>SELECT DISTINCT ON</literal> 查询上游标反向扫描的缺陷（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复规划器，使其估计产生布尔结果的 <literal>GROUP BY</literal> 表达式总是产生两个组，而不考虑表达式的内容（Tom）
     </para>

     <para>
      对于某些布尔测试（如 <replaceable>col</replaceable> <literal>IS NULL</literal>），这比常规的 <literal>GROUP BY</literal> 估计准确得多。
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <application>pg_dump</application> 和 <application>pg_restore</application> 在发送 SQL 命令失败后的错误报告（Tom）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-7-4-21">
  <title>版本 7.4.21</title>

  <note>
  <title>发行日期</title>
  <simpara>2008-06-12</simpara>
 </note>

  <para>
      此发行版在 7.4.20 的基础上包含一个严重的缺陷修复。有关 7.4 主版本新特性的信息，请参阅<xref linkend="release-7-4">。
     </para>

  <sect2>
   <title>迁移到版本 7.4.21</title>

   <para>
      对于运行 7.4.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 7.4.11 的版本升级，请参阅 7.4.11 的发行说明。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      让 <function>pg_get_ruledef()</function> 给负常数加括号（Tom）
     </para>

     <para>
      在此修复之前，视图或规则中的负常数可能被转储成如 <literal>-42::integer</literal>，这有微妙的错误：由于操作符优先级规则，它应该是 <literal>(-42)::integer</literal>。通常这影响不大，但它可能与另一个最近的补丁相互作用，导致 <productname>PostgreSQL</productname> 拒绝原本有效的 <command>SELECT DISTINCT</command> 视图查询。由于这可能导致 <application>pg_dump</application> 输出无法重载，它被当作高优先级修复。转储输出确实不正确的已发布版本只有 8.3.1 和 8.2.7。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-7-4-20">
  <title>版本 7.4.20</title>

  <note>
  <title>发行日期</title>
  <simpara>
      从未发布
     </simpara>
 </note>

  <para>
   本次发布包含对 7.4.19 的多方面修复。有关 7.4 大版本中新特性的信息，请参见<xref linkend="release-7-4">。
  </para>

  <sect2>
   <title>迁移到版本 7.4.20</title>

   <para>
      对于运行 7.4.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 7.4.11 的版本升级，请参阅 7.4.11 的发行说明。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      修复 ISO-8859-5 与其他编码之间的转换对西里尔<quote>Yo</quote>字符（带两个点的 <literal>e</literal> 和 <literal>E</literal>）的处理（Sergey Burladyan）
     </para>
    </listitem>

    <listitem>
     <para>
      修复几个数据类型输入函数，它们允许结果中未使用的字节包含未初始化的不可预测值（Tom）
     </para>

     <para>
      这可能导致两个看起来相同的字面值不被视为相等的失败，导致解析器抱怨 <literal>ORDER BY</literal> 和 <literal>DISTINCT</literal> 表达式不匹配。
     </para>
    </listitem>

    <listitem>
     <para>
      修复正则表达式子串匹配（<literal>substring(<replaceable>string</replaceable> from <replaceable>pattern</replaceable>)</literal>）中的一个极端情形（Tom）
     </para>

     <para>
      问题发生在模式整体有匹配但用户指定了带括号的子表达式而该子表达式没有匹配时。例子是 <literal>substring('foo' from 'foo(bar)?')</literal>。由于 <literal>(bar)</literal> 未匹配，它应返回 NULL，但它错误地返回了整个模式的匹配（即 <literal>foo</literal>）。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>ecpg</application> 的 <function>PGTYPEStimestamp_sub()</function> 函数结果不正确的问题（Michael）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>DatumGetBool</literal> 宏，使其在 <application>gcc</application> 4.3 下不失败（Tom）
     </para>

     <para>
      此问题影响返回布尔的<quote>旧式</quote>（V0）C 函数。修复已在 8.3 中，但当时没有意识到需要向后移植。
     </para>
    </listitem>

    <listitem>
     <para>
      修复长期存在的 <command>LISTEN</command>/<command>NOTIFY</command> 竞态条件（Tom）
     </para>

     <para>
      极少数情况下，刚执行过 <command>LISTEN</command> 的会话可能收不到通知，即使按理应当收到，因为观察到执行 <command>NOTIFY</command> 的并发事务稍后提交了。
     </para>

     <para>
      此修复的一个副作用是：执行了尚未提交的 <command>LISTEN</command> 命令的事务如果去查看，将看不到该 <command>LISTEN</command> 在 <structname>pg_listener</structname> 中的行；以前能看到。这一行为从未被明确记录，但有些应用程序可能依赖旧行为。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>ORDER BY</literal> 和 <literal>GROUP BY</literal> 中常量表达式的显示（Tom）
     </para>

     <para>
      显式转换的常量会被错误显示。这例如可能导致视图定义在转储和重载期间损坏。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>libpq</application>，使其在 COPY OUT 期间正确处理 NOTICE 消息（Tom）
     </para>

     <para>
      只观察到此失败在用户定义数据类型的输出例程发出 NOTICE 时发生，但不能保证它不会因其他原因发生。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-7-4-19">
  <title>版本 7.4.19</title>

  <note>
  <title>发行日期</title>
  <simpara>2008-01-07</simpara>
 </note>

  <para>
      此发行版包含 7.4.18 之后的多种修复，包括对重要安全问题的修复。有关 7.4 主版本新特性的信息，请参阅<xref linkend="release-7-4">。
     </para>

  <sect2>
   <title>迁移到版本 7.4.19</title>

   <para>
      对于运行 7.4.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 7.4.11 的版本升级，请参阅 7.4.11 的发行说明。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      防止索引中的函数以运行 <command>VACUUM</command>、<command>ANALYZE</command> 等的用户的权限执行（Tom）
     </para>

     <para>
      每当产生新的表条目时，都会求值索引表达式和部分索引谓词中使用的函数。人们早就明白，如果修改不可信用户拥有的表，这会带来特洛伊木马代码执行的风险。（注意触发器、默认值、检查约束等也构成同类风险。）但索引中的函数带来额外危险，因为它们会由例程维护操作（如 <command>VACUUM FULL</command>）执行，而这些操作通常在超级用户账户下自动执行。例如，恶意用户可以通过设置特洛伊木马索引定义并等待下一次例程清理来以超级用户权限执行代码。修复安排标准维护操作（包括 <command>VACUUM</command>、<command>ANALYZE</command>、<command>REINDEX</command> 和 <command>CLUSTER</command>）以表所有者而非调用用户的身份执行，使用已用于 <literal>SECURITY DEFINER</literal> 函数的同一权限切换机制。为防止绕过此安全措施，现在禁止在 <literal>SECURITY DEFINER</literal> 上下文中执行 <command>SET SESSION AUTHORIZATION</command> 和 <command>SET ROLE</command>。（CVE-2007-6600）
     </para>
    </listitem>

    <listitem>
     <para>
      修复正则表达式包中的若干缺陷（Tom、Will Drewry）
     </para>

     <para>
      精心构造的正则表达式模式可能导致崩溃、无限或接近无限的循环和/或大量内存消耗，这些都对接受来自不可信来源的正则搜索模式的应用程序构成拒绝服务风险。（CVE-2007-4769、CVE-2007-4772、CVE-2007-6067）
     </para>
    </listitem>

    <listitem>
     <para>
      作为安全措施，要求使用 <filename>/contrib/dblink</filename> 的非超级用户只使用口令认证（Joe）
     </para>

     <para>
      7.4.18 中出现的修复并不完整，它只堵住了部分 <filename>dblink</filename> 函数的漏洞。（CVE-2007-6601、CVE-2007-3278）
     </para>
    </listitem>

    <listitem>
     <para>
      修复某些 <literal>WHERE false AND var IN (SELECT ...)</literal> 情形下的规划器失败（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复使用多字节数据库编码时 <function>translate()</function> 可能的崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PL/Python 在长异常消息上不崩溃（Alvaro）
     </para>
    </listitem>

    <listitem>
     <para>
      <application>ecpg</application> 解析器修复（Michael）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <filename>contrib/tablefunc</filename> 的 <function>crosstab()</function> 把 NULL rowid 本身作为一个类别处理，而不是崩溃（Joe）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <type>tsvector</type> 和 <type>tsquery</type> 输出例程以正确转义反斜杠（Teodor、Bruce）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>to_tsvector()</function> 在超长输入字符串上的崩溃（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      要求在重新生成 <command>configure</command> 脚本时使用特定版本的 <productname>Autoconf</productname>（Peter）
     </para>

     <para>
      这只影响开发者和打包者。此变更是为了防止意外使用未经测试的 <productname>Autoconf</productname> 与 <productname>PostgreSQL</productname> 版本组合。如果确实想使用不同的 <productname>Autoconf</productname> 版本，可以移除版本检查，但结果是否可用由你自己负责。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-7-4-18">
  <title>版本 7.4.18</title>

  <note>
  <title>发行日期</title>
  <simpara>2007-09-17</simpara>
 </note>

  <para>
      此发行版包含 7.4.17 之后的修复。有关 7.4 主版本新特性的信息，请参阅<xref linkend="release-7-4">。
     </para>

  <sect2>
   <title>迁移到版本 7.4.18</title>

   <para>
      对于运行 7.4.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 7.4.11 的版本升级，请参阅 7.4.11 的发行说明。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      防止事务插入行后在同一表上并发 <command>VACUUM</command> 接近结束时中止导致的索引损坏（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <command>CREATE DOMAIN ... DEFAULT NULL</command> 正确工作（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <acronym>SSL</acronym> 错误消息的过度记录（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <varname>log_min_error_statement</varname> 日志记录内存耗尽时的崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      防止 <command>CLUSTER</command> 因试图处理其他会话的临时表而失败（Alvaro）
     </para>
    </listitem>

    <listitem>
     <para>
      作为安全措施，要求使用 <filename>/contrib/dblink</filename> 的非超级用户只使用口令认证（Joe）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-7-4-17">
  <title>版本 7.4.17</title>

  <note>
  <title>发行日期</title>
  <simpara>2007-04-23</simpara>
 </note>

  <para>
      此发行版包含 7.4.16 之后的修复，包括一个安全修复。有关 7.4 主版本新特性的信息，请参阅<xref linkend="release-7-4">。
     </para>

  <sect2>
   <title>迁移到版本 7.4.17</title>

   <para>
      对于运行 7.4.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 7.4.11 的版本升级，请参阅 7.4.11 的发行说明。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
    <para>
      支持在 <varname>search_path</varname> 中显式放置临时表模式，并禁用为函数和操作符搜索它（Tom）
     </para>
    <para>
      这是允许安全定义者函数设置真正安全的 <varname>search_path</varname> 值所必需的。没有它，非特权 SQL 用户可以利用临时对象以安全定义者函数的权限执行代码（CVE-2007-2138）。更多信息请参阅 <command>CREATE FUNCTION</command>。
     </para>
    </listitem>

    <listitem>
    <para>
      <filename>/contrib/tsearch2</filename> 崩溃修复（Teodor）
     </para>
    </listitem>

    <listitem>
    <para>
      修复 <command>VACUUM FULL</command> 处理 <command>UPDATE</command> 链方式中的潜在数据损坏缺陷（Tom、Pavan Deolasee）
     </para>
    </listitem>

    <listitem>
    <para>
      修复哈希索引扩容期间的 PANIC（7.4.15 中引入的缺陷）（Tom）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-7-4-16">
  <title>版本 7.4.16</title>

  <note>
  <title>发行日期</title>
  <simpara>2007-02-05</simpara>
 </note>

  <para>
      此发行版包含 7.4.15 之后的多种修复，包括一个安全修复。有关 7.4 主版本新特性的信息，请参阅<xref linkend="release-7-4">。
     </para>

  <sect2>
   <title>迁移到版本 7.4.16</title>

   <para>
      对于运行 7.4.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 7.4.11 的版本升级，请参阅 7.4.11 的发行说明。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
    <para>
      移除允许已连接用户读取后端内存的安全漏洞（Tom）
     </para>
    <para>
      该漏洞涉及抑制 SQL 函数返回其声明数据类型的常规检查，或更改 SQL 函数中使用的表列的数据类型（CVE-2007-0555）。此错误很容易被利用来导致后端崩溃，而且原则上可能被用来读取用户不应能访问的数据库内容。
     </para>
    </listitem>

    <listitem>
    <para>
      修复罕见的缺陷：btree 索引页分裂可能因选择不可行的分裂点而失败（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
    <para>
      修复由 <literal>UNION</literal> 触发的罕见 Assert() 崩溃（Tom）
     </para>
    </listitem>

    <listitem>
    <para>
      收紧对超过三字节的 UTF8 序列的多字节字符处理的安全性（Tom）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-7-4-15">
  <title>版本 7.4.15</title>

  <note>
  <title>发行日期</title>
  <simpara>2007-01-08</simpara>
 </note>

  <para>
   本次发布包含对 7.4.14 的多方面修复。有关 7.4 大版本中新特性的信息，请参见<xref linkend="release-7-4">。
  </para>

  <sect2>
   <title>迁移到版本 7.4.15</title>

   <para>
      对于运行 7.4.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 7.4.11 的版本升级，请参阅 7.4.11 的发行说明。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      改进 AIX 上 <function>getaddrinfo()</function> 的处理（Tom）
     </para>

     <para>
      这修复了启动统计收集器等问题。
     </para>
    </listitem>

     <listitem>
      <para>
      修复 <command>VACUUM</command> 中的<quote>未能重新找到父键</quote>错误（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      修复影响多 GB 哈希索引的缺陷（Tom）
     </para>
     </listitem>

    <listitem>
     <para>
      修复构造由多个空元素组成的 <literal>ARRAY[]</literal> 时的错误（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      对于新的 <application>initdb</application> 安装，<function>to_number()</function> 和 <function>to_char(numeric)</function> 现在是 <literal>STABLE</literal> 而不是 <literal>IMMUTABLE</literal>（Tom）
     </para>

     <para>
      这是因为 <varname>lc_numeric</varname> 可能改变这些函数的输出。
     </para>
    </listitem>

    <listitem>
     <para>
      改进使用括号的正则表达式的索引利用（Tom）
     </para>

     <para>
      这同时也改进了 <application>psql</application> 的 <literal>\d</literal> 性能。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-7-4-14">
  <title>版本 7.4.14</title>

  <note>
  <title>发行日期</title>
  <simpara>2006-10-16</simpara>
 </note>

  <para>
   本次发布包含对 7.4.13 的多方面修复。有关 7.4 大版本中新特性的信息，请参见<xref linkend="release-7-4">。
  </para>

  <sect2>
   <title>迁移到版本 7.4.14</title>

   <para>
      对于运行 7.4.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 7.4.11 的版本升级，请参阅 7.4.11 的发行说明。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

<itemizedlist>
<listitem><para>
      修复把无类型字面值当作 ANYARRAY 时的核心转储
     </para></listitem>
<listitem><para>
      修复 <function>string_to_array()</function>，处理分隔符字符串的重叠匹配
     </para>
<para>
      例如 <literal>string_to_array('123xx456xxx789', 'xx')</literal>。
     </para></listitem>
<listitem><para>
      修复 <application>psql</application> 的 <literal>\d</literal> 命令模式匹配的极端情形
     </para></listitem>
<listitem><para>
      修复 /contrib/ltree 中损坏索引的缺陷（Teodor）
     </para></listitem>
<listitem><para>
      修复 /contrib/dbmirror 中的反斜杠转义
     </para></listitem>
<listitem><para>
      针对美国 DST 法规的最近变更调整回归测试
     </para> </listitem>
</itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-7-4-13">
  <title>版本 7.4.13</title>

  <note>
  <title>发行日期</title>
  <simpara>2006-05-23</simpara>
 </note>

  <para>
      此发行版包含 7.4.12 之后的多种修复，包括针对极其严重安全问题的补丁。有关 7.4 主版本新特性的信息，请参阅<xref linkend="release-7-4">。
     </para>

  <sect2>
   <title>迁移到版本 7.4.13</title>

   <para>
      对于运行 7.4.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 7.4.11 的版本升级，请参阅 7.4.11 的发行说明。
     </para>

   <para>
      要完全防范 CVE-2006-2313 和 CVE-2006-2314 所述的 SQL 注入攻击，可能需要更改应用程序代码。如果你有把不可信字符串嵌入 SQL 命令的应用程序，应尽快检查它们，确保使用了推荐的转义技术。在大多数情况下，应用程序应使用库或驱动提供的子例程（如 <application>libpq</application> 的 <function>PQescapeStringConn()</function>）来执行字符串转义，而不是依靠<foreignphrase>临时</foreignphrase>代码来做。
     </para>
  </sect2>

  <sect2>
   <title>变更</title>

<itemizedlist>
<listitem><para>
      更改服务器，在所有情形下拒绝无效编码的多字节字符（Tatsuo、Tom）
     </para>
<para>
      虽然 <productname>PostgreSQL</productname> 向这个方向发展已有一段时间，但现在检查统一应用于所有编码和所有文本输入，而且总是错误而不只是警告。此变更可防范 CVE-2006-2313 所述类型的 SQL 注入攻击。
     </para></listitem>

<listitem><para>
      拒绝字符串字面量中对 <literal>\'</literal> 的不安全使用
     </para>
<para>
      作为针对 CVE-2006-2314 所述类型 SQL 注入攻击的服务器端防御，服务器现在只接受 <literal>''</literal> 而不接受 <literal>\'</literal> 作为 SQL 字符串字面量中 ASCII 单引号的表示。默认情况下，只有当 <varname>client_encoding</varname> 设为仅客户端编码（SJIS、BIG5、GBK、GB18030 或 UHC）时才拒绝 <literal>\'</literal>，这正是可能发生 SQL 注入的场景。需要时可用新的配置参数 <varname>backslash_quote</varname> 调整此行为。注意，要完全防范 CVE-2006-2314 可能需要客户端更改；<varname>backslash_quote</varname> 的目的之一是让不安全的客户端明显地不安全。
     </para></listitem>

<listitem><para>
      修改 <application>libpq</application> 的字符串转义例程，使其感知编码问题和 <varname>standard_conforming_strings</varname>
     </para>
<para>
      这为使用 <application>libpq</application> 的应用程序修复了 CVE-2006-2313 和 CVE-2006-2314 所述的安全问题，并使它们为将来转向 SQL 标准字符串字面量语法做好准备。并发使用多个 <productname>PostgreSQL</productname> 连接的应用程序应迁移到 <function>PQescapeStringConn()</function> 和 <function>PQescapeByteaConn()</function>，以确保为每个数据库连接中使用的设置正确转义。以<quote>手工</quote>方式做字符串转义的应用程序应改为依靠库例程。
     </para></listitem>

<listitem><para>
      修复一些不正确的编码转换函数
     </para>
<para>
      <function>win1251_to_iso</function>、<function>alt_to_iso</function>、<function>euc_tw_to_big5</function>、<function>euc_tw_to_mic</function>、<function>mic_to_euc_tw</function> 都在不同程度上损坏。
     </para></listitem>

<listitem><para>
      清理字符串中偶然残留的 <literal>\'</literal> 使用（Bruce、Jan）
     </para></listitem>

<listitem><para>
      修复有时导致 OR 索引扫描漏掉本应返回的行的缺陷
     </para></listitem>

<listitem><para>
      修复 btree 索引被截断情形的 WAL 重放
     </para></listitem>

<listitem><para>
      修复涉及 <literal>|</literal> 的模式的 <literal>SIMILAR TO</literal>（Tom）
     </para></listitem>

<listitem><para>
      修复服务器正确使用自定义 DH SSL 参数（Michael Fuhr）
     </para></listitem>

<listitem><para>
      修复 Intel Mac 上的 Bonjour（Ashley Clark）
     </para></listitem>

<listitem><para>
      修复各种小的内存泄漏
     </para></listitem>
</itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-7-4-12">
  <title>版本 7.4.12</title>

  <note>
  <title>发行日期</title>
  <simpara>2006-02-14</simpara>
 </note>

  <para>
   本次发布包含对 7.4.11 的多方面修复。有关 7.4 大版本中新特性的信息，请参见<xref linkend="release-7-4">。
  </para>

  <sect2>
   <title>迁移到版本 7.4.12</title>

   <para>
      对于运行 7.4.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 7.4.11 的版本升级，请参阅 7.4.11 的发行说明。
     </para>
  </sect2>

  <sect2>
   <title>变更</title>

<itemizedlist>

<listitem><para>
      修复 <command>SET SESSION AUTHORIZATION</command> 中可能的崩溃（CVE-2006-0553）
     </para>
<para>
      如果服务器编译时启用了断言（这不是默认），非特权用户可以使服务器进程崩溃，导致对其他用户的瞬时拒绝服务。感谢 Akio Ishida 报告此问题。
     </para></listitem>

<listitem><para>
      修复自插入行的行可见性逻辑缺陷（Tom）
     </para>
<para>
      极少数情况下，当前命令插入的行可能被看作已经有效，而不应该如此。修复 7.4.9 和 7.3.11 版本中引入的缺陷。
     </para></listitem>

<listitem><para>
      修复 pg_clog 文件创建期间可能导致<quote>文件已存在</quote>错误的竞态条件（Tom）
     </para></listitem>

<listitem><para>
      为预备语句中的 <literal>UNKNOWN</literal> 参数正确检查 <literal>DOMAIN</literal> 约束（Neil）
     </para></listitem>

<listitem><para>
      修复以允许恢复有对自定义操作符跨模式引用的转储（Tom）
     </para></listitem>

<listitem><para>
      修复 configure 期间测试 <function>finite</function> 和 <function>isinf</function> 存在性的可移植性问题（Tom）
     </para></listitem>

</itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-7-4-11">
  <title>版本 7.4.11</title>

  <note>
  <title>发行日期</title>
  <simpara>2006-01-09</simpara>
 </note>

  <para>
   本次发布包含对 7.4.10 的多方面修复。有关 7.4 大版本中新特性的信息，请参见<xref linkend="release-7-4">。
  </para>

  <sect2>
   <title>迁移到版本 7.4.11</title>

   <para>
      对于运行 7.4.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 7.4.8 的版本升级，请参阅 7.4.8 的发行说明。另外，如果你受到下述区域设置或 <application>plperl</application> 问题的影响，可能需要在更新后对文本列上的索引执行 <command>REINDEX</command>。
     </para>
  </sect2>

  <sect2>
   <title>变更</title>

<itemizedlist>

<listitem><para>
      修复在事务之外或失败事务中发出协议级 Describe 消息的问题（Tom）
     </para></listitem>

<listitem><para>
      修复把不同字符组合视为相等的区域设置（如匈牙利语）下的字符串比较（Tom）
     </para>
<para>
      这可能需要 <command>REINDEX</command> 来修复文本列上的现有索引。
     </para></listitem>

<listitem><para>
      在 postmaster 启动期间设置区域设置环境变量，以确保 <application>plperl</application> 之后不会更改区域设置
     </para>
<para>
      这修复了 <application>postmaster</application> 启动时环境变量指定的区域设置与 <application>initdb</application> 所被告知的不同时发生的问题。在这些条件下，任何 <application>plperl</application> 的使用都可能导致索引损坏。如果你遇到过这种情况，可能需要 <command>REINDEX</command> 来修复文本列上的现有索引。
     </para></listitem>

<listitem><para>
      修复某些很少使用的亚洲多字节字符集中 strpos() 和正则表达式处理的长期缺陷（Tatsuo）
     </para></listitem>

<listitem><para>
      修复 <filename>/contrib/pgcrypto</filename> gen_salt 中的缺陷，该缺陷使 MD5 和 XDES 算法不能使用全部可用盐空间（Marko Kreen、Solar Designer）
     </para>
<para>
      Blowfish 和标准 DES 的盐不受影响。
     </para></listitem>

<listitem><para>
      修复 <filename>/contrib/dblink</filename>，当指定的列数与查询实际返回的列数不同时抛出错误而不是崩溃（Joe）
     </para></listitem>

</itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-7-4-10">
  <title>版本 7.4.10</title>

  <note>
  <title>发行日期</title>
  <simpara>2005-12-12</simpara>
 </note>

  <para>
   本次发布包含对 7.4.9 的多方面修复。有关 7.4 大版本中新特性的信息，请参见<xref linkend="release-7-4">。
  </para>

  <sect2>
   <title>迁移到版本 7.4.10</title>

   <para>
      对于运行 7.4.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 7.4.8 的版本升级，请参阅 7.4.8 的发行说明。
     </para>
  </sect2>

  <sect2>
   <title>变更</title>

<itemizedlist>

<listitem><para>
      修复事务日志管理中的竞态条件
     </para>
<para>
      存在一个窄窗口，其中可能为错误的页发起 I/O 操作，导致断言失败或数据损坏。
     </para>
</listitem>

<listitem><para>
      防止客户端在当前事务已中止时发送 Bind 协议消息导致的失败
     </para></listitem>

<listitem><para>
      <filename>/contrib/ltree</filename> 修复（Teodor）
     </para></listitem>

<listitem><para>
      AIX 和 HPUX 编译修复（Tom）
     </para></listitem>

<listitem><para>
      修复外连接的长期规划错误
     </para>
<para>
      此缺陷有时导致虚假错误 <quote>RIGHT JOIN 只支持可归并连接的连接条件</quote>。
     </para></listitem>

<listitem><para>
      防止表被删除后 <application>pg_autovacuum</application> 的核心转储
     </para></listitem>
</itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-7-4-9">
  <title>版本 7.4.9</title>

  <note>
  <title>发行日期</title>
  <simpara>2005-10-04</simpara>
 </note>

  <para>
   本次发布包含对 7.4.8 的多方面修复。有关 7.4 大版本中新特性的信息，请参见<xref linkend="release-7-4">。
  </para>

  <sect2>
   <title>迁移到版本 7.4.9</title>

   <para>
      对于运行 7.4.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 7.4.8 的版本升级，请参阅 7.4.8 的发行说明。
     </para>
  </sect2>

  <sect2>
   <title>变更</title>

<itemizedlist>
<listitem><para>
      修复允许 <command>VACUUM</command> 过早移除 <literal>ctid</literal> 链的错误，并在跟随 <literal>ctid</literal> 链接的代码中添加更多检查
     </para>
<para>
      这修复了一个在极罕见情况下可能导致崩溃的长期问题。
     </para></listitem>
<listitem><para>
      修复 <type>CHAR()</type> 在使用多字节字符集时正确填充空格到指定长度（Yoshiyuki Asaba）
     </para>
<para>
      在以前的版本中，<type>CHAR()</type> 的填充是不正确的，因为它只填充到指定的字节数而不考虑存储了多少字符。
     </para></listitem>
<listitem><para>
      修正 <command>COPY</command> 中只读事务测试的方向
     </para>
<para>
      以前的代码禁止 <command>COPY TO</command>，而它应禁止的是 <command>COPY FROM</command>。
     </para></listitem>
<listitem><para>
      修复只引用内侧关系的外连接 ON 子句的规划问题
     </para></listitem>
<listitem><para>
      对 <literal>x FULL JOIN y ON true</literal> 极端情形的进一步修复
     </para></listitem>
<listitem><para>
      让 <function>array_in</function> 和 <function>array_recv</function> 更严格地验证其 OID 参数
     </para></listitem>
<listitem><para>
      修复列 <literal>a</literal> 上有 GiST 索引时 <literal>UPDATE a=... WHERE a...</literal> 之类查询丢行的问题
     </para></listitem>
<listitem><para>
      改进日期时间解析的健壮性
     </para></listitem>
<listitem><para>
      改进对部分写入 WAL 页的检查
     </para></listitem>
<listitem><para>
      改进启用 SSL 时信号处理的健壮性
     </para></listitem>
<listitem><para>
      postmaster 启动期间不要试图打开超过 <literal>max_files_per_process</literal> 个文件
     </para></listitem>
<listitem><para>
      各种内存泄漏修复
     </para></listitem>
<listitem><para>
      各种可移植性改进
     </para></listitem>
<listitem><para>
      修复 PL/pgSQL 在变量为按引用传递类型时正确处理 <literal>var := var</literal>
     </para></listitem>
<listitem><para>
      更新 <filename>contrib/tsearch2</filename> 以使用当前的 Snowball 代码
     </para></listitem>
</itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-7-4-8">
  <title>版本 7.4.8</title>

  <note>
  <title>发行日期</title>
  <simpara>2005-05-09</simpara>
 </note>

  <para>
      此发行版包含 7.4.7 之后的多种修复，包括几个与安全相关的问题。有关 7.4 主版本新特性的信息，请参阅<xref linkend="release-7-4">。
     </para>

  <sect2>
   <title>迁移到版本 7.4.8</title>

   <para>
      对于运行 7.4.X 的用户，不需要进行转储/恢复。不过，它是处理在 7.4.X 系统目录初始内容中发现的两个重要安全问题的可能方法之一。用 7.4.8 的 initdb 做转储/initdb/重载序列会自动纠正这些问题。
     </para>

   <para>
      较大的安全问题是内建字符集编码转换函数可以由非特权用户从 SQL 命令调用，但这些函数并非为此设计，对恶意选择的参数不安全。修复包括更改这些函数的声明参数列表，使其不能再从 SQL 命令调用。（这不影响编码转换机制对它们的正常使用。）
     </para>

   <para>
      较小的问题是 <filename>contrib/tsearch2</filename> 模块创建了几个被错误声明为返回 <type>internal</type> 的函数，而它们并不接受 <type>internal</type> 参数。这破坏了所有使用 <type>internal</type> 参数的函数的类型安全。
     </para>

   <para>
      强烈建议所有安装通过 initdb 或按照下述手工修复程序修复这些错误。这些错误至少允许非特权数据库用户使其服务器进程崩溃，而且可能允许非特权用户获得数据库超级用户的权限。
     </para>

   <para>
    如果你不想做 initdb，请执行以下程序。作为数据库超级用户，执行：

<programlisting>
BEGIN;
UPDATE pg_proc SET proargtypes[3] = 'internal'::regtype
WHERE pronamespace = 11 AND pronargs = 5
     AND proargtypes[2] = 'cstring'::regtype;
-- The command should report having updated 90 rows;
-- if not, rollback and investigate instead of committing!
COMMIT;
</programlisting>

    然后，如果你安装了 <filename>contrib/tsearch2</filename>，执行：

<programlisting>
BEGIN;
UPDATE pg_proc SET proargtypes[0] = 'internal'::regtype
WHERE oid IN (
   'dex_init(text)'::regprocedure,
   'snb_en_init(text)'::regprocedure,
   'snb_ru_init(text)'::regprocedure,
   'spell_init(text)'::regprocedure,
   'syn_init(text)'::regprocedure
);
-- The command should report having updated 5 rows;
-- if not, rollback and investigate instead of committing!
COMMIT;
</programlisting>

    如果此命令失败并显示类似 <quote>function
    "dex_init(text)" does not exist</quote> 的消息，则要么该数据库中未安装 <filename>tsearch2</filename>，要么你已经做过此更新。
   </para>

   <para>
    上述程序必须在安装的<emphasis>每个</emphasis>数据库中执行，包括 <literal>template1</literal>，理想情况下也包括 <literal>template0</literal>。如果不修复模板数据库，之后创建的任何数据库都会包含相同的错误。<literal>template1</literal> 可以像任何其他数据库一样修复，但修复 <literal>template0</literal> 需要额外的步骤。首先，从任意数据库发出：
<programlisting>
UPDATE pg_database SET datallowconn = true WHERE datname = 'template0';
</programlisting>
     接下来连接到 <literal>template0</literal> 并执行上述修复程序。最后执行：
<programlisting>
-- re-freeze template0:
VACUUM FREEZE;
-- and protect it against future alterations:
UPDATE pg_database SET datallowconn = false WHERE datname = 'template0';
</programlisting>
   </para>
  </sect2>

  <sect2>
   <title>变更</title>

<itemizedlist>
<listitem><para>
      更改编码函数签名以防止误用
     </para></listitem>
<listitem><para>
      更改 <filename>contrib/tsearch2</filename> 以避免不安全地使用 <type>INTERNAL</type> 函数结果
     </para></listitem>
<listitem><para>
      修复古老的竞态条件，它允许事务在某些用途（如 SELECT FOR UPDATE）上比其他用途稍早被视为已提交
     </para>
<para>
      这是一个极其严重的缺陷，因为它可能导致应用程序短暂看到表面的数据不一致。
     </para></listitem>
<listitem><para>
      修复关系扩展与 VACUUM 之间的竞态条件
     </para>
<para>
      这在理论上可能导致丢失一页刚插入的数据，尽管该情形概率似乎很低。没有已知案例造成超过断言失败的后果。
     </para></listitem>
<listitem><para>
      修复 <type>TIME WITH TIME ZONE</type> 值的比较
     </para>
<para>
      在使用了 <literal>--enable-integer-datetimes</literal> 配置开关的情形下比较代码是错误的。注意：如果你在 <type>TIME WITH TIME ZONE</type> 列上有索引，安装此更新后需要对它执行 <command>REINDEX</command>，因为该修复纠正了列值的排序顺序。
     </para></listitem>
<listitem><para>
      修复 <type>TIME WITH TIME ZONE</type> 值的 <function>EXTRACT(EPOCH)</function>
     </para></listitem>
<listitem><para>
      修复 <type>INTERVAL</type> 值负小数秒的错误显示
     </para>
<para>
      此错误只在使用了 <literal>--enable-integer-datetimes</literal> 配置开关时发生。
     </para></listitem>
<listitem><para>
      确保后端关闭期间完成的操作被统计收集器计数
     </para>
<para>
      这有望解决关于 <application>pg_autovacuum</application> 不够频繁地清理系统目录的报告——它没有被通知后端退出期间临时表删除导致的目录删除。
     </para></listitem>
<listitem><para>
      plpgsql 中的额外缓冲区溢出检查（Neil）
     </para></listitem>
<listitem><para>
      修复 pg_dump，正确转储包含 <literal>%</literal> 的触发器名（Neil）
     </para></listitem>
<listitem><para>
      修复 <filename>contrib/pgcrypto</filename> 在较新 OpenSSL 构建上的问题（Marko Kreen）
     </para></listitem>
<listitem><para>
      <filename>contrib/intagg</filename> 的更多 64 位修复
     </para></listitem>
<listitem><para>
      防止对返回 <type>RECORD</type> 的函数的错误优化
     </para></listitem>
<listitem><para>
      防止 <function>to_char(interval)</function> 在月份相关格式上转储核心
     </para></listitem>
<listitem><para>
      防止 <literal>COALESCE(NULL,NULL)</literal> 的崩溃
     </para></listitem>
<listitem><para>
      修复 <function>array_map</function>，使其正确调用 PL 函数
     </para></listitem>
<listitem><para>
      修复 <command>ALTER DATABASE RENAME</command> 中的权限检查
     </para></listitem>
<listitem><para>
      修复 <command>ALTER LANGUAGE RENAME</command>
     </para></listitem>
<listitem><para>
      让 <function>RemoveFromWaitQueue</function> 清理自身
     </para>
<para>
      这修复了一个锁管理错误，它只在事务被踢出锁等待（通常由查询取消）且锁持有者随后在极窄的窗口内释放锁时可见。
     </para></listitem>
<listitem><para>
      修复 <command>INSERT ... SELECT</command> 中出现无类型参数的问题
     </para></listitem>
<listitem><para>
      修复 <command>ALTER TABLE SET WITHOUT OIDS</command> 之后 <command>CLUSTER</command> 的失败
     </para></listitem>
</itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-7-4-7">
  <title>版本 7.4.7</title>

  <note>
  <title>发行日期</title>
  <simpara>2005-01-31</simpara>
 </note>

  <para>
      此发行版包含 7.4.6 之后的多种修复，包括几个与安全相关的问题。有关 7.4 主版本新特性的信息，请参阅<xref linkend="release-7-4">。
     </para>

  <sect2>
   <title>迁移到版本 7.4.7</title>

   <para>
    对于运行 7.4.X 的用户，不需要进行转储/恢复。</para>
  </sect2>

  <sect2>
   <title>变更</title>

<itemizedlist>
<listitem><para>
      不允许非超级用户使用 <command>LOAD</command>
     </para>
<para>
      在会自动执行共享库初始化函数的平台（至少包括 Windows 和基于 ELF 的 Unix）上，<command>LOAD</command> 可被用来让服务器执行任意代码。感谢 NGS Software 报告此问题。
     </para></listitem>
<listitem><para>
      检查聚合函数的创建者是否有权执行指定的转换函数
     </para>
<para>
      这一疏漏使得绕过函数 EXECUTE 权限的拒绝成为可能。
     </para></listitem>
<listitem><para>
      修复 contrib/intagg 的安全和 64 位问题
     </para></listitem>
<listitem><para>
      为一些 contrib 函数添加所需的 STRICT 标记（Kris Jurka）
     </para></listitem>
<listitem><para>
      避免 plpgsql 游标声明参数过多时的缓冲区溢出（Neil）
     </para></listitem>
<listitem><para>
      修复 FULL 和 RIGHT 外连接的规划错误
     </para>
<para>
      连接的结果被错误地认为与左输入的排序相同。这不仅会向用户交付排序错误的输出，而且在嵌套归并连接的情形下可能给出完全错误的答案。
     </para></listitem>
<listitem><para>
      修复 plperl 处理元组字段中引号的问题
     </para></listitem>
<listitem><para>
      修复 SQL 和 GERMAN datestyle 下负 interval 的显示
     </para></listitem>
<listitem><para>
      让 age(timestamptz) 用本地时区而不是 GMT 计算
     </para></listitem>
</itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-7-4-6">
  <title>版本 7.4.6</title>

  <note>
  <title>发行日期</title>
  <simpara>2004-10-22</simpara>
 </note>

  <para>
   本次发布包含对 7.4.5 的多方面修复。有关 7.4 大版本中新特性的信息，请参见<xref linkend="release-7-4">。
  </para>


  <sect2>
   <title>迁移到版本 7.4.6</title>

   <para>
    对于运行 7.4.X 的用户，不需要进行转储/恢复。</para>
  </sect2>

  <sect2>
   <title>变更</title>

<itemizedlist>
<listitem><para>
      修复磁盘上提示位可能未更新的问题
     </para>
<para>
      极少数情况下，这一疏漏可能导致<quote>无法访问事务状态</quote>失败，这使它成为一个潜在的数据丢失缺陷。
     </para></listitem>
<listitem><para>
      确保哈希外连接不漏掉元组
     </para>
<para>
      在恰当的数据分布下，使用哈希连接计划的超大型左连接可能无法输出未匹配的左侧行。
     </para></listitem>
<listitem><para>
      不允许以 root 运行 <application>pg_ctl</application>
     </para>
<para>
      这是为了防范任何可能的安全问题。
     </para></listitem>
<listitem><para>
      避免 <command>make_oidjoins_check</command> 在 <filename>/tmp</filename> 中使用临时文件
     </para>
<para>
      这被报告为安全问题，不过几乎不值得担心，因为非开发者本来就没有理由使用此脚本。
     </para></listitem>
<listitem><para>
      防止强制后端关闭重新发出先前的命令结果
     </para>
<para>
      极少数情况下，客户端可能认为其最后一条命令成功了，而它实际上已被强制数据库关闭中止。
     </para></listitem>
<listitem><para>
      修复 <function>pg_stat_get_backend_idset</function> 中的缺陷
     </para>
<para>
      这可能导致某些系统统计视图行为异常。
     </para></listitem>
<listitem><para>
      修复 postmaster 中的小内存泄漏
     </para></listitem>
<listitem><para>
      修复<quote>预期两个交换的表都有 TOAST 表</quote>缺陷
     </para>
<para>
      这可能发生在如 ALTER TABLE DROP COLUMN 之后再 CLUSTER 的情形。
     </para></listitem>
<listitem><para>
      防止 <literal>pg_ctl restart</literal> 多次添加 <literal>-D</literal>
     </para></listitem>
<listitem><para>
      修复 GiST 索引中 NULL 值的问题
     </para></listitem>
<listitem><para>
      <literal>::</literal> 不再被解释为 ECPG prepare 语句中的变量
     </para></listitem>
</itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-7-4-5">
  <title>版本 7.4.5</title>

  <note>
  <title>发行日期</title>
  <simpara>2004-08-18</simpara>
 </note>

  <para>
      此发行版在 7.4.4 的基础上包含一个严重的缺陷修复。有关 7.4 主版本新特性的信息，请参阅<xref linkend="release-7-4">。
     </para>


  <sect2>
   <title>迁移到版本 7.4.5</title>

   <para>
    对于运行 7.4.X 的用户，不需要进行转储/恢复。</para>
  </sect2>

  <sect2>
   <title>变更</title>

<itemizedlist>
<listitem><para>
      修复并发 B-tree 索引插入期间可能的崩溃
     </para>
<para>
      此补丁修复了并发插入 B-tree 索引可能导致服务器 panic 的罕见情形。不会造成永久损坏，但仍值得重新发布。该缺陷在 7.4 之前的版本中不存在。
     </para></listitem>
</itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-7-4-4">
  <title>版本 7.4.4</title>

  <note>
  <title>发行日期</title>
  <simpara>2004-08-16</simpara>
 </note>

  <para>
   本次发布包含对 7.4.3 的多方面修复。有关 7.4 大版本中新特性的信息，请参见<xref linkend="release-7-4">。
  </para>


  <sect2>
   <title>迁移到版本 7.4.4</title>

   <para>
    对于运行 7.4.X 的用户，不需要进行转储/恢复。</para>
  </sect2>

  <sect2>
   <title>变更</title>

<itemizedlist>
<listitem><para>
      防止崩溃期间已提交事务的可能丢失
     </para>
<para>
      由于事务提交与检查点之间的互锁不足，最近一次检查点之前刚提交的事务可能在数据库崩溃和重启后全部或部分丢失。这是自 <productname>PostgreSQL</productname> 7.1 以来就存在的严重缺陷。
     </para></listitem>
<listitem><para>
      在求值聚合计划的结果列表之前检查 HAVING 限制
     </para></listitem>
<listitem><para>
      避免会话的当前用户 ID 被删除时的崩溃
     </para></listitem>
<listitem><para>
      修复零行情形的哈希 crosstab（Joe）
     </para></listitem>
<listitem><para>
      在外键中重命名列后强制更新缓存
     </para></listitem>
<listitem><para>
      正确美化 UNION 查询的打印
     </para></listitem>
<listitem><para>
      让 psql 在 COPY IN 中正确处理 <literal>\r\n</literal> 换行
     </para></listitem>
<listitem><para>
      <application>pg_dump</application> 对带 grant 选项的 ACL 处理不正确
     </para></listitem>
<listitem><para>
      修复 OS X 和 Solaris 的线程支持
     </para></listitem>
<listitem><para>
      更新了 JDBC 驱动（build 215），包含各种修复
     </para></listitem>
<listitem><para>
      ECPG 修复
     </para></listitem>
<listitem><para>
      翻译更新（多位贡献者）
     </para></listitem>
</itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-7-4-3">
  <title>版本 7.4.3</title>

  <note>
  <title>发行日期</title>
  <simpara>2004-06-14</simpara>
 </note>

  <para>
   本次发布包含对 7.4.2 的多方面修复。有关 7.4 大版本中新特性的信息，请参见<xref linkend="release-7-4">。
  </para>


  <sect2>
   <title>迁移到版本 7.4.3</title>

   <para>
    对于运行 7.4.X 的用户，不需要进行转储/恢复。</para>
  </sect2>

  <sect2>
   <title>变更</title>

<itemizedlist>
<listitem><para>
      修复使用非哈希聚合时的临时内存泄漏（Tom）
     </para></listitem>
<listitem><para>
      ECPG 修复，包括一些针对 Informix 兼容性的（Michael）
     </para></listitem>
<listitem><para>
      修复线程安全编译的问题，特别是 Solaris（Bruce）
     </para></listitem>
<listitem><para>
      修复使用旧网络协议时 COPY IN 结束时的错误（ljb）
     </para></listitem>
<listitem><para>
      pg_autovacuum 的几个重要修复，包括大表、无符号 oid、稳定性、临时表和调试模式的修复（Matthew T. O'Connor）
     </para></listitem>
<listitem><para>
      修复在 NetBSD 和 BSD/OS 上读取 tar 格式转储的问题（Bruce）
     </para></listitem>
<listitem><para>
      若干 JDBC 修复
     </para></listitem>
<listitem><para>
      修复 last_value 等于重启值时的 ALTER SEQUENCE RESTART（Tom）
     </para></listitem>
<listitem><para>
      修复嵌套子查询未重新计算的问题（Tom）
     </para></listitem>
<listitem><para>
      修复 LIMIT/OFFSET 中非常量表达式的问题
     </para></listitem>
<listitem><para>
      支持无连接子句的 FULL JOIN，如 X FULL JOIN Y ON TRUE（Tom）
     </para></listitem>
<listitem><para>
      修复另一个零列表缺陷（Tom）
     </para></listitem>
<listitem><para>
      改进子查询中 GROUP BY 子句里非限定标识符的处理（Tom）
     </para>
<para>
      子查询内的选择列表别名现在优先于来自外层查询级别的名称。
     </para></listitem>
<listitem><para>
      反编译规则时不生成 <quote>NATURAL CROSS JOIN</quote>（Tom）
     </para></listitem>
<listitem><para>
      为二进制 COPY 中的无效字段长度添加检查（Tom）
     </para>
<para>
      这修复了一个难以利用的安全漏洞。
     </para></listitem>
<listitem><para>
      避免 <command>ANALYZE</command> 与 <command>LISTEN</command>/<command>NOTIFY</command> 之间的锁定冲突
     </para></listitem>
<listitem><para>
      大量翻译更新（多位贡献者）
     </para></listitem>
</itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-7-4-2">
  <title>版本 7.4.2</title>

  <note>
  <title>发行日期</title>
  <simpara>2004-03-08</simpara>
 </note>

  <para>
   本次发布包含对 7.4.1 的多方面修复。有关 7.4 大版本中新特性的信息，请参见<xref linkend="release-7-4">。
  </para>


  <sect2>
   <title>迁移到版本 7.4.2</title>

   <para>
      对于运行 7.4.X 的用户，不需要进行转储/恢复。不过，作为合并 7.4.X 系统目录初始内容中发现的两个错误的修复的最简单方法，它可能是可取的。用 7.4.2 的 initdb 做转储/initdb/重载序列会自动纠正这些问题。
     </para>

   <para>
      两个错误中较严重的是数据类型 <type>anyarray</type> 的对齐标签错误；这是一个问题，因为 <structname>pg_statistic</structname> 系统目录使用 <type>anyarray</type> 列。当规划涉及双对齐列（如 <type>float8</type> 和 <type>timestamp</type>）上 <literal>WHERE</literal> 子句的查询时，错误标记可能导致规划器误估甚至崩溃。强烈建议所有安装通过 initdb 或按照下述手工修复程序修复此错误。
     </para>

   <para>
      较小的错误是系统视图 <structname>pg_settings</structname> 应被标记为具有公共更新访问权，以允许用 <literal>UPDATE pg_settings</literal> 替代 <command>SET</command>。这也可以通过 initdb 或手工修复，但除非你想用 <literal>UPDATE pg_settings</literal>，否则不必修复。
     </para>

   <para>
    如果你不想做 initdb，以下程序可以修复 <structname>pg_statistic</structname>。作为数据库超级用户，执行：

<programlisting>
-- clear out old data in pg_statistic:
DELETE FROM pg_statistic;
VACUUM pg_statistic;
-- this should update 1 row:
UPDATE pg_type SET typalign = 'd' WHERE oid = 2277;
-- this should update 6 rows:
UPDATE pg_attribute SET attalign = 'd' WHERE atttypid = 2277;
--
-- At this point you MUST start a fresh backend to avoid a crash!
--
-- repopulate pg_statistic:
ANALYZE;
</programlisting>

    这可以在运行中的数据库上完成，但要注意在重新填充 <structname>pg_statistic</structname> 之前，被更改数据库中运行的所有后端都必须重启才是安全的。
   </para>

   <para>
    要修复 <structname>pg_settings</structname> 错误，只需执行：
<programlisting>
GRANT SELECT, UPDATE ON pg_settings TO PUBLIC;
</programlisting>
   </para>

   <para>
    上述程序必须在安装的<emphasis>每个</emphasis>数据库中执行，包括 <literal>template1</literal>，理想情况下也包括 <literal>template0</literal>。如果不修复模板数据库，之后创建的任何数据库都会包含相同的错误。<literal>template1</literal> 可以像任何其他数据库一样修复，但修复 <literal>template0</literal> 需要额外的步骤。首先，从任意数据库发出：
<programlisting>
UPDATE pg_database SET datallowconn = true WHERE datname = 'template0';
</programlisting>
     接下来连接到 <literal>template0</literal> 并执行上述修复程序。最后执行：
<programlisting>
-- re-freeze template0:
VACUUM FREEZE;
-- and protect it against future alterations:
UPDATE pg_database SET datallowconn = false WHERE datname = 'template0';
</programlisting>
   </para>
  </sect2>

  <sect2>
   <title>变更</title>

<para>
      7.4.2 版本包含 7.3.6 版本中的所有修复，外加以下修复：
     </para>

<itemizedlist>
<listitem><para>
      修复可能导致优化器崩溃的 <structname>pg_statistics</structname> 对齐缺陷
     </para>
<para>
      有关此问题的详情见上文。
     </para></listitem>
<listitem><para>
      允许非超级用户更新 <structname>pg_settings</structname>
     </para></listitem>
<listitem><para>
      修复几个优化器缺陷，其中大多数导致<quote>在子计划目标列表中未找到变量</quote>错误
     </para></listitem>
<listitem><para>
      避免大型多索引扫描启动期间的内存不足失败
     </para></listitem>
<listitem><para>
      修复可能导致 <command>COPY IN</command> 期间<quote>内存不足</quote>错误的多字节问题
     </para></listitem>
<listitem><para>
      修复从无 OID 的表 <command>SELECT INTO</command> / <command>CREATE TABLE AS</command> 的问题
     </para></listitem>
<listitem><para>
      修复并行测试期间 <filename>alter_table</filename> 回归测试的问题
     </para></listitem>
<listitem><para>
      修复达到打开文件限制的问题，特别是在 OS X 上（Tom）
     </para></listitem>
<listitem><para>
      对土耳其语区域设置问题的部分修复
     </para>
<para>
      initdb 现在能在土耳其语区域设置下成功，但仍有一些与 <literal>i/I</literal> 问题相关的不便。
     </para></listitem>
<listitem><para>
      让 pg_dump 在恢复时设置客户端编码
     </para></listitem>
<listitem><para>
      其他次要的 pg_dump 修复
     </para></listitem>
<listitem><para>
      允许 ecpg 再次使用 C 关键字作为列名（Michael）
     </para></listitem>
<listitem><para>
      为 ecpg 的 <literal>SELECT/INSERT/UPDATE/DELETE</literal> 添加 <literal>WHENEVER NOT_FOUND</literal>（Michael）
     </para></listitem>
<listitem><para>
      修复调用返回集合函数的查询导致的 ecpg 崩溃（Michael）
     </para></listitem>
<listitem><para>
      各种其他 ecpg 修复（Michael）
     </para></listitem>
<listitem><para>
      Borland 编译器的修复
     </para></listitem>
<listitem><para>
      线程构建改进（Bruce）
     </para></listitem>
<listitem><para>
      各种其他构建修复
     </para></listitem>
<listitem><para>
      各种 JDBC 修复
     </para></listitem>
</itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-7-4-1">
  <title>版本 7.4.1</title>

  <note>
  <title>发行日期</title>
  <simpara>2003-12-22</simpara>
 </note>

  <para>
   本次发布包含对 7.4 的多方面修复。有关 7.4 大版本中新特性的信息，请参见<xref linkend="release-7-4">。
  </para>


  <sect2>
   <title>迁移到版本 7.4.1</title>

   <para>
      对于运行 7.4 的用户，<emphasis>不</emphasis>需要进行转储/恢复。
     </para>

   <para>
    如果你想在信息模式中安装这些修复，需要把它重新装载到数据库中。这可以通过运行 <command>initdb</command> 初始化新集群来完成，或者在安装新版本后，以超级用户身份在 <application>psql</application> 中对每个数据库（理想情况下包括
    <literal>template1</literal>）运行以下 SQL 命令序列：
<programlisting>
DROP SCHEMA information_schema CASCADE;
\i /usr/local/pgsql/share/information_schema.sql
</programlisting>
    在第二条命令中替换你的安装路径。
   </para>

  </sect2>

  <sect2>
   <title>变更</title>

<itemizedlist>
<listitem><para>
      修复 ECPG 中 <command>CREATE SCHEMA</command> 解析的缺陷（Michael）
     </para></listitem>
<listitem><para>
      修复 <option>--enable-thread-safety</option> 与 <option>--with-perl</option> 一起使用时的编译错误（Peter）
     </para></listitem>
<listitem><para>
      修复使用哈希连接的子查询（Tom）
     </para>
<para>
      某些使用哈希连接的子查询会因不当共享的结构而崩溃。
     </para></listitem>
<listitem><para>
      修复空闲空间映射压缩缺陷（Tom）
     </para>
<para>
      这修复了空闲空间映射的压缩可能导致数据库服务器关闭的缺陷。
     </para>
</listitem>
<listitem><para>
      修复 libpq 的 Borland 编译器构建（Bruce）
     </para></listitem>
<listitem><para>
      修复 <function>netmask()</function> 和 <function>hostmask()</function>，使其返回最大长度的掩码长度（Tom）
     </para>
<para>
      修复这些函数使其返回与 7.4 之前版本一致的值。
     </para>
</listitem>
<listitem><para>
      若干 <filename>contrib/pg_autovacuum</filename> 修复
     </para>
<para>
      修复包括不当的变量初始化、<command>TRUNCATE</command> 后缺失的清理以及长清理的时长计算溢出。
     </para>
</listitem>
<listitem><para>
      允许 <filename>contrib/cube</filename> 在 Cygwin 下编译（Jason Tishler）
     </para></listitem>
<listitem><para>
      修复 Solaris 在未定义口令时使用口令文件的问题（Tom）
     </para>
<para>
      修复 Solaris 上因在未定义口令时使用任何类型的口令认证导致的崩溃。
     </para>
</listitem>
<listitem><para>
      JDBC 线程问题修复及其他修复
     </para></listitem>
<listitem><para>
      修复 <type>bytea</type> 索引查找（Joe）
     </para></listitem>
<listitem><para>
      修复信息模式对位数据类型的支持（Peter）
     </para></listitem>
<listitem><para>
      强制在从 WAL 恢复期间开启 zero_damaged_pages
     </para></listitem>
<listitem><para>
      防止<quote>变量不在子计划目标列表中</quote>的一些晦涩情形
     </para></listitem>
<listitem><para>
      使 <function>PQescapeBytea</function> 与 <function>byteaout</function> 相互一致（Joe）
     </para></listitem>
<listitem><para>
      对大于 0x7e 的字节转义 <type>bytea</type> 输出（Joe）
     </para>
<para>
      如果 <type>bytea</type> 输出和输入使用不同的客户端编码，<type>bytea</type> 值可能被不同的编码破坏。此修复转义了所有可能受影响的字节。
     </para>
</listitem>
<listitem><para>
      为 dblink 的 <function>get_tuple_of_interest()</function> 添加缺失的 <function>SPI_finish()</function> 调用（Joe）
     </para></listitem>
<listitem><para>
      新的捷克语 FAQ
     </para></listitem>
<listitem><para>
      修复信息模式视图 <literal>constraint_column_usage</literal> 对外键的支持（Peter）
     </para></listitem>
<listitem><para>
      ECPG 修复（Michael）
     </para></listitem>
<listitem><para>
      修复多个 <literal>IN</literal> 子查询及子查询内连接的缺陷（Tom）
     </para></listitem>
<listitem><para>
      允许 <literal>COUNT('x')</literal> 工作（Tom）
     </para></listitem>
<listitem><para>
      把 Informix 兼容的 ECPG 头文件安装到单独的目录（Peter）
     </para>
<para>
      一些 Informix 兼容的 ECPG 头文件名与操作系统头文件冲突。通过把它们安装在自己的目录中，减少了名称冲突。
     </para>
</listitem>
<listitem><para>
      修复 SSL 内存泄漏（Neil）
     </para>
<para>
      此发行版修复了 7.4 中 SSL 未释放其分配的全部内存的缺陷。
     </para>
</listitem>
<listitem><para>
      防止 <filename>pg_service.conf</filename> 把服务名用作默认数据库名（Bruce）
     </para></listitem>
<listitem><para>
      修复 FreeBSD 上的本地 ident 认证（Tom）
     </para></listitem>
</itemizedlist>

 </sect2>
</sect1>

<sect1 id="release-7-4">
 <title>版本 7.4</title>

 <note>
  <title>发行日期</title>
  <simpara>2003-11-17</simpara>
 </note>

 <sect2>
  <title>概述</title>

  <para>
      此发行版的主要变更：
     </para>

  <variablelist>
   <varlistentry>
    <term>
     <literal>IN</literal> / <literal>NOT IN</literal> 子查询现在效率高得多
    </term>

    <listitem>
     <para>
      在以前的版本中，<literal>IN</literal>/<literal>NOT IN</literal> 子查询通过顺序扫描子查询寻找匹配来与上层查询连接。7.4 的代码使用与普通连接相同的复杂技术，因此快得多。<literal>IN</literal> 现在通常与等价的 <literal>EXISTS</literal> 子查询一样快或更快；这颠倒了适用于以前版本的传统观念。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term>
     通过使用哈希桶改进了 <literal>GROUP BY</literal> 处理
    </term>

    <listitem>
     <para>
      在以前的版本中，要分组的行必须先排序。7.4 的代码可以不做排序就完成 <literal>GROUP BY</literal>，方法是把结果累积到每组一个条目的哈希表中。但如果估计哈希表太大放不进 <varname>sort_mem</varname>，它仍会使用排序技术。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term>
     新的多键哈希连接能力
    </term>

    <listitem>
     <para>
      在以前的版本中，哈希连接只能在单键上发生。此版本允许多列哈希连接。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term>
     使用显式 <literal>JOIN</literal> 语法的查询现在得到更好的优化
    </term>

    <listitem>
     <para>
      以前的版本只按语法隐含的顺序求值使用显式 <literal>JOIN</literal> 语法的查询。7.4 允许对这些查询做完全优化，即优化器考虑所有可能的连接顺序并选择最高效的。但外连接仍必须遵循声明的顺序。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term>
     更快、更强大的正则表达式代码
    </term>

    <listitem>
     <para>
      整个正则表达式模块已被 Henry Spencer 的新版本（最初为 Tcl 编写）取代。该代码大幅改进了性能并支持多种风格的正则表达式。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term>
     对简单 SQL 函数的函数内联
    </term>

    <listitem>
     <para>
      简单 SQL 函数现在可以通过把其 SQL 包含在主查询中来内联。这通过消除每次调用的开销改进了性能。这意味着简单 SQL 函数现在的行为像宏。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term>
     全面支持 IPv6 连接和 IPv6 地址数据类型
    </term>

    <listitem>
     <para>
      以前的版本只允许 IPv4 连接，IP 数据类型也只支持 IPv4 地址。此版本在这两方面都增加了完整的 IPv6 支持。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term>
     SSL 性能和可靠性的重大改进
    </term>

    <listitem>
     <para>
      几位非常熟悉 SSL API 的人彻底改造了我们的 SSL 代码，以改进 SSL 密钥协商和错误恢复。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term>
     让空闲空间映射高效地重用空索引页面，以及其他空闲空间管理改进
    </term>

    <listitem>
     <para>
      在以前的版本中，因行被删除而变空的 B-tree 索引页只能被索引值与该页上最初索引的行相似的行重用。在 7.4 中，<command>VACUUM</command> 记录空索引页并允许它们被未来的任何索引行重用。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term>
     SQL 标准的信息模式
    </term>

    <listitem>
     <para>
      信息模式提供了访问数据库中定义的模式对象信息的标准化且稳定的方式。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term>
     游标更贴近 SQL 标准
    </term>

    <listitem>
     <para>
      <command>FETCH</command> 和 <command>MOVE</command> 命令已被彻底改造，以更贴近 SQL 标准。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term>
     游标可以存在于事务之外
    </term>

    <listitem>
     <para>
      这些游标也称为可保持游标。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term>
     新的客户端-服务器协议
    </term>

    <listitem>
     <para>
      新协议增加了错误码、更多状态信息、更快的启动、对二进制数据传输的更好支持、与 SQL 命令分离的参数值、协议级的预备语句以及从 <command>COPY</command> 失败更干净的恢复。服务器和客户端仍支持较旧的协议。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term>
     <application>libpq</application> 和
     <application>ECPG</application> 应用现在完全线程安全
    </term>

    <listitem>
     <para>
      虽然以前的 <application>libpq</application> 版本已支持线程，但此版本通过修复数据库连接启动期间使用的一些非线程安全代码改进了线程安全。必须使用 <command>configure</command> 选项 <option>--enable-thread-safety</option> 才能启用此特性。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term>
     新版本的全文索引
    </term>

    <listitem>
     <para>
      <filename>contrib/tsearch2</filename> 中提供了新的全文索引套件。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term>
     新的自动清理（autovacuum）工具
    </term>

    <listitem>
     <para>
      <filename>contrib/autovacuum</filename> 中的新 autovacuum 工具监控数据库统计表中的 <command>INSERT</command>/<command>UPDATE</command>/<command>DELETE</command> 活动，并在需要时自动清理表。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term>
     数组处理得到了改进并被移入服务器核心
    </term>

    <listitem>
     <para>
      数组的许多限制已被移除，数组的行为更像完全受支持的数据类型。
     </para>
    </listitem>
   </varlistentry>

  </variablelist>
 </sect2>

 <sect2>
  <title>迁移到版本 7.4</title>

  <para>
      希望从任何以前的版本迁移数据的用户，需要使用 <application>pg_dump</application> 进行转储/恢复。
     </para>

  <para>
      请注意以下不兼容之处：
     </para>

  <itemizedlist>
   <listitem>
    <para>
      服务器端的自动提交设置被移除，并在客户端应用程序和语言中重新实现。服务器端自动提交给想要控制自己自动提交行为的语言和应用程序带来了太多问题，因此自动提交从服务器中移除并酌情添加到各个客户端 API。
     </para>
   </listitem>

   <listitem>
    <para>
      此版本中错误消息的措辞发生了实质变化。投入了大量努力使消息更一致、更面向用户。如果你的应用程序试图通过解析错误消息来检测不同的错误条件，强烈建议改用新的错误码机制。
     </para>
   </listitem>

   <listitem>
    <para>
      使用显式 <literal>JOIN</literal> 语法的内连接可能行为不同，因为它们现在得到了更好的优化。
     </para>
   </listitem>

   <listitem>
    <para>
      为清晰起见，许多服务器配置参数已被重命名，主要是与日志相关的那些。
     </para>
   </listitem>

   <listitem>
    <para>
      <literal>FETCH 0</literal> 或 <literal>MOVE 0</literal> 现在不做任何事情。在以前的版本中，<literal>FETCH 0</literal> 会获取所有剩余行，<literal>MOVE 0</literal> 会移到游标末尾。
     </para>
   </listitem>

   <listitem>
    <para>
      <command>FETCH</command> 和 <command>MOVE</command> 现在返回实际获取/移动的行数，如果位于游标开头/末尾则返回零。以前的版本会返回传给命令的行数，而不是实际获取或移动的行数。
     </para>
   </listitem>

   <listitem>
    <para>
      <command>COPY</command> 现在能处理使用回车或回车/换行行尾序列的文件。数据值中不再接受字面回车和换行；请改用 <literal>\r</literal> 和 <literal>\n</literal>。
     </para>
   </listitem>

   <listitem>
    <para>
      从 <type>char(<replaceable>n</replaceable>)</type> 转换为 <type>varchar(<replaceable>n</replaceable>)</type> 或 <type>text</type> 时现在会修剪尾随空格。这本来就是大多数人一直期望的。
     </para>
   </listitem>

   <listitem>
    <para>
      数据类型 <type>float(<replaceable>p</replaceable>)</type> 现在用二进制位而不是十进制位度量 <replaceable>p</replaceable>。新行为遵循 SQL 标准。
     </para>
   </listitem>

   <listitem>
    <para>
      有歧义的日期值现在必须匹配 <varname>datestyle</varname> 设置指定的顺序。在以前的版本中，即使 <varname>datestyle</varname> 指定日在前，<literal>10/20/03</literal> 的日期规格也会被解释为十月的日期。7.4 将在日期规格对当前 <varname>datestyle</varname> 设置无效时抛出错误。
     </para>
   </listitem>

   <listitem>
    <para>
      函数 <function>oidrand</function>、<function>oidsrand</function> 和 <function>userfntest</function> 已被移除。这些函数被认定不再有用。
     </para>
   </listitem>

   <listitem>
    <para>
      指定随时间变化的日期/时间值的字符串字面量（如 <literal>'now'</literal> 或 <literal>'today'</literal>）在列默认表达式中不再按预期工作；它们现在使表创建时刻成为默认值，而不是插入时刻。应改用 <function>now()</function>、<function>current_timestamp</function> 或 <function>current_date</function> 等函数。
     </para>

    <para>
      在以前的版本中，有特殊代码使 <literal>'now'</literal> 之类的字符串在 <command>INSERT</command> 时而不是表创建时被解释，但这一变通没有覆盖所有情形。7.4 版本现在要求用 <function>now()</function> 或 <function>current_timestamp()</function> 等函数正确定义默认值。这些在所有情形下都能工作。
     </para>
   </listitem>

   <listitem>
    <para>
      操作符名中不再允许美元符号（<literal>$</literal>）。它可以用作标识符中的非首字符。这是为了改进与其他数据库系统的兼容性，并避免参数占位符（<literal>$<replaceable>n</replaceable></literal>）紧邻操作符书写时的语法问题。
     </para>
   </listitem>

  </itemizedlist>
 </sect2>

 <sect2>
  <title>变更</title>

  <para>
      下面你将找到 7.4 版本与上一个主版本之间变更的详细说明。
     </para>

 <sect3>
  <title>服务器操作变更</title>

  <itemizedlist>
   <listitem>
    <para>
      允许 IPv6 服务器连接（Nigel Kukard、Johan Jordaan、Bruce、Tom、Kurt Roeckx、Andrew Dunstan）
     </para>
   </listitem>

   <listitem>
    <para>
      修复 SSL 以干净地处理错误（Nathan Mueller）
     </para>
    <para>
      在以前的版本中，某些 SSL API 错误报告未被正确处理。此版本修复了这些问题。
     </para>
   </listitem>

   <listitem>
    <para>
      SSL 协议安全和性能改进（Sean Chittenden）
     </para>
    <para>
      SSL 密钥重协商过于频繁，导致 SSL 性能不佳。此外，初始密钥处理也得到了改进。
     </para>
   </listitem>

   <listitem>
    <para>
      检测到死锁时打印锁信息（Tom）
     </para>
    <para>
      这使调试死锁情形更容易。
     </para>
   </listitem>

   <listitem>
    <para>
      定期更新 <filename>/tmp</filename> 套接字的修改时间以避免它们被删除（Tom）
     </para>
    <para>
      这应有助于防止清理 <filename>/tmp</filename> 目录的管理脚本删除服务器套接字文件。
     </para>
   </listitem>

   <listitem><para>
      为 Mac OS X 启用 PAM（Aaron Hillegass）
     </para></listitem>

   <listitem>
    <para>
      使 B-tree 索引完全 WAL 安全（Tom）
     </para>
    <para>
      在以前的版本中，某些罕见情形下服务器崩溃可能导致 B-tree 索引损坏。此版本消除了最后几个罕见情形。
     </para>
   </listitem>

   <listitem><para>
      允许 B-tree 索引压缩和空页重用（Tom）
     </para></listitem>

   <listitem>
    <para>
      修复首个根页分裂期间索引查找不一致的问题（Tom）
     </para>
    <para>
      在以前的版本中，当单页索引分裂为两页时，有一小段时间另一个数据库会话可能看不到某个索引条目。此版本修复了这一罕见失败情形。
     </para>
   </listitem>

   <listitem><para>
      改进空闲空间映射的分配逻辑（Tom）
     </para></listitem>

   <listitem>
    <para>
      在服务器重启之间保留空闲空间信息（Tom）
     </para>
    <para>
      在以前的版本中，postmaster 停止时空闲空间映射不被保存，因此新启动的服务器没有空闲空间信息。此版本保存空闲空间映射，并在服务器重启时重新装载它。
     </para>
   </listitem>

   <listitem><para>
      为 <literal>pg_stat_activity</literal> 添加开始时间（Neil）
     </para></listitem>
   <listitem><para>
      检测损坏磁盘页的新代码；用 <varname>zero_damaged_pages</varname> 清除（Tom）
     </para></listitem>
   <listitem><para>
      新的客户端/服务器协议：更快、无用户名长度限制、允许从 <command>COPY</command> 干净退出（Tom）
     </para></listitem>
   <listitem><para>
      为客户端/服务器协议添加事务状态、表 ID、列 ID（Tom）
     </para></listitem>
   <listitem><para>
      为客户端/服务器协议添加二进制 I/O（Tom）
     </para></listitem>
   <listitem><para>
      移除服务器端自动提交设置；移至客户端应用程序（Tom）
     </para></listitem>
   <listitem><para>
      新的错误消息措辞、错误码和三级错误细节（Tom、Joe、Peter）
     </para></listitem>
  </itemizedlist>
 </sect3>

 <sect3>
  <title>性能改进</title>

  <itemizedlist>
   <listitem><para>
      为 <literal>GROUP BY</literal> 聚合添加哈希（Tom）
     </para></listitem>
   <listitem><para>
      使嵌套循环连接更聪明地使用多列索引（Tom）
     </para></listitem>
   <listitem><para>
      允许多键哈希连接（Tom）
     </para></listitem>
   <listitem><para>
      改进常量折叠（Tom）
     </para></listitem>
   <listitem><para>
      新增内联简单 SQL 函数的能力（Tom）
     </para></listitem>

   <listitem>
    <para>
      降低使用复杂函数的查询的内存使用（Tom）
     </para>
    <para>
      在以前的版本中，返回已分配内存的函数直到查询完成才释放它。此版本允许在函数调用完成时释放函数分配的内存，减少了函数使用的总内存。
     </para>
   </listitem>

   <listitem>
    <para>
      改进 GEQO 优化器性能（Tom）
     </para>
    <para>
      此版本修复了 GEQO 优化器管理潜在查询路径方式中的几个低效之处。
     </para>
   </listitem>

   <listitem>
    <para>
      允许 <literal>IN</literal>/<literal>NOT IN</literal> 通过哈希表处理（Tom）
     </para>
   </listitem>

   <listitem>
    <para>
      改进 <literal>NOT IN (<replaceable>subquery</replaceable>)</literal> 的性能（Tom）
     </para>
   </listitem>

   <listitem>
    <para>
      允许大多数 <literal>IN</literal> 子查询作为连接处理（Tom）
     </para>
   </listitem>

   <listitem>
    <para>
      模式匹配操作可以使用索引而不受区域设置影响（Peter）
     </para>
    <para>
      非 ASCII 区域设置无法使用标准索引做 <literal>LIKE</literal> 比较。此版本增加了一种为 <literal>LIKE</literal> 创建特殊索引的方法。
     </para>
   </listitem>

   <listitem>
    <para>
      允许 postmaster 用 <varname>preload_libraries</varname> 预装载库（Joe）
     </para>
    <para>
      对于需要很长时间装载的共享库，此选项使库可以在 postmaster 中预装载并被所有数据库会话继承。
     </para>
   </listitem>

   <listitem>
    <para>
      改进优化器代价计算，特别是对子查询（Tom）
     </para>
   </listitem>

   <listitem>
    <para>
      当子查询 <literal>ORDER BY</literal> 与上层查询匹配时避免排序（Tom）
     </para>
   </listitem>

   <listitem>
    <para>
      从 <literal>WHERE a.x = b.y AND b.y = 42</literal> 推出 <literal>a.x = 42</literal>（Tom）
     </para>
   </listitem>

   <listitem>
    <para>
      允许复杂连接上的哈希/归并连接（Tom）
     </para>
   </listitem>

   <listitem>
    <para>
      允许更多数据类型的哈希连接（Tom）
     </para>
   </listitem>

   <listitem>
    <para>
      允许显式内连接的连接优化，可用 <varname>join_collapse_limit</varname> 禁用（Tom）
     </para>
   </listitem>

   <listitem>
    <para>
      新增参数 <varname>from_collapse_limit</varname>，控制子查询到连接的转换（Tom）
     </para>
   </listitem>

   <listitem>
    <para>
      使用来自 Tcl 的更快更强大的正则表达式代码（Henry Spencer、Tom）
     </para>
   </listitem>

   <listitem>
    <para>
      在优化器中使用位图关系集（Tom）
     </para>
   </listitem>

   <listitem>
    <para>
      改进连接启动时间（Tom）
     </para>
    <para>
      新的客户端/服务器协议启动数据库会话需要更少的网络包。
     </para>
   </listitem>

   <listitem>
    <para>
      改进触发器/约束的性能（Stephan）
     </para>
   </listitem>

   <listitem>
    <para>
      改进 <literal>col IN (const, const, const, ...)</literal> 的速度（Tom）
     </para>
   </listitem>

   <listitem>
    <para>
      修复罕见情形下损坏的哈希索引（Tom）
     </para>
   </listitem>

   <listitem><para>
      改进哈希索引的并发和速度（Tom）
     </para>
    <para>
      以前的版本哈希索引性能不佳，特别是在高并发情形下。此版本修复了这一点，开发组有兴趣了解比较 B-tree 与哈希索引性能的报告。
     </para>
   </listitem>

   <listitem>
    <para>
      把共享缓冲区对齐到 32 字节边界以改进复制速度（Manfred Spraul）
     </para>
    <para>
      某些 CPU 在地址按 32 字节对齐时执行更快的数据复制。
     </para>
   </listitem>

   <listitem>
    <para>
      为更好的性能重新实现了 <type>numeric</type> 数据类型（Tom）
     </para>
    <para>
      <type>numeric</type> 以前以 100 为基数存储。新代码使用 10000 为基数，性能显著提升。
     </para>
   </listitem>
  </itemizedlist>
 </sect3>

 <sect3>
  <title>服务器配置变更</title>

  <itemizedlist>
   <listitem>
    <para>
      把服务器参数 <varname>server_min_messages</varname> 改名为 <varname>log_min_messages</varname>（Bruce）
     </para>
    <para>
      这是为了使大多数控制服务器日志的参数以 <literal>log_</literal> 开头。
     </para>
   </listitem>

   <listitem><para>
      把 <varname>show_*_stats</varname> 改名为 <varname>log_*_stats</varname>（Bruce）
     </para></listitem>
   <listitem><para>
      把 <varname>show_source_port</varname> 改名为 <varname>log_source_port</varname>（Bruce）
     </para></listitem>
   <listitem><para>
      把 <varname>hostname_lookup</varname> 改名为 <varname>log_hostname</varname>（Bruce）
     </para></listitem>

   <listitem>
    <para>
      新增 <varname>checkpoint_warning</varname>，对过度检查点发出警告（Bruce）
     </para>
    <para>
      在以前的版本中，很难确定检查点是否发生得过于频繁。此特性在过度检查点发生时向服务器日志添加警告。
     </para>
   </listitem>

   <listitem><para>
      新的本地化只读服务器参数（Tom）
     </para></listitem>

   <listitem>
    <para>
      把调试服务器日志消息改为以 <literal>DEBUG</literal> 而不是 <literal>LOG</literal> 输出（Bruce）
     </para>
   </listitem>

   <listitem>
    <para>
      防止服务器日志变量被非超级用户关闭（Bruce）
     </para>
    <para>
      这是一个安全特性，使非超级用户不能禁用管理员启用的日志。
     </para>
   </listitem>

   <listitem>
    <para>
      <varname>log_min_messages</varname>/<varname>client_min_messages</varname> 现在控制 <varname>debug_*</varname> 输出（Bruce）
     </para>
    <para>
      这集中了客户端调试信息，使所有调试输出都可以发送到客户端或服务器日志。
     </para>
   </listitem>

   <listitem>
    <para>
      新增 Mac OS X Rendezvous 服务器支持（Chris Campbell）
     </para>
    <para>
      这允许 Mac OS X 主机向网络查询可用的 <productname>PostgreSQL</productname> 服务器。
     </para>
   </listitem>

   <listitem>
    <para>
      新增用 <varname>log_min_duration_statement</varname> 只打印慢语句的能力（Christopher）
     </para>
    <para>
      这是常被要求的调试特性，允许管理员在服务器日志中只看到慢查询。
     </para>
   </listitem>

   <listitem>
    <para>
      允许 <filename>pg_hba.conf</filename> 接受 CIDR 格式的网络掩码（Andrew Dunstan）
     </para>
    <para>
      这允许管理员把主机 IP 地址和网络掩码字段合并为 <filename>pg_hba.conf</filename> 中的单个 CIDR 字段。
     </para>
   </listitem>

   <listitem><para>
      新的只读参数 <varname>is_superuser</varname>（Tom）
     </para></listitem>

   <listitem>
    <para>
      新参数 <varname>log_error_verbosity</varname>，控制错误细节（Tom）
     </para>
    <para>
      它与新的错误报告特性一起工作，提供提示、文件名和行号等额外错误信息。
     </para>
   </listitem>

   <listitem>
    <para>
      <literal>postgres --describe-config</literal> 现在转储服务器配置变量（Aizaz Ahmed、Peter）
     </para>
    <para>
      此选项对需要知道配置变量名及其最小值、最大值、默认值和描述的管理工具很有用。
     </para>
   </listitem>

   <listitem>
    <para>
      为 <literal>pg_settings</literal> 新增列：<literal>context</literal>、<literal>type</literal>、<literal>source</literal>、<literal>min_val</literal>、<literal>max_val</literal>（Joe）
     </para>
   </listitem>

   <listitem>
    <para>
      如果可能，把默认 <varname>shared_buffers</varname> 设为 1000、<varname>max_connections</varname> 设为 100（Tom）
     </para>
    <para>
      以前的版本默认 64 个共享缓冲区，以便 <productname>PostgreSQL</productname> 甚至能在很老的系统上启动。此版本测试平台允许的共享内存量并在可能时选择更合理的默认值。当然，仍鼓励用户评估其资源负载并相应地设定 <varname>shared_buffers</varname> 的大小。
     </para>
   </listitem>

   <listitem>
    <para>
      新的 <filename>pg_hba.conf</filename> 记录类型 <literal>hostnossl</literal>，阻止 SSL 连接（Jon Jensen）
     </para>
    <para>
      在以前的版本中，如果客户端和服务器都支持 SSL，就没有办法阻止 SSL 连接。此选项提供了该能力。
     </para>
   </listitem>

   <listitem>
    <para>
      移除参数 <varname>geqo_random_seed</varname>（Tom）
     </para>
   </listitem>

   <listitem>
    <para>
      新增服务器参数 <varname>regex_flavor</varname>，控制正则表达式处理（Tom）
     </para>
   </listitem>

   <listitem>
    <para>
      让 <command>pg_ctl</command> 更好地处理非标准端口（Greg）
     </para>
   </listitem>
  </itemizedlist>
 </sect3>

 <sect3>
  <title>查询变更</title>

  <itemizedlist>
   <listitem><para>
      新的 SQL 标准信息模式（Peter）
     </para></listitem>
   <listitem><para>
      新增只读事务（Peter）
     </para></listitem>
   <listitem><para>
      在外键违反消息中打印键名和值（Dmitry Tkach）
     </para></listitem>

   <listitem>
    <para>
      允许用户在 <literal>pg_stat_activity</literal> 中看到自己的查询（Kevin Brown）
     </para>
    <para>
      在以前的版本中，只有超级用户能用 <literal>pg_stat_activity</literal> 看到查询字符串。现在普通用户可以看到自己的查询字符串。
     </para>
   </listitem>

   <listitem>
    <para>
      修复子查询中的聚合以符合 SQL 标准（Tom）
     </para>
    <para>
      SQL 标准规定，如果嵌套子查询中出现的聚合函数的参数只包含外层查询变量，则它属于外层查询。以前的 <productname>PostgreSQL</productname> 版本没有正确处理这个细节。
     </para>
   </listitem>

   <listitem>
    <para>
      新增阻止自动添加查询中引用的表的选项（Nigel J. Andrews）
     </para>
    <para>
      默认情况下，查询中提到的表如果不在 <literal>FROM</literal> 子句中会被自动添加到其中。这与历史 <productname>POSTGRES</productname> 行为兼容但违背 SQL 标准。此选项允许选择标准兼容的行为。
     </para>
   </listitem>

   <listitem>
    <para>
      允许 <literal>UPDATE ... SET col = DEFAULT</literal>（Rod）
     </para>
    <para>
      这允许 <command>UPDATE</command> 把列设为其声明的默认值。
     </para>
   </listitem>

   <listitem>
    <para>
      允许在 <literal>LIMIT</literal>/<literal>OFFSET</literal> 中使用表达式（Tom）
     </para>
    <para>
      在以前的版本中，<literal>LIMIT</literal>/<literal>OFFSET</literal> 只能使用常数，不能用表达式。
     </para>
   </listitem>

   <listitem>
    <para>
      实现 <literal>CREATE TABLE AS EXECUTE</literal>（Neil、Peter）
     </para>
   </listitem>
  </itemizedlist>
 </sect3>

 <sect3>
  <title>对象操作变更</title>

  <itemizedlist>
   <listitem>
    <para>
      使 <command>CREATE SEQUENCE</command> 语法更符合 SQL:2003（Neil）
     </para>
   </listitem>

   <listitem>
    <para>
      新增语句级触发器（Neil）
     </para>
    <para>
      虽然这允许触发器在语句结束时触发，但不允许触发器访问该语句修改的所有行。此能力计划在未来版本中提供。
     </para>
   </listitem>

   <listitem>
    <para>
      为域新增检查约束（Rod）
     </para>
    <para>
      这通过允许域使用检查约束大幅提高了域的用处。
     </para>
   </listitem>

   <listitem>
    <para>
      新增 <command>ALTER DOMAIN</command>（Rod）
     </para>
    <para>
      这允许操纵现有域。
     </para>
   </listitem>

   <listitem>
    <para>
      修复几个零列表缺陷（Tom）
     </para>
    <para>
      <productname>PostgreSQL</productname> 支持零列表。这修复了使用此类表时发生的各种缺陷。
     </para>
   </listitem>

   <listitem>
    <para>
      让 <literal>ALTER TABLE ... ADD PRIMARY KEY</literal> 添加非空约束（Rod）
     </para>
    <para>
      在以前的版本中，<literal>ALTER TABLE ... ADD PRIMARY</literal> 会添加唯一索引但不添加非空约束。此版本修复了这一点。
     </para>
   </listitem>

   <listitem><para>
      新增 <literal>ALTER TABLE ... WITHOUT OIDS</literal>（Rod）
     </para>
    <para>
      这允许控制新行和更新的行是否有 OID 列。这对节省存储空间最有用。
     </para>
   </listitem>

   <listitem>
    <para>
      新增 <literal>ALTER SEQUENCE</literal>，修改最小值、最大值、增量、缓存、循环值（Rod）
     </para>
   </listitem>

   <listitem>
    <para>
      新增 <literal>ALTER TABLE ... CLUSTER ON</literal>（Alvaro Herrera）
     </para>
    <para>
      此命令被 <command>pg_dump</command> 用来记录每个先前聚簇过的表的聚簇列。该信息被数据库范围的 cluster 用来聚簇所有先前聚簇过的表。
     </para>
   </listitem>

   <listitem><para>
      改进域的自动类型转换（Rod、Tom）
     </para></listitem>
   <listitem><para>
      允许标识符中的美元符号，但不能作为首字符（Tom）
     </para></listitem>
   <listitem><para>
      不允许操作符名中的美元符号，使 <literal>x=$1</literal> 可行（Tom）
     </para></listitem>

   <listitem>
    <para>
      允许用 <literal>LIKE <replaceable>subtable</replaceable></literal> 复制表模式，还有 SQL:2003 特性 <literal>INCLUDING DEFAULTS</literal>（Rod）
     </para>
   </listitem>

   <listitem>
    <para>
      为 <command>GRANT</command> 新增 <literal>WITH GRANT OPTION</literal> 子句（Peter）
     </para>
    <para>
      这使 <command>GRANT</command> 能授予其他用户在对象上授予权限的能力。
     </para>
   </listitem>
  </itemizedlist>
 </sect3>

 <sect3>
  <title>工具命令变更</title>

  <itemizedlist>
   <listitem>
    <para>
      为 <command>CREATE TABLE</command> 的临时表新增 <literal>ON COMMIT</literal> 子句（Gavin）
     </para>
    <para>
      这增加了在事务提交时删除表或删除所有行的能力。
     </para>
   </listitem>

   <listitem>
    <para>
      允许用 <literal>WITH HOLD</literal> 在事务之外使用游标（Neil）
     </para>
    <para>
      在以前的版本中，游标在创建它的事务结束时被删除。现在可以用 <literal>WITH HOLD</literal> 选项创建游标，这允许在创建事务提交后继续访问它们。
     </para>
   </listitem>

   <listitem>
    <para>
      <literal>FETCH 0</literal> 和 <literal>MOVE 0</literal> 现在不做任何事情（Bruce）
     </para>
    <para>
      在以前的版本中，<literal>FETCH 0</literal> 获取所有剩余行，<literal>MOVE 0</literal> 移到游标末尾。
     </para>
   </listitem>

   <listitem>
    <para>
      按照 SQL 标准，让 <command>FETCH</command> 和 <command>MOVE</command> 返回获取/移动的行数，如果位于游标开头/末尾则返回零（Bruce）
     </para>
    <para>
      在以前的版本中，<command>FETCH</command> 和 <command>MOVE</command> 返回的行数没有准确反映处理的行数。
     </para>
   </listitem>

   <listitem>
    <para>
      正确处理游标的 <literal>SCROLL</literal>，否则报告错误（Neil）
     </para>
    <para>
      对某些类型的查询允许随机访问（前向和后向滚动）需要一些额外的工作才能做到。如果创建游标时指定了 <literal>SCROLL</literal>，将执行这些额外工作。此外，如果游标以 <literal>NO SCROLL</literal> 创建，则不允许随机访问。
     </para>
   </listitem>

   <listitem>
    <para>
      为 <command>FETCH</command> 和 <command>MOVE</command> 实现 SQL 兼容的选项 <literal>FIRST</literal>、<literal>LAST</literal>、<literal>ABSOLUTE <replaceable>n</replaceable></literal>、<literal>RELATIVE <replaceable>n</replaceable></literal>（Tom）
     </para>
   </listitem>

   <listitem>
    <para>
      允许对 <command>DECLARE CURSOR</command> 使用 <command>EXPLAIN</command>（Tom）
     </para>
   </listitem>

   <listitem>
    <para>
      允许 <command>CLUSTER</command> 默认使用标记为预聚簇的索引（Alvaro Herrera）
     </para>
   </listitem>

   <listitem>
    <para>
      允许 <command>CLUSTER</command> 聚簇所有表（Alvaro Herrera）
     </para>
    <para>
      这允许用单条命令重新聚簇数据库中所有先前聚簇过的表。
     </para>
   </listitem>

   <listitem><para>
      禁止对部分索引使用 <command>CLUSTER</command>（Tom）
     </para></listitem>

   <listitem><para>
      允许 <command>COPY</command> 文件中的 DOS 和 Mac 行尾（Bruce）
     </para></listitem>

   <listitem>
    <para>
      不允许字面回车作为数据值，反斜杠-回车和 <literal>\r</literal> 仍被允许（Bruce）
     </para>
   </listitem>

   <listitem>
    <para>
      <command>COPY</command> 变更（二进制、<literal>\.</literal>）（Tom）
     </para>
   </listitem>

   <listitem>
    <para>
      从 <command>COPY</command> 失败中干净恢复（Tom）
     </para>
   </listitem>

   <listitem>
    <para>
      防止 <command>COPY</command> 中可能的内存泄漏（Tom）
     </para>
   </listitem>

   <listitem>
    <para>
      使 <command>TRUNCATE</command> 事务安全（Rod）
     </para>
    <para>
      <command>TRUNCATE</command> 现在可以在事务内使用。如果事务中止，<command>TRUNCATE</command> 所做的更改会自动回滚。
     </para>
   </listitem>

   <listitem>
    <para>
      允许预备/绑定 <command>FETCH</command> 和 <command>EXPLAIN</command> 等工具命令（Tom）
     </para>
   </listitem>

   <listitem>
    <para>
      新增 <command>EXPLAIN EXECUTE</command>（Neil）
     </para>
   </listitem>

   <listitem>
    <para>
      通过减少 WAL 流量改进索引上的 <command>VACUUM</command> 性能（Tom）
     </para>
   </listitem>

   <listitem>
    <para>
      函数索引已被推广为表达式上的索引（Tom）
     </para>
    <para>
      在以前的版本中，函数索引只支持应用于一个或多个列名的简单函数。此版本允许任何类型的标量表达式。
     </para>
   </listitem>

   <listitem>
    <para>
      让 <command>SHOW TRANSACTION ISOLATION</command> 与 <command>SET TRANSACTION ISOLATION</command> 的输入匹配（Tom）
     </para>
   </listitem>

   <listitem>
    <para>
      让对非本地数据库的 <command>COMMENT ON DATABASE</command> 生成警告而不是错误（Rod）
     </para>

    <para>
      数据库注释存储在数据库本地表中，因此对数据库的注释必须存储在每个数据库中。
     </para>
   </listitem>

   <listitem>
    <para>
      改进 <command>LISTEN</command>/<command>NOTIFY</command> 的可靠性（Tom）
     </para>
   </listitem>

   <listitem>
    <para>
      允许 <command>REINDEX</command> 可靠地重新索引非共享系统目录索引（Tom）
     </para>
    <para>
      这允许重新索引系统表而不需要独立会话，这在以前的版本中是必需的。现在唯一需要独立会话来重新索引的是全局系统表 <literal>pg_database</literal>、<literal>pg_shadow</literal> 和 <literal>pg_group</literal>。
     </para>
   </listitem>
  </itemizedlist>
 </sect3>

 <sect3>
  <title>数据类型和函数变更</title>

  <itemizedlist>
   <listitem>
    <para>
      新服务器参数 <varname>extra_float_digits</varname>，控制浮点数的精度显示（Pedro Ferreira、Tom）
     </para>
    <para>
      这控制导致回归测试问题的输出精度。
     </para>
   </listitem>

   <listitem><para>
      允许 <literal>+1300</literal> 作为数字时区说明符，用于 FJST（Tom）
     </para></listitem>

   <listitem>
    <para>
      移除很少使用的函数 <function>oidrand</function>、<function>oidsrand</function> 和 <function>userfntest</function>（Neil）
     </para>
   </listitem>

   <listitem>
    <para>
      把 <function>md5()</function> 函数加入主服务器，此前已在 <filename>contrib/pgcrypto</filename> 中（Joe）
     </para>
    <para>
      MD5 函数是常被要求的。需要更复杂的加密能力时，请使用 <filename>contrib/pgcrypto</filename>。
     </para>
   </listitem>

   <listitem><para>
      扩大 <type>timestamp</type> 的日期范围（John Cochran）
     </para></listitem>

   <listitem>
    <para>
      更改 <literal>EXTRACT(EPOCH FROM timestamp)</literal>，使 <type>timestamp without time zone</type> 被假定处于本地时间而不是 GMT（Tom）
     </para>
   </listitem>

   <listitem><para>
      以防操作系统不防止除以零，捕获它（Tom）
     </para></listitem>
   <listitem><para>
      把 <type>numeric</type> 数据类型内部改为以 10000 为基数（Tom）
     </para></listitem>
   <listitem><para>
      新函数 <function>hostmask()</function>（Greg Wickham）
     </para></listitem>
   <listitem><para>
      <function>to_char()</function> 和 <function>to_timestamp()</function> 的修复（Karel）
     </para></listitem>

   <listitem>
    <para>
      允许接受任意参数数据类型并返回任意数据类型的函数，使用 <type>anyelement</type> 和 <type>anyarray</type>（Joe）
     </para>
    <para>
      这允许创建能用于任何数据类型的函数。
     </para>
   </listitem>

   <listitem>
    <para>
      数组现在可以指定为 <literal>ARRAY[1,2,3]</literal>、<literal>ARRAY[['a','b'],['c','d']]</literal> 或 <literal>ARRAY[ARRAY[ARRAY[2]]]</literal>（Joe）
     </para>
   </listitem>

   <listitem>
    <para>
      允许数组的正确比较，包括 <literal>ORDER BY</literal> 和 <literal>DISTINCT</literal> 支持（Joe）
     </para>
   </listitem>

   <listitem><para>
      允数组列上的索引（Joe）
     </para></listitem>
   <listitem><para>
      允许用 <literal>||</literal> 连接数组（Joe）
     </para></listitem>

   <listitem>
    <para>
      允许 <literal>WHERE</literal> 限定 <literal><replaceable>expr</replaceable> <replaceable>op</replaceable> ANY/SOME/ALL (<replaceable>array_expr</replaceable>)</literal>（Joe）
     </para>
    <para>
      这允许数组表现得像值列表，用于诸如 <literal>SELECT * FROM tab WHERE col IN (array_val)</literal> 之类的用途。
     </para>
   </listitem>

   <listitem>
    <para>
      新数组函数 <function>array_append</function>、<function>array_cat</function>、<function>array_lower</function>、<function>array_prepend</function>、<function>array_to_string</function>、<function>array_upper</function>、<function>string_to_array</function>（Joe）
     </para>
   </listitem>

   <listitem><para>
      允许用户定义的聚合使用多态函数（Joe）
     </para></listitem>
   <listitem><para>
      允许给空数组赋值（Joe）
     </para></listitem>

   <listitem>
    <para>
      允许 <type>time</type>、<type>timestamp</type> 和 <type>interval</type> 输入值的秒字段为 60（Tom）
     </para>
    <para>
      闰秒需要六十秒的值。
     </para>
   </listitem>

   <listitem><para>
      允许 <type>cidr</type> 数据类型转换为 <type>text</type>（Tom）
     </para></listitem>

   <listitem><para>
      不允许 SET TIMEZONE 中的无效时区名
     </para></listitem>

   <listitem>
    <para>
      当 <type>char</type> 转换为 <type>varchar</type> 或 <type>text</type> 时修剪尾随空格（Tom）
     </para>
   </listitem>

   <listitem>
    <para>
      让 <type>float(<replaceable>p</replaceable>)</type> 用二进制位而不是十进制位度量精度 <replaceable>p</replaceable>（Tom）
     </para>
   </listitem>

   <listitem>
    <para>
      为 <type>inet</type> 和 <type>cidr</type> 数据类型添加 IPv6 支持（Michael Graff）
     </para>
   </listitem>

   <listitem>
    <para>
      新增 <function>family()</function> 函数，报告地址是 IPv4 还是 IPv6（Michael Graff）
     </para>
   </listitem>

   <listitem>
    <para>
      让 <literal>SHOW datestyle</literal> 生成与 <literal>SET datestyle</literal> 所用类似的输出（Tom）
     </para>
   </listitem>

   <listitem>
    <para>
      让 <literal>EXTRACT(TIMEZONE)</literal> 和 <literal>SET/SHOW TIME ZONE</literal> 遵循 SQL 对时区偏移符号的约定，即正值为 UTC 以东（Tom）
     </para>
   </listitem>

   <listitem>
    <para>
      修复 <literal>date_trunc('quarter', ...)</literal>（B&ouml;jthe Zolt&aacute;n）
     </para>
    <para>
      以前的版本对此函数调用返回不正确的值。
     </para>
   </listitem>

   <listitem>
    <para>
      使 <function>initcap()</function> 更兼容 Oracle（Mike Nolan）
     </para>
    <para>
      <function>initcap()</function> 现在把任何非字母数字字符之后出现的字母大写，而不只是空白之后。
     </para>
   </listitem>

   <listitem>
    <para>
      对非 ISO-8601 格式的日期值只允许 <varname>datestyle</varname> 字段顺序（Greg）
     </para>
   </listitem>

   <listitem>
    <para>
      新增 <varname>datestyle</varname> 值 <literal>MDY</literal>、<literal>DMY</literal> 和 <literal>YMD</literal> 来设置输入字段顺序；为向后兼容保留 <literal>US</literal> 和 <literal>European</literal>（Tom）
     </para>
   </listitem>

   <listitem>
    <para>
      像 <literal>'now'</literal> 或 <literal>'today'</literal> 这样的字符串字面量将不再可用作列默认值。请改用 <function>now()</function>、<function>current_timestamp</function> 等函数。（预备语句需要此变更）（Tom）
     </para>
   </listitem>

   <listitem>
    <para>
      在 <function>min()</function>/<function>max()</function> 中把 NaN 当作大于任何其他值（Tom）
     </para>
    <para>
      在大多数用途中 NaN 已排在普通数值之后，但 <function>min()</function> 和 <function>max()</function> 没有正确处理。
     </para>
   </listitem>

   <listitem>
    <para>
      防止 interval 抑制 <literal>:00</literal> 秒的显示
     </para>
   </listitem>

   <listitem>
    <para>
      新函数 <function>pg_get_triggerdef(prettyprint)</function> 和 <function>pg_conversion_is_visible()</function>（Christopher）
     </para>
   </listitem>

   <listitem>
    <para>
      允许时间指定为 <literal>040506</literal> 或 <literal>0405</literal>（Tom）
     </para>
   </listitem>

   <listitem>
    <para>
      输入日期顺序现在必须是 <literal>YYYY-MM-DD</literal>（4 位年份）或匹配 <varname>datestyle</varname>
     </para>
   </listitem>

   <listitem>
    <para>
      让 <function>pg_get_constraintdef</function> 支持唯一、主键和检查约束（Christopher）
     </para>
   </listitem>
  </itemizedlist>
 </sect3>

 <sect3>
  <title>服务器端语言变更</title>

  <itemizedlist>
   <listitem>
    <para>
      防止对零行记录变量使用 <literal>RETURN NEXT</literal> 时 PL/pgSQL 崩溃（Tom）
     </para>
   </listitem>

   <listitem>
    <para>
      让 PL/Python 的 <function>spi_execute</function> 接口正确处理空值（Andrew Bosma）
     </para>
   </listitem>

   <listitem>
    <para>
      允许 PL/pgSQL 不用 <literal>%ROWTYPE</literal> 声明组合类型的变量（Tom）
     </para>
   </listitem>

   <listitem>
    <para>
      修复 PL/Python 的 <function>_quote()</function> 函数以处理大整数
     </para>
   </listitem>

   <listitem>
    <para>
      把 PL/Python 改为不可信语言，现名 <literal>plpythonu</literal>（Kevin Jacobs、Tom）
     </para>
    <para>
      Python 语言不再支持受限执行环境，因此删除了可信版 PL/Python。如果这种情况改变，将重新添加可供非超级用户使用的 PL/Python 版本。
     </para>
   </listitem>

   <listitem>
    <para>
      允许多态 PL/pgSQL 函数（Joe、Tom）
     </para>
   </listitem>

   <listitem>
    <para>
      允许多态 SQL 函数（Joe）
     </para>
   </listitem>

   <listitem>
    <para>
      改进了 PL/pgSQL 的编译函数缓存机制，完全支持多态（Joe）
     </para>
   </listitem>

   <listitem>
    <para>
      在 PL/pgSQL 中新增表示函数实际返回类型的新参数 <literal>$0</literal>（Joe）
     </para>
   </listitem>

   <listitem>
    <para>
      允许 PL/Tcl 和 PL/Python 在多个表上使用同一触发器（Tom）
     </para>
   </listitem>

   <listitem>
    <para>
      修复 PL/Tcl 的 <function>spi_prepare</function>，使其接受参数类型列表中完全限定的类型名（Jan）
     </para>
   </listitem>
  </itemizedlist>
 </sect3>

 <sect3>
  <title>psql 变更</title>

  <itemizedlist>
   <listitem>
    <para>
      新增 <literal>\pset pager always</literal>，总是使用分页器（Greg）
     </para>
    <para>
      这强制使用分页器，即使行数少于屏幕高度。这对跨多个屏幕行的行很有价值。
     </para>
   </listitem>

   <listitem><para>
      改进 Tab 补全（Rod、Ross Reedstrom、Ian Barwick）
     </para></listitem>
   <listitem><para>
      把 <literal>\?</literal> 帮助重排为分组（Harald Armin Massa、Bruce）
     </para></listitem>
   <listitem><para>
      新增列出模式、转换和编码转换的反斜杠命令（Christopher）
     </para></listitem>

   <listitem>
    <para>
      <command>\encoding</command> 现在根据服务器参数 <varname>client_encoding</varname> 变化（Tom）
     </para>
    <para>
      在以前的版本中，<command>\encoding</command> 不知道用 <literal>SET client_encoding</literal> 所做的编码更改。
     </para>
   </listitem>

   <listitem>
    <para>
      把编辑器缓冲区保存到 readline 历史（Ross）
     </para>
    <para>
      当用 <command>\e</command> 编辑查询时，结果被保存到 readline 历史中，可用上箭头检索。
     </para>
   </listitem>

   <listitem><para>
      改进 <command>\d</command> 显示（Christopher）
     </para></listitem>
   <listitem><para>
      增强 HTML 模式使其更符合标准（Greg）
     </para></listitem>

   <listitem>
    <para>
      新的 <command>\set AUTOCOMMIT off</command> 能力（Tom）
     </para>
    <para>
      这取代了被移除的服务器参数 <varname>autocommit</varname>。
     </para>
   </listitem>

   <listitem>
    <para>
      新的 <command>\set VERBOSITY</command>，控制错误细节（Tom）
     </para>
    <para>
      这控制新的错误报告细节。
     </para>
   </listitem>

   <listitem><para>
      新的提示符转义序列 <literal>%x</literal>，显示事务状态（Tom）
     </para></listitem>
   <listitem><para>
      <application>psql</application> 的长选项现在在所有平台上可用
     </para></listitem>
  </itemizedlist>
 </sect3>

 <sect3>
  <title>pg_dump 变更</title>

  <itemizedlist>
   <listitem><para>
      多个 pg_dump 修复，包括 tar 格式和大对象
     </para></listitem>
   <listitem><para>
      允许 pg_dump 转储特定的模式（Neil）
     </para></listitem>

   <listitem>
    <para>
      让 pg_dump 保留列存储特性（Christopher）
     </para>
    <para>
      这保留 <literal>ALTER TABLE ... SET STORAGE</literal> 信息。
     </para>
   </listitem>

   <listitem><para>
      让 pg_dump 保留 <command>CLUSTER</command> 特性（Christopher）
     </para></listitem>

   <listitem>
    <para>
      让 pg_dumpall 用 <command>GRANT</command>/<command>REVOKE</command> 转储数据库级权限（Tom）
     </para>
   </listitem>

   <listitem>
    <para>
      允许 pg_dumpall 支持 pg_dump 的选项 <option>-a</option>、<option>-s</option>、<option>-x</option>（Tom）
     </para>
   </listitem>

   <listitem><para>
      防止 pg_dump 把命令行上指定的标识符转为小写（Tom）
     </para></listitem>

   <listitem>
    <para>
      pg_dump 选项 <option>--use-set-session-authorization</option> 和 <option>--no-reconnect</option> 现在无作用，所有转储都使用 <command>SET SESSION AUTHORIZATION</command>
     </para>
    <para>
      pg_dump 不再重新连接来切换用户，而是始终使用 <command>SET SESSION AUTHORIZATION</command>。这将减少恢复期间的口令提示。
     </para>
   </listitem>

   <listitem>
    <para>
      <application>pg_dump</application> 的长选项现在在所有平台上可用
     </para>
    <para>
      <productname>PostgreSQL</productname> 现在包含自己的长选项处理例程。
     </para>
   </listitem>
  </itemizedlist>
 </sect3>

 <sect3>
  <title>libpq 变更</title>

  <itemizedlist>
   <listitem>
    <para>
      新增用于在 Windows 上释放内存的函数 <function>PQfreemem</function>，建议用于 <command>NOTIFY</command>（Bruce）
     </para>
    <para>
      Windows 要求库中分配的内存由同一库中的函数释放，因此 <function>free()</function> 不能释放 libpq 分配的内存。<function>PQfreemem</function> 是释放 libpq 内存的正确方式，特别是在 Windows 上，也建议在其他平台上使用。
     </para>
   </listitem>

   <listitem>
    <para>
      记录服务能力并添加示例文件（Bruce）
     </para>
    <para>
      这允许客户端在客户端机器上的中心文件中查找连接信息。
     </para>
   </listitem>

   <listitem>
    <para>
      让 <function>PQsetdbLogin</function> 与 <function>PQconnectdb</function> 有相同的默认值（Tom）
     </para>
   </listitem>

   <listitem><para>
      允许 libpq 在结果集过大时干净地失败（Tom）
     </para></listitem>

   <listitem>
    <para>
      改进函数 <function>PQunescapeBytea</function> 的性能（Ben Lamb）
     </para>
   </listitem>

   <listitem>
    <para>
      用 <filename>configure</filename> 选项 <option>--enable-thread-safety</option> 允许线程安全的 libpq（Lee Kindness、Philip Yarra）
     </para>
   </listitem>

   <listitem>
    <para>
      允许函数 <function>pqInternalNotice</function> 接受格式串和参数而不只是预先格式化的消息（Tom、Sean Chittenden）
     </para>
   </listitem>

   <listitem>
    <para>
      用 <literal>sslmode</literal> 值 <literal>disable</literal>、<literal>allow</literal>、<literal>prefer</literal> 和 <literal>require</literal> 控制 SSL 协商（Jon Jensen）
     </para>
   </listitem>

   <listitem>
    <para>
      允许新的错误码和文本级别（Tom）
     </para>
   </listitem>

   <listitem>
    <para>
      允许访问查询结果的基础表和列（Tom）
     </para>
    <para>
      这对想知道与特定结果集关联的基础表和列名的查询构建器应用程序很有帮助。
     </para>
   </listitem>

   <listitem><para>
      允许访问当前事务状态（Tom）
     </para></listitem>
   <listitem><para>
      新增把二进制数据直接传递给服务器的能力（Tom）
     </para></listitem>

   <listitem>
    <para>
      新增函数 <function>PQexecPrepared</function> 和 <function>PQsendQueryPrepared</function>，执行先前预备语句的绑定/执行（Tom）
     </para>
    </listitem>
  </itemizedlist>
 </sect3>

 <sect3>
  <title>JDBC 变更</title>

  <itemizedlist>
   <listitem><para>
      允许可更新结果集上的 <function>setNull</function>
     </para></listitem>
   <listitem><para>
      允许预备语句上的 <function>executeBatch</function>（Barry）
     </para></listitem>
   <listitem><para>
      支持 SSL 连接（Barry）
     </para></listitem>
   <listitem><para>
      处理结果集中的模式名（Paul Sorenson）
     </para></listitem>
   <listitem><para>
      新增 refcursor 支持（Nic Ferrier）
     </para></listitem>
  </itemizedlist>
 </sect3>

 <sect3>
  <title>其他接口变更</title>

  <itemizedlist>
   <listitem>
    <para>
      防止 libpgtcl 关闭期间可能的内存泄漏或核心转储（Tom）
     </para>
   </listitem>
   <listitem>
    <para>
      为 ECPG 新增 Informix 兼容性（Michael）
     </para>
    <para>
      这允许 ECPG 处理使用某些 Informix 扩展编写的嵌入式 C 程序。
     </para>
   </listitem>

   <listitem>
    <para>
      为 ECPG 新增定长的 <type>decimal</type> 类型，用于 Informix（Michael）
     </para>
   </listitem>

   <listitem>
    <para>
      用 <filename>configure</filename> 选项 <option>--enable-thread-safety</option> 允许线程安全的嵌入式 SQL 程序（Lee Kindness、Bruce）
     </para>
    <para>
      这允许多个线程同时访问数据库。
     </para>
   </listitem>

   <listitem>
    <para>
      把 Python 客户端 PyGreSQL 移到 <ulink url="http://www.pygresql.org"></ulink>（Marc）
     </para>
   </listitem>
  </itemizedlist>
 </sect3>

 <sect3>
  <title>源代码变更</title>

  <itemizedlist>
   <listitem><para>
      消除对单独的平台几何回归结果文件的需要（Tom）
     </para></listitem>
   <listitem><para>
      改进的 PPC 锁定原语（Reinhard Max）
     </para></listitem>
   <listitem><para>
      新函数 <function>palloc0</function>，分配并清零内存（Bruce）
     </para></listitem>
   <listitem><para>
      修复 s390x CPU（64 位）的锁定代码（Tom）
     </para></listitem>
   <listitem><para>
      允许 OpenBSD 使用本地 ident 凭据（William Ahern）
     </para></listitem>
   <listitem><para>
      使查询计划树对执行器只读（Tom）
     </para></listitem>
   <listitem><para>
      新增 Darwin 启动脚本（David Wheeler）
     </para></listitem>
   <listitem><para>
      允许 libpq 用 Borland C++ 编译器编译（Lester Godwin、Karl Waclawek）
     </para></listitem>
   <listitem><para>
      需要时使用我们自己的 <function>getopt_long()</function> 版本（Peter）
     </para></listitem>
   <listitem><para>
      把管理脚本转换为 C（Peter）
     </para></listitem>
   <listitem><para>
      如果从 CVS 构建，现在需要 Bison &gt;= 1.85 来构建 <productname>PostgreSQL</productname> 语法
     </para></listitem>
   <listitem><para>
      把文档合并为一本书（Peter）
     </para></listitem>
   <listitem><para>
      新增 Windows 兼容函数（Bruce）
     </para></listitem>
   <listitem><para>
      允许客户端接口在 MinGW 下编译（Bruce）
     </para></listitem>
   <listitem><para>
      新的用于错误报告的 <function>ereport()</function> 函数（Tom）
     </para></listitem>
   <listitem><para>
      支持 Linux 上的 Intel 编译器（Peter）
     </para></listitem>
   <listitem><para>
      改进 Linux 启动脚本（Slawomir Sudnik、Darko Prenosil）
     </para></listitem>
   <listitem><para>
      新增对 AMD Opteron 和 Itanium 的支持（Jeffrey W. Baker、Bruce）
     </para></listitem>
   <listitem>
    <para>
      从 <command>configure</command> 移除 <option>--enable-recode</option> 选项
     </para>
    <para>
      既然有了 <command>CREATE CONVERSION</command>，这不再需要。
     </para>
   </listitem>
   <listitem>
    <para>
      如果未找到自旋锁代码则产生编译错误（Bruce）
     </para>
    <para>
      没有自旋锁代码的平台现在将编译失败，而不是静默使用信号量。此失败可以用新的 <command>configure</command> 选项禁用。
     </para>
   </listitem>
  </itemizedlist>
 </sect3>

 <sect3>
  <title>附加模块变更</title>

  <itemizedlist>
   <listitem><para>
      把 dbmirror 许可证改为 BSD
     </para></listitem>
   <listitem><para>
      改进 earthdistance（Bruno Wolff III）
     </para></listitem>
   <listitem><para>
      pgcrypto 的可移植性改进（Marko Kreen）
     </para></listitem>
   <listitem><para>
      防止 xml 中的崩溃（John Gray、Michael Richards）
     </para></listitem>
   <listitem><para>
      更新 oracle
     </para></listitem>
   <listitem><para>
      更新 mysql
     </para></listitem>
   <listitem><para>
      更新 cube（Bruno Wolff III）
     </para></listitem>
   <listitem><para>
      更新 earthdistance 以使用 cube（Bruno Wolff III）
     </para></listitem>
   <listitem><para>
      更新 btree_gist（Oleg）
     </para></listitem>
   <listitem><para>
      新的 tsearch2 全文搜索模块（Oleg、Teodor）
     </para></listitem>
   <listitem><para>
      为 tablefuncs 新增基于哈希的 crosstab 函数（Joe）
     </para></listitem>
   <listitem><para>
      为 tablefuncs 新增用来对 <function>connectby()</function> 兄弟节点排序的 serial 列（Nabil Sayegh、Joe）
     </para></listitem>
   <listitem><para>
      为 dblink 新增命名持久连接（Shridhar Daithanka）
     </para></listitem>
   <listitem><para>
      新的 pg_autovacuum 允许自动 <command>VACUUM</command>（Matthew T. O'Connor）
     </para></listitem>
   <listitem><para>
      让 pgbench 遵循环境变量 <envar>PGHOST</envar>、<envar>PGPORT</envar>、<envar>PGUSER</envar>（Tatsuo）
     </para></listitem>
   <listitem><para>
      改进 intarray（Teodor Sigaev）
     </para></listitem>
   <listitem><para>
      改进 pgstattuple（Rod）
     </para></listitem>
   <listitem><para>
      修复 fuzzystrmatch 中 <function>metaphone()</function> 的缺陷
     </para></listitem>
   <listitem><para>
      改进 adddepend（Rod）
     </para></listitem>
   <listitem><para>
      更新 spi/timetravel（B&ouml;jthe Zolt&aacute;n）
     </para></listitem>
   <listitem><para>
      修复 dbase 的 <option>-s</option> 选项并改进非 ASCII 处理（Thomas Behr、M&aacute;rcio Smiderle）
     </para></listitem>
   <listitem><para>
      移除 array 模块，因为其功能现已默认包含（Joe）
     </para></listitem>
  </itemizedlist>
 </sect3>
 </sect2>
</sect1>
