<!--
$Header: /cvsroot/pgsql/doc/src/sgml/Attic/libpgtcl.sgml,v 1.39 2003/10/31 00:18:55 tgl Exp $
-->

<chapter id="pgtcl">
 <title><application>pgtcl</application> - Tcl 绑定库</title>

 <indexterm zone="pgtcl">
  <primary>libpgtcl</primary>
 </indexterm>

 <indexterm zone="pgtcl">
  <primary>pgtcl</primary>
 </indexterm>

 <indexterm zone="pgtcl">
  <primary>Tcl</primary>
 </indexterm>

  <para>
   <application>pgtcl</application> 是一个 Tcl 包，供客户端程序与
   <ProductName>PostgreSQL</ProductName> 服务器交互。它使 Tcl 脚本可以使用
   <application>libpq</application> 的大部分功能。
  </para>

 <sect1 id="pgtcl-overview">
  <title>概述</title>

  <para>
   <xref linkend="pgtcl-commands-table">概述了
   <application>pgtcl</application> 中可用的命令。这些命令将在后续页面中进一步描述。
  </para>


<table id="pgtcl-commands-table">
<title><application>pgtcl</application> 命令</title>
<tgroup cols="2">
<thead>
  <row>
    <entry>命令</entry>
    <entry>描述</entry>
  </row>
</thead>

<tbody>
  <row>
    <entry><function>pg_connect</function></entry>
    <entry>打开一个到服务器的连接</entry>
  </row>
  <row>
    <entry><function>pg_disconnect</function></entry>
    <entry>关闭一个到服务器的连接</entry>
  </row>
  <row>
    <entry><function>pg_conndefaults</function></entry>
    <entry>获取连接选项及其默认值</entry>
  </row>
  <row>
    <entry><function>pg_exec</function></entry>
    <entry>向服务器发送一条命令</entry>
  </row>
  <row>
    <entry><function>pg_result</function></entry>
    <entry>获取关于命令结果的信息</entry>
  </row>
  <row>
    <entry><function>pg_select</function></entry>
    <entry>循环遍历查询的结果</entry>
  </row>
  <row>
    <entry><function>pg_execute</function></entry>
    <entry>发送一条查询并可选地遍历其结果</entry>
  </row>
  <row>
    <entry><function>pg_listen</function></entry>
    <entry>设置或更改异步通知消息的回调</entry>
  </row>
  <row>
    <entry><function>pg_on_connection_loss</function></entry>
    <entry>设置或更改意外断开连接的回调</entry>
  </row>

  <row>
    <entry><function>pg_lo_creat</function></entry>
    <entry>创建一个大对象</entry>
  </row>
  <row>
    <entry><function>pg_lo_open</function></entry>
    <entry>打开一个大对象</entry>
  </row>
  <row>
    <entry><function>pg_lo_close</function></entry>
    <entry>关闭一个大对象</entry>
  </row>
  <row>
    <entry><function>pg_lo_read</function></entry>
    <entry>从大对象读取</entry>
  </row>
  <row>
    <entry><function>pg_lo_write</function></entry>
    <entry>写入大对象</entry>
  </row>
  <row>
    <entry><function>pg_lo_lseek</function></entry>
    <entry>在大对象中定位到某个位置</entry>
  </row>
  <row>
    <entry><function>pg_lo_tell</function></entry>
    <entry>返回大对象的当前定位位置</entry>
  </row>
  <row>
    <entry><function>pg_lo_unlink</function></entry>
    <entry>删除一个大对象</entry>
  </row>
  <row>
    <entry><function>pg_lo_import</function></entry>
    <entry>从文件导入一个大对象</entry>
  </row>
  <row>
    <entry><function>pg_lo_export</function></entry>
    <entry>将大对象导出到文件</entry>
  </row>
</tbody>
</tgroup>
</table>

  <para>
   <function>pg_lo_*</function> 命令是
   <ProductName>PostgreSQL</ProductName> 大对象功能的接口。<indexterm><primary>大对象</><secondary>在 pgtcl 中</></>这些函数的设计模仿标准 Unix 文件系统接口中类似的文件系统函数。<function>pg_lo_*</function> 命令应当在一个
   <command>BEGIN</command>/<command>COMMIT</command> 事务块中使用，因为
   <function>pg_lo_open</function> 返回的描述符只在当前事务中有效。<function>pg_lo_import</function> 和
   <function>pg_lo_export</function> <emphasis>必须</emphasis>在一个 <command>BEGIN</command>/<command>COMMIT</command> 事务块中使用。
  </para>

 </sect1>

<sect1 id="libpgtcl-loading">
<title>将 <application>pgtcl</application> 载入应用程序</title>

   <para>
    在使用 <application>pgtcl</application> 命令之前，你必须把
    <filename>libpgtcl</> 库载入你的 Tcl 应用程序。这通常用 Tcl 的
    <literal>load</> 命令完成。下面是一个例子：

<programlisting>
load libpgtcl[info sharedlibextension]
</programlisting>

    推荐使用 <literal>info sharedlibextension</>，而不是在程序中硬编码 <literal>.so</> 或 <literal>.sl</>。
   </para>

   <para>
    除非系统的动态载入器知道到哪里寻找 <filename>libpgtcl</> 共享库文件，否则 <literal>load</> 命令会失败。你可能需要使用
    <command>ldconfig</>，或者设置环境变量
    <envar>LD_LIBRARY_PATH</>，或者使用你的平台上的一些等价设施才能让它工作。更多信息请参阅
    <productname>PostgreSQL</> 的安装说明。
   </para>

   <para>
    <filename>libpgtcl</> 又依赖于 <filename>libpq</>，因此动态载入器还必须能够找到 <filename>libpq</> 共享库。在实践中这很少成为问题，因为这两个共享库通常存放在同一个目录中，但在某些配置下它可能成为一个绊脚石。
   </para>

   <para>
    如果你的应用程序使用自定义的可执行文件，你可以选择把
    <filename>libpgtcl</> 静态绑定到可执行文件中，从而避免 <literal>load</> 命令以及动态链接的潜在问题。一个例子参见 <application>pgtclsh</> 的源代码。
   </para>

</sect1>

<sect1 id="libpgtcl-ref">
<title><application>pgtcl</application> 命令参考</title>

<refentry ID="PGTCL-PGCONNECT">
 <refmeta>
  <refentrytitle>pg_connect</refentrytitle>
 </refmeta>

 <refnamediv>
  <refname>pg_connect</refname>
  <refpurpose>打开一个到服务器的连接</refpurpose>
  <indexterm ID="IX-PGTCL-PGCONNECT-2"><primary>pg_connect</primary></indexterm>
 </refnamediv>

 <refsynopsisdiv>
<synopsis>
pg_connect -conninfo <parameter>connectOptions</parameter>
pg_connect <parameter>dbName</parameter> <optional role="tcl">-host <parameter>hostName</parameter></optional> <optional role="tcl">-port <parameter>portNumber</parameter></optional> <optional role="tcl">-tty <parameter>tty</parameter</optional> <optional role="tcl">-options <parameter>serverOptions</parameter></optional>
</synopsis>
 </refsynopsisdiv>

 <refsect1>
  <title>描述</title>

  <para>
   <function>pg_connect</function> 打开一个到
   <ProductName>PostgreSQL</ProductName> 服务器的连接。
  </para>

  <para>
   有两种可用的语法。在较老的一种中，每个可能的选项在
   <command>pg_connect</command> 命令中都有一个单独的选项开关。在较新的一种中，提供的是单个选项字符串，其中可以包含多个选项值。可以用 <function>pg_conndefaults</function> 获取关于较新语法中可用选项的信息。
  </para>
 </refsect1>

 <refsect1>
  <title>参数</title>

  <variablelist>
   <title>新风格</title>

   <varlistentry>
    <term><parameter>connectOptions</parameter></term>
    <listitem>
     <para>
      一个连接选项字符串，每个选项都写成
      <literal>keyword = value</> 的形式。有效选项的列表可以在
      <application>libpq</> 函数 <function>PQconnectdb</> 的描述中找到。
     </para>
    </listitem>
   </varlistentry>
  </variablelist>

  <variablelist>
   <title>旧风格</title>

   <varlistentry>
    <term><parameter>dbName</parameter></term>
    <listitem>
     <para>
      要连接的数据库名。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><option>-host <parameter>hostName</parameter></option></term>
    <listitem>
     <para>
      要连接的数据库服务器的主机名。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><option>-port <parameter>portNumber</parameter></option></term>
    <listitem>
     <para>
      要连接的数据库服务器的 TCP 端口号。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><option>-tty <parameter>tty</parameter></option></term>
    <listitem>
     <para>
      用于服务器可选调试输出的文件或
      <acronym>TTY</acronym>。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><option>-options <parameter>serverOptions</parameter></option></term>
    <listitem>
     <para>
      要传递给服务器的额外配置选项。
     </para>
    </listitem>
   </varlistentry>
  </variablelist>
 </refsect1>

 <refsect1>
  <title>返回值</title>

  <para>
   如果成功，返回一个数据库连接的句柄。句柄以 <literal>pgsql</literal> 前缀开头。
  </para>
 </refsect1>
</refentry>


<refentry ID="PGTCL-PGDISCONNECT">
 <refmeta>
  <refentrytitle>pg_disconnect</refentrytitle>
 </refmeta>

 <refnamediv>
  <refname>pg_disconnect</refname>
  <refpurpose>关闭一个到服务器的连接</refpurpose>
  <indexterm ID="IX-PGTCL-PGDISCONNECT-2"><primary>pg_disconnect</primary></indexterm>
 </refnamediv>

 <refsynopsisdiv>
<synopsis>
pg_disconnect <parameter>conn</parameter>
</synopsis>
 </refsynopsisdiv>

 <refsect1>
  <title>描述</title>

  <para>
   <function>pg_disconnect</function> 关闭一个到
   <productname>PostgreSQL</productname> 服务器的连接。
  </para>
 </refsect1>

 <refsect1>
  <title>参数</title>

  <variablelist>
   <varlistentry>
    <term><parameter>conn</parameter></term>
    <listitem>
     <para>
      要关闭的连接的句柄。
     </para>
    </listitem>
   </varlistentry>
  </variablelist>
 </refsect1>

 <refsect1>
  <title>返回值</title>

  <para>
   无
  </para>
 </refsect1>
</refentry>


<refentry ID="PGTCL-PGCONNDEFAULTS">
 <refmeta>
  <refentrytitle>pg_conndefaults</refentrytitle>
 </refmeta>

 <refnamediv>
  <refname>pg_conndefaults</refname>
  <refpurpose>获取连接选项及其默认值</refpurpose>
  <indexterm ID="IX-PGTCL-PGCONNDEFAULTS-2"><primary>pg_conndefaults</primary></indexterm>
 </refnamediv>

 <refsynopsisdiv>
<synopsis>
pg_conndefaults
</synopsis>
 </refsynopsisdiv>

 <refsect1>
  <title>描述</title>

  <para>
   <function>pg_conndefaults</function> 返回关于
   <function>pg_connect
   -conninfo</function> 中可用的连接选项的信息以及每个选项的当前默认值。
  </para>
 </refsect1>

 <refsect1>
  <title>参数</title>

  <para>
   无
  </para>
 </refsect1>

 <refsect1>
  <title>返回值</title>

  <para>
   结果是一个列表，描述可能的连接选项及其当前默认值。列表中的每个条目都是一个如下格式的子列表：
<synopsis>
{optname label dispchar dispsize value}
</synopsis>
   其中 <replaceable>optname</> 可用作
   <function>pg_connect -conninfo</function> 的一个选项。
  </para>
 </refsect1>
</refentry>


<refentry ID="PGTCL-PGEXEC">
 <refmeta>
  <refentrytitle>pg_exec</refentrytitle>
 </refmeta>

 <refnamediv>
  <refname>pg_exec</refname>
  <refpurpose>向服务器发送一条命令</refpurpose>
  <indexterm ID="IX-PGTCL-PGEXEC-2"><primary>pg_exec</primary></indexterm>
 </refnamediv>

 <refsynopsisdiv>
<synopsis>
pg_exec <parameter>conn</parameter> <parameter>commandString</parameter>
</synopsis>
 </refsynopsisdiv>

 <refsect1>
  <title>描述</title>

  <para>
   <function>pg_exec</function> 向
   <productname>PostgreSQL</productname> 服务器提交一条命令并返回一个结果。命令结果句柄以连接句柄开头，再加上一个句点和一个结果编号。
  </para>

  <para>
   注意，没有发生 Tcl 错误并不能证明命令成功了！服务器返回的错误消息会被作为带有失败状态的命令结果来处理，而不是让
   <function>pg_exec</function> 产生一个 Tcl 错误。
  </para>
 </refsect1>

 <refsect1>
  <title>参数</title>

  <variablelist>
   <varlistentry>
    <term><parameter>conn</parameter></term>
    <listitem>
     <para>
      要在其上执行命令的连接的句柄。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><parameter>commandString</parameter></term>
    <listitem>
     <para>
      要执行的 SQL 命令。
     </para>
    </listitem>
   </varlistentry>
  </variablelist>
 </refsect1>

 <refsect1>
  <title>返回值</title>

  <para>
   一个结果句柄。如果
   <application>pgtcl</application> 无法获得服务器响应，将返回一个 Tcl 错误。否则，会创建一个命令结果对象并返回它的一个句柄。这个句柄可以传递给
   <function>pg_result</function> 以获取该命令的结果。
  </para>
 </refsect1
</refentry>


<refentry ID="PGTCL-PGRESULT">
 <refmeta>
  <refentrytitle>pg_result</refentrytitle>
 </refmeta>

 <refnamediv>
  <refname>pg_result</refname>
  <refpurpose>获取关于命令结果的信息</refpurpose>
  <indexterm ID="IX-PGTCL-PGRESULT-2"><primary>pg_result</primary></indexterm>
 </refnamediv>

 <refsynopsisdiv>
<synopsis>
pg_result <parameter>resultHandle</parameter> <parameter>resultOption</parameter>
</synopsis>
 </refsynopsisdiv>

 <refsect1>
  <title>描述</title>

  <para>
   <function>pg_result</function> 返回关于先前
   <function>pg_exec</function> 所创建命令结果的信息。
  </para>

  <para>
   你可以根据需要把一个命令结果保留任意长的时间，但用完之后，务必通过执行
   <function>pg_result -clear</function> 来释放它。否则就会发生内存泄漏，<application>pgtcl</> 最终会开始抱怨你创建了太多命令结果对象。
  </para>
 </refsect1>

 <refsect1>
  <title>参数</title>

  <variablelist>
   <varlistentry>
    <term><parameter>resultHandle</parameter></term>
    <listitem>
     <para>
      命令结果的句柄。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><parameter>resultOption</parameter></term>
    <listitem>
     <para>
      下列选项之一，指定要返回哪部分结果信息：

      <variablelist>
       <varlistentry>
        <term><option>-status</option></term>
        <listitem>
         <para>
          结果的状态。
         </para>
        </listitem>
       </varlistentry>

       <varlistentry>
        <term><option>-error</option></term>
        <listitem>
         <para>
          错误消息，如果状态表明是一个错误，否则为空字符串。
         </para>
        </listitem>
       </varlistentry>

       <varlistentry>
        <term><option>-conn</option></term>
        <listitem>
         <para>
          产生该结果的连接。
         </para>
        </listitem>
       </varlistentry>

       <varlistentry>
        <term><option>-oid</option></term>
        <listitem>
         <para>
          如果命令是一条 <command>INSERT</command>，则为被插入行的 OID，否则为 0。
         </para>
        </listitem>
       </varlistentry>

       <varlistentry>
        <term><option>-numTuples</option></term>
        <listitem>
         <para>
          查询返回的行（元组）数。
         </para>
        </listitem>
       </varlistentry>

       <varlistentry>
        <term><option>-cmdTuples</option></term>
        <listitem>
         <para>
          被该命令影响的行（元组）数。
         </para>
        </listitem>
       </varlistentry>

       <varlistentry>
        <term><option>-numAttrs</option></term>
        <listitem>
         <para>
          每行中的列（属性）数。
         </para>
        </listitem>
       </varlistentry>

       <varlistentry>
        <term><option>-assign <parameter>arrayName</parameter></option></term>
        <listitem>
         <para>
          把结果赋给一个数组，使用形如
          <literal>(rowNumber, columnName)</literal> 的下标。
         </para>
        </listitem>
       </varlistentry>

       <varlistentry>
        <term><option>-assignbyidx <parameter>arrayName</> <optional role="tcl"><parameter>appendstr</></optional></option></term>
        <listitem>
         <para>
          使用第一列的值和其余列的名称作为键，把结果赋给一个数组。如果给定了
          <parameter>appendstr</>，则把它追加到每个键之后。简而言之，除了每行的第一列之外的所有列都会被存入数组，使用形如
          <literal>(firstColumnValue, columnNameAppendStr)</literal> 的下标。
         </para>
        </listitem>
       </varlistentry>

       <varlistentry>
        <term><option>-getTuple <parameter>rowNumber</parameter></option></term>
        <listitem>
         <para>
          以列表形式返回指定行的各列。行号从零开始。
         </para>
        </listitem>
       </varlistentry>

       <varlistentry>
        <term><option>-tupleArray <parameter>rowNumber</> <parameter>arrayName</></option></term>
        <listitem>
         <para>
          把该行的各列存入数组
          <parameter>arrayName</parameter>，以列名作为索引。行号从零开始。
         </para>
        </listitem>
       </varlistentry>

       <varlistentry>
        <term><option>-attributes</option></term>
        <listitem>
         <para>
          返回结果中各列名称的列表。
         </para>
        </listitem>
       </varlistentry>

       <varlistentry>
        <term><option>-lAttributes</option></term>
        <listitem>
         <para>
          返回一个子列表的列表，每列对应
          <literal>{name typeOid
          typeSize}</literal>。
         </para>
        </listitem>
       </varlistentry>

       <varlistentry>
        <term><option>-clear</option></term>
        <listitem>
         <para>
          清除命令结果对象。
         </para>
        </listitem>
       </varlistentry>
      </variablelist>
     </para>
    </listitem>
   </varlistentry>
  </variablelist>
 </refsect1>

 <refsect1>
  <title>返回值</title>

  <para>
   结果取决于所选的选项，如上所述。
  </para>
 </refsect1>
</refentry>


<refentry ID="PGTCL-PGSELECT">
 <refmeta>
  <refentrytitle>pg_select</refentrytitle>
 </refmeta>

 <refnamediv>
  <refname>pg_select</refname>
  <refpurpose>循环遍历查询的结果</refpurpose>
  <indexterm ID="IX-PGTCL-PGSELECT-2"><primary>pg_select</primary></indexterm>
 </refnamediv>

 <refsynopsisdiv>
<synopsis>
pg_select <parameter>conn</parameter> <parameter>commandString</parameter> <parameter>arrayVar</parameter> <parameter>procedure</parameter>
</synopsis>
 </refsynopsisdiv>

 <refsect1>
  <title>描述</title>

  <para>
   <function>pg_select</function> 向
   <productname>PostgreSQL</productname> 服务器提交一条查询（<command>SELECT</command> 语句），并对结果中的每一行执行给定的代码块。<parameter>commandString</parameter> 必须是一条
   <command>SELECT</command> 语句；任何其他内容都会返回一个错误。<parameter>arrayVar</parameter> 变量是循环中使用的一个数组名。对于每一行，<parameter>arrayVar</parameter> 都会用该行的值填充，以列名作为数组下标。然后执行
   <parameter>procedure</parameter>。
  </para>

  <para>
   除了列值之外，数组中还会生成下列特殊条目：

   <variablelist>
    <varlistentry>
     <term><literal>.headers</></term>
     <listitem>
      <para>
       查询返回的列名的列表。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><literal>.numcols</></term>
     <listitem>
      <para>
       查询返回的列数。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><literal>.tupno</></term>
     <listitem>
      <para>
       当前行号，从零开始，循环体每迭代一次递增一次。
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
  </para>
 </refsect1>

 <refsect1>
  <title>参数</title>

  <variablelist>
   <varlistentry>
    <term><parameter>conn</parameter></term>
    <listitem>
     <para>
      要在其上执行查询的连接的句柄。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><parameter>commandString</parameter></term>
    <listitem>
     <para>
      要执行的 SQL 查询。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><parameter>arrayVar</parameter></term>
    <listitem>
     <para>
      用于返回行的数组变量。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><parameter>procedure</parameter></term>
    <listitem>
     <para>
      为每个返回的行运行的过程。
     </para>
    </listitem>
   </varlistentry>
  </variablelist>
 </refsect1>

 <refsect1>
  <title>返回值</title>
  <para>
   无
  </para>
 </refsect1>

 <refsect1>
  <title>示例</title>

  <para>
   这个例子假定表 <classname>table1</> 具有列 <structfield>control</> 和 <structfield>name</>（还可能有其他列）：
<programlisting>
pg_select $pgconn "SELECT * FROM table1;" array {
    puts [format "%5d %s" $array(control) $array(name)]
}
</programlisting>
  </para>
 </refsect1>
</refentry>


<refentry ID="PGTCL-PGEXECUTE">
 <refmeta>
  <refentrytitle>pg_execute</refentrytitle>
 </refmeta>

 <refnamediv>
  <refname>pg_execute</refname>
  <refpurpose>发送一条查询并可选地遍历其结果</refpurpose>
  <indexterm ID="IX-PGTCL-PGEXECUTE-2"><primary>pg_execute</primary></indexterm>
 </refnamediv>

 <refsynopsisdiv>
<synopsis>
pg_execute <optional role="tcl">-array <parameter>arrayVar</parameter></optional> <optional role="tcl">-oid <parameter>oidVar</parameter></optional> <parameter>conn</parameter> <parameter>commandString</parameter> <optional role="tcl"><parameter>procedure</parameter></optional>
</synopsis>
 </refsynopsisdiv>

 <refsect1>
  <title>描述</title>

  <para>
   <function>pg_execute</function> 向
   <productname>PostgreSQL</> 服务器提交一条命令。
  </para>

  <para>
   如果该命令不是 <command>SELECT</command> 语句，则返回被该命令影响的行数。如果该命令是一条 <command>INSERT</command> 语句且只插入了一行，那么在提供了可选的 <parameter>-oid</parameter>
   参数时，被插入行的 OID 会被存入变量
   <parameter>oidVar</>。
  </para>

  <para>
   如果该命令是一条 <command>SELECT</command> 语句，那么对于结果中的每一行，如果提供了
   <parameter>arrayVar</parameter> 变量，行值就会被存入该变量，并以列名作为数组下标，否则存入以列名命名的变量中，然后如果提供了可选的
   <parameter>procedure</parameter>，则执行它。（只有当查询将只返回一行时，省略
   <parameter>procedure</parameter> 才大概是合理的。）返回被选中的行数。
  </para>

  <para>
   <parameter>procedure</parameter> 可以使用 Tcl 命令
   <literal>break</literal>、<literal>continue</literal> 和
   <literal>return</literal>，其行为符合预期。注意，如果
   <parameter>procedure</parameter> 执行了
   <literal>return</literal>，那么 <function>pg_execute</function>
   不会返回受影响的行数。
  </para>

  <para>
   <function>pg_execute</function> 是一个较新的函数，它提供了
   <function>pg_select</function> 功能的一个超集，并且在许多不需要访问结果句柄的场合可以取代
   <function>pg_exec</function>。
  </para>

  <para>
   对于由服务器处理的错误，<function>pg_execute</function> 会抛出一个 Tcl 错误并返回一个双元素列表。第一个元素是一个错误码，例如 <literal>PGRES_FATAL_ERROR</literal>，第二个元素是服务器的错误文本。对于更严重的错误，例如无法与服务器通信，<function>pg_execute</function> 会抛出一个 Tcl 错误并只返回错误消息文本。
  </para>
 </refsect1>

 <refsect1>
  <title>参数</title>

  <variablelist>
   <varlistentry>
    <term><option>-array <parameter>arrayVar</parameter></option></term>
    <listitem>
     <para>
      指定一个数组变量的名称，结果行将被存入其中，以列名作为索引。如果
      <parameter>commandString</> 不是 <command>SELECT</>
      语句，则忽略此参数。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><option>-oid <parameter>oidVar</parameter></option></term>
    <listitem>
     <para>
      指定一个变量的名称，<command>INSERT</command>
      语句产生的 OID 将被存入该变量。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><parameter>conn</parameter></term>
    <listitem>
     <para>
      要在其上执行命令的连接的句柄。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><parameter>commandString</parameter></term>
    <listitem>
     <para>
      要执行的 SQL 命令。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><parameter>procedure</parameter></term>
    <listitem>
     <para>
      可选的过程，为 <command>SELECT</command>
      语句的每个结果行执行。
     </para>
    </listitem>
   </varlistentry>
  </variablelist>
 </refsect1>

 <refsect1>
  <title>返回值</title>

  <para>
   被该命令影响或返回的行数。
  </para>
 </refsect1>

 <refsect1>
  <title>示例</title>

  <para>
   在下面的例子中，为了清晰起见，省略了用
   <literal>catch</literal> 进行的错误检查。
  </para>

  <para>
   插入一行并把 OID 保存到 <varname>result_oid</> 中：
<programlisting>
pg_execute -oid result_oid $pgconn "INSERT INTO mytable VALUES (1);"
</programlisting>
  </para>

  <para>
   打印每一行中的列 <literal>item</> 和 <literal>value</>：
<programlisting>
pg_execute -array d $pgconn "SELECT item, value FROM mytable;" {
    puts "Item=$d(item) Value=$d(value)"
}
</programlisting>
  </para>

  <para>
   找出最大值和最小值，并把它们存入
   <literal>$s(max)</> 和 <literal>$s(min)</>：
<programlisting>
pg_execute -array s $pgconn "SELECT max(value) AS max, min(value) AS min FROM mytable;"
</programlisting>
  </para>

  <para>
   找出最大值和最小值，并把它们存入
   <literal>$max</> 和 <literal>$min</>：
<programlisting>
pg_execute $pgconn "SELECT max(value) AS max, min(value) AS min FROM mytable;"
</programlisting>
  </para>
 </refsect1>
</refentry>


<refentry ID="PGTCL-PGLISTEN">
 <refmeta>
  <refentrytitle>pg_listen</refentrytitle>
 </refmeta>

 <refnamediv>
  <refname>pg_listen</refname>
  <refpurpose>设置或更改异步通知消息的回调</refpurpose>
  <indexterm ID="IX-PGTCL-PGLISTEN-2"><primary>pg_listen</primary></indexterm>
 </refnamediv>

 <refsynopsisdiv>
<synopsis>
pg_listen <parameter>conn</parameter> <parameter>notifyName</parameter> <optional role="tcl"><parameter>callbackCommand</parameter></optional>
</synopsis>
 </refsynopsisdiv>

 <refsect1>
  <title>描述</title>

  <para>
   <function>pg_listen</function> 创建、更改或取消一个监听来自 <productname>PostgreSQL</productname> 服务器的异步通知消息的请求。带有
   <parameter>callbackCommand</> 参数时，会建立该请求，或者替换一个已存在请求的命令字符串。不带
   <parameter>callbackCommand</> 参数时，则取消先前的请求。
  </para>

  <para>
   在 <function>pg_listen</function> 请求建立之后，每当从服务器收到带有给定名称的通知消息时，就会执行指定的命令字符串。这发生在任何
   <productname>PostgreSQL</productname> 客户端应用发出一条引用该名称的 <command>NOTIFY</command><indexterm><primary>NOTIFY</><secondary
   sortas="pgtcl">在 pgtcl 中</></> 命令时。命令字符串从
   Tcl 空闲循环中执行。那是用 Tk 编写的应用程序的正常空闲状态。在非 Tk 的 Tcl
   shell 中，你可以执行 <function>update</function> 或
   <function>vwait</function> 来进入空闲循环。
  </para>

  <para>
   在使用 <function>pg_listen</function> 时，你不应直接调用
   SQL 语句 <command>LISTEN</command>
   或 <command>UNLISTEN</command>。
   <application>pgtcl</application>
   会替你负责发出这些语句。但如果你想自己发送一条通知消息，可以用
   <function>pg_exec</function> 调用 SQL
   <command>NOTIFY</command> 语句。
  </para>
 </refsect1>

 <refsect1>
  <title>参数</title>

  <variablelist>
   <varlistentry>
    <term><parameter>conn</parameter></term>
    <listitem>
     <para>
      要在其上监听通知的连接的句柄。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><parameter>notifyName</parameter></term>
    <listitem>
     <para>
      要开始或停止监听的通知条件的名称。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><parameter>callbackCommand</parameter></term>
    <listitem>
     <para>
      如果提供，给出在收到匹配的通知时执行的命令字符串。
     </para>
    </listitem>
   </varlistentry>
  </variablelist>
 </refsect1>

 <refsect1>
  <title>返回值</title>

  <para>
   无
  </para>
 </refsect1>
</refentry>


<refentry ID="PGTCL-PGON-CONNECTION-LOSS">
 <refmeta>
  <refentrytitle>pg_on_connection_loss</refentrytitle>
 </refmeta>

 <refnamediv>
  <refname>pg_on_connection_loss</refname>
  <refpurpose>设置或更改意外断开连接的回调</refpurpose>
  <indexterm ID="IX-PGTCL-PGON-CONNECTION-LOSS-2"><primary>pg_on_connection_loss</primary></indexterm>
 </refnamediv>

 <refsynopsisdiv>
<synopsis>
pg_on_connection_loss <parameter>conn</parameter> <optional role="tcl"><parameter>callbackCommand</parameter></optional>
</synopsis>
 </refsynopsisdiv>

 <refsect1>
  <title>描述</title>

  <para>
   <function>pg_on_connection_loss</function> 创建、更改或取消一个请求：在发生到数据库的连接意外丢失时执行一个回调命令。带有
   <parameter>callbackCommand</> 参数时，会建立该请求，或者替换一个已存在请求的命令字符串。不带
   <parameter>callbackCommand</> 参数时，则取消先前的请求。
  </para>

  <para>
   回调命令字符串从 Tcl 空闲循环中执行。那是用 Tk 编写的应用程序的正常空闲状态。在非 Tk 的 Tcl shell 中，你可以执行 <function>update</function>
   或 <function>vwait</function> 来进入空闲循环。
  </para>
 </refsect1>

 <refsect1>
  <title>参数</title>

  <variablelist>
   <varlistentry>
    <term><parameter>conn</parameter></term>
    <listitem>
     <para>
      要监视其连接丢失的句柄。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><parameter>callbackCommand</parameter></term>
    <listitem>
     <para>
      如果提供，给出在检测到连接丢失时执行的命令字符串。
     </para>
    </listitem>
   </varlistentry>
  </variablelist>
 </refsect1>

 <refsect1>
  <title>返回值</title>

  <para>
   无
  </para>
 </refsect1>
</refentry>


<refentry ID="PGTCL-PGLOCREAT">
 <refmeta>
  <refentrytitle>pg_lo_creat</refentrytitle>
 </refmeta>

 <refnamediv>
  <refname>pg_lo_creat</refname>
  <refpurpose>创建一个大对象</refpurpose>
  <indexterm ID="IX-PGTCL-PGLOCREAT-2"><primary>pg_lo_creat</primary></indexterm>
 </refnamediv>

 <refsynopsisdiv>
<synopsis>
pg_lo_creat <parameter>conn</parameter> <parameter>mode</parameter>
</synopsis>
 </refsynopsisdiv>

 <refsect1>
  <title>描述</title>

  <para>
   <function>pg_lo_creat</function> 创建一个大对象。
  </para>
 </refsect1>

 <refsect1>
  <title>参数</title>

  <variablelist>
   <varlistentry>
    <term><parameter>conn</parameter></term>
    <listitem>
     <para>
      到要在其中创建大对象的数据库的连接句柄。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><parameter>mode</parameter></term>
    <listitem>
     <para>
      大对象的访问模式。它可以是
      <literal>INV_READ</> 和 <literal>INV_WRITE</> 的任意按位或组合。<quote>或</quote>操作符是
      <literal>|</literal>。例如：
<programlisting>
[pg_lo_creat $conn "INV_READ|INV_WRITE"]
</programlisting>
     </para>
    </listitem>
   </varlistentry>
  </variablelist>
 </refsect1>

 <refsect1>
  <title>返回值</title>

  <para>
   所创建的大对象的 OID。
  </para>
 </refsect1>
</refentry>


<refentry ID="PGTCL-PGLOOPEN">
 <refmeta>
  <refentrytitle>pg_lo_open</refentrytitle>
 </refmeta>

 <refnamediv>
  <refname>pg_lo_open</refname>
  <refpurpose>打开一个大对象</refpurpose>
  <indexterm ID="IX-PGTCL-PGLOOPEN-2"><primary>pg_lo_open</primary></indexterm>
 </refnamediv>

 <refsynopsisdiv>
<synopsis>
pg_lo_open <parameter>conn</parameter> <parameter>loid</parameter> <parameter>mode</parameter>
</synopsis>
 </refsynopsisdiv>

 <refsect1>
  <title>描述</title>

  <para>
   <function>pg_lo_open</function> 打开一个大对象。
  </para>
 </refsect1>

 <refsect1>
  <title>参数</title>

  <variablelist>
   <varlistentry>
    <term><parameter>conn</parameter></term>

    <listitem>
     <para>
      到大对象所在的数据库的连接句柄。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><parameter>loid</parameter></term>
    <listitem>
     <para>
      大对象的 OID。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><parameter>mode</parameter></term>
    <listitem>
     <para>
      指定大对象的访问模式。模式可以是
      <literal>r</>、<literal>w</> 或 <literal>rw</>。
     </para>
    </listitem>
   </varlistentry>
  </variablelist>
 </refsect1>

 <refsect1>
  <title>返回值</title>

  <para>
   一个供后续大对象命令使用的描述符。
  </para>
 </refsect1>
</refentry>


<refentry ID="PGTCL-PGLOCLOSE">
 <refmeta>
  <refentrytitle>pg_lo_close</refentrytitle>
 </refmeta>

 <refnamediv>
  <refname>pg_lo_close</refname>
  <refpurpose>关闭一个大对象</refpurpose>
  <indexterm ID="IX-PGTCL-PGLOCLOSE-2"><primary>pg_lo_close</primary></indexterm>
 </refnamediv>

 <refsynopsisdiv>
<synopsis>
pg_lo_close <parameter>conn</parameter> <parameter>descriptor</parameter>
</synopsis>
 </refsynopsisdiv>

 <refsect1>
  <title>描述</title>

  <para>
   <function>pg_lo_close</function> 关闭一个大对象。
  </para>
 </refsect1>

 <refsect1>
  <title>参数</title>

  <variablelist>
   <varlistentry>
    <term><parameter>conn</parameter></term>
    <listitem>
     <para>
      到大对象所在的数据库的连接句柄。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><parameter>descriptor</parameter></term>
    <listitem>
     <para>
      来自 <function>pg_lo_open</function> 的大对象描述符。
     </para>
    </listitem>
   </varlistentry>
  </variablelist>
 </refsect1>

 <refsect1>
  <title>返回值</title>

  <para>
   无
  </para>
 </refsect1>
</refentry>


<refentry ID="PGTCL-PGLOREAD">
 <refmeta>
  <refentrytitle>pg_lo_read</refentrytitle>
 </refmeta>

 <refnamediv>
  <refname>pg_lo_read</refname>
  <refpurpose>从大对象读取</refpurpose>
  <indexterm ID="IX-PGTCL-PGLOREAD-2"><primary>pg_lo_read</primary></indexterm>
 </refnamediv>

 <refsynopsisdiv>
<synopsis>
pg_lo_read <parameter>conn</parameter> <parameter>descriptor</parameter> <parameter>bufVar</parameter> <parameter>len</parameter>
</synopsis>
 </refsynopsisdiv>

 <refsect1>
  <title>描述</title>

  <para>
   <function>pg_lo_read</function> 从大对象中最多读取
   <parameter>len</parameter> 个字节，存入名为
   <parameter>bufVar</parameter> 的变量。
  </para>
 </refsect1>

 <refsect1>
  <title>参数</title>

  <variablelist>
   <varlistentry>
    <term><parameter>conn</parameter></term>
    <listitem>
     <para>
      到大对象所在的数据库的连接句柄。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><parameter>descriptor</parameter></term>
    <listitem>
     <para>
      来自 <function>pg_lo_open</function> 的大对象描述符。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><parameter>bufVar</parameter></term>
    <listitem>
     <para>
      用于容纳大对象片段的缓冲区变量的名称。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><parameter>len</parameter></term>
    <listitem>
     <para>
      要读取的最大字节数。
     </para>
    </listitem>
   </varlistentry>
  </variablelist>
 </refsect1>

 <refsect1>
  <title>返回值</title>

  <para>
   返回实际读取的字节数；如果先到达了大对象的末尾，它可能小于请求的数量。发生错误时，返回值为负数。
  </para>
 </refsect1>
</refentry>


<refentry ID="PGTCL-PGLOWRITE">
 <refmeta>
  <refentrytitle>pg_lo_write</refentrytitle>
 </refmeta>

 <refnamediv>
  <refname>pg_lo_write</refname>
  <refpurpose>写入大对象</refpurpose>
  <indexterm ID="IX-PGTCL-PGLOWRITE-2"><primary>pg_lo_write</primary></indexterm>
 </refnamediv>

 <refsynopsisdiv>
<synopsis>
pg_lo_write <parameter>conn</parameter> <parameter>descriptor</parameter> <parameter>buf</parameter> <parameter>len</parameter>
</synopsis>
 </refsynopsisdiv>

 <refsect1>
  <title>描述</title>

  <para>
   <function>pg_lo_write</function> 从变量
   <parameter>buf</parameter> 中最多向大对象写入
   <parameter>len</parameter> 个字节。
  </para>
 </refsect1>

 <refsect1>
  <title>参数</title>

  <variablelist>
   <varlistentry>
    <term><parameter>conn</parameter></term>
    <listitem>
     <para>
      到大对象所在的数据库的连接句柄。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><parameter>descriptor</parameter></term>
    <listitem>
     <para>
      来自 <function>pg_lo_open</function> 的大对象描述符。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><parameter>buf</parameter></term>
    <listitem>
     <para>
      要写入大对象的字符串（不是变量名，而是值本身）。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><parameter>len</parameter></term>
    <listitem>
     <para>
      要写入的最大字节数。写入的数量将是该值与字符串长度中较小的那个。
     </para>
    </listitem>
   </varlistentry>
  </variablelist>
 </refsect1>

 <refsect1>
  <title>返回值</title>

  <para>
   返回实际写入的字节数；通常与请求的数量相同。发生错误时，返回值为负数。
  </para>
 </refsect1>
</refentry>


<refentry ID="PGTCL-PGLOLSEEK">
 <refmeta>
  <refentrytitle>pg_lo_lseek</refentrytitle>
 </refmeta>

 <refnamediv>
  <refname>pg_lo_lseek</refname>
  <refpurpose>在大对象中定位到某个位置</refpurpose>
  <indexterm ID="IX-PGTCL-PGLOLSEEK-2"><primary>pg_lo_lseek</primary></indexterm>
 </refnamediv>

 <refsynopsisdiv>
<synopsis>
pg_lo_lseek <parameter>conn</parameter> <parameter>descriptor</parameter> <parameter>offset</parameter> <parameter>whence</parameter>
</synopsis>
 </refsynopsisdiv>

 <refsect1>
  <title>描述</title>

  <para>
   <function>pg_lo_lseek</function> 把当前读/写位置移动到从 <parameter>whence</parameter> 指定的位置起
   <parameter>offset</parameter> 字节处。
  </para>
 </refsect1>

 <refsect1>
  <title>参数</title>

  <variablelist>
   <varlistentry>
    <term><parameter>conn</parameter></term>
    <listitem>
     <para>
      到大对象所在的数据库的连接句柄。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><parameter>descriptor</parameter></term>
    <listitem>
     <para>
      来自 <function>pg_lo_open</function> 的大对象描述符。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><parameter>offset</parameter></term>
    <listitem>
     <para>
      新的定位位置，以字节为单位。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><parameter>whence</parameter></term>
    <listitem>
     <para>
      指定从哪里开始计算新的定位位置：<literal>SEEK_CUR</>（从当前位置）、<literal>SEEK_END</>（从末尾）或 <literal>SEEK_SET</>（从开头）。
     </para>
    </listitem>
   </varlistentry>
  </variablelist>
 </refsect1>

 <refsect1>
  <title>返回值</title>

  <para>
   无
  </para>
 </refsect1>
</refentry>


<refentry ID="PGTCL-PGLOTELL">
 <refmeta>
  <refentrytitle>pg_lo_tell</refentrytitle>
 </refmeta>

 <refnamediv>
  <refname>pg_lo_tell</refname>
  <refpurpose>返回大对象的当前定位位置</refpurpose>
  <indexterm ID="IX-PGTCL-PGLOTELL-2"><primary>pg_lo_tell</primary></indexterm>
 </refnamediv>

 <refsynopsisdiv>
<synopsis>
pg_lo_tell <parameter>conn</parameter> <parameter>descriptor</parameter>
</synopsis>
 </refsynopsisdiv>

 <refsect1>
  <title>描述</title>

  <para>
   <function>pg_lo_tell</function> 返回从大对象开头起的当前读/写位置（以字节为单位）。
  </para>
 </refsect1>

 <refsect1>
  <title>参数</title>

  <variablelist>
   <varlistentry>
    <term><parameter>conn</parameter></term>

    <listitem>
     <para>
      到大对象所在的数据库的连接句柄。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><parameter>descriptor</parameter></term>
    <listitem>
     <para>
      来自 <function>pg_lo_open</function> 的大对象描述符。
     </para>
    </listitem>
   </varlistentry>
  </variablelist>
 </refsect1>

 <refsect1>
  <title>返回值</title>

  <para>
   一个从零开始的字节偏移量，适合作为
   <function>pg_lo_lseek</function> 的输入。
  </para>
 </refsect1>
</refentry>


<refentry ID="PGTCL-PGLOUNLINK">
 <refmeta>
  <refentrytitle>pg_lo_unlink</refentrytitle>
 </refmeta>

 <refnamediv>
  <refname>pg_lo_unlink</refname>
  <refpurpose>删除一个大对象</refpurpose>
  <indexterm ID="IX-PGTCL-PGLOUNLINK-2"><primary>pg_lo_unlink</primary></indexterm>
 </refnamediv>

 <refsynopsisdiv>
<synopsis>
pg_lo_unlink <parameter>conn</parameter> <parameter>loid</parameter>
</synopsis>
 </refsynopsisdiv>

 <refsect1>
  <title>描述</title>

  <para>
   <function>pg_lo_unlink</function> 删除指定的大对象。
  </para>
 </refsect1>

 <refsect1>
  <title>参数</title>

  <variablelist>
   <varlistentry>
    <term><parameter>conn</parameter></term>
    <listitem>
     <para>
      到大对象所在的数据库的连接句柄。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><parameter>loid</parameter></term>
    <listitem>
     <para>
      大对象的 OID。
     </para>
    </listitem>
   </varlistentry>
  </variablelist>
 </refsect1>

 <refsect1>
  <title>返回值</title>

  <para>
   无
  </para>
 </refsect1>
</refentry>


<refentry ID="PGTCL-PGLOIMPORT">
 <refmeta>
  <refentrytitle>pg_lo_import</refentrytitle>
 </refmeta>

 <refnamediv>
  <refname>pg_lo_import</refname>
  <refpurpose>从文件导入一个大对象</refpurpose>
  <indexterm ID="IX-PGTCL-PGLOIMPORT-2"><primary>pg_lo_import</primary></indexterm>
 </refnamediv>

 <refsynopsisdiv>
<synopsis>
pg_lo_import <parameter>conn</parameter> <parameter>filename</parameter>
</synopsis>
 </refsynopsisdiv>

 <refsect1>
  <title>描述</title>

  <para>
   <function>pg_lo_import</function> 读取指定的文件并把内容放入一个新的大对象。
  </para>
 </refsect1>

 <refsect1>
  <title>参数</title>

  <variablelist>
   <varlistentry>
    <term><parameter>conn</parameter></term>
    <listitem>
     <para>
      到要在其中创建大对象的数据库的连接句柄。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><parameter>filename</parameter></term>
    <listitem>
     <para>
      指定要从中导入数据的文件。
     </para>
    </listitem>
   </varlistentry>
  </variablelist>
 </refsect1>

 <refsect1>
  <title>返回值</title>

  <para>
   所创建的大对象的 OID。
  </para>
 </refsect1>

 <refsect1>
  <title>说明</title>

  <para>
   必须在一个
   <command>BEGIN</>/<command>COMMIT</> 事务块内调用
   <function>pg_lo_import</function>。
  </para>
 </refsect1>
</refentry>


<refentry ID="PGTCL-PGLOEXPORT">
 <refmeta>
  <refentrytitle>pg_lo_export</refentrytitle>
 </refmeta>

 <refnamediv>
  <refname>pg_lo_export</refname>
  <refpurpose>将大对象导出到文件</refpurpose>
  <indexterm ID="IX-PGTCL-PGLOEXPORT-2"><primary>pg_lo_export</primary></indexterm>
 </refnamediv>

 <refsynopsisdiv>
<synopsis>
pg_lo_export <parameter>conn</parameter> <parameter>loid</parameter> <parameter>filename</parameter>
</synopsis>
 </refsynopsisdiv>

 <refsect1>
  <title>描述</title>

  <para>
   <function>pg_lo_export</function> 把指定的大对象写入一个文件。
  </para>
 </refsect1>

 <refsect1>
  <title>参数</title>

  <variablelist>
   <varlistentry>
    <term><parameter>conn</parameter></term>
    <listitem>
     <para>
      到大对象所在的数据库的连接句柄。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><parameter>loid</parameter></term>
    <listitem>
     <para>
      大对象的 OID。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><parameter>filename</parameter></term>
    <listitem>
     <para>
      指定要把数据导出到其中的文件。
     </para>
    </listitem>
   </varlistentry>
  </variablelist>
 </refsect1>

 <refsect1>
  <title>返回值</title>
  <para>
   无
  </para>
 </refsect1>

 <refsect1>
  <title>说明</title>

  <para>
   必须在一个
   <command>BEGIN</>/<command>COMMIT</> 事务块内调用
   <function>pg_lo_export</function>。
  </para>
 </refsect1>
</refentry>

</sect1>


<sect1 id="pgtcl-examplesect">
 <title>示例程序</title>

 <para>
  <xref linkend="pgtcl-example">展示了一个如何使用
  <application>pgtcl</application> 命令的小例子。
 </para>

 <example id="pgtcl-example">
  <title><application>pgtcl</application> 示例程序</title>

<programlisting>
# getDBs :
#   get the names of all the databases at a given host and port number
#   with the defaults being the localhost and port 5432
#   return them in alphabetical order
proc getDBs { {host "localhost"} {port "5432"} } {
    # datnames is the list to be result
    set conn [pg_connect template1 -host $host -port $port]
    set res [pg_exec $conn "SELECT datname FROM pg_database ORDER BY datname;"]
    set ntups [pg_result $res -numTuples]
    for {set i 0} {$i < $ntups} {incr i} {
	lappend datnames [pg_result $res -getTuple $i]
    }
    pg_result $res -clear
    pg_disconnect $conn
    return $datnames
}
</programlisting>
  </example>
 </sect1>

</chapter>
