<!--
$Header: /cvsroot/pgsql/doc/src/sgml/ref/insert.sgml,v 1.25 2003/09/12 00:12:47 tgl Exp $
PostgreSQL documentation
-->

<refentry id="SQL-INSERT">
 <refmeta>
  <refentrytitle id="SQL-INSERT-TITLE">INSERT</refentrytitle>
  <refmiscinfo>SQL - 语言语句</refmiscinfo>
 </refmeta>

 <refnamediv>
  <refname>INSERT</refname>
  <refpurpose>在一个表中创建新行</refpurpose>
 </refnamediv>

 <indexterm zone="sql-insert">
  <primary>INSERT</primary>
 </indexterm>

 <refsynopsisdiv>
<synopsis>
INSERT INTO <replaceable class="PARAMETER">table</replaceable> [ ( <replaceable class="PARAMETER">column</replaceable> [, ...] ) ]
    { DEFAULT VALUES | VALUES ( { <replaceable class="PARAMETER">expression</replaceable> | DEFAULT } [, ...] ) | <replaceable class="PARAMETER">query</replaceable> }
</synopsis>
 </refsynopsisdiv>

 <refsect1>
  <title>描述</title>

  <para>
   <command>INSERT</command>允许我们向一个表中插入新行。可以一次插入一行，也可以插入一个查询结果的多个行。
  </para>

  <para>
   目标列表中的列可以按任意顺序列出。目标列表中没有出现的每个列将使用默认值插入，即其声明的默认值或空值。
  </para>

  <para>
   如果每个列的表达式数据类型不正确，将尝试自动类型转换。
  </para>

  <para>
   要向一个表插入数据，你必须拥有该表上的<literal>INSERT</literal>权限。如果使用
   <replaceable class="PARAMETER">query</replaceable>子句插入来自查询的行，你还需要拥有查询中使用的任何表上的<literal>SELECT</literal>权限。
  </para>
 </refsect1>

 <refsect1>
  <title>参数</title>

  <variablelist>
   <varlistentry>
    <term><replaceable class="PARAMETER">table</replaceable></term>
    <listitem>
     <para>
      一个已存在表的名称（可以有模式限定）。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><replaceable class="PARAMETER">column</replaceable></term>
    <listitem>
     <para>
      <replaceable class="PARAMETER">table</replaceable>中一个列的名称。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><literal>DEFAULT VALUES</literal></term>
    <listitem>
     <para>
      所有列都将用它们的默认值填充。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><replaceable class="PARAMETER">expression</replaceable></term>
    <listitem>
     <para>
      要赋予<replaceable
      class="PARAMETER">column</replaceable>的一个表达式或值。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><literal>DEFAULT</literal></term>
    <listitem>
     <para>
      该列将用它的默认值填充。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><replaceable class="PARAMETER">query</replaceable></term>
    <listitem>
     <para>
      提供要插入行的查询（<command>SELECT</command>语句）。其语法的描述请参考
      <command>SELECT</command>语句。
     </para>
    </listitem>
   </varlistentry>
  </variablelist>
 </refsect1>

 <refsect1>
  <title>输出</title>

  <para>
   成功完成时，<command>INSERT</>命令返回如下形式的命令标签
<screen>
INSERT <replaceable>oid</replaceable> <replaceable class="parameter">count</replaceable>
</screen>
   其中<replaceable class="parameter">count</replaceable>是插入的行数。如果
   <replaceable class="parameter">count</replaceable>恰好为一，并且目标表有
   OID，那么<replaceable class="parameter">oid</replaceable>就是分配给被插入行的
   <acronym>OID</acronym>。否则<replaceable class="parameter">oid</replaceable>为零。
  </para>
 </refsect1>

 <refsect1>
  <title>示例</title>

  <para>
   向表 <literal>films</literal> 插入一行：

<programlisting>
INSERT INTO films VALUES
    ('UA502', 'Bananas', 105, '1971-07-13', 'Comedy', '82 minutes');
</programlisting>
  </para>

  <para>
   在第二个例子中，省略了最后一列 <literal>len</literal>，因此它将使用默认值空值：

<programlisting>
INSERT INTO films (code, title, did, date_prod, kind)
    VALUES ('T_601', 'Yojimbo', 106, '1961-06-16', 'Drama');
</programlisting>
  </para>

  <para>
   第三个例子对日期列使用 <literal>DEFAULT</literal> 子句而不是指定一个值：

<programlisting>
INSERT INTO films VALUES
    ('UA502', 'Bananas', 105, DEFAULT, 'Comedy', '82 minutes');
INSERT INTO films (code, title, did, date_prod, kind)
    VALUES ('T_601', 'Yojimbo', 106, DEFAULT, 'Drama');
</programlisting>
  </para>

  <para>
   这个例子从表 <literal>tmp</literal> 向表 <literal>films</literal>
   插入若干行：

<programlisting>
INSERT INTO films SELECT * FROM tmp;
</programlisting>
  </para>

  <para>
   这个例子向数组列插入数据：

<programlisting>
-- Create an empty 3x3 gameboard for noughts-and-crosses
-- (all of these commands create the same board)
INSERT INTO tictactoe (game, board[1:3][1:3])
    VALUES (1,'{{"","",""},{},{"",""}}');
INSERT INTO tictactoe (game, board[3][3])
    VALUES (2,'{}');
INSERT INTO tictactoe (game, board)
    VALUES (3,'{{,,},{,,},{,,}}');
</programlisting>
  </para>
 </refsect1>

 <refsect1>
  <title>兼容性</title>

  <para>
   <command>INSERT</command>完全符合 SQL 标准。<replaceable
   class="PARAMETER">query</replaceable>子句可能存在的限制记录在
   <xref linkend="sql-select" endterm="sql-select-title">中。
  </para>
 </refsect1>
</refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode:sgml
sgml-omittag:nil
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
sgml-parent-document:nil
sgml-default-dtd-file:"../reference.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:("/usr/lib/sgml/catalog")
sgml-local-ecat-files:nil
End:
-->
