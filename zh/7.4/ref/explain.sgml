<!--
$Header: /cvsroot/pgsql/doc/src/sgml/ref/explain.sgml,v 1.31 2003/10/17 01:14:26 tgl Exp $
PostgreSQL documentation
-->

<refentry id="SQL-EXPLAIN">
 <refmeta>
  <refentrytitle id="SQL-EXPLAIN-TITLE">EXPLAIN</refentrytitle>
  <refmiscinfo>SQL - Language Statements</refmiscinfo>
 </refmeta>

 <refnamediv>
  <refname>EXPLAIN</refname>
  <refpurpose>显示一条语句的执行计划</refpurpose>
 </refnamediv>

 <indexterm zone="sql-explain">
  <primary>EXPLAIN</primary>
 </indexterm>

 <refsynopsisdiv>
<synopsis>
EXPLAIN [ ANALYZE ] [ VERBOSE ] <replaceable class="parameter">statement</replaceable>
</synopsis>
 </refsynopsisdiv>

 <refsect1>
  <title>描述</title>

  <para>
   这条命令显示<productname>PostgreSQL</productname>规划器为给定语句生成的执行计划。执行计划显示语句引用的表将被如何扫描——用普通的顺序扫描、索引扫描等——以及当引用多个表时，将使用什么连接算法把每个输入表中所需的行汇集到一起。
  </para>

  <para>
   显示中最关键的部分是估计的语句执行代价，即规划器对运行该语句所需时间的猜测（以磁盘页获取次数为单位度量）。实际上会显示两个数字：返回第一行之前的启动时间，以及返回所有行的总时间。对大多数查询来说，总时间才是重要的，但在诸如
   <literal>EXISTS</literal>中的子查询这样的上下文中，规划器会选择最小的启动时间而不是最小的总时间（因为执行器在取到一行后无论如何都会停止）。另外，如果你用<literal>LIMIT</literal>子句限制返回的行数，规划器会在端点代价之间做适当的插值，来估计哪个计划实际上最便宜。
  </para>

  <para>
   <literal>ANALYZE</literal>选项使语句被真正执行，而不仅仅是规划。每个计划节点内部花费的总耗时（毫秒）以及它实际返回的总行数会被添加到显示中。这对于查看规划器的估计是否接近现实很有用。
  </para>

  <important>
   <para>
    请记住，使用<literal>ANALYZE</literal>时语句会被真正执行。虽然<command>EXPLAIN</command>会丢弃<command>SELECT</command>
    本会返回的任何输出，但语句的其他副作用会照常发生。如果你想在<command>INSERT</command>、<command>UPDATE</command>、<command>DELETE</command>或<command>EXECUTE</command>语句上使用
    <command>EXPLAIN ANALYZE</command>而又不让命令影响你的数据，可以使用这样的方法：
<programlisting>
BEGIN;
EXPLAIN ANALYZE ...;
ROLLBACK;
</programlisting>
   </para>
  </important>
 </refsect1>

 <refsect1>
  <title>参数</title>

  <variablelist>
   <varlistentry>
    <term><literal>ANALYZE</literal></term>
    <listitem>
     <para>
      执行命令并显示实际的运行时间。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><literal>VERBOSE</literal></term>
    <listitem>
     <para>
      显示计划树的完整内部表示，而不仅仅是一个摘要。通常这个选项只对调试<productname>PostgreSQL</productname>有用。<literal>VERBOSE</literal>输出是否美化打印取决于
      <varname>explain_pretty_print</varname>配置参数的设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><replaceable class="parameter">statement</replaceable></term>
    <listitem>
     <para>
      任何你希望看到其执行计划的<command>SELECT</>、<command>INSERT</>、<command>UPDATE</>、<command>DELETE</>、<command>EXECUTE</>或<command>DECLARE</>语句。
     </para>
    </listitem>
   </varlistentry>
  </variablelist>
 </refsect1>

 <refsect1>
  <title>注解</title>

  <para>
   关于优化器在<productname>PostgreSQL</productname>中对代价信息的使用，目前只有很少的文档。更多信息请参阅<xref linkend="using-explain">。
  </para>

  <para>
   为了让<productname>PostgreSQL</productname>查询规划器在优化查询时能做出充分知情的决定，应当运行<command>ANALYZE</command>
   语句来记录表中数据分布的统计信息。如果你没有这样做（或者自上次运行<command>ANALYZE</command>以来表中数据的统计分布发生了显著变化），估计的代价不太可能符合查询的真实特性，因此可能选择一个较差的查询计划。
  </para>

  <para>
   在<productname>PostgreSQL</productname> 7.3 之前，计划以
   <literal>NOTICE</literal>消息的形式发出。现在它作为查询结果出现（格式化为一个只有单个文本列的表）。
  </para>
 </refsect1>

 <refsect1>
  <title>示例</title>

  <para>
   要显示对一个只有单个<type>integer</type>列和 10000 行的表的简单查询的计划：

<programlisting>
EXPLAIN SELECT * FROM foo;

                       QUERY PLAN
---------------------------------------------------------
 Seq Scan on foo  (cost=0.00..155.00 rows=10000 width=4)
(1 row)
</programlisting>
  </para>

  <para>
   如果有一个索引，并且我们的查询带有可索引的<literal>WHERE</literal>
   条件，<command>EXPLAIN</command>可能会显示一个不同的计划：

<programlisting>
EXPLAIN SELECT * FROM foo WHERE i = 4;

                         QUERY PLAN
--------------------------------------------------------------
 Index Scan using fi on foo  (cost=0.00..5.98 rows=1 width=4)
   Index Cond: (i = 4)
(2 rows)
</programlisting>
  </para>

  <para>
   下面是一个使用聚合函数的查询的查询计划示例：

<programlisting>
EXPLAIN SELECT sum(i) FROM foo WHERE i &lt; 10;

                             QUERY PLAN
---------------------------------------------------------------------
 Aggregate  (cost=23.93..23.93 rows=1 width=4)
   -&gt;  Index Scan using fi on foo  (cost=0.00..23.92 rows=6 width=4)
         Index Cond: (i &lt; 10)
(3 rows)
</programlisting>
  </para>

  <para>
   下面是使用<command>EXPLAIN EXECUTE</command>显示一个预备查询的执行计划的例子：

<programlisting>
PREPARE query(int, int) AS SELECT sum(bar) FROM test
    WHERE id &gt; $1 AND id &lt; $2
    GROUP BY foo;

EXPLAIN ANALYZE EXECUTE query(100, 200);

                                                       QUERY PLAN                                                        
-------------------------------------------------------------------------------------------------------------------------
 HashAggregate  (cost=39.53..39.53 rows=1 width=8) (actual time=0.661..0.672 rows=7 loops=1)
   -&gt;  Index Scan using test_pkey on test  (cost=0.00..32.97 rows=1311 width=8) (actual time=0.050..0.395 rows=99 loops=1)
         Index Cond: ((id &gt; $1) AND (id &lt; $2))
 Total runtime: 0.851 ms
(4 rows)
</programlisting>
  </para>

  <para>
   当然，这里显示的具体数字取决于所涉及表的实际内容。还要注意，由于规划器的改进，这些数字、甚至被选中的查询策略都可能随
   <productname>PostgreSQL</productname>版本而变化。此外，<command>ANALYZE</command>命令使用随机抽样来估计数据统计；因此，即使表中数据的实际分布没有变化，在重新运行一次
   <command>ANALYZE</command>之后，代价估计也有可能改变。
  </para>
 </refsect1>

 <refsect1>
  <title>兼容性</title>

  <para>
   SQL 标准中没有定义<command>EXPLAIN</command>语句。
  </para>
 </refsect1>
</refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode:sgml
sgml-omittag:nil
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
sgml-parent-document:nil
sgml-default-dtd-file:"../reference.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:"/usr/lib/sgml/catalog"
sgml-local-ecat-files:nil
End:
-->
