<!--
$Header: /cvsroot/pgsql/doc/src/sgml/ref/alter_sequence.sgml,v 1.6 2003/11/01 01:56:29 petere Exp $
PostgreSQL documentation
-->

<refentry id="SQL-ALTERSEQUENCE">
 <refmeta>
  <refentrytitle id="SQL-ALTERSEQUENCE-TITLE">ALTER SEQUENCE</refentrytitle>
  <refmiscinfo>SQL - 语言语句</refmiscinfo>
 </refmeta>

 <refnamediv>
  <refname>
   ALTER SEQUENCE
  </refname>
  <refpurpose>
   更改序列发生器的定义
  </refpurpose>
 </refnamediv> 

 <indexterm zone="sql-altersequence">
  <primary>ALTER SEQUENCE</primary>
 </indexterm>

 <refsynopsisdiv>
  <synopsis>
ALTER SEQUENCE <replaceable class="parameter">name</replaceable> [ INCREMENT [ BY ] <replaceable class="parameter">increment</replaceable> ]
    [ MINVALUE <replaceable class="parameter">minvalue</replaceable> | NO MINVALUE ] [ MAXVALUE <replaceable class="parameter">maxvalue</replaceable> | NO MAXVALUE ]
    [ RESTART [ WITH ] <replaceable class="parameter">start</replaceable> ] [ CACHE <replaceable class="parameter">cache</replaceable> ] [ [ NO ] CYCLE ]
  </synopsis>
 </refsynopsisdiv>

 <refsect1>
  <title>描述</title>

  <para>
   <command>ALTER SEQUENCE</command> 更改现有序列发生器的参数。在
   <command>ALTER SEQUENCE</command> 命令中未明确设置的参数，会保留其原有设置。
  </para>
 </refsect1>

 <refsect1>
  <title>参数</title>

   <para>
    <variablelist>
     <varlistentry>
      <term><replaceable class="parameter">name</replaceable></term>
      <listitem>
       <para>
        要更改的序列名称（可以是模式限定的）。
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><replaceable class="parameter">increment</replaceable></term>
      <listitem>
       <para>
        子句<literal>INCREMENT BY <replaceable
        class="parameter">increment</replaceable></literal>是可选的。正值会创建升序序列，负值会创建降序序列。如果未指定，则保留原有的增量值。
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><replaceable class="parameter">minvalue</replaceable></term>
      <term><literal>NO MINVALUE</literal></term>
      <listitem>
       <para>
        可选子句<literal>MINVALUE <replaceable
        class="parameter">minvalue</replaceable></literal>确定序列可生成的最小值。如果指定了<literal>NO MINVALUE</literal>，则升序序列和降序序列将分别使用默认值 1 和
        -2<superscript>63</>-1。如果两种选项都未指定，则保留当前最小值。
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><replaceable class="parameter">maxvalue</replaceable></term>
      <term><literal>NO MAXVALUE</literal></term>
      <listitem>
       <para>
        可选子句<literal>MAXVALUE <replaceable
        class="parameter">maxvalue</replaceable></literal>确定序列的最大值。如果指定了<literal>NO MAXVALUE</literal>，则升序序列和降序序列将分别使用默认值
        2<superscript>63</>-1 和 -1。如果两种选项都未指定，则保留当前最大值。
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><replaceable class="parameter">start</replaceable></term>
      <listitem>
       <para>
        可选子句<literal>RESTART WITH <replaceable
        class="parameter">start</replaceable></literal>更改序列的当前值。
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><replaceable class="parameter">cache</replaceable></term>
      <listitem>
       <para>
        子句<literal>CACHE <replaceable
        class="parameter">cache</replaceable></literal>启用预分配序列号并将其存储在内存中，以便更快访问。最小值为 1（一次只能生成一个值，即不缓存）。如果未指定，则保留旧的缓存值。
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term>CYCLE</term>
      <listitem>
       <para>
        可选的<literal>CYCLE</literal>关键字可用于启用序列回绕：当递增或递减序列分别达到
        <replaceable class="parameter">maxvalue</replaceable>或
        <replaceable class="parameter">minvalue</replaceable>时回绕。如果达到限制，下一个生成的数将分别为
        <replaceable class="parameter">minvalue</replaceable>或
        <replaceable class="parameter">maxvalue</replaceable>。
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term>NO CYCLE</term>
      <listitem>
       <para>
        如果指定了可选关键字<literal>NO CYCLE</literal>，则当序列达到其最大值后，任何对
        <function>nextval</function>的调用都会返回错误。如果既未指定<literal>CYCLE</literal>也未指定
        <literal>NO CYCLE</literal>，则保留原有的循环行为。
       </para>
      </listitem>
     </varlistentry>
    </variablelist>
   </para>
  </refsect1>

 <refsect1>
  <title>示例</title>

  <para>
   从 105 开始，重新启动一个名为 <literal>serial</literal> 的序列：
<programlisting>
ALTER SEQUENCE serial RESTART WITH 105;
</programlisting>
  </para>
 </refsect1>

 <refsect1>
  <title>注解</title>

  <para>
   为避免阻塞从同一序列获取号码的并发事务，<command>ALTER SEQUENCE</command> 从不回滚；更改立即生效且不可逆。
  </para>

  <para>
   <command>ALTER SEQUENCE</command> 不会立即影响当前后端之外的其他后端中已预分配（缓存）了序列值的
   <literal>nextval</>结果。这些后端会在注意到序列参数更改之前用完所有缓存的值。当前后端会立即受到影响。
  </para>
 </refsect1>


 <refsect1>
  <title>
   兼容性
  </title>

  <refsect2>
   <title>
    SQL99
   </title>

   <para>
    <command>ALTER SEQUENCE</command> 是 <productname>PostgreSQL</productname>
    的一种语言扩展。<acronym>SQL99</acronym> 中没有 <command>ALTER SEQUENCE</command>
    语句。
   </para>
  </refsect2>
 </refsect1>
</refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:nil
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
sgml-parent-document:nil
sgml-default-dtd-file:"../reference.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:"/usr/lib/sgml/catalog"
sgml-local-ecat-files:nil
End:
-->
