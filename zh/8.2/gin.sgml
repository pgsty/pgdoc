<!-- $PostgreSQL: pgsql/doc/src/sgml/gin.sgml,v 2.19 2009/04/09 19:07:44 tgl Exp $ -->

<chapter id="GIN">
<title>GIN 索引</title>

   <indexterm>
    <primary>index</primary>
    <secondary>GIN</secondary>
   </indexterm>

<sect1 id="gin-intro">
 <title>简介</title>

 <para>
   <acronym>GIN</acronym> 表示通用倒排索引（Generalized Inverted
   Index）。它是一种存储一组（键，张贴列表）对的索引结构，其中<quote>张贴列表</quote>（posting list）是键出现在其中的行的集合。每个被索引的值可以包含多个键，因此同一个行 ID 可以出现在多个张贴列表中。
 </para>

 <para>
   之所以说它是通用的，是因为 <acronym>GIN</acronym> 索引不需要知道自己所加速的操作是什么，而是使用为特定数据类型定义的自定义策略。
 </para>

 <para>
  <acronym>GIN</acronym> 的一个优点在于，它允许由数据类型所属领域的专家来开发带有合适访问方法的自定义数据类型，而不必依赖数据库专家。这一点与使用 <acronym>GiST</acronym> 的优势非常相似。
 </para>

 <para>
  <productname>PostgreSQL</productname> 中的 <acronym>GIN</acronym> 实现主要由
  Teodor Sigaev 和 Oleg Bartunov 维护。关于 <acronym>GIN</acronym> 的更多信息，可在他们的<ulink url="http://www.sai.msu.su/~megera/wiki/Gin">网站</ulink>上找到。
 </para>
</sect1>

<sect1 id="gin-extensibility">
 <title>可扩展性</title>

 <para>
   <acronym>GIN</acronym> 接口具有很高的抽象层次，访问方法实现者只需实现所访问数据类型的语义。<acronym>GIN</acronym> 层本身会处理并发、日志记录以及树结构的搜索。
 </para>

 <para>
   要让一个 <acronym>GIN</acronym> 访问方法工作起来，只需实现四个用户定义的方法，它们定义了树中键的行为，以及键、被索引值和可索引查询之间的关系。简言之，<acronym>GIN</acronym>
   将可扩展性与通用性、代码复用以及清晰的接口结合了起来。
 </para>

 <para>
   索引操作符类必须为<acronym>GIN</acronym>提供的四个方法是：
 </para>

 <variablelist>
    <varlistentry>
     <term>int compare(Datum a, Datum b)</term>
     <listitem>
      <para>
       比较键（不是被索引的值！）并返回一个小于零、等于零或大于零的整数，用以表示第一个键是小于、等于还是大于第二个键。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>Datum* extractValue(Datum inputValue, uint32 *nkeys)</term>
     <listitem>
      <para>
       给定一个要被索引的值，返回一个键数组。返回键的数量必须存入
       <literal>*nkeys</>。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>Datum* extractQuery(Datum query, uint32 *nkeys,
        StrategyNumber n)</term>
     <listitem>
      <para>
       给定一个待查询的值，返回一个键数组；也就是说，<literal>query</> 是一个可索引操作符右侧的值，而该操作符左侧是被索引列。<literal>n</> 是该操作符在操作符类中的策略号（见<xref linkend="xindex-strategies">）。通常，<function>extractQuery</> 需要查看 <literal>n</>，以确定
       <literal>query</> 的数据类型，以及需要提取的键值。返回键的数量必须存入 <literal>*nkeys</>。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>bool consistent(bool check[], StrategyNumber n, Datum query)</term>
     <listitem>
      <para>
       如果被索引值满足带有策略号 <literal>n</> 的查询操作符（如果该操作符在操作符类中被标记为 RECHECK，则是可能满足），则返回 TRUE。<literal>check</> 数组的长度与
       <function>extractQuery</> 此前为此查询返回的键数量相同。如果被索引值包含相应的查询键，则 <literal>check</>
       数组的对应元素为 TRUE，也就是说，如果 (check[i] == TRUE)，则
       <function>extractQuery</> 结果数组的第 i 个键出现在被索引值中。原始的 <literal>query</> 值（不是提取出的键数组！）也会被传入，以防 <function>consistent</> 方法需要查看它。
      </para>
     </listitem>
    </varlistentry>
  </variablelist>

</sect1>

<sect1 id="gin-implementation">
 <title>实现</title>

 <para>
  在内部，一个 <acronym>GIN</acronym> 索引包含一个基于键构建的
  B-tree 索引，其中每个键都是被索引值的一个元素（例如数组成员），而叶子页中的每个元组要么是指向堆指针 B-tree 的指针（PT，posting tree），要么在列表足够小时是堆指针的列表（PL，posting list）。
 </para>



</sect1>

<sect1 id="gin-tips">
<title>GIN 使用提示和技巧</title>

 <variablelist>
  <varlistentry>
   <term>创建还是插入</term>
   <listitem>
    <para>
     向 <acronym>GIN</acronym> 索引插入数据可能较慢，因为每个值很可能需要插入许多键。因此，对于向表中执行的批量插入，建议先删除 GIN 索引，待批量插入完成后再重建它。
    </para>

   </listitem>
  </varlistentry>

  <varlistentry>
   <term><xref linkend="guc-gin-fuzzy-search-limit"></term>
   <listitem>
    <para>
     开发 <acronym>GIN</acronym> 索引的首要目标，是为
     <productname>PostgreSQL</productname> 中高度可扩展的全文搜索提供支持，而全文搜索经常会返回非常大的结果集。此外，这种情况常发生在查询包含非常高频的词时，因此庞大的结果集甚至没有用处。由于从索引中读取许多元组并对其排序可能需要很长时间，这在生产环境中是不可接受的。（注意，索引搜索本身是非常快的。）
    </para>
    <para>
     为了便于受控地执行这类查询，<acronym>GIN</acronym> 对返回行数设置了一个可配置的软上限，即配置参数
     <varname>gin_fuzzy_search_limit</varname>。默认值为 0（表示无限制）。如果设置了非零限制，那么返回集合将是整个结果集的一个随机子集。
    </para>
    <para>
     <quote>软</quote>的意思是，实际返回结果的数量可能会与指定的限制略有不同，这取决于查询以及系统随机数生成器的质量。
    </para>
   </listitem>
  </varlistentry>
 </variablelist>

</sect1>

<sect1 id="gin-limit">
 <title>限制</title>

 <para>
  <acronym>GIN</acronym> 不支持全索引扫描：因为每个值往往对应许多键，每个堆指针会被返回多次，而没有简便的方法可以避免这一点。
 </para>

 <para>
  当<function>extractQuery</function>返回零个键时，<acronym>GIN</>会报错。根据操作符的不同，空查询可能匹配全部、部分或不匹配任何被索引值（例如，每个数组都包含空数组，但都不与空数组重叠），<acronym>GIN</>无法确定正确答案，即使能确定全索引扫描是正确的，也无法产生全索引扫描的结果。
 </para>

 <para>
  <function>extractValue</>返回零个键并不是错误，但在这种情况下，被索引值不会在索引中有任何表示。这也是全索引扫描没有用的另一个原因&mdash;它会漏掉这样的行。
 </para>

 <para>
  <acronym>GIN</acronym> 只按等值匹配来搜索键。这一点将来可能会有所改进。
 </para>
</sect1>

<sect1 id="gin-examples">
 <title>示例</title>

 <para>
  <productname>PostgreSQL</productname> 源码发行版为所有内部类型的一维数组提供了
  <acronym>GIN</acronym> 操作符类。下列 <filename>contrib</filename> 模块也包含 <acronym>GIN</acronym>
  操作符类：

 <variablelist>
  <varlistentry>
   <term>intarray</term>
   <listitem>
    <para>对 int4[] 的增强支持</para>
   </listitem>
  </varlistentry>

  <varlistentry>
   <term>tsearch2</term>
   <listitem>
    <para>倒排文本索引支持。对于非常大且基本静态的文档集，这要快得多。</para>
   </listitem>
  </varlistentry>
 </variablelist>
 </para>
</sect1>

</chapter>
