<!-- $PostgreSQL: pgsql/doc/src/sgml/ref/pg_config-ref.sgml,v 1.30 2010/07/05 18:54:37 tgl Exp $ -->

<refentry id="app-pgconfig">
 <refmeta>
  <refentrytitle>pg_config</refentrytitle>
  <manvolnum>1</manvolnum>
  <refmiscinfo>Application</refmiscinfo>
 </refmeta>

 <refnamediv>
  <refname>pg_config</refname>
  <refpurpose>获取已安装的<productname>PostgreSQL</productname>版本的信息</refpurpose>
 </refnamediv>

 <indexterm zone="app-pgconfig">
  <primary>pg_config</primary>
 </indexterm>

 <refsynopsisdiv>
  <cmdsynopsis>
   <command>pg_config</command>
   <arg rep="repeat"><replaceable>option</replaceable></arg>
  </cmdsynopsis>
 </refsynopsisdiv>

 <refsect1>
  <title>Description</>
  <para>
   <application>pg_config</application>工具打印当前已安装的<productname>PostgreSQL</productname>版本的配置参数。例如，希望与<productname>PostgreSQL</productname>进行接口的软件包可以使用它，以便找到所需的头文件和库。
  </para>
 </refsect1>


 <refsect1>
  <title>Options</title>

  <para>
   要使用 <application>pg_config</application>，请提供下列一个或多个选项：
   <variablelist>
    <varlistentry>
     <term><option>--bindir</option></>
     <listitem>
      <para>
       打印用户可执行文件的位置。例如，可以用它来寻找 <command>psql</command> 程序。通常这也是 <filename>pg_config</filename> 程序所在的位置。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>--docdir</option></>
     <listitem>
      <para>
       打印文档文件的位置。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>--htmldir</option></>
     <listitem>
      <para>
       打印文档文件所在的位置。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>--includedir</option></>
     <listitem>
      <para>
       打印 HTML 文档文件所在的位置。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>--pkgincludedir</option></>
     <listitem>
      <para>
       打印客户端接口的 C 头文件所在的位置。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>--includedir-server</option></>
     <listitem>
      <para>
       打印其他 C 头文件所在的位置。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>--libdir</option></>
     <listitem>
      <para>
       打印服务器编程所用 C 头文件所在的位置。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>--pkglibdir</option></>
     <listitem>
      <para>
       打印目标代码库所在的位置。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>--localedir</option></>
     <listitem>
      <para>
       打印区域支持文件的位置。（如果在构建 <productname>PostgreSQL</productname> 时没有配置区域支持，这将是一个空字符串。）
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>--mandir</option></>
     <listitem>
      <para>
       打印手册页的位置。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>--sharedir</option></>
     <listitem>
      <para>
       打印手册页所在的位置。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>--sysconfdir</option></>
     <listitem>
      <para>
       打印与体系结构无关的支持文件所在的位置。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>--pgxs</option></>
     <listitem>
      <para>
       打印系统范围的配置文件所在的位置。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>--configure</option></>
     <listitem>
      <para>
       打印为构建 <productname>PostgreSQL</productname> 而配置时传递给 <filename>configure</filename> 脚本的选项。这可用于复现完全相同的配置，或者查明某个二进制包是用什么选项构建的。（不过注意，二进制包通常包含厂商特有的自定义补丁。）另请参见下面的例子。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>--cc</option></>
     <listitem>
      <para>
       打印为构建<varname>PostgreSQL</varname>而运行<productname>configure</>脚本时给定的选项。这可用于重现完全相同的配置，或者查明某个二进制软件包是用哪些选项构建的。（但请注意，二进制软件包通常包含供应商特定的自定义补丁。）另请参见下面的示例。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>--cppflags</option></>
     <listitem>
      <para>
       打印构建 <productname>PostgreSQL</productname> 时使用的 <varname>CPPFLAGS</varname> 变量的值。它显示预处理时需要的 C 编译器开关（通常是 <literal>-I</literal> 开关）。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>--cflags</option></>
     <listitem>
      <para>
       打印构建 <productname>PostgreSQL</productname> 时使用的 <varname>CFLAGS</varname> 变量的值。它显示 C 编译器开关。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>--cflags_sl</option></>
     <listitem>
      <para>
       打印构建<productname>PostgreSQL</productname>时所用的<varname>CFLAGS</varname>变量值。这显示 C 编译器开关。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>--ldflags</option></>
     <listitem>
      <para>
       打印构建<productname>PostgreSQL</productname>时所用的<varname>CFLAGS_SL</varname>变量值。这显示构建共享库时使用的额外 C 编译器开关。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>--ldflags_ex</option></>
     <listitem>
      <para>
       打印构建<productname>PostgreSQL</productname>时所用的<varname>LDFLAGS</varname>变量值。这显示链接器开关。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>--ldflags_sl</option></>
     <listitem>
      <para>
       打印构建<productname>PostgreSQL</productname>时所用的<varname>LDFLAGS_EX</varname>变量值。这显示仅用于构建可执行文件的链接器开关。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>--libs</option></>
     <listitem>
      <para>
       打印构建 <productname>PostgreSQL</productname> 时使用的 <varname>LIBS</varname> 变量的值。它通常包含链接进 <productname>PostgreSQL</productname> 的外部库的 <literal>-l</literal> 开关。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>--version</option></>
     <listitem>
      <para>
       打印 <productname>PostgreSQL</productname> 的版本。
      </para>
     </listitem>
    </varlistentry>
   </variablelist>

   如果给出了多个选项，信息会按该顺序打印，每行一项。如果没有给出选项，则打印所有可用信息，并带有标签。
  </para>
 </refsect1>


 <refsect1>
  <title>Notes</title>

  <para>
   <option>--includedir-server</option> 选项是在 <productname>PostgreSQL</productname> 7.2 中加入的。在之前的版本中，服务器头文件与客户端头文件安装在同一位置，可以用 <option>--includedir</option> 选项查询。要让你的软件包同时处理这两种情况，请先尝试较新的选项，并测试退出状态以确定它是否成功。
  </para>

  <para>
   选项<option>--docdir</option>、<option>--pkgincludedir</option>、
   <option>--localedir</option>、<option>--mandir</option>、
   <option>--sharedir</option>、<option>--sysconfdir</option>、
   <option>--cc</option>、<option>--cppflags</option>、
   <option>--cflags</option>、<option>--cflags_sl</option>、
   <option>--ldflags</option>、<option>--ldflags_sl</option>以及<option>--libs</option>是在<productname>PostgreSQL</productname> 8.1 中加入的。选项<option>--htmldir</option>是在<productname>PostgreSQL</productname> 8.4 中加入的。选项<option>--ldflags_ex</option>是在<productname>PostgreSQL</productname> 9.0 中加入的。
  </para>

  <para>
   在 <productname>PostgreSQL</productname> 7.1 之前的发行版中，还没有 <command>pg_config</command>，当时不存在获取等效配置信息的方法。
  </para>
 </refsect1>


 <refsect1>
  <title>Example</title>

  <para>
   要重现当前 PostgreSQL 安装的构建配置，请运行下列命令：
<programlisting>
eval ./configure `pg_config --configure`
</programlisting>
   <literal>pg_config --configure</literal>的输出包含 shell 引号，因此带空格的参数能够被正确表示。因此，要得到正确的结果，需要使用<literal>eval</literal>。
  </para>
 </refsect1>

</refentry>
