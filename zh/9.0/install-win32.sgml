<!-- doc/src/sgml/install-windows.sgml -->

<chapter id="install-windows">
 <title>在 <productname>Windows</productname> 上从源代码安装</title>

 <indexterm>
  <primary>安装</primary>
  <secondary>在 Windows 上</secondary>
 </indexterm>

 <para>
  建议大多数用户下载适用于 Windows 的二进制发行版，该发行版以一键安装程序包的形式提供，可从
  <productname>PostgreSQL</productname> 网站获取。从
  源码构建仅面向开发 <productname>PostgreSQL</productname> 或扩展
  的人员。
 </para>

 <para>
  在 <productname>Windows</productname> 上构建 PostgreSQL 有几种
  不同的方法。使用 Microsoft 工具进行构建的最简单方式，是安装
  受支持版本的
  <productname>Microsoft Platform SDK</productname> 并使用其自带的
  编译器。也可以使用完整的
  <productname>Microsoft Visual C++ 2005 或 2008</productname> 进行
  构建。在某些情况下，除编译器之外还需要安装
  <productname>Platform SDK</productname>。
 </para>

 <para>
  也可以使用 <productname>MinGW</productname> 提供的 GNU 编译器
  工具构建 PostgreSQL，或者在较旧版本的
  <productname>Windows</productname> 上使用
  <productname>Cygwin</productname>。
 </para>

 <para>
  最后，客户端访问库（<application>libpq</application>）可以用
  <productname>Visual C++ 7.1</productname> 或
  <productname>Borland C++</productname> 构建，以便与用这些工具
  构建的静态链接应用兼容。
 </para>

 <para>
  使用 <productname>MinGW</productname> 或
  <productname>Cygwin</productname> 构建时采用常规构建系统，参见
  <xref linkend="installation"> 以及
  <xref linkend="installation-notes-mingw"> 和 <xref linkend="installation-notes-cygwin"> 中的具体说明。
  这两种构建无法生成 64 位二进制文件。不推荐使用
  <productname>Cygwin</productname>，它只应用于原生构建无法工作的
  较旧版本 <productname>Windows</productname>，例如
  <productname>Windows 98</productname>。只有在用
  <productname>MinGW</productname> 构建其他模块时才推荐使用它。
  官方二进制文件是使用 <productname>Visual Studio</productname>
  构建的。
 </para>

 <sect1 id="install-windows-full">
  <title>Building with <productname>Visual C++</productname> or the
  <productname>Platform SDK</productname></title>

 <para>
  PostgreSQL 可以用 Microsoft 的 Visual C++ 编译器套件构建。这些
  编译器可以来自
  <productname>Visual Studio</productname>、
  <productname>Visual Studio Express</productname> 或某些版本的
  <productname>Platform SDK</productname>。如果你还没有搭好
  <productname>Visual Studio</productname> 环境，最简单的方式是使用
  <productname>Platform SDK</productname> 中的编译器，它可以从
  Microsoft 免费下载。
 </para>

 <para>
  PostgreSQL 支持
  <productname>Visual Studio 2005</productname> 和
  <productname>Visual Studio 2008</productname> 的编译器。如果只使用
  Platform SDK，或者为 64 位 Windows 构建，则只支持
  <productname>Visual Studio 2008</productname>。尚不支持
  <productname>Visual Studio 2010</productname>。
 </para>

 <para>
  使用 <productname>Platform SDK</productname> 构建时，支持 SDK 的
  6.0 到 7.0 版。更旧或更新的版本都无法工作。特别地，7.0a 及之后
  的版本无法工作，因为它们包含
  <productname>Visual Studio 2010</productname> 的编译器。
 </para>

 <para>
  使用 <productname>Visual C++</productname> 构建所需的工具位于
  <filename>src/tools/msvc</filename> 目录。构建时，请确保系统
  PATH 中没有 <productname>MinGW</productname> 或
  <productname>Cygwin</productname> 的工具。另外，请确保 PATH 中有
  所有必需的 Visual C++ 工具。在
  <productname>Visual Studio</productname> 中，启动
  <application>Visual Studio Command Prompt</application>。在
  <productname>Platform SDK</productname> 中，启动开始菜单中 SDK
  下列出的 <application>CMD shell</application>。如果希望构建 64 位
  版本，必须使用该命令的 64 位版本，反之亦然。所有命令都应当在
  <filename>src\tools\msvc</filename> 目录中运行。
 </para>

 <para>
  构建之前，你可能需要编辑文件
  <filename>config.pl</filename>，以反映你要更改的配置选项，或要
  使用的第三方库的路径。完整配置的确定方式是：先读取并解析文件
  <filename>config_default.pl</filename>，然后应用
  <filename>config.pl</filename> 中的更改。例如，要指定
  <productname>Python</productname> 安装的位置，把以下内容放进
  <filename>config.pl</filename>：
<programlisting>
$config->{python} = 'c:\python26';
</programlisting>
  只需指定与 <filename>config_default.pl</filename> 中不同的参数。
 </para>

 <para>
  如果需要设置其他环境变量，可创建一个名为
  <filename>buildenv.pl</filename> 的文件，并把所需的命令放在其中。
  例如，要在 bison 不在 PATH 中时添加它的路径，可创建包含以下内容
  的文件：
<programlisting>
$ENV{PATH}=$ENV{PATH} . ';c:\some\where\bison\bin';
</programlisting>
 </para>

 <sect2>
  <title>要求</title>
  <para>
   构建 <productname>PostgreSQL</productname> 需要以下附加产品。可用
   <filename>config.pl</filename> 文件指定各个库所在的目录。

   <variablelist>
    <varlistentry>
     <term><productname>Microsoft Platform SDK</productname></term>
     <listitem><para>
      建议升级到最新可用版本的
      <productname>Microsoft Platform SDK</productname>，它可以从
      <ulink url="http://www.microsoft.com/downloads/"></> 下载。
     </para>
     <para>
      必须始终包含 SDK 的
      <application>Windows Headers and Libraries</application> 部分。
      如果安装的 <productname>Platform SDK</productname> 包含
      <application>Visual C++ Compilers</application>，则构建不需要
      <productname>Visual Studio</productname>。
     </para></listitem>
    </varlistentry>

    <varlistentry>
     <term><productname>ActiveState Perl</productname></term>
     <listitem><para>
      运行构建生成脚本需要 ActiveState Perl。MinGW 或 Cygwin 的
      Perl 无法工作。它也必须位于 PATH 中。二进制文件可以从
      <ulink url="http://www.activestate.com"></> 下载（注意：需要
      5.8 或更高版本，免费的 Standard Distribution 就足够了）。
     </para></listitem>
    </varlistentry>

   </variablelist>
  </para>
  <para>
   以下附加产品不是起步所必需的，但构建完整的软件包时需要。可用
   <filename>config.pl</filename> 文件指定各个库所在的目录。

   <variablelist>
    <varlistentry>
     <term><productname>ActiveState TCL</productname></term>
     <listitem><para>
      Required for building <application>PL/TCL</application> (Note: version
      8.4 is required, the free Standard Distribution is sufficient).
     </para></listitem>
    </varlistentry>

    <varlistentry>
     <term><productname>Bison</productname> and
      <productname>Flex</productname></term>
     <listitem><para>
      从 Git 构建时需要 Bison 和 Flex，从发布文件构建则不需要。
      注意，只有 Bison 1.875 或 2.2 及之后的版本可用。另外，需要
      Flex 2.5.31 或更高版本。Bison 可以从
      <ulink url="http://gnuwin32.sourceforge.net"></> 下载。Flex 可以
      从
      <ulink url="http://www.postgresql.org/ftp/misc/winflex/"></> 下载。
     </para>

     <note>
      <para>
        GnuWin32 的 Bison 发行版似乎有一个缺陷：当它安装在名称中
        带空格的目录（如英文安装的默认位置
        <filename>C:\Program Files\GnuWin32</filename>）中时会
        失常。可考虑改为安装到
        <filename>C:\GnuWin32</filename>。
       </para>
     </note>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><productname>Diff</productname></term>
     <listitem><para>
      运行回归测试需要 Diff，可以从
      <ulink url="http://gnuwin32.sourceforge.net"></> 下载。
     </para></listitem>
    </varlistentry>

    <varlistentry>
     <term><productname>Gettext</productname></term>
     <listitem><para>
      以 NLS 支持构建需要 Gettext，可以从
      <ulink url="http://gnuwin32.sourceforge.net"></> 下载。注意，
      二进制文件、依赖和开发者文件全都需要。
     </para></listitem>
    </varlistentry>

    <varlistentry>
     <term><productname>MIT Kerberos</productname></term>
     <listitem><para>
      Kerberos 认证支持需要。MIT Kerberos 可以从
      <ulink url="http://web.mit.edu/Kerberos/dist/index.html"></> 下载。
     </para></listitem>
    </varlistentry>

    <varlistentry>
     <term><productname>libxml2</productname> and
      <productname>libxslt</productname></term>
     <listitem><para>
      XML 支持需要。二进制文件可以从
      <ulink url="http://zlatkovic.com/pub/libxml"></> 下载，或源码从
      <ulink url="http://xmlsoft.org"></> 下载。注意，libxml2 需要
      iconv，它可在同一下载位置获得。
     </para></listitem>
    </varlistentry>

    <varlistentry>
     <term><productname>openssl</productname></term>
     <listitem><para>
      SSL 支持需要。二进制文件可以从
      <ulink url="http://www.slproweb.com/products/Win32OpenSSL.html"></>
      下载，或源码从 <ulink url="http://www.openssl.org"></> 下载。
     </para></listitem>
    </varlistentry>

    <varlistentry>
     <term><productname>ossp-uuid</productname></term>
     <listitem><para>
      UUID-OSSP 支持（仅 contrib）需要。源码可以从
      <ulink url="http://www.ossp.org/pkg/lib/uuid/"></> 下载。
     </para></listitem>
    </varlistentry>

    <varlistentry>
     <term><productname>Python</productname></term>
     <listitem><para>
      构建 <application>PL/Python</application> 需要。二进制文件可以
      从 <ulink url="http://www.python.org"></> 下载。
     </para></listitem>
    </varlistentry>

    <varlistentry>
     <term><productname>zlib</productname></term>
     <listitem><para>
      <application>pg_dump</application> 和
      <application>pg_restore</application> 的压缩支持需要。二进制
      文件可以从 <ulink url="http://www.zlib.net"></> 下载。
     </para></listitem>
    </varlistentry>

   </variablelist>
  </para>
 </sect2>

 <sect2>
  <title>64 位 Windows 的特殊注意事项</title>

  <para>
   在 64 位 Windows 上，PostgreSQL 只能为 x64 架构构建，不支持
   Itanium 处理器。
  </para>

  <para>
   不支持在同一个构建树中混合 32 位和 64 位版本。构建系统会自动
   检测它运行在 32 位还是 64 位环境中，并相应地构建 PostgreSQL。
   因此，构建之前启动正确的命令提示符很重要。
  </para>

  <para>
   要使用服务器端的第三方库（如 <productname>python</> 或
   <productname>openssl</>），该库也<emphasis>必须</emphasis>是 64 位
   的。不支持在 64 位服务器中装载 32 位库。PostgreSQL 支持的若干
   第三方库可能只有 32 位版本，这种情况下它们不能与 64 位
   PostgreSQL 一起使用。
  </para>
 </sect2>

 <sect2>
  <title>构建</title>

  <para>
   要以发布配置（默认）构建整个 PostgreSQL，运行以下命令：
<screen>
<userinput>build</userinput>
</screen>
   要以调试配置构建整个 PostgreSQL，运行以下命令：
<screen>
<userinput>build DEBUG</userinput>
</screen>
   要只构建单个项目（例如 psql），运行以下命令：
<screen>
<userinput>build psql</userinput>
<userinput>build DEBUG psql</userinput>
</screen>
   要把默认构建配置改为调试，把以下内容放进
   <filename>buildenv.pl</filename> 文件：
<programlisting>
$ENV{CONFIG}="Debug";
</programlisting>
  </para>

  <para>
   也可以在 Visual Studio GUI 内部构建。这种情况下，需要从命令
   提示符运行：
<screen>
<userinput>perl mkvcbuild.pl</userinput>
</screen>
   然后在 Visual Studio 中打开生成的
   <filename>pgsql.sln</filename>（位于源码树根目录）。
  </para>
 </sect2>

 <sect2>
  <title>清理和安装</title>

  <para>
   大多数时候，Visual Studio 的自动依赖跟踪会处理好更改的文件。但
   如果发生了大的更改，可能需要清理安装。为此，只需运行
   <filename>clean.bat</filename> 命令，它会自动清除所有生成的文件。
   也可以带
   <parameter>dist</parameter> 参数运行它，此时它的行为类似于
   <userinput>make distclean</userinput>，还会删除 flex/bison 的
   输出文件。
  </para>

  <para>
   默认情况下，所有文件都写入
   <filename>debug</filename> 或 <filename>release</filename> 目录的
   子目录中。要按标准布局安装这些文件，并生成初始化和使用数据库
   所需的文件，运行以下命令：
<screen>
<userinput>install c:\destination\directory</userinput>
</screen>
  </para>
 </sect2>

 <sect2>
  <title>运行回归测试</title>

  <para>
   要运行回归测试，先确保已完成所有必需部分的构建。另外，确保装载
   系统所有部分所需的 DLL（如过程语言所需的 Perl 和 Python DLL）
   位于系统路径中。如果不是，可通过
   <filename>buildenv.pl</filename> 文件设置。要运行测试，从
   <filename>src\tools\msvc</filename> 目录运行以下命令之一：
<screen>
<userinput>vcregress check</userinput>
<userinput>vcregress installcheck</userinput>
<userinput>vcregress plcheck</userinput>
<userinput>vcregress contribcheck</userinput>
</screen>

   要更改所用的调度表（默认为 parallel），把它附加到命令行，
   例如：
<screen>
<userinput>vcregress check serial</userinput>
</screen>

   关于回归测试的更多信息，参见
   <xref linkend="regress">。
  </para>
 </sect2>

 <sect2>
  <title>构建文档</title>

  <para>
   以 HTML 格式构建 PostgreSQL 文档需要若干工具和文件。为所有这些
   文件创建一个根目录，并把它们存放在下面列表中的各子目录里。
   <variablelist>
    <varlistentry>
     <term>OpenJade 1.3.1-2</term>
     <listitem><para>
      从
      <ulink url="http://sourceforge.net/projects/openjade/files/openjade/1.3.1/openjade-1_3_1-2-bin.zip/download"></>
      下载并解压到子目录 <filename>openjade-1.3.1</filename>。
     </para></listitem>
    </varlistentry>

    <varlistentry>
     <term>DocBook DTD 4.2</term>
     <listitem><para>
      从
      <ulink url="http://www.oasis-open.org/docbook/sgml/4.2/docbook-4.2.zip"></>
      下载并解压到子目录 <filename>docbook</filename>。
     </para></listitem>
    </varlistentry>

    <varlistentry>
     <term>DocBook DSSSL 1.79</term>
     <listitem><para>
      从
      <ulink url="http://sourceforge.net/projects/docbook/files/docbook-dsssl/1.79/docbook-dsssl-1.79.zip/download"></>
      下载并解压到子目录
      <filename>docbook-dsssl-1.79</filename>。
     </para></listitem>
    </varlistentry>

    <varlistentry>
     <term>ISO 字符实体</term>
     <listitem><para>
      从
      <ulink url="http://www.oasis-open.org/cover/ISOEnts.zip"></> 下载
      并解压到子目录 <filename>docbook</filename>。
     </para></listitem>
    </varlistentry>
   </variablelist>
   编辑 <filename>buildenv.pl</filename> 文件，为根目录的位置添加
   一个变量，例如：
<programlisting>
$ENV{DOCROOT}='c:\docbook';
</programlisting>
   要构建文档，运行命令
   <filename>builddoc.bat</filename>。注意，为了生成索引，它实际上
   会运行两次构建。生成的 HTML 文件位于
   <filename>doc\src\sgml</filename>。
  </para>
 </sect2>

 </sect1>

 <sect1 id="install-windows-libpq">
  <title>用 <productname>Visual C++</productname> 或
  <productname>Borland C++</productname> 构建
  <application>libpq</application></title>

 <para>
  只有在需要带有不同调试/发布标志的版本，或需要静态库以链接进应用
  时，才推荐使用 <productname>Visual C++ 7.1-9.0</productname> 或
  <productname>Borland C++</productname> 构建 libpq。日常使用推荐
  <productname>MinGW</productname>、
  <productname>Visual Studio</productname> 或
  <productname>Platform SDK</productname> 方法。
 </para>

 <para>
  要使用
  <productname>Visual Studio 7.1 或更高版本</productname> 构建
  <application>libpq</application> 客户端库，切换到
  <filename>src</filename> 目录并输入命令：
<screen>
<userinput>nmake /f win32.mak</userinput>
</screen>
 </para>
 <para>
 要使用
 <productname>Visual Studio 8.0 或更高版本</productname> 构建 64 位
 版本的 <application>libpq</application> 客户端库，切换到
 <filename>src</filename> 目录并输入命令：
<screen>
<userinput>nmake /f win32.mak CPU=AMD64</userinput>
</screen>
 支持的变量的更多细节参见
 <filename>win32.mak</filename> 文件。
 </para>

 <para>
  要使用
  <productname>Borland C++</productname> 构建
  <application>libpq</application> 客户端库，切换到
  <filename>src</filename> 目录并输入命令：
<screen>
<userinput>make -N -DCFG=Release /f bcc32.mak</userinput>
</screen>
 </para>

 <sect2>
 <title>生成的文件</title>
 <para>
  将构建以下文件：

  <variablelist>
   <varlistentry>
    <term><filename>interfaces\libpq\Release\libpq.dll</filename></term>
    <listitem>
     <para>
      动态可链接的前端库
     </para>
    </listitem>
   </varlistentry>
  
   <varlistentry>
    <term><filename>interfaces\libpq\Release\libpqdll.lib</filename></term>
    <listitem>
     <para>
      用于把程序链接到 <filename>libpq.dll</filename> 的导入库
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><filename>interfaces\libpq\Release\libpq.lib</filename></term>
    <listitem>
     <para>
      前端库的静态版本
     </para>
    </listitem>
   </varlistentry>

  </variablelist>
 </para>

 <para>
  通常不需要安装任何客户端文件。应当把
  <filename>libpq.dll</filename> 文件放在与应用的可执行文件相同的
  目录中。除非绝对必要，不要把
  <filename>libpq.dll</filename> 安装到
  <filename>Windows</>、
  <filename>System</> 或 <filename>System32</> 目录。如果该文件
  用安装程序安装，则应利用文件中包含的
  <symbol>VERSIONINFO</symbol> 资源带版本检查地安装，以确保不会
  覆盖较新的库版本。
 </para>

 <para>
  如果打算在这台机器上用
  <application>libpq</application> 进行开发，则必须把源码树的
  <filename>src\include</filename> 和
  <filename>src\interfaces\libpq</filename> 子目录加入编译器设置
  的包含路径。
 </para>

 <para>
  要使用该库，必须把
  <filename>libpqdll.lib</filename> 文件加入你的项目。（在
  Visual C++ 中，只需右键单击项目并选择添加它。）
 </para>
 </sect2>
 </sect1>
</chapter>
