<!-- $PostgreSQL: pgsql/doc/src/sgml/recovery-config.sgml,v 2.8 2010/06/11 10:13:09 heikki Exp $ -->

<chapter Id="recovery-config">
  <title>恢复配置</title>

  <indexterm>
   <primary>configuration</primary>
   <secondary>of recovery</secondary>
   <tertiary>of a standby server</tertiary>
  </indexterm>

   <para>本章介绍 <filename>recovery.conf</filename><indexterm><primary>recovery.conf</primary></indexterm> 文件中可用的设置。它们只在恢复期间生效。如果之后还要进行恢复，必须重新设置这些参数。恢复开始后，就不能再更改它们。</para>

   <para><filename>recovery.conf</filename> 中的设置使用 <literal>name = 'value'</literal> 格式，每行指定一个参数。井号（<literal>#</literal>）表示该行余下内容为注释。要在参数值中嵌入单引号，请写两个单引号（<literal>''</literal>）。</para>

   <para>安装目录的 <filename>share/</filename> 目录中提供了示例文件 <filename>share/recovery.conf.sample</filename>。</para>

  <sect1 id="archive-recovery-settings">

    <title>归档恢复设置</title>
     <variablelist>

     <varlistentry id="restore-command" xreflabel="restore_command">
      <term><varname>restore_command</varname> (<type>string</type>)</term>
      <indexterm>
        <primary><varname>restore_command</> recovery parameter</primary>
      </indexterm>
      <listitem>
       <para>
        用于获取 WAL 文件系列的一个已归档段的本地 shell 命令。这个参数是归档恢复所必需的，但是对于流复制是可选的。在该字符串中的任何<literal>%f</literal>会被替换为从归档中获得的文件的名字，并且任何<literal>%p</literal>会被在服务器上的复制目标路径名替换（该路径名是相对于当前工作目录的，即集簇的数据目录）。任何<literal>%r</literal>会被包含上一个可用重启点的文件的名字所替换。在那些必须被保留用于使得一次恢复变成可重启的文件中，这个文件是其中最早的一个，因此这个信息可以被用来把归档截断为支持从当前恢复重启所需的最小值。<literal>%r</literal>通常只被温备配置（见<xref linkend="warm-standby">）所使用。要嵌入一个真正的<literal>%</literal>字符，需要写成<literal>%%</literal>。
       </para>

       <para>
        很重要的一点是，该命令只有在成功时才返回一个为零的退出状态。该命令<emphasis>将</emphasis>会被询问不存在于归档中的文件名，当这样被询问时它必须返回非零。示例：
<programlisting>
restore_command = 'cp /mnt/server/archivedir/%f "%p"'
restore_command = 'copy "C:\\server\\archivedir\\%f" "%p"'  # Windows
</programlisting>
       </para>
      </listitem>
     </varlistentry>

     <varlistentry id="archive-cleanup-command" xreflabel="archive_cleanup_command">
      <term><varname>archive_cleanup_command</varname> (<type>string</type>)</term>
      <indexterm>
        <primary><varname>archive_cleanup_command</> recovery parameter</primary>
      </indexterm>
      <listitem>
       <para>
        这个可选参数指定将在每个重启点执行的一个 shell 命令。<varname>archive_cleanup_command</varname>的目的是提供一种清理备服务器不再需要的旧已归档 WAL 文件的机制。任何<literal>%r</literal>会被包含上一个可用重启点的文件的名字所替换。在那些必须被<emphasis>保留</emphasis>用于使得一次恢复变成可重启的文件中，这个文件是其中最早的一个，因此所有早于<literal>%r</literal>的文件都可以被安全地移除。这个信息可以被用来把归档截断为支持从当前恢复重启所需的最小值。<literal>contrib</literal>中提供的<application>pg_archivecleanup</application>工具（见<xref linkend="pgarchivecleanup">）是典型的单备库配置中<varname>archive_cleanup_command</varname>的一个便捷目标，例如：
<programlisting> archive_cleanup_command = 'pg_archivecleanup /mnt/server/archivedir %r' </programlisting>但要注意，如果多个备服务器从同一个归档目录恢复，就需要确保在 WAL 文件不再被任何服务器需要之前不要删除它们。<varname>archive_cleanup_command</varname>通常用于温备配置（见<xref linkend="warm-standby">）。要在命令中嵌入一个真正的<literal>%</literal>字符，需要写成<literal>%%</literal>。
       </para>
       <para>
        如果该命令返回一个非零的退出状态，那么一个 WARNING 日志消息将被写入。
       </para>
      </listitem>
     </varlistentry>

     <varlistentry id="recovery-end-command" xreflabel="recovery_end_command">
      <term><varname>recovery_end_command</varname> (<type>string</type>)</term>
      <indexterm>
        <primary><varname>recovery_end_command</> recovery parameter</primary>
      </indexterm>
      <listitem>
       <para>此参数指定一个 shell 命令，在恢复结束时仅执行一次。此参数可选。<varname>recovery_end_command</varname> 用于提供复制或恢复后的清理机制。与 <xref linkend="archive-cleanup-command"> 一样，所有 <literal>%r</literal> 都会被替换为包含最后一个有效重启点的文件的名称。</para>
       <para>
        如果命令返回非零退出状态，将写入一条 WARNING 日志消息，但数据库仍会继续启动。例外是，如果命令被信号终止，数据库将不会继续启动。
       </para>
      </listitem>
     </varlistentry>

    </variablelist>

  </sect1>

  <sect1 id="recovery-target-settings">

    <title>恢复目标设置</title>
     <variablelist>

     <varlistentry id="recovery-target-time" xreflabel="recovery_target_time">
      <term><varname>recovery_target_time</varname>
           (<type>timestamp</type>)
      </term>
      <indexterm>
        <primary><varname>recovery_target_time</> recovery parameter</primary>
      </indexterm>
      <listitem>
       <para>
        此参数指定恢复要进行到的时间戳。在 <varname>recovery_target_time</varname> 和 <xref linkend="recovery-target-xid"> 中，最多只能指定一个。默认行为是恢复到 WAL 日志的末尾。精确的停止点还受 <xref linkend="recovery-target-inclusive"> 影响。
       </para>
      </listitem>
     </varlistentry>

     <varlistentry id="recovery-target-xid" xreflabel="recovery_target_xid">
      <term><varname>recovery_target_xid</varname> (<type>string</type>)</term>
      <indexterm>
        <primary><varname>recovery_target_xid</> recovery parameter</primary>
      </indexterm>
      <listitem>
       <para>
        此参数指定恢复要进行到的事务 ID。请注意，虽然事务 ID 在事务开始时按顺序分配，但事务完成的顺序可能与 ID 的数值顺序不同。恢复的事务是那些在指定事务之前提交的事务，并可选择包括指定事务本身。在 <varname>recovery_target_xid</varname> 和 <xref linkend="recovery-target-time"> 中，最多只能指定一个。默认行为是恢复到 WAL 日志的末尾。精确的停止点还受 <xref linkend="recovery-target-inclusive"> 影响。
       </para>
      </listitem>
     </varlistentry>

     <varlistentry id="recovery-target-inclusive"
                   xreflabel="recovery_target_inclusive">
      <term><varname>recovery_target_inclusive</varname>
        (<type>boolean</type>)
      </term>
      <indexterm>
        <primary><varname>recovery_target_inclusive</> recovery parameter</primary>
      </indexterm>
      <listitem>
       <para>
        指定是在指定的恢复目标之后停止（<literal>true</literal>），还是在恢复目标之前停止（<literal>false</literal>）。适用于 <xref linkend="recovery-target-time"> 和 <xref linkend="recovery-target-xid"> 中为此恢复指定的那一个。它分别表示提交时间或 ID 恰好等于目标值的事务是否会被包含在恢复中。默认值是 <literal>true</literal>。
       </para>
      </listitem>
     </varlistentry>

     <varlistentry id="recovery-target-timeline"
                   xreflabel="recovery_target_timeline">
      <term><varname>recovery_target_timeline</varname>
        (<type>string</type>)
      </term>
      <indexterm>
        <primary><varname>recovery_target_timeline</> recovery parameter</primary>
      </indexterm>
      <listitem>
       <para>
        指定恢复到某条特定时间线。默认沿着制作基础备份时的当前时间线恢复。只有在复杂的再次恢复场景中才需要设置此参数：需要返回的状态本身就是一次时间点恢复之后达到的状态。相关讨论参见 <xref linkend="backup-timelines">。
       </para>
      </listitem>
     </varlistentry>

     </variablelist>
   </sect1>

  <sect1 id="standby-settings">

    <title>备库设置</title>
     <variablelist>

       <varlistentry id="standby-mode" xreflabel="standby_mode">
        <term><varname>standby_mode</varname> (<type>boolean</type>)</term>
        <indexterm>
          <primary><varname>standby_mode</> recovery parameter</primary>
        </indexterm>
        <listitem>
         <para>指定是否将 <productname>PostgreSQL</productname> 服务器作为备库启动。如果此参数为 <literal>on</literal>，服务器到达已归档 WAL 的末尾时不会停止恢复，而会继续尝试使用 <varname>restore_command</varname> 获取新的 WAL 段，和/或按照 <varname>primary_conninfo</varname> 设置连接主库，以继续恢复。</para>
        </listitem>
       </varlistentry>
       <varlistentry id="primary-conninfo" xreflabel="primary_conninfo">
        <term><varname>primary_conninfo</varname> (<type>string</type>)</term>
        <indexterm>
          <primary><varname>primary_conninfo</> recovery parameter</primary>
        </indexterm>
        <listitem>
         <para>
          指定备库用来连接主库的连接串。该串采用 libpq 的 <function>PQconnectdb</function> 函数所接受的格式，见 <xref linkend="libpq-connect">。如果串中未指定某个选项，则会检查相应的环境变量（见 <xref linkend="libpq-envars">）。如果环境变量也未设置，则使用默认值。
         </para>
         <para>
          连接串应当指定主服务器的主机名（或地址），如果端口与备库的默认端口不同，还应指定端口号。还要指定一个用户名，它对应主库上一个具有 <literal>SUPERUSER</literal> 和 <literal>LOGIN</literal> 权限的角色（见 <xref linkend="streaming-replication-authentication">）。如果主库要求密码认证，还需要提供密码。密码可以放在 <varname>primary_conninfo</varname> 串中，也可以放在备库服务器上一个单独的 <filename>~/.pgpass</filename> 文件中（数据库名使用 <literal>replication</literal>）。不要在 <varname>primary_conninfo</varname> 串中指定数据库名。
         </para>
         <para>如果 <varname>standby_mode</varname> 为 <literal>off</literal>，此设置不起作用。</para>
        </listitem>
       </varlistentry>
       <varlistentry id="trigger-file" xreflabel="trigger_file">
        <term><varname>trigger_file</varname> (<type>string</type>)</term>
        <indexterm>
          <primary><varname>trigger_file</> recovery parameter</primary>
        </indexterm>
        <listitem>
         <para>
          指定一个触发文件，其出现会使备库结束恢复。如果没有指定触发文件，备库永远不会退出恢复。如果 <varname>standby_mode</varname> 为 <literal>off</literal>，此设置不起作用。
         </para>
        </listitem>
       </varlistentry>

     </variablelist>
   </sect1>

</chapter>
