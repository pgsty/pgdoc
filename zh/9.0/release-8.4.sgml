<!-- doc/src/sgml/release-8.4.sgml -->
<!-- See header comment in release.sgml about typical markup -->

 <sect1 id="release-8-4-22">
  <title>版本 8.4.22</title>

  <note>
  <title>发行日期</title>
  <simpara>2014-07-24</simpara>
 </note>

  <para>
   本次发布包含对 8.4.21 的多方面修复。有关 8.4 大版本中新特性的信息，请参见<xref linkend="release-8-4">。.
  </para>

  <para>
      这预计是 <productname>PostgreSQL</productname> 8.4.X 系列的最后一个发行版。建议用户尽快更新到较新的发行分支。
     </para>

  <sect2>
   <title>迁移到版本 8.4.22</title>

   <para>
    对于运行 8.4.X 的用户，不需要进行转储/恢复。</para>

   <para>
      不过，本版本纠正了某些 GiST 索引的索引损坏问题。请参见下面第一条变更日志，以确认你的安装是否受影响以及应采取的步骤。
     </para>

   <para>
    不过，如果你是从earlier than 8.4.19 的版本升级，请参见<xref linkend="release-8-4-19">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      正确初始化 <filename>contrib/btree_gist</filename> 在 <type>bit</type> 列上索引的填充字节（Heikki Linnakangas）
     </para>

     <para>
      这一错误可能导致本应比较相等的值不被视为相等，从而产生错误的查询结果。在 <type>bit</type> 或 <type>bit varying</type> 列上建有 GiST 索引的用户，在安装此更新后应对这些索引执行 <command>REINDEX</command>。
     </para>
    </listitem>

    <listitem>
     <para>
      在删除 GIN 列表页时防止残缺页（torn page）（Heikki Linnakangas）
     </para>

     <para>
      此修复可防止在页面更新写入磁盘期间发生系统崩溃时可能出现的索引损坏。
     </para>
    </listitem>

    <listitem>
     <para>
      修复嵌套调用 <function>ReceiveSharedInvalidMessages</function> 期间缓存失效可能不正确的问题（Andres Freund）
     </para>
    </listitem>

    <listitem>
     <para>
      当子查询的目标列表中存在集合返回函数时，不再假定其输出是唯一的（David Rowley）
     </para>

     <para>
      这一疏忽可能导致诸如 <literal>WHERE x IN (SELECT y, generate_series(1,10) FROM t GROUP BY y)</literal> 之类的构造被错误优化。
     </para>
    </listitem>

    <listitem>
     <para>
      修复未能对结构化类型的组合元素中的字段执行解 TOAST 的错误（Tom Lane）
     </para>

     <para>
      这纠正了 TOAST 指针可能未经解引用就被复制到其他表的情况。如果原始数据随后被删除，当这个已悬空的指针被使用时，会导致诸如 <quote>missing chunk number 0 for toast value ...</quote> 之类的错误。
     </para>
    </listitem>

    <listitem>
     <para>
      修复对 Append 计划节点输出的整行引用导致 <quote>record type has not been registered</quote> 失败的问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复在回卷游标时调用用户定义函数可能崩溃的问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复为 <literal>FROM</literal> 中的函数求值参数时查询级内存泄漏的问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复正则表达式处理中的会话级内存泄漏（Tom Lane、Arthur O'Dwyer、Greg Stark）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>hungarian.stop</filename> 中的数据编码错误（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复对在当前事务中插入、随后被一个现已回滚的子事务删除的行的活跃性检查（Andres Freund）
     </para>

     <para>
      如果同一事务中稍后执行了 <command>CREATE INDEX</command> 或 <command>CLUSTER</command>，这可能导致问题（至少是虚假警告，最坏是无限循环）。
     </para>
    </listitem>

    <listitem>
     <para>
      在 <command>PREPARE TRANSACTION</command> 期间清除 <structname>pg_stat_activity</structname>.<structfield>xact_start</structfield>（Andres Freund）
     </para>

     <para>
      在 <command>PREPARE</command> 之后，发起会话已不在事务中，因此不应继续显示事务开始时间。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <command>REASSIGN OWNED</command> 对文本搜索对象失败的问题（&Aacute;lvaro Herrera）
     </para>
    </listitem>

    <listitem>
     <para>
      在 postmaster 启动期间阻塞信号（Tom Lane）
     </para>

     <para>
      这确保了 postmaster 在例如启动过程中收到 <systemitem>SIGINT</systemitem> 时能够正确清理自身。
     </para>
    </listitem>

    <listitem>
     <para>
      保护 <literal>make check</literal> 期间启动的临时 postmaster 的 Unix 域套接字（Noah Misch）
     </para>

     <para>
      正如我们此前在 CVE-2014-0067 中指出的，任何能够访问套接字文件的本地用户都可以以服务器的引导超级用户身份连接，进而以运行测试的操作系统用户身份执行任意代码。此变更通过把服务器的套接字放到 <filename>/tmp</filename> 下一个临时的、模式为 0700 的子目录中来防御这一风险。但在不支持 Unix 套接字的平台（尤其是 Windows）上风险仍然存在，因为那时临时 postmaster 必须接受本地 TCP 连接。
     </para>

     <para>
      此变更的一个有用副作用是简化了覆盖 <literal>DEFAULT_PGSOCKET_DIR</literal> 的构建中的 <literal>make check</literal> 测试。常用的非默认值（如 <filename>/var/run/postgresql</filename>）往往不允许构建用户写入，此前需要一些变通办法，现在不再需要了。
     </para>
    </listitem>

    <listitem>
     <para>
      在 Windows 上，允许新会话从配置文件获取 PGC_BACKEND 参数（例如<xref linkend="guc-log-connections">）的值（Amit Kapila）
     </para>

     <para>
      此前，如果启动后在文件中更改了这类参数，更改不会生效。
     </para>
    </listitem>

    <listitem>
     <para>
      在 Windows 上正确地为可执行文件路径名加引号（Nikhil Deshpande）
     </para>

     <para>
      如果安装路径同时包含空格和 <literal>@</literal> 符号，这一疏忽可能导致 <application>initdb</application> 和 <application>pg_upgrade</application> 在 Windows 上失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 OS X 上 <application>libpython</application> 的链接（Tom Lane）
     </para>

     <para>
      我们先前使用的方法在 Xcode 5.0 及之后版本提供的 Python 库上可能失败。
     </para>
    </listitem>

    <listitem>
     <para>
      当服务器持续以快于客户端吸收速度的速率发送数据时，避免 <application>libpq</application> 中的缓冲区膨胀（Shin-ichi Morita、Tom Lane）
     </para>

     <para>
      <application>libpq</application> 可能被迫不断增大其输入缓冲区，直到内存耗尽（这会被误导性地报告为 <quote>lost synchronization with server</quote>）。在正常情况下，数据持续以快于 <function>recv()</function> 循环吸收速度的速率传输是相当牵强的，但在客户端因调度器约束被人为减速时曾观察到这种情况。
     </para>
    </listitem>

    <listitem>
     <para>
      确保 <application>libpq</application> 中的 LDAP 查找尝试按预期超时（Laurenz Albe）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_restore</application> 对旧式大对象注释的处理（Tom Lane）
     </para>

     <para>
      如果归档文件由 9.0 之前版本的 <application>pg_dump</application> 生成且包含较多大对象注释，直接恢复到数据库通常会失败。
     </para>
    </listitem>

    <listitem>
     <para>
      在 <filename>contrib/pgcrypto</filename> 函数中，确保返回前清除栈变量中的敏感信息（Marko Kreen）
     </para>
    </listitem>

    <listitem>
     <para>
      在 <filename>contrib/uuid-ossp</filename> 中，跨调用缓存 OSSP UUID 库的状态（Tom Lane）
     </para>

     <para>
      这提高了 UUID 生成的效率，并在有 <filename>/dev/urandom</filename> 的平台上减少了从中抽取的熵量。
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2014e
      for DST law changes in Crimea, Egypt, and Morocco。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-4-21">
  <title>版本 8.4.21</title>

  <note>
  <title>发行日期</title>
  <simpara>2014-03-20</simpara>
 </note>

  <para>
   本次发布包含对 8.4.20 的多方面修复。有关 8.4 大版本中新特性的信息，请参见<xref linkend="release-8-4">。.
  </para>

  <para>
      <productname>PostgreSQL</productname> 社区将于 2014 年 7 月停止对 8.4.X 版本系列的更新。建议用户尽快更新到较新的版本分支。
     </para>

  <sect2>
   <title>迁移到版本 8.4.21</title>

   <para>
    对于运行 8.4.X 的用户，不需要进行转储/恢复。</para>

   <para>
    不过，如果你是从earlier than 8.4.19 的版本升级，请参见<xref linkend="release-8-4-19">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      无条件恢复 GIN 元页以避免残缺页风险（Heikki Linnakangas）
     </para>

     <para>
      虽然这一疏忽理论上可能导致索引损坏，但实际上不太可能造成任何问题，因为 GIN 元页的活动部分小于标准的 512 字节磁盘扇区。
     </para>
    </listitem>

    <listitem>
     <para>
      允许正则表达式操作符被查询取消请求提前终止（Tom Lane）
     </para>

     <para>
      这防止了病态正则表达式长时间不可中断地锁死服务器进程的场景。
     </para>
    </listitem>

    <listitem>
     <para>
      移除试图允许 <literal>OVERLAPS</literal> 接受单元素行参数的错误代码（Joshua Yanovski）
     </para>

     <para>
      这段代码从未正确工作过，而且该情况既未被 SQL 标准规定也未被记录，因此移除它似乎比修复它更好。
     </para>
    </listitem>

    <listitem>
     <para>
      避免在反解析规则或视图时获取超出 <literal>AccessShareLock</literal> 的锁（Dean Rasheed）
     </para>

     <para>
      这一疏忽导致 <application>pg_dump</application> 对规则中作为 <literal>INSERT</literal>/<literal>UPDATE</literal>/<literal>DELETE</literal> 命令目标的表意外获取了 <literal>RowExclusiveLock</literal> 锁。虽然通常无害，但它可能干扰试图在这些表上获取例如 <literal>ShareLock</literal> 的并发事务。
     </para>
    </listitem>

    <listitem>
     <para>
      在报告非 <literal>ERROR</literal> 消息期间防止中断（Tom Lane）
     </para>

     <para>
      这可以防止因递归进入 <function>syslog()</function> 而导致的罕见服务器进程冻结，或许还能防止其他相关问题。
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2014a
      for DST law changes in Fiji and Turkey, plus historical changes in
      Israel and Ukraine。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-4-20">
  <title>版本 8.4.20</title>

  <note>
  <title>发行日期</title>
  <simpara>2014-02-20</simpara>
 </note>

  <para>
   本次发布包含对 8.4.19 的多方面修复。有关 8.4 大版本中新特性的信息，请参见<xref linkend="release-8-4">。.
  </para>

  <para>
      <productname>PostgreSQL</productname> 社区将于 2014 年 7 月停止对 8.4.X 版本系列的更新。建议用户尽快更新到较新的版本分支。
     </para>

  <sect2>
   <title>迁移到版本 8.4.20</title>

   <para>
    对于运行 8.4.X 的用户，不需要进行转储/恢复。</para>

   <para>
    不过，如果你是从earlier than 8.4.19 的版本升级，请参见<xref linkend="release-8-4-19">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      加固 <literal>GRANT ... WITH ADMIN OPTION</literal> 的限制（Noah Misch）
     </para>

     <para>
      不带 <literal>ADMIN OPTION</literal> 授予角色本应阻止被授予者向该角色添加或移除成员，但这一限制很容易通过先执行 <literal>SET ROLE</literal> 绕过。其安全影响主要在于角色成员可以撤销其他人的访问权限，这违背了授权者的意愿。未经批准添加角色成员的影响相对较小，因为不配合的角色成员本来就可以通过创建视图或 <literal>SECURITY DEFINER</literal> 函数把自己的大部分权限提供给他人。（CVE-2014-0060）
     </para>
    </listitem>

    <listitem>
     <para>
      防止通过手工调用 PL 验证器函数进行权限提升（Andres Freund）
     </para>

     <para>
      PL 验证器函数的主要角色是在 <command>CREATE FUNCTION</command> 期间被隐式调用，但它们也是普通 SQL 函数，用户可以显式调用。对实际以其他语言编写的函数调用验证器此前未被检查，可能被用于权限提升。修复方法是在每个验证器函数中加入对权限检查函数的调用。非核心过程语言如果自带验证器函数，也需要做同样的修改。（CVE-2014-0061）
     </para>
    </listitem>

    <listitem>
     <para>
      避免在表和索引 DDL 期间进行多次名称查找（Robert Haas、Andres Freund）
     </para>

     <para>
      如果由于并发活动导致名称查找得出不同结论，我们可能会对不同的表执行 DDL 的不同部分。至少在 <command>CREATE INDEX</command> 的情况下，这可被用来让权限检查针对与索引创建不同的表执行，从而造成权限提升攻击。（CVE-2014-0062）
     </para>
    </listitem>

    <listitem>
     <para>
      防止长日期时间字符串造成缓冲区越界（Noah Misch）
     </para>

     <para>
      <literal>MAXDATELEN</literal> 常量对于 <type>interval</type> 类型可能的最长值来说太小，导致 <function>interval_out()</function> 中可能出现缓冲区越界。虽然日期时间输入函数在避免缓冲区越界方面更谨慎，但该限制仍短到导致它们拒绝某些有效输入，例如包含很长时区名的输入。<application>ecpg</application> 库存在这些漏洞以及它自身的一些漏洞。（CVE-2014-0063）
     </para>
    </listitem>

    <listitem>
     <para>
      防止大小计算中的整数溢出导致缓冲区越界（Noah Misch、Heikki Linnakangas）
     </para>

     <para>
      若干函数（大多是类型输入函数）在计算分配大小时未检查溢出。如果发生溢出，就会分配过小的缓冲区并被写越界。（CVE-2014-0064）
     </para>
    </listitem>

    <listitem>
     <para>
      防止固定大小缓冲区越界（Peter Eisentraut、Jozef Mlich）
     </para>

     <para>
      使用 <function>strlcpy()</function> 及相关函数，以明确保证固定大小缓冲区不会被越界写入。与前面几项不同，这些情况是否真的构成现实问题尚不清楚，因为在大多数情况下，输入字符串的大小似乎已有先前约束。尽管如此，消除所有此类 Coverity 警告似乎是明智的。（CVE-2014-0065）
     </para>
    </listitem>

    <listitem>
     <para>
      避免在 <function>crypt()</function> 返回 NULL 时崩溃（Honza Horak、Bruce Momjian）
     </para>

     <para>
      <function>crypt()</function> 可能返回 NULL 的场景相对较少，但如果发生，<filename>contrib/chkpass</filename> 就会崩溃。一个可能出问题的实际情况是 <application>libc</application> 被配置为拒绝执行未获批准的哈希算法（例如 <quote>FIPS 模式</quote>）。（CVE-2014-0066）
     </para>
    </listitem>

    <listitem>
     <para>
      在回归测试说明中记录 <literal>make check</literal> 的风险（Noah Misch、Tom Lane）
     </para>

     <para>
      由于 <literal>make check</literal> 启动的临时服务器使用 <quote>trust</quote> 认证，同一台机器上的其他用户可以以数据库超级用户身份连接它，进而可能利用启动测试的操作系统用户的权限。未来版本可能会在测试流程中加入防止此风险的变更，但需要先进行一些公开讨论。因此目前只是警告：当同一台机器上存在不受信任的用户时，不要使用 <literal>make check</literal>。（CVE-2014-0067）
     </para>
    </listitem>

    <listitem>
     <para>
      修复当关系的某些段不是完整大小时 WAL 记录可能被错误重放的问题（Greg Stark、Tom Lane）
     </para>

     <para>
      WAL 更新可能被应用到错误的页面，甚至可能偏后很多页。除了损坏数据外，已观察到这一错误导致备库相比主库显著<quote>膨胀</quote>，因为更新被应用到了远超文件末尾应在位置的地方。这种失败模式在崩溃恢复期间似乎不构成重大风险，只在初始同步从快速变化的主库基础备份创建的备库时出现。
     </para>
    </listitem>

    <listitem>
     <para>
      确保对 GIN 索引非叶页面的插入在合适时写入整页 WAL 记录（Heikki Linnakangas）
     </para>

     <para>
      先前的代码在系统崩溃期间发生部分页写入时存在索引损坏风险。
     </para>
    </listitem>

    <listitem>
     <para>
      修复服务器进程退出期间的竞态条件（Robert Haas）
     </para>

     <para>
      确保信号处理程序不会在进程的 <varname>MyProc</varname> 指针不再有效后尝试使用它。
     </para>
    </listitem>

    <listitem>
     <para>
      修复错误报告逻辑中对 <varname>errno</varname> 的不安全引用（Christian Kruse）
     </para>

     <para>
      这通常会导致奇怪的行为，例如 <literal>HINT</literal> 字段缺失或不恰当。
     </para>
    </listitem>

    <listitem>
     <para>
      修复服务器启动期间过早使用 <function>ereport()</function> 可能导致的崩溃（Tom Lane）
     </para>

     <para>
      我们在实际中见到的主要情况是：如果服务器在一个它没有读取权限的目录中被启动，就会崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      在 OpenSSL 套接字写函数中正确清除重试标志（Alexander Kukushkin）
     </para>

     <para>
      这一遗漏可能导致 SSL 加密连接意外断开后服务器锁死。
     </para>
    </listitem>

    <listitem>
     <para>
      修复对包含转义的 Unicode 标识符（<literal>U&amp;"..."</literal> 语法）的长度检查（Tom Lane）
     </para>

     <para>
      如果标识符的转义形式过长，就会为这类标识符打印虚假的截断警告，但标识符在反转义之后实际上并不需要截断。
     </para>
    </listitem>

    <listitem>
     <para>
      修复嵌套子选择（例如 <literal>WHERE (... x IN (SELECT ...) ...) IN (SELECT ...)</literal>）因无效计划而可能崩溃的问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      确保即使表列中的所有值都<quote>过宽</quote>，<command>ANALYZE</command> 也会为该列创建统计信息（Tom Lane）
     </para>

     <para>
      <command>ANALYZE</command> 有意在直方图和最常用值计算中省略非常宽的值，但在所有采样条目都过宽的情况下没有做出合理的处理。
     </para>
    </listitem>

    <listitem>
     <para>
      在 <literal>ALTER TABLE ... SET TABLESPACE</literal> 中，允许不经权限检查就使用数据库的默认表空间（Stephen Frost）
     </para>

     <para>
      <literal>CREATE TABLE</literal> 一直允许这种用法，但 <literal>ALTER TABLE</literal> 没有照做。
     </para>
    </listitem>

    <listitem>
     <para>
      修复当 <literal>CASE</literal> 的某些分支返回集合而另一些不返回时的 <quote>cannot accept a set</quote> 错误（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 pgstat 函数中对全零客户端地址的检查（Kevin Grittner）
     </para>
    </listitem>

    <listitem>
     <para>
      修复文本搜索解析器可能错误分类多字节字符的问题（Tom Lane）
     </para>

     <para>
      在 C 区域设置下使用多字节编码时，非 ASCII 字符可能被错误分类。在 Cygwin 上，非 C 区域设置也可能失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>plainto_tsquery()</function> 中可能的不当行为（Heikki Linnakangas）
     </para>

     <para>
      在复制重叠内存区域时使用 <function>memmove()</function> 而不是 <function>memcpy()</function>。尚无实际造成问题的现场报告，但这无疑是有风险的。
     </para>
    </listitem>

    <listitem>
     <para>
      在区域检查方面接受 <literal>SHIFT_JIS</literal> 作为编码名（Tatsuo Ishii）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>PQhost()</function> 在 Windows 上的不当行为（Fujii Masao）
     </para>

     <para>
      如果未指定主机，它应返回 <literal>localhost</literal>。
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <application>libpq</application> 和 <application>psql</application> 对 <literal>COPY TO STDOUT/FROM STDIN</literal> 期间失败的错误处理（Tom Lane）
     </para>

     <para>
      这尤其修复了 9.2 及更高版本中，如果服务器连接在 <literal>COPY FROM STDIN</literal> 期间丢失可能发生的无限循环。该场景的变体在旧版本或其他客户端应用中也可能出现。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>ecpg</application> 中未对齐的描述符（MauMau）
     </para>
    </listitem>

    <listitem>
     <para>
      在 <application>ecpg</application> 中正确处理连接参数中缺少主机名的情况（Michael Meskes）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/dblink</filename> 连接启动中的性能退化（Joe Conway）
     </para>

     <para>
      在客户端和服务器编码一致时避免一次不必要的往返。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/isn</filename> 中 ISMN 值校验位计算错误的问题（Fabien Coelho）
     </para>
    </listitem>

    <listitem>
     <para>
      确保仅安装客户端代码的流程按文档说明工作（Peter Eisentraut）
     </para>
    </listitem>

    <listitem>
     <para>
      在 Mingw 和 Cygwin 构建中，把 <application>libpq</application> DLL 安装到 <filename>bin</filename> 目录（Andrew Dunstan）
     </para>

     <para>
      这与 MSVC 构建长期以来的做法一致。它应该能解决 <application>psql</application> 等程序因找不到 DLL 而无法启动的问题。
     </para>
    </listitem>

    <listitem>
     <para>
      不再生成纯文本的 <filename>HISTORY</filename> 和 <filename>src/test/regress/README</filename> 文件（Tom Lane）
     </para>

     <para>
      这些文本文件与主文档的 HTML 和 PDF 格式重复。维护它们的麻烦远远超过纯文本格式可能的读者。发行版压缩包仍会包含这些名字的文件，但它们只是提示读者查阅主文档的存根。纯文本 <filename>INSTALL</filename> 文件仍将维护，因为它可以说还有使用场景。
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2013i（约旦的夏令时法规变更以及古巴的历史变更）。
     </para>

     <para>
      此外，时区 <literal>Asia/Riyadh87</literal>、<literal>Asia/Riyadh88</literal> 和 <literal>Asia/Riyadh89</literal> 已被移除，因为 IANA 不再维护它们，而且它们从未代表过实际的民用计时实践。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-4-19">
  <title>版本 8.4.19</title>

  <note>
  <title>发行日期</title>
  <simpara>2013-12-05</simpara>
 </note>

  <para>
   本次发布包含对 8.4.18 的多方面修复。有关 8.4 大版本中新特性的信息，请参见<xref linkend="release-8-4">。.
  </para>

  <sect2>
   <title>迁移到版本 8.4.19</title>

   <para>
    对于运行 8.4.X 的用户，不需要进行转储/恢复。</para>

   <para>
      不过，此发行版纠正了一个潜在的数据损坏问题。请参阅下面第一条变更记录，以确定你的安装是否受到影响以及如果受到影响可以采取什么措施。
     </para>

   <para>
    不过，如果你是从earlier than 8.4.17 的版本升级，请参见<xref linkend="release-8-4-17">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      修复 <command>VACUUM</command> 判断是否可以更新 <structfield>relfrozenxid</structfield> 的测试（Andres Freund）
     </para>

     <para>
      某些情况下，<command>VACUUM</command>（手工或自动）可能错误地推进表的 <structfield>relfrozenxid</structfield> 值，使元组逃过冻结，导致这些行在经过 2^31 个事务后变得不可见。由于需要多次错误推进才会发生实际丢失，数据丢失的概率相当低，但并非为零。从 8.4.8 或更早版本升级的用户不受影响，但之后的所有版本都包含该缺陷。
     </para>

     <para>
      升级后，在把 <link linkend="guc-vacuum-freeze-table-age"><varname>vacuum_freeze_table_age</varname></link> 设为零的情况下对所有数据库中的所有表执行清理，可以缓解此问题。这会修复任何潜伏的损坏，但无法修复所有既有的数据错误。不过，如果安装在其生命周期内执行的事务少于 2^31 个更新（可用 <literal>SELECT txid_current() < 2^31</> 检查），则在执行此清理之后即可认为该安装是安全的。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 GIN 索引 posting 树页面删除中的竞态条件（Heikki Linnakangas）
     </para>

     <para>
      这可能导致瞬时的错误结果或查询失败。
     </para>
    </listitem>

    <listitem>
     <para>
      避免展平 <literal>SELECT</literal> 列表中包含包裹在子 <literal>SELECT</literal> 中的易变函数的子查询（Tom Lane）
     </para>

     <para>
      这避免了因易变函数被额外求值而导致的意外结果。
     </para>
    </listitem>

    <listitem>
     <para>
      修复规划器对外连接中嵌套的非简单变量子查询输出的处理（Tom Lane）
     </para>

     <para>
      这一错误可能为涉及 <literal>JOIN</literal> 语法中多级子查询的查询生成错误的计划。
     </para>
    </listitem>

    <listitem>
     <para>
      修复临时文件被过早删除的问题（Andres Freund）
     </para>
    </listitem>

    <listitem>
     <para>
      修复规则打印中可能读取越过内存末尾的问题（Peter Eisentraut）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <type>int2vector</type> 和 <type>oidvector</type> 值的数组切片（Tom Lane）
     </para>

     <para>
      这类表达式现在会被隐式提升为常规的 <type>int2</type> 或 <type>oid</type> 数组。
     </para>
    </listitem>

    <listitem>
     <para>
      修复使用 SQL 标准的简单 GMT 偏移时区时的错误行为（Tom Lane）
     </para>

     <para>
      某些情况下，系统会使用简单 GMT 偏移值，而本应使用选择简单偏移之前一直生效的常规时区设置。此变更还使 <function>timeofday</function> 函数遵循简单 GMT 偏移时区。
     </para>
    </listitem>

    <listitem>
     <para>
      防止记录 Windows 错误码翻译时可能出现的不当行为（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      正确地为 <application>pg_ctl</application> 生成的命令行加引号（Naoya Anzai、Tom Lane）
     </para>

     <para>
      此修复仅适用于 Windows。
     </para>
    </listitem>

    <listitem>
     <para>
      修复当源数据库通过 <command>ALTER DATABASE SET</command> 设置 <link linkend="guc-default-transaction-read-only"><varname>default_transaction_read_only</varname></link> 时 <application>pg_dumpall</application> 无法工作的问题（Kevin Grittner）
     </para>

     <para>
      此前，生成的脚本在恢复期间会失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>ecpg</application> 对声明为 <type>varchar</type> 的变量列表的处理（Zolt&aacute;n B&ouml;sz&ouml;rm&eacute;nyi）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <filename>contrib/lo</filename> 防御不正确的触发器定义（Marc Cousin）
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2013h
      for DST law changes in Argentina, Brazil, Jordan, Libya,
      Liechtenstein, Morocco, and Palestine。Also, new timezone
      abbreviations WIB, WIT, WITA for Indonesia.
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-4-18">
  <title>版本 8.4.18</title>

  <note>
  <title>发行日期</title>
  <simpara>2013-10-10</simpara>
 </note>

  <para>
   本次发布包含对 8.4.17 的多方面修复。有关 8.4 大版本中新特性的信息，请参见<xref linkend="release-8-4">。.
  </para>

  <sect2>
   <title>迁移到版本 8.4.18</title>

   <para>
    对于运行 8.4.X 的用户，不需要进行转储/恢复。</para>

   <para>
    不过，如果你是从earlier than 8.4.17 的版本升级，请参见<xref linkend="release-8-4-17">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      防止尝试对标识符做大小写折叠时损坏多字节字符（Andrew Dunstan）
     </para>

     <para>
      <productname>PostgreSQL</productname> 只在使用单字节服务器编码时才对非 ASCII 字符做大小写折叠。
     </para>
    </listitem>

    <listitem>
     <para>
      修复由 <function>lo_open()</function> 失败导致的内存泄漏（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <varname>work_mem</varname> 使用超过 24GB 内存时的内存过度提交缺陷（Stephen Frost）
     </para>
    </listitem>

    <listitem>
     <para>
      修复使用 SSL 时 libpq 中的死锁缺陷（Stephen Frost）
     </para>
    </listitem>

    <listitem>
     <para>
      正确计算包含大量 NULL 值的布尔列的行数估计（Andrew Gierth）
     </para>

     <para>
      此前，<literal>col IS NOT TRUE</literal> 和 <literal>col IS NOT FALSE</literal> 这类测试在估计计划代价时没有正确考虑 NULL 值。
     </para>
    </listitem>

    <listitem>
     <para>
      防止把 <literal>WHERE</literal> 子句下推到不安全的 <literal>UNION/INTERSECT</literal> 子查询中（Tom Lane）
     </para>

     <para>
      <literal>SELECT</literal> 列表中包含集合返回函数或易变函数的 <literal>UNION</literal> 或 <literal>INTERSECT</literal> 子查询可能被不当优化，导致运行时错误或错误的查询结果。
     </para>
    </listitem>

    <listitem>
     <para>
      修复罕见的 <quote>failed to locate grouping columns</quote> 规划器失败情况（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      改进视图转储代码对被引用表中已删除列的处理（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复并发执行 <command>CREATE INDEX CONCURRENTLY</command> 操作时可能出现的死锁（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>regexp_matches()</function> 对零长度匹配的处理（Jeevan Chalke）
     </para>

     <para>
      此前，像 '^' 这样的零长度匹配可能返回过多的匹配。
     </para>
    </listitem>

    <listitem>
     <para>
      修复过于复杂的正则表达式导致崩溃的问题（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      修复反向引用与非贪婪量词组合时正则表达式匹配失败的问题（Jeevan Chalke）
     </para>
    </listitem>

    <listitem>
     <para>
      除非启用了函数体检查，否则防止 <command>CREATE FUNCTION</command> 检查 <command>SET</command> 变量（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>pgp_pub_decrypt()</function>，使其可用于带口令的密钥（Marko Kreen）
     </para>
    </listitem>

    <listitem>
     <para>
      移除无索引表清理期间罕见的不准确警告（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      避免在预备查询中执行事务控制命令（例如 <command>ROLLBACK</command>）时可能出现的失败（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      确保浮点数据输入在所有平台上都接受标准的<quote>infinity</quote>（无穷大）写法（Tom Lane）
     </para>

     <para>
      C99 标准规定允许的写法为 <literal>inf</literal>、<literal>+inf</literal>、<literal>-inf</literal>、<literal>infinity</literal>、<literal>+infinity</literal> 和 <literal>-infinity</literal>。即使平台的 <function>strtod</function> 函数不支持，也确保我们能识别这些写法。
     </para>
    </listitem>

    <listitem>
     <para>
      扩展行与记录和数组比较的能力（Rafal Rzepecki、Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2013d（以色列、摩洛哥、巴勒斯坦和巴拉圭的夏令时法规变更）。另外，还修正了麦格理岛的历史时区数据。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-4-17">
  <title>版本 8.4.17</title>

  <note>
  <title>发行日期</title>
  <simpara>2013-04-04</simpara>
 </note>

  <para>
   本次发布包含对 8.4.16 的多方面修复。有关 8.4 大版本中新特性的信息，请参见<xref linkend="release-8-4">。.
  </para>

  <sect2>
   <title>迁移到版本 8.4.17</title>

   <para>
    对于运行 8.4.X 的用户，不需要进行转储/恢复。</para>

   <para>
      不过，本版本纠正了 GiST 索引管理中的若干错误。安装此更新后，建议对满足下述一个或多个条件的 GiST 索引执行 <command>REINDEX</command>。
     </para>

   <para>
    不过，如果你是从earlier than 8.4.10 的版本升级，请参见<xref linkend="release-8-4-10">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      在每个 postmaster 子进程中重置 OpenSSL 随机状态（Marko Kreen）
     </para>

     <para>
      这避免了另一种情况：另一数据库用户可能相对容易地猜出 <filename>contrib/pgcrypto</filename> 函数生成的随机数。只有当 postmaster 配置了 <varname>ssl</varname> = <literal>on</literal> 而大多数连接并不使用 SSL 加密时，这一风险才比较显著。（CVE-2013-1900）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 GiST 索引在不合适的情况下使用<quote>模糊</quote>几何比较的问题（Alexander Korotkov）
     </para>

     <para>
      核心几何类型使用<quote>模糊</quote>相等进行比较，但 <function>gist_box_same</function> 必须做精确比较，否则使用它的 GiST 索引可能变得不一致。安装此更新后，用户应对 <type>box</type>、<type>polygon</type>、<type>circle</type> 或 <type>point</type> 列上的所有 GiST 索引执行 <command>REINDEX</command>，因为它们都使用 <function>gist_box_same</function>。
     </para>
    </listitem>

    <listitem>
     <para>
      修复对可变宽度数据类型（即 <type>text</type>、<type>bytea</type>、<type>bit</type> 和 <type>numeric</type> 列）使用 <filename>contrib/btree_gist</filename> 的 GiST 索引中错误的范围并集和惩罚逻辑（Tom Lane）
     </para>

     <para>
      这些错误可能导致不一致的索引：某些存在的键搜索不到，还会造成无用的索引膨胀。建议用户在安装此更新后对这类索引执行 <command>REINDEX</command>。
     </para>
    </listitem>

    <listitem>
     <para>
      修复多列索引的 GiST 页面分裂代码中的缺陷（Tom Lane）
     </para>

     <para>
      这些错误可能导致不一致的索引：某些存在的键搜索不到，还会使索引的搜索效率不必要地低下。建议用户在安装此更新后对多列 GiST 索引执行 <command>REINDEX</command>。
     </para>
    </listitem>

    <listitem>
     <para>
      修复正则表达式编译中的无限循环风险（Tom Lane、Don Porter）
     </para>
    </listitem>

    <listitem>
     <para>
      修复正则表达式编译中潜在的空指针解引用（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>to_char()</function>，在适当的地方只使用 ASCII 大小写折叠规则（Tom Lane）
     </para>

     <para>
      这修复了一些本应与区域设置无关的模板模式在土耳其区域下错误处理 <quote><literal>I</literal></quote> 和 <quote><literal>i</literal></quote> 的问题。
     </para>
    </listitem>

    <listitem>
     <para>
      修复时间戳 <literal>1999-12-31 24:00:00</literal> 被不当拒绝的问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      移除无用的 <quote>picksplit doesn't support secondary split</quote> 日志消息（Josh Hansen、Tom Lane）
     </para>

     <para>
      这条消息似乎是为预期中从未编写、而且可能永远不会编写的代码而添加的，因为 GiST 对二次分裂的默认处理其实相当好。所以不再为此打扰最终用户。
     </para>
    </listitem>

    <listitem>
     <para>
      修复会话最后几个事务的提交/中止计数可能未能发送到统计收集器的问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      消除 PL/Perl 的 <function>spi_prepare()</function> 函数中的内存泄漏（Alex Hunsaker、Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_dumpall</application> 对包含 <quote><literal>=</literal></quote> 的数据库名的处理（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      避免在给定不正确的连接串时 <application>pg_dump</application> 崩溃（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <application>pg_dump</application> 忽略无效索引（Michael Paquier）
     </para>

     <para>
      转储无效索引可能在恢复时引起问题，例如当索引创建失败的原因是它试图强制执行表中数据不满足的唯一性条件时。另外，如果索引创建实际上仍在进行中，把它视为未提交的 DDL 变更是合理的，<application>pg_dump</application> 本来就不会转储它。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/pg_trgm</filename> 的 <function>similarity()</function> 函数，使其对无三元组的字符串返回零（Tom Lane）
     </para>

     <para>
      此前它由于内部除以零而返回 <literal>NaN</literal>。
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2013b（智利、海地、摩洛哥、巴拉圭和一些俄罗斯地区的夏令时法规变更）。另外，还修正了众多地区的历史时区数据。
     </para>

     <para>
      此外，更新时区缩写文件以反映俄罗斯和其他地方的近期变更：<literal>CHOT</literal>、<literal>GET</literal>、<literal>IRKT</literal>、<literal>KGT</literal>、<literal>KRAT</literal>、<literal>MAGT</literal>、<literal>MAWT</literal>、<literal>MSK</literal>、<literal>NOVT</literal>、<literal>OMST</literal>、<literal>TKT</literal>、<literal>VLAT</literal>、<literal>WST</literal>、<literal>YAKT</literal>、<literal>YEKT</literal> 现在遵循其当前含义，同时把 <literal>VOLT</literal>（Europe/Volgograd）和 <literal>MIST</literal>（Antarctica/Macquarie）加入默认缩写列表。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-4-16">
  <title>版本 8.4.16</title>

  <note>
  <title>发行日期</title>
  <simpara>2013-02-07</simpara>
 </note>

  <para>
   本次发布包含对 8.4.15 的多方面修复。有关 8.4 大版本中新特性的信息，请参见<xref linkend="release-8-4">。.
  </para>

  <sect2>
   <title>迁移到版本 8.4.16</title>

   <para>
    对于运行 8.4.X 的用户，不需要进行转储/恢复。</para>

   <para>
    不过，如果你是从earlier than 8.4.10 的版本升级，请参见<xref linkend="release-8-4-10">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      防止从 SQL 执行 <function>enum_recv</function>（Tom Lane）
     </para>

     <para>
      该函数声明有误，允许一条简单的 SQL 命令使服务器崩溃。原则上攻击者可能利用它检查服务器内存的内容。感谢 Sumit Soni（通过 Secunia SVCRP）报告此问题。（CVE-2013-0255）
     </para>
    </listitem>

    <listitem>
     <para>
      在截断关系文件时更新最小恢复点（Heikki Linnakangas）
     </para>

     <para>
      数据一旦被丢弃，在时间线上更早的点停止恢复就不再安全。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 SQL 文法，允许对子 SELECT 结果做下标或字段选择（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      防止扫描 <structname>pg_tablespace</structname> 时的竞态条件（Stephen Frost、Tom Lane）
     </para>

     <para>
      如果 <structname>pg_tablespace</structname> 条目存在并发更新，<command>CREATE DATABASE</command> 和 <command>DROP DATABASE</command> 可能行为异常。
     </para>
    </listitem>

    <listitem>
     <para>
      防止 <command>DROP OWNED</command> 试图删除整个数据库或表空间（&Aacute;lvaro Herrera）
     </para>

     <para>
      为安全起见，这些对象的所有权必须重新分配，而不是删除。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <link linkend="guc-vacuum-freeze-table-age"><varname>vacuum_freeze_table_age</varname></link> 实现中的错误（Andres Freund）
     </para>

     <para>
      在已存在超过 <link linkend="guc-vacuum-freeze-min-age"><varname>vacuum_freeze_min_age</varname></link> 个事务的安装中，这一失误使自动清理无法使用部分表扫描，而总是进行全表扫描。
     </para>
    </listitem>

    <listitem>
     <para>
      防止 <symbol>RowExpr</symbol> 或 <symbol>XmlExpr</symbol> 被二次解析分析时的不当行为（Andres Freund、Tom Lane）
     </para>

     <para>
      这一失误在 <literal>CREATE TABLE LIKE INCLUDING INDEXES</literal> 等上下文中可能被用户看到。
     </para>
    </listitem>

    <listitem>
     <para>
      增强对哈希表大小计算中整数溢出的防御（Jeff Davis）
     </para>
    </listitem>

    <listitem>
     <para>
      拒绝 <function>to_date()</function> 中超出范围的日期（Hitoshi Harada）
     </para>
    </listitem>

    <listitem>
     <para>
      确保在 Windows 上非 ASCII 提示符字符串被转换到正确的代码页（Alexander Law、Noah Misch）
     </para>

     <para>
      这一缺陷影响 <application>psql</application> 和其他一些客户端程序。
     </para>
    </listitem>

    <listitem>
     <para>
      修复未连接数据库时 <application>psql</application> 的 <command>\?</command> 命令可能崩溃的问题（Meng Qingzhong）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>libpq</application> 的 <function>PQprintTuples</function> 中的一字节缓冲区越界（Xi Wang）
     </para>

     <para>
      这个古老的函数没有被 <productname>PostgreSQL</productname> 本身在任何地方使用，但仍可能被某些客户端代码使用。
     </para>
    </listitem>

    <listitem>
     <para>
      让 <application>ecpglib</application> 正确使用翻译后的消息（Chen Huajun）
     </para>
    </listitem>

    <listitem>
     <para>
      在 MSVC 上正确安装 <application>ecpg_compat</application> 和 <application>pgtypes</application> 库（Jiang Guiqing）
     </para>
    </listitem>

    <listitem>
     <para>
      重新排列 configure 对系统提供函数的测试，使其不被 libedit/libreadline 的虚假导出所欺骗（Christoph Berg）
     </para>
    </listitem>

    <listitem>
     <para>
      确保 Windows 构建号随时间递增（Magnus Hagander）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <application>pgxs</application> 在为 Windows 交叉编译时以正确的 <literal>.exe</literal> 后缀构建可执行文件（Zoltan Boszormenyi）
     </para>
    </listitem>

    <listitem>
     <para>
      新增时区缩写 <literal>FET</literal>（Tom Lane）
     </para>

     <para>
      它现在用于一些东欧时区。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-4-15">
  <title>版本 8.4.15</title>

  <note>
  <title>发行日期</title>
  <simpara>2012-12-06</simpara>
 </note>

  <para>
   本次发布包含对 8.4.14 的多方面修复。有关 8.4 大版本中新特性的信息，请参见<xref linkend="release-8-4">。.
  </para>

  <sect2>
   <title>迁移到版本 8.4.15</title>

   <para>
    对于运行 8.4.X 的用户，不需要进行转储/恢复。</para>

   <para>
    不过，如果你是从earlier than 8.4.10 的版本升级，请参见<xref linkend="release-8-4-10">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      修复与 <command>CREATE INDEX CONCURRENTLY</command> 相关的多个缺陷（Andres Freund、Tom Lane）
     </para>

     <para>
      修复 <command>CREATE INDEX CONCURRENTLY</command>，使其在更改索引 <structname>pg_index</structname> 行的状态时使用就地更新。这防止了可能导致并发会话错过更新目标索引（从而产生损坏的并发创建索引）的竞态条件。
     </para>

     <para>
      另外，修复各种其他操作，确保它们忽略由失败的 <command>CREATE INDEX CONCURRENTLY</command> 命令产生的无效索引。其中最重要的是 <command>VACUUM</command>，因为在采取纠正措施修复或删除无效索引之前，很容易就有自动清理在该表上启动。
     </para>
    </listitem>

    <listitem>
     <para>
      避免内存不足时内部哈希表损坏（Hitoshi Harada）
     </para>
    </listitem>

    <listitem>
     <para>
      修复外连接之上非严格等值子句的规划（Tom Lane）
     </para>

     <para>
      规划器可能从把非严格构造与别的值等同的子句中推导出错误的约束，例如当 <literal>foo</literal> 来自外连接的可空侧时的 <literal>WHERE COALESCE(foo, 0) = 0</literal>。
     </para>
    </listitem>

    <listitem>
     <para>
      改进规划器从等价类证明排他约束的能力（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复哈希子计划中的部分行匹配，使其正确处理跨类型情况（Tom Lane）
     </para>

     <para>
      这影响多列 <literal>NOT IN</literal> 子计划，例如 <literal>WHERE (a, b) NOT IN (SELECT x, y FROM ...)</literal>，当 <literal>b</literal> 和 <literal>y</literal> 分别是 <type>int4</type> 和 <type>int8</type> 时。根据所涉及的具体数据类型，这一失误会导致错误答案或崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      在为 <literal>AFTER ROW UPDATE/DELETE</literal> 触发器重新获取旧元组时获取缓冲区锁（Andres Freund）
     </para>

     <para>
      在非常罕见的情况下，这一疏忽可能导致向外键强制触发器的预检查逻辑传递不正确的数据。这可能导致崩溃，或者在是否触发触发器上做出错误决定。
     </para>
    </listitem>

    <listitem>
     <para>
      修复<command>ALTER COLUMN TYPE</command>以正确重建继承的检查约束（Tom Lane）
     </para>

     <para>
      这在 8.4 之前的版本中工作正常，现在在 8.4 及更高版本中也工作正常。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <command>REASSIGN OWNED</command> 对表空间授权的处理（&Aacute;lvaro Herrera）
     </para>
    </listitem>

    <listitem>
     <para>
      忽略视图中系统列的不正确 <structname>pg_attribute</structname> 条目（Tom Lane）
     </para>

     <para>
      视图没有任何系统列。然而，我们在把表转换为视图时忘记移除这类条目。这一问题在 9.3 及更高版本中已正确修复，但在以前的分支中，我们需要防御已经错误转换的视图。
     </para>
    </listitem>

    <listitem>
     <para>
      修复规则打印，使其正确转储 <literal>INSERT INTO <replaceable>table</replaceable> DEFAULT VALUES</literal>（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      防止当查询中有过多 <literal>UNION</literal>/<literal>INTERSECT</literal>/<literal>EXCEPT</literal> 子句时栈溢出（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      防止用最小可能整数值除以 -1 时依赖平台的失败（Xi Wang、Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复日期解析中可能越过字符串末尾的访问（Hitoshi Harada）
     </para>
    </listitem>

    <listitem>
     <para>
      当 Unix 域套接字的路径名长度超过平台特定限制时，生成可理解的错误消息（Tom Lane、Andrew Dunstan）
     </para>

     <para>
      此前这会导致相当无用的结果，例如 <quote>Non-recoverable failure in name resolution</quote>。
     </para>
    </listitem>

    <listitem>
     <para>
      修复向客户端发送组合列值时的内存泄漏（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      使 <application>pg_ctl</application> 读取 <filename>postmaster.pid</filename> 文件更健壮（Heikki Linnakangas）
     </para>

     <para>
      这修复了竞态条件和可能的文件描述符泄漏。
     </para>
    </listitem>

    <listitem>
     <para>
      修复当呈现错误编码的数据且 <varname>client_encoding</varname> 设置为仅客户端编码（如 SJIS）时 <application>psql</application> 可能崩溃的问题（Jiang Guiqing）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_dump</application> 以 <literal>tar</literal> 输出格式生成的 <filename>restore.sql</filename> 脚本中的缺陷（Tom Lane）
     </para>

     <para>
      该脚本在表名包含大写字符时会直接失败。此外，让该脚本既能以常规 COPY 模式也能以 <option>--inserts</option> 模式恢复数据。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_restore</application>，使其接受符合 POSIX 的 <literal>tar</literal> 文件（Brian Weaver、Tom Lane）
     </para>

     <para>
      <application>pg_dump</application> 的 <literal>tar</literal> 输出模式的原始编码生成的文件并不完全符合 POSIX 标准。这在 9.3 版本中已得到纠正。此补丁更新了以前的分支，使它们同时接受错误格式和纠正后的格式，希望借此避免 9.3 发布时的兼容性问题。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_resetxlog</application>，使其在给定数据目录相对路径时正确定位 <filename>postmaster.pid</filename>（Tom Lane）
     </para>

     <para>
      这一失误可能导致 <application>pg_resetxlog</application> 没有注意到有活动 postmaster 正在使用该数据目录。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>libpq</application> 的 <function>lo_import()</function> 和 <function>lo_export()</function> 函数，使其正确报告文件 I/O 错误（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>ecpg</application> 对嵌套结构指针变量的处理（Muhammad Usama）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <filename>contrib/pageinspect</filename> 的 btree 页面检查函数在检查页面时持有缓冲区锁（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pgxs</application> 在 AIX 上构建可加载模块的支持（Tom Lane）
     </para>

     <para>
      在原始源码树之外构建模块在 AIX 上不工作。
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2012j
      for DST law changes in Cuba, Israel, Jordan, Libya, Palestine, Western
      Samoa, and portions of Brazil。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-4-14">
  <title>版本 8.4.14</title>

  <note>
  <title>发行日期</title>
  <simpara>2012-09-24</simpara>
 </note>

  <para>
   本次发布包含对 8.4.13 的多方面修复。有关 8.4 大版本中新特性的信息，请参见<xref linkend="release-8-4">。.
  </para>

  <sect2>
   <title>迁移到版本 8.4.14</title>

   <para>
    对于运行 8.4.X 的用户，不需要进行转储/恢复。</para>

   <para>
    不过，如果你是从earlier than 8.4.10 的版本升级，请参见<xref linkend="release-8-4-10">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      修复规划器对执行器参数的分配，并修复执行器对 CTE 计划节点的重新扫描逻辑（Tom Lane）
     </para>

     <para>
      这些错误可能导致多次扫描同一 <literal>WITH</literal> 子查询的查询给出错误答案。
     </para>
    </listitem>

    <listitem>
     <para>
      改进 GiST 索引的页面分裂决策（Alexander Korotkov、Robert Haas、Tom Lane）
     </para>

     <para>
      多列 GiST 索引可能因此错误而遭受意外膨胀。
     </para>
    </listitem>

    <listitem>
     <para>
      修复级联权限撤销，使其在权限仍被持有时停止（Tom Lane）
     </para>

     <para>
      如果我们撤销某个角色 <replaceable>X</replaceable> 的授予选项，但 <replaceable>X</replaceable> 仍通过别人的授予持有该选项，我们就不应从 <replaceable>X</replaceable> 曾授予该权限的角色 <replaceable>Y</replaceable> 那里递归撤销相应权限。
     </para>
    </listitem>

    <listitem>
     <para>
      修复使用 PL/Perl 时对 <literal>SIGFPE</literal> 的处理（Andres Freund）
     </para>

     <para>
      Perl 会把进程的 <literal>SIGFPE</literal> 处理程序重置为 <literal>SIG_IGN</literal>，这可能导致之后崩溃。在初始化 PL/Perl 之后恢复正常的 Postgres 信号处理程序。
     </para>
    </listitem>

    <listitem>
     <para>
      防止递归 PL/Perl 函数在执行期间被重定义时 PL/Perl 崩溃（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      规避 PL/Perl 中可能的错误优化（Tom Lane）
     </para>

     <para>
      一些 Linux 发行版包含错误版本的 <filename>pthread.h</filename>，导致 PL/Perl 中的编译代码不正确，当一个 PL/Perl 函数调用另一个会抛出错误的函数时会导致崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2014e，以反映克里米亚、埃及和摩洛哥的夏令时法规变更。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-4-13">
  <title>版本 8.4.13</title>

  <note>
  <title>发行日期</title>
  <simpara>2012-08-17</simpara>
 </note>

  <para>
   本次发布包含对 8.4.12 的多方面修复。有关 8.4 大版本中新特性的信息，请参见<xref linkend="release-8-4">。.
  </para>

  <sect2>
   <title>迁移到版本 8.4.13</title>

   <para>
    对于运行 8.4.X 的用户，不需要进行转储/恢复。</para>

   <para>
    不过，如果你是从earlier than 8.4.10 的版本升级，请参见<xref linkend="release-8-4-10">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      防止通过 XML 实体引用访问外部文件/URL（Noah Misch、Tom Lane）
     </para>

     <para>
      <function>xml_parse()</function> 会尝试按需获取外部文件或 URL 来解析 XML 值中的 DTD 和实体引用，从而使无特权数据库用户能够尝试以数据库服务器的权限获取数据。虽然外部数据不会直接返回给用户，但如果数据不能解析为有效 XML，其部分内容可能暴露在错误消息中；而且无论如何，仅仅能够检查文件是否存在对攻击者就可能有用。（CVE-2012-3489）
     </para>
    </listitem>

    <listitem>
     <para>
      防止通过 <filename>contrib/xml2</filename> 的 <function>xslt_process()</function> 访问外部文件/URL（Peter Eisentraut）
     </para>

     <para>
      <application>libxslt</application> 提供通过样式表命令读写文件和 URL 的能力，从而使无特权数据库用户能够以数据库服务器的权限读写数据。现通过正确使用 <application>libxslt</application> 的安全选项禁用该能力。（CVE-2012-3488）
     </para>

     <para>
      此外，移除了 <function>xslt_process()</function> 从外部文件/URL 获取文档和样式表的能力。虽然这是一个有记载的<quote>特性</quote>，但长期以来被认为是个坏主意。CVE-2012-3489 的修复破坏了该能力，与其花费精力修复它，我们干脆将其移除。
     </para>
    </listitem>

    <listitem>
     <para>
      防止 btree 索引页被过早回收（Noah Misch）
     </para>

     <para>
      当我们允许只读事务跳过分配 XID 时，引入了已删除 btree 页在只读事务仍在访问它时就被回收的可能性。这会导致错误的索引搜索结果。由于时序要求，这种错误在实际中发生的概率似乎很低，但仍应修复。
     </para>
    </listitem>

    <listitem>
     <para>
      修复新创建或重置序列的崩溃安全缺陷（Tom Lane）
     </para>

     <para>
      如果在新创建或重置的序列上执行了 <command>ALTER SEQUENCE</command>，然后恰好对它调用了一次 <function>nextval()</function>，随后服务器崩溃，WAL 重放会把序列恢复到看起来没有执行过 <function>nextval()</function> 的状态，从而使下一个 <function>nextval()</function> 调用再次返回第一个序列值。这种情况尤其可能出现在 <type>serial</type> 列上，因为创建 serial 列的序列包含一个 <command>ALTER SEQUENCE OWNED BY</command> 步骤。
     </para>
    </listitem>

    <listitem>
     <para>
      确保 <function>pg_start_backup()</function> 之后对 <filename>backup_label</filename> 文件执行 fsync（Dave Kerr）
     </para>
    </listitem>

    <listitem>
     <para>
      回移植压缩 fsync 请求队列的 9.1 改进（Robert Haas）
     </para>

     <para>
      这改进了检查点期间的性能。9.1 的变更已经过足够的实际测试，看起来可以安全回移植。
     </para>
    </listitem>

    <listitem>
     <para>
      只允许被直接阻塞的进程自动取消自动清理（Tom Lane）
     </para>

     <para>
      原始编码在某些情况下可能允许不一致的行为；特别是自动清理可能在不足 <literal>deadlock_timeout</literal> 宽限期之后就被取消。
     </para>
    </listitem>

    <listitem>
     <para>
      改进自动清理取消的日志记录（Robert Haas）
     </para>
    </listitem>

    <listitem>
     <para>
      修复日志收集器，使 <literal>log_truncate_on_rotation</literal> 在服务器启动后的第一次日志轮转期间就能工作（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复附加到嵌套集合操作（<literal>UNION</literal>/<literal>INTERSECT</literal>/<literal>EXCEPT</literal>）上的 <literal>WITH</literal>（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      确保对子查询的整行引用不包含任何多余的 <literal>GROUP BY</literal> 或 <literal>ORDER BY</literal> 列（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      禁止在 <command>CREATE TABLE</command> 期间复制 <literal>CHECK</literal> 约束和索引定义中的整行引用（Tom Lane）
     </para>

     <para>
      这种情况可能出现在带 <literal>LIKE</literal> 或 <literal>INHERITS</literal> 的 <command>CREATE TABLE</command> 中。被复制的整行变量被错误地标记为原表而不是新表的行类型。对 <literal>LIKE</literal> 拒绝这种情况似乎是合理的，因为行类型之后很可能分化。对 <literal>INHERITS</literal>，理想情况下应该允许它，并隐式强制转换为父表的行类型；但这需要的工作量超出了回移植的安全范围。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>ARRAY(SELECT ...)</literal> 子查询中的内存泄漏（Heikki Linnakangas、Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复从正则表达式中提取公共前缀的问题（Tom Lane）
     </para>

     <para>
      代码可能被带量词的带括号子表达式（例如 <literal>^(foo)?bar</literal>）混淆。这会导致对此类模式搜索的索引优化不正确。
     </para>
    </listitem>

    <listitem>
     <para>
      修复解析 <type>interval</type> 常量中带符号的 <replaceable>hh</replaceable><literal>:</literal><replaceable>mm</replaceable> 和 <replaceable>hh</replaceable><literal>:</literal><replaceable>mm</replaceable><literal>:</literal><replaceable>ss</replaceable> 字段的缺陷（Amit Kapila、Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/xml2</filename> 的 <function>xslt_process()</function> 中的错误报告（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2012e，以反映摩洛哥和托克劳的夏令时法规变更
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-4-12">
  <title>版本 8.4.12</title>

  <note>
  <title>发行日期</title>
  <simpara>2012-06-04</simpara>
 </note>

  <para>
   本次发布包含对 8.4.11 的多方面修复。有关 8.4 大版本中新特性的信息，请参见<xref linkend="release-8-4">。.
  </para>

  <sect2>
   <title>迁移到版本 8.4.12</title>

   <para>
    对于运行 8.4.X 的用户，不需要进行转储/恢复。</para>

   <para>
    不过，如果你是从earlier than 8.4.10 的版本升级，请参见<xref linkend="release-8-4-10">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      修复 <filename>contrib/pgcrypto</filename> 的 DES <function>crypt()</function> 函数中不正确的口令变换（Solar Designer）
     </para>

     <para>
      如果口令串包含字节值 <literal>0x80</literal>，口令的其余部分会被忽略，使口令比看起来弱得多。修复后，字符串的其余部分会被正确纳入 DES 哈希。因此，受此缺陷影响的任何已存储口令值将不再匹配，所以可能需要更新已存储的值。（CVE-2012-2143）
     </para>
    </listitem>

    <listitem>
     <para>
      忽略过程语言调用处理程序的 <literal>SECURITY DEFINER</literal> 和 <literal>SET</literal> 属性（Tom Lane）
     </para>

     <para>
      把这类属性应用于调用处理程序可能导致服务器崩溃。（CVE-2012-2655）
     </para>
    </listitem>

    <listitem>
     <para>
      允许 <type>timestamp</type> 输入中的数字时区偏移距 UTC 最多 16 小时（Tom Lane）
     </para>

     <para>
      一些历史时区的偏移超过此前 15 小时的限制。这可能导致转储的数据值在重载期间被拒绝。
     </para>
    </listitem>

    <listitem>
     <para>
      修复时间戳转换，使其在给定时间恰好是当前时区最后一次夏令时转换时间时能正确处理（Tom Lane）
     </para>

     <para>
      这一疏忽已存在很长时间，但此前未被注意到，因为大多数使用夏令时的时区被假定有无限的未来夏令时转换序列。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <type>text</type> 到 <type>name</type> 以及 <type>char</type> 到 <type>name</type> 的转换，使其在多字节编码下正确执行字符串截断（Karl Schnaitter）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>to_tsquery()</function> 中的内存复制缺陷（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      修复规划器对子查询内外层 PlaceHolderVar 的处理（Tom Lane）
     </para>

     <para>
      该缺陷涉及引用来自外层查询外连接可空侧变量的子 SELECT。在 9.1 中，受此缺陷影响的查询会报 <quote>ERROR: Upper-level PlaceHolderVar found where not expected</quote> 失败。但在 9.0 和 8.4 中，你会静默地得到可能错误的答案，因为传入子查询的值不会在该为空时变为空。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <structname>pg_attribute</structname> 非常大会话启动缓慢的问题（Tom Lane）
     </para>

     <para>
      如果 <structname>pg_attribute</structname> 超过 <varname>shared_buffers</varname> 的四分之一，会话启动期间有时需要的缓存重建代码就会触发同步扫描逻辑，使其耗时比正常情况多数倍。如果许多新会话同时启动，问题尤为严重。
     </para>
    </listitem>

    <listitem>
     <para>
      确保顺序扫描以合理的频率检查查询取消（Merlin Moncure）
     </para>

     <para>
      遇到许多不含存活元组的连续页面的扫描在此期间不会响应中断。
     </para>
    </listitem>

    <listitem>
     <para>
      确保 <function>PGSemaphoreLock()</function> 的 Windows 实现在返回前清除 <varname>ImmediateInterruptOK</varname>（Tom Lane）
     </para>

     <para>
      这一疏忽意味着同一查询稍后收到的查询取消中断可能在不安全的时刻被接受，后果不可预测且不好。
     </para>
    </listitem>

    <listitem>
     <para>
      在打印视图或规则时安全地显示整行变量（Abbas Butt、Tom Lane）
     </para>

     <para>
      涉及有歧义名称（即该名称既可能是查询的表名也可能是列名）的极端情况此前以有歧义的方式打印，存在视图或规则在转储和重载后被不同解释的风险。现通过附加无操作转换来避免歧义情况。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <command>COPY FROM</command>，使其正确处理对应无效编码的空值标记串（Tom Lane）
     </para>

     <para>
      诸如 <literal>E'\\0'</literal> 这样的空值标记串本应有效，过去也确实有效，但该情况在 8.4 中被破坏。
     </para>
    </listitem>

    <listitem>
     <para>
      确保自动清理工作进程正确执行栈深度检查（Heikki Linnakangas）
     </para>

     <para>
      此前，由自动 <command>ANALYZE</command> 调用的函数中的无限递归可能导致工作进程崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      修复日志收集器，使其在高负载下不丢失日志连贯性（Andrew Dunstan）
     </para>

     <para>
      此前，如果收集器过于忙碌，可能无法重新组装大消息。
     </para>
    </listitem>

    <listitem>
     <para>
      修复日志收集器，确保它在收到 <systemitem>SIGHUP</systemitem> 后会重新开始文件轮转（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 GIN 索引的 WAL 重放逻辑，使其在索引随后被删除时不失败（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PL/pgSQL 的 <command>RETURN NEXT</command> 命令中的内存泄漏（Joe Conway）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PL/pgSQL 的 <command>GET DIAGNOSTICS</command> 命令在目标是函数第一个变量时的问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>psql</application> 扩展显示（<command>\x</command>）模式中可能越过内存末尾的访问（Peter Eisentraut）
     </para>
    </listitem>

    <listitem>
     <para>
      修复数据库包含大量对象时 <application>pg_dump</application> 的若干性能问题（Jeff Janes、Tom Lane）
     </para>

     <para>
      如果数据库包含许多模式、许多对象处于依赖循环中、或有许多被拥有的序列，<application>pg_dump</application> 可能变得非常慢。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/dblink</filename> 的 <function>dblink_exec()</function>，使其在出错时不泄漏临时数据库连接（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/dblink</filename>，使其在错误消息中报告正确的连接名（Kyotaro Horiguchi）
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2012c
      for DST law changes in Antarctica, Armenia, Chile, Cuba, Falkland
      Islands, Gaza, Haiti, Hebron, Morocco, Syria, and Tokelau Islands;
      also historical corrections for Canada。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-4-11">
  <title>版本 8.4.11</title>

  <note>
  <title>发行日期</title>
  <simpara>2012-02-27</simpara>
 </note>

  <para>
   本次发布包含对 8.4.10 的多方面修复。有关 8.4 大版本中新特性的信息，请参见<xref linkend="release-8-4">。.
  </para>

  <sect2>
   <title>迁移到版本 8.4.11</title>

   <para>
    对于运行 8.4.X 的用户，不需要进行转储/恢复。</para>

   <para>
    不过，如果你是从earlier than 8.4.10 的版本升级，请参见<xref linkend="release-8-4-10">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      让 <command>CREATE TRIGGER</command> 要求对触发器函数有执行权限（Robert Haas）
     </para>

     <para>
      这一缺失的检查可能允许另一用户通过把触发器函数安装到自己拥有的表上，以伪造的输入数据执行该触发器函数。这只对标记为 <literal>SECURITY DEFINER</literal> 的触发器函数有影响，因为其他情况下触发器函数本来就以表所有者身份运行。（CVE-2012-0866）
     </para>
    </listitem>

    <listitem>
     <para>
      移除 SSL 证书中通用名长度的任意限制（Heikki Linnakangas）
     </para>

     <para>
      <application>libpq</application> 和服务器此前都会把从 SSL 证书提取的通用名截断到 32 字节。通常这最坏只会造成意外的验证失败，但在一些相当难以置信的场景下，它可能允许一个证书持有者冒充另一个。受害者必须恰好有 32 字节长的通用名，而攻击者必须说服受信任的 CA 颁发一个通用名以该字符串为前缀的证书。冒充服务器还需要额外的攻击手段来重定向客户端连接。（CVE-2012-0867）
     </para>
    </listitem>

    <listitem>
     <para>
      把 <application>pg_dump</application> 注释中写入的名称里的换行符转换为空格（Robert Haas）
     </para>

     <para>
      <application>pg_dump</application> 此前对其输出脚本中 SQL 注释内写出的对象名清理不慎。包含换行符的名称至少会使脚本语法错误。恶意构造的对象名可能在脚本重载时构成 SQL 注入风险。（CVE-2012-0868）
     </para>
    </listitem>

    <listitem>
     <para>
      修复插入与清理并发导致 btree 索引损坏的问题（Tom Lane）
     </para>

     <para>
      插入导致的索引页分裂有时会使并发运行的 <command>VACUUM</command> 漏删本应删除的索引项。在相应的表行被删除后，悬空的索引项会导致错误（例如 <quote>could not read block N in file ...</quote>），或者更糟：当不相关的行被重新插入到现已空闲的表位置后，查询会静默地得到错误结果。该缺陷自 8.2 起就存在，但发生频率极低，直到现在才被诊断出来。如果你有理由怀疑它曾在你的数据库中发生，对受影响的索引重建索引即可修复。
     </para>
    </listitem>

    <listitem>
     <para>
      更改表所有者时，不仅更新表级权限，也更新逐列权限（Tom Lane）
     </para>

     <para>
      未做到这一点意味着，此前授予的所有列权限仍显示为由旧所有者授予。这导致新所有者和超级用户都无法撤销这些现在无法追溯到表所有者的权限。
     </para>
    </listitem>

    <listitem>
     <para>
      允许 <command>ALTER USER/DATABASE SET</command> 中某些设置取不存在的值（Heikki Linnakangas）
     </para>

     <para>
      允许把 <varname>default_text_search_config</varname>、<varname>default_tablespace</varname> 和 <varname>temp_tablespaces</varname> 设为未知的名称。这是因为设置要在其中使用的另一个数据库中它们可能是已知的；对表空间的情况，则是因为表空间可能尚未创建。此前 <varname>search_path</varname> 已被认识到有同样的问题，这些设置现在的行为与它一致。
     </para>
    </listitem>

    <listitem>
     <para>
      避免在提交后删除表文件出现问题时崩溃（Tom Lane）
     </para>

     <para>
      删除表应只在事务提交后才删除底层磁盘文件。如果那时失败（例如因为文件权限错误），代码本应只发出警告消息并继续，因为此时中止事务已为时过晚。这一逻辑从 8.4 起被破坏，导致这类情况引发 PANIC 和无法重启的数据库。
     </para>
    </listitem>

    <listitem>
     <para>
      在 WAL 重放期间正确跟踪 OID 计数器，即使它发生回卷（Tom Lane）
     </para>

     <para>
      此前 OID 计数器会一直停留在高值，直到系统退出重放模式。其实际后果通常为零，但在某些场景下，已提升为主库的备库服务器在需要 OID 值时，可能要很长时间才能把 OID 计数器推进到合理的值。
     </para>
    </listitem>

    <listitem>
     <para>
      修复带 <literal>*</literal> 的正则表达式反向引用（Tom Lane）
     </para>

     <para>
      代码实际上并不强制精确字符串匹配，而是接受任何满足反向引用符号所引用的模式子表达式的字符串。
     </para>

     <para>
      类似问题仍困扰着嵌入在更大带量词表达式中（而非量词直接作用对象）的反向引用。这将在未来的 <productname>PostgreSQL</productname> 版本中解决。
     </para>
    </listitem>

    <listitem>
     <para>
      修复最近引入的 <type>inet</type>/<type>cidr</type> 值处理中的内存泄漏（Heikki Linnakangas）
     </para>

     <para>
      <productname>PostgreSQL</productname> 2011 年 12 月版本中的一个补丁导致这些操作中的内存泄漏，在此类列上构建 btree 索引等场景中可能相当显著。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 SQL 语言函数中 <command>CREATE TABLE AS</command>/<command>SELECT INTO</command> 之后的悬空指针（Tom Lane）
     </para>

     <para>
      大多数情况下这只会在启用断言的构建中导致断言失败，但似乎可能有更糟的后果。
     </para>
    </listitem>

    <listitem>
     <para>
      避免 Windows 上 syslogger 中文件句柄的双重关闭（MauMau）
     </para>

     <para>
      通常这个错误不可见，但在调试版 Windows 上运行时会导致异常。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 plpgsql 中与 I/O 转换相关的内存泄漏（Andres Freund、Jan Urbanski、Tom Lane）
     </para>

     <para>
      某些操作会一直泄漏内存直到当前函数结束。
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <application>pg_dump</application> 对继承表列的处理（Tom Lane）
     </para>

     <para>
      <application>pg_dump</application> 错误处理了子列与父列默认表达式不同的情况。如果默认值在文本上与父列相同但实际不同（例如因为模式搜索路径差异），它不会被识别为不同，因此转储和恢复后子列会被允许继承父列的默认值。子列为 <literal>NOT NULL</literal> 而父列不是的情况也可能被微妙地错误恢复。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_restore</application> 直接写数据库模式对 INSERT 式表数据的处理（Tom Lane）
     </para>

     <para>
      当使用 2011 年 9 月或 12 月版本的 <application>pg_restore</application> 时，对以 <option>--inserts</option> 或 <option>--column-inserts</option> 选项制作的归档文件直接恢复到数据库会失败，这是对另一个问题的修复中的一个疏忽所致。归档文件本身没有问题，文本模式输出也正常。
     </para>
    </listitem>

    <listitem>
     <para>
      允许 <application>ecpg</application> 的 <literal>DEALLOCATE</literal> 语句中使用 <literal>AT</literal> 选项（Michael Meskes）
     </para>

     <para>
      支持它的基础设施已存在一段时间，但由于疏忽仍有一个错误检查拒绝这种情况。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/intarray</filename> 的 <literal>int[] &amp; int[]</literal> 操作符中的错误（Guillaume Lelarge）
     </para>

     <para>
      如果两个输入数组共有的最小整数是 1，且任一数组中有更小的值，那么 1 会被错误地从结果中遗漏。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/pgcrypto</filename> 的 <function>encrypt_iv()</function> 和 <function>decrypt_iv()</function> 中的错误检测（Marko Kreen）
     </para>

     <para>
      这些函数未能报告某些类型的无效输入错误，而是对错误输入返回随机垃圾值。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/test_parser</filename> 中的一字节缓冲区越界（Paul Guyot）
     </para>

     <para>
      代码会尝试多读一个字节，这在极端情况下会崩溃。由于 <filename>contrib/test_parser</filename> 只是示例代码，这本身不是安全问题，但坏的示例代码仍然是坏的。
     </para>
    </listitem>

    <listitem>
     <para>
      在 ARM 上如果可用则使用 <function>__sync_lock_test_and_set()</function> 实现自旋锁（Martin Pitt）
     </para>

     <para>
      该函数取代了我们此前对 <literal>SWPB</literal> 指令的使用，该指令已被弃用且在 ARMv6 及更高版本上不可用。报告表明旧代码在较新的 ARM 板上不会以明显方式失败，而只是不对并发访问互锁，导致多进程操作中的离奇失败。
     </para>
    </listitem>

    <listitem>
     <para>
      在使用接受该选项的 gcc 版本构建时使用 <option>-fexcess-precision=standard</option> 选项（Andrew Dunstan）
     </para>

     <para>
      这防止了较新版本 gcc 产生"创造性"结果的各种场景。
     </para>
    </listitem>

    <listitem>
     <para>
      允许在 FreeBSD 上使用线程化 Python（Chris Rees）
     </para>

     <para>
      我们的 configure 脚本此前认为这种组合无法工作；但 FreeBSD 修复了问题，因此移除该错误检查。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-4-10">
  <title>版本 8.4.10</title>

  <note>
  <title>发行日期</title>
  <simpara>2011-12-05</simpara>
 </note>

  <para>
   本次发布包含对 8.4.9 的多方面修复。有关 8.4 大版本中新特性的信息，请参见<xref linkend="release-8-4">。.
  </para>

  <sect2>
   <title>迁移到版本 8.4.10</title>

   <para>
    对于运行 8.4.X 的用户，不需要进行转储/恢复。</para>

   <para>
      不过，<literal>information_schema.referential_constraints</literal> 视图的定义中被发现存在一个长期错误。如果你依赖该视图的正确结果，应按下面第一条变更日志条目的说明替换其定义。
     </para>

   <para>
    不过，如果你是从earlier than 8.4.8 的版本升级，请参见<xref linkend="release-8-4-8">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      修复 <literal>information_schema.referential_constraints</literal> 视图中的缺陷（Tom Lane）
     </para>

     <para>
      该视图在把外键约束与它依赖的主键或唯一键约束匹配时不够谨慎。这可能导致某个外键约束完全不被显示、被显示多次，或者声称它依赖于一个并非其实际依赖的约束。
     </para>

     <para>
      由于该视图定义是由 <application>initdb</application> 安装的，仅靠升级不会修复问题。如果需要修复现有安装，可以（以超级用户身份）删除 <literal>information_schema</literal> 模式，然后通过载入 <filename><replaceable>SHAREDIR</replaceable>/information_schema.sql</filename> 重新创建它。（如果不确定 <replaceable>SHAREDIR</replaceable> 在哪里，运行 <literal>pg_config --sharedir</literal>。）这必须在每个要修复的数据库中重复执行。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 GIN 索引更新 WAL 记录的错误重放（Tom Lane）
     </para>

     <para>
      这可能导致崩溃后或热备服务器上瞬时找不到索引项。不过，该问题会在该索引下一次 <command>VACUUM</command> 时被修复。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>CREATE TABLE dest AS SELECT * FROM src</literal> 或 <literal>INSERT INTO dest SELECT * FROM src</literal> 期间与 TOAST 相关的数据损坏（Tom Lane）
     </para>

     <para>
      如果表曾被 <command>ALTER TABLE ADD COLUMN</command> 修改过，在某些极端情况下，尝试把它的数据原样复制到另一个表可能产生损坏的结果。该问题只会在 8.4 及更高版本中以这种确切形式出现，但我们也给更早的版本打了补丁，以防有其他代码路径可能触发同一缺陷。
     </para>
    </listitem>

    <listitem>
     <para>
      修复从过期 syscache 条目访问 toast 表时的竞态条件（Tom Lane）
     </para>

     <para>
      典型症状是诸如 <quote>missing chunk number 0 for toast value NNNNN in pg_toast_2619</quote> 的瞬时错误，其中引用的 toast 表总是属于某个系统目录。
     </para>
    </listitem>

    <listitem>
     <para>
      跟踪函数对参数默认值表达式中所用项的依赖（Tom Lane）
     </para>

     <para>
      此前，被引用的对象可以在未删除或修改函数的情况下被删除，导致函数被使用时行为异常。注意，仅仅安装此更新不会修复缺失的依赖条目；要做到这一点，之后需要对每个这样的函数执行 <command>CREATE OR REPLACE</command>。如果你的函数默认值依赖于非内建对象，建议这样做。
     </para>
    </listitem>

    <listitem>
     <para>
      允许内联带多个 OUT 参数的集合返回 SQL 函数（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <function>DatumGetInetP()</function> 解包带 1 字节头的 inet datum，并新增一个不做此事的宏 <function>DatumGetInetPP()</function>（Heikki Linnakangas）
     </para>

     <para>
      此变更不影响任何核心代码，但可能避免期望 <function>DatumGetInetP()</function> 按常规约定产生已解包 datum 的附加代码中的崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <type>money</type> 类型输入和输出的区域支持（Tom Lane）
     </para>

     <para>
      除了不支持所有标准 <link linkend="guc-lc-monetary"><varname>lc_monetary</varname></link> 格式化选项外，输入和输出函数还不一致，这意味着在某些区域设置下转储的 <type>money</type> 值无法重新读取。
     </para>
    </listitem>

    <listitem>
     <para>
      不让 <link linkend="guc-transform-null-equals"><varname>transform_null_equals</varname></link> 影响 <literal>CASE foo WHEN NULL ...</literal> 构造（Heikki Linnakangas）
     </para>

     <para>
      <varname>transform_null_equals</varname> 只应影响用户直接写的 <literal>foo = NULL</literal> 表达式，而不是这种形式的 <literal>CASE</literal> 在内部生成的相等性检查。
     </para>
    </listitem>

    <listitem>
     <para>
      更改外键触发器的创建顺序，以更好地支持自引用外键（Tom Lane）
     </para>

     <para>
      对于引用自身表的级联外键，行更新会作为一个事件同时触发 <literal>ON UPDATE</literal> 触发器和 <literal>CHECK</literal> 触发器。<literal>ON UPDATE</literal> 触发器必须先执行，否则 <literal>CHECK</literal> 会检查行的非最终状态，并可能抛出不恰当的错误。然而这些触发器的触发顺序由其名称决定，而由于触发器使用遵循 <quote>RI_ConstraintTrigger_NNNN</quote> 约定的自动生成名称，它们通常按创建顺序排序。彻底的修复需要修改该约定（我们将在 9.2 中做），但在现有版本中更改它似乎有风险。因此此补丁只更改触发器的创建顺序。遇到这类错误的用户应删除并重新创建外键约束，使其触发器处于正确顺序。
     </para>
    </listitem>

    <listitem>
     <para>
      避免在跟踪缓冲区分配率时发生浮点下溢（Greg Matthews）
     </para>

     <para>
      虽然这本身无害，但在某些平台上会导致烦人的内核日志消息。
     </para>
    </listitem>

    <listitem>
     <para>
      在 Windows 下启动子进程时保留配置文件名和行号值（Tom Lane）
     </para>

     <para>
      此前它们不会在 <structname>pg_settings</structname> 视图中正确显示。
     </para>
    </listitem>

    <listitem>
     <para>
      在 <application>psql</application> 的命令历史中保留命令内的空行（Robert Haas）
     </para>

     <para>
      例如，如果从字符串字面量中删除了空行，先前的行为可能导致问题。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_dump</application>，使其转储自动生成类型（如表行类型）之间的用户定义转换（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      使用首选版本的 <application>xsubpp</application> 构建 PL/Perl，而不一定是操作系统的主副本（David Wheeler、Alex Hunsaker）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/dict_int</filename> 和 <filename>contrib/dict_xsyn</filename> 中的错误编码（Tom Lane）
     </para>

     <para>
      一些函数错误地假定 <function>palloc()</function> 返回的内存保证被清零。
     </para>
    </listitem>

    <listitem>
     <para>
      让 <function>pgstatindex()</function> 及时响应查询取消中断（Robert Haas）
     </para>
    </listitem>

    <listitem>
     <para>
      确保 VPATH 构建正确安装所有服务器头文件（Peter Eisentraut）
     </para>
    </listitem>

    <listitem>
     <para>
      缩短详细错误消息中报告的文件名（Peter Eisentraut）
     </para>

     <para>
      常规构建一直只报告包含错误消息调用的 C 文件的名称，但 VPATH 构建此前报告绝对路径名。
     </para>
    </listitem>

    <listitem>
     <para>
      修复中美洲 Windows 时区名的解释（Tom Lane）
     </para>

     <para>
      把<quote>Central America Standard Time</quote>映射为 <literal>CST6</literal> 而不是 <literal>CST6CDT</literal>，因为中美洲各地通常不实行夏令时。
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2011n
      for DST law changes in Brazil, Cuba, Fiji, Palestine, Russia, and Samoa;
      also historical corrections for Alaska and British East Africa。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-4-9">
  <title>版本 8.4.9</title>

  <note>
  <title>发行日期</title>
  <simpara>2011-09-26</simpara>
 </note>

  <para>
   本次发布包含对 8.4.8 的多方面修复。有关 8.4 大版本中新特性的信息，请参见<xref linkend="release-8-4">。.
  </para>

  <sect2>
   <title>迁移到版本 8.4.9</title>

   <para>
    对于运行 8.4.X 的用户，不需要进行转储/恢复。</para>

   <para>
    不过，如果你是从earlier than 8.4.8 的版本升级，请参见<xref linkend="release-8-4-8">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      修复对不确定的 HOT 更新元组建立索引的缺陷（Tom Lane）
     </para>

     <para>
      这些缺陷可能导致重建系统目录索引之后索引损坏。据信它们不影响用户索引。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 GiST 索引页面分裂处理中的多个缺陷（Heikki Linnakangas）
     </para>

     <para>
      发生概率很低，但它们可能导致索引损坏。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>tsvector_concat()</function> 中可能的缓冲区越界（Tom Lane）
     </para>

     <para>
      该函数可能低估其结果所需的内存量，导致服务器崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>xml_recv</function> 处理<quote>standalone</quote>参数时的崩溃（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <function>pg_options_to_table</function> 对没有值的选项返回 NULL（Tom Lane）
     </para>

     <para>
      此前这类情况会导致服务器崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      避免在 <command>ANALYZE</command> 和 SJIS-2004 编码转换中可能越过内存末尾的访问（Noah Misch）
     </para>

     <para>
      这修复了一些概率极低的服务器崩溃场景。
     </para>
    </listitem>

    <listitem>
     <para>
      防止启动进程与 bgwriter 进程交互时的间歇性挂起（Simon Riggs）
     </para>

     <para>
      这影响非热备情况下的恢复。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 relcache init 文件失效中的竞态条件（Tom Lane）
     </para>

     <para>
      存在一个时间窗口，新的后端进程可能读到过期的 init 文件，却错过本应告知它数据已过期的失效消息。结果是目录访问中的离奇失败，通常在启动稍后阶段报 <quote>could not read block 0 in file ...</quote>。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 GiST 索引扫描结束时的内存泄漏（Tom Lane）
     </para>

     <para>
      执行许多独立 GiST 索引扫描的命令（例如对已包含许多行的表验证新的基于 GiST 的排他约束）会因此泄漏瞬时需要大量内存。
     </para>
    </listitem>

    <listitem>
     <para>
      修复支持可持有游标和 plpgsql 的 <literal>RETURN NEXT</literal> 命令的 tuplestore 中不正确的内存核算（可能导致内存膨胀）（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复构造大型有损位图时的性能问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复唯一列的连接选择率估计（Tom Lane）
     </para>

     <para>
      这修复了一个可能错误估计连接结果大小的规划器启发式规则。
     </para>
    </listitem>

    <listitem>
     <para>
      修复只出现在子 SELECT 目标列表中的嵌套 PlaceHolderVar 表达式（Tom Lane）
     </para>

     <para>
      这一失误可能导致外连接的输出错误地显示为 NULL。
     </para>
    </listitem>

    <listitem>
     <para>
      允许嵌套的 <literal>EXISTS</literal> 查询被正确优化（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复数组和路径创建函数，确保填充字节为零（Tom Lane）
     </para>

     <para>
      这避免了一些规划器认为语义相等的常量不相等的情况，这种情况会导致糟糕的优化。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <command>EXPLAIN</command>，使其处理内层索引扫描子计划中的门控 Result 节点（Tom Lane）
     </para>

     <para>
      这一疏忽的常见症状是 <quote>bogus varno</quote> 错误。
     </para>
    </listitem>

    <listitem>
     <para>
      规避破坏 WAL 重放的 gcc 4.6.0 缺陷（Tom Lane）
     </para>

     <para>
      这可能导致服务器崩溃后已提交事务的丢失。
     </para>
    </listitem>

    <listitem>
     <para>
      修复视图中 <literal>VALUES</literal> 的转储缺陷（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      禁止对序列执行 <literal>SELECT FOR UPDATE/SHARE</literal>（Tom Lane）
     </para>

     <para>
      这种操作不像预期那样工作，可能导致失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <command>VACUUM</command>，使其总是更新 <literal>pg_class</literal>.<literal>reltuples</literal>/<literal>relpages</literal>（Tom Lane）
     </para>

     <para>
      这修复了一些自动清理可能对何时清理表做出越来越差决策的场景。
     </para>
    </listitem>

    <listitem>
     <para>
      防御计算哈希表大小时的整数溢出（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <command>CLUSTER</command> 可能尝试访问已移除的 TOAST 数据的情况（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <quote>peer</quote> 认证使用凭据控制消息时的可移植性缺陷（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复需要多次往返时的 SSPI 登录（Ahmed Shinwari、Magnus Hagander）
     </para>

     <para>
      该问题的典型症状是 SSPI 登录期间的 <quote>The function requested is not supported</quote> 错误。
     </para>
    </listitem>

    <listitem>
     <para>
      如果 <filename>pg_hba.conf</filename> 包含 <literal>hostssl</literal> 但 SSL 被禁用则抛出错误（Tom Lane）
     </para>

     <para>
      这被认定比此前静默忽略这类行的行为更用户友好。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>pg_srand48</function> 种子初始化中的笔误（Andres Freund）
     </para>

     <para>
      这导致未能使用所提供种子的所有位。该函数在大多数平台上不使用（只在没有 <function>srandom</function> 的平台上使用），而且无论如何看待，随机性低于预期的种子带来的潜在安全暴露似乎都很小。
     </para>
    </listitem>

    <listitem>
     <para>
      避免 <literal>LIMIT</literal> 与 <literal>OFFSET</literal> 值之和超过 2^63 时的整数溢出（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      为 <function>generate_series()</function> 的 <type>int4</type> 和 <type>int8</type> 版本添加溢出检查（Robert Haas）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>to_char()</function> 中移除尾随零的问题（Marti Raudsepp）
     </para>

     <para>
      在使用 <literal>FM</literal> 且小数点后没有数字位的格式中，小数点左侧的零可能被错误移除。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>pg_size_pretty()</function>，避免接近 2^63 的输入溢出（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      放宽 plpgsql 对记录值中 typmod 匹配的检查（Tom Lane）
     </para>

     <para>
      过于严格的检查可能导致丢弃本应保留的长度修饰符。
     </para>
    </listitem>

    <listitem>
     <para>
      在 <application>initdb</application> 期间正确处理区域名中的引号（Heikki Linnakangas）
     </para>

     <para>
      这种情况可能出现在某些 Windows 区域设置下，例如<quote>People's Republic of China</quote>。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_upgrade</application>，使其在从 8.3 升级期间保留 toast 表的 relfrozenxid（Bruce Momjian）
     </para>

     <para>
      不这样做可能导致升级后 <filename>pg_clog</filename> 文件被过早删除。
     </para>
    </listitem>

    <listitem>
     <para>
      在 <application>pg_ctl</application> 中，支持 Windows 上服务注册的静默模式（MauMau）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>psql</application> 在从其他文件执行 <literal>COPY</literal> 期间脚本文件行号计数的问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_restore</application> 直接写数据库模式对 <varname>standard_conforming_strings</varname> 的处理（Tom Lane）
     </para>

     <para>
      当从以 <varname>standard_conforming_strings</varname> 设为 <literal>on</literal> 制作的归档文件直接恢复到数据库服务器时，<application>pg_restore</application> 可能发出不正确的命令。
     </para>
    </listitem>

    <listitem>
     <para>
      对并行 <application>pg_restore</application> 不支持的情况更加用户友好（Tom Lane）
     </para>

     <para>
      此变更确保这类情况在任何恢复动作发生之前就被检测并报告。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>libpq</application> 的 LDAP 服务查找代码中的越界写入和内存泄漏（Albe Laurenz）
     </para>
    </listitem>

    <listitem>
     <para>
      在 <application>libpq</application> 中，避免使用非阻塞 I/O 和 SSL 连接时的失败（Martin Pihlak、Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      改进 libpq 对连接启动期间失败的处理（Tom Lane）
     </para>

     <para>
      特别是，对 SSL 连接启动期间服务器报告 <function>fork()</function> 失败的响应现在更合理。
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <application>libpq</application> 对 SSL 失败的错误报告（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>PQsetvalue()</function>，避免向最初来自服务器查询的 <structname>PGresult</structname> 添加新元组时可能崩溃（Andrew Chernow）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <application>ecpglib</application> 以 15 位精度写 <type>double</type> 值（Akira Kurosawa）
     </para>
    </listitem>

    <listitem>
     <para>
      在 <application>ecpglib</application> 中，确保出错后恢复 <literal>LC_NUMERIC</literal> 设置（Michael Meskes）
     </para>
    </listitem>

    <listitem>
     <para>
      应用上游对 blowfish 有符号字符缺陷的修复（CVE-2011-2483）（Tom Lane）
     </para>

     <para>
      <filename>contrib/pg_crypto</filename> 的 blowfish 加密代码在 char 为有符号的平台（大多数平台如此）上可能给出错误结果，导致加密口令比应有的强度更弱。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/seg</filename> 中的内存泄漏（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>pgstatindex()</function>，使其对空索引给出一致结果（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      允许使用 perl 5.14 构建（Alex Hunsaker）
     </para>
    </listitem>

    <listitem>
     <para>
      更新 configure 脚本探测系统函数是否存在的方法（Tom Lane）
     </para>

     <para>
      我们在 8.3 和 8.2 中使用的 autoconf 版本可能被执行链接时优化的编译器欺骗。
     </para>
    </listitem>

    <listitem>
     <para>
      修复构建和安装文件路径包含空格的各种问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2011i
      for DST law changes in Canada, Egypt, Russia, Samoa, and South Sudan。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-4-8">
  <title>版本 8.4.8</title>

  <note>
  <title>发行日期</title>
  <simpara>2011-04-18</simpara>
 </note>

  <para>
   本次发布包含对 8.4.7 的多方面修复。有关 8.4 大版本中新特性的信息，请参见<xref linkend="release-8-4">。.
  </para>

  <sect2>
   <title>迁移到版本 8.4.8</title>

   <para>
    对于运行 8.4.X 的用户，不需要进行转储/恢复。</para>

   <para>
      不过，如果你的安装是通过运行 <application>pg_upgrade</application> 从前一个大版本升级的，应采取措施防止因 <application>pg_upgrade</application> 一个现已修复的缺陷导致可能的数据丢失。推荐的解决方案是对所有 TOAST 表执行 <command>VACUUM FREEZE</command>。更多信息见 <ulink url="http://wiki.postgresql.org/wiki/20110408pg_upgrade_fix"> http://wiki.postgresql.org/wiki/20110408pg_upgrade_fix</ulink>。
     </para>

   <para>
    不过，如果你是从earlier than 8.4.2 的版本升级，请参见<xref linkend="release-8-4-2">。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      修复 <application>pg_upgrade</application> 对 TOAST 表的处理（Bruce Momjian）
     </para>

     <para>
      在 <application>pg_upgrade</application> 期间，TOAST 表的 <structname>pg_class</structname>.<structfield>relfrozenxid</structfield> 值未被正确复制到新安装。这之后可能导致 <literal>pg_clog</literal> 文件在仍被需要用于验证 TOAST 表中的元组时被丢弃，导致 <quote>could not access status of transaction</quote> 失败。
     </para>

     <para>
      这一错误对曾用 <application>pg_upgrade</application> 升级的安装构成显著的数据丢失风险。此补丁纠正了未来使用 <application>pg_upgrade</application> 时的问题，但本身不能治愈已用带缺陷版本的 <application>pg_upgrade</application> 处理过的安装中的问题。
     </para>
    </listitem>

    <listitem>
     <para>
      抑制错误的 <quote>PD_ALL_VISIBLE flag was incorrectly set</quote> 警告（Heikki Linnakangas）
     </para>

     <para>
      <command>VACUUM</command> 有时会在实际有效的情况下发出此警告。
     </para>
    </listitem>

    <listitem>
     <para>
      禁止在组合类型中包含其自身（Tom Lane）
     </para>

     <para>
      这防止了服务器在处理该组合类型时无限递归的场景。虽然这种结构有一些可能的用途，但它们似乎不足以抵消确保其始终安全工作所需的努力。
     </para>
    </listitem>

    <listitem>
     <para>
      避免目录缓存初始化期间的潜在死锁（Nikhil Sontakke）
     </para>

     <para>
      某些情况下，缓存加载代码会在锁定索引的目录之前获取系统索引上的共享锁。这可能与尝试以另一种更标准的顺序获取排他锁的进程死锁。
     </para>
    </listitem>

    <listitem>
     <para>
      修复当目标元组存在并发更新时 <literal>BEFORE ROW UPDATE</literal> 触发器处理中的悬空指针问题（Tom Lane）
     </para>

     <para>
      已观察到该缺陷在尝试执行 <literal>UPDATE RETURNING ctid</literal> 时导致间歇性的 <quote>cannot extract system attribute from virtual tuple</quote> 失败。发生更严重错误（例如为更新后的元组生成错误索引项）的概率非常小。
     </para>
    </listitem>

    <listitem>
     <para>
      当表存在未决的延迟触发器事件时禁止 <command>DROP TABLE</command>（Tom Lane）
     </para>

     <para>
      此前 <command>DROP</command> 会照常执行，导致触发器最终触发时报 <quote>could not open relation with OID nnn</quote> 错误。
     </para>
    </listitem>

    <listitem>
     <para>
      防止 GEQO 优化期间由恒假 WHERE 条件触发的崩溃（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      改进规划器对半连接和反连接情况的处理（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复文本搜索的选择率估计，使其考虑 NULL（Jesper Krogh）
     </para>
    </listitem>

    <listitem>
     <para>
      改进 PL/pgSQL 处理带已删除列的行类型的能力（Pavel Stehule）
     </para>

     <para>
      这是先前在 9.0 中所做修复的向后移植。
     </para>
    </listitem>

    <listitem>
     <para>
      修复涉及数组切片的 PL/Python 内存泄漏（Daniel Popowich）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_restore</application>，使其能处理 TOC 文件中的长行（超过 1KB）（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      加入更多防护，防止因过于激进的编译器优化导致的除零崩溃（Aurelien Jarno）
     </para>
    </listitem>

    <listitem>
     <para>
      支持在 FreeBSD 和 OpenBSD 的 MIPS 上使用 dlopen()（Tom Lane）
     </para>

     <para>
      此前有一个硬编码的假设：该系统函数在这些系统的 MIPS 硬件上不可用。现在改用编译期测试，因为较新的版本已经有了它。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 HP-UX 上的编译失败（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 Windows 上 <application>libintl</application> 的版本不兼容问题（Hiroshi Inoue）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 Windows 构建脚本中 <application>xcopy</application> 的用法，使其在 Windows 7 下正确工作（Andrew Dunstan）
     </para>

     <para>
      这只影响构建脚本，不影响安装或使用。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_regress</application> 在 Cygwin 上使用的路径分隔符（Andrew Dunstan）
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2011f
      for DST law changes in Chile, Cuba, Falkland Islands, Morocco, Samoa,
      and Turkey; also historical corrections for South Australia, Alaska,
      and Hawaii。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-4-7">
  <title>版本 8.4.7</title>

  <note>
  <title>发行日期</title>
  <simpara>2011-01-31</simpara>
 </note>

  <para>
   本次发布包含对 8.4.6 的多方面修复。有关 8.4 大版本中新特性的信息，请参见<xref linkend="release-8-4">。.
  </para>

  <sect2>
   <title>迁移到版本 8.4.7</title>

   <para>
      对于运行 8.4.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.4.2 的版本升级，请参阅<xref linkend="release-8-4-2">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      避免 <command>EXPLAIN</command> 尝试显示简单形式 <literal>CASE</literal> 表达式时的失败（Tom Lane）
     </para>

     <para>
      如果 <literal>CASE</literal> 的测试表达式是常量，规划器可能把 <literal>CASE</literal> 简化为令表达式显示代码困惑的形式，导致 <quote>unexpected CASE WHEN clause</quote> 错误。
     </para>
    </listitem>

    <listitem>
     <para>
      修复对现有下标范围之前的数组切片赋值的问题（Tom Lane）
     </para>

     <para>
      如果新增下标与第一个既有下标之间存在间隙，代码会错误计算需要从旧数组的空值位图复制多少条目，可能导致数据损坏或崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      避免规划器对非常久远的日期值出现意外的转换溢出（Tom Lane）
     </para>

     <para>
      <type>date</type> 类型支持的日期范围比 <type>timestamp</type> 类型能表示的更宽，但规划器假定它总是可以不受惩罚地把 date 转换为 timestamp。
     </para>
    </listitem>

    <listitem>
     <para>
      修复启用 <varname>standard_conforming_strings</varname> 时 <application>pg_restore</application> 对大对象（BLOB）的文本输出（Tom Lane）
     </para>

     <para>
      虽然直接恢复到数据库时工作正确，但如果要求 <application>pg_restore</application> 生成 SQL 文本输出且源数据库中已启用 <varname>standard_conforming_strings</varname>，字符串转义是不正确的。
     </para>
    </listitem>

    <listitem>
     <para>
      修复对包含 <literal>... &amp; !(subexpression) | ...</literal> 的 <type>tsquery</type> 值的错误解析（Tom Lane）
     </para>

     <para>
      包含这种操作符组合的查询没有被正确执行。同样的错误也存在于 <filename>contrib/intarray</filename> 的 <type>query_int</type> 类型和 <filename>contrib/ltree</filename> 的 <type>ltxtquery</type> 类型中。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/intarray</filename> 的 <type>query_int</type> 类型输入函数中的缓冲区越界（Apple）
     </para>

     <para>
      该缺陷构成安全风险，因为函数的返回地址可能被覆写。感谢 Apple Inc 的安全团队报告此问题并提供修复。（CVE-2010-4015）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/seg</filename> 的 GiST picksplit 算法中的缺陷（Alexander Korotkov）
     </para>

     <para>
      这可能导致 <type>seg</type> 列上的 GiST 索引相当低效，尽管答案并非真正错误。如果你有这样的索引，考虑在安装此更新后对其执行 <command>REINDEX</command>。（这与上一次更新中在 <filename>contrib/cube</filename> 中修复的缺陷相同。）
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-4-6">
  <title>版本 8.4.6</title>

  <note>
  <title>发行日期</title>
  <simpara>2010-12-16</simpara>
 </note>

  <para>
   本次发布包含对 8.4.5 的多方面修复。有关 8.4 大版本中新特性的信息，请参见<xref linkend="release-8-4">。.
  </para>

  <sect2>
   <title>迁移到版本 8.4.6</title>

   <para>
      对于运行 8.4.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.4.2 的版本升级，请参阅<xref linkend="release-8-4-2">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      强制 Linux 上的默认 <link linkend="guc-wal-sync-method"><varname>wal_sync_method</varname></link> 为 <literal>fdatasync</literal>（Tom Lane、Marti Raudsepp）
     </para>

     <para>
      Linux 上的默认值实际上多年来一直是 <literal>fdatasync</literal>，但最近的内核变更导致 <productname>PostgreSQL</productname> 改选 <literal>open_datasync</literal>。这一选择没有带来任何性能改进，反而在某些文件系统（特别是使用 <literal>data=journal</literal> 挂载选项的 <literal>ext4</literal>）上导致彻底失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 GIN 索引 WAL 重放逻辑中的若干缺陷（Tom Lane）
     </para>

     <para>
      这可能导致复制期间的 <quote>bad buffer id: 0</quote> 失败或索引内容损坏。
     </para>
    </listitem>

    <listitem>
     <para>
      修复当起始检查点 WAL 记录与其重做点不在同一 WAL 段时从基础备份恢复的问题（Jeff Davis）
     </para>
    </listitem>

    <listitem>
     <para>
      修复多个工作进程长期保持活动时自动清理工作进程的持续变慢（Tom Lane）
     </para>

     <para>
      如果自动清理工作进程处理了足够多的表，其有效 <varname>vacuum_cost_limit</varname> 可能降到接近零，导致其运行极慢。
     </para>
    </listitem>

    <listitem>
     <para>
      新增在 <literal>IA64</literal> 上检测寄存器栈溢出的支持（Tom Lane）
     </para>

     <para>
      <literal>IA64</literal> 架构有两个硬件栈。要完全防止栈溢出失败需要同时检查两者。
     </para>
    </listitem>

    <listitem>
     <para>
      在 <function>copyObject()</function> 中添加栈溢出检查（Tom Lane）
     </para>

     <para>
      给定足够复杂的查询，某些代码路径可能因栈溢出而崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      修复临时 GiST 索引中页面分裂的检测（Heikki Linnakangas）
     </para>

     <para>
      在临时索引中可能出现<quote>并发</quote>页面分裂，例如当插入时有打开的游标正在扫描该索引。GiST 未能检测到这种情况，因此当游标继续执行时可能给出错误结果。
     </para>
    </listitem>

    <listitem>
     <para>
      修复早期连接处理期间的错误检查（Tom Lane）
     </para>

     <para>
      某些情况下对子进程过多的检查被跳过，可能在尝试把新子进程添加到固定大小数组时导致 postmaster 崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      改进窗口函数的效率（Tom Lane）
     </para>

     <para>
      某些需要提前读取大量元组、但 <varname>work_mem</varname> 足够大可以全部保存在内存中的情况意外地慢。<function>percent_rank()</function>、<function>cume_dist()</function> 和 <function>ntile()</function> 尤其受此问题影响。
     </para>
    </listitem>

    <listitem>
     <para>
      避免在 <command>ANALYZE</command> 复杂索引表达式时内存泄漏（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      确保使用整行 Var 的索引仍依赖于其表（Tom Lane）
     </para>

     <para>
      像 <literal>create index i on t (foo(t.*))</literal> 这样声明的索引在删除其表时不会自动被删除。
     </para>
    </listitem>

    <listitem>
     <para>
      不要<quote>内联</quote>带多个 <literal>OUT</literal> 参数的 SQL 函数（Tom Lane）
     </para>

     <para>
      这避免了因丢失预期结果行类型信息而可能出现的崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      修复当 <literal>ORDER BY</literal>、<literal>LIMIT</literal>、<literal>FOR UPDATE</literal> 或 <literal>WITH</literal> 附加到 <literal>INSERT ... VALUES</literal> 的 <literal>VALUES</literal> 部分时的正确行为（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>COALESCE()</literal> 表达式的常量折叠（Tom Lane）
     </para>

     <para>
      规划器有时会尝试求值实际上永远不可达的子表达式，可能导致意外错误。
     </para>
    </listitem>

    <listitem>
     <para>
      修复当连接接受（<function>accept()</function> 或紧随其后的调用之一）失败且 postmaster 编译时带 GSSAPI 支持时的 postmaster 崩溃（Alexander Chernikov）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <varname>log_temp_files</varname> 处于活动状态时临时文件漏删的问题（Tom Lane）
     </para>

     <para>
      如果尝试发出日志消息时出错，就不会执行删除，导致临时文件累积。
     </para>
    </listitem>

    <listitem>
     <para>
      为 <structname>InhRelation</structname> 节点添加打印功能（Tom Lane）
     </para>

     <para>
      这避免了启用 <varname>debug_print_parse</varname> 且执行某些类型的查询时的失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复点到水平线段距离的错误计算（Tom Lane）
     </para>

     <para>
      该缺陷影响多个不同的几何距离测量操作符。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>ecpg</application> 中事务状态的错误计算（Itagaki Takahiro）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>PL/pgSQL</application> 对<quote>简单</quote>表达式的处理，使其在递归或错误恢复情况下不失败（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>PL/Python</application> 对集合返回函数的处理（Jan Urbanski）
     </para>

     <para>
      在生成集合结果的迭代器内尝试调用 SPI 函数会失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/cube</filename> 的 GiST picksplit 算法中的缺陷（Alexander Korotkov）
     </para>

     <para>
      这可能导致 <type>cube</type> 列上的 GiST 索引相当低效，尽管答案并非真正错误。如果你有这样的索引，考虑在安装此更新后对其执行 <command>REINDEX</command>。
     </para>
    </listitem>

    <listitem>
     <para>
      <filename>contrib/dblink</filename> 除了在创建新连接时外不再发出<quote>identifier will be truncated</quote>通知（Itagaki Takahiro）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/pgcrypto</filename> 在缺少公钥时可能的核心转储（Marti Raudsepp）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/xml2</filename> 的 XPath 查询函数中的内存泄漏（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2010o
      for DST law changes in Fiji and Samoa;
      also historical corrections for Hong Kong。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-4-5">
  <title>版本 8.4.5</title>

  <note>
  <title>发行日期</title>
  <simpara>2010-10-04</simpara>
 </note>

  <para>
   本次发布包含对 8.4.4 的多方面修复。有关 8.4 大版本中新特性的信息，请参见<xref linkend="release-8-4">。.
  </para>

  <sect2>
   <title>迁移到版本 8.4.5</title>

   <para>
      对于运行 8.4.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.4.2 的版本升级，请参阅<xref linkend="release-8-4-2">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      在 PL/Perl 和 PL/Tcl 中为每个调用方 SQL 用户 ID 使用单独的解释器（Tom Lane）
     </para>

     <para>
      此变更防止了可通过破坏稍后将在同一会话中以另一 SQL 用户身份执行的 Perl 或 Tcl 代码（例如在 <literal>SECURITY DEFINER</literal> 函数内）而造成的安全问题。大多数脚本语言都提供许多实现这一点的手段，例如重新定义目标函数调用的标准函数或操作符。如果没有此变更，任何拥有 Perl 或 Tcl 语言使用权的 SQL 用户基本上都能以目标函数所有者的 SQL 权限为所欲为。
     </para>

     <para>
      此变更的代价是 Perl 和 Tcl 函数之间的有意通信变得更困难。为提供出路，PL/PerlU 和 PL/TclU 函数继续每会话只使用一个解释器。这不被视为安全问题，因为所有这类函数本来就以数据库超级用户的信任级别执行。
     </para>

     <para>
      声称提供可信执行的第三方过程语言很可能存在类似的安全问题。我们建议就安全关键用途联系你所依赖的任何 PL 的作者。
     </para>

     <para>
      感谢 Tim Bunce 指出此问题（CVE-2010-3433）。
     </para>
    </listitem>

    <listitem>
     <para>
      禁止以并非它预期配合使用的系统目录列的参数调用 <function>pg_get_expr()</function>，以防止可能的崩溃（Heikki Linnakangas、Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      在 Windows 上把退出码 128（<literal>ERROR_WAIT_NO_CHILDREN</literal>）视为非致命（Magnus Hagander）
     </para>

     <para>
      高负载下，Windows 进程有时会在启动时以此错误码失败。此前 postmaster 把这视为 panic 情况并重启整个数据库，但这似乎反应过度。
     </para>
    </listitem>

    <listitem>
     <para>
      修复占位符求值位置不正确的问题（Tom Lane）
     </para>

     <para>
      当外连接的内侧是输出列表中含非严格表达式的子 SELECT 时，该缺陷可能导致本应为空的查询输出非空。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>UNION ALL</literal> 成员关系可能被重复扫描的问题（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复<quote>无法处理未规划的子查询</quote>错误（Tom Lane）
     </para>

     <para>
      当子查询包含一个连接别名引用，而该引用展开成包含另一个子查询的表达式时，会出现此错误。
     </para>
    </listitem>

    <listitem>
     <para>
      修复对引用视图或子查询且出现在嵌套子查询中的整行 Var 的错误处理（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复跨类型 <literal>IN</literal> 比较的错误处理（Tom Lane）
     </para>

     <para>
      如果规划器试图用先排序、再去重、再普通连接的计划实现 <literal>IN</literal> 连接，这可能导致失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <type>tsvector</type> 列的 <command>ANALYZE</command> 统计信息计算（Jan Urbanski）
     </para>

     <para>
      原来的编码可能产生不正确的统计信息，导致之后糟糕的计划选择。
     </para>
    </listitem>

    <listitem>
     <para>
      改进规划器对 <function>array_agg()</function>、<function>string_agg()</function> 及类似聚合函数所用内存的估计（Hitoshi Harada）
     </para>

     <para>
      此前严重的低估可能因不当选择哈希聚合计划而导致内存不足失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复未将缓存计划标记为瞬态的失败（Tom Lane）
     </para>

     <para>
      如果在某个被引用表正在执行 <command>CREATE INDEX CONCURRENTLY</command> 期间准备了计划，应当在索引可供使用后重新规划。这此前没有可靠地发生。
     </para>
    </listitem>

    <listitem>
     <para>
      在偶有报告的 btree 失败情形中把 PANIC 降为 ERROR，并在产生的错误消息中提供更多细节（Tom Lane）
     </para>

     <para>
      这应能改进系统对损坏索引的健壮性。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 GIN 索引部分匹配查询的错误搜索逻辑（Tom Lane）
     </para>

     <para>
      涉及多个 GIN 索引条件的 AND/OR 组合的情形并不总是给出正确答案，而且有时比必要的慢得多。
     </para>
    </listitem>

    <listitem>
     <para>
      防止 show_session_authorization() 在自动清理进程中崩溃（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      防御返回 setof record 但并非所有返回行实际为同一行类型的函数（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复子事务回滚期间待处理触发器事件列表可能损坏的问题（Tom Lane）
     </para>

     <para>
      这可能导致崩溃或触发器错误触发。
     </para>
    </listitem>

    <listitem>
     <para>
      修复对按引用传递的函数结果计算哈希时可能的失败（Tao Ma、Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      改进归并连接对连接列中 NULL 的处理（Tom Lane）
     </para>

     <para>
      如果排序顺序使 NULL 排在高位，归并连接现在可以在到达第一个 NULL 时完全停止。
     </para>
    </listitem>

    <listitem>
     <para>
      写入锁文件（<filename>postmaster.pid</filename> 和套接字锁文件两者）内容时注意对其执行 fsync（Tom Lane）
     </para>

     <para>
      如果机器在 postmaster 启动后不久崩溃，这一疏漏可能导致锁文件内容损坏。这又可能使后续启动 postmaster 的尝试失败，直到手工删除锁文件。
     </para>
    </listitem>

    <listitem>
     <para>
      避免在为深层嵌套的子事务分配 XID 时递归（Andres Freund、Robert Haas）
     </para>

     <para>
      在栈空间有限时，原来的编码可能导致崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      避免 walwriter 进程持有打开的旧 WAL 段（Magnus Hagander、Heikki Linnakangas）
     </para>

     <para>
      以前的编码会阻止删除不再需要的段。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <varname>log_line_prefix</varname> 的 <literal>%i</literal> 转义，它可能在后端启动早期产生垃圾（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      防止误解 TOAST 表的部分指定关系选项（Itagaki Takahiro）
     </para>

     <para>
      特别是，如果为表设置了任何其他 reloption，<literal>fillfactor</literal> 会被读为零，导致严重膨胀。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <command>ALTER TABLE ... ADD CONSTRAINT</command> 中的继承计数跟踪（Robert Haas）
     </para>
    </listitem>

    <listitem>
     <para>
      修复启用归档时 <command>ALTER TABLE ... SET TABLESPACE</command> 中可能的数据损坏（Jeff Davis）
     </para>
    </listitem>

    <listitem>
     <para>
      允许 <command>CREATE DATABASE</command> 和 <command>ALTER DATABASE ... SET TABLESPACE</command> 被查询取消中断（Guillaume Lelarge）
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <command>CREATE INDEX</command> 对所提议索引表达式是否不可变的检查（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <command>REASSIGN OWNED</command> 以处理操作符类和操作符族（Asko Tiidumaa）
     </para>
    </listitem>

    <listitem>
     <para>
      修复比较两个空 <type>tsquery</type> 值时可能的核心转储（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>LIKE</literal> 对包含 <literal>%</literal> 后跟 <literal>_</literal> 的模式的处理（Tom Lane）
     </para>

     <para>
      我们此前修复过它，但仍有一些被错误处理的情形。
     </para>
    </listitem>

    <listitem>
     <para>
      重新允许输入公元 0001-01-01 之前的儒略日期（Tom Lane）
     </para>

     <para>
      像 <literal>'J100000'::date</literal> 这样的输入在 8.4 之前可行，但被新增的错误检查无意中破坏。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PL/pgSQL：如果在迭代某游标的 <literal>FOR</literal> 循环内关闭该游标，抛出错误而不是崩溃（Heikki Linnakangas）
     </para>
    </listitem>

    <listitem>
     <para>
      在 PL/Python 中，防御 <function>PyCObject_AsVoidPtr</function> 和 <function>PyCObject_FromVoidPtr</function> 的空指针结果（Peter Eisentraut）
     </para>
    </listitem>

    <listitem>
     <para>
      在 <application>libpq</application> 中，修复同时指定 <literal>host</literal> 和 <literal>hostaddr</literal> 情形下的完整 SSL 证书验证（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      让 psql 在自动提交关闭模式下把 <command>DISCARD ALL</command> 识别为不应包裹在事务块中的命令（Itagaki Takahiro）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_dump</application> 处理 SQL/MED 对象的一些问题（Tom Lane）
     </para>

     <para>
      特别是，<application>pg_dump</application> 由非超级用户运行时总会失败，这并非本意。
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <application>pg_dump</application> 和 <application>pg_restore</application> 对不可寻址归档文件的处理（Tom Lane、Robert Haas）
     </para>

     <para>
      这对并行恢复的正常运作很重要。
     </para>
    </listitem>

    <listitem>
     <para>
      改进并行 pg_restore 应对选择性恢复（<literal>-L</literal> 选项）的能力（Tom Lane）
     </para>

     <para>
      如果 <literal>-L</literal> 文件指定了非默认的恢复顺序，原来的代码倾向于失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>ecpg</application> 以正确处理来自 <literal>RETURNING</literal> 子句的数据（Michael Meskes）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>ecpg</application> 中的一些内存泄漏（Zoltan Boszormenyi）
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <filename>contrib/dblink</filename> 对包含已删除列的表的处理（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/dblink</filename> 中<quote>重复连接名</quote>错误后的连接泄漏（Itagaki Takahiro）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/dblink</filename> 以正确处理超过 62 字节的连接名（Itagaki Takahiro）
     </para>
    </listitem>

    <listitem>
     <para>
      为 <filename>contrib/hstore</filename> 新增 <function>hstore(text, text)</function> 函数（Robert Haas）
     </para>

     <para>
      此函数是现已废弃的 <literal>=&gt;</literal> 操作符的推荐替代。向后移植它使得面向未来的代码可以用于较旧的服务器版本。注意该补丁只有在 <filename>contrib/hstore</filename> 于特定数据库中安装或重新安装之后才生效。用户也可以改为手工执行 <command>CREATE FUNCTION</command> 命令。
     </para>
    </listitem>

    <listitem>
     <para>
      更新构建基础设施和文档，反映源代码仓库从 CVS 迁移到 Git 的变化（Magnus Hagander 等）
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2010l
      for DST law changes in Egypt and Palestine; also historical corrections
      for Finland。
     </para>

     <para>
      此变更还为两个密克罗尼西亚时区新增了名称：Pacific/Chuuk 现在优先于 Pacific/Truk（首选缩写为 CHUT 而非 TRUT），Pacific/Pohnpei 优先于 Pacific/Ponape。
     </para>
    </listitem>

    <listitem>
     <para>
      让 Windows 的 <quote>N. Central Asia Standard Time</quote> 时区映射到 Asia/Novosibirsk 而不是 Asia/Almaty（Magnus Hagander）
     </para>

     <para>
      Microsoft 在 KB976098 的时区更新中改变了此区域的 DST 行为。Asia/Novosibirsk 更匹配其新行为。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-4-4">
  <title>版本 8.4.4</title>

  <note>
  <title>发行日期</title>
  <simpara>2010-05-17</simpara>
 </note>

  <para>
   本次发布包含对 8.4.3 的多方面修复。有关 8.4 大版本中新特性的信息，请参见<xref linkend="release-8-4">。.
  </para>

  <sect2>
   <title>迁移到版本 8.4.4</title>

   <para>
      对于运行 8.4.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.4.2 的版本升级，请参阅<xref linkend="release-8-4-2">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      用应用于整个解释器的操作码掩码来强制 <literal>plperl</literal> 中的限制，而不是使用 <filename>Safe.pm</filename>（Tim Bunce、Andrew Dunstan）
     </para>

     <para>
      近期的事态发展使我们确信 <filename>Safe.pm</filename> 太不安全，不能依靠它使 <literal>plperl</literal> 可信。此变更完全移除了对 <filename>Safe.pm</filename> 的使用，改为使用始终应用操作码掩码的独立解释器。此变更的良好副作用包括：现在可以在 <literal>plperl</literal> 中以自然的方式使用 Perl 的 <literal>strict</literal> 编译指令，Perl 的 <literal>$a</literal> 和 <literal>$b</literal> 变量在排序例程中按预期工作，以及函数编译显著加快。（CVE-2010-1169）
     </para>
    </listitem>

    <listitem>
     <para>
      防止 PL/Tcl 执行来自 <structname>pltcl_modules</structname> 的不可信代码（Tom）
     </para>

     <para>
      PL/Tcl 从数据库表自动装载 Tcl 代码的特性可能被利用于特洛伊木马攻击，因为对谁能创建该表或向其中插入没有限制。此变更禁用该特性，除非 <structname>pltcl_modules</structname> 由超级用户拥有。（但是，不检查该表上的权限，因此确实需要较不安全的模块表的安装仍可以向受信任的非超级用户授予适当权限。）此外，除非确实要执行 <literal>pltclu</literal> 函数，否则防止把代码装载到无限制的<quote>普通</quote>Tcl 解释器中。（CVE-2010-1170）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>ALTER ... SET TABLESPACE</literal> 的 WAL 重放期间的数据损坏（Tom）
     </para>

     <para>
      当 <varname>archive_mode</varname> 开启时，<literal>ALTER ... SET TABLESPACE</literal> 生成的 WAL 记录的重放逻辑不正确。它可能把数据写到错误的位置，导致可能无法恢复的数据损坏。数据损坏会在备机从属上被观察到；如果在提交 <literal>ALTER</literal> 之后、下一个检查点之前发生数据库崩溃和恢复，主机上也可能发生。
     </para>
    </listitem>

    <listitem>
     <para>
      修复在重建 relcache 条目期间收到缓存重置消息时可能的崩溃（Heikki）
     </para>

     <para>
      此错误是在 8.4.3 中修复相关失败时引入的。
     </para>
    </listitem>

    <listitem>
     <para>
      在为函数运行语言验证器时应用逐函数 GUC 设置（Itagaki Takahiro）
     </para>

     <para>
      这避免了函数代码在没有该设置时无效所导致的失败；例如 <varname>search_path</varname> 不正确时 SQL 函数可能无法解析。
     </para>
    </listitem>

    <listitem>
     <para>
      当 <varname>constraint_exclusion</varname> = <literal>partition</literal> 时，对继承的 <command>UPDATE</command> 和 <command>DELETE</command> 目标表做约束排除（Tom）
     </para>

     <para>
      由于疏忽，此设置此前只在 <command>SELECT</command> 命令中触发约束排除检查。
     </para>
    </listitem>

    <listitem>
     <para>
      不允许非特权用户重置仅超级用户可用的参数设置（Alvaro）
     </para>

     <para>
      此前，如果非特权用户为自己运行 <literal>ALTER USER ... RESET ALL</literal>，或为他拥有的数据库运行 <literal>ALTER DATABASE ... RESET ALL</literal>，这将移除该用户或数据库的所有特殊参数设置，包括本应只可由超级用户更改的那些。现在，<command>ALTER</command> 只移除用户有权更改的参数。
     </para>
    </listitem>

    <listitem>
     <para>
      避免后端关闭发生在需要向日志条目添加 <literal>CONTEXT</literal> 内容时导致可能的崩溃（Tom）
     </para>

     <para>
      某些情况下上下文打印函数会失败，因为到打印日志消息时当前事务已被回滚。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <varname>recovery_end_command</varname> 中 <literal>%r</literal> 参数的错误处理（Heikki）
     </para>

     <para>
      该值总是出来为零。
     </para>
    </listitem>

    <listitem>
     <para>
      确保归档进程尽快响应 <varname>archive_command</varname> 的变更（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 pl/pgsql 的 <literal>CASE</literal> 语句在 case 表达式是不返回行的查询时不失败（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      为现代 Perl 版本更新 pl/perl 的 <filename>ppport.h</filename>（Andrew）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 pl/python 中的若干内存泄漏（Andreas Freund、Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      在 ecpg 中正确处理空字符串连接参数（Michael）
     </para>
    </listitem>

    <listitem>
     <para>
      防止 <application>psql</application> 在展开引用自身的变量时无限递归（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>psql</application> 的 <literal>\copy</literal>，不在 <literal>\copy (select ...)</literal> 内的点周围添加空格（Tom）
     </para>

     <para>
      在数字字面量的小数点周围添加空格会导致语法错误。
     </para>
    </listitem>

    <listitem>
     <para>
      避免 <application>psql</application> 在与 <varname>client_encoding</varname> 不匹配的区域设置环境中运行时的格式化失败（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复使用 <filename>contrib/intarray</filename> 操作符的不可满足查询产生不必要的 <quote>GIN 索引不支持整索引扫描</quote>错误（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      确保 <filename>contrib/pgstattuple</filename> 函数及时响应取消中断（Tatsuhito Kasahara）
     </para>
    </listitem>

    <listitem>
     <para>
      让服务器启动正确处理 <function>shmget()</function> 对已存在的共享内存段返回 <literal>EINVAL</literal> 的情形（Tom）
     </para>

     <para>
      此行为曾在包括 OS X 在内的 BSD 衍生内核上观察到。它导致完全误导性的启动失败，抱怨共享内存请求大小过大。
     </para>
    </listitem>

    <listitem>
     <para>
      避免 Windows 上 syslogger 进程可能的崩溃（Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      更健壮地处理 Windows 注册表中不完整的时区信息（Magnus）
     </para>
    </listitem>

    <listitem>
     <para>
      更新已知 Windows 时区名称集合（Magnus）
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2010j
      for DST law changes in Argentina, Australian Antarctic, Bangladesh,
      Mexico, Morocco, Pakistan, Palestine, Russia, Syria, Tunisia;
      also historical corrections for Taiwan。
     </para>

     <para>
      另外，把 <literal>PKST</literal>（巴基斯坦夏令时）加入默认时区缩写集合。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-4-3">
  <title>版本 8.4.3</title>

  <note>
  <title>发行日期</title>
  <simpara>2010-03-15</simpara>
 </note>

  <para>
   本次发布包含对 8.4.2 的多方面修复。有关 8.4 大版本中新特性的信息，请参见<xref linkend="release-8-4">。.
  </para>

  <sect2>
   <title>迁移到版本 8.4.3</title>

   <para>
      对于运行 8.4.X 的用户，不需要进行转储/恢复。不过，如果你是从早于 8.4.2 的版本升级，请参阅<xref linkend="release-8-4-2">。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      新增配置参数 <varname>ssl_renegotiation_limit</varname>，控制对 SSL 连接进行会话密钥重协商的频率（Magnus）
     </para>

     <para>
      可以把它设为零以完全禁用重协商，这在使用有缺陷的 SSL 库时可能是必需的。特别是，某些厂商发布的 CVE-2009-3555 临时补丁会导致重协商尝试失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复后端启动期间可能的死锁（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复因未干净地处理 relcache 重载期间的错误而可能的崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复因使用指向缓存计划的悬空指针而可能的崩溃（Tatsuo）
     </para>
    </listitem>

    <listitem>
     <para>
      修复因过于激进地使 <command>ROLLBACK</command> 的缓存计划失效而可能的崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复试图从子事务启动失败中恢复时可能的崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复与使用保存点以及客户端编码不同于服务器编码相关的服务器内存泄漏（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 GIST 索引页分裂的恢复结束清理期间发出的不正确 WAL 数据（Yoichi Hirai）
     </para>

     <para>
      如果在完成未完成的 GIST 插入之后、在恢复结束清理期间崩溃，这会导致索引损坏，或者更可能在 WAL 重放期间出错。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 GIN 索引 WAL 重做清理方法中的缺陷（Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 GIN 索引搜索中扫描键的不正确比较（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <type>bit</type> 类型的 <function>substring()</function> 把任何负长度都视为<quote>字符串的其余全部</quote>（Tom）
     </para>

     <para>
      以前的编码只对 -1 这样处理，对其他负值会产生无效的结果值，可能导致崩溃（CVE-2010-0442）。
     </para>
    </listitem>

    <listitem>
     <para>
      修复整数到位串的转换：当输出位宽比给定整数宽出非 8 位倍数时，正确处理第一个小数字节（Tom Lane）
     </para>
    </listitem>

    <listitem>
     <para>
      修复若干病态缓慢的正则表达式匹配情形（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复试图内联返回包含已删除列的组合类型集合的 SQL 函数时出现的缺陷（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复试图更新组合类型数组列中某元素的字段时的缺陷（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      避免 <command>EXPLAIN</command> 需要打印 FieldStore 或赋值 ArrayRef 表达式时的失败（Tom）
     </para>

     <para>
      既然 <command>EXPLAIN VERBOSE</command> 试图打印计划节点目标列表，这些情形现在可能出现。
     </para>
    </listitem>

    <listitem>
     <para>
      避免某些情况下未加修饰的字面字符串出现在 <command>UNION</command>/<command>INTERSECT</command>/<command>EXCEPT</command> 内的子查询中时不必要的强制转换失败（Tom）
     </para>

     <para>
      这修复了某些在 8.4 之前可用的情形的回归。
     </para>
    </listitem>

    <listitem>
     <para>
      避免某些情况下整行 Var 的行类型包含已删除列时不希望出现的行类型兼容性检查失败（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复备份历史文件中的 <literal>STOP WAL LOCATION</literal> 条目：当结束位置恰好位于段边界时报告下一个 WAL 段的名称（Itagaki Takahiro）
     </para>
    </listitem>

    <listitem>
     <para>
      总是把目录 ID 传递给 <command>CREATE FOREIGN DATA WRAPPER</command> 中指定的选项验证器函数（Martin Pihlak）
     </para>
    </listitem>

    <listitem>
     <para>
      修复更多临时文件泄漏的情形（Heikki）
     </para>

     <para>
      这纠正了上一个小版本中引入的问题。失败的一种情形是返回集合的 plpgsql 函数在另一个函数的异常处理器中被调用。
     </para>
    </listitem>

    <listitem>
     <para>
      新增对 <literal>FULL JOIN ON FALSE</literal> 的支持（Tom）
     </para>

     <para>
      这防止了某些现在可被简化为常量假连接条件的查询相对 8.4 之前版本的回归。
     </para>
    </listitem>

    <listitem>
     <para>
      改进布尔变量情形的约束排除处理，特别是使排除带有 <quote>bool_column = false</quote> 约束的分区成为可能（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      防止把 <literal>INOUT</literal> 转换当作表示二进制兼容（Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      在警告无法授予或撤销列级权限时在消息中包含列名（Stephen Frost）
     </para>

     <para>
      这比以前更有用，并有助于防止 <command>REVOKE</command> 产生多条消息时的困惑，那些消息此前看起来像是重复的。
     </para>
    </listitem>

    <listitem>
     <para>
      读取 <filename>pg_hba.conf</filename> 及相关文件时，如果 <literal>@</literal> 出现在引号内，不要把 <literal>@something</literal> 当作文件包含请求；另外，永远不要把单独的 <literal>@</literal> 当作文件包含请求（Tom）
     </para>

     <para>
      这防止了角色名或数据库名以 <literal>@</literal> 开头时的古怪行为。如果需要包含路径名包含空格的文件，仍然可以做到，但必须写 <literal>@"/path to/file"</literal> 而不是把引号放在整个构造之外。
     </para>
    </listitem>

    <listitem>
     <para>
      防止在某些平台上把目录命名为 <filename>pg_hba.conf</filename> 及相关文件中的包含目标时出现无限循环（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>SSL_read</function> 或 <function>SSL_write</function> 失败而未设置 <varname>errno</varname> 时可能的无限循环（Tom）
     </para>

     <para>
      据报道这在某些 Windows 版本的 <application>openssl</application> 上可能发生。
     </para>
    </listitem>

    <listitem>
     <para>
      不允许在本地连接上使用 <acronym>GSSAPI</acronym> 认证，因为它需要主机名才能正确工作（Magnus）
     </para>
    </listitem>

    <listitem>
     <para>
      保护 <application>ecpg</application> 免遭应用程序意外释放字符串（Michael）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <application>ecpg</application> 在连接消失时报告正确的 SQLSTATE（Michael）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>psql</application> <literal>\d</literal> 输出中单元格内容的翻译（Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>psql</application> 的 <literal>numericlocale</literal> 选项，使其不在 latex 和 troff 输出格式中格式化不应格式化的字符串（Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>psql</application> 中每查询的小内存泄漏（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      当同时指定 <literal>ON_ERROR_STOP</literal> 和 <literal>--single-transaction</literal> 且隐含的 <command>COMMIT</command> 期间发生错误时，让 <application>psql</application> 返回正确的退出状态（3）（Bruce）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_dump</application> 对外部服务器权限的输出（Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      修复并行 <application>pg_restore</application> 中因依赖 ID 超出范围而可能的崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复组合列被设为 NULL 的一种情形下 plpgsql 的失败（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复从 PL/PerlU 调用 PL/Perl 函数或相反时可能的失败（Tim Bunce）
     </para>
    </listitem>

    <listitem>
     <para>
      在 PL/Python 中添加 <literal>volatile</literal> 标记，以避免可能的编译器特定错误行为（Zdenek Kotala）
     </para>
    </listitem>

    <listitem>
     <para>
      确保 PL/Tcl 完全初始化 Tcl 解释器（Tom）
     </para>

     <para>
      此疏漏唯一已知的症状是使用 Tcl 8.5 或更高版本时 Tcl 的 <literal>clock</literal> 命令行为异常。
     </para>
    </listitem>

    <listitem>
     <para>
      防止对在失败的事务或子事务内创建的门户运行 <function>ExecutorEnd</function>（Tom）
     </para>

     <para>
      已知这会在使用 <filename>contrib/auto_explain</filename> 时引起问题。
     </para>
    </listitem>

    <listitem>
     <para>
      防止在向 <function>dblink_build_sql_*</function> 函数指定过多键列时 <filename>contrib/dblink</filename> 崩溃（Rushabh Lathia、Joe Conway）
     </para>
    </listitem>

    <listitem>
     <para>
      允许 <filename>contrib/ltree</filename> 操作中使用零维数组（Tom）
     </para>

     <para>
      这种情形此前被作为错误拒绝，但把它与零元素数组同样处理更方便。特别是，这避免了当 <type>ltree</type> 操作应用于 <literal>ARRAY(SELECT ...)</literal> 的结果而子查询不返回行时不必要的失败。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/xml2</filename> 中因草率内存管理导致的若干崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      让 <filename>contrib/xml2</filename> 在 Windows 上的构建更健壮（Andrew）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 Windows 信号处理中的竞态条件（Radu Ilie）
     </para>

     <para>
      此缺陷的一个已知症状是 <structname>pg_listener</structname> 中的行可能在重负载下被丢弃。
     </para>
    </listitem>

    <listitem>
     <para>
      让 configure 脚本在 C 编译器不提供可用的 64 位整数数据类型时报告失败（Tom）
     </para>

     <para>
      这种情形已经损坏了一段时间，似乎不再值得支持，因此改为在 configure 时拒绝它。
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2010e
      for DST law changes in Bangladesh, Chile, Fiji, Mexico, Paraguay, Samoa。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-4-2">
  <title>版本 8.4.2</title>

  <note>
  <title>发行日期</title>
  <simpara>2009-12-14</simpara>
 </note>

  <para>
   本次发布包含对 8.4.1 的多方面修复。有关 8.4 大版本中新特性的信息，请参见<xref linkend="release-8-4">。.
  </para>

  <sect2>
   <title>迁移到版本 8.4.2</title>

   <para>
      对于运行 8.4.X 的用户，不需要进行转储/恢复。不过，如果你有任何哈希索引，应当在更新到 8.4.2 后对它们执行 <command>REINDEX</command>，以修复可能的损坏。
     </para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      防范索引函数更改会话局部状态导致的间接安全威胁（Gurjeet Singh、Tom）
     </para>

     <para>
      此变更防止据称不可变的索引函数可能破坏超级用户的会话（CVE-2009-4136）。
     </para>
    </listitem>

    <listitem>
     <para>
      拒绝在公共名（CN）字段中包含嵌入空字节的 SSL 证书（Magnus）
     </para>

     <para>
      这防止在 SSL 验证期间证书与服务器或客户端名称的非预期匹配（CVE-2009-4034）。
     </para>
    </listitem>

    <listitem>
     <para>
      修复哈希索引损坏（Tom）
     </para>

     <para>
      8.4 中让哈希索引按哈希值排序保存条目的变更没有更新桶分裂和压缩例程以保持该顺序。因此应用这两种操作之一可能导致索引永久损坏，即搜索可能找不到实际存在的条目。为处理此问题，建议在安装此更新后对你可能拥有的任何哈希索引执行 <literal>REINDEX</literal>。
     </para>
    </listitem>

    <listitem>
     <para>
      修复后端启动时缓存初始化期间可能的崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      避免在空同义词词典上崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      防止信号在不安全的时刻中断 <literal>VACUUM</literal>（Alvaro）
     </para>

     <para>
      此修复防止了 <literal>VACUUM FULL</literal> 在已提交其元组移动后被取消时的 PANIC，以及普通 <literal>VACUUM</literal> 在截断表之后被中断时的瞬态错误。
     </para>
    </listitem>

    <listitem>
     <para>
      修复哈希表大小计算中因整数溢出而可能的崩溃（Tom）
     </para>

     <para>
      这可能发生在规划器对哈希连接结果大小的估计极大时。
     </para>
    </listitem>

    <listitem>
     <para>
      修复试图对内部依赖对象执行 <literal>DROP</literal> 时的崩溃（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <type>inet</type>/<type>cidr</type> 比较中非常罕见的崩溃（Chris Mikkelson）
     </para>
    </listitem>

    <listitem>
     <para>
      确保预备事务所持有的共享元组级锁不被忽略（Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      修复在子事务内访问的游标所用临时文件被过早删除的问题（Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      修复轮转到新 CSV 日志文件时 syslogger 进程的内存泄漏（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复重新解析 <filename>pg_hba.conf</filename> 时 postmaster 的内存泄漏（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 Windows 权限降级逻辑（Jesse Morris）
     </para>

     <para>
      这修复了数据库在 Windows 上启动失败的一些情形，这些情形常伴随误导性错误消息，例如<quote>找不到匹配的 postgres 可执行文件</quote>。
     </para>
    </listitem>

    <listitem>
     <para>
      让主查询中的 <literal>FOR UPDATE/SHARE</literal> 不传播到 <literal>WITH</literal> 查询中（Tom）
     </para>

     <para>
      例如，在
      <programlisting>
WITH w AS (SELECT * FROM foo) SELECT * FROM w, bar ... FOR UPDATE
      </programlisting>
      中，<literal>FOR UPDATE</literal> 现在将影响 <literal>bar</literal> 而不影响 <literal>foo</literal>。这比原来 8.4 的行为更有用也更一致，原来的行为试图把 <literal>FOR UPDATE</literal> 传播到 <literal>WITH</literal> 查询中，但由于各种实现限制总是失败。这也遵循 <literal>WITH</literal> 查询如同独立于主查询一样执行的设计规则。
     </para>
    </listitem>

    <listitem>
     <para>
      修复一个 <literal>WITH RECURSIVE</literal> 查询紧邻另一个之内时的缺陷（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复哈希索引中的并发缺陷（Tom）
     </para>

     <para>
      并发插入可能导致索引扫描瞬时报告错误结果。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 GiST 索引页分裂的不正确逻辑：当分裂依赖于索引的非首列时（Paul Ramsey）
     </para>
    </listitem>

    <listitem>
     <para>
      修复启用 <literal>fastupdate</literal> 的多列 GIN 索引搜索结果错误的问题（Teodor）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 GIN 索引 WAL 条目创建中的缺陷（Tom）
     </para>

     <para>
      这些缺陷在 <varname>full_page_writes</varname> 开启时被掩盖，但在它关闭时，如果下一个检查点之前发生崩溃，WAL 重放失败是必然的。
     </para>
    </listitem>

    <listitem>
     <para>
      如果回收或删除旧 WAL 文件在检查点结束时失败，不要报错退出（Heikki）
     </para>

     <para>
      更好的做法是把该问题视为非致命并允许检查点完成。未来的检查点会重试删除。这类问题在正常运行中不应出现，但已知可能由设计不当的 Windows 反病毒和备份软件引起。
     </para>
    </listitem>

    <listitem>
     <para>
      确保 WAL 文件不会在 Windows 上被重复归档（Heikki）
     </para>

     <para>
      这是其他进程干扰删除不再需要的文件时可能出现的另一种症状。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PAM 口令处理使其更健壮（Tom）
     </para>

     <para>
      已知以前的代码在 Linux <literal>pam_krb5</literal> PAM 模块与 Microsoft Active Directory 域控制器组合下失败。它在其他地方可能也有问题，因为它对 PAM 栈会传递给它什么参数做了无根据的假设。
     </para>
    </listitem>

    <listitem>
     <para>
      提高 GSSAPI 和 SSPI 认证方法的最大认证令牌（Kerberos 票据）大小（Ian Turner）
     </para>

     <para>
      虽然旧的 2000 字节限制对 Unix Kerberos 实现绰绰有余，Windows 域控制器签发的票据可能大得多。
     </para>
    </listitem>

    <listitem>
     <para>
      确保域约束在 <literal>ARRAY[...]::domain</literal>（域基于数组类型）这类构造中得到强制执行（Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      修复某些以组合类型列作为外键的情形下的外键逻辑（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      确保游标的快照在创建后不被修改（Alvaro）
     </para>

     <para>
      如果同一事务中稍后的操作修改了游标本应返回的数据，这可能导致游标交付错误结果。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>CREATE TABLE</literal>，使其正确合并来自不同继承父表的默认表达式（Tom）
     </para>

     <para>
      这以前可用，但在 8.4 中损坏了。
     </para>
    </listitem>

    <listitem>
     <para>
      重新启用对序列的访问统计信息收集（Akira Kurosawa）
     </para>

     <para>
      这以前可用，但在 8.3 中损坏了。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <literal>CREATE OR REPLACE FUNCTION</literal> 期间所有权依赖的处理（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复对 <literal>WHERE</literal> <replaceable>x</replaceable>=<replaceable>x</replaceable> 条件的不正确处理（Tom）
     </para>

     <para>
      某些情况下它们可能被当作冗余而忽略，但它们并非冗余——它们等价于 <replaceable>x</replaceable> <literal>IS NOT NULL</literal>。
     </para>
    </listitem>

    <listitem>
     <para>
      修复对文本上相同的易变表达式用哈希聚合实现 <literal>DISTINCT</literal> 时的错误计划构造（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复易变 <literal>SELECT DISTINCT ON</literal> 表达式的断言失败（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <function>ts_stat()</function>，使其在空 <type>tsvector</type> 值上不失败（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      让文本搜索解析器接受 XML 属性中的下划线（Peter）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <type>xml</type> 二进制输入的编码处理（Heikki）
     </para>

     <para>
      如果 XML 头未指定编码，现在默认采用 UTF-8；以前的处理不一致。
     </para>
    </listitem>

    <listitem>
     <para>
      修复从 <literal>plperl</literal> 调用 <literal>plperlu</literal> 或相反时的缺陷（Tom）
     </para>

     <para>
      内层函数的错误退出可能因未能为外层函数重新选择正确的 Perl 解释器而导致崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      修复重定义 PL/Perl 函数时的会话期内存泄漏（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      确保 Perl 数组在由返回集合的 PL/Perl 函数返回时被正确转换为 <productname>PostgreSQL</productname> 数组（Andrew Dunstan、Abhijit Menon-Sen）
     </para>

     <para>
      对非返回集合的函数这早已正确工作。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 PL/Python 异常处理中的罕见崩溃（Peter）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>ecpg</application> 中 <literal>DECLARE CURSOR</literal> 语句内注释的问题（Michael）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>ecpg</application>，使其不把最近新增的关键字当作保留字（Tom）
     </para>

     <para>
      这影响关键字 <literal>CALLED</literal>、<literal>CATALOG</literal>、<literal>DEFINER</literal>、<literal>ENUM</literal>、<literal>FOLLOWING</literal>、<literal>INVOKER</literal>、<literal>OPTIONS</literal>、<literal>PARTITION</literal>、<literal>PRECEDING</literal>、<literal>RANGE</literal>、<literal>SECURITY</literal>、<literal>SERVER</literal>、<literal>UNBOUNDED</literal> 和 <literal>WRAPPER</literal>。
     </para>
    </listitem>

    <listitem>
     <para>
      重新允许 <application>psql</application> 的 <literal>\df</literal> 函数名参数中使用正则表达式特殊字符（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      在 <filename>contrib/fuzzystrmatch</filename> 中，修正非默认代价下 <function>levenshtein</function> 距离的计算（Marcin Mank）
     </para>
    </listitem>

    <listitem>
     <para>
      在 <filename>contrib/pg_standby</filename> 中，禁用在 Windows 上用信号触发故障转移（Fujii Masao）
     </para>

     <para>
      这从未有过任何用处，因为 Windows 没有 Unix 式信号，但最近的变更使它实际上崩溃。
     </para>
    </listitem>

    <listitem>
     <para>
      在 <filename>contrib/vacuumdb</filename> 生成的 <literal>VACUUM</literal> 命令中把 <literal>FREEZE</literal> 和 <literal>VERBOSE</literal> 选项按正确顺序排列（Heikki）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/dblink</filename> 遇到错误时可能的连接泄漏（Tatsuhito Kasahara）
     </para>
    </listitem>

    <listitem>
     <para>
      确保 <application>psql</application> 的 flex 模块用正确的系统头文件定义编译（Tom）
     </para>

     <para>
      这修复了在 <literal>--enable-largefile</literal> 导致生成代码不兼容变更的平台上的构建失败。
     </para>
    </listitem>

    <listitem>
     <para>
      让 postmaster 忽略连接请求包中的任何 <literal>application_name</literal> 参数，以改进与未来 libpq 版本的兼容性（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      更新时区缩写文件以符合当前现实（Joachim Wieland）
     </para>

     <para>
      这包括把 <literal>IDT</literal> 加入默认时区缩写集合。
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2009s
      for DST law changes in Antarctica, Argentina, Bangladesh, Fiji,
      Novokuznetsk, Pakistan, Palestine, Samoa, Syria; also historical
      corrections for Hong Kong。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-4-1">
  <title>版本 8.4.1</title>

  <note>
  <title>发行日期</title>
  <simpara>2009-09-09</simpara>
 </note>

  <para>
   本次发布包含对 8.4 的多方面修复。有关 8.4 大版本中新特性的信息，请参见<xref linkend="release-8-4">。.
  </para>

  <sect2>
   <title>迁移到版本 8.4.1</title>

   <para>
    对于运行 8.4.X 的用户，不需要进行转储/恢复。</para>

  </sect2>

  <sect2>
   <title>变更</title>

   <itemizedlist>

    <listitem>
     <para>
      修复归档恢复结束时的 WAL 页头初始化（Heikki）
     </para>

     <para>
      这可能导致后续归档恢复中无法处理 WAL。
     </para>
    </listitem>

    <listitem>
     <para>
      修复<quote>恢复期间不能创建新的 WAL 条目</quote>错误（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      修复崩溃后可能使已过期行可见的问题（Tom）
     </para>

     <para>
      此缺陷涉及页状态位在服务器崩溃后可能未被正确设置。
     </para>
    </listitem>

    <listitem>
     <para>
      不允许在安全定义者函数内使用 <command>RESET ROLE</command> 和 <command>RESET SESSION AUTHORIZATION</command>（Tom、Heikki）
     </para>

     <para>
      这覆盖了先前禁止在安全定义者函数内使用 <command>SET ROLE</command> 和 <command>SET SESSION AUTHORIZATION</command> 的补丁中遗漏的一种情形。（参见 CVE-2007-6600）
     </para>
    </listitem>

    <listitem>
     <para>
      把对已装载的可装载模块的 <command>LOAD</command> 变为无操作（Tom）
     </para>

     <para>
      此前 <command>LOAD</command> 会试图卸载并重新装载模块，但这不安全也不太有用。
     </para>
    </listitem>

    <listitem>
     <para>
      让窗口函数的 <literal>PARTITION BY</literal> 和 <literal>ORDER BY</literal> 项总是被解释为简单表达式（Tom）
     </para>

     <para>
      在 8.4.0 中这些列表按照用于顶层 <literal>GROUP BY</literal> 和 <literal>ORDER BY</literal> 列表的规则解析。但这不符合 SQL 标准，而且可能导致循环。
     </para>
    </listitem>

    <listitem>
     <para>
      修复半连接规划中的若干错误（Tom）
     </para>

     <para>
      这导致某些 <literal>IN</literal> 或 <literal>EXISTS</literal> 与另一个连接一起使用的情形下查询结果错误。
     </para>
    </listitem>

    <listitem>
     <para>
      修复对处于外连接之内的子查询的整行引用的处理（Tom）
     </para>

     <para>
      例子是 <literal>SELECT COUNT(ss.*) FROM ... LEFT JOIN (SELECT ...) ss ON ...</literal>。这里，对于空扩展的连接行，<literal>ss.*</literal> 会被当作 <literal>ROW(NULL,NULL,...)</literal>，这与简单 NULL 不同。现在它被当作简单 NULL。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 Windows 共享内存分配代码（Tsutomu Yamada、Magnus）
     </para>

     <para>
      此缺陷导致常被报告的<quote>无法重新附加到共享内存</quote>错误消息。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 plperl 的区域设置处理（Heikki）
     </para>

     <para>
      此缺陷可能导致调用 plperl 函数时服务器的区域设置改变，进而导致数据损坏。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 reloptions 的处理，确保设置一个选项不会强制其他选项取默认值（Itagaki Takahiro）
     </para>
    </listitem>

    <listitem>
     <para>
      确保<quote>快速关闭</quote>请求会强制终止打开的会话，即使<quote>智能关闭</quote>已在进行中（Fujii Masao）
     </para>
    </listitem>

    <listitem>
     <para>
      避免 <literal>GROUP BY</literal> 查询中 <function>array_agg()</function> 的内存泄漏（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      把 <function>to_char(..., 'TH')</function> 在 <literal>'HH'</literal>/<literal>'HH12'</literal> 下当作大写序数后缀（Heikki）
     </para>

     <para>
      它以前被当作 <literal>'th'</literal>（小写）处理。
     </para>
    </listitem>

    <listitem>
     <para>
      对 <type>time</type> 和 <type>time with time zone</type> 输入，在 <function>EXTRACT(second)</function> 和 <function>EXTRACT(milliseconds)</function> 的结果中包含小数部分（Tom）
     </para>

     <para>
      这在浮点日期时间配置下一直正常，但在整数日期时间代码中损坏了。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <replaceable>x</replaceable> 超过 200 万且使用整数日期时间时 <literal>INTERVAL '<replaceable>x</replaceable> ms'</literal> 的溢出（Alex Hunsaker）
     </para>
    </listitem>

    <listitem>
     <para>
      改进索引扫描中处理 TOAST 值时的性能（Tom）
     </para>

     <para>
      这对 <ulink url="http://postgis.refractions.net/">PostGIS</ulink> 特别有用。
     </para>
    </listitem>

    <listitem>
     <para>
      修复一个使 <varname>commit_delay</varname> 失效的笔误（Jeff Janes）
     </para>
    </listitem>

    <listitem>
     <para>
      如果服务器以安静模式启动，把早期启动消息输出到 <filename>postmaster.log</filename>（Tom）
     </para>

     <para>
      此前这类错误消息被丢弃，导致调试困难。
     </para>
    </listitem>

    <listitem>
     <para>
      移除已翻译的 FAQ（Peter）
     </para>

     <para>
      它们现在位于 <ulink url="http://wiki.postgresql.org/wiki/FAQ">wiki</ulink>。主 FAQ 此前已被移到 wiki。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_ctl</application>，使其在 <filename>postgresql.conf</filename> 为空时不进入无限循环（Jeff Davis）
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <application>pg_dump</application> 的 <literal>--binary-upgrade</literal> 模式中的若干错误（Bruce、Tom）
     </para>

     <para>
      <literal>pg_dump --binary-upgrade</literal> 由 pg_migrator 使用。
     </para>
    </listitem>

    <listitem>
     <para>
      修复 <filename>contrib/xml2</filename> 的 <function>xslt_process()</function>，使其正确处理最大参数数量（二十个）（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <application>libpq</application> 从 <command>COPY FROM STDIN</command> 期间错误中恢复的代码的健壮性（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      避免在两个库都安装时包含相互冲突的 readline 和 editline 头文件（Zdenek Kotala）
     </para>
    </listitem>

    <listitem>
     <para>
      绕过 gcc 在某些平台上导致<quote>浮点异常</quote>而非<quote>除以零</quote>的缺陷（Tom）
     </para>
    </listitem>

    <listitem>
     <para>
      把时区数据文件更新为 <application>tzdata</application> 版本 2009l
      for DST law changes in Bangladesh, Egypt, Mauritius。
     </para>
    </listitem>

   </itemizedlist>

  </sect2>
 </sect1>

 <sect1 id="release-8-4">
  <title>版本 8.4</title>

  <note>
  <title>发行日期</title>
  <simpara>2009-07-01</simpara>
 </note>

  <sect2>
   <title>概述</title>

   <para>
      经过多年开发，<productname>PostgreSQL</productname> 在许多领域已经功能完备。此发行版展示了添加特性的针对性方法（例如认证、监控、空间复用），并增加了后续 SQL 标准中定义的能力。主要的增强领域是：
     </para>

   <itemizedlist>

    <!-- This list duplicates items below, but without authors or details-->

    <listitem>
     <para>
      窗口函数
     </para>
    </listitem>

    <listitem>
     <para>
      公共表表达式和递归查询
     </para>
    </listitem>

    <listitem>
     <para>
      函数的默认参数和可变参数
     </para>
    </listitem>

    <listitem>
     <para>
      并行恢复
     </para>
    </listitem>

    <listitem>
     <para>
      列权限
     </para>
    </listitem>

    <listitem>
     <para>
      逐数据库的区域设置
     </para>
    </listitem>

    <listitem>
     <para>
      改进的哈希索引
     </para>
    </listitem>

    <listitem>
     <para>
      改进 <literal>EXISTS</literal> 和 <literal>NOT EXISTS</literal> 查询的连接性能
     </para>
    </listitem>

    <listitem>
     <para>
      更易用的热备
     </para>
    </listitem>

    <listitem>
     <para>
      自动调整空闲空间映射的大小
     </para>
    </listitem>

    <listitem>
     <para>
      可见性映射（大幅降低变化缓慢的表的清理开销）
     </para>
    </listitem>

    <listitem>
     <para>
      版本感知的 psql（反斜杠命令可针对较旧的服务器工作）
     </para>
    </listitem>

    <listitem>
     <para>
      支持用 SSL 证书进行用户认证
     </para>
    </listitem>

    <listitem>
     <para>
      逐函数的运行时统计
     </para>
    </listitem>

    <listitem>
     <para>
      在 psql 中轻松编辑函数
     </para>
    </listitem>

    <listitem>
     <para>
      新增 contrib 模块：pg_stat_statements、auto_explain、citext、btree_gin
     </para>
    </listitem>

   </itemizedlist>

   <para>
     上述各项在下面的章节中有更详细的说明。
    </para>

  </sect2>

  <sect2>
   <title>迁移到版本 8.4</title>

   <para>
      希望从任何以前的版本迁移数据的用户，需要使用 <application>pg_dump</application> 进行转储/恢复。
     </para>

   <para>
      请注意以下不兼容之处：
     </para>

   <sect3>
    <title>常规</title>
    <itemizedlist>

     <listitem>
      <para>
      默认使用 64 位整数日期时间（Neil Conway）
     </para>

      <para>
      此前这由 <application>configure</application> 的 <option>--enable-integer-datetimes</option> 选项选择。要保留旧行为，请用 <option>--disable-integer-datetimes</option> 构建。
     </para>
     </listitem>

     <listitem>
      <para>
      移除 <application>ipcclean</application> 工具命令（Bruce）
     </para>

      <para>
      该工具只在少数平台上有效。用户应改用其操作系统的工具。
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title>服务器设置</title>
    <itemizedlist>

     <listitem>
      <para>
      把 <literal>log_min_messages</literal> 的默认设置改为 <literal>warning</literal>（以前是 <literal>notice</literal>），以减少日志文件量（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      把 <literal>max_prepared_transactions</literal> 的默认设置改为零（以前是 5）（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      让 <literal>debug_print_parse</literal>、<literal>debug_print_rewritten</literal> 和 <literal>debug_print_plan</literal> 的输出以 <literal>LOG</literal> 消息级别出现，而不是以前的 <literal>DEBUG1</literal>（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      让 <literal>debug_pretty_print</literal> 默认为 <literal>on</literal>（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      移除 <varname>explain_pretty_print</varname> 参数（不再需要）（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      让 <varname>log_temp_files</varname> 仅超级用户可设置，与其他日志选项一样（Simon Riggs）
     </para>
     </listitem>

     <listitem>
      <para>
      移除当 <literal>log_filename</literal> 中没有 <literal>%</literal> 转义时自动追加纪元时间戳的行为（Robert Haas）
     </para>

      <para>
      此变更是因为一些用户想要固定的日志文件名，以便与外部日志轮转工具配合使用。
     </para>
     </listitem>

     <listitem>
      <para>
      从 <filename>recovery.conf</filename> 中移除 <varname>log_restartpoints</varname>；改用 <varname>log_checkpoints</varname>（Simon）
     </para>
     </listitem>

     <listitem>
      <para>
      移除 <varname>krb_realm</varname> 和 <varname>krb_server_hostname</varname>；它们现在改在 <filename>pg_hba.conf</filename> 中设置（Magnus）
     </para>
     </listitem>

     <listitem>
      <para>
      <link linkend="release-8-4-pg-hba-conf"><filename>pg_hba.conf</filename></link> 中也有重大变更，如下所述。
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title>查询</title>

    <itemizedlist>

     <listitem>
      <para>
      更改 <command>TRUNCATE</command> 和 <command>LOCK</command>，使其应用于指定表的子表（Peter）
     </para>

      <para>
      这些命令现在接受阻止处理子表的 <literal>ONLY</literal> 选项；如果需要旧行为，必须使用此选项。
     </para>
     </listitem>

     <listitem>
      <para>
      <command>SELECT DISTINCT</command> 和 <literal>UNION</literal>/<literal>INTERSECT</literal>/<literal>EXCEPT</literal> 不再总是产生排序的输出（Tom）
     </para>

      <para>
      此前，这类查询总是通过排序/唯一处理（即先排序再删除相邻重复项）来删除重复行。现在它们可以用哈希实现，这不会产生排序的输出。如果应用程序依赖输出按排序顺序，推荐的解决办法是添加 <literal>ORDER BY</literal> 子句。作为短期变通，可以通过禁用 <literal>enable_hashagg</literal> 恢复以前的行为，但那是非常耗费性能的解决办法。不过 <literal>SELECT DISTINCT ON</literal> 从不使用哈希，因此其行为不变。
     </para>
     </listitem>

     <listitem>
      <para>
      强制子表从父表继承 <literal>CHECK</literal> 约束（Alex Hunsaker、Nikhil Sontakke、Tom）
     </para>

      <para>
      此前可以从子表删除这样的约束，使违反约束的行在扫描父表时可见。这被认为不一致，也违背 SQL 标准。
     </para>
     </listitem>

     <listitem>
      <para>
      不允许负的 <literal>LIMIT</literal> 或 <literal>OFFSET</literal> 值，而不是把它们当作零（Simon）
     </para>
     </listitem>

     <listitem>
      <para>
      不允许在事务块之外使用 <command>LOCK TABLE</command>（Tom）
     </para>

      <para>
      这样的操作毫无用处，因为锁会被立即释放。
     </para>
     </listitem>

     <listitem>
      <para>
      序列现在包含一个额外的 <structfield>start_value</structfield> 列（Zoltan Boszormenyi）
     </para>

      <para>
      这支持 <command>ALTER SEQUENCE ... RESTART</command>。
     </para>
     </listitem>

    </itemizedlist>

   </sect3>


   <sect3>
    <title>函数和操作符</title>

    <itemizedlist>

     <listitem>
      <para>
      让 <type>numeric</type> 零的分数次幂返回 <literal>0</literal> 而不是抛出错误，并让 <type>numeric</type> 零的零次幂返回 <literal>1</literal> 而不是出错（Bruce）
     </para>

      <para>
      这与 <type>float8</type> 长期的行为一致。
     </para>
     </listitem>

     <listitem>
      <para>
      允许浮点值的一元减产生负零（Tom）
     </para>

      <para>
      更改后的行为更符合 <acronym>IEEE</acronym> 标准。
     </para>
     </listitem>

     <listitem>
      <para>
      如果转义字符是 <literal>LIKE</literal> 模式的最后一个字符（即它没有可转义的内容），抛出错误（Tom）
     </para>

      <para>
      此前这样的转义字符被静默忽略，因此可能掩盖应用程序逻辑错误。
     </para>
     </listitem>

     <listitem>
      <para>
      移除以前用于 <literal>LIKE</literal> 索引比较的 <literal>~=~</literal> 和 <literal>~&lt;&gt;~</literal> 操作符（Tom）
     </para>

      <para>
      模式索引现在使用常规的相等操作符。
     </para>
     </listitem>

     <listitem>
      <para>
      <function>xpath()</function> 现在把它的参数不做任何更改地传递给 <application>libxml</application>（Andrew）
     </para>

      <para>
      这意味着 XML 参数必须是格式良好的 XML 文档。以前的编码试图允许 XML 片段，但效果不好。
     </para>
     </listitem>

     <listitem>
      <para>
      让 <function>xmlelement()</function> 像格式化内容值一样格式化属性值（Peter）
     </para>

      <para>
      此前属性值按照常规 SQL 输出行为格式化，这有时与 XML 规则相悖。
     </para>
     </listitem>

     <listitem>
      <para>
      重写使用 <application>libxml</application> 的函数的内存管理（Tom）
     </para>

      <para>
      此变更应能避免在 PL/Perl 和其他附加代码中使用 <application>libxml</application> 的一些兼容性问题。
     </para>
     </listitem>

     <listitem>
      <para>
      为哈希函数采用更快的算法（Kenneth Marshall，基于 Bob Jenkins 的工作）
     </para>

      <para>
      许多内建哈希函数现在在小端和大端平台上产生不同的结果。
     </para>
     </listitem>

    </itemizedlist>

    <sect4>
     <title>时间函数和操作符</title>

     <itemizedlist>

      <listitem>
       <para>
      <varname>DateStyle</varname> 不再控制 <type>interval</type> 输出格式化；改为有一个新变量 <varname>IntervalStyle</varname>（Ron Mayer）
     </para>
      </listitem>

      <listitem>
       <para>
      改进 <type>timestamp</type> 和 <type>interval</type> 输出中小数秒处理的一致性（Ron Mayer）
     </para>

       <para>
      这可能导致显示的小数位数与以前不同，或者四舍五入而不是截断。
     </para>
      </listitem>

      <listitem>
       <para>
      让 <function>to_char()</function> 的本地化月/日名称依赖于 <varname>LC_TIME</varname> 而不是 <varname>LC_MESSAGES</varname>（Euler Taveira de Oliveira）
     </para>
      </listitem>

      <listitem>
       <para>
      让 <function>to_date()</function> 和 <function>to_timestamp()</function> 更一致地对无效输入报告错误（Brendan Jurd）
     </para>

       <para>
      以前的版本常常忽略或静默误读与格式字符串不匹配的输入。这类情形现在将导致错误。
     </para>
      </listitem>

      <listitem>
       <para>
      修复 <function>to_timestamp()</function>，使其不要求上/下午（<literal>AM</literal>/<literal>PM</literal>）和纪元（<literal>BC</literal>/<literal>AD</literal>）格式标志的大/小写匹配（Brendan Jurd）
     </para>

       <para>
      例如，输入值 <literal>ad</literal> 现在匹配格式字符串 <literal>AD</literal>。
     </para>
      </listitem>

     </itemizedlist>

    </sect4>

   </sect3>

  </sect2>

  <sect2>
   <title>变更</title>

   <para>
      下面你将找到 <productname>PostgreSQL</productname> 8.4 与上一个主版本之间变更的详细说明。
     </para>

   <sect3>
    <title>性能</title>
    <itemizedlist>

     <listitem>
      <para>
      改进优化器统计信息计算（Jan Urbanski、Tom）
     </para>

      <para>
      特别是，全文搜索操作符的估计得到极大改进。
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <command>SELECT DISTINCT</command> 和 <literal>UNION</literal>/<literal>INTERSECT</literal>/<literal>EXCEPT</literal> 使用哈希（Tom）
     </para>

      <para>
      这意味着这类查询不再自动产生排序的输出。
     </para>
     </listitem>

     <listitem>
      <para>
      创建半连接和反连接的显式概念（Tom）
     </para>

      <para>
      此工作把我们以前对 <literal>IN (SELECT ...)</literal> 子句的临时处理形式化，并将其扩展到 <literal>EXISTS</literal> 和 <literal>NOT EXISTS</literal> 子句。它应能显著改善 <literal>EXISTS</literal> 和 <literal>NOT EXISTS</literal> 查询的规划。一般而言，逻辑上等价的 <literal>IN</literal> 和 <literal>EXISTS</literal> 子句现在应有相近的性能，而以前 <literal>IN</literal> 常常胜出。
     </para>
     </listitem>

     <listitem>
      <para>
      改进外连接之下子查询的优化（Tom）
     </para>

      <para>
      此前，如果子查询或视图出现在外连接的可空侧且其结果列表中包含非严格表达式（例如常量），就无法很好地优化。
     </para>
     </listitem>

     <listitem>
      <para>
      通过使用 Boyer-Moore-Horspool 搜索改进 <function>text_position()</function> 及相关函数的性能（David Rowley）
     </para>

      <para>
      这对长搜索模式特别有帮助。
     </para>
     </listitem>

     <listitem>
      <para>
      通过仅在请求时写入统计信息收集文件来降低其写入的 I/O 负载（Martin Pihlak）
     </para>
     </listitem>

     <listitem>
      <para>
      改进批量插入的性能（Robert Haas、Simon）
     </para>
     </listitem>

     <listitem>
      <para>
      把 <varname>default_statistics_target</varname> 的默认值从 <literal>10</literal> 提高到 <literal>100</literal>（Greg Sabino Mullane、Tom）
     </para>

      <para>
      最大值也从 <literal>1000</literal> 提高到 <literal>10000</literal>。
     </para>
     </listitem>

     <listitem>
      <para>
      在涉及继承或 <literal>UNION ALL</literal> 的查询中默认执行 <varname>constraint_exclusion</varname> 检查（Tom）
     </para>

      <para>
      新增了 <varname>constraint_exclusion</varname> 设置 <literal>partition</literal> 来指定此行为。
     </para>
     </listitem>

     <listitem>
      <para>
      允许位图索引扫描的 I/O 预读（Greg Stark）
     </para>

      <para>
      预读量由 <varname>effective_io_concurrency</varname> 控制。此特性仅在内核支持 <function>posix_fadvise()</function> 时可用。
     </para>
     </listitem>

     <listitem>
      <para>
      在 <literal>FROM</literal> 子句中内联简单的返回集合的 <acronym>SQL</acronym> 函数（Richard Rowell）
     </para>
     </listitem>

     <listitem>
      <para>
      通过为外部关系中特别常见的连接键值提供特殊情形，改进多批哈希连接的性能（Bryce Cutt、Ramon Lawrence）
     </para>
     </listitem>

     <listitem>
      <para>
      通过抑制<quote>物理 tlist</quote>优化减少多批哈希连接中的临时数据量（Michael Henderson、Ramon Lawrence）
     </para>
     </listitem>

     <listitem>
      <para>
      避免 <command>CREATE INDEX CONCURRENTLY</command> 期间等待空闲在事务中的会话（Simon）
     </para>
     </listitem>

     <listitem>
      <para>
      改进共享缓存失效的性能（Tom）
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title>服务器</title>

    <sect4>
     <title>设置</title>

     <itemizedlist>

      <listitem>
       <para>
      把许多 <filename>postgresql.conf</filename> 设置转换为枚举值，使 <literal>pg_settings</literal> 能显示有效值（Magnus）
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <varname>cursor_tuple_fraction</varname> 参数，控制规划器假定会被获取的游标行的比例（Robert Hell）
     </para>
      </listitem>

      <listitem>
       <para>
      允许 <filename>postgresql.conf</filename> 中自定义变量类名称包含下划线（Tom）
     </para>
      </listitem>

     </itemizedlist>

    </sect4>

    <sect4>
     <title>认证与安全</title>
     <itemizedlist>

      <listitem>
       <para>
      移除对（不安全的）<literal>crypt</literal> 认证方法的支持（Magnus）
     </para>

       <para>
      这实际上使 <productname>PostgreSQL</productname> 7.2 之前的客户端库过时，因为它们不再有任何可用的非明文口令方法。
     </para>
      </listitem>

      <listitem>
       <para>
      支持 <filename>pg_ident.conf</filename> 中的正则表达式（Magnus）
     </para>
      </listitem>

      <listitem>
       <para>
      允许不重启 postmaster 即可更改 <productname>Kerberos</productname>/<acronym>GSSAPI</acronym> 参数（Magnus）
     </para>
      </listitem>

      <listitem>
       <para>
      支持服务器证书文件中的 <acronym>SSL</acronym> 证书链（Andrew Gierth）
     </para>

       <para>
      包含完整证书链使客户端能够验证证书，而无需在本地存储中拥有所有中间 CA 证书——商业 CA 常是这种情形。
     </para>
      </listitem>

      <listitem>
       <para>
      对 <literal>MD5</literal> 认证与 <varname>db_user_namespace</varname> 同时启用的组合报告适当的错误消息（Bruce）
     </para>
      </listitem>
     </itemizedlist>

    </sect4>

    <sect4 id="release-8-4-pg-hba-conf">
     <title><filename>pg_hba.conf</></title>
     <itemizedlist>

      <listitem>
       <para>
      把所有认证选项改为使用 <literal>name=value</literal> 语法（Magnus）
     </para>

       <para>
      这对 <literal>ldap</literal>、<literal>pam</literal> 和 <literal>ident</literal> 认证方法做了不兼容的变更。所有使用这些方法的 <filename>pg_hba.conf</filename> 条目都需要用新格式重写。
     </para>
      </listitem>

      <listitem>
       <para>
      移除 <literal>ident sameuser</literal> 选项，改为在未指定 usermap 时以该行为为默认（Magnus）
     </para>
      </listitem>

      <listitem>
       <para>
      允许所有外部认证方法使用 usermap 参数（Magnus）
     </para>

       <para>
      此前 usermap 只在 <literal>ident</literal> 认证中受支持。
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <literal>clientcert</literal> 选项，控制是否请求客户端证书（Magnus）
     </para>

       <para>
      此前这由服务器数据目录中是否存在根证书文件控制。
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <literal>cert</literal> 认证方法，允许通过 <acronym>SSL</acronym> 证书进行<emphasis>用户</emphasis>认证（Magnus）
     </para>

       <para>
      此前 <acronym>SSL</acronym> 证书只能验证客户端拥有某证书的访问权，而不能认证用户。
     </para>
      </listitem>

      <listitem>
       <para>
      允许在 <filename>pg_hba.conf</filename> 中指定 <literal>krb5</literal>、<literal>gssapi</literal> 和 <literal>sspi</literal> 的 realm 以及 <literal>krb5</literal> 主机设置（Magnus）
     </para>

       <para>
      它们覆盖 <filename>postgresql.conf</filename> 中的设置。
     </para>
      </listitem>

      <listitem>
       <para>
      为 <literal>krb5</literal>、<literal>gssapi</literal> 和 <literal>sspi</literal> 方法新增 <varname>include_realm</varname> 参数（Magnus）
     </para>

       <para>
      这允许使用 usermap 把来自不同 realm 的相同用户名认证为不同的数据库用户。
     </para>
      </listitem>

      <listitem>
       <para>
      装载时完整解析 <filename>pg_hba.conf</filename>，以便立即报告错误（Magnus）
     </para>

       <para>
      此前，文件中的大多数错误直到客户端尝试连接时才会被发现，因此一个有错误的文件可能使系统不可用。按照新行为，如果重载期间检测到错误，则拒绝坏文件，postmaster 继续使用其旧副本。
     </para>
      </listitem>

      <listitem>
       <para>
      显示 <filename>pg_hba.conf</filename> 中的所有解析错误，而不是在第一个之后中止（Selena Deckelmann）
     </para>
      </listitem>

      <listitem>
       <para>
      支持在 <productname>Solaris</productname> 上通过 Unix 域套接字进行 <literal>ident</literal> 认证（Garick Hamlin）
     </para>
      </listitem>

     </itemizedlist>

    </sect4>

    <sect4>
     <title>连续归档</title>
     <itemizedlist>

      <listitem>
       <para>
      为 <function>pg_start_backup()</function> 提供一个选项，强制其隐含的检查点尽快完成（Tom）
     </para>

       <para>
      默认行为避免过多的 I/O 消耗，但如果没有并发查询活动在进行，那就没有意义。
     </para>
      </listitem>

      <listitem>
       <para>
      让 <function>pg_stop_backup()</function> 等待已修改的 <acronym>WAL</acronym> 文件被归档（Simon）
     </para>

       <para>
      这保证备份在 <function>pg_stop_backup()</function> 完成时是有效的。
     </para>
      </listitem>

      <listitem>
       <para>
      启用归档时，在关闭时轮转最后一个 WAL 段，使所有事务可以立即归档（Guillaume Smet、Heikki）
     </para>
      </listitem>

      <listitem>
       <para>
      当连续归档基础备份正在进行时，延迟<quote>智能</quote>关闭（Laurenz Albe）
     </para>
      </listitem>

      <listitem>
       <para>
      如果请求<quote>快速</quote>关闭，则取消正在进行的连续归档基础备份（Laurenz Albe）
     </para>
      </listitem>

      <listitem>
       <para>
      允许 <filename>recovery.conf</filename> 布尔变量接受与 <filename>postgresql.conf</filename> 布尔变量相同的字符串值范围（Bruce）
     </para>
      </listitem>

     </itemizedlist>

    </sect4>

    <sect4>
     <title>监控</title>
     <itemizedlist>

      <listitem>
       <para>
      新增 <function>pg_conf_load_time()</function>，报告 <productname>PostgreSQL</productname> 配置文件上次装载的时间（George Gensure）
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <function>pg_terminate_backend()</function> 以安全地终止后端（<literal>SIGTERM</literal> 信号也可行）（Tom、Bruce）
     </para>

       <para>
      虽然一直可以用 <literal>SIGTERM</literal> 终止单个后端，但这此前被认为不受支持；对该情形的测试发现了一些现已修复的缺陷。
     </para>
      </listitem>

      <listitem>
       <para>
      新增跟踪用户定义函数调用次数和运行时间的能力（Martin Pihlak）
     </para>

       <para>
      函数统计信息出现在新系统视图 <literal>pg_stat_user_functions</literal> 中。跟踪由新参数 <varname>track_functions</varname> 控制。
     </para>
      </listitem>

      <listitem>
       <para>
      通过新参数 <varname>track_activity_query_size</varname> 允许指定 <literal>pg_stat_activity</literal> 中查询字符串的最大大小（Thomas Lee）
     </para>
      </listitem>

      <listitem>
       <para>
      增加发送到 <application>syslog</application> 的最大行长度，希望以此改进性能（Tom）
     </para>
      </listitem>

      <listitem>
       <para>
      新增只读配置变量 <varname>segment_size</varname>、<varname>wal_block_size</varname> 和 <varname>wal_segment_size</varname>（Bernd Helmle）
     </para>
      </listitem>

      <listitem>
       <para>
      报告死锁时，把死锁涉及的所有查询的文本报告给服务器日志（Itagaki Takahiro）
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <function>pg_stat_get_activity(pid)</function> 函数，返回特定进程 id 的信息（Magnus）
     </para>
      </listitem>

      <listitem>
       <para>
      允许通过 <varname>stats_temp_directory</varname> 指定服务器统计信息文件的位置（Magnus）
     </para>

       <para>
      这允许把统计信息文件放在 <acronym>RAM</acronym> 驻留目录中以降低 I/O 需求。在启动/关闭时，该文件被复制到其传统位置（<literal>$PGDATA/global/</literal>），因此跨重启保留。
     </para>
      </listitem>

     </itemizedlist>

    </sect4>

   </sect3>

   <sect3>
    <title>查询</title>
    <itemizedlist>

     <listitem>
      <para>
      新增对 <literal>WINDOW</literal> 函数的支持（Hitoshi Harada）
     </para>
     </listitem>

     <listitem>
      <para>
      新增对 <literal>WITH</literal> 子句（CTE）的支持，包括 <literal>WITH RECURSIVE</literal>（Yoshiyuki Asaba、Tatsuo Ishii、Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <command>TABLE</command> 命令（Peter）
     </para>

      <para>
      <literal>TABLE tablename</literal> 是 <literal>SELECT * FROM tablename</literal> 的 SQL 标准简写。
     </para>
     </listitem>

     <listitem>
      <para>
      允许在指定 <command>SELECT</command>（或 <literal>RETURNING</literal>）列输出标签时省略 <literal>AS</literal>（Hiroshi Saito）
     </para>

      <para>
      只要列标签不是任何 <productname>PostgreSQL</productname> 关键字，这就能工作；否则仍需要 <literal>AS</literal>。
     </para>
     </listitem>

     <listitem>
      <para>
      支持 <command>SELECT</command> 结果列表中的返回集合的函数，包括通过 tuplestore 返回结果的函数（Tom）
     </para>

      <para>
      特别是，这意味着用 PL/pgSQL 和其他 PL 语言编写的函数现在可以这样调用。
     </para>
     </listitem>

     <listitem>
      <para>
      支持聚合和分组查询输出中的返回集合的函数（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <command>SELECT FOR UPDATE</command>/<literal>SHARE</literal> 在继承树上工作（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      为 <acronym>SQL/MED</acronym> 新增基础设施（Martin Pihlak、Peter）
     </para>

      <para>
      目前还没有远程或外部 <acronym>SQL/MED</acronym> 能力，但此变更为管理 <filename>dblink</filename> 和 <filename>plproxy</filename> 等模块的连接信息提供了一个标准化且面向未来的系统。
     </para>
     </listitem>

     <listitem>
      <para>
      当被引用的模式、函数、操作符或操作符类被修改时使缓存计划失效（Martin Pihlak、Tom）
     </para>

      <para>
      这改进了系统对即时 DDL 变更的响应能力。
     </para>
     </listitem>
     <listitem>
      <para>
      允许组合类型的比较，并允许匿名组合类型的数组（Tom）
     </para>

      <para>
      这允许诸如 <literal>row(1, 1.1) = any (array[row(7, 7.7), row(1, 1.0)])</literal> 的构造。这在递归查询中特别有用。
     </para>
     </listitem>

     <listitem>
      <para>
      新增对用码点指定 Unicode 字符串字面量和标识符的支持，例如 <literal>U&amp;'d\0061t\+000061'</literal>（Peter）
     </para>
     </listitem>

     <listitem>
      <para>
      拒绝字符串字面量和 <command>COPY</command> 数据中的 <literal>\000</literal>（Tom）
     </para>

      <para>
      此前这被接受，但效果是终止字符串内容。
     </para>
     </listitem>

     <listitem>
      <para>
      改进解析器报告错误位置的能力（Tom）
     </para>

      <para>
      现在许多此前无法定位的语义错误（如数据类型不匹配）也会报告错误位置。
     </para>
     </listitem>

    </itemizedlist>

    <sect4>
     <title><command>TRUNCATE</></title>
     <itemizedlist>

      <listitem>
       <para>
      支持语句级 <literal>ON TRUNCATE</literal> 触发器（Simon）
     </para>
      </listitem>

      <listitem>
       <para>
      为 <command>TRUNCATE TABLE</command> 新增 <literal>RESTART</literal>/<literal>CONTINUE IDENTITY</literal> 选项（Zoltan Boszormenyi）
     </para>

       <para>
      序列的起始值可以通过 <command>ALTER SEQUENCE START WITH</command> 更改。
     </para>
      </listitem>

      <listitem>
       <para>
      允许 <command>TRUNCATE tab1, tab1</command> 成功（Bruce）
     </para>
      </listitem>

      <listitem>
       <para>
      新增单独的 <command>TRUNCATE</command> 权限（Robert Haas）
     </para>
      </listitem>

     </itemizedlist>

    </sect4>

    <sect4>
     <title><command>EXPLAIN</></title>
     <itemizedlist>

      <listitem>
       <para>
      让 <command>EXPLAIN VERBOSE</command> 显示每个计划节点的输出列（Tom）
     </para>

       <para>
      此前 <command>EXPLAIN VERBOSE</command> 输出查询计划的内部表示。（该行为现在可通过 <varname>debug_print_plan</varname> 获得。）
     </para>
      </listitem>

      <listitem>
       <para>
      让 <command>EXPLAIN</command> 用各自的标签标识子计划和 initplan（Tom）
     </para>
      </listitem>

      <listitem>
       <para>
      让 <command>EXPLAIN</command> 遵循 <varname>debug_print_plan</varname>（Tom）
     </para>
      </listitem>

      <listitem>
       <para>
      允许对 <command>CREATE TABLE AS</command> 使用 <command>EXPLAIN</command>（Peter）
     </para>
      </listitem>

     </itemizedlist>

    </sect4>

    <sect4>
     <title><literal>LIMIT</>/<literal>OFFSET</></title>
     <itemizedlist>

      <listitem>
       <para>
      允许 <literal>LIMIT</literal> 和 <literal>OFFSET</literal> 中的子查询（Tom）
     </para>
      </listitem>

      <listitem>
       <para>
      为 <literal>LIMIT</literal>/<literal>OFFSET</literal> 能力新增 <acronym>SQL</acronym> 标准语法（Peter）
     </para>

       <para>
      即 <literal>OFFSET num {ROW|ROWS} FETCH {FIRST|NEXT} [num] {ROW|ROWS} ONLY</literal>。
     </para>
      </listitem>

     </itemizedlist>

    </sect4>

   </sect3>

   <sect3>
    <title>对象操作</title>
    <itemizedlist>

     <listitem>
      <para>
      新增对列级权限的支持（Stephen Frost、KaiGai Kohei）
     </para>
     </listitem>

     <listitem>
      <para>
      重构多对象 <command>DROP</command> 操作以减少对 <literal>CASCADE</literal> 的需要（Alex Hunsaker）
     </para>

      <para>
      例如，如果表 <literal>B</literal> 依赖于表 <literal>A</literal>，命令 <literal>DROP TABLE A, B</literal> 不再需要 <literal>CASCADE</literal> 选项。
     </para>
     </listitem>

     <listitem>
      <para>
      通过确保在开始删除对象依赖之前获取锁，修复并发 <command>DROP</command> 命令的各种问题（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      改进 <command>DROP</command> 命令期间依赖的报告（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      按照 <acronym>SQL</acronym> 标准为 <command>CREATE TABLE AS</command> 新增 <literal>WITH [NO] DATA</literal> 子句（Peter、Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      新增对用户定义 I/O 转换转换的支持（Heikki）
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <command>CREATE AGGREGATE</command> 使用 <type>internal</type> 转换数据类型（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      为 <command>CREATE TYPE</command> 新增 <literal>LIKE</literal> 子句（Tom）
     </para>

      <para>
      这简化了创建与现有类型使用相同内部表示的数据类型。
     </para>
     </listitem>

     <listitem>
      <para>
      允许为用户定义基本类型指定类型类别和<quote>首选</quote>状态（Tom）
     </para>

      <para>
      这允许对用户定义类型的强制转换行为有更多控制。
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <command>CREATE OR REPLACE VIEW</command> 向视图末尾添加列（Robert Haas）
     </para>
     </listitem>

    </itemizedlist>

    <sect4>
     <title><command>ALTER</></title>
     <itemizedlist>

      <listitem>
       <para>
      新增 <command>ALTER TYPE RENAME</command>（Petr Jelinek）
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <command>ALTER SEQUENCE ... RESTART</command>（不带参数），把序列重置为其初始值（Zoltan Boszormenyi）
     </para>
      </listitem>

      <listitem>
       <para>
      修改 <command>ALTER TABLE</command> 语法，允许对表、索引、序列和视图的所有合理组合（Tom）
     </para>

       <para>
      此变更允许以下新语法：<itemizedlist> <listitem> <para> <command>ALTER SEQUENCE OWNER TO</command>
     </para>
         </listitem>
         <listitem>
          <para>
      <command>ALTER VIEW ALTER COLUMN SET/DROP DEFAULT</command>
     </para>
         </listitem>
         <listitem>
          <para>
      <command>ALTER VIEW OWNER TO</command>
     </para>
         </listitem>
         <listitem>
          <para>
      <command>ALTER VIEW SET SCHEMA</command>
     </para>
         </listitem>
        </itemizedlist>

        这里并没有实际的新功能，但以前你必须写 <command>ALTER TABLE</command> 才能做这些事情，这容易造成混淆。
       </para>
      </listitem>

      <listitem>
       <para>
      新增对语法 <command>ALTER TABLE ... ALTER COLUMN ... SET DATA TYPE</command> 的支持（Peter）
     </para>

       <para>
      这是已支持功能的 <acronym>SQL</acronym> 标准语法。
     </para>
      </listitem>

      <listitem>
       <para>
      让 <command>ALTER TABLE SET WITHOUT OIDS</command> 重写表以物理移除 <type>OID</type> 值（Tom）
     </para>

       <para>
      另外，新增 <command>ALTER TABLE SET WITH OIDS</command> 以重写表来添加 <type>OID</type>。
     </para>
      </listitem>

     </itemizedlist>

    </sect4>

    <sect4>
     <title>数据库操作</title>
     <itemizedlist>

      <listitem>
       <para>
      当未提交的预备事务是原因时，改进 <command>CREATE</command>/<command>DROP</command>/<command>RENAME DATABASE</command> 失败的报告（Tom）
     </para>
      </listitem>

      <listitem>
       <para>
      把 <varname>LC_COLLATE</varname> 和 <varname>LC_CTYPE</varname> 改为逐数据库的设置（Radek Strnad、Heikki）
     </para>

       <para>
      这使排序规则类似于编码——后者一直可以逐数据库配置。
     </para>
      </listitem>

      <listitem>
       <para>
      改进对数据库编码、排序规则（<varname>LC_COLLATE</varname>）和字符分类（<varname>LC_CTYPE</varname>）匹配的检查（Heikki、Tom）
     </para>

       <para>
      特别注意，新数据库的编码和区域设置只有在从 <literal>template0</literal> 复制时才能更改。这防止可能复制与设置不匹配的数据。
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <command>ALTER DATABASE SET TABLESPACE</command>，把数据库移到新表空间（Guillaume Lelarge、Bernd Helmle）
     </para>
      </listitem>

     </itemizedlist>

    </sect4>

   </sect3>

   <sect3>
    <title>工具操作</title>

    <itemizedlist>

     <listitem>
      <para>
      为 <command>CLUSTER</command> 命令和 <application>clusterdb</application> 新增 <literal>VERBOSE</literal> 选项（Jim Cox）
     </para>
     </listitem>

     <listitem>
      <para>
      降低记录待处理触发器事件的内存需求（Tom）
     </para>
     </listitem>

    </itemizedlist>

    <sect4>
     <title>索引</title>
     <itemizedlist>

      <listitem>
       <para>
      显著改进构建和访问哈希索引的速度（Tom Raney、Shreya Bhargava）
     </para>

       <para>
      这使哈希索引有时可以比 btree 索引更快。但哈希索引仍然不是崩溃安全的。
     </para>
      </listitem>

      <listitem>
       <para>
      让哈希索引只存储哈希码，而不是被索引列的完整值（Xiao Meng）
     </para>

       <para>
      这大幅减小长索引值下哈希索引的大小，改进了性能。
     </para>
      </listitem>

      <listitem>
       <para>
      实现 GIN 索引的快速更新选项（Teodor、Oleg）
     </para>

       <para>
      此选项以搜索速度的小幅损失大幅改进更新速度。
     </para>
      </listitem>

      <listitem>
       <para>
      <literal>xxx_pattern_ops</literal> 索引现在可用于简单相等比较，而不仅用于 <literal>LIKE</literal>（Tom）
     </para>
      </listitem>

     </itemizedlist>

    </sect4>

    <sect4>
     <title>全文索引</title>
     <itemizedlist>

      <listitem>
       <para>
      移除在全文本索引上进行 <acronym>GIN</acronym> 加权查找时必须使用 <literal>@@@</literal> 的要求（Tom、Teodor）
     </para>

       <para>
      可以改用常规的 <literal>@@</literal> 文本搜索操作符。
     </para>
      </listitem>

      <listitem>
       <para>
      为 <literal>@@</literal> 文本搜索操作新增优化器选择率函数（Jan Urbanski）
     </para>
      </listitem>

      <listitem>
       <para>
      允许全文搜索中的前缀匹配（Teodor Sigaev、Oleg Bartunov）
     </para>
      </listitem>

      <listitem>
       <para>
      支持多列 <acronym>GIN</acronym> 索引（Teodor Sigaev）
     </para>
      </listitem>

      <listitem>
       <para>
      改进对尼泊尔语和天城文的支持（Teodor）
     </para>
      </listitem>

     </itemizedlist>

    </sect4>

    <sect4>
     <title><command>VACUUM</></title>
     <itemizedlist>

      <listitem>
       <para>
      在单独的逐关系<quote>分叉</quote>文件中跟踪空闲空间（Heikki）
     </para>

       <para>
      <command>VACUUM</command> 发现的空闲空间现在记录在 <filename>*_fsm</filename> 文件中，而不是固定大小的共享内存区域。<varname>max_fsm_pages</varname> 和 <varname>max_fsm_relations</varname> 设置已被移除，大幅简化了空闲空间管理。
     </para>
      </listitem>

      <listitem>
       <para>
      新增可见性映射来跟踪不需要清理的页（Heikki）
     </para>

       <para>
      这允许 <command>VACUUM</command> 在只有表的一部分需要清理时避免扫描整表。可见性映射存储在逐关系的<quote>分叉</quote>文件中。
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <varname>vacuum_freeze_table_age</varname> 参数，控制 <command>VACUUM</command> 何时应忽略可见性映射并做全表扫描以冻结元组（Heikki）
     </para>
      </listitem>

      <listitem>
       <para>
      更仔细地跟踪事务快照（Alvaro）
     </para>

       <para>
      这改进了 <command>VACUUM</command> 在存在长运行事务时回收空间的能力。
     </para>
      </listitem>

      <listitem>
       <para>
      新增在 <command>CREATE TABLE</command> 中指定逐关系 autovacuum 和 <acronym>TOAST</acronym> 参数的能力（Alvaro、Euler Taveira de Oliveira）
     </para>

       <para>
      Autovacuum 选项以前存储在系统表中。
     </para>
      </listitem>

      <listitem>
       <para>
      为 <application>vacuumdb</application> 新增 <literal>--freeze</literal> 选项（Bruce）
     </para>
      </listitem>

     </itemizedlist>

    </sect4>

   </sect3>

   <sect3>
    <title>数据类型</title>
    <itemizedlist>

     <listitem>
      <para>
      为文本搜索同义词词典新增 <literal>CaseSensitive</literal> 选项（Simon）
     </para>
     </listitem>

     <listitem>
      <para>
      改进 <type>NUMERIC</type> 除法的精度（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <type>int2</type> 与 <type>int8</type> 的基本算术操作符（Tom）
     </para>

      <para>
      这消除了某些情况下显式转换的需要。
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <type>UUID</type> 输入在每四个数字后接受可选的连字符（Robert Haas）
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <literal>on</literal>/<literal>off</literal> 作为布尔数据类型的输入（Itagaki Takahiro）
     </para>
     </listitem>

     <listitem>
      <para>
      允许 <type>numeric</type> 类型输入字符串中 <literal>NaN</literal> 周围有空格（Sam Mason）
     </para>
     </listitem>

    </itemizedlist>

    <sect4>
     <title>时间数据类型</title>
     <itemizedlist>

      <listitem>
       <para>
      拒绝年份 <literal>0 BC</literal> 以及 <literal>000</literal> 和 <literal>0000</literal>（Tom）
     </para>

       <para>
      此前它们被解释为 <literal>1 BC</literal>。（注意：年份 <literal>0</literal> 和 <literal>00</literal> 仍被假定为 2000 年。）
     </para>
      </listitem>

      <listitem>
       <para>
      把 <literal>SGT</literal>（新加坡时间）加入已知时区缩写的默认列表（Tom）
     </para>
      </listitem>

      <listitem>
       <para>
      支持 <literal>infinity</literal> 和 <literal>-infinity</literal> 作为 <type>date</type> 类型的值（Tom）
     </para>
      </listitem>

      <listitem>
       <para>
      使 <type>interval</type> 字面量的解析更符合标准（Tom、Ron Mayer）
     </para>

       <para>
      例如，<literal>INTERVAL '1' YEAR</literal> 现在做它本应做的事。
     </para>
      </listitem>

      <listitem>
       <para>
      允许 <type>interval</type> 的小数秒精度指定在 <literal>second</literal> 关键字之后，以符合 <acronym>SQL</acronym> 标准（Tom）
     </para>

       <para>
      此前精度必须指定在 <type>interval</type> 关键字之后。（为向后兼容，该语法仍受支持，但已废弃。）数据类型定义现在将使用标准格式输出。
     </para>
      </listitem>

      <listitem>
       <para>
      支持 <acronym>IS0 8601</acronym> <type>interval</type> 语法（Ron Mayer、Kevin Grittner）
     </para>

       <para>
      例如，<literal>INTERVAL 'P1Y2M3DT4H5M6.7S'</literal> 现在受支持。
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <varname>IntervalStyle</varname> 参数，控制 <type>interval</type> 值的输出方式（Ron Mayer）
     </para>

       <para>
      有效值为：<literal>postgres</literal>、<literal>postgres_verbose</literal>、<literal>sql_standard</literal>、<literal>iso_8601</literal>。此设置还控制只有部分字段带正/负标志的负 <type>interval</type> 输入的处理。
     </para>
      </listitem>

      <listitem>
       <para>
      改进 <type>timestamp</type> 和 <type>interval</type> 输出中小数秒处理的一致性（Ron Mayer）
     </para>
      </listitem>

     </itemizedlist>

    </sect4>

    <sect4>
     <title>数组</title>
     <itemizedlist>

      <listitem>
       <para>
      改进应用于 <literal>ARRAY[]</literal> 构造的转换的处理，例如 <literal>ARRAY[...]::integer[]</literal>（Brendan Jurd）
     </para>

       <para>
      此前 <productname>PostgreSQL</productname> 试图在不参考后续转换的情况下确定 <literal>ARRAY[]</literal> 构造的数据类型。这在许多情况下会不必要地失败，特别是当 <literal>ARRAY[]</literal> 构造为空或只包含 <literal>NULL</literal> 等含糊条目时。现在会参考转换来确定数组元素必须的类型。
     </para>
      </listitem>

      <listitem>
       <para>
      让 <acronym>SQL</acronym> 语法的 <type>ARRAY</type> 维度可选，以匹配 <acronym>SQL</acronym> 标准（Peter）
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <function>array_ndims()</function>，返回数组的维数（Robert Haas）
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <function>array_length()</function>，返回数组指定维度的长度（Jim Nasby、Robert Haas、Peter Eisentraut）
     </para>
      </listitem>

      <listitem>
       <para>
      新增聚合函数 <function>array_agg()</function>，把所有聚合值作为单个数组返回（Robert Haas、Jeff Davis、Peter）
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <function>unnest()</function>，把数组转换为单独的行值（Tom）
     </para>

       <para>
      它与 <function>array_agg()</function> 相反。
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <function>array_fill()</function>，创建用某值初始化的数组（Pavel Stehule）
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <function>generate_subscripts()</function>，简化生成数组下标范围（Pavel Stehule）
     </para>
      </listitem>

     </itemizedlist>

    </sect4>

    <sect4>
     <title>Wide-Value Storage (<acronym>TOAST</>)</title>
     <itemizedlist>

      <listitem>
       <para>
      对短至 32 字节的值考虑 <acronym>TOAST</acronym> 压缩（以前是 256 字节）（Greg Stark）
     </para>
      </listitem>

      <listitem>
       <para>
      要求至少 25% 的空间节省才使用 <acronym>TOAST</acronym> 压缩（以前小值为 20%，大值为有任何节省即可）（Greg）
     </para>
      </listitem>

      <listitem>
       <para>
      改进对混合大小可 TOAST 字段的行的 <acronym>TOAST</acronym> 启发式，使我们倾向于把大值推出行外并且不必要地压缩小值（Greg、Tom）
     </para>
      </listitem>

     </itemizedlist>

    </sect4>

   </sect3>

   <sect3>
    <title>函数</title>
    <itemizedlist>

     <listitem>
      <para>
      文档说明 <function>setseed()</function> 允许 <literal>-1</literal> 到 <literal>1</literal> 的值（不只是 <literal>0</literal> 到 <literal>1</literal>），并强制有效范围（Kris Jurka）
     </para>
     </listitem>

     <listitem>
      <para>
      新增服务器端函数 <function>lo_import(filename, oid)</function>（Tatsuo）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <function>quote_nullable()</function>，行为类似 <function>quote_literal()</function>，但对空参数返回字符串 <literal>NULL</literal>（Brendan Jurd）
     </para>
     </listitem>

     <listitem>
      <para>
      改进全文搜索的 <function>headline()</function> 函数，允许提取多个文本片段（Sushant Sinha）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <function>suppress_redundant_updates_trigger()</function> 触发器函数，避免非数据更改更新的开销（Andrew）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <function>div(numeric, numeric)</function>，执行不舍入的 <type>numeric</type> 除法（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <function>generate_series()</function> 的 <type>timestamp</type> 和 <type>timestamptz</type> 版本（Hitoshi Harada）
     </para>
     </listitem>

    </itemizedlist>

    <sect4>
     <title>对象信息函数</title>
     <itemizedlist>

      <listitem>
       <para>
      实现 <function>current_query()</function>，供需要知道当前运行查询的函数使用（Tomas Doran）
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <function>pg_get_keywords()</function>，返回解析器关键字列表（Dave Page）
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <function>pg_get_functiondef()</function>，查看函数的定义（Abhijit Menon-Sen）
     </para>
      </listitem>

      <listitem>
       <para>
      当反解析不含变量的表达式时，允许 <function>pg_get_expr()</function> 的第二个参数为零（Tom）
     </para>
      </listitem>

      <listitem>
       <para>
      修改 <function>pg_relation_size()</function> 以使用 <literal>regclass</literal>（Heikki）
     </para>

       <para>
      <function>pg_relation_size(data_type_name)</function> 不再有效。
     </para>
      </listitem>

      <listitem>
       <para>
      向 <literal>pg_settings</literal> 输出新增 <literal>boot_val</literal> 和 <literal>reset_val</literal> 列（Greg Smith）
     </para>
      </listitem>

      <listitem>
       <para>
      向 <literal>pg_settings</literal> 输出为在配置文件中设置的变量新增源文件名和行号列（Magnus、Alvaro）
     </para>

       <para>
      出于安全原因，这些列只有超级用户可见。
     </para>
      </listitem>

      <listitem>
       <para>
      新增对 <varname>CURRENT_CATALOG</varname>、<varname>CURRENT_SCHEMA</varname>、<varname>SET CATALOG</varname>、<varname>SET SCHEMA</varname> 的支持（Peter）
     </para>

       <para>
      它们为现有特性提供 <acronym>SQL</acronym> 标准语法。
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <function>pg_typeof()</function>，返回任意值的数据类型（Brendan Jurd）
     </para>
      </listitem>

      <listitem>
       <para>
      让 <function>version()</function> 返回服务器是 32 位还是 64 位二进制的信息（Bruce）
     </para>
      </listitem>

      <listitem>
       <para>
      修复信息模式列 <structfield>is_insertable_into</structfield> 和 <structfield>is_updatable</structfield> 的行为使其一致（Peter）
     </para>
      </listitem>

      <listitem>
       <para>
      改进信息模式 <structfield>datetime_precision</structfield> 列的行为（Peter）
     </para>

       <para>
      这些列现在对 <type>date</type> 列显示零，对未声明精度的 <type>time</type>、<type>timestamp</type> 和 <type>interval</type> 显示 6（默认精度），而不是像以前那样显示空值。
     </para>
      </listitem>

      <listitem>
       <para>
      把剩余的内建返回集合的函数转换为使用 <literal>OUT</literal> 参数（Jaime Casanova）
     </para>

       <para>
      这使调用这些函数无需指定列列表成为可能：<function>pg_show_all_settings()</function>、<function>pg_lock_status()</function>、<function>pg_prepared_xact()</function>、<function>pg_prepared_statement()</function>、<function>pg_cursor()</function>
     </para>
      </listitem>

      <listitem>
       <para>
      让 <function>pg_*_is_visible()</function> 和 <function>has_*_privilege()</function> 函数对无效 OID 返回 <literal>NULL</literal>，而不是报告错误（Tom）
     </para>
      </listitem>

      <listitem>
       <para>
      扩展 <function>has_*_privilege()</function> 函数，允许一次调用查询多个权限的 OR（Stephen Frost、Tom）
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <function>has_column_privilege()</function> 和 <function>has_any_column_privilege()</function> 函数（Stephen Frost、Tom）
     </para>
      </listitem>

     </itemizedlist>

    </sect4>

    <sect4>
     <title>函数创建</title>
     <itemizedlist>

      <listitem>
       <para>
      支持可变参数函数（参数数量可变的函数）（Pavel Stehule）
     </para>

       <para>
      只有尾部参数可以是可选的，且它们必须都是同一数据类型。
     </para>
      </listitem>

      <listitem>
       <para>
      支持函数参数的默认值（Pavel Stehule）
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <command>CREATE FUNCTION ... RETURNS TABLE</command> 子句（Pavel Stehule）
     </para>
      </listitem>

      <listitem>
       <para>
      允许 <acronym>SQL</acronym> 语言函数返回 <command>INSERT</command>/<command>UPDATE</command>/<command>DELETE</command> 的 <literal>RETURNING</literal> 子句的输出（Tom）
     </para>
      </listitem>

     </itemizedlist>

    </sect4>

    <sect4>
     <title>PL/pgSQL 服务器端语言</title>
     <itemizedlist>

      <listitem>
       <para>
      支持 <literal>EXECUTE USING</literal>，便于把数据值插入动态查询字符串（Pavel Stehule）
     </para>
      </listitem>

      <listitem>
       <para>
      允许用 <literal>FOR</literal> 循环遍历游标的结果（Pavel Stehule）
     </para>
      </listitem>

      <listitem>
       <para>
      支持 <literal>RETURN QUERY EXECUTE</literal>（Pavel Stehule）
     </para>
      </listitem>

      <listitem>
       <para>
      改进 <literal>RAISE</literal> 命令（Pavel Stehule）<itemizedlist> <listitem> <para> 支持 <literal>DETAIL</literal> 和 <literal>HINT</literal> 字段
     </para>
         </listitem>
         <listitem>
          <para>
      支持指定 <literal>SQLSTATE</literal> 错误码
     </para>
         </listitem>
         <listitem>
          <para>
      支持异常名参数
     </para>
         </listitem>
         <listitem>
          <para>
      允许异常块中不带参数的 <literal>RAISE</literal> 重新抛出当前错误
     </para>
         </listitem>
        </itemizedlist>
       </para>
      </listitem>

      <listitem>
       <para>
      允许在 <literal>EXCEPTION</literal> 列表中指定 <varname>SQLSTATE</varname> 码（Pavel Stehule）
     </para>

       <para>
      这对处理自定义 <varname>SQLSTATE</varname> 码很有用。
     </para>
      </listitem>

      <listitem>
       <para>
      支持 <literal>CASE</literal> 语句（Pavel Stehule）
     </para>
      </listitem>

      <listitem>
       <para>
      让 <command>RETURN QUERY</command> 设置特殊的 <literal>FOUND</literal> 和 <command>GET DIAGNOSTICS</command> 的 <literal>ROW_COUNT</literal> 变量（Pavel Stehule）
     </para>
      </listitem>

      <listitem>
       <para>
      让 <command>FETCH</command> 和 <command>MOVE</command> 设置 <command>GET DIAGNOSTICS</command> 的 <literal>ROW_COUNT</literal> 变量（Andrew Gierth）
     </para>
      </listitem>

      <listitem>
       <para>
      让不带标签的 <command>EXIT</command> 总是退出最内层循环（Tom）
     </para>

       <para>
      此前，如果存在比任何循环都更贴近嵌套的 <literal>BEGIN</literal> 块，它会退出那个块。新行为与 Oracle(TM) 匹配，也与我们自己文档此前的说法一致。
     </para>
      </listitem>

      <listitem>
       <para>
      使字符串字面量和嵌套块注释的处理与主 SQL 解析器的处理一致（Tom）
     </para>

       <para>
      特别是，<command>RAISE</command> 中的格式字符串现在与任何其他字符串字面字面量一样工作，包括受 <varname>standard_conforming_strings</varname> 约束。此变更还修复了 <varname>standard_conforming_strings</varname> 开启时有效命令会失败的其他情形。
     </para>
      </listitem>

      <listitem>
       <para>
      避免同一函数在不同异常块嵌套深度被调用时的内存泄漏（Tom）
     </para>
      </listitem>

     </itemizedlist>

    </sect4>

   </sect3>

   <sect3>
    <title>客户端应用程序</title>

    <itemizedlist>

     <listitem>
      <para>
      修复 <literal>pg_ctl restart</literal> 以保留命令行参数（Bruce）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <literal>-w</literal>/<literal>--no-password</literal> 选项，在所有具有 <literal>-W</literal>/<literal>--password</literal> 选项的工具中阻止口令提示（Peter）
     </para>
     </listitem>

     <listitem>
      <para>
      移除 <application>createdb</application>、<application>createuser</application>、<application>dropdb</application>、<application>dropuser</application> 的 <option>-q</option>（安静）选项（Peter）
     </para>

      <para>
      这些选项自 <productname>PostgreSQL</productname> 8.3 起就没有效果。
     </para>
     </listitem>

    </itemizedlist>

    <sect4>
     <title><application>psql</></title>
     <itemizedlist>

      <listitem>
       <para>
      移除冗长的启动横幅；现在只建议 <literal>help</literal>（Joshua Drake）
     </para>
      </listitem>

      <listitem>
       <para>
      让 <literal>help</literal> 显示常用反斜杠命令（Greg Sabino Mullane）
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <literal>\pset format wrapped</literal> 模式，把输出折行到屏幕宽度，或者在设置了 <literal>\pset columns</literal> 时也适用于文件/管道输出（Bryce Nesbitt）
     </para>
      </listitem>

      <listitem>
       <para>
      允许 <command>\pset</command> 中布尔值的所有受支持拼法，而不只是 <literal>on</literal> 和 <literal>off</literal>（Bruce）
     </para>

       <para>
      此前，除 <quote>off</quote> 外的任何字符串都被静默地当作 <literal>true</literal>。<application>psql</application> 现在会对无法识别的拼法抱怨（但仍把它们当作 <literal>true</literal>）。
     </para>
      </listitem>

      <listitem>
       <para>
      对宽输出使用分页器（Bruce）
     </para>
      </listitem>

      <listitem>
       <para>
      要求单字母反斜杠命令与其第一个参数之间有一个空格（Bernd Helmle）
     </para>

       <para>
      这消除了一个历史性的歧义来源。
     </para>
      </listitem>

      <listitem>
       <para>
      改进对模式限定和带引号标识符的 Tab 补全支持（Greg Sabino Mullane）
     </para>
      </listitem>

      <listitem>
       <para>
      为 <command>\timing</command> 新增可选的 <literal>on</literal>/<literal>off</literal> 参数（David Fetter）
     </para>
      </listitem>

      <listitem>
       <para>
      在多行上显示访问控制权限（Brendan Jurd、Andreas Scherbaum）
     </para>
      </listitem>

      <listitem>
       <para>
      让 <command>\l</command> 显示数据库访问权限（Andrew Gilligan）
     </para>
      </listitem>

      <listitem>
       <para>
      让 <command>\l+</command> 显示数据库大小（如果权限允许）（Andrew Gilligan）
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <command>\ef</command> 命令来编辑函数定义（Abhijit Menon-Sen）
     </para>
      </listitem>

     </itemizedlist>

    </sect4>

    <sect4>
     <title><application>psql</> \d* commands</title>

     <itemizedlist>

      <listitem>
       <para>
      让没有模式参数的 <command>\d*</command> 命令只在指定 <literal>S</literal> 修饰符时显示系统对象（Greg Sabino Mullane、Bruce）
     </para>

       <para>
      以前的行为在不同的 <command>\d</command> 变体之间不一致，而且在大多数情况下没有提供只查看用户对象的简便方法。
     </para>
      </listitem>

      <listitem>
       <para>
      改进 <command>\d*</command> 命令，使其能用于较旧的 <productname>PostgreSQL</productname> 服务器版本（可追溯到 7.4），而不仅是当前服务器版本（Guillaume Lelarge）
     </para>
      </listitem>

      <listitem>
       <para>
      让 <command>\d</command> 显示引用所选表的外键约束（Kenneth D'Souza）
     </para>
      </listitem>

      <listitem>
       <para>
      让 <command>\d</command> 对序列显示其列值（Euler Taveira de Oliveira）
     </para>
      </listitem>

      <listitem>
       <para>
      向 <command>\d+</command> 显示中添加列存储类型和其他关系选项（Gregory Stark、Euler Taveira de Oliveira）
     </para>
      </listitem>

      <listitem>
       <para>
      在 <command>\dt+</command> 输出中显示关系大小（Dickson S. Guedes）
     </para>
      </listitem>

      <listitem>
       <para>
      在 <command>\dT+</command> 中显示 <literal>enum</literal> 类型的可能值（David Fetter）
     </para>
      </listitem>

      <listitem>
       <para>
      允许 <command>\dC</command> 接受通配符模式，匹配转换涉及的任一数据类型（Tom）
     </para>
      </listitem>

      <listitem>
       <para>
      向 <command>\df</command> 的输出新增函数类型列，并新增只列出所选类型函数的选项（David Fetter）
     </para>
      </listitem>

      <listitem>
       <para>
      让 <command>\df</command> 不隐藏接受或返回 <type>cstring</type> 类型的函数（Tom）
     </para>

       <para>
      此前这类函数被隐藏，因为它们大多是数据类型 I/O 函数，被认为无趣。默认隐藏系统函数的新策略使这个瑕疵不再必要。
     </para>
      </listitem>

     </itemizedlist>

    </sect4>

    <sect4>
     <title><application>pg_dump</></title>
     <itemizedlist>

      <listitem>
       <para>
      为 <application>pg_dump</application>/<application>pg_dumpall</application>/<application>pg_restore</application> 新增 <literal>--no-tablespaces</literal> 选项，使转储可以恢复到表空间布局不匹配的集群（Gavin Roy）
     </para>
      </listitem>

      <listitem>
       <para>
      移除 <application>pg_dump</application> 和 <application>pg_dumpall</application> 的 <option>-d</option> 和 <option>-D</option> 选项（Tom）
     </para>

       <para>
      这些选项太容易与其他 <productname>PostgreSQL</productname> 客户端应用程序中选择数据库名的选项混淆。功能仍然可用，但现在必须拼出长选项名 <option>--inserts</option> 或 <option>--column-inserts</option>。
     </para>
      </listitem>

      <listitem>
       <para>
      移除 <application>pg_dump</application> 和 <application>pg_dumpall</application> 的 <option>-i</option>/<option>--ignore-version</option> 选项（Tom）
     </para>

       <para>
      使用此选项不会抛出错误，但它没有效果。移除此选项是因为版本检查对安全是必要的。
     </para>
      </listitem>

      <listitem>
       <para>
      在转储和恢复期间禁用 <varname>statement_timeout</varname>（Joshua Drake）
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <application>pg_dump</application>/<application>pg_dumpall</application> 选项 <option>--lock-wait-timeout</option>（David Gould）
     </para>

       <para>
      这允许转储在指定时间内无法获得共享锁时失败。
     </para>
      </listitem>

      <listitem>
       <para>
      重排 <application>pg_dump</application> 的 <literal>--data-only</literal> 输出，把被外键引用的表排在引用表之前转储（Tom）
     </para>

       <para>
      这允许在外键已存在时加载数据。如果循环引用使安全排序不可能，则发出 <literal>NOTICE</literal>。
     </para>
      </listitem>

      <listitem>
       <para>
      允许 <application>pg_dump</application>、<application>pg_dumpall</application> 和 <application>pg_restore</application> 使用指定的角色（Benedek L&aacute;szl&oacute;）
     </para>
      </listitem>

      <listitem>
       <para>
      允许 <application>pg_restore</application> 使用多个并发连接执行恢复（Andrew）
     </para>

       <para>
      并发连接数由选项 <literal>--jobs</literal> 控制。这只对自定义格式归档受支持。
     </para>
      </listitem>

     </itemizedlist>

    </sect4>

   </sect3>

   <sect3>
    <title>编程工具</title>

    <sect4>
     <title><application>libpq</></title>
     <itemizedlist>

      <listitem>
       <para>
      允许在导入大对象时指定 <type>OID</type>，通过新函数 <function>lo_import_with_oid()</function>（Tatsuo）
     </para>
      </listitem>

      <listitem>
       <para>
      新增<quote>事件</quote>支持（Andrew Chernow、Merlin Moncure）
     </para>

       <para>
      这新增了注册回调来管理与 <structname>PGconn</structname> 和 <structname>PGresult</structname> 对象关联的私有数据的能力。
     </para>
      </listitem>

      <listitem>
       <para>
      改进错误处理，允许多条错误消息作为多行错误报告返回（Magnus）
     </para>
      </listitem>

      <listitem>
       <para>
      让 <function>PQexecParams()</function> 及相关函数对空查询返回 <varname>PGRES_EMPTY_QUERY</varname>（Tom）
     </para>

       <para>
      它们以前返回 <varname>PGRES_COMMAND_OK</varname>。
     </para>
      </listitem>

      <listitem>
       <para>
      记录如何避免 Windows 上 <function>WSACleanup()</function> 的开销（Andrew Chernow）
     </para>
      </listitem>

      <listitem>
       <para>
      不依赖 Kerberos 票据来确定默认数据库用户名（Magnus）
     </para>

       <para>
      此前，支持 Kerberos 的 libpq 构建会把任何可用 Kerberos 票据中的主体名用作默认数据库用户名，即使连接没有使用 Kerberos 认证。这被认为不一致且令人困惑。现在默认用户名的确定方式在有或没有 Kerberos 时相同。但注意，使用 Kerberos 认证时数据库用户名仍必须与票据匹配。
     </para>
      </listitem>
     </itemizedlist>

    </sect4>

    <sect4>
     <title><application>libpq</> <acronym>SSL</> (Secure Sockets Layer)
      support</title>
     <itemizedlist>

      <listitem>
       <para>
      修复 <acronym>SSL</acronym> 连接的证书验证（Magnus）
     </para>

       <para>
      <application>libpq</application> 现在支持在建立 <acronym>SSL</acronym> 连接时同时验证证书和服务器名称。如果没有可用于验证的根证书，<acronym>SSL</acronym> 连接将失败。<literal>sslmode</literal> 参数用于启用证书验证并设置检查级别。默认仍是不做任何验证，允许连接到启用 SSL 的服务器而不要求客户端上有根证书。
     </para>
      </listitem>

      <listitem>
       <para>
      支持通配符服务器证书（Magnus）
     </para>

       <para>
      如果证书 <acronym>CN</acronym> 以 <literal>*</literal> 开头，匹配主机名时它将被视为通配符，允许同一证书用于多个服务器。
     </para>
      </listitem>

      <listitem>
       <para>
      允许指定客户端证书的文件位置（Mark Woodward、Alvaro、Magnus）
     </para>
      </listitem>

      <listitem>
       <para>
      新增 <function>PQinitOpenSSL</function> 函数，允许对 OpenSSL/libcrypto 初始化有更多控制（Andrew Chernow）
     </para>
      </listitem>

      <listitem>
       <para>
      当没有数据库连接保持打开时，让 <application>libpq</application> 注销其 <application>OpenSSL</application> 回调（Bruce、Magnus、Russell Smith）
     </para>

       <para>
      这对卸载 libpq 库的应用程序是必需的，否则无效的 <application>OpenSSL</application> 回调将残留。
     </para>
      </listitem>

     </itemizedlist>

    </sect4>

    <sect4>
     <title><application>ecpg</></title>
     <itemizedlist>

      <listitem>
       <para>
      新增消息的本地化支持（Euler Taveira de Oliveira）
     </para>
      </listitem>

      <listitem>
       <para>
      ecpg 解析器现在从服务器解析器自动生成（Michael）
     </para>

       <para>
      此前 ecpg 解析器是手工维护的。
     </para>
      </listitem>

     </itemizedlist>

    </sect4>

    <sect4>
     <title>Server Programming Interface (<acronym>SPI</>)</title>
     <itemizedlist>

      <listitem>
       <para>
      新增对外线参数的单次使用计划的支持（Tom）
     </para>
      </listitem>

      <listitem>
       <para>
      为 <function>SPI_execute()</function> 新增 <varname>SPI_OK_REWRITTEN</varname> 返回码（Heikki）
     </para>

       <para>
      这在命令被重写为另一种类型的命令时使用。
     </para>
      </listitem>

      <listitem>
       <para>
      从 <filename>executor/spi.h</filename> 中移除不必要的包含（Tom）
     </para>

       <para>
      使用 SPI 的模块如果依赖 <filename>spi.h</filename> 替它们包含内容，可能需要添加一些 <literal>#include</literal> 行。
     </para>
      </listitem>

     </itemizedlist>

    </sect4>

   </sect3>

   <sect3>
    <title>构建选项</title>
    <itemizedlist>

     <listitem>
      <para>
      更新构建系统以使用 <productname>Autoconf</productname> 2.61（Peter）
     </para>
     </listitem>

     <listitem>
      <para>
      源代码构建需要 <productname>GNU bison</productname>（Peter）
     </para>

      <para>
      这实际上多年来已是必需，但现在不再有声称支持其他解析器工具的基础设施。
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <application>pg_config</application> 的 <literal>--htmldir</literal> 选项（Peter）
     </para>
     </listitem>

     <listitem>
      <para>
      在服务器内部按值传递 <type>float4</type>（Zoltan Boszormenyi）
     </para>

      <para>
      新增 <application>configure</application> 选项 <literal>--disable-float4-byval</literal> 以使用旧行为。使用旧式（版本 0）调用约定并传递或返回 <type>float4</type> 值的外部 C 函数将被此变更破坏，因此如果有这样的函数且不想更新它们，可能需要该 <application>configure</application> 选项。
     </para>
     </listitem>

     <listitem>
      <para>
      在 64 位平台上，服务器内部按值传递 <type>float8</type>、<type>int8</type> 及相关数据类型（Zoltan Boszormenyi）
     </para>

      <para>
      新增 <application>configure</application> 选项 <literal>--disable-float8-byval</literal> 以使用旧行为。如上所述，此变更可能破坏旧式外部 C 函数。
     </para>
     </listitem>

     <listitem>
      <para>
      新增 configure 选项 <literal>--with-segsize</literal>、<literal>--with-blocksize</literal>、<literal>--with-wal-blocksize</literal>、<literal>--with-wal-segsize</literal>（Zdenek Kotala、Tom）
     </para>

      <para>
      这简化了对几个常量的构建期控制，这些常量以前只能通过编辑 <filename>pg_config_manual.h</filename> 更改。
     </para>
     </listitem>

     <listitem>
      <para>
      允许在 <productname>Solaris</productname> 2.5 上进行线程化构建（Bruce）
     </para>
     </listitem>

     <listitem>
      <para>
      在 <productname>Solaris</productname> 上使用系统的 <function>getopt_long()</function>（Zdenek Kotala、Tom）
     </para>

      <para>
      这使选项处理更符合 Solaris 用户的期望。
     </para>
     </listitem>

     <listitem>
      <para>
      新增对 <productname>Linux</productname> 上 <productname>Sun Studio</productname> 编译器的支持（Julius Stroffek）
     </para>
     </listitem>

     <listitem>
      <para>
      把主版本号附加到后端 <application>gettext</application> 域，把 <literal>soname</literal> 主版本号附加到库的 <application>gettext</application> 域（Peter）
     </para>

      <para>
      这简化了多个版本的并行安装。
     </para>
     </listitem>

     <listitem>
      <para>
      新增用 <application>gcov</application> 进行代码覆盖率测试的支持（Michelle Caisse）
     </para>
     </listitem>

     <listitem>
      <para>
      允许在 <productname>Mingw</productname> 和 <productname>Cygwin</productname> 上进行树外构建（Richard Evans）
     </para>
     </listitem>

     <listitem>
      <para>
      修复把 <productname>Mingw</productname> 用作交叉编译源平台的问题（Peter）
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title>源代码</title>
    <itemizedlist>

     <listitem>
      <para>
      支持 64 位时区数据文件（Heikki）
     </para>

      <para>
      这新增了对 2038 年以后的夏令时（<acronym>DST</acronym>）计算的支持。
     </para>
     </listitem>

     <listitem>
      <para>
      弃用平台 <type>time_t</type> 数据类型的使用（Tom）
     </para>

      <para>
      一些平台已迁移到 64 位 <type>time_t</type>，一些没有，而 Windows 拿不定主意。定义 <type>pg_time_t</type>，其含义与 <type>time_t</type> 相同，但始终为 64 位（除非平台没有 64 位整数类型），并在所有模块 API 和磁盘上数据格式中使用该类型。
     </para>
     </listitem>

     <listitem>
      <para>
      修复交叉编译时处理时区数据库的缺陷（Richard Evans）
     </para>
     </listitem>

     <listitem>
      <para>
      一步链接后端目标文件，而不是分阶段（Peter）
     </para>
     </listitem>

     <listitem>
      <para>
      改进 <application>gettext</application> 支持以允许更好地翻译复数（Peter）
     </para>
     </listitem>

     <listitem>
      <para>
      为 PL 语言新增消息翻译支持（Alvaro、Peter）
     </para>
     </listitem>

     <listitem>
      <para>
      新增更多 <application>DTrace</application> 探针（Robert Lor）
     </para>
     </listitem>

     <listitem>
      <para>
      在 <application>Mac OS X Leopard</application> 和其他非 Solaris 平台上启用 <application>DTrace</application> 支持（Robert Lor）
     </para>
     </listitem>

     <listitem>
      <para>
      通过提供通用函数来简化和标准化 C 字符串与 <type>text</type> datum 之间的转换（Brendan Jurd、Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      清理 <filename>include/catalog/</filename> 头文件，使前端程序可以包含它们而不必包含 <filename>postgres.h</filename>（Zdenek Kotala）
     </para>
     </listitem>

     <listitem>
      <para>
      让 <type>name</type> 按 char 对齐，并抑制索引中 <type>name</type> 条目的零填充（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      如果动态装载的代码执行 <function>exit()</function>，更好地恢复（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      新增钩子让插件监控执行器（Itagaki Takahiro）
     </para>
     </listitem>

     <listitem>
      <para>
      新增钩子，允许覆盖规划器的统计信息查找行为（Simon Riggs）
     </para>
     </listitem>

     <listitem>
      <para>
      为自定义共享内存需求新增 <function>shmem_startup_hook()</function>（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      把索引访问方法 <function>amgetmulti</function> 入口点替换为 <function>amgetbitmap</function>，并扩展 <function>amgettuple</function> 的 API 以支持运行时确定操作符有损性（Heikki、Tom、Teodor）
     </para>

      <para>
      GIN 和 GiST 操作符类 <function>consistent</function> 函数的 API 也被扩展了。
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <acronym>GIN</acronym> 索引中部分匹配搜索的支持（Teodor Sigaev、Oleg Bartunov）
     </para>
     </listitem>

     <listitem>
      <para>
      把 <structname>pg_class</structname> 列 <structfield>reltriggers</structfield> 替换为布尔 <structfield>relhastriggers</structfield>（Simon）
     </para>

      <para>
      同时移除未使用的 <structname>pg_class</structname> 列 <structfield>relukeys</structfield>、<structfield>relfkeys</structfield> 和 <structfield>relrefs</structfield>。
     </para>
     </listitem>

     <listitem>
      <para>
      向 <structname>pg_class</structname> 新增 <structfield>relistemp</structfield> 列，便于识别临时表（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      把平台 <acronym>FAQ</acronym> 移入主文档（Peter）
     </para>
     </listitem>

     <listitem>
      <para>
      防止解析器输入文件在有冲突的状态下构建（Peter）
     </para>
     </listitem>

     <listitem>
      <para>
      新增对 <literal>KOI8U</literal>（乌克兰语）编码的支持（Peter）
     </para>
     </listitem>

     <listitem>
      <para>
      新增日语消息翻译（Japan PostgreSQL Users Group）
     </para>

      <para>
      这以前作为独立项目维护。
     </para>
     </listitem>

     <listitem>
      <para>
      修复在 <application>MSVC</application> 构建的系统上设置 <varname>LC_MESSAGES</varname> 的问题（Hiroshi Inoue、Hiroshi Saito、Magnus）
     </para>
     </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title>附加模块</title>

    <itemizedlist>

     <listitem>
      <para>
      新增 <filename>contrib/auto_explain</filename>，对超过指定时长的查询自动运行 <command>EXPLAIN</command>（Itagaki Takahiro、Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <filename>contrib/btree_gin</filename>，允许 GIN 索引处理更多数据类型（Oleg、Teodor）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <filename>contrib/citext</filename>，提供不区分大小写、多字节感知的文本数据类型（David Wheeler）
     </para>
     </listitem>

     <listitem>
      <para>
      新增 <filename>contrib/pg_stat_statements</filename>，用于服务器范围的语句执行统计跟踪（Itagaki Takahiro）
     </para>
     </listitem>

     <listitem>
      <para>
      为 <filename>contrib/pgbench</filename> 新增时长和查询模式选项（Itagaki Takahiro）
     </para>
     </listitem>

     <listitem>
      <para>
      让 <filename>contrib/pgbench</filename> 使用表名 <structname>pgbench_accounts</structname>、<structname>pgbench_branches</structname>、<structname>pgbench_history</structname> 和 <structname>pgbench_tellers</structname>，而不只是 <structname>accounts</structname>、<structname>branches</structname>、<structname>history</structname> 和 <structname>tellers</structname>（Tom）
     </para>

      <para>
      这是为了降低运行 <application>pgbench</application> 时意外破坏真实数据的风险。
     </para>
     </listitem>

     <listitem>
      <para>
      修复 <filename>contrib/pgstattuple</filename> 以处理超过 20 亿页的表和索引（Tatsuhito Kasahara）
     </para>
     </listitem>

     <listitem>
      <para>
      在 <filename>contrib/fuzzystrmatch</filename> 中，新增允许用户指定插入、删除和替换代价的 Levenshtein 字符串距离函数版本（Volkan Yazici）
     </para>
     </listitem>

     <listitem>
      <para>
      让 <filename>contrib/ltree</filename> 支持多字节编码（laser）
     </para>
     </listitem>

     <listitem>
      <para>
      使 <filename>contrib/dblink</filename> 能使用存储在 SQL/MED 目录中的连接信息（Joe Conway）
     </para>
     </listitem>

     <listitem>
      <para>
      改进 <filename>contrib/dblink</filename> 对来自远程服务器错误的报告（Joe Conway）
     </para>
     </listitem>

     <listitem>
      <para>
      让 <filename>contrib/dblink</filename> 设置 <varname>client_encoding</varname> 以匹配本地数据库的编码（Joe Conway）
     </para>

      <para>
      这防止与使用不同编码的远程数据库通信时的编码问题。
     </para>
     </listitem>

     <listitem>
      <para>
      确保 <filename>contrib/dblink</filename> 使用用户提供的口令，而不是意外地从服务器的 <filename>.pgpass</filename> 文件获取（Joe Conway）
     </para>

      <para>
      这是一个小的安全增强。
     </para>
     </listitem>

     <listitem>
      <para>
      为 <filename>contrib/pageinspect</filename> 新增 <function>fsm_page_contents()</function>（Heikki）
     </para>
     </listitem>

     <listitem>
      <para>
      修改 <function>get_raw_page()</function> 以支持空闲空间映射（<filename>*_fsm</filename>）文件。同时更新 <filename>contrib/pg_freespacemap</filename>。
     </para>
     </listitem>

     <listitem>
      <para>
      为 <filename>contrib/pg_trgm</filename> 新增多字节编码支持（Teodor）
     </para>
     </listitem>

     <listitem>
      <para>
      重写 <filename>contrib/intagg</filename> 以使用新函数 <function>array_agg()</function> 和 <function>unnest()</function>（Tom）
     </para>
     </listitem>

     <listitem>
      <para>
      让 <filename>contrib/pg_standby</filename> 在故障转移前恢复所有可用 WAL（Fujii Masao、Simon、Heikki）
     </para>

      <para>
      为使这安全工作，现在需要在 <filename>recovery.conf</filename> 中设置新的 <literal>recovery_end_command</literal> 选项，以便在故障转移后清理触发器文件。<application>pg_standby</application> 将不再自行删除触发器文件。
     </para>
     </listitem>

     <listitem>
      <para>
      现在 <filename>contrib/pg_standby</filename> 的 <option>-l</option> 选项是无操作，因为使用符号链接不安全（Simon）
     </para>
     </listitem>

    </itemizedlist>

   </sect3>
  </sect2>
 </sect1>
