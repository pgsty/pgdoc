<!-- $PostgreSQL: pgsql/doc/src/sgml/contrib.sgml,v 1.18 2010/06/14 17:25:24 sriggs Exp $ -->

<appendix id="contrib">
 <title>额外提供的模块与扩展</title>

 <para>
  本附录包含关于 <productname>PostgreSQL</> 发布的
  <literal>contrib</literal> 目录中各模块的信息。其中包括移植工具、分析实用程序，以及不属于 PostgreSQL 核心系统的插件特性——之所以不纳入核心，主要是因为它们面向有限的用户群，或者过于实验性，不适合进入主源码树。但这并不妨碍它们发挥作用。
 </para>

 <para>
  从源码发布构建时，除非构建"world"目标（见<xref linkend="build">），否则这些模块不会自动构建。你可以用以下命令构建并安装所有这些模块：
<screen>
<userinput>gmake</userinput>
<userinput>gmake install</userinput>
</screen>
  在已完成配置的源码树的 <literal>contrib</literal> 目录中运行；或者，要只构建并安装某一个选定的模块，就在该模块的子目录中执行相同的操作。许多模块带有回归测试，可以通过运行以下命令执行：
<screen>
<userinput>gmake installcheck</userinput>
</screen>
  前提是已有一个 <productname>PostgreSQL</> 服务器在运行。（注意，这里不支持 <literal>gmake check</>；你必须有一个可用的数据库服务器来执行这些测试，而且必须已经构建并安装了要测试的模块。）
 </para>

 <para>
  如果你使用的是预打包版本的 <productname>PostgreSQL</productname>，这些组件通常会作为单独的子包提供，例如
  <literal>postgresql-contrib</literal>。
 </para>

 <para>
  许多模块提供了新的用户定义函数、操作符或类型。要使用其中某个模块，在安装其代码之后，你还需要运行该模块提供的
  <literal>.sql</> 文件中的 SQL 命令，把这些新对象注册到数据库系统中。例如，

<programlisting>
psql -d dbname -f <replaceable>SHAREDIR</>/contrib/<replaceable>module</>.sql
</programlisting>

  这里的 <replaceable>SHAREDIR</> 指安装的<quote>share</quote>目录（<literal>pg_config --sharedir</> 会告诉你它是什么）。在大多数情况下，该脚本必须由数据库超级用户运行。
 </para>

 <para>
  你需要在每一个希望使用该模块功能的数据库中运行这个
  <literal>.sql</> 文件。或者，在数据库 <literal>template1</> 中运行它，这样该模块默认会被复制到随后创建的数据库中。
 </para>

 <para>
  你可以修改 <literal>.sql</> 文件中的第一条命令，来决定模块的对象创建在数据库中的哪个模式里。默认情况下，它们会被放在
  <literal>public</literal> 中。
 </para>

 <para>
  在 <productname>PostgreSQL</> 大版本升级之后，请再次运行安装脚本，即使模块的对象可能已通过转储和恢复从旧安装迁移过来。这样可以确保任何新函数都可用，并且所有必要的修正都会被应用。
 </para>

 &adminpack;
 &auto-explain;
 &btree-gin;
 &btree-gist;
 &chkpass;
 &citext;
 &cube;
 &dblink;
 &dict-int;
 &dict-xsyn;
 &earthdistance;
 &fuzzystrmatch;
 &hstore;
 &intagg;
 &intarray;
 &isn;
 &lo;
 &ltree;
 &oid2name;
 &pageinspect;
 &passwordcheck;
 &pgarchivecleanup;
 &pgbench;
 &pgbuffercache;
 &pgcrypto;
 &pgfreespacemap;
 &pgrowlocks;
 &pgstandby;
 &pgstatstatements;
 &pgstattuple;
 &pgtrgm;
 &pgupgrade;
 &seg;
 &contrib-spi;
 &sslinfo;
 &tablefunc;
 &test-parser;
 &tsearch2;
 &unaccent;
 &uuid-ossp;
 &vacuumlo;
 &xml2;

</appendix>
