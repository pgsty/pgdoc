<!--
$Header: /cvsroot/pgsql/doc/src/sgml/ref/set.sgml,v 1.68 2002/10/13 16:55:05 tgl Exp $
PostgreSQL documentation
-->

<refentry id="SQL-SET">
 <refmeta>
  <refentrytitle id="SQL-SET-TITLE">SET</refentrytitle>
  <refmiscinfo>SQL - 语言语句</refmiscinfo>
 </refmeta>
 <refnamediv>
  <refname>SET</refname>
  <refpurpose>更改一个运行时参数</refpurpose>
 </refnamediv>
 <refsynopsisdiv>
  <synopsis>
SET [ SESSION | LOCAL ] <replaceable class="PARAMETER">variable</replaceable> { TO | = } { <replaceable class="PARAMETER">value</replaceable> | '<replaceable class="PARAMETER">value</replaceable>' | DEFAULT }
SET [ SESSION | LOCAL ] TIME ZONE { <replaceable class="PARAMETER">timezone</replaceable> | LOCAL | DEFAULT }
  </synopsis>

  <refsect2 id="R2-SQL-SET-1">
   <title>输入</title>
   <para>
    <variablelist>

     <varlistentry>
      <term><option>SESSION</option></term>
      <listitem>
       <para>
        指定命令对当前会话生效。
        （如果 SESSION 和 LOCAL 都未出现，
        这是默认行为。）
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>LOCAL</option></term>
      <listitem>
       <para>
        指定命令只对当前事务生效。在 <command>COMMIT</command> 或 <command>ROLLBACK</command>
        之后，会话级的设置重新生效。注意，
        如果在 <command>BEGIN</command> 块之外执行
        <command>SET LOCAL</command>，它看起来
        不会有任何效果，因为事务
        会立即结束。
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><replaceable class="PARAMETER">variable</replaceable></term>
      <listitem>
       <para>
        一个可设置的运行时参数。
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><replaceable class="PARAMETER">value</replaceable></term>
      <listitem>
       <para>
        参数的新值。<option>DEFAULT</option> 可用于
        指定把参数重置为默认
        值。允许使用字符串列表，但更复杂的
        构造可能需要加单引号或双引号。
       </para>
      </listitem>
     </varlistentry>
    </variablelist>
   </para>
  </refsect2>

 </refsynopsisdiv>
 
 <refsect1 id="R1-SQL-SET-1">
  <title>描述</title>

  <para>
   <command>SET</command> 命令更改运行时配置
   参数。<citetitle>管理员指南</citetitle> 中列出的许多运行时参数都可以用
   <command>SET</command> 即时更改。
   （但有些需要超级用户权限
   才能更改，另一些在服务器或会话启动后不能更改。）
   注意 <command>SET</command> 只影响当前
   会话使用的值。
  </para>

  <para>
   如果在一个随后被中止的事务中发出<command>SET</command>或<command>SET SESSION</command>，
   那么在事务回滚时，<command>SET</command>命令的效果就会消失。
   （这一行为与<productname>PostgreSQL</productname> 7.3 之前的版本不同，
   在那些版本中，<command>SET</command>的效果不会在后续出错后回滚。）
   一旦该事务提交，这些效果
   就会持续到会话结束，除非被另一个<command>SET</command>覆盖。
  </para>

  <para>
   <command>SET LOCAL</command>的效果只持续到当前事务结束，
   无论事务是否提交。一种特殊情况是：在同一个事务中先执行
   <command>SET</command>，再执行<command>SET LOCAL</command>。
   这样<command>SET LOCAL</command>设置的值会一直生效到事务结束，
   但在此之后（如果该事务被提交），<command>SET</command>设置的值
   将生效。
  </para>

  <para>
   即使把 <literal>autocommit</literal> 设为 <literal>off</literal>，<command>SET</command>
   也不会开始一个新的事务块。详情见
   <citetitle>管理员指南</citetitle> 的
   <literal>autocommit</literal> 一节。
  </para>

  <para>
   下面是可以设置的一些参数的补充细节：

   <variablelist>
    <varlistentry>
     <term><varname>DATESTYLE</varname></term>
     <listitem>
      <para>
       选择日期/时间的表示风格。这里涉及两个独立的
       设置：默认的日期/时间输出和对有歧义输入的
       解释。
      </para>

      <para>
       以下是日期/时间的输出风格：

       <variablelist>
        <varlistentry>
         <term><literal>ISO</literal></term>
         <listitem>
          <para>
           使用 ISO 8601 风格的日期和时间（<literal>YYYY-MM-DD
            HH:MM:SS</literal>）。这是默认值。
          </para>
         </listitem>
        </varlistentry>

        <varlistentry>
         <term><literal>SQL</literal></term>
         <listitem>
          <para>
           使用 Oracle/Ingres 风格的日期和时间。注意这种
           风格与 SQL（它强制使用 ISO 8601
           风格）毫无关系，这个选项的命名是一个历史意外。
          </para>
         </listitem>
        </varlistentry>

        <varlistentry>
         <term><literal>PostgreSQL</literal></term>
         <listitem>
          <para>
           使用传统的 <productname>PostgreSQL</productname> 格式。
          </para>
         </listitem>
        </varlistentry>

        <varlistentry>
         <term><literal>German</literal></term>
         <listitem>
          <para>
           数值日期表示使用 <literal>dd.mm.yyyy</literal>。
          </para>
         </listitem>
        </varlistentry>
       </variablelist>
      </para>

      <para>
       下面两个选项既决定 <quote>SQL</quote> 和 <quote>PostgreSQL</quote> 输出格式的
       子风格，也决定有歧义日期输入的首选
       解释。

       <variablelist>
        <varlistentry>
         <term><literal>European</literal></term>
         <listitem>
          <para>
           数值日期表示使用 <literal>dd/mm/yyyy</literal>。
          </para>
         </listitem>
        </varlistentry>

        <varlistentry>
         <term><literal>NonEuropean</literal></term>
         <term><literal>US</literal></term>
         <listitem>
          <para>
           数值日期表示使用 <literal>mm/dd/yyyy</literal>。
          </para>
         </listitem>
        </varlistentry>
       </variablelist>
      </para>

      <para>
       <command>SET DATESTYLE</command> 的值可以取自
       第一个列表（输出风格），或第二个列表
       （子风格），或从两个列表中各取一个并用逗号分隔。
      </para>

      <para>
       <command>SET DATESTYLE</command> 影响输入的解释
       并提供几种标准输出格式。如果
       应用需要不同的变化形式或对输入输出的更严格
       控制，可以考虑使用
       <function>to_char</function> 函数
       族。
      </para>

      <para>
       除了通过 <command>SET</command> 或
       配置文件条目设置日期风格的正常方法之外，还有几种
       现已不推荐的方法：
       <simplelist>
        <member>
         设置 postmaster 的 <envar>PGDATESTYLE</envar> 环境
         变量。（它会被任何其他方法
         覆盖。）
        </member>
        <member>
         使用选项 <option>-o -e</option> 运行 postmaster，把日期设为
         <literal>European</literal> 约定。
         （这会覆盖环境变量和配置文件
         条目。）
        </member>
        <member>
         设置客户端的 <envar>PGDATESTYLE</envar> 环境变量。
         如果在基于 <application>libpq</application> 的客户端前端环境中设置了
         <envar>PGDATESTYLE</envar>，<application>libpq</application> 会在连接启动时自动把 <varname>DATESTYLE</varname> 设为
         <envar>PGDATESTYLE</envar> 的值。这等价于
         手动发出 <command>SET DATESTYLE</command>。
        </member>
       </simplelist>
      </para>

     </listitem>
    </varlistentry>

    <varlistentry>
     <term>NAMES</term>
     <listitem>
      <para>
       <command>SET NAMES</command> 是 <command>SET CLIENT_ENCODING</command> 的别名。
      </para>
     </listitem>
    </varlistentry>

     <varlistentry>
      <term>SEED</term>
      <listitem>
       <para>
       设置随机数生成器的内部种子。
	
       <variablelist>
        <varlistentry>
         <term><replaceable class="parameter">value</replaceable></term>
         <listitem>
          <para>
           <function>random</function> 函数使用的
           种子的值。允许的
           值是 0 到 1 之间的浮点数，之后会
           乘以 2<superscript>31</superscript>-1。
          </para>
         </listitem>
        </varlistentry>
       </variablelist>
      </para>

      <para>
       种子也可以通过调用
       <function>setseed</function> SQL 函数来设置：

       <programlisting>
SELECT setseed(<replaceable>value</replaceable>);
       </programlisting>
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>SERVER_ENCODING</term>
     <listitem>
      <para>
       显示服务器端的多字节编码。（目前这个
       参数只能显示不能设置，因为编码是在
       <application>initdb</application> 时确定的。）
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>TIME ZONE</term>
     <term>TIME ZONE</term>
     <listitem>
      <para>
       设置会话的默认时区。参数可以是
       SQL 时间间隔常量、整数或双精度
       常量，或者一个表示宿主操作系统
       可识别的时区名的字符串。
      </para>

      <para>
       下面是一些典型的时区设置值：

       <variablelist>
        <varlistentry>
         <term>'PST8PDT'</term>
         <listitem>
          <para>
           把时区设为加利福尼亚州伯克利。
          </para>
         </listitem>
        </varlistentry>
        <varlistentry>
         <term>'Portugal'</term>
         <listitem>
          <para>
           把时区设为葡萄牙。
          </para>
         </listitem>
        </varlistentry>
        <varlistentry>
         <term>'Europe/Rome'</term>
         <listitem>
          <para>
           把时区设为意大利。
          </para>
         </listitem>
        </varlistentry>
        <varlistentry>
         <term>7</term>
         <listitem>
          <para>
           把时区设为 GMT 以西 7 小时偏移（相当于
           PDT）。
          </para>
         </listitem>
        </varlistentry>
        <varlistentry>
         <term>INTERVAL '08:00' HOUR TO MINUTE</term>
         <listitem>
          <para>
           把时区设为 GMT 以西 8 小时偏移（相当于
           PST）。
          </para>
         </listitem>
        </varlistentry>
        <varlistentry>
         <term>LOCAL</term>
         <term>DEFAULT</term>
         <listitem>
          <para>
           把时区设为你的本地时区（你的
           操作系统默认的时区）。
          </para>
         </listitem>
        </varlistentry>
       </variablelist>
      </para>

      <para>
       可用的时区名取决于你的操作系统。例如在 Linux 上，
       <filename>/usr/share/zoneinfo</filename> 包含时区
       数据库；该目录中文件的名称可以
       用作这个命令的参数。
      </para>

      <para>
       如果指定了无效的时区，时区
       会变成 GMT（至少在大多数系统上如此）。
      </para>

      <para>
       如果在基于 <application>libpq</application> 的客户端前端环境中设置了
       <envar>PGTZ</envar> 环境变量，<application>libpq</application> 会在
       连接启动时自动把
       <command>SET TIMEZONE</command> 设为
       <envar>PGTZ</envar> 的值。 
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
  </para>

  <para>
   使用 <xref linkend="SQL-SHOW" endterm="SQL-SHOW-title"> 显示
   参数的当前设置。
  </para>
  
 </refsect1>

 <refsect1>
  <title>诊断</title>
  <para>
    
   <variablelist>
    <varlistentry>
     <term><computeroutput>SET</computeroutput></term>
     <listitem>
      <para>
       成功时返回的消息。
      </para>
     </listitem>
    </varlistentry>
     
    <varlistentry>
     <term><computeroutput>ERROR:  '<replaceable>name</replaceable> is not a
	 valid option name</computeroutput></term>
     <listitem>
      <para>
       你试图设置的参数不存在。
      </para>
     </listitem>
    </varlistentry>
     
    <varlistentry>
     <term><computeroutput>ERROR:  '<replaceable>name</replaceable>':
	 permission denied</computeroutput></term>
     <listitem>
      <para>
       你必须是超级用户才能更改某些设置。
      </para>
     </listitem>
    </varlistentry>
     
    <varlistentry>
     <term><computeroutput>ERROR:  '<replaceable>name</replaceable>' cannot
	 be changed after server start</computeroutput></term>
     <listitem>
      <para>
       有些参数在服务器启动后就是固定的。
      </para>
     </listitem>
    </varlistentry>
     
   </variablelist>
  </para>
 </refsect1>

 
 <refsect1>
  <title>示例</title>
  <para>
   把日期风格设为带欧洲约定的传统
   <productname>PostgreSQL</productname> 风格：
   <screen>
SET DATESTYLE TO PostgreSQL,European;
   </screen>
  </para>

  <para>
   把时区设为加利福尼亚州伯克利，使用引号来
   保留时区名的大写拼写（注意这个
   例子中的日期风格是 <literal>PostgreSQL</literal>）： 

   <screen>
SET TIME ZONE 'PST8PDT';
SELECT CURRENT_TIMESTAMP AS today;
               today                
------------------------------------
 Tue Feb 26 07:32:21.42834 2002 PST
   </screen>
  </para>

  <para>
   把时区设为意大利（注意需要单引号来处理
   特殊字符）：

   <screen>
SET TIME ZONE 'Europe/Rome';
SELECT CURRENT_TIMESTAMP AS today;

             today             
-------------------------------
 2002-10-08 05:39:35.008271+02
   </screen>
  </para>
 </refsect1>

 <refsect1 id="R1-SQL-SET-3">
  <title>兼容性</title>

  <refsect2 id="R2-SQL-SET-4">
   <title>
    SQL92
   </title>

   <para>
    <literal>SET TIME ZONE</literal>
    扩展了
    <acronym>SQL9x</acronym> 中定义的语法。<acronym>SQL9x</acronym> 只允许
    数值形式的时区偏移，而
    <productname>PostgreSQL</productname> 还允许完整的时区
    说明字符串。所有其他 <literal>SET</literal>
    特性都是
    <productname>PostgreSQL</productname> 扩展。
   </para>
  </refsect2>
 </refsect1>

 <refsect1>
  <title>另见</title>

  <para>
    <function>set_config</function> 函数提供了等价的
    功能。见 <citetitle>PostgreSQL 用户指南</citetitle> 中的
    <citetitle>杂项函数</citetitle>。
  </para>
 </refsect1>
</refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode:sgml
sgml-omittag:nil
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
sgml-parent-document:nil
sgml-default-dtd-file:"../reference.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:("/usr/lib/sgml/catalog")
sgml-local-ecat-files:nil
End:
-->
