<!-- $Header: /cvsroot/pgsql/doc/src/sgml/ref/create_cast.sgml,v 1.6.2.1 2003/02/01 22:09:41 tgl Exp $ -->

<refentry id="SQL-CREATECAST">
 <refmeta>
  <refentrytitle id="SQL-CREATECAST-TITLE">CREATE CAST</refentrytitle>
  <refmiscinfo>SQL - 语言语句</refmiscinfo>
 </refmeta>

 <refnamediv>
  <refname>CREATE CAST</refname>
  <refpurpose>定义一个用户定义的转换</refpurpose>
 </refnamediv>

 <refsynopsisdiv>
<synopsis>
CREATE CAST (<replaceable>sourcetype</replaceable> AS <replaceable>targettype</replaceable>)
    WITH FUNCTION <replaceable>funcname</replaceable> (<replaceable>argtype</replaceable>)
    [ AS ASSIGNMENT | AS IMPLICIT ]

CREATE CAST (<replaceable>sourcetype</replaceable> AS <replaceable>targettype</replaceable>)
    WITHOUT FUNCTION
    [ AS ASSIGNMENT | AS IMPLICIT ]
</synopsis>
 </refsynopsisdiv>
  
 <refsect1 id="sql-createcast-description">
  <title>描述</title>

  <para>
   <command>CREATE CAST</command> 定义一个新的转换。转换
   指定如何执行两种数据类型之间的
   转换。例如，
<programlisting>
SELECT CAST(42 AS text);
</programlisting>
   通过调用一个先前指定的函数（这里是
   <literal>text(int4)</>）把整数常量 42 转换为
   <type>text</type> 类型。（如果没有定义合适的转换，
   该转换失败。）
  </para>

  <para>
   两种类型可以是<firstterm>二进制兼容</firstterm>的，这
   意味着它们可以<quote>免费</quote>地互相
   转换而无需调用任何函数。这要求
   相应的值使用相同的内部表示。例如，
   <type>text</type> 和 <type>varchar</type> 类型是
   二进制兼容的。
  </para>
  <para>
   默认情况下，一个转换只能在显式转换请求中被调用，
   也就是显式的 <literal>CAST(<replaceable>x</> AS
   <replaceable>typename</>)</literal>、
   <replaceable>x</><literal>::</><replaceable>typename</> 或
   <replaceable>typename</>(<replaceable>x</>) 构造。
  </para>
  <para>
   如果转换被标记为 <literal>AS ASSIGNMENT</>，那么在向目标数据类型的列赋值时
   它可以被隐式调用。
   例如，假设 <literal>foo.f1</literal> 是一个
   <type>text</type> 类型的列，那么
<programlisting>
INSERT INTO foo(f1) VALUES(42);
</programlisting>
   只有在从 <type>integer</type> 类型到
   <type>text</type> 类型的转换被标记为 <literal>AS ASSIGNMENT</> 时才
   是允许的，否则
   不允许。
   （我们通常用<firstterm>赋值转换</firstterm>这个术语来描述这种转换。）
  </para>
  <para>
   如果转换被标记为 <literal>AS IMPLICIT</>，那么它在任何上下文中都可以被
   隐式调用，无论是赋值还是表达式内部。例如，由于
   <literal>||</> 接受
   <type>text</> 参数，
<programlisting>
SELECT 'The time is ' || now();
</programlisting>
   只有在从 <type>timestamp</> 到
   <type>text</type> 的转换被标记为 <literal>AS IMPLICIT</> 时才
   允许。否则就必须显式写出转换，例如
<programlisting>
SELECT 'The time is ' || CAST(now() AS text);
</programlisting>
   （我们通常用<firstterm>隐式转换</firstterm>这个术语来描述这种转换。）
  </para>
  <para>
   在把转换标记为隐式方面明智的做法是保守。过多的隐式
   转换路径可能导致
   <productname>PostgreSQL</productname> 对命令做出令人惊讶的
   解释，或者因为存在多种可能的解释而完全
   无法解析命令。一个好的
   经验法则是在同一通用类型类别中的类型之间，只让
   保持信息的转换可以隐式调用。例如，从 <type>int2</type> 到
   <type>int4</type> 的转换可以合理地隐式，但从
   <type>float8</type> 到 <type>int4</type> 的转换或许应该
   只用于赋值。跨类型类别的转换，例如从
   <type>text</> 到 <type>int4</>，最好只用于显式转换。
  </para>
  <para>
   要能创建一个转换，你必须拥有源数据类型或目标
   数据类型。要创建一个二进制兼容的转换，你必须
   是超级用户
   （这个限制是因为一个错误的二进制兼容转换
   很容易使服务器崩溃）。
  </para>
 
   <variablelist>
    <title>参数</title>

    <varlistentry>
     <term><replaceable>sourcetype</replaceable></term>
     <listitem>
      <para>
       转换的源数据类型的名称。
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><replaceable>targettype</replaceable></term>
     <listitem>
      <para>
       转换的目标数据类型的名称。
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><replaceable>funcname</replaceable>(<replaceable>argtype</replaceable>)</term>
     <listitem>
      <para>
       用来执行该转换的函数。函数名可以
       带模式限定。如果不带，该函数将在路径中
       查找。参数类型必须与
       源类型相同，结果数据类型必须与该转换的
       目标类型匹配。
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><literal>WITHOUT FUNCTION</literal></term>
     <listitem>
      <para>
       表示源类型和目标类型是二进制
       兼容的，因此不需要函数来执行该转换。
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><literal>AS ASSIGNMENT</literal></term>
     <listitem>
      <para>
       表示该转换可以在赋值
       上下文中被隐式调用。
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><literal>AS IMPLICIT</literal></term>
     <listitem>
      <para>
       表示该转换可以在任何上下文中被隐式
       调用。
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
 </refsect1>
 <refsect1 id="sql-createcast-notes">
  <title>注意</title>
  <para>
   使用 <command>DROP CAST</command> 删除用户定义的转换。
  </para>
  <para>
   记住，如果你希望能够双向转换类型，需要
   显式地在两个方向上声明转换。
  </para>
  <para>
   在 PostgreSQL 7.3 之前，每个与某个数据类型同名、
   返回该数据类型并且接受一个不同类型
   参数的函数都自动是一个转换函数。面对模式（schema）的引入以及为了能在
   目录中表示
   二进制兼容的转换，这一约定已被
   放弃。（内建的转换函数
   仍然遵循这一命名
   方案，但现在它们必须作为转换显示在
   <literal>pg_cast</> 中。）
  </para>
 </refsect1>
 <refsect1 id="sql-createcast-examples">
  <title>示例</title>
  <para>
   要用函数 <literal>int4(text)</literal> 创建一个从
   <type>text</type> 类型到
   <type>int4</type> 类型的转换：
<programlisting>
CREATE CAST (text AS int4) WITH FUNCTION int4(text);
</programlisting>
   （这个转换已经在系统中预定义。）
  </para>
 </refsect1>
 <refsect1 id="sql-createcast-compat">
  <title>兼容性</title>
  <para>
   <command>CREATE CAST</command> 命令符合
   SQL99，只是
   SQL99 没有为二进制兼容类型作规定。
   <literal>AS IMPLICIT</> 也是一种
   <productname>PostgreSQL</productname> 扩展。
  </para>
 </refsect1>
 <refsect1 id="sql-createcast-seealso">
  <title>另见</title>
  <para>
   <xref linkend="sql-createfunction" endterm="sql-createfunction-title">,
   <xref linkend="sql-createtype" endterm="sql-createtype-title">,
   <xref linkend="sql-dropcast" endterm="sql-dropcast-title">,
   <citetitle>PostgreSQL 程序员指南</citetitle>
  </para>
 </refsect1>
</refentry>
<!-- Keep this comment at the end of the file
Local variables:
mode:sgml
sgml-omittag:nil
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
sgml-parent-document:nil
sgml-default-dtd-file:"../reference.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:("/usr/lib/sgml/catalog")
sgml-local-ecat-files:nil
End:
-->
