<!--
$Header: /cvsroot/pgsql/doc/src/sgml/plpgsql.sgml,v 1.8 2002/09/21 18:32:53 petere Exp $
-->

<chapter id="plpgsql"> 
  <title><application>PL/pgSQL</application> - <acronym>SQL</acronym> 过程语言</title>

 <indexterm zone="plpgsql">
  <primary>PL/pgSQL</primary>
 </indexterm>

 <para>
  <application>PL/pgSQL</application> 是一种用于
  <productname>PostgreSQL</productname> 数据库系统的可载入过程语言。
 </para>
   
 <para>
  本包最初由 Jan Wieck 编写。本文档部分由 Roberto Mello（<email>rmello@fslc.usu.edu</email>）编写。
 </para>

<sect1 id="plpgsql-overview">
   <title>概述</title>

   <para>
    <application>PL/pgSQL</application> 的设计目标是创建一种可载入过程语言，它

    <itemizedlist>
     <listitem>
      <para>
       可以用于创建函数和触发器过程，
      </para>
     </listitem>
     <listitem>
      <para>
       为 <acronym>SQL</acronym> 语言增加控制结构，
      </para>
     </listitem>
     <listitem>
      <para>
       可以执行复杂计算，
      </para>
     </listitem>
     <listitem>
      <para>
       继承所有用户定义的类型、函数和操作符，
      </para>
     </listitem>
     <listitem>
      <para>
       可以定义为被服务器信任，
      </para>
     </listitem>
     <listitem>
      <para>
       易于使用。
      </para>
     </listitem>
    </itemizedlist>
   </para>
   <para>
    <application>PL/pgSQL</application> 调用处理器在函数第一次被调用时（在任一后端进程内）解析函数的源文本，并生成一棵内部二进制指令树。这棵指令树完整地翻译了 <application>PL/pgSQL</application> 的语句结构，但函数中使用的各个 <acronym>SQL</acronym> 表达式和 <acronym>SQL</acronym> 查询并不会立即翻译。
   </para>
   <para>
    当每个表达式和 <acronym>SQL</acronym> 查询在函数中第一次被使用时，
    <application>PL/pgSQL</application> 解释器会创建一个预备执行计划（使用
    <acronym>SPI</acronym> 管理器的 <function>SPI_prepare</function> 和
    <function>SPI_saveplan</function> 函数）。之后对该表达式或查询的访问会重用这个预备计划。这样，一个包含条件代码、其中含有许多可能需要执行计划的语句的函数，只会预备并保存那些在数据库连接的生命周期内真正用到的计划。这可以大大减少为过程语言函数中的语句解析和生成查询计划所需的总时间。一个缺点是，某个特定表达式或查询中的错误可能要到执行到函数的那一部分时才会被发现。
   </para>
  <para>
   一旦 <application>PL/pgSQL</application> 为函数中的某个特定查询生成了查询计划，它就会在数据库连接的生命周期内一直重用该计划。这通常对性能有利，但如果你动态地更改数据库模式，也可能引发一些问题。例如：

<programlisting>
CREATE FUNCTION populate() RETURNS INTEGER AS '
DECLARE
    -- Declarations
BEGIN
    PERFORM my_function();
END;
' LANGUAGE 'plpgsql';
</programlisting>
    如果你执行上面的函数，它会在为 PERFORM 语句生成的查询计划中引用
    <function>my_function()</function> 的 OID。之后，如果你删除并重新创建
    <function>my_function()</function>，那么 <function>populate()</function>
    将再也找不到 <function>my_function()</function> 了。此时你必须重新创建
    <function>populate()</function>，或者至少启动一个新的数据库会话，使它得以重新编译。
   </para>

   <para>
    由于 <application>PL/pgSQL</application> 以这种方式保存执行计划，直接出现在 <application>PL/pgSQL</application> 函数中的查询在每次执行时都必须引用相同的表和字段；也就是说，你不能在查询中把参数用作表名或字段名。要绕过这一限制，可以用 <application>PL/pgSQL</application> 的
    EXECUTE 语句构造动态查询——代价是每次执行都要构造一个新的查询计划。
   </para>

   <note>
	<para>
	 <application>PL/pgSQL</application> 的 EXECUTE 语句与
	 <productname>PostgreSQL</productname> 后端支持的 EXECUTE 语句无关。后端的 EXECUTE 语句不能在 <application>PL/pgSQL</application> 函数中使用（也不需要）。
	</para>
   </note>

   <para>
    除了用户定义类型的输入/输出转换和计算函数之外，凡是可以用 C 语言函数定义的事情，也都可以用 <application>PL/pgSQL</application> 完成。可以创建复杂条件计算函数，之后用它们定义操作符或在函数索引中使用。
   </para>
  <sect2 id="plpgsql-advantages">
   <title>使用 <application>PL/pgSQL</application> 的优点</title>

   <itemizedlist>
    <listitem>
     <para>
      更好的性能（见<xref linkend="plpgsql-advantages-performance">）
     </para>
    </listitem>

    <listitem>
     <para>
      SQL 支持（见<xref linkend="plpgsql-advantages-sqlsupport">）
     </para>
    </listitem> 

    <listitem>
     <para>
      可移植性（见<xref linkend="plpgsql-advantages-portability">）
     </para>
    </listitem>
   </itemizedlist>

   <sect3 id="plpgsql-advantages-performance">
    <title>更好的性能</title>

    <para>
	  <acronym>SQL</acronym> 是 <productname>PostgreSQL</>（以及大多数其他关系数据库）用作查询语言的语言。它可移植且易学。但每条
	  <acronym>SQL</acronym> 语句都必须由数据库服务器逐条执行。
    </para>

    <para>
     这意味着你的客户端应用程序必须把每个查询发送到数据库服务器，等待其处理，接收结果，做一些计算，然后再向服务器发送其他查询。所有这些都会产生进程间通信开销；如果你的客户端与数据库服务器不在同一台机器上，还会产生网络开销。
    </para>

    <para>
     使用 <application>PL/pgSQL</application>，你可以把一块计算和一系列查询组合在数据库服务器<emphasis>内部</emphasis>，从而既拥有过程语言的能力，又拥有 SQL 的易用性，同时因为省去了整个客户端/服务器通信开销而节省大量时间。这可以带来可观的性能提升。
    </para>
   </sect3>

   <sect3 id="plpgsql-advantages-sqlsupport">
    <title>SQL 支持</title>

    <para>
     <application>PL/pgSQL</application> 为
     <acronym>SQL</acronym> 的灵活和易用增添了过程语言的能力。使用
     <application>PL/pgSQL</application> 可以使用 SQL 的所有数据类型、列、操作符和函数。    
    </para>
   </sect3>

   <sect3 id="plpgsql-advantages-portability">
    <title>可移植性</title>

    <para>
	  由于 <application>PL/pgSQL</application> 函数运行在
	  <productname>PostgreSQL</> 内部，这些函数可以在
	  <productname>PostgreSQL</> 运行的任何平台上运行。因此你可以重用代码并降低开发成本。
    </para>
   </sect3>
  </sect2>

  <sect2 id="plpgsql-overview-developing-in-plpgsql">
   <title>在 <application>PL/pgSQL</application> 中开发</title>

   <para>
    在 <application>PL/pgSQL</application> 中开发相当直接，尤其是如果你曾用其他数据库过程语言（如 Oracle 的
    <application>PL/SQL</application>）开发过。在
    <application>PL/pgSQL</application> 中开发的两种好方法是：

    <itemizedlist>
     <listitem>
      <para>
       使用文本编辑器并用 <command>psql</command> 重新装载文件
      </para>
     </listitem>

     <listitem>
      <para>
       使用 <productname>PostgreSQL</> 的 GUI 工具：
       <application>PgAccess</>
      </para>
     </listitem>
    </itemizedlist>
   </para>

   <para>
	 在 <application>PL/pgSQL</> 中开发的一个好方法是，直接用你选定的文本编辑器创建函数，同时在另一个窗口用 <command>psql</command>（<productname>PostgreSQL</> 的交互监视器）装载这些函数。如果你这样做，最好用 <command>CREATE OR REPLACE FUNCTION</command> 编写函数。这样只需重新装载文件即可更新函数定义。例如：
<programlisting>
CREATE OR REPLACE FUNCTION testfunc(INTEGER) RETURNS INTEGER AS '
	  ....
end;
' LANGUAGE 'plpgsql';
</programlisting>
   </para>

   <para>
    在运行 <command>psql</command> 期间，可以用
<programlisting>
    \i filename.sql
</programlisting>
    装载或重新装载这样的函数定义文件，然后立即发出 SQL 命令来测试该函数。
   </para>

   <para>
    在 <application>PL/pgSQL</application> 中开发的另一个好方法是使用
    <productname>PostgreSQL</> 的 GUI 工具：<application>PgAccess</>。它会为你做一些好事，比如转义单引号、让重新创建和调试函数变得容易。
   </para>
  </sect2>
 </sect1>

<sect1 id="plpgsql-structure">
  <title><application>PL/pgSQL</application> 的结构</title>

    <para>
     <application>PL/pgSQL</application> 是一种<emphasis>块结构</emphasis>语言。函数定义的完整文本必须是一个<firstterm>块</>。块的定义为：

<synopsis>
<optional> &lt;&lt;label&gt;&gt; </optional>
<optional> DECLARE
    <replaceable>declarations</replaceable> </optional>
BEGIN
    <replaceable>statements</replaceable>
END;
</synopsis>
    </para>

    <para>
     块的语句部分中的任何<firstterm>语句</>都可以是一个<firstterm>子块</>。子块可用于逻辑分组，或把变量局部化到一小群语句。
    </para>

    <para>
     块前导声明区中声明的变量，在每次进入该块时都会被初始化为它们的默认值，而不是每次函数调用只初始化一次。例如：
<programlisting>
CREATE FUNCTION somefunc() RETURNS INTEGER AS '
DECLARE
   quantity INTEGER := 30;
BEGIN
   RAISE NOTICE ''Quantity here is %'',quantity;  -- Quantity here is 30
   quantity := 50;
   --
   -- Create a sub-block
   --
   DECLARE
      quantity INTEGER := 80;
   BEGIN
      RAISE NOTICE ''Quantity here is %'',quantity;  -- Quantity here is 80
   END;

   RAISE NOTICE ''Quantity here is %'',quantity;  -- Quantity here is 50

   RETURN quantity;
END;
' LANGUAGE 'plpgsql';
</programlisting>
    </para>

    <para>
     重要的是，不要把 <application>PL/pgSQL</application> 中用于语句分组的
     BEGIN/END 与事务控制的数据库命令混淆。
     <application>PL/pgSQL</application> 的 BEGIN/END 只用于分组；它们不开始或结束事务。函数和触发器过程总是在由外层查询建立的事务中执行——
     它们不能开始或提交事务，因为 <productname>PostgreSQL</productname>
     没有嵌套事务。
    </para>

   <sect2>
    <title>词法细节</title>

    <para>
     块中的每条语句和每个声明都以分号结束。
    </para>

    <para>
     所有关键字和标识符都可以用大小写混合书写。除非加双引号，标识符会被隐式转换为小写。
    </para>

    <para>
     <application>PL/pgSQL</application> 中有两类注释。双划线 <literal>--</literal>
     开始一个延续到行尾的注释。<literal>/*</literal> 开始一个延续到下一个
     <literal>*/</literal> 的块注释。块注释不能嵌套，但双划线注释可以包含在块注释中，双划线也可以隐藏块注释定界符 <literal>/*</literal> 和
     <literal>*/</literal>。
    </para>
   </sect2>
  </sect1>

<sect1 id="plpgsql-declarations">
    <title>声明</title>

    <para>
     块中使用的所有变量、行和记录都必须在该块的声明区中声明。（唯一的例外是，在整数值范围上迭代的 FOR 循环的循环变量会被自动声明为
     integer 变量。）
    </para>

    <para>
     <application>PL/pgSQL</> 变量可以有任何 SQL 数据类型，例如
     <type>INTEGER</type>、<type>VARCHAR</type> 和
     <type>CHAR</type>。
    </para>

    <para>
     下面是一些变量声明的例子：
<programlisting>
user_id INTEGER;
quantity NUMERIC(5);
url VARCHAR;
myrow tablename%ROWTYPE;
myfield tablename.fieldname%TYPE;
arow RECORD;
</programlisting>
    </para>

    <para>
     变量声明的一般语法为：
<synopsis>
<replaceable>name</replaceable> <optional> CONSTANT </optional> <replaceable>type</replaceable> <optional> NOT NULL </optional> <optional> { DEFAULT | := } <replaceable>expression</replaceable> </optional>;
</synopsis>
     </para>

     <para>
      如果给出了 DEFAULT 子句，它指定进入块时赋给变量的初始值。如果没有给出 DEFAULT 子句，变量被初始化为 <acronym>SQL</acronym> 空值。
     </para>

     <para>
      CONSTANT 选项阻止对变量赋值，使其值在块的持续期间保持不变。如果指定了 NOT NULL，赋予空值会导致运行时错误。所有声明为 NOT NULL
      的变量都必须指定一个非空默认值。
     </para>

     <para>
      默认值在每次进入块时求值。因此，例如把 '<literal>now</literal>'
      赋给一个 <type>timestamp</type> 类型的变量，会使该变量具有当前函数调用的时间，而不是函数预编译时的时间。
     </para>

     <para>
      例子：
<programlisting>
quantity INTEGER DEFAULT 32;
url varchar := ''http://mysite.com'';
user_id CONSTANT INTEGER := 10;
</programlisting>
     </para>

    <sect2 id="plpgsql-declaration-aliases">
     <title>函数参数的别名</title>

    <para>
<synopsis>
<replaceable>name</replaceable> ALIAS FOR <replaceable>$n</replaceable>;
</synopsis>
    </para>

     <para>
      传递给函数的参数以标识符 <literal>$1</literal>、<literal>$2</literal>
      等命名。可以选择为 <literal>$n</literal> 参数名声明别名以提高可读性。之后既可以用别名、也可以用数字标识符来引用参数值。一些例子：
<programlisting>
CREATE FUNCTION sales_tax(REAL) RETURNS REAL AS '
DECLARE
    subtotal ALIAS FOR $1;
BEGIN
    return subtotal * 0.06;
END;
' LANGUAGE 'plpgsql';


CREATE FUNCTION instr(VARCHAR,INTEGER) RETURNS INTEGER AS '
DECLARE
    v_string ALIAS FOR $1;
    index ALIAS FOR $2;
BEGIN
    -- Some computations here
END;
' LANGUAGE 'plpgsql';


CREATE FUNCTION use_many_fields(tablename) RETURNS TEXT AS '
DECLARE
    in_t ALIAS FOR $1;
BEGIN
    RETURN in_t.f1 || in_t.f3 || in_t.f5 || in_t.f7;
END;
' LANGUAGE 'plpgsql';
</programlisting>
     </para>
    </sect2>

    <sect2 id="plpgsql-declaration-rowtypes">
     <title>行类型</title>

    <para>
<synopsis>
<replaceable>name</replaceable> <replaceable>tablename</replaceable><literal>%ROWTYPE</literal>;
</synopsis>
    </para>

   <para>
    复合类型的变量称为<firstterm>行</>变量（或<firstterm>行类型</>变量）。这样的变量可以保存
    SELECT 或 FOR
    查询结果的一整行，只要该查询的列集与变量声明的类型相匹配。行值的各个字段用通常的点表示法访问，例如
    <literal>rowvar.field</literal>。
   </para>

   <para>
    目前，行变量只能使用
    <literal>%ROWTYPE</literal>
    表示法声明；虽然你可能期望直接用表名作为类型声明，但这在
    <application>PL/pgSQL</application> 函数中不会被接受。
   </para>

   <para>
    函数的参数可以是复合类型（完整的表行）。这种情况下，对应的标识符
    $n 将是一个行变量，可以从中选择字段，例如
    <literal>$1.user_id</literal>。
   </para>

   <para>
    行类型变量中只能访问表行的用户定义属性，不能访问 OID 或其他系统属性（因为该行可能来自视图）。行类型的字段继承表对于
    <type>char(n)</type> 等数据类型的字段大小或精度。
<programlisting>
CREATE FUNCTION use_two_tables(tablename) RETURNS TEXT AS '
DECLARE
    in_t ALIAS FOR $1;
    use_t table2name%ROWTYPE;
BEGIN
    SELECT * INTO use_t FROM table2name WHERE ... ;
    RETURN in_t.f1 || use_t.f3 || in_t.f5 || use_t.f7;
END;
' LANGUAGE 'plpgsql';
</programlisting>
   </para>
  </sect2>

    <sect2 id="plpgsql-declaration-records">
     <title>记录</title>

    <para>
<synopsis>
<replaceable>name</replaceable> RECORD;
</synopsis>
    </para>

   <para>
    记录变量类似于行类型变量，但它们没有预定义的结构。它们在
    SELECT 或 FOR 命令期间被赋值时，采用被赋行的实际行结构。记录变量的子结构在每次被赋值时都可以改变。这样做的后果是，记录变量在第一次被赋值之前<emphasis>没有</>子结构，任何访问其字段的尝试都会引发运行时错误。
   </para>

   <para>
    注意 <literal>RECORD</> 不是真正的数据类型，只是一个占位符。
   </para>
  </sect2>

    <sect2 id="plpgsql-declaration-attributes">
     <title>属性</title>

     <para>
      使用 <type>%TYPE</type> 和 <type>%ROWTYPE</type>
      属性，可以声明与另一个数据库项（例如一个表字段）具有相同数据类型或结构的变量。
     </para>

     <variablelist>
      <varlistentry>
       <term>
        <replaceable>variable</replaceable>%TYPE
       </term>
       <listitem>
        <para>
         <type>%TYPE</type> 提供一个变量或数据库列的数据类型。可以用它来声明将保存数据库值的变量。例如，假设你的
         <type>users</type> 表中有一个名为 <type>user_id</type> 的列。要声明一个与
         <structname>users</>.<structfield>user_id</> 数据类型相同的变量，可以这样写：
<programlisting>
user_id   users.user_id%TYPE;
</programlisting>
        </para>

        <para>
         使用 <type>%TYPE</type>，你不需要知道所引用结构的数据类型，而且最重要的是，如果所引用项的数据类型将来发生变化（例如你把
         user_id 的表定义从 INTEGER 改为 REAL），可能不需要更改你的函数定义。
        </para>
       </listitem>
      </varlistentry>

      <varlistentry>
       <term>
        <literal><replaceable>table</replaceable>%ROWTYPE</literal>
       </term>
       <listitem>
        <para>
	 <type>%ROWTYPE</type> 提供对应于指定表整行的复合数据类型。<replaceable>table</replaceable> 必须是数据库中现有的表或视图的名称。
        </para>

<programlisting>
DECLARE
    users_rec users%ROWTYPE;
    user_id users.user_id%TYPE;
BEGIN
    user_id := users_rec.user_id;
    ...

CREATE FUNCTION does_view_exist(INTEGER) RETURNS bool AS '
   DECLARE
        key ALIAS FOR $1;
        table_data cs_materialized_views%ROWTYPE;
   BEGIN
        SELECT INTO table_data * FROM cs_materialized_views
               WHERE sort_key=key;

        IF NOT FOUND THEN
           RETURN false;
        END IF;
        RETURN true;
   END;
' LANGUAGE 'plpgsql';
</programlisting>
       </listitem>
      </varlistentry>
     </variablelist>
    </sect2>

    <sect2 id="plpgsql-declaration-renaming-vars">
     <title>RENAME</title>

     <para>
<synopsis>
RENAME <replaceable>oldname</replaceable> TO <replaceable>newname</replaceable>;
</synopsis>

      使用 RENAME 声明可以更改变量、记录或行的名称。这主要用于在触发器过程中用另一个名字引用 NEW 或 OLD。另见 ALIAS。
     </para>

     <para>
      例子：
<programlisting>
RENAME id TO user_id;
RENAME this_var TO that_var;
</programlisting>
     </para>

    <note>
    <para>
	  RENAME 在 <productname>PostgreSQL</>
	  7.3 中似乎是坏的。修复它的优先级很低，因为 ALIAS 已覆盖 RENAME
	  的大多数实际用途。
    </para>
    </note>

    </sect2>
  </sect1>

<sect1 id="plpgsql-expressions">
  <title>表达式</title>

    <para>
     <application>PL/pgSQL</application> 语句中使用的所有表达式都由服务器常规的 SQL 执行器处理。看起来只包含常量的表达式实际上可能需要运行时求值（例如 <type>timestamp</type> 类型的
     <literal>'now'</literal>），因此 <application>PL/pgSQL</application>
     解析器无法识别 NULL 关键字之外的真实常量值。所有表达式都通过使用 <acronym>SPI</acronym> 管理器执行查询
<synopsis>
SELECT <replaceable>expression</replaceable>
</synopsis>
     在内部求值。求值时，<application>PL/pgSQL</application> 变量标识符的出现被替换为参数，变量的实际值通过参数数组传递给执行器。这使
     SELECT 的查询计划只需预备一次，之后即可重用于后续求值。
    </para>

    <para>
     <productname>PostgreSQL</productname> 主解析器所做的求值对常量值的解释有一些副作用。具体来说，下面这两个函数的行为是有差异的：

<programlisting>
CREATE FUNCTION logfunc1 (TEXT) RETURNS TIMESTAMP AS '
    DECLARE
        logtxt ALIAS FOR $1;
    BEGIN
        INSERT INTO logtable VALUES (logtxt, ''now'');
        RETURN ''now'';
    END;
' LANGUAGE 'plpgsql';
</programlisting>

     和

<programlisting>
CREATE FUNCTION logfunc2 (TEXT) RETURNS TIMESTAMP AS '
    DECLARE
        logtxt ALIAS FOR $1;
        curtime timestamp;
    BEGIN
        curtime := ''now'';
        INSERT INTO logtable VALUES (logtxt, curtime);
        RETURN curtime;
    END;
' LANGUAGE 'plpgsql';
</programlisting>

     在 <function>logfunc1()</function> 的情况下，<productname>PostgreSQL</productname> 主解析器在为
     INSERT 预备计划时，知道字符串
     <literal>'now'</literal> 应被解释为 <type>timestamp</type>，因为
     <classname>logtable</classname> 的目标列就是该类型。于是它会在此时把它变成一个常量，而这个常量值在该会话的生命周期内
     <function>logfunc1</function> 的所有调用中都会被使用。不用说，这并不是程序员想要的。
    </para>

    <para>
     在 <function>logfunc2()</function> 的情况下，<productname>PostgreSQL</productname> 主解析器不知道
     <literal>'now'</literal> 应变成什么类型，因此它返回一个包含字符串
     <literal>'now'</literal> 的 <type>text</type> 类型数据值。在随后向局部变量 <varname>curtime</varname> 赋值期间，
     <application>PL/pgSQL</application> 解释器通过调用
     <function>text_out()</function> 和 <function>timestamp_in()</function>
     函数把这个字符串转换为 <type>timestamp</type> 类型。这样，计算出的时间戳就会如程序员所期望的那样在每次执行时更新。
    </para>

    <para>
     记录变量的可变性质在这方面带来一个问题。当记录变量的字段被用于表达式或语句中时，各字段的数据类型在同一表达式的多次调用之间不能改变，因为表达式将使用首次到达该表达式时出现的数据类型来规划。在编写处理多个表的事件的触发器过程时要记住这一点。（必要时可以用
     EXECUTE 绕过这个问题。）
    </para>
  </sect1>

<sect1 id="plpgsql-statements">
  <title>基本语句</title>

   <para>
    在本节及随后几节中，我们描述 <application>PL/pgSQL</application> 显式理解的所有语句类型。凡是没有被识别为这些语句类型之一的内容，都被推定为
    SQL 查询，并被发送到主数据库引擎执行（在此之后，语句中使用的任何
    <application>PL/pgSQL</application> 变量都会被替换）。因此，例如 SQL
    <command>INSERT</>、<command>UPDATE</> 和 <command>DELETE</> 命令可以被视为 <application>PL/pgSQL</application> 的语句，但这里不专门列出。
   </para>

   <sect2 id="plpgsql-statements-assignment">
    <title>赋值</title>

    <para>
     向变量或行/记录字段赋值写作：
<synopsis>
<replaceable>identifier</replaceable> := <replaceable>expression</replaceable>;
</synopsis>
     如上文所述，这种语句中的表达式通过向主数据库引擎发送 SQL
     <command>SELECT</> 命令来求值。表达式必须产生单个值。
    </para>

    <para>
     如果表达式的结果数据类型与变量的数据类型不匹配，或者变量有特定的大小/
     精度（比如 <type>char(20)</type>），结果值将由
     <application>PL/pgSQL</application> 解释器使用结果类型的输出函数和变量类型的输入函数隐式转换。注意，如果结果值的字符串形式不被输入函数接受，这可能潜在地导致输入函数产生运行时错误。
    </para>

    <para>
     例子：
<programlisting>
user_id := 20;
tax := subtotal * 0.06;
</programlisting>
    </para>
   </sect2>

   <sect2 id="plpgsql-select-into">
    <title>SELECT INTO</title>

    <para>
     产生多列（但只有一行）的 SELECT 命令的结果可以赋给一个记录变量、行类型变量或标量变量列表。写法是：

<synopsis>
SELECT INTO <replaceable>target</replaceable> <replaceable>expressions</replaceable> FROM ...;
</synopsis>

     其中 <replaceable>target</replaceable> 可以是记录变量、行变量，或由简单变量和记录/行字段组成的逗号分隔列表。注意，这与
     <productname>PostgreSQL</> 对 SELECT INTO 的正常解释大不相同——后者的
     INTO 目标是一张新建的表。（如果你想在
     <application>PL/pgSQL</application> 函数内部从 SELECT 结果创建表，请使用语法 <command>CREATE TABLE ... AS SELECT</command>。）
    </para>

    <para>
     如果用一行或一个变量列表作为目标，选出的值必须与目标的结构完全匹配，否则会发生运行时错误。当目标是记录变量时，它会自动把自己配置为查询结果列的行类型。
    </para>

    <para>
     除了 INTO 子句之外，SELECT 语句与普通 SQL
     SELECT 查询相同，可以使用其全部能力。
    </para>

    <para>
     如果 SELECT 查询返回零行，会向目标赋予空值。如果 SELECT 查询返回多行，第一行被赋给目标，其余的都被丢弃。（注意，除非你使用了
     ORDER BY，否则<quote>第一行</>没有明确的定义。）
    </para>

    <para>
     目前，INTO 子句几乎可以出现在 SELECT
     查询的任何位置，但建议像上面描述的那样把它紧放在
     SELECT 关键字之后。<application>PL/pgSQL</application>
     的未来版本可能对 INTO 子句的位置不那么宽容。
    </para>

    <para>
     你可以在 SELECT INTO 语句之后立即使用
     <literal>FOUND</literal> 来判断赋值是否成功（即 SELECT
     语句是否至少返回了一行）。例如：

<programlisting>
SELECT INTO myrec * FROM EMP WHERE empname = myname;
IF NOT FOUND THEN
    RAISE EXCEPTION ''employee % not found'', myname;
END IF;
</programlisting>

     要测试记录/行结果是否为空，也可以使用 <literal>IS NULL</literal>（或 <literal>ISNULL</>）条件。但没有办法知道是否有额外的行被丢弃。
    </para>

    <para>
<programlisting>
DECLARE
    users_rec RECORD;
    full_name varchar;
BEGIN
    SELECT INTO users_rec * FROM users WHERE user_id=3;

    IF users_rec.homepage IS NULL THEN
        -- user entered no homepage, return "http://"

        RETURN ''http://'';
    END IF;
END;
</programlisting>
    </para>
   </sect2>

   <sect2 id="plpgsql-statements-perform">
    <title>执行无结果的表达式或查询</title>

    <para>
     有时人们希望求一个表达式或查询的值但丢弃其结果（通常是因为正在调用一个有有用副作用但没有有用结果值的函数）。在
     <application>PL/pgSQL</application> 中要做到这一点，使用
     PERFORM 语句：

<synopsis>
PERFORM <replaceable>query</replaceable>;
</synopsis>

     这会执行一条 <command>SELECT</command>
     <replaceable>query</replaceable> 并丢弃结果。
     <application>PL/pgSQL</application> 变量照常在查询中被替换。此外，如果查询产生了至少一行，特殊变量 <literal>FOUND</literal> 会被设置为真；如果没有产生行，则为假。
    </para>

    <note>
    <para>
     人们可能以为不带 INTO 子句的 SELECT
     能达到这个效果，但目前唯一被接受的方式是 PERFORM。
    </para>
    </note>

    <para>
     一个例子：
<programlisting>
PERFORM create_mv(''cs_session_page_requests_mv'', my_query);
</programlisting>
    </para>
   </sect2>

   <sect2 id="plpgsql-statements-executing-dyn-queries">
    <title>执行动态查询</title>

    <para>
     很多时候你会想在 <application>PL/pgSQL</application> 函数内部生成动态查询，也就是每次执行时会涉及不同的表或不同数据类型的查询。
     <application>PL/pgSQL</application> 为查询缓存计划的常规做法在这种场景下行不通。为处理这类问题，提供了 EXECUTE
     语句：

<synopsis>
EXECUTE <replaceable class="command">query-string</replaceable>;
</synopsis>

     其中 <replaceable>query-string</replaceable> 是一个产生字符串（类型为 <type>text</type>）的表达式，该字符串包含要执行的
     <replaceable>query</replaceable>。这个字符串被按字面送给 SQL 引擎。
    </para>

    <para>
     特别注意，查询字符串不做 <application>PL/pgSQL</> 变量替换。变量的值必须在构造查询字符串时插入其中。
    </para>

    <para>
    处理动态查询时，你将不得不面对 <application>PL/pgSQL</application>
    中单引号的转义问题。请参阅<xref linkend="plpgsql-porting">中的表，那里有详细的解释，可以为你省些力气。
    </para>

    <para>
     与 <application>PL/pgSQL</> 中的所有其他查询不同，由
     EXECUTE 语句运行的 <replaceable>query</replaceable>
     不会在服务器生命周期内只预备和保存一次。相反，该
     <replaceable>query</replaceable> 在语句每次运行时都会重新预备。<replaceable>query-string</replaceable> 可以在函数内动态创建，以对可变的表和字段执行操作。
    </para>

    <para>
     SELECT 查询的结果会被 EXECUTE 丢弃，而且
     SELECT INTO 目前不能在 EXECUTE 中使用。因此，从动态创建的 SELECT 中提取结果的唯一方法是使用稍后描述的
     FOR-IN-EXECUTE 形式。
    </para>

    <para>
     一个例子：
<informalexample>
<programlisting>
EXECUTE ''UPDATE tbl SET ''
        || quote_ident(fieldname)
        || '' = ''
        || quote_literal(newvalue)
        || '' WHERE ...'';
</programlisting>
</informalexample>
    </para>

    <para>
     这个例子展示了函数
     <function>quote_ident</function>(<type>TEXT</type>) 和
     <function>quote_literal</function>(<type>TEXT</type>) 的使用。包含字段和表标识符的变量应传递给函数
     <function>quote_ident()</function>。包含动态查询字符串字面量元素的变量应传递给 <function>quote_literal()</function>。两者都会采取适当的步骤，返回以单引号或双引号括起的输入文本，并正确转义任何嵌入的特殊字符。
    </para>

    <para>
     下面是一个大得多的动态查询与 EXECUTE 的例子：
<programlisting>
CREATE FUNCTION cs_update_referrer_type_proc() RETURNS INTEGER AS '
DECLARE
    referrer_keys RECORD;  -- Declare a generic record to be used in a FOR
    a_output varchar(4000);
BEGIN 
    a_output := ''CREATE FUNCTION cs_find_referrer_type(varchar,varchar,varchar) 
                  RETURNS VARCHAR AS '''' 
                     DECLARE 
                         v_host ALIAS FOR $1; 
                         v_domain ALIAS FOR $2; 
                         v_url ALIAS FOR $3;
                     BEGIN ''; 

    -- 
    -- Notice how we scan through the results of a query in a FOR loop
    -- using the FOR &lt;record&gt; construct.
    --

    FOR referrer_keys IN SELECT * FROM cs_referrer_keys ORDER BY try_order LOOP
        a_output := a_output || '' IF v_'' || referrer_keys.kind || '' LIKE '''''''''' 
                 || referrer_keys.key_string || '''''''''' THEN RETURN '''''' 
                 || referrer_keys.referrer_type || ''''''; END IF;''; 
    END LOOP; 
  
    a_output := a_output || '' RETURN NULL; END; '''' LANGUAGE ''''plpgsql'''';''; 
 
    -- This works because we are not substituting any variables
    -- Otherwise it would fail. Look at PERFORM for another way to run functions
    
    EXECUTE a_output; 
END; 
' LANGUAGE 'plpgsql';
</programlisting>
    </para>
   </sect2>

   <sect2 id="plpgsql-statements-diagnostics">
    <title>获取结果状态</title>

    <para>
	 有几种方法可以确定命令的效果。第一种方法是使用
	 <literal>GET DIAGNOSTICS</literal>，其形式为：

<synopsis>
GET DIAGNOSTICS <replaceable>variable</replaceable> = <replaceable>item</replaceable> <optional> , ... </optional> ;
</synopsis>

     这个命令允许检索系统状态指示器。每个 <replaceable>item</replaceable>
     是一个关键字，标识要赋给指定变量的状态值（该变量应具有接收它的正确数据类型）。当前可用的状态项有 <varname>ROW_COUNT</>（最近发送到
     <acronym>SQL</acronym> 引擎的 <acronym>SQL</acronym> 查询所处理的行数）和 <varname>RESULT_OID</>（最近一条 <acronym>SQL</acronym>
     查询插入的最后一行的 OID）。注意 <varname>RESULT_OID</> 只在
     INSERT 查询之后有用。
    </para>
    <para>
<informalexample>
<programlisting>
      GET DIAGNOSTICS var_integer = ROW_COUNT;
</programlisting>
</informalexample>
    </para>

    <para>
     有一个名为 <literal>FOUND</literal> 的特殊变量，类型为
     <type>boolean</type>。<literal>FOUND</literal> 在每个
     <application>PL/pgSQL</application> 函数内初始为假。它被下列各类语句设置：
	 <itemizedlist>
	  <listitem>
	   <para>
		SELECT INTO 语句在返回行时把
		<literal>FOUND</literal> 设为真，未返回行时设为假。
	   </para>
	  </listitem>
	  <listitem>
	   <para>
		PERFORM 语句在产生（并丢弃）行时把
		<literal>FOUND</literal> 设为真，未产生行时设为假。
	   </para>
	  </listitem>
	  <listitem>
	   <para>
		UPDATE、INSERT 和 DELETE 语句在至少影响一行时把
		<literal>FOUND</literal> 设为真，未影响行时设为假。
	   </para>
	  </listitem>
	  <listitem>
	   <para>
		FETCH 语句在返回行时把 <literal>FOUND</literal>
		设为真，未返回行时设为假。
	   </para>
	  </listitem>
	  <listitem>
	   <para>
		FOR 语句在迭代一次或多次时把
		<literal>FOUND</literal> 设为真，否则设为假。这适用于
		FOR 语句的所有三种变体（整数 FOR 循环、记录集 FOR 循环和动态记录集 FOR 循环）。<literal>FOUND</literal> 只在
		FOR 循环退出时设置：在循环体执行期间，<literal>FOUND</literal> 不会被 FOR 语句修改，尽管循环体内其他语句的执行可能会改变它。
	   </para>
	  </listitem>
	 </itemizedlist>
     <literal>FOUND</literal> 是一个局部变量；对它的任何更改都只影响当前的
     <application>PL/pgSQL</application> 函数。
    </para>

   </sect2>
  </sect1>

<sect1 id="plpgsql-control-structures">
   <title>控制结构</title>

   <para>
    控制结构也许是 <application>PL/pgSQL</> 最有用（也最重要）的部分。使用
    <application>PL/pgSQL</> 的控制结构，你可以非常灵活而强大地操纵
    <productname>PostgreSQL</> 数据。
   </para>

   <sect2 id="plpgsql-statements-returning">
    <title>从函数返回</title>

    <para>
<synopsis>
RETURN <replaceable>expression</replaceable>;
</synopsis>

     带表达式的 RETURN 用于从不返回集合的
     <application>PL/pgSQL</> 函数返回。函数终止，<replaceable>expression</replaceable> 的值被返回给调用者。
    </para>

    <para>
     要返回复合（行）值，必须把一个记录或行变量写作
     <replaceable>expression</replaceable>。返回标量类型时，可以使用任何表达式。表达式的结果会像赋值所描述的那样自动转换为函数的返回类型。（如果你已把函数声明为返回 <type>void</>，则表达式可以省略，并且在任何情况下都会被忽略。）
    </para>

    <para>
     函数的返回值不能没有定义。如果控制到达函数顶层块的末尾而没有遇到
     RETURN 语句，将发生运行时错误。
    </para>

    <para>
     当 <application>PL/pgSQL</application> 函数被声明为返回
     <literal>SETOF</literal> <replaceable>sometype</> 时，要遵循的过程略有不同。这种情况下，要返回的各个项在 RETURN NEXT 命令中指定，然后用一条不带参数的最终 RETURN 命令表示函数已执行完毕。RETURN NEXT 既可用于标量数据类型，也可用于复合数据类型；后者将返回整张"表"的结果。使用 RETURN NEXT 的函数应按以下方式调用：

<programlisting>
SELECT * FROM some_func();
</programlisting>

     也就是说，函数在 FROM 子句中用作表源。

<synopsis>
RETURN NEXT <replaceable>expression</replaceable>;
</synopsis>

     RETURN NEXT 实际上并不从函数返回；它只是保存表达式（或记录或行变量，视返回的数据类型而定）的值。然后执行继续到
     <application>PL/pgSQL</application> 函数中的下一条语句。随着相继的
     RETURN NEXT 命令被执行，结果集逐渐建立。最后的 RETURN（不需要带参数）使控制退出函数。
    </para>

   <note>
    <para>
     <application>PL/pgSQL</application> 当前对 RETURN NEXT 的实现是在从函数返回之前保存整个结果集，如上所述。这意味着，如果
     <application>PL/pgSQL</application> 函数产生非常大的结果集，性能可能不佳：为避免耗尽内存，数据会被写入磁盘，但函数本身要等到整个结果集生成之后才会返回。<application>PL/pgSQL</application> 的未来版本可能允许用户定义没有这一限制的返回集合的函数。目前，数据开始写入磁盘的时机由 <varname>SORT_MEM</> 配置变量控制。有足够内存把更大结果集保存在内存中的管理员应考虑增大此参数。
    </para>
   </note>
  </sect2>

   <sect2 id="plpgsql-conditionals">
    <title>条件</title>

    <para>
     <literal>IF</> 语句让你根据某些条件执行命令。
     <application>PL/pgSQL</> 有四种 <literal>IF</> 形式：
    <itemizedlist>
     <listitem>
      <para><literal>IF ... THEN</></>
     </listitem>
     <listitem>
      <para><literal>IF ... THEN ... ELSE</></>
     </listitem>
     <listitem>
      <para><literal>IF ... THEN ... ELSE IF</> 和</>
     </listitem>
     <listitem>
      <para><literal>IF ... THEN ... ELSIF ... THEN ... ELSE</></>
     </listitem>
    </itemizedlist>
    </para>

    <sect3>
     <title><literal>IF-THEN</></title>

       <para>
<synopsis>
IF <replaceable>boolean-expression</replaceable> THEN
    <replaceable>statements</replaceable>
END IF;
</synopsis>

        IF-THEN 语句是 IF 最简单的形式。条件为真时，THEN 和
        END IF 之间的语句将被执行。否则，它们被跳过。

<programlisting>
IF v_user_id &lt;&gt; 0 THEN
    UPDATE users SET email = v_email WHERE user_id = v_user_id;
END IF;
</programlisting>
       </para>
     </sect3>

     <sect3>
      <title><literal>IF-THEN-ELSE</></title>

       <para>
<synopsis>
IF <replaceable>boolean-expression</replaceable> THEN
    <replaceable>statements</replaceable>
ELSE
    <replaceable>statements</replaceable>
END IF;
</synopsis>

        IF-THEN-ELSE 语句在 IF-THEN 的基础上增加了对条件求值为假时应执行的另一组语句的指定。

<programlisting>
IF parentid IS NULL or parentid = ''''
THEN 
    return fullname;
ELSE
    return hp_true_filename(parentid) || ''/'' || fullname;
END IF;


IF v_count > 0 THEN 
    INSERT INTO users_count(count) VALUES(v_count);
    return ''t'';
ELSE 
    return ''f'';
END IF;
</programlisting>
       </para>
     </sect3>

     <sect3>
      <title><literal>IF-THEN-ELSE IF</></title>

       <para>
        IF 语句可以嵌套，如下例所示：
<programlisting>
IF demo_row.sex = ''m'' THEN
  pretty_sex := ''man'';
ELSE
  IF demo_row.sex = ''f'' THEN
    pretty_sex := ''woman'';
  END IF;
END IF;
</programlisting>
       </para>

       <para>
        使用这种形式时，你实际上是在外层 IF 语句的 ELSE 部分中嵌套了一个
        IF 语句。因此，每个嵌套的 IF 需要一个 END IF 语句，父级
        IF-ELSE 也需要一个。这可行，但当要检查的备选很多时就变得乏味。
       </para>
     </sect3>

     <sect3>
      <title><literal>IF-THEN-ELSIF-ELSE</></title>

       <para>
<synopsis>
IF <replaceable>boolean-expression</replaceable> THEN
    <replaceable>statements</replaceable>
<optional> ELSIF <replaceable>boolean-expression</replaceable> THEN
    <replaceable>statements</replaceable>
<optional> ELSIF <replaceable>boolean-expression</replaceable> THEN
    <replaceable>statements</replaceable>
    ...
</optional>
</optional>
<optional> ELSE
    <replaceable>statements</replaceable> </optional>
END IF;
</synopsis>

        <literal>IF-THEN-ELSIF-ELSE</> 提供了在一条语句中检查许多备选的更方便的方法。形式上它等价于嵌套的
        <literal>IF-THEN-ELSE-IF-THEN</> 命令，但只需要一个
        <literal>END IF</>。
       </para>

       <para>
        下面是一个例子：

<programlisting>
IF number = 0 THEN
    result := ''zero'';
ELSIF number &gt; 0 THEN 
    result := ''positive'';
ELSIF number &lt; 0 THEN
    result := ''negative'';
ELSE
    -- hmm, the only other possibility is that number IS NULL
    result := ''NULL'';
END IF;
</programlisting>
       </para>

       <para>
        最后的 ELSE 部分是可选的。
       </para>

     </sect3>
   </sect2>

   <sect2 id="plpgsql-control-structures-loops">
    <title>简单循环</title>

    <para>
     使用 LOOP、EXIT、WHILE 和 FOR 语句，你可以让
     <application>PL/pgSQL</application> 函数重复执行一系列命令。
    </para>

     <sect3>
      <title>LOOP</title>

       <para>
<synopsis>
<optional>&lt;&lt;label&gt;&gt;</optional>
LOOP
    <replaceable>statements</replaceable>
END LOOP;
</synopsis>

        LOOP 定义一个无限重复的无条件循环，直到被 EXIT 或 RETURN 语句终止。可选的标签可供嵌套循环中的 EXIT 语句用来指定应终止哪一层嵌套。
       </para>
     </sect3>

     <sect3>
      <title>EXIT</title>

       <para>
<synopsis>
EXIT <optional> <replaceable>label</replaceable> </optional> <optional> WHEN <replaceable>expression</replaceable> </optional>;
</synopsis>

        如果没有给出 <replaceable>label</replaceable>，最内层的循环被终止，接下来执行 END LOOP 之后的语句。如果给出了
        <replaceable>label</replaceable>，它必须是当前层或某个外层嵌套循环或块的标签。然后命名的循环或块被终止，控制继续到该循环/块对应的 END 之后的语句。
       </para>

       <para>
        如果存在 WHEN，只有当指定条件为真时才退出循环，否则控制传递到
        EXIT 之后的语句。
       </para>

       <para>
        例子：
<programlisting>
LOOP
    -- some computations
    IF count > 0 THEN
        EXIT;  -- exit loop
    END IF;
END LOOP;

LOOP
    -- some computations
    EXIT WHEN count > 0;
END LOOP;

BEGIN
    -- some computations
    IF stocks > 100000 THEN
        EXIT;  -- illegal. Can't use EXIT outside of a LOOP
    END IF;
END;
</programlisting>
       </para>
     </sect3>

     <sect3>
      <title>WHILE</title>

       <para>
<synopsis>
<optional>&lt;&lt;label&gt;&gt;</optional>
WHILE <replaceable>expression</replaceable> LOOP
    <replaceable>statements</replaceable>
END LOOP;
</synopsis>

        WHILE 语句在条件表达式求值为真期间重复一个语句序列。条件在每次进入循环体之前检查。
       </para>

       <para>
        例如：
<programlisting>
WHILE amount_owed > 0 AND gift_certificate_balance > 0 LOOP
    -- some computations here
END LOOP;

WHILE NOT boolean_expression LOOP
    -- some computations here
END LOOP;
</programlisting>
       </para>
     </sect3>

     <sect3>
      <title>FOR（整数 for 循环）</title>

       <para>
<synopsis>
<optional>&lt;&lt;label&gt;&gt;</optional>
FOR <replaceable>name</replaceable> IN <optional> REVERSE </optional> <replaceable>expression</replaceable> .. <replaceable>expression</replaceable> LOOP
    <replaceable>statements</replaceable>
END LOOP;
</synopsis>

        这种形式的 FOR 创建一个在整数值范围上迭代的循环。变量
        <replaceable>name</replaceable> 自动定义为 integer 类型，且只存在于循环内部。给出范围下界和上界的两个表达式在进入循环时求值一次。迭代步长通常为 1，指定 REVERSE 时为 -1。
       </para>

       <para>
        整数 FOR 循环的一些例子：
<programlisting>
FOR i IN 1..10 LOOP
  -- some expressions here

    RAISE NOTICE ''i is %'',i;
END LOOP;

FOR i IN REVERSE 10..1 LOOP
    -- some expressions here
END LOOP;
</programlisting>
       </para>
     </sect3>
   </sect2>

   <sect2 id="plpgsql-records-iterating">
    <title>遍历查询结果</title>

    <para>
     使用另一种类型的 FOR 循环，你可以遍历查询的结果并相应地操纵那些数据。语法为：
<synopsis>
<optional>&lt;&lt;label&gt;&gt;</optional>
FOR <replaceable>record | row</replaceable> IN <replaceable>select_query</replaceable> LOOP
    <replaceable>statements</replaceable>
END LOOP;
</synopsis>
     记录或行变量被相继赋以 SELECT 查询产生的所有行，并对每一行执行循环体。下面是一个例子：
    </para>

    <para>
<programlisting>
CREATE FUNCTION cs_refresh_mviews () RETURNS INTEGER AS '
DECLARE
     mviews RECORD;
BEGIN
     PERFORM cs_log(''Refreshing materialized views...'');

     FOR mviews IN SELECT * FROM cs_materialized_views ORDER BY sort_key LOOP

         -- Now "mviews" has one record from cs_materialized_views

         PERFORM cs_log(''Refreshing materialized view '' || quote_ident(mviews.mv_name) || ''...'');
         EXECUTE ''TRUNCATE TABLE  '' || quote_ident(mviews.mv_name);
         EXECUTE ''INSERT INTO '' || quote_ident(mviews.mv_name) || '' '' || mviews.mv_query;
     END LOOP;

     PERFORM cs_log(''Done refreshing materialized views.'');
     RETURN 1;
end;
' LANGUAGE 'plpgsql';
</programlisting>

     如果循环被 EXIT 语句终止，最后赋的行值在循环之后仍可访问。
    </para>

    <para>
     FOR-IN-EXECUTE 语句是遍历记录的另一种方式：
<synopsis>
<optional>&lt;&lt;label&gt;&gt;</optional>
FOR <replaceable>record | row</replaceable> IN EXECUTE <replaceable>text_expression</replaceable> LOOP 
    <replaceable>statements</replaceable>
END LOOP;
</synopsis>
     这与前面的形式类似，不同之处在于源 SELECT 语句是作为字符串表达式指定的，它在每次进入 FOR 循环时被求值并重新规划。这使程序员可以在预规划查询的速度和动态查询的灵活性之间做出选择，就像普通的
     EXECUTE 语句一样。
    </para>

    <note>
    <para>
     <application>PL/pgSQL</> 解析器目前通过检查 FOR 之后提到的目标变量是否被声明为记录/行变量来区分两类 FOR 循环（整数或返回记录）。如果不是，就推定为整数 FOR 循环。当真正的问题比如说只是 FOR
     变量名拼错时，这可能导致相当不直观的错误消息。
    </para>
    </note>
  </sect2>
  </sect1>

<sect1 id="plpgsql-cursors">
   <title>游标</title>

   <para>
    不是一次执行整个查询，而是可以建立一个封装该查询的<firstterm>游标</>，然后每次读取查询结果的少数几行。这样做的一个原因是避免结果包含大量行时的内存溢出。（不过，
    <application>PL/pgSQL</application> 用户通常不需要担心这一点，因为
    FOR 循环内部自动使用游标来避免内存问题。）更有趣的用法是返回函数所创建游标的引用，让调用者读取这些行。这提供了一种从函数返回大型行集的高效方式。
   </para>

   <sect2 id="plpgsql-cursor-declarations">
    <title>声明游标变量</title>

    <para>
     <application>PL/pgSQL</application> 中对游标的所有访问都通过游标变量进行，游标变量总是属于特殊数据类型 <type>refcursor</>。创建游标变量的一种方法是直接把它声明为 <type>refcursor</> 类型的变量。另一种方法是使用游标声明语法，其一般形式为：
<synopsis>
<replaceable>name</replaceable> CURSOR <optional> ( <replaceable>arguments</replaceable> ) </optional> FOR <replaceable>select_query</replaceable> ;
</synopsis>
     （为兼容 Oracle，<literal>FOR</> 可以换成 <literal>IS</literal>。）<replaceable>arguments</replaceable>（如果有）是一个由
     <replaceable>name</replaceable> <replaceable>datatype</replaceable>
     对组成的逗号分隔列表，这些名称在给定查询中将被参数值替换。替换这些名称的实际值将在游标打开时指定。
    </para>
    <para>
     一些例子：
<programlisting>
DECLARE
    curs1 refcursor;
    curs2 CURSOR FOR SELECT * from tenk1;
    curs3 CURSOR (key int) IS SELECT * from tenk1 where unique1 = key;
</programlisting>
     这三个变量的数据类型都是 <type>refcursor</type>，但第一个可用于任何查询，第二个已有完全指定的查询<firstterm>绑定</>于其上，最后一个有一个参数化查询绑定于其上。（打开游标时，<literal>key</> 将被替换为一个整数参数值。）变量 <literal>curs1</> 被称为<firstterm>未绑定</>的，因为它没有绑定到任何特定查询。
    </para>
   </sect2>

   <sect2 id="plpgsql-cursor-opening">
    <title>打开游标</title>

    <para>
     游标可以用来检索行之前，必须先被<firstterm>打开</>。（这等价于 SQL
     命令 <command>DECLARE CURSOR</>。）<application>PL/pgSQL</application>
     有四种形式的 OPEN 语句，其中两种使用未绑定游标变量，另外两种使用绑定游标变量。
    </para>

    <sect3>
     <title>OPEN FOR SELECT</title>

       <para>
<synopsis>
OPEN <replaceable>unbound-cursor</replaceable> FOR SELECT ...;
</synopsis>

        游标变量被打开并被给定要执行的指定查询。游标不能已经处于打开状态，而且它必须被声明为未绑定游标（即简单的
	<type>refcursor</> 变量）。SELECT 查询的处理方式与
	<application>PL/pgSQL</application> 中其他 SELECT 语句相同：替换
	<application>PL/pgSQL</application> 变量名，并缓存查询计划供可能的重用。

<programlisting>
OPEN curs1 FOR SELECT * FROM foo WHERE key = mykey;
</programlisting>
       </para>
     </sect3>

    <sect3>
     <title>OPEN FOR EXECUTE</title>

       <para>
<synopsis>
OPEN <replaceable>unbound-cursor</replaceable> FOR EXECUTE <replaceable class="command">query-string</replaceable>;
</synopsis>

        游标变量被打开并被给定要执行的指定查询。游标不能已经处于打开状态，而且它必须被声明为未绑定游标（即简单的
	<type>refcursor</> 变量）。查询以与 EXECUTE 命令中相同的方式指定为字符串表达式。与通常一样，这提供了灵活性，使查询可以随每次运行而变化。

<programlisting>
OPEN curs1 FOR EXECUTE ''SELECT * FROM '' || quote_ident($1);
</programlisting>
       </para>
     </sect3>

    <sect3>
     <title>打开绑定游标</title>

       <para>
<synopsis>
OPEN <replaceable>bound-cursor</replaceable> <optional> ( <replaceable>argument_values</replaceable> ) </optional>;
</synopsis>

        这种形式的 OPEN 用于打开其查询在声明时就绑定于其上的游标变量。游标不能已经处于打开状态。当且仅当游标被声明为接受参数时，必须出现实参值表达式列表。这些值将在查询中被替换。绑定游标的查询计划总是被视为可缓存的——这种情况没有 EXECUTE 的等价物。

<programlisting>
OPEN curs2;
OPEN curs3(42);
</programlisting>
       </para>
     </sect3>
   </sect2>

   <sect2 id="plpgsql-cursor-using">
    <title>使用游标</title>

    <para>
     游标一旦被打开，就可以用这里描述的语句操纵它。
    </para>

    <para>
     这些操纵不必发生在最初打开游标的同一个函数中。你可以从函数中返回
     <type>refcursor</> 值，让调用者操作该游标。（在内部，<type>refcursor</> 值只是包含该游标活动查询的 Portal 的字符串名称。这个名称可以被传递、赋给其他 <type>refcursor</> 变量等等，而不会干扰 Portal。）
    </para>

    <para>
     所有 Portal 都在事务结束时隐式关闭。因此 <type>refcursor</> 值只在事务结束之前可用于引用打开的游标。
    </para>

    <sect3>
     <title>FETCH</title>

       <para>
<synopsis>
FETCH <replaceable>cursor</replaceable> INTO <replaceable>target</replaceable>;
</synopsis>

        FETCH 从游标检索下一行到目标中，目标可以是行变量、记录变量或简单变量的逗号分隔列表，就像 SELECT INTO 一样。与 SELECT INTO
	一样，可以检查特殊变量 <literal>FOUND</literal> 来看是否取得了行。

<programlisting>
FETCH curs1 INTO rowvar;
FETCH curs2 INTO foo,bar,baz;
</programlisting>
       </para>
     </sect3>

    <sect3>
     <title>CLOSE</title>

       <para>
<synopsis>
CLOSE <replaceable>cursor</replaceable>;
</synopsis>

        CLOSE 关闭打开游标底层的 Portal。这可用于在事务结束之前释放资源，或释放游标变量以便再次打开。

<programlisting>
CLOSE curs1;
</programlisting>
       </para>
     </sect3>

    <sect3>
     <title>返回游标</title>

       <para>

        <application>PL/pgSQL</application> 函数可以向调用者返回游标。这用于从函数返回多行或多列。函数打开游标并把游标名返回给调用者。然后调用者可以从游标中获取行。游标可以由调用者关闭，也可以在事务关闭时自动关闭。

       </para>

       <para>
        函数返回的游标名可以由调用者指定，也可以自动生成。下面的例子展示了如何由调用者提供游标名：

<programlisting>
CREATE TABLE test (col text);
INSERT INTO test VALUES ('123');

CREATE FUNCTION reffunc(refcursor) RETURNS refcursor AS '
BEGIN
        OPEN $1 FOR SELECT col FROM test;
        RETURN $1;
END;
' LANGUAGE 'plpgsql';

BEGIN;
SELECT reffunc('funccursor');
FETCH ALL IN funccursor;
COMMIT;
</programlisting>
       </para>

       <para>
        下面的例子使用自动游标名生成：

<programlisting>
CREATE FUNCTION reffunc2() RETURNS refcursor AS '
DECLARE
        ref refcursor;
BEGIN
        OPEN ref FOR SELECT col FROM test;
        RETURN ref;
END;
' LANGUAGE 'plpgsql';

BEGIN;
SELECT reffunc2();
  
        reffunc2      
  --------------------
   &lt;unnamed cursor 1&gt;
  (1 row)

FETCH ALL IN "&lt;unnamed cursor 1&gt;";
COMMIT;
</programlisting>
       </para>
     </sect3>
   </sect2>
  </sect1>

<sect1 id="plpgsql-errors-and-messages">
   <title>错误和消息</title>

   <para>
    使用 RAISE 语句报告消息和引发错误。

<synopsis>
RAISE <replaceable class="parameter">level</replaceable> '<replaceable class="parameter">format</replaceable>' <optional>, <replaceable class="parameter">variable</replaceable> <optional>...</optional></optional>;
</synopsis>

    可能的级别有 <literal>DEBUG</literal>（把消息写入服务器日志）、<literal>LOG</literal>（把消息以更高的优先级写入服务器日志）、<literal>INFO</literal>、<literal>NOTICE</literal> 和
    <literal>WARNING</literal>（把消息写入服务器日志并发送给客户端，优先级依次更高）以及 <literal>EXCEPTION</literal>（引发错误并中止当前事务）。特定优先级的错误消息是被报告给客户端、写入服务器日志还是两者都做，由 <option>SERVER_MIN_MESSAGES</option> 和
    <option>CLIENT_MIN_MESSAGES</option> 配置变量控制。更多信息参见
    <citetitle>PostgreSQL 管理员指南</citetitle>。
   </para>

   <para>
    在格式字符串内部，<literal>%</literal> 被下一个可选参数的外部表示替换。写 <literal>%%</literal> 可以输出字面的 <literal>%</literal>。注意，可选参数目前必须是简单变量而不是表达式，格式必须是简单的字符串字面量。
   </para>

   <!--
   This example should work, but does not:
   	RAISE NOTICE ''Id number '' || key || '' not found!'';
   Put it back when we allow non-string-literal formats.
    -->

   <para>
    例子：
<programlisting>
RAISE NOTICE ''Calling cs_create_job(%)'',v_job_id;
</programlisting>

    在这个例子中，v_job_id 的值将替换字符串中的 <literal>%</literal>。
   </para>

   <para>
<programlisting>
RAISE EXCEPTION ''Inexistent ID --> %'',user_id;
</programlisting>
    这将以给定的错误消息中止事务。
   </para>

  <sect2 id="plpgsql-exceptions">
   <title>异常</title>

    <para>
     <productname>PostgreSQL</productname> 没有一个非常聪明的异常处理模型。每当解析器、规划器/优化器或执行器断定一条语句无法再处理下去时，整个事务都会被中止，系统跳回主循环以从客户端应用程序获取下一条查询。
    </para>

    <para>
     可以挂钩到错误机制中来注意到这种情况的发生。但目前无法知道到底是什么导致了中止（输入/输出转换错误、浮点错误、解析错误）。而且此时数据库后端有可能处于不一致状态，因此返回上层执行器或发出更多命令可能会损坏整个数据库。
    </para>

    <para>
     因此，<application>PL/pgSQL</application> 目前在函数或触发器过程执行期间遇到中止时所做的唯一事情，就是写一些额外的
     <literal>NOTICE</literal> 级别日志消息，说明这发生在哪个函数中以及哪里（行号和语句类型）。该错误总是会停止函数的执行。
    </para>
  </sect2>
 </sect1>

<sect1 id="plpgsql-trigger">
  <title>触发器过程</title>

  <para>
	<application>PL/pgSQL</application> 可用于定义触发器过程。触发器过程用 <command>CREATE FUNCTION</> 命令创建，声明为无参数、返回类型为 <type>TRIGGER</type> 的函数。注意，即使函数期望接收
	<command>CREATE TRIGGER</> 中指定的参数，也必须声明为无参数——
	触发器参数通过 <varname>TG_ARGV</> 传递，如下所述。
  </para>

  <para>
   当 <application>PL/pgSQL</application> 函数作为触发器被调用时，顶层块中会自动创建若干特殊变量。它们是：

   <variablelist>
    <varlistentry>
     <term><varname>NEW</varname></term>
     <listitem>
      <para>
       数据类型 <type>RECORD</type>；在行级触发器中保存
       INSERT/UPDATE 操作的新数据库行的变量。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><varname>OLD</varname></term>
     <listitem>
      <para>
       数据类型 <type>RECORD</type>；在行级触发器中保存
       UPDATE/DELETE 操作的旧数据库行的变量。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><varname>TG_NAME</varname></term>
     <listitem>
      <para>
       数据类型 <type>name</type>；包含实际触发的触发器名称的变量。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><varname>TG_WHEN</varname></term>
     <listitem>
      <para>
       数据类型 <type>text</type>；依据触发器定义为
              <literal>BEFORE</literal> 或 <literal>AFTER</literal> 的字符串。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><varname>TG_LEVEL</varname></term>
     <listitem>
      <para>
       数据类型 <type>text</type>；依据触发器定义为
              <literal>ROW</literal> 或 <literal>STATEMENT</literal> 的字符串。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><varname>TG_OP</varname></term>
     <listitem>
      <para>
       数据类型 <type>text</type>；取值为 <literal>INSERT</literal>、<literal>UPDATE</literal> 或 <literal>DELETE</literal> 的字符串，说明触发器是针对哪个操作触发的。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><varname>TG_RELID</varname></term>
     <listitem>
      <para>
       数据类型 <type>oid</type>；导致触发器调用的表的对象 ID。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><varname>TG_RELNAME</varname></term>
     <listitem>
      <para>
       数据类型 <type>name</type>；导致触发器调用的表的名称。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><varname>TG_NARGS</varname></term>
     <listitem>
      <para>
       数据类型 <type>integer</type>；<command>CREATE TRIGGER</command>
       语句中给触发器过程的参数个数。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><varname>TG_ARGV[]</varname></term>
     <listitem>
      <para>
       数据类型为 <type>text</type> 的数组；来自
              <command>CREATE TRIGGER</command> 语句的参数。索引从 0 计数，并且可以以表达式形式给出。无效索引（&lt; 0 或 &gt;=
       <varname>tg_nargs</>）得到空值。
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
  </para>

   <para>
    触发器函数必须返回空值，或返回与触发该触发器的表的结构完全相同的记录/行值。BEFORE 触发器可以返回空值，向触发器管理器示意跳过这一行的其余操作（即不触发后续触发器，并且不对这一行执行
    INSERT/UPDATE/DELETE）。如果返回非空值，操作将以该行值继续进行。注意，返回与 NEW 原值不同的行值会改变将被插入或更新的行。可以直接替换 NEW 中的单个值并返回它，也可以构造一个完整的新记录/行来返回。
   </para>

   <para>
    AFTER 触发器的返回值被忽略；不妨总是返回空值。但 AFTER 触发器仍可通过引发错误中止操作。
   </para>

   <example>
    <title>一个 <application>PL/pgSQL</application> 触发器过程例子</title>

    <para>
     这个示例触发器确保每当表中有行被插入或更新时，当前用户名和时间都被记入该行。它还确保给出了雇员姓名，并且薪金为正值。

<programlisting>
CREATE TABLE emp (
    empname text,
    salary integer,
    last_date timestamp,
    last_user text
);

CREATE FUNCTION emp_stamp () RETURNS TRIGGER AS '
    BEGIN
        -- Check that empname and salary are given
        IF NEW.empname ISNULL THEN
            RAISE EXCEPTION ''empname cannot be NULL value'';
        END IF;
        IF NEW.salary ISNULL THEN
            RAISE EXCEPTION ''% cannot have NULL salary'', NEW.empname;
        END IF;

        -- Who works for us when she must pay for?
        IF NEW.salary < 0 THEN
            RAISE EXCEPTION ''% cannot have a negative salary'', NEW.empname;
        END IF;

        -- Remember who changed the payroll when
        NEW.last_date := ''now'';
        NEW.last_user := current_user;
        RETURN NEW;
    END;
' LANGUAGE 'plpgsql';

CREATE TRIGGER emp_stamp BEFORE INSERT OR UPDATE ON emp
    FOR EACH ROW EXECUTE PROCEDURE emp_stamp();
</programlisting>
    </para>
   </example>
  </sect1>

  <!-- **** PL/pgSQL Examples **** -->

<sect1 id="plpgsql-examples">
   <title>例子</title>

   <para>
    这里只给出几个函数，演示编写 <application>PL/pgSQL</>
    函数有多么容易。更复杂的例子，程序员可以查看
    <application>PL/pgSQL</> 的回归测试。
   </para>

   <para>
    用 <application>PL/pgSQL</application> 编写函数的一个令人头疼的细节是单引号的处理。<command>CREATE FUNCTION</> 中函数的源文本必须是字面字符串。字面字符串内部的单引号必须加倍或用反斜线引用。我们仍在寻找一种优雅的替代方案。在此期间，应像下面的例子那样把单引号加倍。<productname>PostgreSQL</productname> 未来版本中对此的任何解决方案都将是向前兼容的。
   </para>

   <para>
    关于在不同情况下如何转义单引号的详细解释和例子，请参见<xref linkend="plpgsql-quote">。
   </para>

   <example>
    <title>一个使整数递增的简单 <application>PL/pgSQL</application> 函数</title>

    <para>
     下面两个 <application>PL/pgSQL</application> 函数与 C 语言函数讨论中的对应函数相同。这个函数接收一个 <type>integer</type> 并使它递增一，返回递增后的值。
    </para>

<programlisting>
CREATE FUNCTION add_one (integer) RETURNS INTEGER AS '
    BEGIN
        RETURN $1 + 1;
    END;
' LANGUAGE 'plpgsql';
</programlisting>
   </example>

   <example>
    <title>一个连接文本的简单 <application>PL/pgSQL</application> 函数</title>

    <para>
     这个函数接收两个 <type>text</type> 参数并返回它们连接的结果。
    </para>

<programlisting>
CREATE FUNCTION concat_text (TEXT, TEXT) RETURNS TEXT AS '
    BEGIN
        RETURN $1 || $2;
    END;
' LANGUAGE 'plpgsql';
</programlisting>
   </example>

   <example>
    <title>一个处理复合类型的 <application>PL/pgSQL</application> 函数</title>

    <para>
     在这个例子中，我们以 <literal>EMP</>（一张表）和一个
     <type>integer</type> 作为函数的参数，函数返回一个
     <type>boolean</type>。如果 <structname>EMP</> 表的
     <structfield>salary</> 字段为 <literal>NULL</literal>，我们返回
     <literal>f</>。否则，我们把该字段与传给函数的 <type>integer</type>
     相比较，返回比较的 <type>boolean</type> 结果（t 或 f）。这是与 C
     函数中的例子等价的 <application>PL/pgSQL</application> 版本。
    </para>

<programlisting>
CREATE FUNCTION c_overpaid (EMP, INTEGER) RETURNS BOOLEAN AS '
    DECLARE
        emprec ALIAS FOR $1;
        sallim ALIAS FOR $2;
    BEGIN
        IF emprec.salary ISNULL THEN
            RETURN ''f'';
        END IF;
        RETURN emprec.salary > sallim;
    END;
' LANGUAGE 'plpgsql';
</programlisting>
   </example>
  </sect1>

  <!-- **** Porting from Oracle PL/SQL **** -->

<sect1 id="plpgsql-porting">

  <sect1info>
   <date>
    February 2001
   </date>
   <author>
    <firstname>Roberto</firstname>
    <surname>Mello</surname>
    <affiliation>
     <address>
      <email>rmello@fslc.usu.edu</email>
     </address>
    </affiliation>
   </author>
<!--
  Breaks HTML manifest file
   <legalnotice>
    <para>
     Except for portions of this document quoted from other sources,
     this document is licensed under the BSD License.
    </para>
   </legalnotice>
-->
  </sect1info>

  <title>从 Oracle PL/SQL 移植</title>

  <indexterm zone="plpgsql-porting">
   <primary>Oracle</primary>
  </indexterm>

  <indexterm zone="plpgsql-porting">
   <primary>PL/SQL</primary>
  </indexterm>

  <note>
   <title>作者</title>
   <para>
    Roberto Mello（<email>rmello@fslc.usu.edu</email>）
   </para>
  </note>

  <para>
   本节解释 Oracle 的 PL/SQL 与 <productname>PostgreSQL</> 的
   <application>PL/pgSQL</application> 语言之间的差异，希望能帮助开发者把应用程序从 Oracle 移植到 <productname>PostgreSQL</>。这里的大部分代码来自 <ulink url="http://www.arsdigita.com">ArsDigita</ulink> 的
   <ulink url="http://www.arsdigita.com/asj/clickstream">Clickstream
   模块</ulink>，那是我 2000 年夏天在
   <ulink url="http://www.openforce.net">OpenForce Inc.</ulink> 实习期间移植到 <productname>PostgreSQL</> 的。
  </para>

  <para>
   <application>PL/pgSQL</application> 在许多方面与 PL/SQL 相似。它是一种块结构的命令式语言（所有变量都必须声明）。PL/SQL 的功能比
   <productname>PostgreSQL</> 的对应语言多得多，但
   <application>PL/pgSQL</application> 提供了大量功能，并且正在不断改进。
  </para>

  <sect2>
   <title>主要差异</title>

   <para>
    从 Oracle 移植到 <productname>PostgreSQL</> 时要记住的一些事项：

    <itemizedlist>
     <listitem>
      <para>
       <productname>PostgreSQL</> 中没有默认参数。
      </para>
     </listitem>

     <listitem>
      <para>
       在 <productname>PostgreSQL</> 中可以重载函数。这常被用来绕开缺少默认参数的问题。
      </para>
     </listitem>

     <listitem>
      <para>
       赋值、循环和条件都相似。
      </para>
     </listitem>

     <listitem>
      <para>
       <productname>PostgreSQL</> 中不需要游标，把查询放进 FOR
       语句即可（见下面的例子）
      </para>
     </listitem>

     <listitem>
      <para>
       在 <productname>PostgreSQL</> 中你<emphasis>需要</emphasis>转义单引号。见<xref linkend="plpgsql-quote">。
      </para>
     </listitem>
    </itemizedlist>
   </para>

   <sect3 id="plpgsql-quote">
    <title>单引号转义</title>

    <para>
     在 <productname>PostgreSQL</> 中，你需要转义函数定义内部的单引号。这有时会导致相当有趣的代码，尤其是当你创建一个生成其他函数的函数时，如<xref linkend="plpgsql-porting-nastyquote">中那样。转义大量单引号时要记住的一件事是：除开头/结尾的引号外，所有其他引号都将以偶数个出现。
    </para>

    <para>
     <xref linkend="plpgsql-quoting-table">给出了全部内幕。（你会喜欢这张小图表的。）
    </para>

    <table id="plpgsql-quoting-table">
     <title>单引号转义表</title>

     <tgroup cols="4">
      <thead>
       <row>
        <entry>引号数</entry>
        <entry>用途</entry>
        <entry>例子</entry>
        <entry>结果</entry>
       </row>
      </thead>

      <tbody>
       <row>
        <entry>1</entry>
        <entry>用于开始/结束函数体</entry>
        <entry><programlisting>
CREATE FUNCTION foo() RETURNS INTEGER AS '...'
LANGUAGE 'plpgsql';
</programlisting></entry>
        <entry>原样</entry>
       </row>

       <row>
        <entry>2</entry>
        <entry>在赋值、SELECT 语句中定界字符串等</entry>
        <entry><programlisting>
a_output := ''Blah'';
SELECT * FROM users WHERE f_name=''foobar'';
</programlisting></entry>
        <entry><literal>SELECT * FROM users WHERE f_name='foobar';</literal></entry>
       </row>

       <row>
        <entry>4</entry>
        <entry>
         当你需要在结果字符串中出现两个单引号而不终止该字符串时。
        </entry>
        <entry><programlisting>
a_output := a_output || '' AND name 
    LIKE ''''foobar'''' AND ...''
</programlisting></entry>
        <entry><literal>AND name LIKE 'foobar' AND ...</literal></entry>
       </row>

       <row>
        <entry>6</entry>
        <entry>
         当你想要结果字符串中出现成对引号<emphasis>并且</emphasis>终止该字符串时。
        </entry>
        <entry><programlisting>
a_output := a_output || '' AND name 
    LIKE ''''foobar''''''
</programlisting></entry>
        <entry>
         <literal>AND name LIKE 'foobar'</literal>
        </entry>
       </row>

       <row>
        <entry>10</entry>
        <entry>
         当你想要结果字符串中出现两个单引号（这需要 8 个引号）<emphasis>并且</emphasis>终止该字符串（再加 2 个）。只有当你用函数生成其他函数时（如<xref linkend="plpgsql-porting-nastyquote">中那样），才可能需要这样做。
        </entry>
        <entry><programlisting>
a_output := a_output || '' if v_'' || 
    referrer_keys.kind || '' like '''''''''' 
    || referrer_keys.key_string || '''''''''' 
    then return ''''''  || referrer_keys.referrer_type 
    || ''''''; end if;''; 
</programlisting></entry>
        <entry>
         <literal>if v_<...> like ''<...>'' then return ''<...>''; end if;</literal>
        </entry>
       </row>
      </tbody>
     </tgroup>
    </table>
   </sect3>
  </sect2>

  <sect2 id="plpgsql-porting-functions">
   <title>
    移植函数
   </title>

   <example>
    <title>
     一个简单函数
    </title>

    <para>
     下面是一个 Oracle 函数：
<programlisting>
CREATE OR REPLACE FUNCTION cs_fmt_browser_version(v_name IN varchar, v_version IN varchar)
RETURN varchar IS
BEGIN
    IF v_version IS NULL THEN
        RETURN v_name;
    END IF;
    RETURN v_name || '/' || v_version;
END;
/
SHOW ERRORS;
</programlisting>
    </para>

    <para>
     让我们过一遍这个函数，看看它与 <application>PL/pgSQL</> 的差异：

     <itemizedlist>
      <listitem>
       <para>
        <productname>PostgreSQL</productname> 没有命名参数。你必须在函数内部显式地为它们建立别名。
       </para>
      </listitem>

      <listitem>
       <para>
        Oracle 的函数可以有 <literal>IN</literal>、<literal>OUT</literal>
        和 <literal>INOUT</literal> 参数。例如 <literal>INOUT</literal>
        表示参数将接收一个值并返回另一个值。<productname>PostgreSQL</>
        只有<quote>IN</quote>参数，而且函数只能返回单个值。
       </para>
      </listitem>

      <listitem>
       <para>
        函数原型（不是函数体）中的 <literal>RETURN</literal> 关键字在
        <productname>PostgreSQL</productname> 中变为
        <literal>RETURNS</literal>。
       </para>
      </listitem>

      <listitem>
       <para>
        在 <productname>PostgreSQL</> 中，函数是用单引号作为定界符创建的，因此你必须转义函数内部的单引号（这有时相当恼人；参见<xref linkend="plpgsql-quote">）。
       </para>
      </listitem>

      <listitem>
       <para>
        <productname>PostgreSQL</> 中不存在 <literal>/show errors</literal>
        命令。
       </para>
      </listitem>
     </itemizedlist>
    </para>

    <para>
     那么让我们看看这个函数移植到 <productname>PostgreSQL</> 后的样子：

<programlisting>
CREATE OR REPLACE FUNCTION cs_fmt_browser_version(VARCHAR, VARCHAR)
RETURNS VARCHAR AS '
DECLARE
    v_name ALIAS FOR $1;
    v_version ALIAS FOR $2;
BEGIN
    IF v_version IS NULL THEN
        return v_name;
    END IF;
    RETURN v_name || ''/'' || v_version;
END;
' LANGUAGE 'plpgsql';
</programlisting>
    </para>
   </example>

   <example id="plpgsql-porting-nastyquote">
    <title>
     一个创建另一个函数的函数
    </title>

    <para>
     下面的过程从 <command>SELECT</command> 语句抓取行，并把结果用
     <literal>IF</literal> 语句构造成一个大函数，以求高效。请特别注意游标、<literal>FOR</literal> 循环中的差异，以及在
     <productname>PostgreSQL</> 中转义单引号的必要性。

<programlisting>
CREATE OR REPLACE PROCEDURE cs_update_referrer_type_proc IS
    CURSOR referrer_keys IS 
        SELECT * FROM cs_referrer_keys 
        ORDER BY try_order;

    a_output VARCHAR(4000); 
BEGIN 
    a_output := 'CREATE OR REPLACE FUNCTION cs_find_referrer_type(v_host IN VARCHAR, v_domain IN VARCHAR, 
v_url IN VARCHAR) RETURN VARCHAR IS BEGIN'; 

    FOR referrer_key IN referrer_keys LOOP 
        a_output := a_output || ' IF v_' || referrer_key.kind || ' LIKE ''' || 
referrer_key.key_string || ''' THEN RETURN ''' || referrer_key.referrer_type || 
'''; END IF;'; 
    END LOOP; 

    a_output := a_output || ' RETURN NULL; END;'; 
    EXECUTE IMMEDIATE a_output; 
END; 
/ 
show errors
</programlisting>
    </para>

    <para>
     下面是这个函数在 <productname>PostgreSQL</> 中的最终样子：

<programlisting>
CREATE FUNCTION cs_update_referrer_type_proc() RETURNS INTEGER AS '
DECLARE
    referrer_keys RECORD;  -- Declare a generic record to be used in a FOR
    a_output varchar(4000);
BEGIN 
    a_output := ''CREATE FUNCTION cs_find_referrer_type(VARCHAR,VARCHAR,VARCHAR) 
                  RETURNS VARCHAR AS '''' 
                     DECLARE 
                         v_host ALIAS FOR $1; 
                         v_domain ALIAS FOR $2; 
                         v_url ALIAS FOR $3;
                     BEGIN ''; 

    -- 
    -- Notice how we scan through the results of a query in a FOR loop
    -- using the FOR &lt;record&gt; construct.
    --

    FOR referrer_keys IN SELECT * FROM cs_referrer_keys ORDER BY try_order LOOP
        a_output := a_output || '' IF v_'' || referrer_keys.kind || '' LIKE '''''''''' 
                 || referrer_keys.key_string || '''''''''' THEN RETURN '''''' 
                 || referrer_keys.referrer_type || ''''''; END IF;''; 
    END LOOP; 
  
    a_output := a_output || '' RETURN NULL; END; '''' LANGUAGE ''''plpgsql'''';''; 
 
    -- This works because we are not substituting any variables
    -- Otherwise it would fail. Look at PERFORM for another way to run functions
    
    EXECUTE a_output; 
END; 
' LANGUAGE 'plpgsql';
</programlisting>
    </para>
   </example>

   <example>
    <title>
     一个带大量字符串处理和 OUT 参数的过程
    </title>

    <para>
     下面的 Oracle PL/SQL 过程用于解析 URL 并返回若干元素（主机、路径和查询）。它是一个过程，因为在 <application>PL/pgSQL</application> 中函数只能返回一个值（见<xref linkend="plpgsql-porting-procedures">）。在 <productname>PostgreSQL</> 中，绕过这个问题的一种办法是把过程拆成三个不同的函数：一个返回主机，一个返回路径，一个返回查询。
    </para>

<programlisting>
CREATE OR REPLACE PROCEDURE cs_parse_url(
    v_url IN VARCHAR,
    v_host OUT VARCHAR,  -- This will be passed back
    v_path OUT VARCHAR,  -- This one too
    v_query OUT VARCHAR) -- And this one
is
    a_pos1 INTEGER;
    a_pos2 INTEGER;
begin
    v_host := NULL;
    v_path := NULL;
    v_query := NULL;
    a_pos1 := instr(v_url, '//'); -- <productname>PostgreSQL</> doesn't have an instr function

    IF a_pos1 = 0 THEN
        RETURN;
    END IF;
    a_pos2 := instr(v_url, '/', a_pos1 + 2);
    IF a_pos2 = 0 THEN
        v_host := substr(v_url, a_pos1 + 2);
        v_path := '/';
        RETURN;
    END IF;

    v_host := substr(v_url, a_pos1 + 2, a_pos2 - a_pos1 - 2);
    a_pos1 := instr(v_url, '?', a_pos2 + 1);

    IF a_pos1 = 0 THEN
        v_path := substr(v_url, a_pos2);
        RETURN;
    END IF;

    v_path := substr(v_url, a_pos2, a_pos1 - a_pos2);
    v_query := substr(v_url, a_pos1 + 1);
END;
/
show errors;
</programlisting>

    <para>
     下面是这个过程翻译成 <productname>PostgreSQL</> 的可能样子：

<programlisting>
CREATE OR REPLACE FUNCTION cs_parse_url_host(VARCHAR) RETURNS VARCHAR AS ' 
DECLARE 
    v_url ALIAS FOR $1; 
    v_host VARCHAR; 
    v_path VARCHAR; 
    a_pos1 INTEGER; 
    a_pos2 INTEGER; 
    a_pos3 INTEGER; 
BEGIN 
    v_host := NULL; 
    a_pos1 := instr(v_url,''//''); 

    IF a_pos1 = 0 THEN 
        RETURN '''';  -- Return a blank
    END IF; 

    a_pos2 := instr(v_url,''/'',a_pos1 + 2); 
    IF a_pos2 = 0 THEN 
        v_host := substr(v_url, a_pos1 + 2); 
        v_path := ''/''; 
        RETURN v_host; 
    END IF; 

    v_host := substr(v_url, a_pos1 + 2, a_pos2 - a_pos1 - 2 ); 
    RETURN v_host; 
END; 
' LANGUAGE 'plpgsql';
</programlisting>
    </para>
   </example>

   <note>
    <para>
     <productname>PostgreSQL</> 没有 <function>instr</function> 函数，因此你可以用其他函数的组合来绕过。我对这样做感到厌倦，于是编写了自己的 <function>instr</function> 函数，其行为与 Oracle 的完全一样（这让生活更轻松）。代码见<xref linkend="plpgsql-porting-appendix">。
    </para>
   </note>
  </sect2>

  <sect2 id="plpgsql-porting-procedures">
   <title>
    过程
   </title>

   <para>
    Oracle 的过程给开发者多了一点灵活性，因为不需要显式返回任何东西，但可以通过 <literal>INOUT</> 或 <literal>OUT</> 参数返回。
   </para>

   <para>
    一个例子：

<programlisting>
CREATE OR REPLACE PROCEDURE cs_create_job(v_job_id IN INTEGER) IS
    a_running_job_count INTEGER;
    PRAGMA AUTONOMOUS_TRANSACTION;<co id="co.plpgsql-porting-pragma">
BEGIN
    LOCK TABLE cs_jobs IN EXCLUSIVE MODE;<co id="co.plpgsql-porting-locktable">

    SELECT count(*) INTO a_running_job_count 
    FROM cs_jobs
    WHERE end_stamp IS NULL;

    IF a_running_job_count > 0 THEN
        COMMIT; -- free lock<co id="co.plpgsql-porting-commit">
        raise_application_error(-20000, 'Unable to create a new job: a job is currently running.');
    END IF;

    DELETE FROM cs_active_job;
    INSERT INTO cs_active_job(job_id) VALUES (v_job_id);

    BEGIN
        INSERT INTO cs_jobs (job_id, start_stamp) VALUES (v_job_id, sysdate);
        EXCEPTION WHEN dup_val_on_index THEN NULL; -- don't worry if it already exists<co id="co.plpgsql-porting-exception">
    END;
    COMMIT;
END;
/
show errors
</programlisting>
   </para>

   <para>
    这样的过程可以很容易地转换为返回 <type>INTEGER</type> 的
    <productname>PostgreSQL</> 函数。这个过程特别有意思，因为它能教给我们一些东西：

    <calloutlist>
     <callout arearefs="co.plpgsql-porting-pragma">
      <para>
       <productname>PostgreSQL</> 中没有 <literal>pragma</literal> 语句。
      </para>
     </callout>

     <callout arearefs="co.plpgsql-porting-locktable">
      <para>
       如果你在 <application>PL/pgSQL</> 中执行 <command>LOCK TABLE</command>，该锁在调用事务结束之前不会被释放。
      </para>
     </callout>

     <callout arearefs="co.plpgsql-porting-commit">
      <para>
       <application>PL/pgSQL</application> 过程中也不能有事务。整个函数（以及其中调用的其他函数）在一个事务中执行，如果出了问题，<productname>PostgreSQL</> 会回滚结果。因此只允许一条
       <command>BEGIN</command> 语句。
      </para>
     </callout>

     <callout arearefs="co.plpgsql-porting-exception">
      <para>
       exception when 需要换成 <literal>IF</literal> 语句。
      </para>
     </callout>
    </calloutlist>
   </para>

   <para>
    那么让我们看看把这个过程移植到 <application>PL/pgSQL</> 的一种方式：

<programlisting>
CREATE OR REPLACE FUNCTION cs_create_job(INTEGER) RETURNS INTEGER AS '
DECLARE
    v_job_id ALIAS FOR $1;
    a_running_job_count INTEGER;
    a_num INTEGER;
    -- PRAGMA AUTONOMOUS_TRANSACTION;
BEGIN
    LOCK TABLE cs_jobs IN EXCLUSIVE MODE;
    SELECT count(*) INTO a_running_job_count 
    FROM cs_jobs 
    WHERE end_stamp IS NULL;

    IF a_running_job_count > 0
    THEN
        -- COMMIT; -- free lock
        RAISE EXCEPTION ''Unable to create a new job: a job is currently running.'';
    END IF;

    DELETE FROM cs_active_job;
    INSERT INTO cs_active_job(job_id) VALUES (v_job_id);

    SELECT count(*) into a_num 
    FROM cs_jobs 
    WHERE job_id=v_job_id;
    IF NOT FOUND THEN  -- If nothing was returned in the last query
        -- This job is not in the table so lets insert it.
        INSERT INTO cs_jobs(job_id, start_stamp) VALUES (v_job_id, sysdate());
        RETURN 1;
    ELSE
        RAISE NOTICE ''Job already running.'';<co id="co.plpgsql-porting-raise">
    END IF;

    RETURN 0;
END;
' LANGUAGE 'plpgsql';
</programlisting>

    <calloutlist>
     <callout arearefs="co.plpgsql-porting-raise">
      <para>
       注意你可以在 <application>PL/pgSQL</> 中引发通知（或错误）。
      </para>
     </callout>
    </calloutlist>
   </para>
  </sect2>

  <sect2 id="plpgsql-porting-packages">
   <title>
    包
   </title>

   <note>
    <para>
     我自己对包用得不多，如果这里有错误，请告诉我。
    </para>
   </note>

   <para>
    包是 Oracle 提供的一种把 PL/SQL 语句和函数封装到一个实体中的方式，就像 Java 类一样，你在其中定义方法和对象。可以用一个
    <quote><literal>.</literal></quote>（点）来访问这些对象/方法。下面是
    ACS 4（<ulink url="http://www.arsdigita.com/doc/">ArsDigita Community
    System</ulink>）中一个 Oracle 包的例子：

<programlisting>
CREATE OR REPLACE PACKAGE BODY acs
AS
  FUNCTION add_user (
    user_id     IN users.user_id%TYPE DEFAULT NULL,
    object_type     IN acs_objects.object_type%TYPE DEFAULT 'user',
    creation_date   IN acs_objects.creation_date%TYPE DEFAULT sysdate,
    creation_user   IN acs_objects.creation_user%TYPE DEFAULT NULL,
    creation_ip     IN acs_objects.creation_ip%TYPE DEFAULT NULL,
  ...
  ) RETURN users.user_id%TYPE
  IS
    v_user_id       users.user_id%TYPE;
    v_rel_id        membership_rels.rel_id%TYPE;
  BEGIN
    v_user_id := acs_user.new (user_id, object_type, creation_date,
                creation_user, creation_ip, email, ...
    RETURN v_user_id;
  END;
END acs;
/
show errors
</programlisting>
   </para>

   <para>
    我们把 Oracle 包的不同对象创建为采用标准命名约定的函数，以此移植到
    <productname>PostgreSQL</>。我们还必须注意其他一些细节，比如
    <productname>PostgreSQL</> 函数缺少默认参数。上面的包会变成类似这样：

<programlisting>
CREATE FUNCTION acs__add_user(INTEGER,INTEGER,VARCHAR,TIMESTAMP,INTEGER,INTEGER,...)
RETURNS INTEGER AS '
DECLARE
    user_id ALIAS FOR $1;
    object_type ALIAS FOR $2;
    creation_date ALIAS FOR $3;
    creation_user ALIAS FOR $4;
    creation_ip ALIAS FOR $5;
    ...
    v_user_id users.user_id%TYPE;
    v_rel_id membership_rels.rel_id%TYPE;
BEGIN
    v_user_id := acs_user__new(user_id,object_type,creation_date,creation_user,creation_ip, ...);
    ...

    RETURN v_user_id;
END;
' LANGUAGE 'plpgsql';
</programlisting>
   </para>
  </sect2>

  <sect2 id="plpgsql-porting-other">
   <title>
    其他需要注意的事项
   </title>

   <sect3>
    <title>EXECUTE</title>

    <para>
     <productname>PostgreSQL</> 版的 <command>EXECUTE</command> 工作良好，但你必须记得按<xref linkend="plpgsql-statements-executing-dyn-queries">中所述使用
     <function>quote_literal(TEXT)</function> 和
     <function>quote_string(TEXT)</function>。<literal>EXECUTE ''SELECT * from $1'';</literal> 这类构造除非使用了这些函数，否则不会工作。
    </para>
   </sect3>

   <sect3 id="plpgsql-porting-optimization">
    <title>优化 <application>PL/pgSQL</application> 函数</title>

    <para>
     <productname>PostgreSQL</> 提供两个函数创建修饰符用于优化执行：<literal>iscachable</literal>（函数在给定相同参数时总是返回相同的结果）和 <literal>isstrict</literal>（函数在任何参数为空时返回空）。详情请查阅 <command>CREATE FUNCTION</command> 参考页。
    </para>

    <para>
     要利用这些优化属性，你的 <command>CREATE FUNCTION</command> 语句中必须使用 <literal>WITH</literal> 修饰符。类似这样：

<programlisting>
CREATE FUNCTION foo(...) RETURNS INTEGER AS '
...
' LANGUAGE 'plpgsql'
WITH (isstrict, iscachable);
</programlisting>
    </para>
   </sect3>
  </sect2>

  <sect2 id="plpgsql-porting-appendix">
   <title>
    附录
   </title>

   <sect3>
    <title>
     我的 <function>instr</function> 函数的代码
    </title>

    <comment>
     这个函数也许应该被集成到核心中。
    </comment>

<programlisting>
--
-- instr functions that mimic Oracle's counterpart
-- Syntax: instr(string1,string2,[n],[m]) where [] denotes optional params.
-- 
-- Searches string1 beginning at the nth character for the mth
-- occurrence of string2. If n is negative, search backwards. If m is
-- not passed, assume 1 (search starts at first character).
--
-- by Roberto Mello (rmello@fslc.usu.edu)
-- modified by Robert Gaszewski (graszew@poland.com)
-- Licensed under the GPL v2 or later.
--

CREATE FUNCTION instr(VARCHAR,VARCHAR) RETURNS INTEGER AS '
DECLARE
    pos integer;
BEGIN
    pos:= instr($1,$2,1);
    RETURN pos;
END;
' LANGUAGE 'plpgsql';


CREATE FUNCTION instr(VARCHAR,VARCHAR,INTEGER) RETURNS INTEGER AS '
DECLARE
    string ALIAS FOR $1;
    string_to_search ALIAS FOR $2;
    beg_index ALIAS FOR $3;
    pos integer NOT NULL DEFAULT 0;
    temp_str VARCHAR;
    beg INTEGER;
    length INTEGER;
    ss_length INTEGER;
BEGIN
    IF beg_index > 0 THEN

       temp_str := substring(string FROM beg_index);
       pos := position(string_to_search IN temp_str);

       IF pos = 0 THEN
	         RETURN 0;
	     ELSE
	         RETURN pos + beg_index - 1;
	     END IF;
    ELSE
       ss_length := char_length(string_to_search);
       length := char_length(string);
       beg := length + beg_index - ss_length + 2;

       WHILE beg > 0 LOOP
           temp_str := substring(string FROM beg FOR ss_length);
	         pos := position(string_to_search IN temp_str);

	         IF pos > 0 THEN
		           RETURN beg;
	         END IF;

	         beg := beg - 1;
       END LOOP;
       RETURN 0;
    END IF;
END;
' LANGUAGE 'plpgsql';

--
-- Written by Robert Gaszewski (graszew@poland.com)
-- Licensed under the GPL v2 or later.
--
CREATE FUNCTION instr(VARCHAR,VARCHAR,INTEGER,INTEGER) RETURNS INTEGER AS '
DECLARE
    string ALIAS FOR $1;
    string_to_search ALIAS FOR $2;
    beg_index ALIAS FOR $3;
    occur_index ALIAS FOR $4;
    pos integer NOT NULL DEFAULT 0;
    occur_number INTEGER NOT NULL DEFAULT 0;
    temp_str VARCHAR;
    beg INTEGER;
    i INTEGER;
    length INTEGER;
    ss_length INTEGER;
BEGIN
    IF beg_index > 0 THEN
        beg := beg_index;
        temp_str := substring(string FROM beg_index);

        FOR i IN 1..occur_index LOOP
            pos := position(string_to_search IN temp_str);

            IF i = 1 THEN
                beg := beg + pos - 1;
            ELSE
                beg := beg + pos;
            END IF;

            temp_str := substring(string FROM beg + 1);
        END LOOP;

        IF pos = 0 THEN
            RETURN 0;
        ELSE
            RETURN beg;
        END IF;
    ELSE
        ss_length := char_length(string_to_search);
        length := char_length(string);
        beg := length + beg_index - ss_length + 2;

        WHILE beg > 0 LOOP
            temp_str := substring(string FROM beg FOR ss_length);
            pos := position(string_to_search IN temp_str);

            IF pos > 0 THEN
                occur_number := occur_number + 1;

                IF occur_number = occur_index THEN
                    RETURN beg;
                END IF;
            END IF;

            beg := beg - 1;
        END LOOP;

        RETURN 0;
    END IF;
END;
' LANGUAGE 'plpgsql';
</programlisting>
   </sect3>
  </sect2>

 </sect1>

</chapter>

<!-- Keep this comment at the end of the file
Local variables:
mode:sgml
sgml-omittag:nil
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
sgml-parent-document:nil
sgml-default-dtd-file:"./reference.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:("/usr/lib/sgml/catalog")
sgml-local-ecat-files:nil
End:
-->
